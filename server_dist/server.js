"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc19) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc19 = __getOwnPropDesc(from, key)) || desc19.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// server/env.ts
function validateEnv() {
  if (validatedEnv) return validatedEnv;
  try {
    validatedEnv = envSchema.parse(process.env);
    console.log("\u2705 Environment variables validated successfully");
    return validatedEnv;
  } catch (error) {
    if (error instanceof import_zod.z.ZodError) {
      console.error("\u274C Environment validation failed:");
      error.errors.forEach((err) => {
        console.error(`  - ${err.path.join(".")}: ${err.message}`);
      });
      console.error("\n\u{1F4CB} Required environment variables:");
      console.error("  DATABASE_URL, FRONTEND_URL, BACKEND_URL");
      console.error("\n\u{1F4CB} Optional (for production):");
      console.error(
        "  STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET"
      );
      console.error(
        "  TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER, TWILIO_VERIFY_SERVICE_SID"
      );
      process.exit(1);
    }
    throw error;
  }
}
var import_zod, envSchema, validatedEnv;
var init_env = __esm({
  "server/env.ts"() {
    "use strict";
    import_zod = require("zod");
    envSchema = import_zod.z.object({
      // Database
      DATABASE_URL: import_zod.z.string().min(1, "DATABASE_URL is required"),
      // Server
      NODE_ENV: import_zod.z.enum(["development", "production", "test"]).default("development"),
      PORT: import_zod.z.string().default("5000"),
      FRONTEND_URL: import_zod.z.string().url("FRONTEND_URL must be a valid URL"),
      BACKEND_URL: import_zod.z.string().url("BACKEND_URL must be a valid URL"),
      // Stripe (Optional in development)
      STRIPE_SECRET_KEY: import_zod.z.string().refine(
        (val) => !val || val.startsWith("sk_"),
        "STRIPE_SECRET_KEY must start with sk_ or be empty"
      ).optional().default(""),
      STRIPE_PUBLISHABLE_KEY: import_zod.z.string().refine(
        (val) => !val || val.startsWith("pk_"),
        "STRIPE_PUBLISHABLE_KEY must start with pk_ or be empty"
      ).optional().default(""),
      STRIPE_WEBHOOK_SECRET: import_zod.z.string().refine(
        (val) => !val || val.startsWith("whsec_"),
        "STRIPE_WEBHOOK_SECRET must start with whsec_ or be empty"
      ).optional().default(""),
      // Twilio (Optional in development)
      TWILIO_ACCOUNT_SID: import_zod.z.string().refine(
        (val) => !val || val.startsWith("AC"),
        "TWILIO_ACCOUNT_SID must start with AC or be empty"
      ).optional().default(""),
      TWILIO_AUTH_TOKEN: import_zod.z.string().optional().default(""),
      TWILIO_PHONE_NUMBER: import_zod.z.string().refine(
        (val) => !val || val.startsWith("+"),
        "TWILIO_PHONE_NUMBER must start with + or be empty"
      ).optional().default(""),
      TWILIO_VERIFY_SERVICE_SID: import_zod.z.string().refine(
        (val) => !val || val.startsWith("VA"),
        "TWILIO_VERIFY_SERVICE_SID must start with VA or be empty"
      ).optional().default(""),
      // Optional services
      RESEND_API_KEY: import_zod.z.string().optional(),
      OPENAI_API_KEY: import_zod.z.string().optional()
    });
    validatedEnv = null;
  }
});

// server/logger.ts
var Logger, logger;
var init_logger = __esm({
  "server/logger.ts"() {
    "use strict";
    Logger = class {
      minLevel = null;
      getMinLevel() {
        if (this.minLevel === null) {
          this.minLevel = process.env.NODE_ENV === "production" ? 1 /* INFO */ : 0 /* DEBUG */;
        }
        return this.minLevel;
      }
      formatMessage(level, message, context) {
        const timestamp2 = (/* @__PURE__ */ new Date()).toISOString();
        const contextStr = context ? ` ${JSON.stringify(context)}` : "";
        return `[${timestamp2}] ${level}: ${message}${contextStr}`;
      }
      shouldLog(level) {
        return level >= this.getMinLevel();
      }
      debug(message, context) {
        if (this.shouldLog(0 /* DEBUG */)) {
          console.log(this.formatMessage("DEBUG", message, context));
        }
      }
      info(message, context) {
        if (this.shouldLog(1 /* INFO */)) {
          console.log(this.formatMessage("INFO", message, context));
        }
      }
      warn(message, context) {
        if (this.shouldLog(2 /* WARN */)) {
          console.warn(this.formatMessage("WARN", message, context));
        }
      }
      error(message, error, context) {
        if (this.shouldLog(3 /* ERROR */)) {
          const errorContext = {
            ...context,
            error: error instanceof Error ? {
              message: error.message,
              stack: error.stack,
              name: error.name
            } : error
          };
          console.error(this.formatMessage("ERROR", message, errorContext));
        }
      }
      // Specific loggers for common operations
      payment(message, context) {
        this.info(`[PAYMENT] ${message}`, context);
      }
      order(message, context) {
        this.info(`[ORDER] ${message}`, context);
      }
      delivery(message, context) {
        this.info(`[DELIVERY] ${message}`, context);
      }
      webhook(message, context) {
        this.info(`[WEBHOOK] ${message}`, context);
      }
      security(message, context) {
        this.warn(`[SECURITY] ${message}`, context);
      }
    };
    logger = new Logger();
  }
});

// shared/schema-mysql.ts
var schema_mysql_exports = {};
__export(schema_mysql_exports, {
  achievements: () => achievements,
  addresses: () => addresses,
  aiRecommendations: () => aiRecommendations,
  appSettings: () => appSettings,
  auditLogs: () => auditLogs,
  businessCategories: () => businessCategories,
  businesses: () => businesses,
  callLogs: () => callLogs,
  couponUsage: () => couponUsage,
  coupons: () => coupons,
  deliveryDrivers: () => deliveryDrivers,
  deliveryHeatmap: () => deliveryHeatmap,
  deliveryProofs: () => deliveryProofs,
  deliveryRequests: () => deliveryRequests,
  deliveryZones: () => deliveryZones,
  drivers: () => drivers,
  favorites: () => favorites,
  giftCardDesigns: () => giftCardDesigns,
  giftCardTransactions: () => giftCardTransactions,
  giftCards: () => giftCards,
  groupOrderInvitations: () => groupOrderInvitations,
  groupOrderParticipants: () => groupOrderParticipants,
  groupOrders: () => groupOrders,
  insertOrderSchema: () => insertOrderSchema,
  insertUserSchema: () => insertUserSchema,
  loyaltyChallengeProgress: () => loyaltyChallengeProgress,
  loyaltyChallenges: () => loyaltyChallenges,
  loyaltyPoints: () => loyaltyPoints,
  loyaltyRedemptions: () => loyaltyRedemptions,
  loyaltyRewards: () => loyaltyRewards,
  loyaltyTransactions: () => loyaltyTransactions,
  orderIssues: () => orderIssues,
  orders: () => orders,
  pagoMovilVerifications: () => pagoMovilVerifications,
  paymentAccounts: () => paymentAccounts,
  paymentMethods: () => paymentMethods,
  paymentProofs: () => paymentProofs,
  payments: () => payments,
  payouts: () => payouts,
  products: () => products,
  proximityAlerts: () => proximityAlerts,
  refreshTokens: () => refreshTokens,
  refunds: () => refunds,
  reviewResponses: () => reviewResponses,
  reviewTags: () => reviewTags,
  reviews: () => reviews,
  scheduledOrders: () => scheduledOrders,
  subscriptionBenefits: () => subscriptionBenefits,
  subscriptionPlans: () => subscriptionPlans,
  subscriptions: () => subscriptions,
  substitutions: () => substitutions,
  supportChats: () => supportChats,
  supportMessages: () => supportMessages,
  supportTickets: () => supportTickets,
  systemSettings: () => systemSettings,
  ticketMessages: () => ticketMessages,
  transactions: () => transactions,
  userAchievements: () => userAchievements,
  userFavorites: () => userFavorites,
  userPreferences: () => userPreferences,
  users: () => users,
  walletTransactions: () => walletTransactions,
  wallets: () => wallets,
  withdrawalRequests: () => withdrawalRequests,
  withdrawals: () => withdrawals
});
var import_drizzle_orm, import_crypto, import_mysql_core, import_drizzle_zod, import_zod2, users, addresses, orders, businesses, wallets, transactions, payments, walletTransactions, insertUserSchema, insertOrderSchema, systemSettings, products, pagoMovilVerifications, paymentAccounts, payouts, withdrawals, withdrawalRequests, deliveryDrivers, auditLogs, drivers, refreshTokens, scheduledOrders, supportChats, supportMessages, reviews, reviewResponses, reviewTags, callLogs, deliveryZones, coupons, favorites, deliveryHeatmap, proximityAlerts, deliveryProofs, paymentMethods, paymentProofs, couponUsage, loyaltyPoints, loyaltyTransactions, loyaltyRewards, loyaltyRedemptions, loyaltyChallenges, loyaltyChallengeProgress, achievements, userAchievements, userFavorites, userPreferences, aiRecommendations, supportTickets, groupOrders, groupOrderParticipants, groupOrderInvitations, subscriptions, subscriptionBenefits, subscriptionPlans, giftCards, giftCardTransactions, giftCardDesigns, deliveryRequests, ticketMessages, orderIssues, refunds, businessCategories, substitutions, appSettings;
var init_schema_mysql = __esm({
  "shared/schema-mysql.ts"() {
    "use strict";
    import_drizzle_orm = require("drizzle-orm");
    import_crypto = require("crypto");
    import_mysql_core = require("drizzle-orm/mysql-core");
    import_drizzle_zod = require("drizzle-zod");
    import_zod2 = require("zod");
    users = (0, import_mysql_core.mysqlTable)("users", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      email: (0, import_mysql_core.text)("email"),
      password: (0, import_mysql_core.text)("password"),
      name: (0, import_mysql_core.text)("name").notNull(),
      phone: (0, import_mysql_core.text)("phone").notNull(),
      role: (0, import_mysql_core.text)("role").notNull().default("customer"),
      emailVerified: (0, import_mysql_core.boolean)("email_verified").notNull().default(false),
      phoneVerified: (0, import_mysql_core.boolean)("phone_verified").notNull().default(false),
      biometricEnabled: (0, import_mysql_core.boolean)("biometric_enabled").notNull().default(false),
      verificationCode: (0, import_mysql_core.text)("verification_code"),
      verificationExpires: (0, import_mysql_core.timestamp)("verification_expires"),
      // Datos personales España
      dni: (0, import_mysql_core.varchar)("dni", { length: 20 }),
      // DNI/NIE
      address: (0, import_mysql_core.text)("address"),
      // Dirección completa
      // Verificación de identidad
      idDocumentUrl: (0, import_mysql_core.text)("id_document_url"),
      // Foto DNI/NIE anverso
      idDocumentBackUrl: (0, import_mysql_core.text)("id_document_back_url"),
      // Foto DNI/NIE reverso
      autonomoDocumentUrl: (0, import_mysql_core.text)("autonomo_document_url"),
      // Cert. autónomo/empresa
      verificationStatus: (0, import_mysql_core.varchar)("verification_status", { length: 20 }).default(
        "pending"
      ),
      // pending, verified, rejected
      verificationNotes: (0, import_mysql_core.text)("verification_notes"),
      // Legacy Venezuela (mantener para no romper)
      // Legacy — mantener columnas para no romper BD existente
      pagoMovilPhone: (0, import_mysql_core.text)("pago_movil_phone"),
      pagoMovilBank: (0, import_mysql_core.text)("pago_movil_bank"),
      pagoMovilCedula: (0, import_mysql_core.text)("pago_movil_cedula"),
      bankAccount: (0, import_mysql_core.text)("bank_account"),
      // Preferencias de notificaciones (JSON: {promotions, news}; los avisos de
      // pedidos siempre se envían por ser operativos)
      notificationPreferences: (0, import_mysql_core.text)("notification_preferences"),
      // Referidos: código propio del usuario y quién lo invitó
      referralCode: (0, import_mysql_core.varchar)("referral_code", { length: 20 }),
      referredBy: (0, import_mysql_core.varchar)("referred_by", { length: 255 }),
      referralRewardedAt: (0, import_mysql_core.timestamp)("referral_rewarded_at"),
      isActive: (0, import_mysql_core.boolean)("is_active").notNull().default(true),
      isOnline: (0, import_mysql_core.boolean)("is_online").notNull().default(false),
      lastActiveAt: (0, import_mysql_core.timestamp)("last_active_at"),
      profileImage: (0, import_mysql_core.text)("profile_image"),
      pushToken: (0, import_mysql_core.text)("push_token"),
      // Stripe Connect (cuenta vinculada del usuario)
      stripeAccountId: (0, import_mysql_core.varchar)("stripe_account_id", { length: 255 }),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at")
    });
    addresses = (0, import_mysql_core.mysqlTable)("addresses", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      label: (0, import_mysql_core.text)("label").notNull(),
      street: (0, import_mysql_core.text)("street").notNull(),
      city: (0, import_mysql_core.text)("city").notNull(),
      state: (0, import_mysql_core.text)("state").notNull(),
      zipCode: (0, import_mysql_core.text)("zip_code"),
      isDefault: (0, import_mysql_core.boolean)("is_default").notNull().default(false),
      latitude: (0, import_mysql_core.text)("latitude"),
      longitude: (0, import_mysql_core.text)("longitude"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    orders = (0, import_mysql_core.mysqlTable)("orders", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      // Número secuencial público con formato #CY000001 (trazabilidad de facturas)
      orderNumber: (0, import_mysql_core.int)("order_number").unique(),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.text)("business_id").notNull(),
      businessName: (0, import_mysql_core.text)("business_name").notNull(),
      businessImage: (0, import_mysql_core.text)("business_image"),
      items: (0, import_mysql_core.text)("items").notNull(),
      status: (0, import_mysql_core.text)("status").notNull().default("pending"),
      // pending, accepted, preparing, on_the_way, delivered, cancelled
      subtotal: (0, import_mysql_core.int)("subtotal").notNull(),
      productosBase: (0, import_mysql_core.int)("productos_base").default(0),
      // Precio base sin markup ComeYa
      nemyCommission: (0, import_mysql_core.int)("nemy_commission").default(0),
      // 15% markup ComeYa
      deliveryFee: (0, import_mysql_core.int)("delivery_fee").notNull(),
      total: (0, import_mysql_core.int)("total").notNull(),
      paymentMethod: (0, import_mysql_core.text)("payment_method").notNull(),
      paymentProvider: (0, import_mysql_core.varchar)("payment_provider", { length: 50 }).default(
        "stripe_bizum"
      ),
      orderType: (0, import_mysql_core.text)("order_type"),
      // delivery | pickup
      estimatedPickupTime: (0, import_mysql_core.int)("estimated_pickup_time"),
      // minutos estimados para pickup
      pickupReadyAt: (0, import_mysql_core.timestamp)("pickup_ready_at"),
      // cuando el pedido estuvo listo para recoger
      customerArrivedAt: (0, import_mysql_core.timestamp)("customer_arrived_at"),
      // cuando el cliente avisó que llegó
      pickupCode: (0, import_mysql_core.varchar)("pickup_code", { length: 6 }),
      // código de 6 dígitos para recoger
      pickupQrCode: (0, import_mysql_core.text)("pickup_qr_code"),
      // QR code único para escanear
      // Referencia de pago (Stripe/PayPal)
      paymentReference: (0, import_mysql_core.text)("pago_movil_reference"),
      paymentProofUrl: (0, import_mysql_core.text)("pago_movil_proof_url"),
      deliveryAddress: (0, import_mysql_core.text)("delivery_address").notNull(),
      deliveryPersonId: (0, import_mysql_core.text)("delivery_person_id"),
      notes: (0, import_mysql_core.text)("notes"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      estimatedDelivery: (0, import_mysql_core.timestamp)("estimated_delivery"),
      // Aviso anticipado a repartidores (5-10 min / 10-20 min)
      estimatedPrepMinutes: (0, import_mysql_core.int)("estimated_prep_minutes"),
      estimatedPrepRange: (0, import_mysql_core.varchar)("estimated_prep_range", { length: 20 }),
      // Ventana de entrega agrupada para pedidos de mercado (escaparate)
      deliveryWindow: (0, import_mysql_core.varchar)("delivery_window", { length: 20 }),
      // Campos para cancelación y comisiones
      cancelledAt: (0, import_mysql_core.timestamp)("cancelled_at"),
      cancelledBy: (0, import_mysql_core.varchar)("cancelled_by", { length: 255 }),
      cancellationReason: (0, import_mysql_core.text)("cancellation_reason"),
      refundAmount: (0, import_mysql_core.int)("refund_amount"),
      penaltyAmount: (0, import_mysql_core.int)("penalty_amount"),
      // penalización por cancelación
      refundStatus: (0, import_mysql_core.text)("refund_status"),
      // pending, processed, failed
      businessResponseAt: (0, import_mysql_core.timestamp)("business_response_at"),
      // cuando el negocio respondió
      platformFee: (0, import_mysql_core.int)("platform_fee"),
      // comisión ComeYa
      businessEarnings: (0, import_mysql_core.int)("business_earnings"),
      // ganancia negocio
      deliveryEarnings: (0, import_mysql_core.int)("delivery_earnings"),
      // ganancia repartidor
      distanceKm: (0, import_mysql_core.int)("distance_km"),
      // distancia en metros x100
      deliveredAt: (0, import_mysql_core.timestamp)("delivered_at"),
      // cuando se entregó
      deliveryLatitude: (0, import_mysql_core.text)("delivery_latitude"),
      deliveryLongitude: (0, import_mysql_core.text)("delivery_longitude"),
      // Preferencias de sustitución (Stock Out)
      substitutionPreference: (0, import_mysql_core.text)("substitution_preference").default("refund"),
      // refund, call, substitute
      itemSubstitutionPreferences: (0, import_mysql_core.text)("item_substitution_preferences"),
      // JSON: {productId: "refund"|"call"|"substitute"}
      substituteProductIds: (0, import_mysql_core.text)("substitute_product_ids"),
      // JSON: {productId: substituteProductId} elegidos por el cliente
      // Fecha programada de entrega (pedidos programados materializados)
      scheduledFor: (0, import_mysql_core.timestamp)("scheduled_for"),
      // Ocultación por el admin (soft delete): fuera de las listas, conservado
      deletedAt: (0, import_mysql_core.timestamp)("deleted_at"),
      // Pago en efectivo
      cashPaymentAmount: (0, import_mysql_core.int)("cash_payment_amount"),
      // Con cuánto paga el cliente (centavos)
      cashChangeAmount: (0, import_mysql_core.int)("cash_change_amount"),
      // Cambio a entregar (centavos)
      // Cronómetro de arrepentimiento
      regretPeriodEndsAt: (0, import_mysql_core.timestamp)("regret_period_ends_at"),
      // Cuando termina el periodo de 60s
      regretPeriodConfirmed: (0, import_mysql_core.boolean)("regret_period_confirmed").default(false),
      // Cliente confirmó después de 60s (no va a cancelar)
      regretPeriodConfirmedAt: (0, import_mysql_core.timestamp)("regret_period_confirmed_at"),
      // Cuándo confirmó el período
      confirmedToBusinessAt: (0, import_mysql_core.timestamp)("confirmed_to_business_at"),
      // Cuando se notificó al negocio
      // Llamada automática al negocio
      callAttempted: (0, import_mysql_core.boolean)("call_attempted").default(false),
      // Si ya se intentó llamar al negocio
      callAttemptedAt: (0, import_mysql_core.timestamp)("call_attempted_at"),
      // Cuando se intentó la llamada
      // Campos adicionales de pago
      paidAt: (0, import_mysql_core.timestamp)("paid_at"),
      refundedAt: (0, import_mysql_core.timestamp)("refunded_at"),
      driverPaidAt: (0, import_mysql_core.timestamp)("driver_paid_at"),
      driverPaymentStatus: (0, import_mysql_core.text)("driver_payment_status").default("pending"),
      // Confirmación de recepción por cliente (para liberar fondos)
      confirmedByCustomer: (0, import_mysql_core.boolean)("confirmed_by_customer").default(false),
      // Si el cliente confirmó que RECIBIÓ el pedido
      confirmedByCustomerAt: (0, import_mysql_core.timestamp)("confirmed_by_customer_at"),
      // Cuándo confirmó la recepción
      fundsReleased: (0, import_mysql_core.boolean)("funds_released").default(false),
      // Si ya se liberaron los fondos
      fundsReleasedAt: (0, import_mysql_core.timestamp)("funds_released_at"),
      // Cuándo se liberaron
      stripePaymentIntentId: (0, import_mysql_core.text)("stripe_payment_intent_id"),
      // PaymentIntent de Stripe
      businessTransferId: (0, import_mysql_core.text)("business_transfer_id"),
      // ID de transfer a negocio
      driverTransferId: (0, import_mysql_core.text)("driver_transfer_id"),
      // ID de transfer a repartidor
      // Asignación de repartidor
      assignedAt: (0, import_mysql_core.timestamp)("assigned_at"),
      // Cuando se asignó el repartidor
      driverPickedUpAt: (0, import_mysql_core.timestamp)("driver_picked_up_at"),
      // Cuando repartidor recogió el pedido
      driverArrivedAt: (0, import_mysql_core.timestamp)("driver_arrived_at"),
      // Cuando repartidor llegó con el cliente
      // Liquidación de efectivo (para pedidos cash)
      cashCollected: (0, import_mysql_core.boolean)("cash_collected").default(false),
      // Si el repartidor ya cobró el efectivo
      cashSettled: (0, import_mysql_core.boolean)("cash_settled").default(false),
      // Si ya liquidó con negocio/plataforma
      cashSettledAt: (0, import_mysql_core.timestamp)("cash_settled_at"),
      // Cuando liquidó
      // Prueba de entrega
      deliveryProofPhoto: (0, import_mysql_core.text)("delivery_proof_photo"),
      // URL de foto de entrega
      deliveryProofPhotoTimestamp: (0, import_mysql_core.timestamp)("delivery_proof_photo_timestamp"),
      deliveryRoute: (0, import_mysql_core.text)("delivery_route"),
      // JSON con ruta completa del repartidor
      deliveryDistance: (0, import_mysql_core.int)("delivery_distance"),
      // Distancia real recorrida en metros
      // Validación GPS
      deliveryGpsAccuracy: (0, import_mysql_core.int)("delivery_gps_accuracy"),
      // Precisión del GPS en metros
      deliveryGpsValidated: (0, import_mysql_core.boolean)("delivery_gps_validated").default(false),
      // Si se validó la ubicación
      // Compartir tracking
      trackingToken: (0, import_mysql_core.varchar)("tracking_token", { length: 255 }),
      // Token para compartir tracking
      trackingTokenExpires: (0, import_mysql_core.timestamp)("tracking_token_expires"),
      // Chat messages — JSON array de mensajes entre cliente y repartidor
      chatMessages: (0, import_mysql_core.text)("chat_messages"),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    businesses = (0, import_mysql_core.mysqlTable)("businesses", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      ownerId: (0, import_mysql_core.varchar)("owner_id", { length: 255 }),
      name: (0, import_mysql_core.text)("name").notNull(),
      description: (0, import_mysql_core.text)("description"),
      type: (0, import_mysql_core.text)("type").notNull().default("restaurant"),
      // restaurant, market
      image: (0, import_mysql_core.text)("image"),
      coverImage: (0, import_mysql_core.text)("cover_image"),
      address: (0, import_mysql_core.text)("address"),
      phone: (0, import_mysql_core.text)("phone"),
      phoneVerified: (0, import_mysql_core.boolean)("phone_verified").notNull().default(false),
      email: (0, import_mysql_core.text)("email"),
      rating: (0, import_mysql_core.int)("rating").default(0),
      // stored as 0-50 (for 0.0-5.0)
      totalRatings: (0, import_mysql_core.int)("total_ratings").default(0),
      deliveryTime: (0, import_mysql_core.text)("delivery_time").default("30-45 min"),
      deliveryFee: (0, import_mysql_core.int)("delivery_fee").default(300),
      // en centavos de euro (3€)
      minOrder: (0, import_mysql_core.int)("min_order").default(1e3),
      // en centavos de euro (10€ mínimo)
      isActive: (0, import_mysql_core.boolean)("is_active").notNull().default(true),
      isOpen: (0, import_mysql_core.boolean)("is_open").notNull().default(true),
      openingHours: (0, import_mysql_core.text)("opening_hours"),
      // JSON string
      categories: (0, import_mysql_core.text)("categories"),
      // comma-separated
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      // Campos para ubicación y zonas de entrega
      latitude: (0, import_mysql_core.text)("latitude"),
      longitude: (0, import_mysql_core.text)("longitude"),
      maxDeliveryRadiusKm: (0, import_mysql_core.int)("max_delivery_radius_km").default(10),
      // Radio máximo de entrega
      baseFeePerKm: (0, import_mysql_core.int)("base_fee_per_km").default(500),
      // Costo por km en centavos
      verificationStatus: (0, import_mysql_core.text)("verification_status").default("pending"),
      // pending, verified, rejected
      verificationDocuments: (0, import_mysql_core.text)("verification_documents"),
      // JSON con URLs de documentos
      // Control operativo de negocios
      maxSimultaneousOrders: (0, import_mysql_core.int)("max_simultaneous_orders").default(10),
      // Límite de pedidos activos
      isPaused: (0, import_mysql_core.boolean)("is_paused").notNull().default(false),
      // Pausado por sistema o manual
      pauseReason: (0, import_mysql_core.text)("pause_reason"),
      // Razón de pausa: manual, too_many_orders, delayed_orders
      pausedAt: (0, import_mysql_core.timestamp)("paused_at"),
      pausedUntil: (0, import_mysql_core.timestamp)("paused_until"),
      // Pausa temporal
      autoResumeEnabled: (0, import_mysql_core.boolean)("auto_resume_enabled").notNull().default(true),
      isFeatured: (0, import_mysql_core.boolean)("is_featured").notNull().default(false),
      // Destacado en pantalla de login
      featuredOrder: (0, import_mysql_core.int)("featured_order").default(0),
      // Orden de aparición en carrusel
      // Modo Slammed (Saturado)
      isSlammed: (0, import_mysql_core.boolean)("is_slammed").notNull().default(false),
      // Negocio saturado
      slammedExtraMinutes: (0, import_mysql_core.int)("slammed_extra_minutes").default(20),
      // Minutos extra cuando está saturado
      slammedAt: (0, import_mysql_core.timestamp)("slammed_at"),
      // Cuando se activó el modo saturado
      pagoMovilPhone: (0, import_mysql_core.text)("pago_movil_phone"),
      pagoMovilBank: (0, import_mysql_core.text)("pago_movil_bank"),
      pagoMovilCedula: (0, import_mysql_core.text)("pago_movil_cedula"),
      verificationCode: (0, import_mysql_core.text)("verification_code"),
      verificationExpires: (0, import_mysql_core.timestamp)("verification_expires"),
      // Stripe Connect
      stripeAccountId: (0, import_mysql_core.varchar)("stripe_account_id", { length: 255 }),
      stripeAccountStatus: (0, import_mysql_core.varchar)("stripe_account_status", { length: 50 }).default(
        "not_connected"
      ),
      // not_connected, pending, active
      // Niveles de partner
      partnerLevel: (0, import_mysql_core.varchar)("partner_level", { length: 20 }).default("bronze"),
      // bronze, silver, gold, platinum
      partnerLevelUpdatedAt: (0, import_mysql_core.timestamp)("partner_level_updated_at"),
      customCommission: (0, import_mysql_core.int)("custom_commission"),
      // null = usa global, numero = % especifico para este negocio
      totalOrdersCompleted: (0, import_mysql_core.int)("total_orders_completed").default(0),
      totalRevenueGenerated: (0, import_mysql_core.int)("total_revenue_generated").default(0),
      // en centavos
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    wallets = (0, import_mysql_core.mysqlTable)("wallets", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      balance: (0, import_mysql_core.int)("balance").notNull().default(0),
      // en centavos - saldo disponible
      pendingBalance: (0, import_mysql_core.int)("pending_balance").notNull().default(0),
      // dinero en tránsito
      cashOwed: (0, import_mysql_core.int)("cash_owed").notNull().default(0),
      // efectivo que debe liquidar (para repartidores)
      totalEarned: (0, import_mysql_core.int)("total_earned").notNull().default(0),
      totalWithdrawn: (0, import_mysql_core.int)("total_withdrawn").notNull().default(0),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    transactions = (0, import_mysql_core.mysqlTable)("transactions", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      walletId: (0, import_mysql_core.varchar)("wallet_id", { length: 255 }),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }),
      type: (0, import_mysql_core.text)("type").notNull(),
      // income, commission, withdrawal, refund, penalty, tip, payment, transfer, delivery_payment
      amount: (0, import_mysql_core.int)("amount").notNull(),
      // en centavos (positivo = ingreso, negativo = egreso)
      balanceBefore: (0, import_mysql_core.int)("balance_before"),
      balanceAfter: (0, import_mysql_core.int)("balance_after"),
      description: (0, import_mysql_core.text)("description"),
      status: (0, import_mysql_core.text)("status").notNull().default("completed"),
      // pending, completed, failed, cancelled
      metadata: (0, import_mysql_core.text)("metadata"),
      // JSON con info adicional
      pagoMovilReference: (0, import_mysql_core.text)("pago_movil_reference"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    payments = (0, import_mysql_core.mysqlTable)("payments", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      customerId: (0, import_mysql_core.varchar)("customer_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      driverId: (0, import_mysql_core.varchar)("driver_id", { length: 255 }),
      amount: (0, import_mysql_core.int)("amount").notNull(),
      // en centavos
      currency: (0, import_mysql_core.text)("currency").notNull().default("EUR"),
      status: (0, import_mysql_core.text)("status").notNull().default("pending"),
      paymentMethod: (0, import_mysql_core.text)("payment_method").notNull().default("stripe_bizum"),
      orderType: (0, import_mysql_core.text)("order_type").notNull().default("delivery"),
      // delivery | pickup
      pagoMovilReference: (0, import_mysql_core.text)("pago_movil_reference"),
      processedAt: (0, import_mysql_core.timestamp)("processed_at"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    walletTransactions = transactions;
    insertUserSchema = (0, import_drizzle_zod.createInsertSchema)(users).pick({
      email: true,
      password: true,
      name: true,
      phone: true,
      role: true
    }).extend({
      phone: import_zod2.z.string().min(10, "Phone number is required"),
      name: import_zod2.z.string().min(1, "Name is required"),
      email: import_zod2.z.string().email().optional().nullable(),
      password: import_zod2.z.string().optional().nullable()
    });
    insertOrderSchema = (0, import_drizzle_zod.createInsertSchema)(orders).pick({
      userId: true,
      businessId: true,
      businessName: true,
      businessImage: true,
      items: true,
      status: true,
      subtotal: true,
      deliveryFee: true,
      total: true,
      paymentMethod: true,
      deliveryAddress: true,
      notes: true,
      substitutionPreference: true,
      itemSubstitutionPreferences: true,
      cashPaymentAmount: true,
      cashChangeAmount: true
    });
    systemSettings = (0, import_mysql_core.mysqlTable)("system_settings", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      key: (0, import_mysql_core.varchar)("key", { length: 255 }).notNull().unique(),
      value: (0, import_mysql_core.text)("value").notNull(),
      type: (0, import_mysql_core.text)("type").notNull().default("string"),
      // string, number, boolean, json
      category: (0, import_mysql_core.text)("category").notNull(),
      // payments, commissions, operations, security
      description: (0, import_mysql_core.text)("description"),
      isPublic: (0, import_mysql_core.boolean)("is_public").notNull().default(false),
      // Si es visible para clientes
      updatedBy: (0, import_mysql_core.varchar)("updated_by", { length: 255 }),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    products = (0, import_mysql_core.mysqlTable)("products", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      name: (0, import_mysql_core.text)("name").notNull(),
      description: (0, import_mysql_core.text)("description"),
      price: (0, import_mysql_core.int)("price").notNull(),
      // en centavos
      image: (0, import_mysql_core.text)("image"),
      category: (0, import_mysql_core.text)("category"),
      isAvailable: (0, import_mysql_core.boolean)("is_available").notNull().default(true),
      is86: (0, import_mysql_core.boolean)("is_86").notNull().default(false),
      // Menú 86 (agotado temporalmente)
      soldByWeight: (0, import_mysql_core.boolean)("sold_by_weight").notNull().default(false),
      weightUnit: (0, import_mysql_core.text)("weight_unit").default("kg"),
      // kg, lb, g
      stock: (0, import_mysql_core.int)("stock"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    pagoMovilVerifications = (0, import_mysql_core.mysqlTable)("pago_movil_verifications", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      reference: (0, import_mysql_core.varchar)("reference", { length: 50 }).notNull().unique(),
      amount: (0, import_mysql_core.int)("amount").notNull(),
      proofUrl: (0, import_mysql_core.text)("proof_url"),
      clientPhone: (0, import_mysql_core.varchar)("client_phone", { length: 20 }),
      clientBank: (0, import_mysql_core.varchar)("client_bank", { length: 50 }),
      destPhone: (0, import_mysql_core.varchar)("dest_phone", { length: 20 }),
      destBank: (0, import_mysql_core.varchar)("dest_bank", { length: 50 }),
      destCedula: (0, import_mysql_core.varchar)("dest_cedula", { length: 20 }),
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).notNull().default("pending"),
      verifiedBy: (0, import_mysql_core.varchar)("verified_by", { length: 255 }),
      verifiedAt: (0, import_mysql_core.timestamp)("verified_at"),
      rejectedReason: (0, import_mysql_core.text)("rejected_reason"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    paymentAccounts = (0, import_mysql_core.mysqlTable)("payment_accounts", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      method: (0, import_mysql_core.varchar)("method", { length: 50 }).notNull(),
      // pago_movil, binance, zinli, zelle, cash
      isDefault: (0, import_mysql_core.boolean)("is_default").notNull().default(false),
      // Pago Móvil
      pagoMovilPhone: (0, import_mysql_core.varchar)("pago_movil_phone", { length: 20 }),
      pagoMovilBank: (0, import_mysql_core.varchar)("pago_movil_bank", { length: 50 }),
      pagoMovilCedula: (0, import_mysql_core.varchar)("pago_movil_cedula", { length: 20 }),
      // Binance
      binanceId: (0, import_mysql_core.varchar)("binance_id", { length: 100 }),
      binanceEmail: (0, import_mysql_core.varchar)("binance_email", { length: 255 }),
      // Zinli / Zelle
      zinliEmail: (0, import_mysql_core.varchar)("zinli_email", { length: 255 }),
      zelleEmail: (0, import_mysql_core.varchar)("zelle_email", { length: 255 }),
      zellePhone: (0, import_mysql_core.varchar)("zelle_phone", { length: 20 }),
      // Metadata
      label: (0, import_mysql_core.varchar)("label", { length: 100 }),
      // ej: "Mi Banesco principal"
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    payouts = (0, import_mysql_core.mysqlTable)("payouts", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      recipientId: (0, import_mysql_core.varchar)("recipient_id", { length: 255 }).notNull(),
      // negocio o driver
      recipientType: (0, import_mysql_core.varchar)("recipient_type", { length: 20 }).notNull(),
      // business, driver
      amount: (0, import_mysql_core.int)("amount").notNull(),
      // en centavos
      method: (0, import_mysql_core.varchar)("method", { length: 50 }),
      // pago_movil, binance, zinli, zelle, cash
      // Snapshot de la cuenta destino al momento del pago
      accountSnapshot: (0, import_mysql_core.text)("account_snapshot"),
      // JSON con datos de la cuenta usada
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).notNull().default("pending"),
      // pending, paid, stripe_auto
      paidBy: (0, import_mysql_core.varchar)("paid_by", { length: 255 }),
      // admin que marcó como pagado
      paidAt: (0, import_mysql_core.timestamp)("paid_at"),
      stripeTransferId: (0, import_mysql_core.varchar)("stripe_transfer_id", { length: 255 }),
      // ID del transfer de Stripe
      proofUrl: (0, import_mysql_core.text)("proof_url"),
      // comprobante de transferencia subido por el admin
      notes: (0, import_mysql_core.text)("notes"),
      // Descuento aplicado por una incidencia/reembolso del que este destinatario
      // es responsable (modelo Uber "order error adjustment"). amount ya lo refleja.
      adjustmentAmount: (0, import_mysql_core.int)("adjustment_amount").notNull().default(0),
      adjustmentReason: (0, import_mysql_core.text)("adjustment_reason"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    withdrawals = payouts;
    withdrawalRequests = payouts;
    deliveryDrivers = (0, import_mysql_core.mysqlTable)("delivery_drivers", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull().unique(),
      vehicleType: (0, import_mysql_core.text)("vehicle_type").notNull(),
      // bike, motorcycle, car
      vehiclePlate: (0, import_mysql_core.text)("vehicle_plate"),
      vehiclePhoto: (0, import_mysql_core.text)("vehicle_photo"),
      vehicleBrand: (0, import_mysql_core.varchar)("vehicle_brand", { length: 100 }),
      vehicleModel: (0, import_mysql_core.varchar)("vehicle_model", { length: 100 }),
      vehicleColor: (0, import_mysql_core.varchar)("vehicle_color", { length: 50 }),
      // Documentos del vehículo (verificación del repartidor)
      vehiclePlatePhoto: (0, import_mysql_core.text)("vehicle_plate_photo"),
      vehicleItvPhoto: (0, import_mysql_core.text)("vehicle_itv_photo"),
      vehicleInsurancePhoto: (0, import_mysql_core.text)("vehicle_insurance_photo"),
      vehicleLicensePhoto: (0, import_mysql_core.text)("vehicle_license_photo"),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at"),
      isAvailable: (0, import_mysql_core.boolean)("is_available").notNull().default(false),
      currentLatitude: (0, import_mysql_core.text)("current_latitude"),
      currentLongitude: (0, import_mysql_core.text)("current_longitude"),
      lastLocationUpdate: (0, import_mysql_core.timestamp)("last_location_update"),
      totalDeliveries: (0, import_mysql_core.int)("total_deliveries").notNull().default(0),
      rating: (0, import_mysql_core.int)("rating").default(0),
      // stored as 0-50 (for 0.0-5.0)
      totalRatings: (0, import_mysql_core.int)("total_ratings").default(0),
      strikes: (0, import_mysql_core.int)("strikes").notNull().default(0),
      // Sistema de strikes
      isBlocked: (0, import_mysql_core.boolean)("is_blocked").notNull().default(false),
      blockedReason: (0, import_mysql_core.text)("blocked_reason"),
      blockedUntil: (0, import_mysql_core.timestamp)("blocked_until"),
      // GPS tracking y ruta
      routeHistory: (0, import_mysql_core.text)("route_history"),
      // JSON con historial de rutas
      totalDistanceTraveled: (0, import_mysql_core.int)("total_distance_traveled").default(0),
      // metros totales
      averageSpeed: (0, import_mysql_core.int)("average_speed").default(0),
      // km/h promedio
      gpsAccuracyAverage: (0, import_mysql_core.int)("gps_accuracy_average").default(0),
      // precisión promedio en metros
      // Stripe Connect
      stripeAccountId: (0, import_mysql_core.varchar)("stripe_account_id", { length: 255 }),
      stripeAccountStatus: (0, import_mysql_core.varchar)("stripe_account_status", { length: 50 }).default(
        "not_connected"
      ),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    auditLogs = (0, import_mysql_core.mysqlTable)("audit_logs", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      action: (0, import_mysql_core.text)("action").notNull(),
      // create_order, cancel_order, update_settings, etc
      entityType: (0, import_mysql_core.text)("entity_type").notNull(),
      // order, user, business, settings
      entityId: (0, import_mysql_core.varchar)("entity_id", { length: 255 }),
      changes: (0, import_mysql_core.text)("changes"),
      // JSON con cambios realizados
      ipAddress: (0, import_mysql_core.text)("ip_address"),
      userAgent: (0, import_mysql_core.text)("user_agent"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    drivers = deliveryDrivers;
    refreshTokens = (0, import_mysql_core.mysqlTable)("refresh_tokens", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      token: (0, import_mysql_core.text)("token").notNull(),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at").notNull(),
      revoked: (0, import_mysql_core.boolean)("revoked").notNull().default(false),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    scheduledOrders = (0, import_mysql_core.mysqlTable)("scheduled_orders", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      items: (0, import_mysql_core.text)("items").notNull(),
      // JSON
      scheduledFor: (0, import_mysql_core.timestamp)("scheduled_for").notNull(),
      deliveryAddress: (0, import_mysql_core.text)("delivery_address").notNull(),
      deliveryLatitude: (0, import_mysql_core.text)("delivery_latitude"),
      deliveryLongitude: (0, import_mysql_core.text)("delivery_longitude"),
      paymentMethod: (0, import_mysql_core.text)("payment_method").notNull(),
      notes: (0, import_mysql_core.text)("notes"),
      status: (0, import_mysql_core.text)("status").notNull().default("pending"),
      // pending, processed, failed, cancelled
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }),
      // ID del pedido creado
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    supportChats = (0, import_mysql_core.mysqlTable)("support_chats", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      status: (0, import_mysql_core.text)("status").notNull().default("active"),
      // active, closed, escalated
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    supportMessages = (0, import_mysql_core.mysqlTable)("support_messages", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      chatId: (0, import_mysql_core.varchar)("chat_id", { length: 255 }).notNull(),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }),
      // null si es del bot
      message: (0, import_mysql_core.text)("message").notNull(),
      isBot: (0, import_mysql_core.boolean)("is_bot").notNull().default(false),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    reviews = (0, import_mysql_core.mysqlTable)("reviews", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      rating: (0, import_mysql_core.int)("rating").notNull(),
      // 1-5 (rating general, legacy)
      foodRating: (0, import_mysql_core.int)("food_rating"),
      // 1-5
      deliveryRating: (0, import_mysql_core.int)("delivery_rating"),
      // 1-5
      packagingRating: (0, import_mysql_core.int)("packaging_rating"),
      // 1-5
      deliveryPersonId: (0, import_mysql_core.varchar)("delivery_person_id", { length: 255 }),
      deliveryPersonRating: (0, import_mysql_core.int)("delivery_person_rating"),
      // 1-5
      comment: (0, import_mysql_core.text)("comment"),
      photos: (0, import_mysql_core.text)("photos"),
      // JSON array de URLs
      tags: (0, import_mysql_core.text)("tags"),
      // JSON array de tag IDs
      tipAmount: (0, import_mysql_core.int)("tip_amount"),
      // propina al repartidor en centavos
      approved: (0, import_mysql_core.boolean)("approved").notNull().default(true),
      flagged: (0, import_mysql_core.boolean)("flagged").notNull().default(false),
      moderationReason: (0, import_mysql_core.text)("moderation_reason"),
      businessResponse: (0, import_mysql_core.text)("business_response"),
      businessResponseAt: (0, import_mysql_core.timestamp)("business_response_at"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    reviewResponses = (0, import_mysql_core.mysqlTable)("review_responses", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      reviewId: (0, import_mysql_core.varchar)("review_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      responseText: (0, import_mysql_core.text)("response_text").notNull(),
      respondedBy: (0, import_mysql_core.varchar)("responded_by", { length: 255 }).notNull(),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    reviewTags = (0, import_mysql_core.mysqlTable)("review_tags", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      tagName: (0, import_mysql_core.varchar)("tag_name", { length: 100 }).notNull().unique(),
      category: (0, import_mysql_core.varchar)("category", { length: 50 }).notNull(),
      icon: (0, import_mysql_core.varchar)("icon", { length: 50 }),
      isPositive: (0, import_mysql_core.boolean)("is_positive").default(true),
      displayOrder: (0, import_mysql_core.int)("display_order").default(0),
      isActive: (0, import_mysql_core.boolean)("is_active").default(true),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    callLogs = (0, import_mysql_core.mysqlTable)("call_logs", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().$defaultFn(() => crypto.randomUUID()),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      callSid: (0, import_mysql_core.varchar)("call_sid", { length: 255 }),
      phoneNumber: (0, import_mysql_core.varchar)("phone_number", { length: 50 }),
      purpose: (0, import_mysql_core.varchar)("purpose", { length: 50 }).default("order_notification"),
      // order_notification, reminder
      status: (0, import_mysql_core.varchar)("status", { length: 50 }).default("initiated"),
      // initiated, ringing, answered, completed, failed, no-answer
      duration: (0, import_mysql_core.int)("duration"),
      // in seconds
      outcome: (0, import_mysql_core.varchar)("outcome", { length: 50 }),
      // accepted, rejected, no-answer
      response: (0, import_mysql_core.varchar)("response", { length: 10 }),
      // digits pressed by business
      responseAction: (0, import_mysql_core.varchar)("response_action", { length: 50 }),
      // accept, reject
      retryCount: (0, import_mysql_core.int)("retry_count").default(0),
      error: (0, import_mysql_core.text)("error"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      completedAt: (0, import_mysql_core.timestamp)("completed_at"),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at")
    });
    deliveryZones = (0, import_mysql_core.mysqlTable)("delivery_zones", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      name: (0, import_mysql_core.text)("name").notNull(),
      description: (0, import_mysql_core.text)("description"),
      deliveryFee: (0, import_mysql_core.int)("deliveryFee").notNull(),
      // en centavos
      maxDeliveryTime: (0, import_mysql_core.int)("maxDeliveryTime").default(45),
      // minutos
      isActive: (0, import_mysql_core.boolean)("isActive").notNull().default(true),
      coordinates: (0, import_mysql_core.text)("coordinates"),
      // JSON con polígono de coordenadas
      centerLatitude: (0, import_mysql_core.text)("centerLatitude"),
      centerLongitude: (0, import_mysql_core.text)("centerLongitude"),
      radiusKm: (0, import_mysql_core.int)("radiusKm").default(5),
      createdAt: (0, import_mysql_core.timestamp)("createdAt").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updatedAt").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    coupons = (0, import_mysql_core.mysqlTable)("coupons", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      code: (0, import_mysql_core.varchar)("code", { length: 50 }).notNull().unique(),
      discountType: (0, import_mysql_core.varchar)("discount_type", { length: 20 }).notNull(),
      // percentage, fixed
      discountValue: (0, import_mysql_core.int)("discount_value").notNull(),
      // en centavos o porcentaje
      minOrderAmount: (0, import_mysql_core.int)("min_order_amount").default(0),
      // mínimo de pedido en centavos
      maxUses: (0, import_mysql_core.int)("max_uses"),
      // null = ilimitado
      maxUsesPerUser: (0, import_mysql_core.int)("max_uses_per_user").default(1),
      usedCount: (0, import_mysql_core.int)("used_count").notNull().default(0),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at"),
      isActive: (0, import_mysql_core.boolean)("is_active").notNull().default(true),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    favorites = (0, import_mysql_core.mysqlTable)("favorites", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }),
      productId: (0, import_mysql_core.varchar)("product_id", { length: 255 }),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    deliveryHeatmap = (0, import_mysql_core.mysqlTable)("delivery_heatmap", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      latitude: (0, import_mysql_core.text)("latitude").notNull(),
      longitude: (0, import_mysql_core.text)("longitude").notNull(),
      orderCount: (0, import_mysql_core.int)("order_count").notNull().default(1),
      totalRevenue: (0, import_mysql_core.int)("total_revenue").notNull().default(0),
      // en centavos
      averageDeliveryTime: (0, import_mysql_core.int)("average_delivery_time").default(0),
      // en segundos
      lastOrderAt: (0, import_mysql_core.timestamp)("last_order_at"),
      gridCell: (0, import_mysql_core.varchar)("grid_cell", { length: 50 }),
      // Para agrupar por celda de grid
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    proximityAlerts = (0, import_mysql_core.mysqlTable)("proximity_alerts", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().$defaultFn(() => (0, import_crypto.randomUUID)()),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      driverId: (0, import_mysql_core.varchar)("driver_id", { length: 255 }).notNull(),
      alertType: (0, import_mysql_core.varchar)("alert_type", { length: 50 }).notNull(),
      // approaching, nearby, arrived
      distance: (0, import_mysql_core.int)("distance").notNull(),
      // metros
      destinationType: (0, import_mysql_core.varchar)("destination_type", { length: 50 }).notNull(),
      // business, customer
      notificationSent: (0, import_mysql_core.boolean)("notification_sent").default(false),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    deliveryProofs = (0, import_mysql_core.mysqlTable)("delivery_proofs", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().$defaultFn(() => (0, import_crypto.randomUUID)()),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull().unique(),
      driverId: (0, import_mysql_core.varchar)("driver_id", { length: 255 }).notNull(),
      photoUrl: (0, import_mysql_core.text)("photo_url").notNull(),
      photoBase64: (0, import_mysql_core.text)("photo_base64"),
      // Backup en base64
      latitude: (0, import_mysql_core.text)("latitude").notNull(),
      longitude: (0, import_mysql_core.text)("longitude").notNull(),
      accuracy: (0, import_mysql_core.int)("accuracy"),
      // Precisión GPS en metros
      route: (0, import_mysql_core.text)("route"),
      // JSON con breadcrumbs de la ruta
      routeDistance: (0, import_mysql_core.int)("route_distance"),
      // Distancia total en metros
      timestamp: (0, import_mysql_core.timestamp)("timestamp").notNull(),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    paymentMethods = (0, import_mysql_core.mysqlTable)("payment_methods", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      name: (0, import_mysql_core.varchar)("name", { length: 100 }).notNull(),
      provider: (0, import_mysql_core.varchar)("provider", { length: 50 }).notNull().unique(),
      displayName: (0, import_mysql_core.varchar)("display_name", { length: 100 }).notNull(),
      isActive: (0, import_mysql_core.boolean)("is_active").default(true),
      requiresManualVerification: (0, import_mysql_core.boolean)("requires_manual_verification").default(
        false
      ),
      commissionPercentage: (0, import_mysql_core.decimal)("commission_percentage", {
        precision: 5,
        scale: 2
      }).default("0.00"),
      iconUrl: (0, import_mysql_core.varchar)("icon_url", { length: 255 }),
      instructions: (0, import_mysql_core.text)("instructions"),
      config: (0, import_mysql_core.text)("config"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    paymentProofs = (0, import_mysql_core.mysqlTable)("payment_proofs", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }),
      giftCardId: (0, import_mysql_core.varchar)("gift_card_id", { length: 255 }),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      paymentProvider: (0, import_mysql_core.varchar)("payment_provider", { length: 50 }).notNull(),
      proofImageUrl: (0, import_mysql_core.varchar)("proof_image_url", { length: 500 }),
      referenceNumber: (0, import_mysql_core.varchar)("reference_number", { length: 100 }),
      amount: (0, import_mysql_core.int)("amount").notNull(),
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).default("pending"),
      verifiedBy: (0, import_mysql_core.varchar)("verified_by", { length: 255 }),
      verifiedAt: (0, import_mysql_core.timestamp)("verified_at"),
      verificationNotes: (0, import_mysql_core.text)("verification_notes"),
      submittedAt: (0, import_mysql_core.timestamp)("submitted_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    couponUsage = (0, import_mysql_core.mysqlTable)("coupon_usage", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      couponId: (0, import_mysql_core.varchar)("coupon_id", { length: 255 }).notNull(),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      discountApplied: (0, import_mysql_core.int)("discount_applied").notNull(),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    loyaltyPoints = (0, import_mysql_core.mysqlTable)("loyalty_points", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull().unique(),
      currentPoints: (0, import_mysql_core.int)("current_points").default(0),
      totalEarned: (0, import_mysql_core.int)("total_earned").default(0),
      totalRedeemed: (0, import_mysql_core.int)("total_redeemed").default(0),
      tier: (0, import_mysql_core.varchar)("tier", { length: 20 }).default("bronze"),
      tierUpdatedAt: (0, import_mysql_core.timestamp)("tier_updated_at"),
      pointsToNextTier: (0, import_mysql_core.int)("points_to_next_tier").default(1e3),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    loyaltyTransactions = (0, import_mysql_core.mysqlTable)("loyalty_transactions", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      type: (0, import_mysql_core.varchar)("type", { length: 50 }).notNull(),
      points: (0, import_mysql_core.int)("points").notNull(),
      description: (0, import_mysql_core.text)("description"),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }),
      rewardId: (0, import_mysql_core.varchar)("reward_id", { length: 255 }),
      metadata: (0, import_mysql_core.text)("metadata"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    loyaltyRewards = (0, import_mysql_core.mysqlTable)("loyalty_rewards", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      title: (0, import_mysql_core.varchar)("title", { length: 255 }).notNull(),
      description: (0, import_mysql_core.text)("description"),
      pointsCost: (0, import_mysql_core.int)("points_cost").notNull(),
      type: (0, import_mysql_core.varchar)("type", { length: 50 }).notNull(),
      value: (0, import_mysql_core.int)("value").notNull(),
      isAvailable: (0, import_mysql_core.boolean)("is_available").default(true),
      minTier: (0, import_mysql_core.varchar)("min_tier", { length: 20 }),
      maxRedemptions: (0, import_mysql_core.int)("max_redemptions"),
      currentRedemptions: (0, import_mysql_core.int)("current_redemptions").default(0),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at"),
      imageUrl: (0, import_mysql_core.text)("image_url"),
      terms: (0, import_mysql_core.text)("terms"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    loyaltyRedemptions = (0, import_mysql_core.mysqlTable)("loyalty_redemptions", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      rewardId: (0, import_mysql_core.varchar)("reward_id", { length: 255 }).notNull(),
      pointsSpent: (0, import_mysql_core.int)("points_spent").notNull(),
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).default("active"),
      couponCode: (0, import_mysql_core.varchar)("coupon_code", { length: 50 }),
      usedAt: (0, import_mysql_core.timestamp)("used_at"),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    loyaltyChallenges = (0, import_mysql_core.mysqlTable)("loyalty_challenges", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      title: (0, import_mysql_core.varchar)("title", { length: 255 }).notNull(),
      description: (0, import_mysql_core.text)("description"),
      type: (0, import_mysql_core.varchar)("type", { length: 50 }).notNull(),
      target: (0, import_mysql_core.int)("target").notNull(),
      rewardPoints: (0, import_mysql_core.int)("reward_points").notNull(),
      isActive: (0, import_mysql_core.boolean)("is_active").default(true),
      startsAt: (0, import_mysql_core.timestamp)("starts_at"),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    loyaltyChallengeProgress = (0, import_mysql_core.mysqlTable)(
      "loyalty_challenge_progress",
      {
        id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
        userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
        challengeId: (0, import_mysql_core.varchar)("challenge_id", { length: 255 }).notNull(),
        progress: (0, import_mysql_core.int)("progress").default(0),
        completed: (0, import_mysql_core.boolean)("completed").default(false),
        completedAt: (0, import_mysql_core.timestamp)("completed_at"),
        claimed: (0, import_mysql_core.boolean)("claimed").default(false),
        claimedAt: (0, import_mysql_core.timestamp)("claimed_at"),
        createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
        updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
          import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
        )
      }
    );
    achievements = (0, import_mysql_core.mysqlTable)("achievements", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      name: (0, import_mysql_core.varchar)("name", { length: 255 }).notNull(),
      description: (0, import_mysql_core.text)("description"),
      icon: (0, import_mysql_core.varchar)("icon", { length: 100 }),
      category: (0, import_mysql_core.varchar)("category", { length: 50 }),
      requirementType: (0, import_mysql_core.varchar)("requirement_type", { length: 50 }),
      requirementValue: (0, import_mysql_core.int)("requirement_value"),
      rewardPoints: (0, import_mysql_core.int)("reward_points").default(0),
      badgeImageUrl: (0, import_mysql_core.text)("badge_image_url"),
      isActive: (0, import_mysql_core.boolean)("is_active").default(true),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    userAchievements = (0, import_mysql_core.mysqlTable)("user_achievements", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      achievementId: (0, import_mysql_core.varchar)("achievement_id", { length: 255 }).notNull(),
      unlockedAt: (0, import_mysql_core.timestamp)("unlocked_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    userFavorites = (0, import_mysql_core.mysqlTable)("user_favorites", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      itemType: (0, import_mysql_core.varchar)("item_type", { length: 50 }).notNull(),
      itemId: (0, import_mysql_core.varchar)("item_id", { length: 255 }).notNull(),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    userPreferences = (0, import_mysql_core.mysqlTable)("user_preferences", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull().unique(),
      cuisineTypes: (0, import_mysql_core.text)("cuisine_types"),
      priceRange: (0, import_mysql_core.varchar)("price_range", { length: 20 }).default("mid"),
      dietaryRestrictions: (0, import_mysql_core.text)("dietary_restrictions"),
      preferredOrderTimes: (0, import_mysql_core.text)("preferred_order_times"),
      favoriteCategories: (0, import_mysql_core.text)("favorite_categories"),
      spiceLevel: (0, import_mysql_core.int)("spice_level").default(3),
      healthScore: (0, import_mysql_core.int)("health_score").default(5),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    aiRecommendations = (0, import_mysql_core.mysqlTable)("ai_recommendations", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      recommendationType: (0, import_mysql_core.varchar)("recommendation_type", { length: 50 }).notNull(),
      itemType: (0, import_mysql_core.varchar)("item_type", { length: 50 }).notNull(),
      itemId: (0, import_mysql_core.varchar)("item_id", { length: 255 }).notNull(),
      confidenceScore: (0, import_mysql_core.int)("confidence_score").notNull(),
      reason: (0, import_mysql_core.text)("reason"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at"),
      clicked: (0, import_mysql_core.boolean)("clicked").default(false),
      ordered: (0, import_mysql_core.boolean)("ordered").default(false)
    });
    supportTickets = (0, import_mysql_core.mysqlTable)("support_tickets", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }),
      subject: (0, import_mysql_core.varchar)("subject", { length: 255 }).notNull(),
      category: (0, import_mysql_core.varchar)("category", { length: 50 }),
      priority: (0, import_mysql_core.varchar)("priority", { length: 20 }).default("medium"),
      status: (0, import_mysql_core.varchar)("status", { length: 50 }).default("open"),
      assignedTo: (0, import_mysql_core.varchar)("assigned_to", { length: 255 }),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      ),
      resolvedAt: (0, import_mysql_core.timestamp)("resolved_at")
    });
    groupOrders = (0, import_mysql_core.mysqlTable)("group_orders", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      hostUserId: (0, import_mysql_core.varchar)("host_user_id", { length: 255 }).notNull(),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      deliveryAddressId: (0, import_mysql_core.varchar)("delivery_address_id", { length: 255 }),
      splitMethod: (0, import_mysql_core.varchar)("split_method", { length: 20 }).default("equal"),
      status: (0, import_mysql_core.varchar)("status", { length: 50 }).notNull().default("open"),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at").notNull(),
      finalOrderId: (0, import_mysql_core.varchar)("final_order_id", { length: 255 }),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    groupOrderParticipants = (0, import_mysql_core.mysqlTable)("group_order_participants", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      groupOrderId: (0, import_mysql_core.varchar)("group_order_id", { length: 255 }).notNull(),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull(),
      items: (0, import_mysql_core.text)("items").notNull(),
      subtotal: (0, import_mysql_core.int)("subtotal").notNull(),
      paid: (0, import_mysql_core.boolean)("paid").default(false),
      paymentMethod: (0, import_mysql_core.varchar)("payment_method", { length: 50 }),
      joinedAt: (0, import_mysql_core.timestamp)("joined_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    groupOrderInvitations = (0, import_mysql_core.mysqlTable)("group_order_invitations", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      groupOrderId: (0, import_mysql_core.varchar)("group_order_id", { length: 255 }).notNull(),
      invitedBy: (0, import_mysql_core.varchar)("invited_by", { length: 255 }).notNull(),
      invitedUserId: (0, import_mysql_core.varchar)("invited_user_id", { length: 255 }),
      invitedPhone: (0, import_mysql_core.varchar)("invited_phone", { length: 20 }),
      status: (0, import_mysql_core.varchar)("status", { length: 50 }).default("pending"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      respondedAt: (0, import_mysql_core.timestamp)("responded_at")
    });
    subscriptions = (0, import_mysql_core.mysqlTable)("subscriptions", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      userId: (0, import_mysql_core.varchar)("user_id", { length: 255 }).notNull().unique(),
      plan: (0, import_mysql_core.varchar)("plan", { length: 50 }).notNull().default("free"),
      status: (0, import_mysql_core.varchar)("status", { length: 50 }).notNull().default("active"),
      price: (0, import_mysql_core.int)("price").notNull().default(0),
      billingCycle: (0, import_mysql_core.varchar)("billing_cycle", { length: 20 }).default("monthly"),
      autoRenew: (0, import_mysql_core.boolean)("auto_renew").notNull().default(true),
      currentPeriodStart: (0, import_mysql_core.timestamp)("current_period_start").notNull(),
      currentPeriodEnd: (0, import_mysql_core.timestamp)("current_period_end").notNull(),
      cancelledAt: (0, import_mysql_core.timestamp)("cancelled_at"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    subscriptionBenefits = (0, import_mysql_core.mysqlTable)("subscription_benefits", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      plan: (0, import_mysql_core.varchar)("plan", { length: 50 }).notNull(),
      benefitType: (0, import_mysql_core.varchar)("benefit_type", { length: 50 }).notNull(),
      benefitValue: (0, import_mysql_core.int)("benefit_value"),
      description: (0, import_mysql_core.text)("description"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    subscriptionPlans = (0, import_mysql_core.mysqlTable)("subscription_plans", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      planKey: (0, import_mysql_core.varchar)("plan_key", { length: 50 }).notNull().unique(),
      name: (0, import_mysql_core.varchar)("name", { length: 100 }).notNull(),
      description: (0, import_mysql_core.text)("description"),
      price: (0, import_mysql_core.int)("price").notNull().default(0),
      billingCycle: (0, import_mysql_core.varchar)("billing_cycle", { length: 20 }).default("monthly"),
      isActive: (0, import_mysql_core.boolean)("is_active").notNull().default(true),
      displayOrder: (0, import_mysql_core.int)("display_order").default(0),
      color: (0, import_mysql_core.varchar)("color", { length: 20 }).default("#DC2626"),
      icon: (0, import_mysql_core.varchar)("icon", { length: 50 }).default("star"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    giftCards = (0, import_mysql_core.mysqlTable)("gift_cards", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      code: (0, import_mysql_core.varchar)("code", { length: 50 }).notNull().unique(),
      amount: (0, import_mysql_core.int)("original_amount").notNull(),
      balance: (0, import_mysql_core.int)("balance").notNull(),
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).notNull().default("active"),
      purchasedBy: (0, import_mysql_core.varchar)("from_user_id", { length: 255 }),
      recipientEmail: (0, import_mysql_core.varchar)("to_email", { length: 255 }),
      recipientPhone: (0, import_mysql_core.varchar)("to_phone", { length: 20 }),
      toUserId: (0, import_mysql_core.varchar)("to_user_id", { length: 255 }),
      message: (0, import_mysql_core.text)("message"),
      design: (0, import_mysql_core.varchar)("design", { length: 50 }).default("default"),
      expiresAt: (0, import_mysql_core.timestamp)("expires_at"),
      redeemedAt: (0, import_mysql_core.timestamp)("redeemed_at"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    giftCardTransactions = (0, import_mysql_core.mysqlTable)("gift_card_transactions", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      giftCardId: (0, import_mysql_core.varchar)("gift_card_id", { length: 255 }).notNull(),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }),
      amount: (0, import_mysql_core.int)("amount").notNull(),
      balanceAfter: (0, import_mysql_core.int)("balance_after").notNull(),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    giftCardDesigns = (0, import_mysql_core.mysqlTable)("gift_card_designs", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      name: (0, import_mysql_core.varchar)("name", { length: 100 }).notNull().unique(),
      imageUrl: (0, import_mysql_core.text)("image_url").notNull(),
      category: (0, import_mysql_core.varchar)("category", { length: 50 }).default("general"),
      isActive: (0, import_mysql_core.boolean)("is_active").default(true),
      displayOrder: (0, import_mysql_core.int)("display_order").default(0),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    deliveryRequests = (0, import_mysql_core.mysqlTable)("delivery_requests", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      businessId: (0, import_mysql_core.varchar)("business_id", { length: 255 }).notNull(),
      businessName: (0, import_mysql_core.varchar)("business_name", { length: 255 }).notNull(),
      pickupAddress: (0, import_mysql_core.text)("pickup_address").notNull(),
      pickupLatitude: (0, import_mysql_core.text)("pickup_latitude"),
      pickupLongitude: (0, import_mysql_core.text)("pickup_longitude"),
      dropoffAddress: (0, import_mysql_core.text)("dropoff_address").notNull(),
      dropoffLatitude: (0, import_mysql_core.text)("dropoff_latitude"),
      dropoffLongitude: (0, import_mysql_core.text)("dropoff_longitude"),
      contactPhone: (0, import_mysql_core.varchar)("contact_phone", { length: 30 }),
      fee: (0, import_mysql_core.int)("fee").notNull().default(350),
      // tarifa plana 3,50 €
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).notNull().default("pending"),
      // pending, accepted, picked_up, delivered, cancelled
      driverId: (0, import_mysql_core.varchar)("driver_id", { length: 255 }),
      notes: (0, import_mysql_core.text)("notes"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      acceptedAt: (0, import_mysql_core.timestamp)("accepted_at"),
      deliveredAt: (0, import_mysql_core.timestamp)("delivered_at")
    });
    ticketMessages = (0, import_mysql_core.mysqlTable)("ticket_messages", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      ticketId: (0, import_mysql_core.varchar)("ticket_id", { length: 255 }).notNull(),
      senderId: (0, import_mysql_core.varchar)("sender_id", { length: 255 }).notNull(),
      senderType: (0, import_mysql_core.varchar)("sender_type", { length: 20 }).notNull(),
      // user, admin
      message: (0, import_mysql_core.text)("message").notNull(),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`)
    });
    orderIssues = (0, import_mysql_core.mysqlTable)("order_issues", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      ticketId: (0, import_mysql_core.varchar)("ticket_id", { length: 255 }),
      // support_tickets.id — hilo de mensajes
      reportedBy: (0, import_mysql_core.varchar)("reported_by", { length: 255 }).notNull(),
      reporterRole: (0, import_mysql_core.varchar)("reporter_role", { length: 30 }).notNull(),
      // customer, business_owner, delivery_driver
      // missing_items, wrong_items, damaged, quality, late_delivery,
      // never_arrived, incomplete, driver_issue, other
      issueType: (0, import_mysql_core.varchar)("issue_type", { length: 40 }).notNull(),
      description: (0, import_mysql_core.text)("description").notNull(),
      photos: (0, import_mysql_core.text)("photos"),
      // JSON array de URLs (Cloudinary)
      affectedItems: (0, import_mysql_core.text)("affected_items"),
      // JSON array para reembolso parcial por ítem
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).notNull().default("open"),
      // open, in_review, resolved, rejected
      priority: (0, import_mysql_core.varchar)("priority", { length: 20 }).notNull().default("medium"),
      // low, medium, high, urgent
      // refund_full, refund_partial, redelivery, rejected
      resolutionType: (0, import_mysql_core.varchar)("resolution_type", { length: 30 }),
      resolutionAmount: (0, import_mysql_core.int)("resolution_amount"),
      // en centavos
      liableParty: (0, import_mysql_core.varchar)("liable_party", { length: 20 }),
      // business, driver, platform
      customerMessage: (0, import_mysql_core.text)("customer_message"),
      // respuesta visible para el cliente
      internalNote: (0, import_mysql_core.text)("internal_note"),
      // nota solo para admins
      assignedTo: (0, import_mysql_core.varchar)("assigned_to", { length: 255 }),
      resolvedBy: (0, import_mysql_core.varchar)("resolved_by", { length: 255 }),
      resolvedAt: (0, import_mysql_core.timestamp)("resolved_at"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    refunds = (0, import_mysql_core.mysqlTable)("refunds", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      issueId: (0, import_mysql_core.varchar)("issue_id", { length: 255 }),
      // order_issues.id si nace de una incidencia
      customerId: (0, import_mysql_core.varchar)("customer_id", { length: 255 }).notNull(),
      amount: (0, import_mysql_core.int)("amount").notNull(),
      // en centavos
      type: (0, import_mysql_core.varchar)("type", { length: 20 }).notNull(),
      // issue, cancellation, dispute, manual
      reason: (0, import_mysql_core.text)("reason"),
      // stripe = devolución al PaymentIntent original (automática)
      // manual_transfer = el admin transfiere a mano y sube comprobante
      // cash_none = pagó en efectivo y nunca se cobró: nada que devolver
      method: (0, import_mysql_core.varchar)("method", { length: 20 }).notNull(),
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).notNull().default("pending"),
      // pending, processing, completed, failed
      stripeRefundId: (0, import_mysql_core.varchar)("stripe_refund_id", { length: 255 }),
      stripePaymentIntentId: (0, import_mysql_core.varchar)("stripe_payment_intent_id", { length: 255 }),
      liableParty: (0, import_mysql_core.varchar)("liable_party", { length: 20 }),
      // business, driver, platform
      businessDeduction: (0, import_mysql_core.int)("business_deduction").notNull().default(0),
      driverDeduction: (0, import_mysql_core.int)("driver_deduction").notNull().default(0),
      platformCost: (0, import_mysql_core.int)("platform_cost").notNull().default(0),
      payoutAdjusted: (0, import_mysql_core.boolean)("payout_adjusted").notNull().default(false),
      // si ya se descontó del payout
      requestedBy: (0, import_mysql_core.varchar)("requested_by", { length: 255 }),
      // admin que lo autorizó (null = automático)
      processedAt: (0, import_mysql_core.timestamp)("processed_at"),
      failureReason: (0, import_mysql_core.text)("failure_reason"),
      proofUrl: (0, import_mysql_core.text)("proof_url"),
      // comprobante de la transferencia manual
      notes: (0, import_mysql_core.text)("notes"),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    businessCategories = (0, import_mysql_core.mysqlTable)("business_categories", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      name: (0, import_mysql_core.varchar)("name", { length: 100 }).notNull(),
      slug: (0, import_mysql_core.varchar)("slug", { length: 100 }).notNull().unique(),
      icon: (0, import_mysql_core.varchar)("icon", { length: 50 }).notNull().default("grid"),
      color: (0, import_mysql_core.varchar)("color", { length: 20 }).notNull().default("#6B7280"),
      description: (0, import_mysql_core.text)("description"),
      isActive: (0, import_mysql_core.boolean)("is_active").notNull().default(true),
      displayOrder: (0, import_mysql_core.int)("display_order").notNull().default(0),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    substitutions = (0, import_mysql_core.mysqlTable)("substitutions", {
      id: (0, import_mysql_core.varchar)("id", { length: 255 }).primaryKey().default(import_drizzle_orm.sql`(UUID())`),
      orderId: (0, import_mysql_core.varchar)("order_id", { length: 255 }).notNull(),
      itemProductId: (0, import_mysql_core.varchar)("item_product_id", { length: 255 }).notNull(),
      itemName: (0, import_mysql_core.text)("item_name"),
      originalPrice: (0, import_mysql_core.int)("original_price"),
      // centavos
      substituteProductId: (0, import_mysql_core.varchar)("substitute_product_id", { length: 255 }).notNull(),
      substituteName: (0, import_mysql_core.text)("substitute_name"),
      substituteImage: (0, import_mysql_core.text)("substitute_image"),
      substitutePrice: (0, import_mysql_core.int)("substitute_price"),
      // centavos
      priceDelta: (0, import_mysql_core.int)("price_delta").default(0),
      // centavos; delta TOTAL (unitario x cantidad)
      quantity: (0, import_mysql_core.int)("quantity").default(1),
      status: (0, import_mysql_core.varchar)("status", { length: 20 }).notNull().default("proposed"),
      // proposed | approved | rejected | applied
      proposedBy: (0, import_mysql_core.varchar)("proposed_by", { length: 255 }),
      stripePaymentIntentId: (0, import_mysql_core.varchar)("stripe_payment_intent_id", { length: 255 }),
      createdAt: (0, import_mysql_core.timestamp)("created_at").default(import_drizzle_orm.sql`CURRENT_TIMESTAMP`),
      decidedAt: (0, import_mysql_core.timestamp)("decided_at"),
      appliedAt: (0, import_mysql_core.timestamp)("applied_at"),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
    appSettings = (0, import_mysql_core.mysqlTable)("app_settings", {
      key: (0, import_mysql_core.varchar)("key", { length: 191 }).primaryKey(),
      value: (0, import_mysql_core.text)("value").notNull(),
      updatedAt: (0, import_mysql_core.timestamp)("updated_at").default(
        import_drizzle_orm.sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`
      )
    });
  }
});

// server/gamificationSeed.ts
var gamificationSeed_exports = {};
__export(gamificationSeed_exports, {
  seedGamification: () => seedGamification
});
async function seedGamification() {
  try {
    const existingRewards = await db.select().from(loyaltyRewards).limit(1);
    if (existingRewards.length === 0) {
      await db.insert(loyaltyRewards).values(DEFAULT_REWARDS);
      console.log("\u2705 Gamification rewards seeded");
    }
    const existingAchievements = await db.select().from(achievements).limit(1);
    if (existingAchievements.length === 0) {
      await db.insert(achievements).values(DEFAULT_ACHIEVEMENTS);
      console.log("\u2705 Gamification achievements seeded");
    }
  } catch (error) {
    console.error("\u274C Gamification seed error:", error);
  }
}
var DEFAULT_REWARDS, DEFAULT_ACHIEVEMENTS;
var init_gamificationSeed = __esm({
  "server/gamificationSeed.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    DEFAULT_REWARDS = [
      {
        id: crypto.randomUUID(),
        title: "Env\xEDo Gratis",
        description: "Env\xEDo gratuito en tu pr\xF3ximo pedido en Soria",
        pointsCost: 500,
        type: "shipping_discount",
        value: "free_shipping",
        minTier: "bronze",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "V\xE1lido solo en Soria. Expira en 30 d\xEDas.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "15% Descuento",
        description: "Descuento del 15% en cualquier pedido en Soria",
        pointsCost: 750,
        type: "percent_discount",
        value: "15",
        minTier: "bronze",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "M\xEDnimo pedido 15\u20AC. V\xE1lido en Soria.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "5\u20AC Cashback",
        description: "Recibe 5 euros de descuento en tu pr\xF3ximo pedido",
        pointsCost: 1e3,
        type: "fixed_discount",
        value: "5",
        minTier: "bronze",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "M\xEDnimo pedido 20\u20AC. V\xE1lido 30 d\xEDas.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "20% Descuento VIP",
        description: "Descuento exclusivo para miembros Gold y Platinum",
        pointsCost: 1500,
        type: "percent_discount",
        value: "20",
        minTier: "gold",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "Exclusivo Gold/Platinum. M\xEDnimo 25\u20AC.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "10\u20AC Cashback",
        description: "Recibe 10 euros de descuento en tu pr\xF3ximo pedido",
        pointsCost: 2e3,
        type: "fixed_discount",
        value: "10",
        minTier: "silver",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "M\xEDnimo pedido 30\u20AC. V\xE1lido 30 d\xEDas.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "10% de descuento",
        description: "Obt\xE9n un 10% de descuento en tu pr\xF3ximo pedido",
        pointsCost: 500,
        type: "percent_discount",
        value: "10",
        minTier: "bronze",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "V\xE1lido en cualquier pedido.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "Env\xEDo gratis",
        description: "Env\xEDo gratis en tu pr\xF3ximo pedido",
        pointsCost: 300,
        type: "shipping_discount",
        value: "free_shipping",
        minTier: "bronze",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "V\xE1lido 15 d\xEDas.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "Bebida gratis",
        description: "Obt\xE9n una bebida gratis valorada hasta 5\u20AC",
        pointsCost: 750,
        type: "free_item",
        value: "drink_5",
        minTier: "bronze",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "Hasta 5\u20AC de valor.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "15% de descuento",
        description: "15% de descuento en pedidos mayores a 20\u20AC",
        pointsCost: 1e3,
        type: "percent_discount",
        value: "15",
        minTier: "bronze",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "Solo pedidos >20\u20AC.",
        isAvailable: true,
        currentRedemptions: 0
      },
      {
        id: crypto.randomUUID(),
        title: "Cena gratis",
        description: "Una cena completa gratis (hasta 30\u20AC)",
        pointsCost: 3e3,
        type: "free_item",
        value: "dinner_30",
        minTier: "gold",
        maxRedemptions: null,
        expiresAt: null,
        imageUrl: null,
        terms: "Hasta 30\u20AC. Solo Gold/Platinum.",
        isAvailable: true,
        currentRedemptions: 0
      }
    ];
    DEFAULT_ACHIEVEMENTS = [
      {
        id: crypto.randomUUID(),
        name: "Primer Pedido",
        description: "Realiza tu primer pedido",
        icon: "\u{1F3AF}",
        requirementType: "order_count",
        requirementValue: 1,
        rewardPoints: 50,
        isActive: true
      },
      {
        id: crypto.randomUUID(),
        name: "Cliente Fiel",
        description: "Completa 5 pedidos",
        icon: "\u{1F49A}",
        requirementType: "order_count",
        requirementValue: 5,
        rewardPoints: 100,
        isActive: true
      },
      {
        id: crypto.randomUUID(),
        name: "Pedidor",
        description: "Completa 10 pedidos",
        icon: "\u2B50",
        requirementType: "order_count",
        requirementValue: 10,
        rewardPoints: 200,
        isActive: true
      },
      {
        id: crypto.randomUUID(),
        name: "VIP",
        description: "Completa 25 pedidos",
        icon: "\u{1F451}",
        requirementType: "order_count",
        requirementValue: 25,
        rewardPoints: 500,
        isActive: true
      },
      {
        id: crypto.randomUUID(),
        name: "Gastador",
        description: "Gasta 100\u20AC en total",
        icon: "\u{1F4B0}",
        requirementType: "total_spent",
        requirementValue: 100,
        rewardPoints: 100,
        isActive: true
      },
      {
        id: crypto.randomUUID(),
        name: "Gran Gastador",
        description: "Gasta 500\u20AC en total",
        icon: "\u{1F3C6}",
        requirementType: "total_spent",
        requirementValue: 500,
        rewardPoints: 300,
        isActive: true
      },
      {
        id: crypto.randomUUID(),
        name: "Mega Gastador",
        description: "Gasta 2000\u20AC en total",
        icon: "\u{1F48E}",
        requirementType: "total_spent",
        requirementValue: 2e3,
        rewardPoints: 1e3,
        isActive: true
      },
      {
        id: crypto.randomUUID(),
        name: "Critico",
        description: "Escribe 3 rese\xF1as",
        icon: "\u{1F4DD}",
        requirementType: "review_count",
        requirementValue: 3,
        rewardPoints: 75,
        isActive: true
      }
    ];
  }
});

// server/orderNumberService.ts
var orderNumberService_exports = {};
__export(orderNumberService_exports, {
  backfillOrderNumbers: () => backfillOrderNumbers,
  ensureOrderNumberSchema: () => ensureOrderNumberSchema,
  formatOrderNumber: () => formatOrderNumber,
  nextOrderNumber: () => nextOrderNumber,
  orderRef: () => orderRef,
  orderRefFromId: () => orderRefFromId
});
async function nextOrderNumber() {
  try {
    const [result] = await db.execute(import_drizzle_orm2.sql`
      INSERT INTO order_number_sequence (id, next_value)
      VALUES (1, 1)
      ON DUPLICATE KEY UPDATE next_value = LAST_INSERT_ID(next_value + 1)
    `);
    const insertId = result?.insertId ?? result?.[0]?.insertId ?? null;
    if (insertId && Number.isFinite(Number(insertId))) {
      return Number(insertId);
    }
    const [rows] = await db.execute(
      import_drizzle_orm2.sql`SELECT next_value AS v FROM order_number_sequence WHERE id = 1`
    );
    const row = rows[0];
    return row ? Number(row.v) - 1 : null;
  } catch (error) {
    console.error("nextOrderNumber error:", error);
    return null;
  }
}
function formatOrderNumber(n) {
  if (n == null || !Number.isFinite(Number(n))) return "";
  return `#CY${String(Math.max(0, Math.trunc(Number(n)))).padStart(6, "0")}`;
}
function orderRef(order) {
  const formatted = formatOrderNumber(order?.orderNumber);
  if (formatted) return formatted;
  const id = String(order?.id ?? "");
  return id ? `#${id.slice(-6).toUpperCase()}` : "#\u2014";
}
async function orderRefFromId(orderId) {
  if (!orderId) return "#\u2014";
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [row] = await db.select({ orderNumber: orders2.orderNumber, id: orders2.id }).from(orders2).where(eq79(orders2.id, orderId)).limit(1);
    return orderRef(row ?? { id: orderId });
  } catch {
    return `#${String(orderId).slice(-6).toUpperCase()}`;
  }
}
async function backfillOrderNumbers() {
  try {
    const [maxRows] = await db.execute(
      import_drizzle_orm2.sql`SELECT COALESCE(MAX(order_number), 0) AS base FROM orders`
    );
    const base = Number(maxRows[0]?.base ?? 0);
    const [res] = await db.execute(import_drizzle_orm2.sql`
      UPDATE orders o
      JOIN (
        SELECT id, rn FROM (
          SELECT id, ROW_NUMBER() OVER (ORDER BY created_at ASC, id ASC) AS rn
          FROM orders
          WHERE order_number IS NULL
        ) t
      ) ranked ON ranked.id = o.id
      SET o.order_number = ranked.rn + ${base}
    `);
    await db.execute(import_drizzle_orm2.sql`
      INSERT INTO order_number_sequence (id, next_value)
      VALUES (1, COALESCE((SELECT MAX(order_number) FROM orders), 0) + 1)
      ON DUPLICATE KEY UPDATE
        next_value = GREATEST(next_value, COALESCE((SELECT MAX(order_number) FROM orders), 0) + 1)
    `);
    const affected = Number(res?.affectedRows ?? 0);
    if (affected > 0) {
      console.log(
        `\u{1F522} Backfill de numeraci\xF3n: ${affected} pedidos numerados (base ${base})`
      );
    }
    return affected;
  } catch (error) {
    console.error("backfillOrderNumbers error:", error);
    return 0;
  }
}
async function ensureOrderNumberSchema() {
  try {
    await db.execute(import_drizzle_orm2.sql`
      CREATE TABLE IF NOT EXISTS order_number_sequence (
        id TINYINT PRIMARY KEY DEFAULT 1,
        next_value INT NOT NULL DEFAULT 1
      )
    `);
    await db.execute(
      import_drizzle_orm2.sql`ALTER TABLE orders ADD COLUMN order_number INT NULL`
    );
    console.log("\u2705 Numeraci\xF3n de pedidos #CY asegurada");
  } catch (err) {
    if (err?.code !== "ER_DUP_FIELDNAME" && err?.errno !== 1060) {
      console.error("ensureOrderNumberSchema:", err?.message ?? err);
    }
  }
  try {
    await db.execute(
      import_drizzle_orm2.sql`CREATE UNIQUE INDEX idx_orders_order_number ON orders (order_number)`
    );
  } catch {
  }
}
var import_drizzle_orm2;
var init_orderNumberService = __esm({
  "server/orderNumberService.ts"() {
    "use strict";
    init_db();
    import_drizzle_orm2 = require("drizzle-orm");
  }
});

// server/db.ts
var db_exports = {};
__export(db_exports, {
  connection: () => connection,
  db: () => db,
  ensureTestSchema: () => ensureTestSchema,
  runStartupMigrations: () => runStartupMigrations
});
function createConnectionConfig() {
  const mysqlUrl = process.env.MYSQL_DATABASE_URL || process.env.DATABASE_URL;
  if (mysqlUrl) {
    const url = new URL(mysqlUrl);
    const config4 = {
      host: url.hostname,
      port: parseInt(url.port || "3306"),
      user: url.username,
      password: url.password,
      database: url.pathname.slice(1),
      // Remove leading /
      waitForConnections: true,
      connectionLimit: 3,
      maxIdle: 3,
      idleTimeout: 6e4,
      queueLimit: 0,
      connectTimeout: 3e4,
      enableKeepAlive: true,
      keepAliveInitialDelay: 1e4,
      charset: "utf8mb4",
      // Forzar UTC: sin esto mysql2 usa la zona horaria local de la sesión
      // y los timestamp se desplazaban horas respecto al reloj real
      timezone: "Z"
    };
    if (url.searchParams.get("ssl-mode") === "DISABLED") {
      config4.ssl = false;
      console.log("\u274C SSL disabled for MySQL connection");
    } else if (url.hostname === "localhost" || url.hostname === "127.0.0.1") {
      config4.ssl = false;
      console.log("\u{1F3E0} SSL disabled for localhost connection");
    } else {
      const caEnv = process.env.AIVEN_CA_CERT;
      const caPath = import_path.default.join(process.cwd(), "ca.pem");
      const ca = caEnv ? Buffer.from(caEnv, "base64").toString("utf-8") : import_fs.default.existsSync(caPath) ? import_fs.default.readFileSync(caPath, "utf-8") : null;
      if (ca) {
        config4.ssl = { ca, rejectUnauthorized: true };
        console.log("\u{1F4DC} Using SSL certificate for MySQL connection");
      } else {
        config4.ssl = { rejectUnauthorized: false };
        console.log("\u{1F512} Using SSL without CA certificate");
      }
    }
    return config4;
  }
  const host = process.env.DB_HOST || "localhost";
  const config3 = {
    host,
    port: parseInt(process.env.DB_PORT || "3306"),
    user: process.env.DB_USER || "root",
    password: process.env.DB_PASSWORD,
    database: process.env.DB_NAME,
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0,
    timezone: "Z"
  };
  const fallbackCharset = process.env.DB_CHARSET || "utf8mb4";
  config3.charset = normalizeCharset(fallbackCharset);
  if (host !== "localhost" && host !== "127.0.0.1") {
    config3.ssl = {
      rejectUnauthorized: false
    };
    console.log(
      "\u{1F512} Using SSL with disabled certificate verification for",
      host
    );
  }
  return config3;
}
function normalizeCharset(charset) {
  const normalized = charset.toLowerCase();
  if (normalized === "cesu8" || normalized === "cesu-8") {
    return "utf8mb4";
  }
  return charset;
}
async function runStartupMigrations() {
  const conn = await connection.getConnection();
  try {
    try {
      await conn.query(
        `ALTER TABLE businesses ADD COLUMN custom_commission INT DEFAULT NULL`
      );
      console.log("Added custom_commission to businesses");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note:", err.message);
    }
    for (const table of ["proximity_alerts", "delivery_proofs"]) {
      try {
        await conn.query(
          `ALTER TABLE ${table} MODIFY id VARCHAR(255) NOT NULL DEFAULT (UUID())`
        );
        console.log(`\u2705 ${table} id default UUID asegurado`);
      } catch (err) {
        console.log(`Migration note (${table}):`, err.message);
      }
    }
    try {
      await conn.query(
        `ALTER TABLE orders ADD COLUMN scheduled_for TIMESTAMP NULL DEFAULT NULL`
      );
      console.log("\u2705 Added scheduled_for to orders");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note (scheduled_for):", err.message);
    }
    try {
      await conn.query(
        `ALTER TABLE users ADD COLUMN profile_image TEXT DEFAULT NULL`
      );
      console.log("Added profile_image to users");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note:", err.message);
    }
    try {
      await conn.query(
        `ALTER TABLE users ADD COLUMN bank_account TEXT DEFAULT NULL`
      );
      console.log("Added bank_account to users");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note:", err.message);
    }
    try {
      await conn.query(
        `ALTER TABLE orders ADD COLUMN chat_messages TEXT DEFAULT NULL`
      );
      console.log("\u2705 Added chat_messages to orders");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note:", err.message);
    }
    try {
      await conn.query(
        `ALTER TABLE orders ADD COLUMN deleted_at DATETIME NULL DEFAULT NULL`
      );
      console.log("\u2705 Added deleted_at to orders");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note (deleted_at):", err.message);
    }
    try {
      await conn.query(
        `ALTER TABLE reviews ADD COLUMN delivery_person_rating INT DEFAULT NULL`
      );
      console.log("\u2705 Added delivery_person_rating to reviews");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note:", err.message);
    }
    try {
      await conn.query(
        `ALTER TABLE reviews ADD COLUMN tip_amount INT DEFAULT NULL`
      );
      console.log("\u2705 Added tip_amount to reviews");
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note:", err.message);
    }
    for (const col of ["food_rating", "delivery_rating", "packaging_rating"]) {
      try {
        await conn.query(
          `ALTER TABLE reviews ADD COLUMN ${col} INT DEFAULT NULL`
        );
        console.log(`\u2705 Added ${col} to reviews`);
      } catch (err) {
        if (err.code !== "ER_DUP_FIELDNAME")
          console.log("Migration note:", err.message);
      }
    }
    for (const [col, ddl] of [
      ["delivery_person_id", "VARCHAR(255) DEFAULT NULL"],
      ["photos", "TEXT DEFAULT NULL"],
      ["tags", "TEXT DEFAULT NULL"]
    ]) {
      try {
        await conn.query(
          `ALTER TABLE reviews ADD COLUMN ${col} ${ddl}`
        );
        console.log(`\u2705 Added ${col} to reviews`);
      } catch (err) {
        if (err.code !== "ER_DUP_FIELDNAME")
          console.log("Migration note:", err.message);
      }
    }
    try {
      await conn.query(
        `CREATE TABLE IF NOT EXISTS app_settings (
          \`key\` VARCHAR(191) NOT NULL PRIMARY KEY,
          value TEXT NOT NULL,
          updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
        )`
      );
    } catch (err) {
      console.log("Migration note (app_settings):", err.message);
    }
    try {
      await conn.query(
        `CREATE TABLE IF NOT EXISTS substitutions (
          id VARCHAR(255) NOT NULL PRIMARY KEY,
          order_id VARCHAR(255) NOT NULL,
          item_product_id VARCHAR(255) NOT NULL,
          item_name TEXT,
          original_price INT,
          substitute_product_id VARCHAR(255) NOT NULL,
          substitute_name TEXT,
          substitute_image TEXT,
          substitute_price INT,
          price_delta INT DEFAULT 0,
          quantity INT DEFAULT 1,
          status VARCHAR(20) NOT NULL DEFAULT 'proposed',
          proposed_by VARCHAR(255),
          stripe_payment_intent_id VARCHAR(255),
          created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
          decided_at TIMESTAMP NULL,
          applied_at TIMESTAMP NULL,
          updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
          INDEX idx_substitutions_order (order_id)
        )`
      );
    } catch (err) {
      console.log("Migration note (substitutions):", err.message);
    }
    try {
      await conn.query(
        `ALTER TABLE substitutions ADD COLUMN quantity INT DEFAULT 1`
      );
    } catch (err) {
      if (err.code !== "ER_DUP_FIELDNAME")
        console.log("Migration note (substitutions.quantity):", err.message);
    }
  } finally {
    conn.release();
  }
}
async function ensureColumn(conn, tableName, columnName, addSql) {
  const [rows] = await conn.query(
    `
      SELECT COUNT(*) as count
      FROM information_schema.columns
      WHERE table_schema = DATABASE()
        AND table_name = ?
        AND column_name = ?
    `,
    [tableName, columnName]
  );
  const count3 = Array.isArray(rows) ? rows[0]?.count : 0;
  if (!count3) {
    await conn.query(addSql);
  }
}
async function ensureTestSchema() {
  if (useDbStubs) {
    return;
  }
  const conn = await connection.getConnection();
  try {
    await ensureColumn(
      conn,
      "users",
      "profile_image",
      "ALTER TABLE users ADD COLUMN profile_image TEXT DEFAULT NULL"
    );
    await ensureColumn(
      conn,
      "users",
      "stripe_account_id",
      "ALTER TABLE users ADD COLUMN stripe_account_id TEXT DEFAULT NULL"
    );
    await ensureColumn(
      conn,
      "users",
      "bank_account",
      "ALTER TABLE users ADD COLUMN bank_account TEXT DEFAULT NULL"
    );
  } finally {
    conn.release();
  }
}
var import_mysql2, import_promise, import_fs, import_path, import_dotenv, isTest, useDbStubs, connection, db;
var init_db = __esm({
  "server/db.ts"() {
    "use strict";
    import_mysql2 = require("drizzle-orm/mysql2");
    import_promise = __toESM(require("mysql2/promise"));
    import_fs = __toESM(require("fs"));
    import_path = __toESM(require("path"));
    import_dotenv = require("dotenv");
    (0, import_dotenv.config)({ path: ".env.local", override: true });
    (0, import_dotenv.config)({ path: ".env", override: false });
    isTest = process.env.NODE_ENV === "test";
    useDbStubs = process.env.USE_DB_STUBS === "true";
    if (isTest && useDbStubs) {
      console.log("\u{1F9EA} Test mode: using in-memory stubs for db");
      connection = {
        getConnection: async () => ({ release() {
        } })
      };
      db = {
        select: () => ({
          from: () => ({
            where: () => ({ limit: async () => [] })
          })
        }),
        insert: () => ({ values: async () => ({}) }),
        update: () => ({ set: () => ({ where: async () => ({}) }) })
      };
    } else {
      connection = import_promise.default.createPool(createConnectionConfig());
      const originalGetConnection = connection.getConnection.bind(connection);
      connection.getConnection = async () => {
        const conn = await originalGetConnection();
        await conn.query("SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci");
        return conn;
      };
      db = (0, import_mysql2.drizzle)(connection);
      if (!isTest) {
        Promise.resolve().then(() => (init_gamificationSeed(), gamificationSeed_exports)).then(({ seedGamification: seedGamification2 }) => {
          setTimeout(() => seedGamification2(), 2e3);
        }).catch(() => {
        });
        Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports)).then(async (svc) => {
          await svc.ensureOrderNumberSchema();
          await svc.backfillOrderNumbers();
        }).catch((e) => console.error("orderNumberService init:", e));
        connection.getConnection().then(async (conn) => {
          console.log("\u2705 Database connected successfully");
          conn.release();
          runStartupMigrations().catch(
            (err) => console.error("Migration error:", err.message)
          );
        }).catch((err) => {
          console.error("\u274C Database connection failed:", err.message);
        });
      }
    }
  }
});

// server/websocket.ts
var websocket_exports = {};
__export(websocket_exports, {
  getIO: () => getIO,
  initializeWebSocket: () => initializeWebSocket,
  notifyAdminFraud: () => notifyAdminFraud,
  notifyAdminNewIssue: () => notifyAdminNewIssue,
  notifyAdminNewPayout: () => notifyAdminNewPayout,
  notifyAdminNewProof: () => notifyAdminNewProof,
  notifyAdminNewTicket: () => notifyAdminNewTicket,
  notifyAdminOrderStuck: () => notifyAdminOrderStuck,
  notifyAdmins: () => notifyAdmins,
  notifyDriverAssigned: () => notifyDriverAssigned,
  notifyNewOrder: () => notifyNewOrder,
  notifyOrderStatusChange: () => notifyOrderStatusChange,
  notifyPaymentVerified: () => notifyPaymentVerified
});
function decodeSocketUser(socket) {
  const token = socket.handshake?.auth?.token || socket.handshake?.query?.token;
  if (!token || typeof token !== "string") return null;
  try {
    const decoded = import_jsonwebtoken.default.verify(
      token,
      process.env.JWT_SECRET || "comeya_local_secret_key"
    );
    if (!decoded?.id) return null;
    return { id: decoded.id, role: decoded.role || "customer" };
  } catch {
    return null;
  }
}
async function canJoinOrderRoom(user, orderId) {
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [order] = await db2.select({
      userId: orders2.userId,
      deliveryPersonId: orders2.deliveryPersonId,
      businessId: orders2.businessId
    }).from(orders2).where(eq79(orders2.id, orderId)).limit(1);
    if (!order) return false;
    if (user.role === "admin" || user.role === "super_admin") return true;
    if (order.userId === user.id || order.deliveryPersonId === user.id) {
      return true;
    }
    if (order.businessId) {
      const [biz] = await db2.select({ ownerId: businesses3.ownerId }).from(businesses3).where(eq79(businesses3.id, order.businessId)).limit(1);
      if (biz?.ownerId === user.id) return true;
    }
    return false;
  } catch {
    return false;
  }
}
function initializeWebSocket(httpServer2) {
  io = new import_socket.Server(httpServer2, {
    cors: {
      origin: process.env.FRONTEND_URL || "*",
      methods: ["GET", "POST"],
      credentials: true
    }
  });
  io.on("connection", (socket) => {
    logger.info(`\u{1F50C} WebSocket connected: ${socket.id}`);
    socket.data.user = decodeSocketUser(socket);
    socket.on("join_order", async (data) => {
      const user = socket.data.user;
      if (!user || !data?.orderId) return;
      if (await canJoinOrderRoom(user, data.orderId)) {
        socket.join(`order:${data.orderId}`);
        logger.info(`\u{1F4CD} User ${user.id} joined order:${data.orderId}`);
      }
    });
    socket.on(
      "join",
      (data) => {
        const user = socket.data.user;
        const userId = user?.id || data.userId;
        const role = user?.role || data.role;
        socket.join(`user:${userId}`);
        if (role === "business_owner" && data.businessId) {
          socket.join(`business:${data.businessId}`);
          logger.info(`\u{1F454} Business ${data.businessId} joined room`);
        }
        if (role === "delivery_driver") {
          socket.join("drivers");
          logger.info(`\u{1F697} Driver ${userId} joined drivers room`);
        }
        if ((user?.role === "admin" || user?.role === "super_admin") && user?.id) {
          socket.join("admins");
          logger.info(`\u{1F468}\u200D\u{1F4BC} Admin ${user.id} joined admins room`);
        } else if (role === "admin" || role === "super_admin") {
          logger.security("Intento de join a admins sin token v\xE1lido", {
            socketId: socket.id,
            claimedUserId: data.userId
          });
        }
      }
    );
    socket.on("disconnect", () => {
      logger.info(`\u{1F50C} WebSocket disconnected: ${socket.id}`);
    });
  });
  return io;
}
function getIO() {
  if (!io) throw new Error("WebSocket not initialized");
  return io;
}
function notifyNewOrder(businessId, order) {
  if (!io) return;
  io.to(`business:${businessId}`).emit("new_order", order);
  io.to("admins").emit("new_order", order);
  logger.info(`\u{1F4E6} New order notification sent to business ${businessId}`);
}
function notifyOrderStatusChange(userId, orderId, status) {
  if (!io) return;
  io.to(`user:${userId}`).emit("order_status_changed", { orderId, status });
}
function notifyDriverAssigned(driverId, order) {
  if (!io) return;
  io.to(`user:${driverId}`).emit("order_assigned", order);
}
function notifyPaymentVerified(businessId, orderId) {
  if (!io) return;
  io.to(`business:${businessId}`).emit("payment_verified", { orderId });
}
function notifyAdminFraud(data) {
  if (!io) return;
  io.to("admins").emit("admin_fraud_detected", {
    ...data,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  });
  logger.warn(`\u{1F6A8} Fraud alert sent to admins: ${data.reason}`);
}
function notifyAdminNewProof(data) {
  if (!io) return;
  io.to("admins").emit("admin_new_proof", {
    ...data,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  });
}
function notifyAdminNewPayout(data) {
  if (!io) return;
  io.to("admins").emit("admin_new_payout", {
    ...data,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  });
}
function notifyAdminNewTicket(data) {
  if (!io) return;
  io.to("admins").emit("admin_new_ticket", {
    ...data,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  });
}
function notifyAdminNewIssue(data) {
  if (!io) return;
  io.to("admins").emit("admin_new_issue", {
    ...data,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  });
}
function notifyAdminOrderStuck(data) {
  if (!io) return;
  io.to("admins").emit("admin_order_stuck", {
    ...data,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  });
}
function notifyAdmins(data) {
  if (!io) return;
  io.to("admins").emit("admin_notification", {
    ...data,
    timestamp: (/* @__PURE__ */ new Date()).toISOString()
  });
}
var import_socket, import_jsonwebtoken, io;
var init_websocket = __esm({
  "server/websocket.ts"() {
    "use strict";
    import_socket = require("socket.io");
    import_jsonwebtoken = __toESM(require("jsonwebtoken"));
    init_logger();
    io = null;
  }
});

// server/smsService.ts
var smsService_exports = {};
__export(smsService_exports, {
  generateVerificationCode: () => generateVerificationCode,
  sendVerificationCode: () => sendVerificationCode,
  sendVerificationSMS: () => sendVerificationSMS,
  verifyCode: () => verifyCode
});
function getTwilioClient() {
  const accountSid = process.env.TWILIO_ACCOUNT_SID;
  const authToken = process.env.TWILIO_AUTH_TOKEN;
  if (!accountSid || !authToken) {
    console.warn(
      "Twilio credentials not configured. SMS will be bypassed in development."
    );
    return null;
  }
  return (0, import_twilio.default)(accountSid, authToken);
}
function getVerifyServiceSid() {
  const serviceSid = process.env.TWILIO_VERIFY_SERVICE_SID;
  if (!serviceSid) {
    console.warn("TWILIO_VERIFY_SERVICE_SID not configured");
    return null;
  }
  return serviceSid;
}
function formatPhoneNumber(phone) {
  const cleaned = phone.replace(/\D/g, "");
  if (phone.startsWith("+")) {
    return phone.replace(/[\s-()]/g, "");
  }
  if (cleaned.startsWith("34")) {
    return `+${cleaned}`;
  }
  if (cleaned.length === 9 && (cleaned.startsWith("6") || cleaned.startsWith("7") || cleaned.startsWith("9"))) {
    return `+34${cleaned}`;
  }
  return `+34${cleaned}`;
}
function generateVerificationCode() {
  return Math.floor(1e5 + Math.random() * 9e5).toString();
}
async function sendVerificationSMS(toPhoneNumber, code) {
  try {
    const client = getTwilioClient();
    const serviceSid = getVerifyServiceSid();
    if (!client || !serviceSid) {
      console.error("Twilio no configurado. No se puede enviar SMS.");
      return false;
    }
    const formattedPhone = formatPhoneNumber(toPhoneNumber);
    const verification = await client.verify.v2.services(serviceSid).verifications.create({
      to: formattedPhone,
      channel: "sms",
      locale: "es"
    });
    console.log(
      `Twilio Verify SMS sent to ${formattedPhone}, status: ${verification.status}`
    );
    return verification.status === "pending";
  } catch (error) {
    console.error("Failed to send verification SMS:", error?.message || error);
    return false;
  }
}
async function verifyCode(toPhoneNumber, code) {
  try {
    const client = getTwilioClient();
    const serviceSid = getVerifyServiceSid();
    if (!client || !serviceSid) {
      console.log(
        `\u{1F527} Twilio not configured, rejecting code for ${toPhoneNumber}`
      );
      return false;
    }
    const formattedPhone = formatPhoneNumber(toPhoneNumber);
    const verificationCheck = await client.verify.v2.services(serviceSid).verificationChecks.create({
      to: formattedPhone,
      code
    });
    console.log(
      `Twilio Verify check for ${formattedPhone}, status: ${verificationCheck.status}`
    );
    return verificationCheck.status === "approved";
  } catch (error) {
    console.error("Failed to verify code:", error?.message || error);
    return false;
  }
}
var import_twilio, sendVerificationCode;
var init_smsService = __esm({
  "server/smsService.ts"() {
    "use strict";
    import_twilio = __toESM(require("twilio"));
    sendVerificationCode = sendVerificationSMS;
  }
});

// server/loyaltyService.ts
var loyaltyService_exports = {};
__export(loyaltyService_exports, {
  LoyaltyService: () => LoyaltyService
});
var import_drizzle_orm4, LoyaltyService;
var init_loyaltyService = __esm({
  "server/loyaltyService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm4 = require("drizzle-orm");
    LoyaltyService = class {
      /**
       * Obtener o crear puntos de lealtad para un usuario
       */
      static async getOrCreateLoyaltyPoints(userId) {
        try {
          const [existing] = await db.select().from(loyaltyPoints).where((0, import_drizzle_orm4.eq)(loyaltyPoints.userId, userId)).limit(1);
          if (existing) return existing;
          const newPoints = {
            id: crypto.randomUUID(),
            userId,
            currentPoints: 0,
            totalEarned: 0,
            totalRedeemed: 0,
            tier: "bronze",
            pointsToNextTier: 1e3
          };
          try {
            await db.insert(loyaltyPoints).values(newPoints);
            return newPoints;
          } catch (insertError) {
            if (insertError?.errno === 1062 || insertError?.code === "ER_DUP_ENTRY") {
              const [existing2] = await db.select().from(loyaltyPoints).where((0, import_drizzle_orm4.eq)(loyaltyPoints.userId, userId)).limit(1);
              if (existing2) return existing2;
            }
            throw insertError;
          }
        } catch (error) {
          console.error("Error getting/creating loyalty points:", error);
          throw error;
        }
      }
      /**
       * Agregar puntos por pedido completado
       */
      static async awardPointsForOrder(userId, orderId, orderTotal) {
        try {
          const points = Math.floor(orderTotal / 100);
          if (points <= 0) return;
          await this.addPoints(
            userId,
            points,
            "earned",
            `Pedido completado #${orderId.slice(-6)}`,
            orderId
          );
          await this.updateChallengeProgress(userId, "orders", 1);
          await this.updateChallengeProgress(userId, "spending", orderTotal);
          try {
            const { ReferralService: ReferralService2 } = await Promise.resolve().then(() => (init_referralService(), referralService_exports));
            await ReferralService2.processReferralRewardForOrder(userId, orderId);
          } catch (referralError) {
            console.error("Error processing referral reward:", referralError);
          }
        } catch (error) {
          console.error("Error awarding points for order:", error);
          throw error;
        }
      }
      /**
       * Agregar puntos
       */
      static async addPoints(userId, points, type, description, orderId, rewardId) {
        try {
          const userPoints = await this.getOrCreateLoyaltyPoints(userId);
          const newCurrent = userPoints.currentPoints + points;
          const newTotal = userPoints.totalEarned + points;
          await db.update(loyaltyPoints).set({
            currentPoints: newCurrent,
            totalEarned: newTotal
          }).where((0, import_drizzle_orm4.eq)(loyaltyPoints.userId, userId));
          await db.insert(loyaltyTransactions).values({
            id: crypto.randomUUID(),
            userId,
            type,
            points,
            description,
            orderId,
            rewardId
          });
          await this.updateTier(userId, newTotal);
          return newCurrent;
        } catch (error) {
          console.error("Error adding points:", error);
          throw error;
        }
      }
      /**
       * Actualizar tier basado en puntos totales
       */
      static async updateTier(userId, totalEarned) {
        try {
          let newTier = "bronze";
          let pointsToNext = 1e3;
          if (totalEarned >= 1e4) {
            newTier = "diamond";
            pointsToNext = 0;
          } else if (totalEarned >= 5e3) {
            newTier = "platinum";
            pointsToNext = 1e4 - totalEarned;
          } else if (totalEarned >= 2500) {
            newTier = "gold";
            pointsToNext = 5e3 - totalEarned;
          } else if (totalEarned >= 1e3) {
            newTier = "silver";
            pointsToNext = 2500 - totalEarned;
          } else {
            pointsToNext = 1e3 - totalEarned;
          }
          await db.update(loyaltyPoints).set({
            tier: newTier,
            tierUpdatedAt: /* @__PURE__ */ new Date(),
            pointsToNextTier: pointsToNext
          }).where((0, import_drizzle_orm4.eq)(loyaltyPoints.userId, userId));
        } catch (error) {
          console.error("Error updating tier:", error);
          throw error;
        }
      }
      /**
       * Canjear recompensa
       */
      static async redeemReward(userId, rewardId) {
        try {
          const [reward] = await db.select().from(loyaltyRewards).where((0, import_drizzle_orm4.eq)(loyaltyRewards.id, rewardId)).limit(1);
          if (!reward || !reward.isAvailable) {
            throw new Error("Recompensa no disponible");
          }
          const userPoints = await this.getOrCreateLoyaltyPoints(userId);
          if (userPoints.currentPoints < reward.pointsCost) {
            throw new Error("Puntos insuficientes");
          }
          if (reward.minTier) {
            const tiers = ["bronze", "silver", "gold", "platinum", "diamond"];
            const userTierIndex = tiers.indexOf(userPoints.tier);
            const minTierIndex = tiers.indexOf(reward.minTier);
            if (userTierIndex < minTierIndex) {
              throw new Error(`Requiere tier ${reward.minTier} o superior`);
            }
          }
          if (reward.maxRedemptions && reward.currentRedemptions >= reward.maxRedemptions) {
            throw new Error("L\xEDmite de canjes alcanzado");
          }
          await db.update(loyaltyPoints).set({
            currentPoints: userPoints.currentPoints - reward.pointsCost,
            totalRedeemed: userPoints.totalRedeemed + reward.pointsCost
          }).where((0, import_drizzle_orm4.eq)(loyaltyPoints.userId, userId));
          await db.insert(loyaltyTransactions).values({
            id: crypto.randomUUID(),
            userId,
            type: "redeemed",
            points: -reward.pointsCost,
            description: `Canjeado: ${reward.title}`,
            rewardId
          });
          const redemptionId = crypto.randomUUID();
          const expiresAt = reward.expiresAt || new Date(Date.now() + 30 * 24 * 60 * 60 * 1e3);
          await db.insert(loyaltyRedemptions).values({
            id: redemptionId,
            userId,
            rewardId,
            pointsSpent: reward.pointsCost,
            status: "active",
            expiresAt
          });
          await db.update(loyaltyRewards).set({ currentRedemptions: reward.currentRedemptions + 1 }).where((0, import_drizzle_orm4.eq)(loyaltyRewards.id, rewardId));
          return { redemptionId, reward };
        } catch (error) {
          console.error("Error redeeming reward:", error);
          throw error;
        }
      }
      /**
       * Obtener historial de transacciones
       */
      static async getTransactionHistory(userId, limit = 50) {
        try {
          return await db.select().from(loyaltyTransactions).where((0, import_drizzle_orm4.eq)(loyaltyTransactions.userId, userId)).orderBy((0, import_drizzle_orm4.desc)(loyaltyTransactions.createdAt)).limit(limit);
        } catch (error) {
          console.error("Error getting transaction history:", error);
          return [];
        }
      }
      /**
       * Obtener recompensas disponibles para un usuario
       */
      static async getAvailableRewards(userId) {
        try {
          const userPoints = await this.getOrCreateLoyaltyPoints(userId);
          const allRewards = await db.select().from(loyaltyRewards).where((0, import_drizzle_orm4.eq)(loyaltyRewards.isAvailable, true));
          return allRewards.filter((reward) => {
            if (reward.minTier) {
              const tiers = ["bronze", "silver", "gold", "platinum", "diamond"];
              const userTierIndex = tiers.indexOf(userPoints.tier);
              const minTierIndex = tiers.indexOf(reward.minTier);
              if (userTierIndex < minTierIndex) return false;
            }
            if (reward.maxRedemptions && reward.currentRedemptions >= reward.maxRedemptions) {
              return false;
            }
            return true;
          });
        } catch (error) {
          console.error("Error getting available rewards:", error);
          return [];
        }
      }
      /**
       * Actualizar progreso de challenges
       */
      static async updateChallengeProgress(userId, type, increment) {
        try {
          const activeChallenges = await db.select().from(loyaltyChallenges).where(
            (0, import_drizzle_orm4.and)(
              (0, import_drizzle_orm4.eq)(loyaltyChallenges.isActive, true),
              (0, import_drizzle_orm4.eq)(loyaltyChallenges.type, type)
            )
          );
          for (const challenge of activeChallenges) {
            if (challenge.expiresAt && new Date(challenge.expiresAt) < /* @__PURE__ */ new Date()) {
              continue;
            }
            const [progress] = await db.select().from(loyaltyChallengeProgress).where(
              (0, import_drizzle_orm4.and)(
                (0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.userId, userId),
                (0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.challengeId, challenge.id)
              )
            ).limit(1);
            if (progress) {
              if (!progress.completed) {
                const newProgress = Math.min(
                  progress.progress + increment,
                  challenge.target
                );
                const completed = newProgress >= challenge.target;
                await db.update(loyaltyChallengeProgress).set({
                  progress: newProgress,
                  completed,
                  completedAt: completed ? /* @__PURE__ */ new Date() : null
                }).where((0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.id, progress.id));
              }
            } else {
              const newProgress = Math.min(increment, challenge.target);
              const completed = newProgress >= challenge.target;
              await db.insert(loyaltyChallengeProgress).values({
                id: crypto.randomUUID(),
                userId,
                challengeId: challenge.id,
                progress: newProgress,
                completed,
                completedAt: completed ? /* @__PURE__ */ new Date() : null
              });
            }
          }
        } catch (error) {
          console.error("Error updating challenge progress:", error);
        }
      }
      /**
       * Reclamar recompensa de challenge completado
       */
      static async claimChallengeReward(userId, challengeId) {
        try {
          const [progress] = await db.select().from(loyaltyChallengeProgress).where(
            (0, import_drizzle_orm4.and)(
              (0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.userId, userId),
              (0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.challengeId, challengeId)
            )
          ).limit(1);
          if (!progress || !progress.completed || progress.claimed) {
            throw new Error("Challenge no completado o ya reclamado");
          }
          const [challenge] = await db.select().from(loyaltyChallenges).where((0, import_drizzle_orm4.eq)(loyaltyChallenges.id, challengeId)).limit(1);
          if (!challenge) {
            throw new Error("Challenge no encontrado");
          }
          await db.update(loyaltyChallengeProgress).set({
            claimed: true,
            claimedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.id, progress.id));
          await this.addPoints(
            userId,
            challenge.rewardPoints,
            "bonus",
            `Challenge completado: ${challenge.title}`
          );
          return challenge.rewardPoints;
        } catch (error) {
          console.error("Error claiming challenge reward:", error);
          throw error;
        }
      }
      /**
       * Obtener challenges del usuario con progreso
       */
      static async getUserChallenges(userId) {
        try {
          const activeChallenges = await db.select().from(loyaltyChallenges).where((0, import_drizzle_orm4.eq)(loyaltyChallenges.isActive, true));
          const result = [];
          for (const challenge of activeChallenges) {
            const [progress] = await db.select().from(loyaltyChallengeProgress).where(
              (0, import_drizzle_orm4.and)(
                (0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.userId, userId),
                (0, import_drizzle_orm4.eq)(loyaltyChallengeProgress.challengeId, challenge.id)
              )
            ).limit(1);
            result.push({
              ...challenge,
              progress: progress?.progress || 0,
              completed: progress?.completed || false,
              claimed: progress?.claimed || false
            });
          }
          return result;
        } catch (error) {
          console.error("Error getting user challenges:", error);
          return [];
        }
      }
    };
  }
});

// server/enhancedPushService.ts
var enhancedPushService_exports = {};
__export(enhancedPushService_exports, {
  notifyDriverNewOrder: () => notifyDriverNewOrder,
  notifyPagoMovilStatus: () => notifyPagoMovilStatus,
  sendOrderStatusNotification: () => sendOrderStatusNotification,
  sendPushNotification: () => sendPushNotification,
  sendPushToUser: () => sendPushToUser
});
async function sendOrderStatusNotification(orderId, userId, newStatus) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm5.eq)(orders.id, orderId)).limit(1);
  if (!order) return;
  const [user] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, userId)).limit(1);
  if (!user || !user.pushToken) return;
  let notification = null;
  switch (newStatus) {
    case "accepted":
      notification = {
        title: "\xA1Pedido aceptado! \u{1F389}",
        // estimatedPrepMinutes es la columna real del schema (minutos estimados)
        body: `${order.businessName} acept\xF3 tu pedido - Listo en ${order.estimatedPrepMinutes || 25} min`,
        data: { orderId, screen: "OrderTracking" }
      };
      break;
    case "preparing":
      notification = {
        title: "Preparando tu pedido \u{1F468}\u200D\u{1F373}",
        body: `${order.businessName} est\xE1 preparando tu pedido`,
        data: { orderId, screen: "OrderTracking" }
      };
      break;
    case "ready":
      if (order.orderType === "pickup") {
        notification = {
          title: "YA PUEDES RECOGER TU PEDIDO",
          body: `${order.businessName} te espera \u2014 muestra tu c\xF3digo QR al llegar`,
          data: { orderId, screen: "OrderTracking" }
        };
      } else {
        notification = {
          title: "Tu pedido est\xE1 listo \u{1F4E6}",
          body: "Esperando a que un repartidor lo recoja",
          data: { orderId, screen: "OrderTracking" }
        };
      }
      break;
    case "assigned_driver":
      if (order.deliveryPersonId) {
        const [driver] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, order.deliveryPersonId)).limit(1);
        const driverName = driver?.name?.split(" ")[0] || "Tu repartidor";
        notification = {
          title: `${driverName} fue asignado \u{1F697}`,
          body: "Pronto recoger\xE1 tu pedido",
          data: { orderId, screen: "OrderTracking" }
        };
      }
      break;
    case "picked_up":
      if (order.deliveryPersonId) {
        const [driver] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, order.deliveryPersonId)).limit(1);
        const driverName = driver?.name?.split(" ")[0] || "Tu repartidor";
        const eta = order.estimatedDeliveryTime || 15;
        notification = {
          title: `${driverName} recogi\xF3 tu pedido \u{1F4E6}`,
          body: `Llega en ${eta} min`,
          data: { orderId, screen: "OrderTracking" }
        };
      }
      break;
    case "on_the_way":
    case "in_transit":
      if (order.deliveryPersonId) {
        const [driver] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, order.deliveryPersonId)).limit(1);
        const driverName = driver?.name?.split(" ")[0] || "Tu repartidor";
        notification = {
          title: `${driverName} va en camino \u{1F697}`,
          body: "Tu pedido est\xE1 de camino a tu puerta",
          data: { orderId, screen: "OrderTracking" }
        };
      }
      break;
    case "arriving":
      if (order.deliveryPersonId) {
        const [driver] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, order.deliveryPersonId)).limit(1);
        const driverName = driver?.name?.split(" ")[0] || "Tu repartidor";
        notification = {
          title: `${driverName} est\xE1 cerca \u26A1`,
          body: "Llega en 2 minutos",
          data: { orderId, screen: "OrderTracking" }
        };
      }
      break;
    case "delivered":
      notification = {
        title: "\xA1Pedido entregado! \u{1F389}",
        body: "\xA1Disfruta tu comida! Confirma la entrega para liberar el pago",
        data: { orderId, screen: "DeliveryConfirmation" }
      };
      break;
    case "cancelled":
      notification = {
        title: "Pedido cancelado",
        body: "Tu pedido ha sido cancelado",
        data: { orderId, screen: "OrderDetails" }
      };
      break;
  }
  if (notification) {
    await sendPushNotification(user.pushToken, notification);
  }
}
async function sendPushNotification(pushToken, payload) {
  try {
    const message = {
      to: pushToken,
      sound: "default",
      title: payload.title,
      body: payload.body,
      data: payload.data || {}
    };
    const response = await fetch("https://exp.host/--/api/v2/push/send", {
      method: "POST",
      headers: {
        Accept: "application/json",
        "Content-Type": "application/json"
      },
      body: JSON.stringify(message)
    });
    if (response.ok) {
      try {
        const result = await response.json();
        const status = result?.data?.status;
        if (status && status !== "ok") {
          const reason = result?.data?.details?.error;
          if (status === "error" && reason === "DeviceNotRegistered") {
            await db.update(users).set({ pushToken: null }).where((0, import_drizzle_orm5.eq)(users.pushToken, pushToken));
            console.log(`\u{1F5D1}\uFE0F Push token inv\xE1lido eliminado (${reason})`);
          } else if (reason === "InvalidCredentials") {
            console.error(
              "\u{1F6A8} Push InvalidCredentials: revisa las credenciales push (Apple Push Key / FCM v1) del proyecto en expo.dev",
              JSON.stringify(result?.data?.details ?? {})
            );
          } else {
            console.error(`Push error status=${status}:`, JSON.stringify(result?.data?.details ?? {}));
          }
        }
      } catch {
      }
    } else {
      console.error(`Expo push HTTP ${response.status}`);
    }
    console.log(`\u{1F4F1} Push notification sent: ${payload.title}`);
  } catch (error) {
    console.error("Error sending push notification:", error);
  }
}
async function notifyPagoMovilStatus(userId, status, orderId, reason) {
  const [user] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, userId)).limit(1);
  if (!user || !user.pushToken) return;
  const notification = status === "verified" ? {
    title: "\u2705 Pago verificado",
    body: "Tu pago fue confirmado. Tu pedido est\xE1 siendo procesado.",
    data: { orderId, screen: "OrderTracking" }
  } : {
    title: "\u274C Pago rechazado",
    body: reason || "Tu comprobante fue rechazado. Por favor verifica los datos.",
    data: { orderId, screen: "PaymentProofUpload" }
  };
  await sendPushNotification(user.pushToken, notification);
}
function parseNotificationPreferences(raw) {
  if (!raw) return DEFAULT_NOTIFICATION_PREFERENCES;
  try {
    const parsed = JSON.parse(raw);
    return {
      promotions: typeof parsed.promotions === "boolean" ? parsed.promotions : DEFAULT_NOTIFICATION_PREFERENCES.promotions,
      news: typeof parsed.news === "boolean" ? parsed.news : DEFAULT_NOTIFICATION_PREFERENCES.news
    };
  } catch {
    return DEFAULT_NOTIFICATION_PREFERENCES;
  }
}
async function sendPushToUser(userId, payload) {
  const [user] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, userId)).limit(1);
  if (!user?.pushToken) return;
  if (payload.category === "promotions" || payload.category === "news") {
    const prefs = parseNotificationPreferences(user.notificationPreferences);
    if (!prefs[payload.category]) return;
  }
  await sendPushNotification(user.pushToken, payload);
}
async function notifyDriverNewOrder(driverId, orderId) {
  const [driver] = await db.select().from(users).where((0, import_drizzle_orm5.eq)(users.id, driverId)).limit(1);
  if (!driver || !driver.pushToken) return;
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm5.eq)(orders.id, orderId)).limit(1);
  if (!order) return;
  const earning = Math.round(order.total * 0.15 / 100);
  await sendPushNotification(driver.pushToken, {
    title: "Nuevo pedido disponible \u{1F4E6}",
    body: `${order.businessName} - Gana $${earning}`,
    data: { orderId, screen: "DriverAvailable" }
  });
}
var import_drizzle_orm5, DEFAULT_NOTIFICATION_PREFERENCES;
var init_enhancedPushService = __esm({
  "server/enhancedPushService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm5 = require("drizzle-orm");
    DEFAULT_NOTIFICATION_PREFERENCES = {
      promotions: true,
      news: true
    };
  }
});

// server/referralService.ts
var referralService_exports = {};
__export(referralService_exports, {
  REFERRAL_REWARD_POINTS: () => REFERRAL_REWARD_POINTS,
  ReferralService: () => ReferralService
});
function generateCode() {
  let suffix = "";
  for (let i = 0; i < 6; i++) {
    suffix += CODE_ALPHABET[Math.floor(Math.random() * CODE_ALPHABET.length)];
  }
  return `CY${suffix}`;
}
var import_drizzle_orm6, REFERRAL_REWARD_POINTS, CODE_ALPHABET, ReferralService;
var init_referralService = __esm({
  "server/referralService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm6 = require("drizzle-orm");
    REFERRAL_REWARD_POINTS = 500;
    CODE_ALPHABET = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
    ReferralService = class {
      /**
       * Obtener el código de referido del usuario, generándolo si no existe.
       */
      static async getOrCreateReferralCode(userId) {
        const [user] = await db.select({ referralCode: users.referralCode }).from(users).where((0, import_drizzle_orm6.eq)(users.id, userId)).limit(1);
        if (!user) throw new Error("Usuario no encontrado");
        if (user.referralCode) return user.referralCode;
        for (let attempt = 0; attempt < 5; attempt++) {
          const code = generateCode();
          try {
            await db.update(users).set({ referralCode: code }).where((0, import_drizzle_orm6.eq)(users.id, userId));
            return code;
          } catch (error) {
            if (error?.errno === 1062 || error?.code === "ER_DUP_ENTRY") continue;
            throw error;
          }
        }
        throw new Error("No se pudo generar un c\xF3digo de referido \xFAnico");
      }
      /**
       * Validar un código de referido y devolver el id del invitador.
       * Devuelve null si el código no existe o coincide con el propio usuario.
       */
      static async resolveReferralCode(code, selfUserId) {
        if (!code) return null;
        const [inviter] = await db.select({ id: users.id }).from(users).where((0, import_drizzle_orm6.eq)(users.referralCode, code.toUpperCase())).limit(1);
        if (!inviter) return null;
        if (selfUserId && inviter.id === selfUserId) return null;
        return inviter.id;
      }
      /**
       * Otorgar la recompensa al invitador cuando su invitado completa su primer
       * pedido. Idempotente: una vez recompensado, no vuelve a otorgar.
       */
      static async processReferralRewardForOrder(buyerId, orderId) {
        try {
          const [buyer] = await db.select({
            referredBy: users.referredBy,
            referralRewardedAt: users.referralRewardedAt
          }).from(users).where((0, import_drizzle_orm6.eq)(users.id, buyerId)).limit(1);
          if (!buyer?.referredBy || buyer.referralRewardedAt) return;
          const { loyaltyTransactions: loyaltyTransactions2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          const [existingReward] = await db.select({ id: loyaltyTransactions2.id }).from(loyaltyTransactions2).where(
            (0, import_drizzle_orm6.and)(
              (0, import_drizzle_orm6.eq)(loyaltyTransactions2.orderId, orderId),
              (0, import_drizzle_orm6.eq)(loyaltyTransactions2.type, "referral")
            )
          ).limit(1);
          if (existingReward) return;
          await db.update(users).set({ referralRewardedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm6.and)((0, import_drizzle_orm6.eq)(users.id, buyerId), (0, import_drizzle_orm6.isNull)(users.referralRewardedAt)));
          const { LoyaltyService: LoyaltyService2 } = await Promise.resolve().then(() => (init_loyaltyService(), loyaltyService_exports));
          await LoyaltyService2.addPoints(
            buyer.referredBy,
            REFERRAL_REWARD_POINTS,
            "referral",
            `Recompensa por invitar a un amigo (pedido #${orderId.slice(-6)})`,
            orderId
          );
          try {
            const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
            await sendPushToUser2(buyer.referredBy, {
              title: "\u{1F389} \xA1Invitaci\xF3n exitosa!",
              body: `Tu invitado complet\xF3 su primer pedido. Has ganado ${REFERRAL_REWARD_POINTS} puntos.`,
              data: { screen: "Gamification" },
              category: "orders"
            });
          } catch (pushError) {
            console.error("Error sending referral push:", pushError);
          }
        } catch (error) {
          console.error("Error processing referral reward:", error);
        }
      }
      /**
       * Resumen de referidos para la pantalla "Invita y Gana".
       */
      static async getReferralSummary(userId) {
        const referralCode = await this.getOrCreateReferralCode(userId);
        const invited = await db.select({
          id: users.id,
          name: users.name,
          rewarded: users.referralRewardedAt
        }).from(users).where((0, import_drizzle_orm6.and)((0, import_drizzle_orm6.eq)(users.referredBy, userId), (0, import_drizzle_orm6.ne)(users.id, userId)));
        const completed = invited.filter((u) => !!u.rewarded).length;
        return {
          referralCode,
          shareLink: `https://app.comeya.es?ref=${referralCode}`,
          rewardPoints: REFERRAL_REWARD_POINTS,
          invitedCount: invited.length,
          completedCount: completed
        };
      }
    };
  }
});

// server/config.ts
var config_exports = {};
__export(config_exports, {
  CONFIG: () => CONFIG,
  invalidateSettingsCache: () => invalidateSettingsCache
});
async function getSettings() {
  if (Date.now() < cacheExpiry && Object.keys(cache).length > 0) return cache;
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { systemSettings: systemSettings2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const rows = await db2.select().from(systemSettings2);
    cache = {};
    for (const row of rows) cache[row.key] = row.value;
    cacheExpiry = Date.now() + CACHE_TTL;
  } catch {
  }
  return cache;
}
async function get(key, def) {
  const s = await getSettings();
  return s[key] ?? def;
}
async function getNum(key, def) {
  const v = await get(key, String(def));
  const n = parseFloat(v);
  return isNaN(n) ? def : n;
}
function invalidateSettingsCache() {
  cache = {};
  cacheExpiry = 0;
}
var cache, cacheExpiry, CACHE_TTL, CONFIG;
var init_config = __esm({
  "server/config.ts"() {
    "use strict";
    cache = {};
    cacheExpiry = 0;
    CACHE_TTL = 60 * 1e3;
    CONFIG = {
      // Comisiones — key existente en BD: "comeya_commission_pct" o "nemy_commission"
      async commission() {
        return await getNum("comeya_commission_pct", 15) / 100;
      },
      async commissionDivisor() {
        return 1 + await CONFIG.commission();
      },
      // Delivery
      async deliveryFee() {
        return getNum("default_delivery_fee_cents", 300);
      },
      async deliveryTime() {
        return get("default_delivery_time", "30-45 min");
      },
      // Datos de pago ComeYa — editables por el admin desde el panel
      async bizumPhone() {
        return get("comeya_bizum_phone", "600 000 000");
      },
      async iban() {
        return get("comeya_iban", "ES00 0000 0000 0000 0000 0000");
      },
      async paypalEmail() {
        return get("comeya_paypal_email", "pagos@comeya.es");
      },
      async titular() {
        return get("comeya_titular", "ComeYa S.L.");
      },
      async banco() {
        return get("comeya_banco", "Banco Santander");
      }
    };
  }
});

// server/errors.ts
function asyncHandler(fn) {
  return (req, res, next) => {
    Promise.resolve(fn(req, res, next)).catch(next);
  };
}
var AppError, ValidationError, AuthorizationError, NotFoundError, RateLimitError;
var init_errors = __esm({
  "server/errors.ts"() {
    "use strict";
    init_logger();
    init_env();
    AppError = class _AppError extends Error {
      constructor(statusCode, message, isOperational = true) {
        super(message);
        this.statusCode = statusCode;
        this.message = message;
        this.isOperational = isOperational;
        Object.setPrototypeOf(this, _AppError.prototype);
        Error.captureStackTrace(this, this.constructor);
      }
    };
    ValidationError = class extends AppError {
      constructor(message) {
        super(400, message);
      }
    };
    AuthorizationError = class extends AppError {
      constructor(message = "Insufficient permissions") {
        super(403, message);
      }
    };
    NotFoundError = class extends AppError {
      constructor(resource = "Resource") {
        super(404, `${resource} not found`);
      }
    };
    RateLimitError = class extends AppError {
      constructor(message = "Too many requests") {
        super(429, message);
      }
    };
  }
});

// server/utils/paymentState.ts
var paymentState_exports = {};
__export(paymentState_exports, {
  acceptanceBlockedMessage: () => acceptanceBlockedMessage,
  classifyPaymentState: () => classifyPaymentState,
  paymentStateOf: () => paymentStateOf,
  pendingProofOrderIds: () => pendingProofOrderIds
});
function classifyPaymentState(order, pendingProofOrderIds2) {
  const status = String(order?.status ?? "");
  if (status === "payment_failed") return "failed";
  if (status !== "pending") return "paid";
  const method = String(order?.paymentMethod ?? "").toLowerCase();
  if (method === "cash" || method === "efectivo") return "paid";
  if (pendingProofOrderIds2.has(order.id)) return "proof_pending";
  return "awaiting_payment";
}
async function pendingProofOrderIds(orderIds) {
  if (!orderIds.length) return /* @__PURE__ */ new Set();
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { paymentProofs: paymentProofs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { and: and47, eq: eq79, inArray: inArray12 } = await import("drizzle-orm");
    const rows = await db2.select({ orderId: paymentProofs2.orderId }).from(paymentProofs2).where(
      and47(
        eq79(paymentProofs2.status, "pending"),
        inArray12(paymentProofs2.orderId, orderIds)
      )
    );
    return new Set(rows.map((r) => r.orderId).filter(Boolean));
  } catch {
    return /* @__PURE__ */ new Set();
  }
}
async function paymentStateOf(order) {
  if (order?.status !== "pending") return classifyPaymentState(order, /* @__PURE__ */ new Set());
  return classifyPaymentState(order, await pendingProofOrderIds([order.id]));
}
function acceptanceBlockedMessage(state) {
  if (state === "proof_pending") {
    return "El pago de este pedido est\xE1 en verificaci\xF3n por administraci\xF3n. Se activar\xE1 autom\xE1ticamente al aprobarse el comprobante.";
  }
  if (state === "awaiting_payment") {
    return "El cliente a\xFAn no ha completado el pago. El pedido se activar\xE1 autom\xE1ticamente al confirmarse.";
  }
  return null;
}
var init_paymentState = __esm({
  "server/utils/paymentState.ts"() {
    "use strict";
  }
});

// server/paymentConfirmationService.ts
var paymentConfirmationService_exports = {};
__export(paymentConfirmationService_exports, {
  confirmPaidOrder: () => confirmPaidOrder,
  rescueStripePayment: () => rescueStripePayment
});
async function confirmPaidOrder(orderId, paymentIntent, source = "webhook") {
  const [existingOrder] = await db.select().from(orders).where((0, import_drizzle_orm9.eq)(orders.id, orderId)).limit(1);
  if (!existingOrder) {
    return { success: false, message: `Order not found: ${orderId}` };
  }
  const { and: and47 } = await import("drizzle-orm");
  const existingTx = await db.select({ id: transactions.id }).from(transactions).where(
    and47((0, import_drizzle_orm9.eq)(transactions.orderId, orderId), (0, import_drizzle_orm9.eq)(transactions.type, "payment"))
  ).limit(1);
  if (existingOrder.status !== "pending" && existingOrder.paidAt && existingTx.length) {
    return { success: true, message: "already-confirmed" };
  }
  const wasPending = existingOrder.status === "pending";
  if (wasPending) {
    await db.update(orders).set({
      status: "accepted",
      paidAt: /* @__PURE__ */ new Date(),
      stripePaymentIntentId: paymentIntent.id,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm9.eq)(orders.id, orderId));
  }
  if (!existingTx.length) {
    await db.insert(transactions).values({
      id: `txn_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,
      orderId,
      businessId: existingOrder.businessId,
      userId: existingOrder.userId,
      amount: paymentIntent.amount,
      type: "payment",
      status: "completed",
      metadata: JSON.stringify({
        paymentIntentId: paymentIntent.id,
        paymentMethod: paymentIntent.payment_method ?? null,
        currency: paymentIntent.currency ?? "eur",
        confirmedVia: source
      }),
      createdAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    });
  }
  if (wasPending) try {
    const [biz] = await db.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm9.eq)(businesses.id, existingOrder.businessId)).limit(1);
    if (biz?.ownerId) {
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      const { orderRef: orderRef2 } = await Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports));
      await sendPushToUser2(biz.ownerId, {
        title: "\u{1F4B3} Pago recibido \u2014 nuevo pedido",
        body: `${existingOrder.businessName} recibi\xF3 el pedido ${orderRef2(existingOrder)}. Rev\xEDsalo y ponlo en preparaci\xF3n.`,
        data: { orderId, screen: "BusinessOrders" }
      });
    }
    const { notifyNewOrder: notifyNewOrder2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
    notifyNewOrder2(existingOrder.businessId, existingOrder);
  } catch (notifyError) {
    console.error("confirmPaidOrder: failed to notify business", notifyError);
  }
  return { success: true, message: "confirmed" };
}
async function rescueStripePayment(order) {
  const method = String(order?.paymentMethod || "");
  const intentId = order?.stripePaymentIntentId;
  if (!method.startsWith("stripe_") || !intentId) return false;
  if (!process.env.STRIPE_SECRET_KEY) return false;
  try {
    const Stripe3 = (await import("stripe")).default;
    const stripe2 = new Stripe3(process.env.STRIPE_SECRET_KEY);
    const intent = await stripe2.paymentIntents.retrieve(intentId);
    if (intent.status === "succeeded") {
      const result = await confirmPaidOrder(order.id, intent, "reconciliation");
      if (result.success) {
        const { orderRef: orderRef2 } = await Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports));
        console.log(
          `\u{1F527} ${orderRef2(order)} rescatado: pago Stripe confirmado sin webhook`
        );
        return true;
      }
    }
  } catch (error) {
    console.error(
      `rescueStripePayment ${order?.id}:`,
      error?.message ?? error
    );
  }
  return false;
}
var import_drizzle_orm9;
var init_paymentConfirmationService = __esm({
  "server/paymentConfirmationService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm9 = require("drizzle-orm");
  }
});

// server/utils/substitution.ts
var substitution_exports = {};
__export(substitution_exports, {
  collectSubstituteIds: () => collectSubstituteIds
});
function collectSubstituteIds(rows) {
  const ids = /* @__PURE__ */ new Set();
  for (const row of rows) {
    const raw = row?.substitute_product_ids;
    if (!raw) continue;
    let parsed;
    try {
      parsed = JSON.parse(raw);
    } catch {
      continue;
    }
    if (Array.isArray(parsed)) {
      parsed.forEach((id) => {
        if (id) ids.add(String(id));
      });
    } else if (parsed && typeof parsed === "object") {
      Object.values(parsed).forEach((id) => {
        if (id) ids.add(String(id));
      });
    }
  }
  return [...ids];
}
var init_substitution = __esm({
  "server/utils/substitution.ts"() {
    "use strict";
  }
});

// server/businessHoursService.ts
var businessHoursService_exports = {};
__export(businessHoursService_exports, {
  BusinessHoursService: () => BusinessHoursService
});
function getZonedNow() {
  const timezone = process.env.BUSINESS_TIMEZONE || "Europe/Madrid";
  try {
    const parts = new Intl.DateTimeFormat("en-US", {
      timeZone: timezone,
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit",
      hour12: false
    }).formatToParts(/* @__PURE__ */ new Date());
    const get2 = (type) => parseInt(parts.find((p) => p.type === type)?.value || "0", 10);
    const year = get2("year");
    const month = get2("month") - 1;
    const day = get2("day");
    const hour = get2("hour");
    const minute = get2("minute");
    const second = get2("second");
    return new Date(year, month, day, hour, minute, second);
  } catch {
    return /* @__PURE__ */ new Date();
  }
}
function normalizeDayName(value) {
  return (value || "").trim().toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "");
}
function resolveTodaySchedule(hours, dayOfWeek) {
  if (!hours) return null;
  if (Array.isArray(hours)) {
    const byIndex = hours[dayOfWeek];
    if (byIndex && !isDayClosed(byIndex) && (byIndex.openTime || byIndex.open) && (byIndex.closeTime || byIndex.close)) {
      return byIndex;
    }
    const todayNames2 = [
      ["domingo", "sunday"],
      ["lunes", "monday"],
      ["martes", "tuesday"],
      ["miercoles", "wednesday"],
      ["jueves", "thursday"],
      ["viernes", "friday"],
      ["sabado", "saturday"]
    ][dayOfWeek];
    const byName = hours.find((entry) => {
      const normalized = normalizeDayName(entry?.day);
      return todayNames2.some((n) => normalizeDayName(n) === normalized);
    });
    return byName || null;
  }
  const byKey = hours[dayOfWeek] || hours[String(dayOfWeek)];
  if (byKey && !isDayClosed(byKey) && (byKey.openTime || byKey.open) && (byKey.closeTime || byKey.close)) {
    return byKey;
  }
  const todayNames = [
    ["domingo", "sunday"],
    ["lunes", "monday"],
    ["martes", "tuesday"],
    ["miercoles", "wednesday"],
    ["jueves", "thursday"],
    ["viernes", "friday"],
    ["sabado", "saturday"]
  ][dayOfWeek];
  const dayNameKeys = Object.keys(hours);
  const namedKey = dayNameKeys.find(
    (key) => todayNames.some((n) => normalizeDayName(key) === normalizeDayName(n))
  );
  return namedKey ? hours[namedKey] : null;
}
function isDayClosed(schedule) {
  if (schedule.closed === true) return true;
  if (schedule.isOpen === false) return true;
  return false;
}
function parseTimeToMinutes(timeValue) {
  if (!timeValue || typeof timeValue !== "string") return null;
  const [hoursRaw, minutesRaw] = timeValue.split(":");
  const hours = Number(hoursRaw);
  const minutes = Number(minutesRaw);
  if (!Number.isFinite(hours) || !Number.isFinite(minutes)) return null;
  return hours * 60 + minutes;
}
var import_drizzle_orm10, BusinessHoursService;
var init_businessHoursService = __esm({
  "server/businessHoursService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm10 = require("drizzle-orm");
    BusinessHoursService = class {
      // Check if business should be open based on current time
      static async isBusinessOpen(businessId) {
        const [business] = await db.select().from(businesses).where((0, import_drizzle_orm10.eq)(businesses.id, businessId)).limit(1);
        if (!business || !business.openingHours) return true;
        try {
          const hours = JSON.parse(business.openingHours);
          const now = getZonedNow();
          const dayOfWeek = now.getDay();
          const currentHour = now.getHours();
          const currentMinute = now.getMinutes();
          const currentTimeInMinutes = currentHour * 60 + currentMinute;
          const todayHours = resolveTodaySchedule(hours, dayOfWeek);
          if (!todayHours || isDayClosed(todayHours)) return false;
          const openValue = todayHours.openTime || todayHours.open;
          const closeValue = todayHours.closeTime || todayHours.close;
          const openTimeInMinutes = parseTimeToMinutes(openValue);
          const closeTimeInMinutes = parseTimeToMinutes(closeValue);
          if (openTimeInMinutes === null || closeTimeInMinutes === null) {
            return true;
          }
          if (closeTimeInMinutes < openTimeInMinutes) {
            return currentTimeInMinutes >= openTimeInMinutes || currentTimeInMinutes <= closeTimeInMinutes;
          }
          return currentTimeInMinutes >= openTimeInMinutes && currentTimeInMinutes <= closeTimeInMinutes;
        } catch {
          return true;
        }
      }
      // Update all businesses based on their schedules
      static async updateAllBusinessStatuses() {
        const allBusinesses = await db.select().from(businesses);
        for (const business of allBusinesses) {
          if (!business.openingHours) continue;
          const shouldBeOpen = await this.isBusinessOpen(business.id);
          if (business.isOpen !== shouldBeOpen) {
            await db.update(businesses).set({ isOpen: shouldBeOpen }).where((0, import_drizzle_orm10.eq)(businesses.id, business.id));
            console.log(
              `\u{1F4CD} ${business.name}: ${shouldBeOpen ? "ABIERTO" : "CERRADO"}`
            );
          }
        }
      }
    };
  }
});

// server/cloudinaryService.ts
var cloudinaryService_exports = {};
__export(cloudinaryService_exports, {
  CloudinaryService: () => CloudinaryService
});
var import_cloudinary, CloudinaryService;
var init_cloudinaryService = __esm({
  "server/cloudinaryService.ts"() {
    "use strict";
    import_cloudinary = require("cloudinary");
    import_cloudinary.v2.config({
      cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
      api_key: process.env.CLOUDINARY_API_KEY,
      api_secret: process.env.CLOUDINARY_API_SECRET
    });
    CloudinaryService = class {
      /**
       * Sube un archivo (imagen o PDF) a Cloudinary usando resource_type: auto
       * @param base64File - Archivo en formato data:...;base64,xxx
       * @param folder - Carpeta destino en Cloudinary
       * @param publicId - ID público opcional
       * @returns URL segura del archivo subido
       */
      static async uploadDocument(base64File, folder, publicId) {
        try {
          const result = await import_cloudinary.v2.uploader.upload(base64File, {
            folder: `comeya/${folder}`,
            public_id: publicId,
            resource_type: "auto"
          });
          return result.secure_url;
        } catch (error) {
          console.error("Cloudinary document upload error:", error);
          throw new Error(`Error al subir documento: ${error.message}`);
        }
      }
      /**
       * Sube una imagen base64 a Cloudinary
       * @param base64Image - Imagen en formato data:image/...;base64,xxx
       * @param folder - Carpeta destino en Cloudinary
       * @param publicId - ID público opcional (si no se provee, Cloudinary genera uno)
       * @returns URL segura de la imagen subida
       */
      static async uploadImage(base64Image, folder, publicId) {
        try {
          const result = await import_cloudinary.v2.uploader.upload(base64Image, {
            folder: `comeya/${folder}`,
            public_id: publicId,
            resource_type: "image",
            transformation: [{ quality: "auto:good" }, { fetch_format: "auto" }]
          });
          return result.secure_url;
        } catch (error) {
          console.error("Cloudinary upload error:", error);
          throw new Error(`Error al subir imagen: ${error.message}`);
        }
      }
      /**
       * Elimina una imagen de Cloudinary
       * @param publicId - ID público de la imagen (ej: comeya/profiles/user-123)
       */
      static async deleteImage(publicId) {
        try {
          await import_cloudinary.v2.uploader.destroy(publicId);
        } catch (error) {
          console.error("Cloudinary delete error:", error);
          throw new Error(`Error al eliminar imagen: ${error.message}`);
        }
      }
      /**
       * Extrae el public_id de una URL de Cloudinary
       * @param url - URL completa de Cloudinary
       * @returns public_id sin extensión
       */
      static extractPublicId(url) {
        try {
          const match = url.match(/\/v\d+\/(.+)\.\w+$/);
          return match ? match[1] : null;
        } catch {
          return null;
        }
      }
    };
  }
});

// server/deliveryNotificationService.ts
var deliveryNotificationService_exports = {};
__export(deliveryNotificationService_exports, {
  DeliveryNotificationService: () => DeliveryNotificationService,
  deliveryNotificationService: () => deliveryNotificationService
});
var import_drizzle_orm11, DeliveryNotificationService, deliveryNotificationService;
var init_deliveryNotificationService = __esm({
  "server/deliveryNotificationService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm11 = require("drizzle-orm");
    init_enhancedPushService();
    DeliveryNotificationService = class {
      /**
       * Notificar al repartidor que su verificación ha sido aprobada
       */
      static async notifyApproved(userId) {
        try {
          const [user] = await db.select({ name: users.name, phone: users.phone }).from(users).where((0, import_drizzle_orm11.eq)(users.id, userId)).limit(1);
          if (!user) return false;
          await sendPushToUser(userId, {
            title: "\u{1F389} \xA1Est\xE1s aprobado!",
            body: "\xA1Felicidades! Ya puedes empezar a repartidor. Abre la app para ver tus pedidos disponibles.",
            data: {
              screen: "DriverDashboard",
              action: "verification_approved"
            }
          });
          if (user.phone && process.env.TWILIO_ACCOUNT_SID) {
            try {
              const TwilioModule = await import("twilio");
              const TwilioClass = TwilioModule.default || TwilioModule;
              const twilio2 = TwilioClass(
                process.env.TWILIO_ACCOUNT_SID,
                process.env.TWILIO_AUTH_TOKEN
              );
              await twilio2.messages.create({
                body: `\u{1F389} \xA1ComeYa te espera! Tu cuenta de repartidor ha sido aprobada. Abre la app para empezar a trabajar.`,
                from: process.env.TWILIO_PHONE_NUMBER,
                to: user.phone
              });
            } catch {
            }
          }
          return true;
        } catch (error) {
          console.error("Error notifying approval:", error);
          return false;
        }
      }
      /**
       * Notificar al repartidor que ha sido rechazado
       */
      static async notifyRejected(userId, reason) {
        try {
          const [user] = await db.select({ name: users.name, phone: users.phone }).from(users).where((0, import_drizzle_orm11.eq)(users.id, userId)).limit(1);
          if (!user) return false;
          await sendPushToUser(userId, {
            title: "\u{1F4CB} Verificaci\xF3n no aprobada",
            body: `Tu solicitud necesita ajustes: ${reason}. Abre la app para m\xE1s detalles.`,
            data: {
              screen: "DriverVerification",
              action: "verification_rejected",
              reason
            }
          });
          await db.update(users).set({ verificationNotes: reason }).where((0, import_drizzle_orm11.eq)(users.id, userId));
          return true;
        } catch (error) {
          console.error("Error notifying rejection:", error);
          return false;
        }
      }
      /**
       * Notificar al repartidor que hay documentos pendientes por actualizar
       */
      static async notifyDocumentsNeeded(userId, missingDocs) {
        try {
          const docNames = {
            idDocumentUrl: "DNI (frente)",
            idDocumentBackUrl: "DNI (reverso)",
            vehicleLicensePhoto: "Licencia de conducir",
            vehiclePhoto: "Foto del veh\xEDculo",
            vehiclePlatePhoto: "Foto de matr\xEDcula",
            vehicleItvPhoto: "ITV",
            vehicleInsurancePhoto: "Seguro",
            autonomoDocumentUrl: "Documento de aut\xF3nomo"
          };
          const missingNames = missingDocs.map((d) => docNames[d] || d).join(", ");
          await sendPushToUser(userId, {
            title: "\u{1F4C4} Documentos requeridos",
            body: `Para completar tu verificaci\xF3n necesitas: ${missingNames}`,
            data: {
              screen: "DriverVerification",
              action: "documents_needed"
            }
          });
          return true;
        } catch (error) {
          console.error("Error notifying documents needed:", error);
          return false;
        }
      }
      /**
       * Notificar a los admins que hay un nuevo repartidor pendiente
       */
      static async notifyNewDriverToAdmins(userId) {
        try {
          const [user] = await db.select({ name: users.name, phone: users.phone }).from(users).where((0, import_drizzle_orm11.eq)(users.id, userId)).limit(1);
          if (!user) return false;
          try {
            const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
            notifyAdmins2({
              type: "new_delivery_verification",
              userId,
              userName: user.name,
              phone: user.phone,
              message: `Nuevo repartidor pendiente: ${user.name} (${user.phone})`
            });
          } catch {
          }
          const admins = await db.select({ id: users.id }).from(users).where((0, import_drizzle_orm11.or)((0, import_drizzle_orm11.eq)(users.role, "admin"), (0, import_drizzle_orm11.eq)(users.role, "super_admin")));
          for (const admin of admins) {
            await sendPushToUser(admin.id, {
              title: "\u{1F464} Nuevo repartidor",
              body: `${user.name} quiere ser repartidor. Revisa su documentaci\xF3n.`,
              data: {
                screen: "AdminVerification",
                userId
              }
            });
          }
          return true;
        } catch (error) {
          console.error("Error notifying admins:", error);
          return false;
        }
      }
      /**
       * Notificar al repartidor que hay un nuevo pedido asignado
       */
      static async notifyNewOrder(driverId, orderId, businessName) {
        try {
          await sendPushToUser(driverId, {
            title: "\u{1F6CD}\uFE0F Nuevo pedido asignado",
            body: `Recoge el pedido en ${businessName} y ll\xE9valo al cliente`,
            data: {
              screen: "OrderTracking",
              orderId,
              action: "new_order"
            }
          });
          return true;
        } catch (error) {
          console.error("Error notifying new order:", error);
          return false;
        }
      }
      /**
       * Notificar al repartidor que hay un pedido cerca
       */
      static async notifyOrderNearby(driverId, orderId, distance) {
        try {
          await sendPushToUser(driverId, {
            title: "\u{1F4CD} Pedido cerca",
            body: `Tienes un pedido a ${Math.round(distance / 1e3)} km de distancia`,
            data: {
              screen: "DriverMap",
              orderId,
              action: "order_nearby"
            }
          });
          return true;
        } catch (error) {
          console.error("Error notifying order nearby:", error);
          return false;
        }
      }
      /**
       * Notificar al repartidor que su cuenta ha sido bloqueada
       */
      static async notifyBlocked(userId, reason) {
        try {
          await sendPushToUser(userId, {
            title: "\u{1F512} Cuenta suspendida",
            body: `Tu cuenta ha sido suspendida: ${reason}. Contacta soporte para m\xE1s informaci\xF3n.`,
            data: {
              screen: "Support",
              action: "account_blocked"
            }
          });
          return true;
        } catch (error) {
          console.error("Error notifying blocked:", error);
          return false;
        }
      }
      /**
       * Notificar al repartidor sobre strike
       */
      static async notifyStrike(userId, strikeCount, reason) {
        try {
          const remaining = 3 - strikeCount;
          await sendPushToUser(userId, {
            title: `\u26A0\uFE0F Strike(${strikeCount}/3)`,
            body: remaining > 0 ? `Has recibido un strike: ${reason}. Te quedan ${remaining} antes de suspensi\xF3n.` : `Tu cuenta est\xE1 en riesgo: ${reason}`,
            data: {
              screen: "DriverDashboard",
              action: "strike_added"
            }
          });
          return true;
        } catch (error) {
          console.error("Error notifying strike:", error);
          return false;
        }
      }
      /**
       * Notificar al repartidor sobre Earnings
       */
      static async notifyEarnings(driverId, amount, period) {
        try {
          const amountStr = (amount / 100).toFixed(2);
          await sendPushToUser(driverId, {
            title: "\u{1F4B0} Ganancias disponibles",
            body: `Tienes \u20AC${amountStr} ${period}. Solicita tu retiro.`,
            data: {
              screen: "DriverEarnings",
              action: "earnings_available"
            }
          });
          return true;
        } catch (error) {
          console.error("Error notifying earnings:", error);
          return false;
        }
      }
      // ==========================================
      // BROADCAST A TODOS LOS DRIVERS
      // ==========================================
      /**
       * Enviar notificación a todos los drivers activos
       */
      static async broadcastToDrivers(title, body, data) {
        try {
          const allDrivers = await db.select({ id: deliveryDrivers.userId }).from(deliveryDrivers).where((0, import_drizzle_orm11.eq)(deliveryDrivers.isAvailable, true));
          let sent = 0;
          for (const driver of allDrivers) {
            try {
              await sendPushToUser(driver.userId, { title, body, data });
              sent++;
            } catch {
            }
          }
          return sent;
        } catch (error) {
          console.error("Error broadcasting:", error);
          return 0;
        }
      }
    };
    deliveryNotificationService = DeliveryNotificationService;
  }
});

// server/utils/refundPolicy.ts
function isStripePayment(order) {
  return typeof order.paymentMethod === "string" && order.paymentMethod.startsWith("stripe_");
}
function isCashPayment(order) {
  return order.paymentMethod === "cash" || order.paymentMethod === "efectivo";
}
function isManualPayment(order) {
  return !isStripePayment(order) && !isCashPayment(order);
}
function resolveRefundMethod(order) {
  if (isStripePayment(order) && order.stripePaymentIntentId && order.paidAt) {
    return "stripe";
  }
  if (isStripePayment(order) && !order.paidAt) {
    return "no_charge";
  }
  if (isCashPayment(order) && !order.cashCollected) {
    return "cash_none";
  }
  return "manual_transfer";
}
var init_refundPolicy = __esm({
  "server/utils/refundPolicy.ts"() {
    "use strict";
  }
});

// server/refundService.ts
var refundService_exports = {};
__export(refundService_exports, {
  createRefund: () => createRefund,
  defaultLiableParty: () => defaultLiableParty,
  describePaymentForRefund: () => describePaymentForRefund,
  getBusinessName: () => getBusinessName,
  getPaymentKind: () => getPaymentKind,
  getRefundableAmount: () => getRefundableAmount,
  getRefundedTotal: () => getRefundedTotal,
  isCashPayment: () => isCashPayment,
  isManualPayment: () => isManualPayment,
  isStripePayment: () => isStripePayment,
  markRefundPaid: () => markRefundPaid,
  resolveRefundMethod: () => resolveRefundMethod,
  retryRefund: () => retryRefund
});
function getPaymentKind(order) {
  if (isStripePayment(order)) return "stripe";
  if (isCashPayment(order)) return "cash";
  return "manual";
}
async function getStripe() {
  const key = process.env.STRIPE_SECRET_KEY;
  if (!key) return null;
  try {
    const StripeModule = await import("stripe");
    const StripeClass = StripeModule.default || StripeModule;
    return new StripeClass(key);
  } catch {
    return null;
  }
}
async function getRefundedTotal(orderId) {
  const rows = await db.select({ amount: refunds.amount, status: refunds.status }).from(refunds).where((0, import_drizzle_orm12.eq)(refunds.orderId, orderId));
  return rows.filter(
    (r) => r.status === "completed" || r.status === "processing" || r.status === "pending"
  ).reduce(
    (sum3, r) => sum3 + (r.amount || 0),
    0
  );
}
async function getRefundableAmount(order) {
  const already = await getRefundedTotal(order.id);
  return Math.max(0, (order.total || 0) - already);
}
function defaultLiableParty(issueType) {
  return DEFAULT_LIABILITY[issueType] || "platform";
}
function splitCost(amount, liableParty, order) {
  if (liableParty === "business") {
    const cap = order.productosBase || order.subtotal || 0;
    const businessDeduction = Math.min(amount, cap);
    return {
      businessDeduction,
      driverDeduction: 0,
      platformCost: amount - businessDeduction
    };
  }
  if (liableParty === "driver") {
    const cap = order.deliveryFee || 0;
    const driverDeduction = Math.min(amount, cap);
    return {
      businessDeduction: 0,
      driverDeduction,
      platformCost: amount - driverDeduction
    };
  }
  return { businessDeduction: 0, driverDeduction: 0, platformCost: amount };
}
async function applyPayoutAdjustment(orderId, recipientType, deduction, reason) {
  if (deduction <= 0) return false;
  try {
    const [payout] = await db.select().from(payouts).where(
      (0, import_drizzle_orm12.and)(
        (0, import_drizzle_orm12.eq)(payouts.orderId, orderId),
        (0, import_drizzle_orm12.eq)(payouts.recipientType, recipientType),
        (0, import_drizzle_orm12.eq)(payouts.status, "pending")
      )
    ).limit(1);
    if (!payout) return false;
    const applied = Math.min(deduction, payout.amount);
    await db.update(payouts).set({
      amount: payout.amount - applied,
      adjustmentAmount: (payout.adjustmentAmount || 0) + applied,
      adjustmentReason: reason
    }).where((0, import_drizzle_orm12.eq)(payouts.id, payout.id));
    logger.info(
      `Payout ${payout.id} ajustado -${applied} centavos (${recipientType})`,
      { orderId }
    );
    return true;
  } catch (err) {
    logger.error(`Error ajustando payout: ${err.message}`, { orderId });
    return false;
  }
}
async function recordRefundTransaction(customerId, amount, orderId, description) {
  await db.insert(transactions).values({
    userId: customerId,
    orderId,
    type: "refund",
    amount,
    description,
    status: "completed"
  });
}
async function notifyRefund(customerId, orderId, amount, method) {
  const euros = (amount / 100).toFixed(2);
  const withoutCharge = method === "cash_none" || method === "no_charge";
  const ref = await orderRefFromId(orderId);
  const title = withoutCharge ? "Pedido cancelado" : `Devoluci\xF3n de ${euros} \u20AC`;
  const body = withoutCharge ? `Pedido ${ref}: ${METHOD_COPY[method]}` : `Pedido ${ref}. ${METHOD_COPY[method]}`;
  await sendPushToUser(customerId, {
    title,
    body,
    data: { orderId, screen: "OrderTracking", type: "refund" }
  }).catch(() => {
  });
}
async function createRefund(req) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm12.eq)(orders.id, req.orderId)).limit(1);
  if (!order) {
    return { success: false, message: "Pedido no encontrado", error: "not_found" };
  }
  const method = resolveRefundMethod(order);
  if (method === "cash_none" || method === "no_charge") {
    const noChargeId = (0, import_crypto2.randomUUID)();
    await db.insert(refunds).values({
      id: noChargeId,
      orderId: order.id,
      issueId: req.issueId,
      customerId: order.userId,
      amount: 0,
      type: req.type,
      reason: req.reason,
      method,
      status: "completed",
      liableParty: req.liableParty,
      platformCost: 0,
      requestedBy: req.requestedBy,
      processedAt: /* @__PURE__ */ new Date(),
      notes: req.notes
    });
    if (req.notifyCustomer !== false) {
      await notifyRefund(order.userId, order.id, 0, method);
    }
    return {
      success: true,
      refundId: noChargeId,
      method,
      status: "completed",
      amount: 0,
      message: method === "cash_none" ? "El pedido se pagaba en efectivo y no se lleg\xF3 a cobrar" : "El pago nunca se complet\xF3; no hay importe que devolver"
    };
  }
  const refundable = await getRefundableAmount(order);
  if (refundable <= 0) {
    return {
      success: false,
      message: "Este pedido ya est\xE1 reembolsado por completo",
      error: "already_refunded"
    };
  }
  const amount = Math.min(Math.round(req.amount), refundable);
  if (amount <= 0) {
    return { success: false, message: "Importe inv\xE1lido", error: "invalid_amount" };
  }
  const cost = splitCost(amount, req.liableParty, order);
  const realId = (0, import_crypto2.randomUUID)();
  await db.insert(refunds).values({
    id: realId,
    orderId: order.id,
    issueId: req.issueId,
    customerId: order.userId,
    amount,
    type: req.type,
    reason: req.reason,
    method,
    status: method === "manual_transfer" ? "pending" : "processing",
    stripePaymentIntentId: order.stripePaymentIntentId || null,
    liableParty: req.liableParty,
    businessDeduction: cost.businessDeduction,
    driverDeduction: cost.driverDeduction,
    platformCost: cost.platformCost,
    requestedBy: req.requestedBy,
    notes: req.notes
  });
  let finalStatus = "pending";
  let failureReason = null;
  let stripeRefundId = null;
  if (method === "stripe") {
    const stripe2 = await getStripe();
    if (!stripe2) {
      finalStatus = "failed";
      failureReason = "Stripe no est\xE1 configurado (falta STRIPE_SECRET_KEY)";
    } else {
      try {
        const refund = await stripe2.refunds.create({
          payment_intent: order.stripePaymentIntentId,
          amount,
          reason: "requested_by_customer",
          metadata: {
            orderId: order.id,
            refundId: realId,
            issueId: req.issueId || "",
            type: req.type
          }
        });
        stripeRefundId = refund.id;
        finalStatus = refund.status === "failed" ? "failed" : "completed";
        if (refund.status === "failed") failureReason = "Stripe rechaz\xF3 la devoluci\xF3n";
      } catch (err) {
        finalStatus = "failed";
        failureReason = err?.message || "Error al devolver en Stripe";
        logger.error(`Stripe refund failed: ${failureReason}`, {
          orderId: order.id
        });
      }
    }
  } else {
    finalStatus = "pending";
  }
  await db.update(refunds).set({
    status: finalStatus,
    stripeRefundId,
    failureReason,
    processedAt: finalStatus === "completed" ? /* @__PURE__ */ new Date() : null
  }).where((0, import_drizzle_orm12.eq)(refunds.id, realId));
  if (finalStatus !== "failed") {
    let adjusted = false;
    if (cost.businessDeduction > 0) {
      adjusted = await applyPayoutAdjustment(
        order.id,
        "business",
        cost.businessDeduction,
        `Incidencia: ${req.reason}`
      ) || adjusted;
    }
    if (cost.driverDeduction > 0) {
      adjusted = await applyPayoutAdjustment(
        order.id,
        "driver",
        cost.driverDeduction,
        `Incidencia: ${req.reason}`
      ) || adjusted;
    }
    if (adjusted) {
      await db.update(refunds).set({ payoutAdjusted: true }).where((0, import_drizzle_orm12.eq)(refunds.id, realId));
    }
    const totalRefunded = await getRefundedTotal(order.id);
    await db.update(orders).set({
      refundAmount: totalRefunded,
      refundStatus: finalStatus === "completed" ? "processed" : "pending",
      refundedAt: finalStatus === "completed" ? /* @__PURE__ */ new Date() : null,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm12.eq)(orders.id, order.id));
    if (finalStatus === "completed") {
      await recordRefundTransaction(
        order.userId,
        amount,
        order.id,
        `Devoluci\xF3n ${orderRef(order)}: ${req.reason}`
      ).catch(() => {
      });
      if (req.notifyCustomer !== false) {
        await notifyRefund(order.userId, order.id, amount, method);
      }
    }
  } else {
    await db.update(orders).set({ refundStatus: "failed", updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm12.eq)(orders.id, order.id));
    await notifyAdminsRefundFailed(order.id, amount, failureReason || "");
  }
  return {
    success: finalStatus !== "failed",
    refundId: realId,
    method,
    status: finalStatus,
    amount,
    message: finalStatus === "completed" ? `Devoluci\xF3n de ${(amount / 100).toFixed(2)} \u20AC completada` : finalStatus === "pending" ? `Devoluci\xF3n de ${(amount / 100).toFixed(2)} \u20AC registrada, pendiente de transferencia manual` : `No se pudo completar la devoluci\xF3n: ${failureReason}`,
    error: failureReason || void 0
  };
}
async function retryRefund(refundId, adminId) {
  const [row] = await db.select().from(refunds).where((0, import_drizzle_orm12.eq)(refunds.id, refundId)).limit(1);
  if (!row) return { success: false, message: "Devoluci\xF3n no encontrada" };
  if (row.status === "completed")
    return { success: false, message: "Esta devoluci\xF3n ya se complet\xF3" };
  await db.update(refunds).set({ status: "failed", notes: `${row.notes || ""}
[reintentado por admin]`.trim() }).where((0, import_drizzle_orm12.eq)(refunds.id, refundId));
  return createRefund({
    orderId: row.orderId,
    amount: row.amount,
    type: row.type,
    reason: row.reason || "Reintento de devoluci\xF3n",
    issueId: row.issueId || void 0,
    liableParty: row.liableParty || void 0,
    requestedBy: adminId
  });
}
async function markRefundPaid(refundId, adminId, data) {
  const [row] = await db.select().from(refunds).where((0, import_drizzle_orm12.eq)(refunds.id, refundId)).limit(1);
  if (!row) return { success: false, message: "Devoluci\xF3n no encontrada" };
  if (row.status === "completed")
    return { success: false, message: "Ya est\xE1 marcada como pagada" };
  await db.update(refunds).set({
    status: "completed",
    processedAt: /* @__PURE__ */ new Date(),
    proofUrl: data.proofUrl,
    notes: data.notes ?? row.notes,
    requestedBy: row.requestedBy || adminId
  }).where((0, import_drizzle_orm12.eq)(refunds.id, refundId));
  const totalRefunded = await getRefundedTotal(row.orderId);
  await db.update(orders).set({
    refundAmount: totalRefunded,
    refundStatus: "processed",
    refundedAt: /* @__PURE__ */ new Date(),
    updatedAt: /* @__PURE__ */ new Date()
  }).where((0, import_drizzle_orm12.eq)(orders.id, row.orderId));
  await recordRefundTransaction(
    row.customerId,
    row.amount,
    row.orderId,
    `Devoluci\xF3n ${await orderRefFromId(row.orderId)} por transferencia manual`
  ).catch(() => {
  });
  await notifyRefund(row.customerId, row.orderId, row.amount, "manual_transfer");
  return {
    success: true,
    refundId,
    amount: row.amount,
    status: "completed",
    message: "Devoluci\xF3n marcada como pagada"
  };
}
async function notifyAdminsRefundFailed(orderId, amount, reason) {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const admins = await db.select({ id: users5.id }).from(users5).where((0, import_drizzle_orm12.inArray)(users5.role, ["admin", "super_admin"]));
    for (const admin of admins) {
      await sendPushToUser(admin.id, {
        title: "\u26A0\uFE0F Devoluci\xF3n fallida",
        body: `Pedido ${await orderRefFromId(orderId)}: ${(amount / 100).toFixed(2)} \u20AC \u2014 ${reason}`,
        data: {
          orderId,
          screen: "AdminDashboard",
          section: "finance_refunds",
          type: "refund_failed"
        }
      }).catch(() => {
      });
    }
  } catch {
  }
}
async function describePaymentForRefund(order) {
  const kind = getPaymentKind(order);
  if (kind === "stripe") {
    const ok = !!order.stripePaymentIntentId;
    return {
      kind,
      label: order.paymentMethod === "stripe_bizum" ? "Bizum (Stripe)" : "Tarjeta (Stripe)",
      refundMethod: ok ? "stripe" : "manual_transfer",
      automatic: ok,
      note: ok ? "La devoluci\xF3n se hace autom\xE1ticamente al m\xE9todo de pago original. Tarda 5-10 d\xEDas h\xE1biles en verse en la cuenta del cliente." : "No hay PaymentIntent registrado, as\xED que Stripe no puede revertir el cobro. Habr\xE1 que transferir a mano."
    };
  }
  if (kind === "cash") {
    const collected = !!order.cashCollected;
    return {
      kind,
      label: "Efectivo",
      refundMethod: collected ? "manual_transfer" : "cash_none",
      automatic: false,
      note: collected ? "El repartidor ya cobr\xF3 en efectivo: hay que devolver el importe por transferencia y subir el comprobante." : "A\xFAn no se ha cobrado nada, as\xED que no hay importe que devolver."
    };
  }
  return {
    kind,
    label: order.paymentMethod === "bizum_manual" ? "Bizum manual" : order.paymentMethod === "sepa" ? "Transferencia SEPA" : order.paymentMethod === "paypal" ? "PayPal" : String(order.paymentMethod || "Manual"),
    refundMethod: "manual_transfer",
    automatic: false,
    note: "El cobro entr\xF3 fuera de la plataforma, as\xED que la devoluci\xF3n se hace por transferencia manual con comprobante."
  };
}
async function getBusinessName(businessId) {
  try {
    const [biz] = await db.select({ name: businesses.name }).from(businesses).where((0, import_drizzle_orm12.eq)(businesses.id, businessId)).limit(1);
    return biz?.name || "el negocio";
  } catch {
    return "el negocio";
  }
}
var import_drizzle_orm12, import_crypto2, DEFAULT_LIABILITY, METHOD_COPY;
var init_refundService = __esm({
  "server/refundService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm12 = require("drizzle-orm");
    import_crypto2 = require("crypto");
    init_logger();
    init_enhancedPushService();
    init_orderNumberService();
    init_refundPolicy();
    DEFAULT_LIABILITY = {
      missing_items: "business",
      wrong_items: "business",
      incomplete: "business",
      damaged: "business",
      quality: "business",
      never_arrived: "driver",
      driver_issue: "driver",
      late_delivery: "platform",
      other: "platform"
    };
    METHOD_COPY = {
      stripe: "Se ha devuelto a tu m\xE9todo de pago (puede tardar 5-10 d\xEDas h\xE1biles).",
      manual_transfer: "Realizaremos la transferencia en las pr\xF3ximas 24-48 horas al mismo medio con el que pagaste.",
      cash_none: "No se realiz\xF3 ning\xFAn cargo, por lo que no hay importe a devolver.",
      no_charge: "El pago nunca se complet\xF3, no hay importe a devolver."
    };
  }
});

// server/substitutionService.ts
var substitutionService_exports = {};
__export(substitutionService_exports, {
  applyDeltaPaymentFromWebhook: () => applyDeltaPaymentFromWebhook,
  confirmDeltaPayment: () => confirmDeltaPayment,
  decideSubstitution: () => decideSubstitution,
  listSubstitutions: () => listSubstitutions,
  proposeSubstitution: () => proposeSubstitution
});
function parseItems(order) {
  try {
    const raw = typeof order.items === "string" ? JSON.parse(order.items) : order.items;
    return Array.isArray(raw) ? raw : [];
  } catch {
    return [];
  }
}
function itemPriceCents(item) {
  if (item?.product?.price != null) {
    return Math.round(Number(item.product.price) * 100);
  }
  if (item?.price != null) {
    return Math.round(Number(item.price));
  }
  return 0;
}
function parseSubstituteMap(order) {
  try {
    const raw = typeof order.substituteProductIds === "string" ? JSON.parse(order.substituteProductIds) : order.substituteProductIds;
    return raw && typeof raw === "object" ? raw : {};
  } catch {
    return {};
  }
}
function deltaText(delta) {
  const eur = (Math.abs(delta) / 100).toFixed(2);
  return delta < 0 ? `\u2212${eur} \u20AC a devolver` : delta > 0 ? `+${eur} \u20AC a cobrar` : "mismo precio";
}
async function listSubstitutions(orderId) {
  try {
    return await db.select().from(substitutions).where((0, import_drizzle_orm13.eq)(substitutions.orderId, orderId)).orderBy((0, import_drizzle_orm13.desc)(substitutions.createdAt));
  } catch {
    return [];
  }
}
async function notifyCustomer(order, title, body) {
  try {
    await sendPushToUser(order.userId, {
      title,
      body,
      data: { orderId: order.id, screen: "OrderTracking", type: "substitution" }
    });
    getIO().to(`user:${order.userId}`).emit("substitution_update", {
      orderId: order.id
    });
  } catch (err) {
    console.error("substitution notifyCustomer:", err);
  }
}
async function notifyBusiness(order, title, body) {
  try {
    const [biz] = await db.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm13.eq)(businesses.id, order.businessId)).limit(1);
    if (biz?.ownerId) {
      await sendPushToUser(biz.ownerId, {
        title,
        body,
        data: { orderId: order.id, screen: "BusinessOrders" }
      });
    }
    getIO().to(`business:${order.businessId}`).emit("substitution_update", {
      orderId: order.id
    });
  } catch (err) {
    console.error("substitution notifyBusiness:", err);
  }
}
async function applySubstitution(order, sub) {
  const delta = Number(sub.priceDelta) || 0;
  await db.update(orders).set({
    subtotal: Math.max(0, (order.subtotal || 0) + delta),
    total: Math.max(0, (order.total || 0) + delta),
    productosBase: Math.max(0, (order.productosBase || 0) + delta),
    businessEarnings: (order.businessEarnings ?? order.subtotal ?? 0) + delta,
    updatedAt: /* @__PURE__ */ new Date()
  }).where((0, import_drizzle_orm13.eq)(orders.id, order.id));
  let refunded = 0;
  if (delta < 0) {
    const { createRefund: createRefund2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const refund = await createRefund2({
      orderId: order.id,
      amount: -delta,
      type: "substitution",
      reason: `Diferencia por sustituci\xF3n: ${sub.itemName || "producto"} \u2192 ${sub.substituteName || "sustituto"} (${deltaText(delta)})`,
      requestedBy: void 0,
      notifyCustomer: true
    });
    if (!refund.success) {
      console.error(
        `applySubstitution: reembolso fall\xF3 (${order.id}):`,
        refund.message
      );
    } else {
      refunded = -delta;
    }
  }
  await db.update(substitutions).set({ status: "applied", appliedAt: /* @__PURE__ */ new Date(), updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm13.eq)(substitutions.id, sub.id));
  await notifyBusiness(
    order,
    "\u2705 Sustituci\xF3n aplicada",
    `Pedido ${orderRef(order)}: ${sub.itemName || "producto"} \u2192 ${sub.substituteName || "sustituto"} (${deltaText(delta)}).`
  );
  return {
    success: true,
    message: "Sustituci\xF3n aplicada",
    applied: true,
    refunded,
    delta
  };
}
async function proposeSubstitution(order, itemProductId, substituteProductId, proposedBy) {
  if (TERMINAL_STATUSES.includes(String(order.status))) {
    return {
      success: false,
      message: "El pedido ya est\xE1 cerrado, no admite sustituciones"
    };
  }
  const items = parseItems(order);
  const map = parseSubstituteMap(order);
  let item = items.find(
    (it) => it.product?.id === itemProductId || it.id === itemProductId || it.productId === itemProductId
  );
  if (!item && map[itemProductId]) {
    item = items.find((it) => it.id === itemProductId);
    if (!item && itemProductId === "__global__" && items.length === 1) {
      item = items[0];
    }
  }
  if (!item) {
    return {
      success: false,
      message: "Producto del pedido no encontrado. Aplica la sustituci\xF3n desde el producto concreto de la lista."
    };
  }
  const [substitute] = await db.select().from(products).where((0, import_drizzle_orm13.eq)(products.id, substituteProductId)).limit(1);
  if (!substitute) {
    return { success: false, message: "Producto sustituto no encontrado" };
  }
  const originalPrice = itemPriceCents(item);
  const substitutePrice = Number(substitute.price) || 0;
  const quantity = Math.max(1, Number(item.quantity) || 1);
  const unitDelta = substitutePrice - originalPrice;
  const delta = unitDelta * quantity;
  const [existing] = await db.select({ id: substitutions.id }).from(substitutions).where(
    (0, import_drizzle_orm13.and)(
      (0, import_drizzle_orm13.eq)(substitutions.orderId, order.id),
      (0, import_drizzle_orm13.eq)(substitutions.itemProductId, itemProductId),
      (0, import_drizzle_orm13.inArray)(substitutions.status, ["proposed", "approved", "applied"])
    )
  ).limit(1);
  if (existing) {
    return {
      success: false,
      message: "Ya existe una sustituci\xF3n para este producto"
    };
  }
  const preAuthorized = map[itemProductId] === substituteProductId || map["__global__"] === substituteProductId || map[item.product?.id || ""] === substituteProductId || map[item.id || ""] === substituteProductId;
  const subId = (0, import_crypto3.randomUUID)();
  await db.insert(substitutions).values({
    id: subId,
    orderId: order.id,
    itemProductId,
    itemName: item.product?.name || item.name || "Producto",
    originalPrice,
    substituteProductId,
    substituteName: substitute.name,
    substituteImage: substitute.image || null,
    substitutePrice,
    priceDelta: delta,
    quantity,
    status: "proposed",
    proposedBy
  });
  const row = {
    id: subId,
    orderId: order.id,
    itemProductId,
    itemName: item.product?.name || item.name || "Producto",
    originalPrice,
    substituteProductId,
    substituteName: substitute.name,
    substituteImage: substitute.image || null,
    substitutePrice,
    priceDelta: delta,
    quantity,
    status: "proposed"
  };
  if (preAuthorized && delta <= 0) {
    return applySubstitution(order, row);
  }
  await notifyCustomer(
    order,
    "\u{1F504} Sustituci\xF3n propuesta",
    `El negocio propone: ${row.itemName} \u2192 ${row.substituteName} (${deltaText(delta)}). Abre el pedido para aprobarla o rechazarla.`
  );
  return {
    success: true,
    message: "Sustituci\xF3n propuesta al cliente",
    proposed: true,
    delta,
    substitution: row
  };
}
async function decideSubstitution(order, subId, approved, userId) {
  if (order.userId !== userId) {
    return { success: false, message: "No autorizado" };
  }
  const [sub] = await db.select().from(substitutions).where((0, import_drizzle_orm13.and)((0, import_drizzle_orm13.eq)(substitutions.id, subId), (0, import_drizzle_orm13.eq)(substitutions.orderId, order.id))).limit(1);
  if (!sub) return { success: false, message: "Sustituci\xF3n no encontrada" };
  if (sub.status !== "proposed") {
    return { success: false, message: "La sustituci\xF3n ya fue procesada" };
  }
  if (!approved) {
    await db.update(substitutions).set({ status: "rejected", decidedAt: /* @__PURE__ */ new Date(), updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm13.eq)(substitutions.id, subId));
    await notifyBusiness(
      order,
      "\u274C Sustituci\xF3n rechazada",
      `Pedido ${orderRef(order)}: el cliente rechaz\xF3 ${sub.substituteName} por ${sub.itemName}.`
    );
    return { success: true, message: "Sustituci\xF3n rechazada" };
  }
  const delta = Number(sub.priceDelta) || 0;
  if (delta <= 0) {
    return applySubstitution(order, sub);
  }
  if (!process.env.STRIPE_SECRET_KEY) {
    return { success: false, message: "Stripe no configurado" };
  }
  try {
    const Stripe3 = (await import("stripe")).default;
    const stripe2 = new Stripe3(process.env.STRIPE_SECRET_KEY);
    const intent = await stripe2.paymentIntents.create({
      amount: delta,
      currency: "eur",
      automatic_payment_methods: { enabled: true },
      metadata: {
        orderId: order.id,
        substitutionId: subId,
        isDelta: "true",
        itemName: String(sub.itemName || ""),
        substituteName: String(sub.substituteName || "")
      }
    });
    await db.update(substitutions).set({
      stripePaymentIntentId: intent.id,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm13.eq)(substitutions.id, subId));
    return {
      success: true,
      message: "Diferencia pendiente de pago",
      needsPayment: true,
      delta,
      clientSecret: intent.client_secret || void 0,
      paymentIntentId: intent.id,
      substitution: { ...sub, stripePaymentIntentId: intent.id }
    };
  } catch (err) {
    return {
      success: false,
      message: `No se pudo iniciar el pago de la diferencia: ${err.message}`
    };
  }
}
async function confirmDeltaPayment(order, subId, paymentIntentId, userId) {
  if (order.userId !== userId) {
    return { success: false, message: "No autorizado" };
  }
  const [sub] = await db.select().from(substitutions).where((0, import_drizzle_orm13.and)((0, import_drizzle_orm13.eq)(substitutions.id, subId), (0, import_drizzle_orm13.eq)(substitutions.orderId, order.id))).limit(1);
  if (!sub) return { success: false, message: "Sustituci\xF3n no encontrada" };
  if (sub.status !== "proposed") {
    return { success: false, message: "La sustituci\xF3n ya fue procesada" };
  }
  try {
    const Stripe3 = (await import("stripe")).default;
    const stripe2 = new Stripe3(process.env.STRIPE_SECRET_KEY);
    const intent = await stripe2.paymentIntents.retrieve(paymentIntentId);
    if (intent.status !== "succeeded") {
      return {
        success: false,
        message: "El pago de la diferencia a\xFAn no se ha completado"
      };
    }
    const [existing] = await db.select({ metadata: transactions.metadata }).from(transactions).where(
      (0, import_drizzle_orm13.and)(
        (0, import_drizzle_orm13.eq)(transactions.orderId, order.id),
        (0, import_drizzle_orm13.eq)(transactions.type, "payment")
      )
    ).limit(100);
    const already = existing.some((tx) => {
      try {
        return JSON.parse(tx.metadata || "{}").substitutionId === subId;
      } catch {
        return false;
      }
    });
    if (!already) {
      await db.insert(transactions).values({
        id: `txn_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,
        orderId: order.id,
        businessId: order.businessId,
        userId: order.userId,
        amount: intent.amount,
        type: "payment",
        status: "completed",
        metadata: JSON.stringify({
          paymentIntentId: intent.id,
          substitutionId: subId,
          kind: "substitution_delta"
        }),
        createdAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      });
    }
    return applySubstitution(order, sub);
  } catch (err) {
    return { success: false, message: `Error verificando el pago: ${err.message}` };
  }
}
async function applyDeltaPaymentFromWebhook(paymentIntent) {
  const subId = paymentIntent.metadata?.substitutionId;
  const orderId = paymentIntent.metadata?.orderId;
  if (!subId || !orderId) {
    return { success: false, message: "Metadata de sustituci\xF3n incompleta" };
  }
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm13.eq)(orders.id, orderId)).limit(1);
  if (!order) return { success: false, message: "Order not found" };
  const result = await confirmDeltaPayment(
    order,
    subId,
    paymentIntent.id,
    order.userId
  );
  return result;
}
var import_drizzle_orm13, import_crypto3, TERMINAL_STATUSES;
var init_substitutionService = __esm({
  "server/substitutionService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm13 = require("drizzle-orm");
    import_crypto3 = require("crypto");
    init_enhancedPushService();
    init_orderNumberService();
    init_websocket();
    TERMINAL_STATUSES = ["delivered", "cancelled", "payment_failed"];
  }
});

// server/services/googleMapsService.ts
var googleMapsService_exports = {};
__export(googleMapsService_exports, {
  calculateHaversineDistance: () => calculateHaversineDistance,
  decodePolyline: () => decodePolyline,
  estimateDeliveryTimeMinutes: () => estimateDeliveryTimeMinutes,
  geocodeAddress: () => geocodeAddress,
  getDirections: () => getDirections,
  getDirectionsBatch: () => getDirectionsBatch,
  getDistanceMatrix: () => getDistanceMatrix,
  getUsageStats: () => getUsageStats,
  googleMapsService: () => googleMapsService,
  placesAutocomplete: () => placesAutocomplete,
  routeCacheKey: () => routeCacheKey,
  routeStats: () => routeStats
});
function checkDailyLimit(endpoint) {
  const today = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
  let counter = dailyCounters.get(endpoint);
  if (!counter || counter.day !== today) {
    counter = { count: 0, day: today };
    dailyCounters.set(endpoint, counter);
  }
  const limit = DAILY_LIMITS[endpoint] ?? Number.MAX_SAFE_INTEGER;
  if (counter.count >= limit) {
    console.warn(`\u26A0\uFE0F [GoogleMaps] L\xEDmite DIARIO alcanzado: ${endpoint} (${limit}/d\xEDa) \u2014 omitiendo llamada`);
    return false;
  }
  counter.count++;
  return true;
}
function checkRateLimit(endpoint) {
  const now = Date.now();
  const limits = RATE_LIMITS[endpoint] || RATE_LIMITS.all;
  let counter = rateCounters.get(endpoint);
  if (!counter || now > counter.resetAt) {
    counter = { count: 0, resetAt: now + limits.windowMs };
    rateCounters.set(endpoint, counter);
  }
  if (counter.count >= limits.maxRequests) {
    return false;
  }
  counter.count++;
  let globalCounter = rateCounters.get("all");
  if (!globalCounter || now > globalCounter.resetAt) {
    globalCounter = { count: 0, resetAt: now + RATE_LIMITS.all.windowMs };
    rateCounters.set("all", globalCounter);
  }
  if (globalCounter.count >= RATE_LIMITS.all.maxRequests) {
    return false;
  }
  globalCounter.count++;
  return true;
}
function getCacheKey(type, params) {
  const normalized = {};
  for (const [key, value] of Object.entries(params)) {
    if ((key.includes("lat") || key.includes("lng") || key.includes("latitude") || key.includes("longitude")) && typeof value === "number") {
      normalized[key] = Math.round(value * 1e3) / 1e3;
    } else {
      normalized[key] = value;
    }
  }
  return `${type}:${JSON.stringify(normalized)}`;
}
function getFromCache(key, ttl) {
  const entry = cache2.get(key);
  if (!entry) return null;
  if (Date.now() - entry.timestamp > ttl) {
    cache2.delete(key);
    return null;
  }
  return entry.data;
}
function setCache(key, data) {
  cache2.set(key, { data, timestamp: Date.now(), ttl: 0 });
}
async function ensureGeocodeCacheTable() {
  if (geocodeCacheReady) return;
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { sql: sql23 } = await import("drizzle-orm");
    await db2.execute(sql23`
      CREATE TABLE IF NOT EXISTS geocode_cache (
        address_key VARCHAR(255) PRIMARY KEY,
        lat DOUBLE NOT NULL,
        lng DOUBLE NOT NULL,
        formatted_address TEXT,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
      )
    `);
    geocodeCacheReady = true;
  } catch (err) {
    console.error("\u274C [GoogleMaps] No se pudo crear geocode_cache:", err);
  }
}
async function ensureRouteCacheTable() {
  if (routeCacheReady) return;
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    await db2.execute(`
      CREATE TABLE IF NOT EXISTS route_cache (
        cache_key VARCHAR(255) PRIMARY KEY,
        polyline TEXT NOT NULL,
        distance_text VARCHAR(64) DEFAULT NULL,
        distance_value INT DEFAULT NULL,
        duration_text VARCHAR(64) DEFAULT NULL,
        duration_value INT DEFAULT NULL,
        steps_json LONGTEXT DEFAULT NULL,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        INDEX idx_route_cache_created (created_at)
      ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    `);
    routeCacheReady = true;
  } catch (err) {
    console.error("\u274C [GoogleMaps] route_cache no disponible:", err);
  }
}
function routeCacheKey(originLat, originLng, destLat, destLng, mode) {
  const r = (n) => Math.round(n * 1e3) / 1e3;
  return `route:${r(originLat)},${r(originLng)}\u2192${r(destLat)},${r(destLng)}:${mode}`;
}
async function getRouteFromDb(cacheKey, mode) {
  if (!routeCacheReady) return null;
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { sql: sql23 } = await import("drizzle-orm");
    const [rows] = await db2.execute(
      sql23`SELECT polyline, distance_text, distance_value, duration_text, duration_value, steps_json
          FROM route_cache WHERE cache_key = ${cacheKey} LIMIT 1`
    );
    const row = rows?.[0];
    if (!row?.polyline) return null;
    let steps = [];
    try {
      steps = row.steps_json ? JSON.parse(row.steps_json) : [];
    } catch {
    }
    const result = {
      polyline: row.polyline,
      distance: { text: row.distance_text, value: row.distance_value },
      duration: { text: row.duration_text, value: row.duration_value },
      steps,
      startLocation: null,
      endLocation: null
    };
    const memKey = getCacheKey("directions", {
      originLat: Number(cacheKey.split(":")[1]?.split(",")[0]),
      originLng: Number(cacheKey.split(":")[1]?.split(",")[1]?.split("\u2192")[0]),
      destLat: Number(cacheKey.split(":")[2]?.split(",")[0]?.split("\u2192")[1]),
      destLng: Number(cacheKey.split(":")[2]?.split(",")[1]?.split(":")[0]),
      mode
    });
    setCache(memKey, result);
    return result;
  } catch (err) {
    console.error("\u274C [GoogleMaps] Error leyendo route_cache:", err);
    return null;
  }
}
async function saveRouteToDb(cacheKey, result) {
  if (!routeCacheReady) return;
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { sql: sql23 } = await import("drizzle-orm");
    await db2.execute(sql23`
      INSERT INTO route_cache (cache_key, polyline, distance_text, distance_value, duration_text, duration_value, steps_json)
      VALUES (${cacheKey}, ${result.polyline}, ${result.distance?.text ?? null},
              ${result.distance?.value ?? null}, ${result.duration?.text ?? null},
              ${result.duration?.value ?? null}, ${JSON.stringify(result.steps || [])})
      ON DUPLICATE KEY UPDATE created_at = CURRENT_TIMESTAMP
    `);
    routeCacheHitsDb++;
  } catch (err) {
    console.error("\u274C [GoogleMaps] Error guardando route_cache:", err);
  }
}
async function getDirections(originLat, originLng, destLat, destLng, mode = "driving", opts) {
  const memKey = getCacheKey("directions", {
    originLat,
    originLng,
    destLat,
    destLng,
    mode
  });
  const cached = getFromCache(memKey, TTL.directions);
  if (cached) {
    console.log(`\u{1F5FA}\uFE0F [GoogleMaps Cache HIT] Directions`);
    return cached;
  }
  await ensureRouteCacheTable();
  const dbKey = routeCacheKey(originLat, originLng, destLat, destLng, mode);
  const fromDb = await getRouteFromDb(dbKey, mode);
  if (fromDb) {
    routeCacheHitsDb++;
    routeStats.dbHits++;
    console.log(`\u{1F5FA}\uFE0F [RouteCache BD HIT] ${dbKey}`);
    return fromDb;
  }
  const preferOsrm = opts?.preferOsrm === true;
  let result = null;
  if (!preferOsrm && API_KEY && checkRateLimit("directions")) {
    if (checkDailyLimit("directions")) {
      try {
        const url = `https://maps.googleapis.com/maps/api/directions/json?origin=${originLat},${originLng}&destination=${destLat},${destLng}&mode=${mode}&language=es&key=${API_KEY}`;
        const response = await fetch(url, {
          signal: AbortSignal.timeout(8e3)
          // 8s timeout
        });
        const data = await response.json();
        if (data.status === "OK" && data.routes?.length) {
          const route = data.routes[0];
          const leg = route.legs[0];
          result = {
            polyline: route.overview_polyline?.points || "",
            distance: leg.distance,
            duration: leg.duration,
            steps: (leg.steps || []).map((s) => ({
              instruction: s.html_instructions?.replace(/<[^>]*>/g, "").trim() || "",
              distance: s.distance,
              duration: s.duration
            })),
            startLocation: leg.start_location,
            endLocation: leg.end_location
          };
          setCache(memKey, result);
          await saveRouteToDb(dbKey, result);
          console.log(`\u{1F5FA}\uFE0F [GoogleMaps API] Directions fetched & cached`);
          return result;
        }
        console.warn(
          `\u26A0\uFE0F [GoogleMaps] Directions error: ${data.status} \u2014 ${data.error_message || ""} (probando OSRM)`
        );
      } catch (error) {
        console.error(
          `\u274C [GoogleMaps] Directions fetch error:`,
          error.message,
          "(probando OSRM)"
        );
      }
    } else {
      routeStats.dailyLimited++;
      console.warn(
        `\u26A0\uFE0F [GoogleMaps] Directions: l\xEDmite diario alcanzado \u2014 OSRM solamente`
      );
    }
  } else if (!preferOsrm) {
    routeStats.rateLimited++;
    console.warn(`\u26A0\uFE0F [GoogleMaps Rate Limit] Directions \u2014 usando OSRM`);
  } else {
    console.log("\u{1F5FA}\uFE0F [GoogleMaps] preferOsrm \u2014 OSRM directo");
  }
  if (result == null && checkRateLimit("osrm")) {
    result = await fetchOsrmRoute(
      originLat,
      originLng,
      destLat,
      destLng,
      mode
    );
    if (result) {
      setCache(memKey, result);
      await saveRouteToDb(dbKey, result);
    }
  }
  if (result == null) {
    routeStats.failed++;
    console.warn(`\u26A0\uFE0F [GoogleMaps] Sin ruta real para ${dbKey}`);
  }
  return result;
}
async function fetchOsrmRoute(originLat, originLng, destLat, destLng, mode = "driving") {
  const attempt = async (base, profile) => {
    const path3 = `/route/v1/${profile}/${originLng},${originLat};${destLng},${destLat}?overview=full&geometries=polyline&steps=true`;
    const response = await fetch(`${base}${path3}`, {
      signal: AbortSignal.timeout(8e3),
      headers: { "User-Agent": "ComeYa-Delivery-App/1.0" }
    });
    if (!response.ok) return null;
    const data = await response.json();
    const route = data?.routes?.[0];
    if (!route?.geometry) return null;
    const leg = route.legs?.[0];
    const distanceMeters = Math.round(route.distance || leg?.distance || 0);
    const durationSeconds = Math.round(route.duration || leg?.duration || 0);
    const steps = (leg?.steps || []).filter((s) => s.name || s.maneuver?.type === "arrive").slice(0, 25).map((s) => ({
      instruction: s.maneuver?.type === "arrive" ? "Llega a tu destino" : `${maneuverEs(s.maneuver?.type, s.maneuver?.modifier)} ${s.name || ""}`.trim(),
      distance: {
        text: formatMeters(s.distance),
        value: Math.round(s.distance || 0)
      },
      duration: {
        text: formatSeconds(s.duration),
        value: Math.round(s.duration || 0)
      }
    }));
    return {
      polyline: route.geometry,
      distance: {
        text: distanceMeters >= 1e3 ? `${(distanceMeters / 1e3).toFixed(1)} km` : `${distanceMeters} m`,
        value: distanceMeters
      },
      duration: {
        text: `${Math.max(1, Math.round(durationSeconds / 60))} min`,
        value: durationSeconds
      },
      steps,
      startLocation: { lat: originLat, lng: originLng },
      endLocation: { lat: destLat, lng: destLng }
    };
  };
  const mirrors = OSRM_MIRRORS[mode] ?? OSRM_MIRRORS.driving;
  for (const base of mirrors) {
    for (let attemptN = 0; attemptN < 2; attemptN++) {
      try {
        const result = await attempt(base, mode);
        if (result) {
          console.log(`\u{1F5FA}\uFE0F [OSRM] Directions fetched (${base}, intento ${attemptN + 1})`);
          return result;
        }
      } catch {
      }
      if (attemptN === 0) await new Promise((r) => setTimeout(r, 300));
    }
  }
  if (mode === "walking") {
    for (const base of OSRM_MIRRORS.driving) {
      try {
        const result = await attempt(base, "driving");
        if (result) {
          console.log(`\u{1F5FA}\uFE0F [OSRM] Walking fallback \u2192 driving (${base})`);
          return result;
        }
      } catch {
      }
    }
  }
  console.warn("\u26A0\uFE0F [OSRM] Todos los mirrors fallaron");
  return null;
}
function maneuverEs(type, modifier) {
  switch (type) {
    case "depart":
      return "Sal en";
    case "turn":
      if (modifier === "left") return "Gira a la izquierda en";
      if (modifier === "right") return "Gira a la derecha en";
      if (modifier === "straight") return "Contin\xFAa por";
      return "Gira en";
    case "new name":
      return "Contin\xFAa por";
    case "merge":
      return "Incorp\xF3rate en";
    case "on ramp":
      return "Toma la rampa hacia";
    case "off ramp":
      return "Sal hacia";
    case "fork":
      return "Mantente en";
    case "end of road":
      return "Al final de la v\xEDa gira en";
    case "roundabout":
    case "rotary":
      return "En la rotonda toma la salida por";
    default:
      return "Contin\xFAa por";
  }
}
function formatMeters(m) {
  if (!m) return "";
  return m >= 1e3 ? `${(m / 1e3).toFixed(1)} km` : `${Math.round(m)} m`;
}
function formatSeconds(s) {
  if (!s) return "";
  return `${Math.max(1, Math.round(s / 60))} min`;
}
async function geocodeAddress(address) {
  const normalized = address.toLowerCase().trim();
  const cacheKey = getCacheKey("geocoding", { address: normalized });
  const cached = getFromCache(cacheKey, TTL.geocoding);
  if (cached) {
    console.log(`\u{1F5FA}\uFE0F [GoogleMaps Cache HIT] Geocoding`);
    return cached;
  }
  await ensureGeocodeCacheTable();
  if (geocodeCacheReady) {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      const [rows] = await db2.execute(
        sql23`SELECT lat, lng, formatted_address FROM geocode_cache WHERE address_key = ${normalized} LIMIT 1`
      );
      const row = rows?.[0];
      if (row) {
        const result = {
          lat: Number(row.lat),
          lng: Number(row.lng),
          formattedAddress: row.formatted_address || "",
          placeId: ""
        };
        setCache(cacheKey, result);
        return result;
      }
    } catch (err) {
      console.error("\u274C [GoogleMaps] Error leyendo geocode_cache:", err);
    }
  }
  if (!checkDailyLimit("geocoding") || !checkRateLimit("geocoding")) {
    console.warn(`\u26A0\uFE0F [GoogleMaps] Geocoding omitida por l\xEDmite (${normalized})`);
    return null;
  }
  if (!API_KEY) return null;
  try {
    const url = `https://maps.googleapis.com/maps/api/geocode/json?address=${encodeURIComponent(address)}&key=${API_KEY}`;
    const response = await fetch(url, { signal: AbortSignal.timeout(5e3) });
    const data = await response.json();
    if (data.status !== "OK" || !data.results?.length) {
      return null;
    }
    const first = data.results[0];
    if (first.partial_match === true) {
      console.warn(`\u26A0\uFE0F [GoogleMaps] Geocoding parcial (${normalized}) \u2014 descartado`);
      return null;
    }
    const result = {
      lat: first.geometry.location.lat,
      lng: first.geometry.location.lng,
      formattedAddress: first.formatted_address,
      placeId: first.place_id
    };
    setCache(cacheKey, result);
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      await db2.execute(sql23`
        INSERT INTO geocode_cache (address_key, lat, lng, formatted_address)
        VALUES (${normalized}, ${result.lat}, ${result.lng}, ${result.formattedAddress})
        ON DUPLICATE KEY UPDATE lat = VALUES(lat), lng = VALUES(lng)
      `);
    } catch (err) {
      console.error("\u274C [GoogleMaps] Error guardando en geocode_cache:", err);
    }
    return result;
  } catch {
    return null;
  }
}
async function getDirectionsBatch(originLat, originLng, destinations) {
  const results = /* @__PURE__ */ new Map();
  const uncached = [];
  for (const dest of destinations) {
    const cacheKey = getCacheKey("directions", { originLat, originLng, destLat: dest.lat, destLng: dest.lng });
    const cached = getFromCache(cacheKey, TTL.directions);
    if (cached) {
      results.set(dest.id, cached);
    } else {
      uncached.push(dest);
    }
  }
  for (let i = 0; i < uncached.length; i++) {
    const dest = uncached[i];
    if (i > 0) {
      await new Promise((resolve) => setTimeout(resolve, 200));
    }
    const result = await getDirections(originLat, originLng, dest.lat, dest.lng);
    results.set(dest.id, result);
  }
  return results;
}
function calculateHaversineDistance(lat1, lng1, lat2, lng2) {
  const R = 6371;
  const dLat = (lat2 - lat1) * Math.PI / 180;
  const dLng = (lng2 - lng1) * Math.PI / 180;
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLng / 2) * Math.sin(dLng / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
function estimateDeliveryTimeMinutes(distanceKm) {
  const speedKmh = 25;
  const hours = distanceKm / speedKmh;
  return Math.max(5, Math.ceil(hours * 60));
}
function decodePolyline(encoded) {
  const poly = [];
  let index = 0;
  let lat = 0;
  let lng = 0;
  while (index < encoded.length) {
    let b;
    let shift = 0;
    let result = 0;
    do {
      b = encoded.charCodeAt(index++) - 63;
      result |= (b & 31) << shift;
      shift += 5;
    } while (b >= 32);
    lat += result & 1 ? ~(result >> 1) : result >> 1;
    shift = 0;
    result = 0;
    do {
      b = encoded.charCodeAt(index++) - 63;
      result |= (b & 31) << shift;
      shift += 5;
    } while (b >= 32);
    lng += result & 1 ? ~(result >> 1) : result >> 1;
    poly.push({ latitude: lat / 1e5, longitude: lng / 1e5 });
  }
  return poly;
}
async function getDistanceMatrix(origins, destinations) {
  const rows = [];
  const missing = [];
  for (let oi = 0; oi < origins.length; oi++) {
    rows[oi] = [];
    for (let di = 0; di < destinations.length; di++) {
      const cacheKey = getCacheKey("matrix", {
        oLat: origins[oi].lat,
        oLng: origins[oi].lng,
        dLat: destinations[di].lat,
        dLng: destinations[di].lng
      });
      const cached = getFromCache(cacheKey, TTL.distanceMatrix);
      if (cached) {
        rows[oi][di] = cached;
      } else {
        missing.push({ oi, di });
      }
    }
  }
  if (!missing.length) return rows;
  if (!API_KEY || !checkDailyLimit("matrix") || !checkRateLimit("distanceMatrix")) {
    for (const { oi, di } of missing) {
      const km = calculateHaversineDistance(
        origins[oi].lat,
        origins[oi].lng,
        destinations[di].lat,
        destinations[di].lng
      );
      const cell = {
        distanceMeters: Math.round(km * 1e3),
        durationSeconds: Math.max(120, Math.round(km / 25 * 3600))
      };
      rows[oi][di] = cell;
      setCache(
        getCacheKey("matrix", {
          oLat: origins[oi].lat,
          oLng: origins[oi].lng,
          dLat: destinations[di].lat,
          dLng: destinations[di].lng
        }),
        cell
      );
    }
    return rows;
  }
  try {
    const originsStr = origins.map((o) => `${o.lat.toFixed(4)},${o.lng.toFixed(4)}`).join("|");
    const destsStr = destinations.map((d) => `${d.lat.toFixed(4)},${d.lng.toFixed(4)}`).join("|");
    const url = `https://maps.googleapis.com/maps/api/distancematrix/json?origins=${encodeURIComponent(originsStr)}&destinations=${encodeURIComponent(destsStr)}&mode=driving&language=es&key=${API_KEY}`;
    const response = await fetch(url, { signal: AbortSignal.timeout(8e3) });
    const data = await response.json();
    if (data.status !== "OK" || !data.rows) throw new Error(data.status);
    for (let oi = 0; oi < data.rows.length; oi++) {
      for (let di = 0; di < data.rows[oi].elements.length; di++) {
        const el = data.rows[oi].elements[di];
        const cell = el.status === "OK" ? {
          distanceMeters: el.distance?.value ?? null,
          durationSeconds: el.duration?.value ?? null
        } : null;
        if (cell && cell.distanceMeters != null) {
          rows[oi][di] = cell;
          setCache(
            getCacheKey("matrix", {
              oLat: origins[oi].lat,
              oLng: origins[oi].lng,
              dLat: destinations[di].lat,
              dLng: destinations[di].lng
            }),
            cell
          );
        }
      }
    }
  } catch (e) {
    console.warn("\u26A0\uFE0F [GoogleMaps] Distance Matrix fall\xF3, usando Haversine:", e);
    for (const { oi, di } of missing) {
      const km = calculateHaversineDistance(
        origins[oi].lat,
        origins[oi].lng,
        destinations[di].lat,
        destinations[di].lng
      );
      rows[oi][di] = {
        distanceMeters: Math.round(km * 1e3),
        durationSeconds: Math.max(120, Math.round(km / 25 * 3600))
      };
    }
  }
  return rows;
}
async function placesAutocomplete(input, bias) {
  const normalized = input.trim().toLowerCase();
  if (normalized.length < 3) return [];
  const biasLat = bias ? Math.round(bias.lat * 100) / 100 : null;
  const biasLng = bias ? Math.round(bias.lng * 100) / 100 : null;
  const cacheKey = getCacheKey("places", { input: normalized, biasLat, biasLng });
  const cached = getFromCache(cacheKey, TTL.places);
  if (cached) return cached;
  if (!API_KEY || !checkDailyLimit("places") || !checkRateLimit("places")) {
    return [];
  }
  try {
    let url = `https://maps.googleapis.com/maps/api/place/autocomplete/json?input=${encodeURIComponent(input)}&language=es&components=country:es&key=${API_KEY}`;
    if (bias?.lat != null && bias?.lng != null) {
      url += `&location=${bias.lat},${bias.lng}&radius=${bias.radiusM || 3e4}`;
    }
    const response = await fetch(url, { signal: AbortSignal.timeout(5e3) });
    const data = await response.json();
    if (data.status !== "OK" || !data.predictions?.length) return [];
    const predictions = data.predictions.slice(0, 5).map((p) => ({
      description: p.description,
      mainText: p.structured_formatting?.main_text || p.description,
      secondaryText: p.structured_formatting?.secondary_text || "",
      placeId: p.place_id
    }));
    setCache(cacheKey, predictions);
    return predictions;
  } catch {
    return [];
  }
}
function getUsageStats() {
  const stats = {};
  const now = Date.now();
  for (const [endpoint, counter] of rateCounters.entries()) {
    const limits = RATE_LIMITS[endpoint] || RATE_LIMITS.all;
    stats[endpoint] = {
      used: counter.count,
      limit: limits.maxRequests,
      remaining: Math.max(0, limits.maxRequests - counter.count),
      resetIn: Math.max(0, Math.ceil((counter.resetAt - now) / 1e3))
    };
  }
  return {
    cacheSize: cache2.size,
    rateLimiters: stats,
    apiKeyConfigured: !!API_KEY
  };
}
var API_KEY, cache2, TTL, rateCounters, RATE_LIMITS, dailyCounters, DAILY_LIMITS, geocodeCacheReady, routeCacheReady, routeStats, routeCacheHitsDb, OSRM_MIRRORS, googleMapsService;
var init_googleMapsService = __esm({
  "server/services/googleMapsService.ts"() {
    "use strict";
    API_KEY = process.env.GOOGLE_MAPS_API_KEY || process.env.EXPO_PUBLIC_GOOGLE_MAPS_API_KEY || process.env.GOOGLE_MAPS_WEB_KEY || process.env.EXPO_PUBLIC_GOOGLE_MAPS_WEB_API_KEY || "";
    cache2 = /* @__PURE__ */ new Map();
    TTL = {
      // Directions: cache largo (las calles no cambian frecuentemente)
      directions: 30 * 60 * 1e3,
      // 30 minutos
      // Geocoding: cache muy largo (las direcciones no cambian)
      geocoding: 24 * 60 * 60 * 1e3,
      // 24 horas
      // Distance Matrix: cache medio
      distanceMatrix: 15 * 60 * 1e3,
      // 15 minutos
      // Places: cache medio
      places: 60 * 60 * 1e3
      // 1 hora
    };
    rateCounters = /* @__PURE__ */ new Map();
    RATE_LIMITS = {
      directions: { maxRequests: Number(process.env.GOOGLE_DIRECTIONS_RATE_LIMIT || 120), windowMs: 60 * 1e3 },
      // 120 req/min
      geocoding: { maxRequests: 40, windowMs: 60 * 1e3 },
      // 40 req/min
      distanceMatrix: { maxRequests: 20, windowMs: 60 * 1e3 },
      // 20 req/min
      places: { maxRequests: 20, windowMs: 60 * 1e3 },
      // 20 req/min
      // OSRM es GRATIS: su contador es propio y NUNCA queda bloqueado por los
      // límites de Google (antes el límite de directions lo cortaba también)
      osrm: { maxRequests: 90, windowMs: 60 * 1e3 },
      // 90 req/min
      all: { maxRequests: 150, windowMs: 60 * 1e3 }
      // Global: 150 req/min total
    };
    dailyCounters = /* @__PURE__ */ new Map();
    DAILY_LIMITS = {
      geocoding: Number(process.env.GOOGLE_GEOCODE_DAILY_LIMIT || 500),
      directions: Number(process.env.GOOGLE_DIRECTIONS_DAILY_LIMIT || 5e3),
      matrix: Number(process.env.GOOGLE_MATRIX_DAILY_LIMIT || 1e3),
      places: Number(process.env.GOOGLE_PLACES_DAILY_LIMIT || 500)
    };
    setInterval(() => {
      const now = Date.now();
      for (const [key, entry] of cache2.entries()) {
        if (now - entry.timestamp > 36e5) {
          cache2.delete(key);
        }
      }
    }, 30 * 60 * 1e3);
    geocodeCacheReady = false;
    routeCacheReady = false;
    routeStats = {
      dbHits: 0,
      failed: 0,
      rateLimited: 0,
      dailyLimited: 0
    };
    routeCacheHitsDb = 0;
    OSRM_MIRRORS = {
      driving: [
        "https://router.project-osrm.org",
        "https://routing.openstreetmap.de/routed-car"
      ],
      walking: ["https://routing.openstreetmap.de/routed-foot"]
    };
    googleMapsService = {
      getDirections,
      geocodeAddress,
      getDirectionsBatch,
      getDistanceMatrix,
      placesAutocomplete,
      calculateHaversineDistance,
      estimateDeliveryTimeMinutes,
      decodePolyline,
      getUsageStats
    };
  }
});

// server/dbHelper.ts
var dbHelper_exports = {};
__export(dbHelper_exports, {
  executeWithRetry: () => executeWithRetry,
  queryWithRetry: () => queryWithRetry
});
async function executeWithRetry(queryFn, maxRetries = 3) {
  let lastError;
  for (let attempt = 1; attempt <= maxRetries; attempt++) {
    let conn;
    try {
      console.log(`\u{1F504} Database query attempt ${attempt}/${maxRetries}`);
      conn = await connection.getConnection();
      const result = await queryFn(conn);
      console.log(`\u2705 Query successful on attempt ${attempt}`);
      return result;
    } catch (error) {
      lastError = error;
      console.error(`\u274C Attempt ${attempt} failed:`, error.message, error.code);
      if (error.code === "ECONNRESET" || error.code === "ETIMEDOUT" || error.code === "PROTOCOL_CONNECTION_LOST") {
        if (attempt < maxRetries) {
          const delay = Math.min(1e3 * Math.pow(2, attempt - 1), 5e3);
          console.log(`\u23F3 Retrying in ${delay}ms...`);
          await new Promise((resolve) => setTimeout(resolve, delay));
        }
      } else {
        throw error;
      }
    } finally {
      if (conn) {
        try {
          conn.release();
        } catch (releaseError) {
          console.error("Error releasing connection:", releaseError);
        }
      }
    }
  }
  console.error("\u274C All retry attempts exhausted");
  throw lastError;
}
async function queryWithRetry(sql23, params = []) {
  return executeWithRetry(async (conn) => {
    const [rows] = await conn.query(sql23, params);
    return rows;
  });
}
var init_dbHelper = __esm({
  "server/dbHelper.ts"() {
    "use strict";
    init_db();
  }
});

// server/utils/coordinates.ts
var coordinates_exports = {};
__export(coordinates_exports, {
  isValidLatLng: () => isValidLatLng
});
function isValidLatLng(lat, lng) {
  const la = Number(lat);
  const ln = Number(lng);
  return Number.isFinite(la) && Number.isFinite(ln) && la !== 0 && ln !== 0 && Math.abs(la) <= 90 && Math.abs(ln) <= 180;
}
var init_coordinates = __esm({
  "server/utils/coordinates.ts"() {
    "use strict";
  }
});

// server/stripeClient.ts
var stripeClient_exports = {};
__export(stripeClient_exports, {
  getStripe: () => getStripe2,
  mapStripeRequirements: () => mapStripeRequirements
});
function getStripe2() {
  if (!process.env.STRIPE_SECRET_KEY) {
    throw new Error("STRIPE_SECRET_KEY no configurado");
  }
  if (!stripeInstance) {
    stripeInstance = new import_stripe.default(process.env.STRIPE_SECRET_KEY, {
      apiVersion: "2026-02-25.clover"
    });
  }
  return stripeInstance;
}
function mapStripeRequirements(currentlyDue) {
  return (currentlyDue || []).map((r) => REQUIREMENT_LABELS[r] || r);
}
var import_stripe, stripeInstance, REQUIREMENT_LABELS;
var init_stripeClient = __esm({
  "server/stripeClient.ts"() {
    "use strict";
    import_stripe = __toESM(require("stripe"));
    stripeInstance = null;
    REQUIREMENT_LABELS = {
      "individual.first_name": "Nombre del titular",
      "individual.last_name": "Apellidos del titular",
      "individual.dob.day": "Fecha de nacimiento (d\xEDa)",
      "individual.dob.month": "Fecha de nacimiento (mes)",
      "individual.dob.year": "Fecha de nacimiento (a\xF1o)",
      "individual.address.line1": "Direcci\xF3n del titular",
      "individual.address.city": "Ciudad del titular",
      "individual.address.postal_code": "C\xF3digo postal",
      "individual.phone": "Tel\xE9fono del titular",
      "individual.email": "Email del titular",
      "individual.verification.document": "Documento de identidad",
      "business_profile.url": "Web del negocio"
    };
  }
});

// server/subscriptionService.ts
var subscriptionService_exports = {};
__export(subscriptionService_exports, {
  SubscriptionService: () => SubscriptionService
});
var import_drizzle_orm15, SubscriptionService;
var init_subscriptionService = __esm({
  "server/subscriptionService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm15 = require("drizzle-orm");
    SubscriptionService = class {
      // Planes hardcoded como fallback si la BD no tiene datos.
      // Estructura Soria 2026. Precios en céntimos: 499 = 4,99 €, etc.
      static PLANS_FALLBACK = {
        free: {
          name: "Free",
          price: 0,
          benefits: {
            freeDelivery: false,
            discountPercentage: 0,
            prioritySupport: false,
            exclusiveDeals: false,
            noMinimumOrder: false
          }
        },
        soria_local: {
          name: "Plan Soria Local",
          price: 499,
          // 4,99 €/mes
          benefits: {
            freeDelivery: true,
            discountPercentage: 50,
            prioritySupport: false,
            exclusiveDeals: false,
            noMinimumOrder: false
          }
        },
        impulso_local: {
          name: "Impulso Local",
          price: 2900,
          // 29 €/mes + comisión 10%
          benefits: {
            freeDelivery: false,
            discountPercentage: 0,
            prioritySupport: false,
            exclusiveDeals: false,
            noMinimumOrder: false
          }
        },
        top_soria: {
          name: "Top Soria",
          price: 7900,
          // 79 €/mes
          benefits: {
            freeDelivery: false,
            discountPercentage: 0,
            prioritySupport: true,
            exclusiveDeals: true,
            noMinimumOrder: false
          }
        },
        premium_soria: {
          name: "Premium Soria",
          price: 9900,
          // 99 €/mes
          benefits: {
            freeDelivery: false,
            discountPercentage: 0,
            prioritySupport: true,
            exclusiveDeals: true,
            noMinimumOrder: false
          }
        },
        logistica_local: {
          name: "Log\xEDstica Local (B2B)",
          price: 3900,
          // 39 €/mes + 3,50 € por entrega
          benefits: {
            freeDelivery: false,
            discountPercentage: 0,
            prioritySupport: false,
            exclusiveDeals: false,
            noMinimumOrder: false
          }
        },
        escaparate_soria: {
          name: "Escaparate Soria",
          price: 1900,
          // 19 €/mes + comisión 8%
          benefits: {
            freeDelivery: false,
            discountPercentage: 0,
            prioritySupport: false,
            exclusiveDeals: false,
            noMinimumOrder: false
          }
        },
        express_semana: {
          name: "Express Semanal (Eventos)",
          price: 4900,
          // 49 €/semana
          benefits: {
            freeDelivery: false,
            discountPercentage: 0,
            prioritySupport: true,
            exclusiveDeals: false,
            noMinimumOrder: false
          }
        }
      };
      // Leer planes desde BD (con fallback)
      static async getPlansFromDB() {
        try {
          const plans = await db.select().from(subscriptionPlans).orderBy(subscriptionPlans.displayOrder);
          const benefits = await db.select().from(subscriptionBenefits);
          if (!plans.length) return this.PLANS_FALLBACK;
          const result = {};
          for (const plan of plans) {
            const planBenefits = benefits.filter((b) => b.plan === plan.planKey);
            const discountBenefit = planBenefits.find(
              (b) => b.benefitType === "discount"
            );
            const active = (b) => (b.benefitValue ?? 0) > 0;
            result[plan.planKey] = {
              name: plan.name,
              price: plan.price,
              // en centavos
              description: plan.description,
              color: plan.color,
              icon: plan.icon,
              billingCycle: plan.billingCycle || "monthly",
              benefits: {
                freeDelivery: planBenefits.some(
                  (b) => b.benefitType === "free_delivery" && active(b)
                ),
                discountPercentage: discountBenefit ? discountBenefit.benefitValue ?? 0 : 0,
                prioritySupport: planBenefits.some(
                  (b) => b.benefitType === "priority_support" && active(b)
                ),
                exclusiveDeals: planBenefits.some(
                  (b) => b.benefitType === "exclusive_deals" && active(b)
                ),
                noMinimumOrder: planBenefits.some(
                  (b) => b.benefitType === "no_minimum" && active(b)
                )
              },
              benefitsList: planBenefits.map((b) => ({
                id: b.id,
                type: b.benefitType,
                value: b.benefitValue,
                description: b.description
              }))
            };
          }
          return result;
        } catch {
          return this.PLANS_FALLBACK;
        }
      }
      // Compatibilidad: PLANS como getter que devuelve fallback
      static get PLANS() {
        return this.PLANS_FALLBACK;
      }
      // Suscripción ACTIVA del usuario (status active y período vigente), o null
      static async getActiveSubscription(userId) {
        const [subscription] = await db.select().from(subscriptions).where((0, import_drizzle_orm15.eq)(subscriptions.userId, userId)).limit(1);
        if (!subscription || subscription.status !== "active" || subscription.currentPeriodEnd && subscription.currentPeriodEnd < /* @__PURE__ */ new Date()) {
          return null;
        }
        return subscription;
      }
      // Tasa de comisión de la plataforma para un negocio (0-1).
      // Prioridad: suscripción activa (impulso_local 10%, escaparate_soria 8%),
      // luego comisión personalizada del admin, luego 15% por defecto.
      static async getBusinessCommissionRate(businessId) {
        try {
          const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          const [business] = await db.select().from(businesses3).where((0, import_drizzle_orm15.eq)(businesses3.id, businessId)).limit(1);
          if (!business) return 0.15;
          if (business.ownerId) {
            const sub = await this.getActiveSubscription(business.ownerId);
            if (sub?.plan === "impulso_local") return 0.1;
            if (sub?.plan === "escaparate_soria") return 0.08;
          }
          if (business.customCommission != null && business.customCommission > 0) {
            return business.customCommission / 100;
          }
          return 0.15;
        } catch {
          return 0.15;
        }
      }
      // Efectos al activar un plan (Top/Premium → negocio destacado)
      static async applyPlanActivationSideEffects(userId, plan) {
        try {
          if (!["top_soria", "premium_soria", "express_semana"].includes(plan)) {
            return;
          }
          const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          await db.update(businesses3).set({ isFeatured: true }).where((0, import_drizzle_orm15.eq)(businesses3.ownerId, userId));
        } catch (err) {
          console.error("Error applying plan activation side effects:", err);
        }
      }
      // Revertir efectos del plan al expirar o cancelar (quitar destacado)
      static async removePlanSideEffects(userId, plan) {
        try {
          if (!["top_soria", "premium_soria", "express_semana"].includes(plan)) {
            return;
          }
          const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          await db.update(businesses3).set({ isFeatured: false }).where((0, import_drizzle_orm15.eq)(businesses3.ownerId, userId));
        } catch (err) {
          console.error("Error removing plan side effects:", err);
        }
      }
      // Obtener suscripción del usuario
      static async getUserSubscription(userId) {
        const [subscription] = await db.select().from(subscriptions).where((0, import_drizzle_orm15.eq)(subscriptions.userId, userId)).limit(1);
        if (!subscription) {
          throw new Error("No se encontr\xF3 suscripci\xF3n para el usuario");
        }
        if (subscription.status === "pending_payment") {
          return {
            ...subscription,
            benefits: this.PLANS_FALLBACK[subscription.plan]?.benefits || this.PLANS_FALLBACK.free.benefits
          };
        }
        const now = /* @__PURE__ */ new Date();
        if (subscription.currentPeriodEnd && subscription.currentPeriodEnd < now && subscription.status === "active") {
          await db.update(subscriptions).set({ status: "expired" }).where((0, import_drizzle_orm15.eq)(subscriptions.id, subscription.id));
          await this.removePlanSideEffects(userId, subscription.plan);
          return {
            plan: "free",
            status: "expired",
            benefits: this.PLANS_FALLBACK.free.benefits
          };
        }
        const plans = await this.getPlansFromDB();
        const planData = plans[subscription.plan] || plans["free"] || this.PLANS_FALLBACK.free;
        const planBenefits = subscription.status === "active" ? planData.benefits : this.PLANS_FALLBACK.free.benefits;
        return { ...subscription, benefits: planBenefits, planDetails: planData };
      }
      // Iniciar suscripción — crea registro pending_payment, el usuario debe pagar y subir comprobante
      static async initSubscription(userId, plan, billingCycle = "monthly") {
        const plans = await this.getPlansFromDB();
        const planData = plans[plan];
        if (!planData) throw new Error("Plan inv\xE1lido");
        const [existing] = await db.select().from(subscriptions).where((0, import_drizzle_orm15.eq)(subscriptions.userId, userId)).limit(1);
        const now = /* @__PURE__ */ new Date();
        const periodEnd = new Date(now);
        if (billingCycle === "weekly") {
          periodEnd.setDate(periodEnd.getDate() + 7);
        } else if (billingCycle === "yearly") {
          periodEnd.setFullYear(periodEnd.getFullYear() + 1);
        } else {
          periodEnd.setMonth(periodEnd.getMonth() + 1);
        }
        if (existing) {
          if (existing.status === "active" && existing.plan === plan) {
            return {
              success: true,
              subscriptionId: existing.id,
              amount: planData.price,
              plan,
              alreadyActive: true
            };
          }
          await db.update(subscriptions).set({
            plan,
            status: "pending_payment",
            price: planData.price,
            billingCycle,
            autoRenew: true,
            currentPeriodStart: now,
            currentPeriodEnd: periodEnd,
            cancelledAt: null
          }).where((0, import_drizzle_orm15.eq)(subscriptions.id, existing.id));
          return {
            success: true,
            subscriptionId: existing.id,
            amount: planData.price,
            plan
          };
        } else {
          const { randomUUID: randomUUID5 } = await import("crypto");
          const id = randomUUID5();
          await db.insert(subscriptions).values({
            id,
            userId,
            plan,
            status: "pending_payment",
            price: planData.price,
            billingCycle,
            autoRenew: true,
            currentPeriodStart: now,
            currentPeriodEnd: periodEnd
          });
          return {
            success: true,
            subscriptionId: id,
            amount: planData.price,
            plan
          };
        }
      }
      // ELIMINADO: subscribe() activaba sin cobrar. Usar initSubscription() + confirmación de pago.
      static async subscribe(_userId, _plan, _billingCycle) {
        throw new Error(
          "Uso incorrecto: usa initSubscription() y confirma el pago antes de activar."
        );
      }
      // Cancelar suscripción
      static async cancelSubscription(userId) {
        const [subscription] = await db.select().from(subscriptions).where((0, import_drizzle_orm15.eq)(subscriptions.userId, userId)).limit(1);
        if (!subscription) {
          throw new Error("No tienes ninguna suscripci\xF3n");
        }
        if (subscription.status !== "active") {
          throw new Error(
            "Solo puedes cancelar una suscripci\xF3n activa. Estado actual: " + subscription.status
          );
        }
        if (!subscription.autoRenew && subscription.cancelledAt) {
          return {
            success: true,
            alreadyCancelled: true,
            message: "Tu suscripci\xF3n ya estaba programada para cancelarse al final del per\xEDodo",
            periodEnd: subscription.currentPeriodEnd
          };
        }
        await db.update(subscriptions).set({
          autoRenew: false,
          cancelledAt: /* @__PURE__ */ new Date()
        }).where((0, import_drizzle_orm15.eq)(subscriptions.id, subscription.id));
        return {
          success: true,
          message: "Suscripci\xF3n cancelada. Seguir\xE1s teniendo acceso hasta el final del per\xEDodo",
          periodEnd: subscription.currentPeriodEnd
        };
      }
      static async applySubscriptionBenefits(userId, orderTotal, deliveryFee) {
        try {
          const subscription = await this.getUserSubscription(userId);
          if (subscription.plan === "free" || subscription.status !== "active") {
            return {
              discount: 0,
              deliveryFee,
              appliedBenefits: [],
              freeDelivery: false,
              discountPercentage: 0
            };
          }
          const benefits = await db.select().from(subscriptionBenefits).where((0, import_drizzle_orm15.eq)(subscriptionBenefits.plan, subscription.plan));
          const appliedBenefits = [];
          let finalDeliveryFee = deliveryFee;
          let discount = 0;
          let discountPercentage = 0;
          let freeDelivery = false;
          if (subscription.plan === "soria_local") {
            const minOrderBenefit = benefits.find(
              (b) => b.benefitType === "min_order"
            );
            const minOrder = minOrderBenefit?.benefitValue ?? 1500;
            if (orderTotal >= minOrder) {
              const periodStart = subscription.currentPeriodStart || /* @__PURE__ */ new Date(0);
              const ordersThisPeriod = await db.select().from(orders).where(
                (0, import_drizzle_orm15.and)(
                  (0, import_drizzle_orm15.eq)(orders.userId, userId),
                  (0, import_drizzle_orm15.gte)(orders.createdAt, periodStart)
                )
              );
              const freeLimit = benefits.find((b) => b.benefitType === "free_delivery")?.benefitValue ?? 4;
              if (ordersThisPeriod.length < freeLimit) {
                finalDeliveryFee = 0;
                freeDelivery = true;
                appliedBenefits.push("Env\xEDo gratis (Plan Soria Local)");
              } else {
                finalDeliveryFee = Math.round(deliveryFee * 0.5);
                appliedBenefits.push("50% de descuento en el env\xEDo (Plan Soria Local)");
              }
            }
          }
          for (const b of benefits) {
            const val = b.benefitValue ?? 0;
            if (val <= 0) continue;
            switch (b.benefitType) {
              case "free_delivery":
                if (subscription.plan === "soria_local") break;
                finalDeliveryFee = 0;
                freeDelivery = true;
                appliedBenefits.push(b.description || "Env\xEDo gratis");
                break;
              case "min_order":
                break;
              // solo informativo
              case "discount":
              case "discount_percentage":
                if (subscription.plan === "soria_local") break;
                discountPercentage = val;
                discount = Math.round(orderTotal * (val / 100));
                appliedBenefits.push(b.description || `${val}% descuento`);
                break;
              case "priority_support":
              case "exclusive_deals":
              case "no_minimum":
              case "analytics":
              case "featured":
              case "priority":
              case "commission_rate":
              case "flat_delivery_fee":
              case "delivery_window":
              case "image_design":
              default:
                if (b.description) appliedBenefits.push(b.description);
                break;
            }
          }
          return {
            discount,
            deliveryFee: finalDeliveryFee,
            appliedBenefits,
            freeDelivery,
            discountPercentage
          };
        } catch {
          return {
            discount: 0,
            deliveryFee,
            appliedBenefits: [],
            freeDelivery: false,
            discountPercentage: 0
          };
        }
      }
      // Renovar suscripciones vencidas (cron job)
      static async renewSubscriptions() {
        const now = /* @__PURE__ */ new Date();
        const expiredSubs = await db.select().from(subscriptions).where(
          (0, import_drizzle_orm15.and)(
            (0, import_drizzle_orm15.eq)(subscriptions.status, "active"),
            (0, import_drizzle_orm15.eq)(subscriptions.autoRenew, true)
          )
        );
        const renewed = [];
        for (const sub of expiredSubs) {
          if (sub.currentPeriodEnd && sub.currentPeriodEnd < now) {
            const newPeriodEnd = new Date(sub.currentPeriodEnd);
            if (sub.billingCycle === "monthly") {
              newPeriodEnd.setMonth(newPeriodEnd.getMonth() + 1);
            } else {
              newPeriodEnd.setFullYear(newPeriodEnd.getFullYear() + 1);
            }
            await db.update(subscriptions).set({
              currentPeriodStart: sub.currentPeriodEnd,
              currentPeriodEnd: newPeriodEnd
            }).where((0, import_drizzle_orm15.eq)(subscriptions.id, sub.id));
            renewed.push(sub.id);
          }
        }
        return { success: true, renewed: renewed.length };
      }
      // Descuento de comisión para dueños de negocio con suscripción Business
      static async getBusinessCommissionDiscount(userId) {
        const BASE = 0.15;
        try {
          const sub = await this.getUserSubscription(userId);
          if (sub.plan == "free" || sub.status != "active") {
            return { commissionRate: BASE, baseRate: BASE, discountPercent: 0, plan: sub.plan };
          }
          const benefits = await db.select().from(subscriptionBenefits).where((0, import_drizzle_orm15.eq)(subscriptionBenefits.plan, sub.plan));
          let discountPercent = 0;
          for (const b of benefits) {
            if (b.benefitType == "reduced_commission" && b.benefitValue && b.benefitValue > 0) {
              discountPercent = b.benefitValue;
              break;
            }
          }
          return {
            commissionRate: BASE - BASE * discountPercent / 100,
            baseRate: BASE,
            discountPercent,
            plan: sub.plan
          };
        } catch {
          return { commissionRate: BASE, baseRate: BASE, discountPercent: 0, plan: "free" };
        }
      }
      // Obtener beneficios para negocios
      static async getBusinessBenefits(userId) {
        try {
          const subscription = await this.getUserSubscription(userId);
          if (subscription.plan === "free" || subscription.status !== "active") {
            return {
              commissionDiscount: 0,
              featuredListing: false,
              analyticsAccess: false,
              prioritySupport: false,
              promotionalTools: false,
              plan: "free"
            };
          }
          const benefits = await db.select().from(subscriptionBenefits).where((0, import_drizzle_orm15.eq)(subscriptionBenefits.plan, subscription.plan));
          const result = {
            commissionDiscount: 0,
            featuredListing: false,
            analyticsAccess: false,
            prioritySupport: false,
            promotionalTools: false,
            plan: subscription.plan
          };
          for (const b of benefits) {
            switch (b.benefitType) {
              case "reduced_commission":
                result.commissionDiscount = b.benefitValue ?? 0;
                break;
              case "featured_listing":
                result.featuredListing = (b.benefitValue ?? 0) > 0;
                break;
              case "analytics_access":
                result.analyticsAccess = (b.benefitValue ?? 0) > 0;
                break;
              case "priority_support":
                result.prioritySupport = (b.benefitValue ?? 0) > 0;
                break;
              case "promotional_tools":
                result.promotionalTools = (b.benefitValue ?? 0) > 0;
                break;
            }
          }
          return result;
        } catch {
          return {
            commissionDiscount: 0,
            featuredListing: false,
            analyticsAccess: false,
            prioritySupport: false,
            promotionalTools: false,
            plan: "free"
          };
        }
      }
    };
  }
});

// server/paymentService.ts
var paymentService_exports = {};
__export(paymentService_exports, {
  confirmPaymentIntent: () => confirmPaymentIntent,
  createPaymentIntent: () => createPaymentIntent,
  createSetupIntent: () => createSetupIntent,
  creditWallet: () => creditWallet,
  processDeliveredOrder: () => processDeliveredOrder,
  processSuccessfulPayment: () => processSuccessfulPayment
});
function getStripe3() {
  if (!stripeInstance2) {
    const key = process.env.STRIPE_SECRET_KEY;
    if (!key) {
      throw new Error(
        "Stripe is not configured. Please add STRIPE_SECRET_KEY."
      );
    }
    stripeInstance2 = new import_stripe2.default(key, {
      apiVersion: "2024-12-18.acacia"
    });
  }
  return stripeInstance2;
}
async function createPaymentIntent(params) {
  try {
    const { orderId, amount, customerId, businessId, driverId, paymentMethod } = params;
    const paymentIntent = await stripe.paymentIntents.create({
      amount: Math.round(amount * 100),
      // Convert to cents
      currency: "mxn",
      payment_method: paymentMethod,
      confirmation_method: "manual",
      confirm: paymentMethod ? true : false,
      metadata: {
        orderId,
        customerId,
        businessId,
        driverId: driverId || "",
        type: "order_payment"
      }
    });
    await db.insert(payments).values({
      id: paymentIntent.id,
      orderId,
      customerId,
      businessId,
      driverId,
      amount,
      currency: "MXN",
      status: paymentIntent.status,
      paymentMethod: paymentMethod || "card",
      stripePaymentIntentId: paymentIntent.id,
      createdAt: /* @__PURE__ */ new Date()
    });
    return {
      success: true,
      paymentIntent: {
        id: paymentIntent.id,
        clientSecret: paymentIntent.client_secret,
        status: paymentIntent.status
      }
    };
  } catch (error) {
    console.error("Create PaymentIntent error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
async function confirmPaymentIntent(paymentIntentId) {
  try {
    const paymentIntent = await stripe.paymentIntents.confirm(paymentIntentId);
    await db.update(payments).set({
      status: paymentIntent.status,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm16.eq)(payments.stripePaymentIntentId, paymentIntentId));
    return {
      success: true,
      status: paymentIntent.status
    };
  } catch (error) {
    console.error("Confirm PaymentIntent error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
async function creditWallet(userId, amount, type, orderId, description) {
  return await db.transaction(async (tx) => {
    let [wallet] = await tx.select().from(wallets).where((0, import_drizzle_orm16.eq)(wallets.userId, userId)).limit(1);
    if (!wallet) {
      const result = await tx.insert(wallets).values({
        userId,
        balance: 0,
        pendingBalance: 0,
        totalEarned: 0,
        totalWithdrawn: 0,
        cashOwed: 0
      }).$returningId();
      const newId = Array.isArray(result) ? result[0].id : result.insertId;
      [wallet] = await tx.select().from(wallets).where((0, import_drizzle_orm16.eq)(wallets.id, newId)).limit(1);
    }
    const newBalance = (wallet?.balance || 0) + amount;
    await tx.update(wallets).set({
      balance: newBalance,
      totalEarned: (wallet?.totalEarned || 0) + amount,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm16.eq)(wallets.userId, userId));
    await tx.insert(transactions).values({
      walletId: wallet.id,
      userId,
      amount,
      type,
      status: "completed",
      description: description || `${type} - Orden ${orderId ?? ""}`.trim(),
      orderId,
      createdAt: /* @__PURE__ */ new Date()
    });
    return { success: true, newBalance };
  });
}
async function processSuccessfulPayment(paymentIntentId) {
  try {
    const [payment] = await db.select().from(payments).where((0, import_drizzle_orm16.eq)(payments.stripePaymentIntentId, paymentIntentId)).limit(1);
    if (!payment) {
      throw new Error("Payment not found");
    }
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm16.eq)(orders.id, payment.orderId)).limit(1);
    if (!order) {
      throw new Error(
        `Order ${payment.orderId} not found for payment ${paymentIntentId}`
      );
    }
    const commissions = await financialService.calculateCommissions(
      payment.amount,
      order.deliveryFee || 0,
      order.productosBase || void 0,
      order.nemyCommission || void 0
    );
    const platformAmount = commissions.platform;
    const businessAmount = commissions.business;
    const driverAmount = payment.driverId ? commissions.driver : 0;
    await financialService.updateWalletBalance(
      payment.businessId,
      businessAmount,
      "commission",
      payment.orderId,
      `Venta con tarjeta - Pedido ${payment.orderId}`
    );
    if (payment.driverId && driverAmount > 0) {
      await financialService.updateWalletBalance(
        payment.driverId,
        driverAmount,
        "delivery_fee",
        payment.orderId,
        `Tarifa de entrega - Pedido ${payment.orderId}`
      );
    }
    await db.update(orders).set({
      status: "paid",
      paidAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm16.eq)(orders.id, payment.orderId));
    await db.update(payments).set({
      status: "succeeded",
      processedAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm16.eq)(payments.id, payment.id));
    return {
      success: true,
      distribution: {
        platform: platformAmount,
        business: businessAmount,
        driver: driverAmount
      }
    };
  } catch (error) {
    console.error("Process successful payment error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
async function createSetupIntent(customerId) {
  try {
    const setupIntent = await stripe.setupIntents.create({
      customer: customerId,
      payment_method_types: ["card"],
      usage: "off_session"
    });
    return {
      success: true,
      setupIntent: {
        id: setupIntent.id,
        clientSecret: setupIntent.client_secret
      }
    };
  } catch (error) {
    console.error("Create SetupIntent error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
async function processDeliveredOrder(orderId) {
  try {
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm16.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      throw new Error("Order not found");
    }
    const [payment] = await db.select().from(payments).where((0, import_drizzle_orm16.eq)(payments.orderId, orderId)).limit(1);
    if (payment && payment.status === "succeeded") {
      await db.update(orders).set({
        status: "delivered",
        deliveredAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm16.eq)(orders.id, orderId));
      return {
        success: true,
        message: "Order marked as delivered, funds already distributed"
      };
    }
    return {
      success: false,
      error: "Payment not found or not succeeded"
    };
  } catch (error) {
    console.error("Process delivered order error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
var import_stripe2, import_drizzle_orm16, stripeInstance2, stripe;
var init_paymentService = __esm({
  "server/paymentService.ts"() {
    "use strict";
    import_stripe2 = __toESM(require("stripe"));
    init_db();
    init_schema_mysql();
    import_drizzle_orm16 = require("drizzle-orm");
    init_unifiedFinancialService();
    stripeInstance2 = null;
    stripe = new Proxy({}, {
      get(_, prop) {
        return getStripe3()[prop];
      }
    });
  }
});

// server/cashPaymentService.ts
var cashPaymentService_exports = {};
__export(cashPaymentService_exports, {
  calculateChange: () => calculateChange,
  confirmCashDelivery: () => confirmCashDelivery,
  getCashPaymentDetails: () => getCashPaymentDetails,
  processCashPayment: () => processCashPayment
});
async function processCashPayment(params) {
  try {
    const { orderId, customerId, businessId, cashReceived, orderTotal } = params;
    if (cashReceived < orderTotal) {
      return {
        success: false,
        error: `Efectivo insuficiente. Se requieren ${orderTotal} \u20AC, recibido ${cashReceived} \u20AC`
      };
    }
    const change = cashReceived - orderTotal;
    const result = await db.insert(payments).values({
      orderId,
      customerId,
      businessId,
      amount: orderTotal,
      currency: "EUR",
      status: "succeeded",
      paymentMethod: "cash",
      processedAt: /* @__PURE__ */ new Date()
    });
    const paymentId = result[0].insertId.toString();
    await db.update(orders).set({
      status: "paid",
      paymentMethod: "cash",
      paidAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm17.eq)(orders.id, orderId));
    return {
      success: true,
      change,
      paymentId
    };
  } catch (error) {
    console.error("Process cash payment error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
async function confirmCashDelivery(orderId, driverId) {
  try {
    await db.update(orders).set({
      status: "delivered",
      deliveredAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm17.eq)(orders.id, orderId));
    await processCashCommissions(orderId);
    return {
      success: true,
      message: "Cash delivery confirmed"
    };
  } catch (error) {
    console.error("Confirm cash delivery error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
async function processCashCommissions(orderId) {
  try {
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm17.eq)(orders.id, orderId)).limit(1);
    if (!order) return;
    const commissions = await financialService.calculateCommissions(
      order.total,
      order.deliveryFee || 0,
      order.productosBase || void 0,
      order.nemyCommission || void 0
    );
    const platformAmount = commissions.platform;
    const businessAmount = commissions.business;
    const driverAmount = commissions.driver;
    const { creditWallet: creditWallet2 } = await Promise.resolve().then(() => (init_paymentService(), paymentService_exports));
    await creditWallet2(
      order.businessId,
      businessAmount,
      "cash_commission",
      orderId
    );
    if (order.deliveryPersonId) {
      await creditWallet2(
        order.deliveryPersonId,
        driverAmount,
        "cash_delivery_fee",
        orderId
      );
    }
    if (platformAmount > 0) {
      console.log(
        `\u2139\uFE0F Plataforma debe registrar $${platformAmount} para pedido ${orderId}`
      );
    }
    console.log(`\u{1F4B0} Cash commissions processed for order ${orderId}`);
  } catch (error) {
    console.error("Process cash commissions error:", error);
  }
}
async function getCashPaymentDetails(orderId) {
  try {
    const [payment] = await db.select().from(payments).where((0, import_drizzle_orm17.eq)(payments.orderId, orderId)).limit(1);
    if (!payment) {
      return {
        success: false,
        error: "Cash payment not found"
      };
    }
    return {
      success: true,
      payment
    };
  } catch (error) {
    console.error("Get cash payment details error:", error);
    return {
      success: false,
      error: error.message
    };
  }
}
async function calculateChange(orderTotal, cashReceived) {
  if (cashReceived < orderTotal) {
    return {
      success: false,
      error: "Efectivo insuficiente",
      shortage: orderTotal - cashReceived
    };
  }
  const change = cashReceived - orderTotal;
  const denominations = [500, 200, 100, 50, 20, 10, 5, 2, 1, 0.5];
  const changeBreakdown = {};
  let remainingChange = change;
  for (const denom of denominations) {
    if (remainingChange >= denom) {
      const count3 = Math.floor(remainingChange / denom);
      changeBreakdown[`$${denom}`] = count3;
      remainingChange = Math.round((remainingChange - count3 * denom) * 100) / 100;
    }
  }
  return {
    success: true,
    change,
    breakdown: changeBreakdown,
    message: change === 0 ? "Pago exacto" : `Cambio: $${change}`
  };
}
var import_drizzle_orm17;
var init_cashPaymentService = __esm({
  "server/cashPaymentService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm17 = require("drizzle-orm");
    init_unifiedFinancialService();
  }
});

// server/unifiedFinancialService.ts
var unifiedFinancialService_exports = {};
__export(unifiedFinancialService_exports, {
  UnifiedFinancialService: () => UnifiedFinancialService,
  financialService: () => financialService
});
var import_drizzle_orm18, UnifiedFinancialService, financialService;
var init_unifiedFinancialService = __esm({
  "server/unifiedFinancialService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm18 = require("drizzle-orm");
    init_subscriptionService();
    init_logger();
    UnifiedFinancialService = class _UnifiedFinancialService {
      static instance;
      cachedRates = null;
      cacheExpiry = 0;
      CACHE_DURATION = 5 * 60 * 1e3;
      // 5 minutes
      constructor() {
      }
      static getInstance() {
        if (!_UnifiedFinancialService.instance) {
          _UnifiedFinancialService.instance = new _UnifiedFinancialService();
        }
        return _UnifiedFinancialService.instance;
      }
      // Get commission rates with caching and validation
      async getCommissionRates() {
        const now = Date.now();
        if (this.cachedRates && now < this.cacheExpiry) {
          return this.cachedRates;
        }
        try {
          const settings = await db.select().from(systemSettings).where((0, import_drizzle_orm18.eq)(systemSettings.category, "commissions"));
          const platformRate = parseFloat(
            settings.find((s) => s.key === "platform_commission_rate")?.value || "0.15"
          );
          const businessRate = parseFloat(
            settings.find((s) => s.key === "business_commission_rate")?.value || "1.00"
          );
          const driverRate = parseFloat(
            settings.find((s) => s.key === "driver_commission_rate")?.value || "1.00"
          );
          if (platformRate < 0 || platformRate > 1) {
            throw new Error(
              `Platform commission (markup) must be between 0% and 100% of products. Current: ${(platformRate * 100).toFixed(2)}%`
            );
          }
          if (businessRate <= 0 || businessRate > 1) {
            throw new Error(
              `Business share must be between 0 and 1 (representa % del precio base de productos). Current: ${businessRate}`
            );
          }
          if (driverRate <= 0 || driverRate > 1) {
            throw new Error(
              `Driver share must be between 0 and 1 (representa % de la tarifa de entrega). Current: ${driverRate}`
            );
          }
          this.cachedRates = {
            platform: platformRate,
            business: businessRate,
            driver: driverRate
          };
          this.cacheExpiry = now + this.CACHE_DURATION;
          return this.cachedRates;
        } catch (error) {
          console.error("Error getting commission rates:", error);
          throw error;
        }
      }
      // Calculate commissions - Modelo: 100% producto al negocio, 15% del producto a ComeYa, 100% delivery fee al driver
      async calculateCommissions(totalAmount, deliveryFee = 0, productosBase, nemyCommission, businessOwnerId) {
        const safeTotal = Math.max(0, totalAmount || 0);
        const safeDeliveryFee = Math.max(0, deliveryFee || 0);
        let productBase = productosBase && productosBase > 0 ? productosBase : safeTotal - safeDeliveryFee;
        if (!productosBase || productosBase <= 0) {
          const baseWithoutDelivery = safeTotal - safeDeliveryFee;
          productBase = baseWithoutDelivery > 0 ? Math.round(baseWithoutDelivery / 1.15) : 0;
        }
        const platformAmount = nemyCommission && nemyCommission > 0 ? nemyCommission : Math.round(
          productBase * await this.getBusinessCommissionRate(businessOwnerId)
        );
        const businessAmount = productBase;
        const driverAmount = safeDeliveryFee;
        let distributedTotal = platformAmount + businessAmount + driverAmount;
        if (distributedTotal !== safeTotal) {
          const delta = safeTotal - distributedTotal;
          distributedTotal += delta;
          productBase += delta;
        }
        return {
          platform: platformAmount,
          business: productBase,
          driver: driverAmount,
          total: platformAmount + productBase + driverAmount
        };
      }
      // Update cash owed (for cash deliveries) with COMPLETE AUDIT TRAIL
      async updateCashOwed(userId, amount, orderId, description) {
        return await db.transaction(async (tx) => {
          let [wallet] = await tx.select().from(wallets).where((0, import_drizzle_orm18.eq)(wallets.userId, userId)).limit(1);
          if (!wallet) {
            throw new Error(`Wallet not found for user ${userId}`);
          }
          const newCashOwed = wallet.cashOwed + amount;
          await tx.update(wallets).set({
            cashOwed: newCashOwed,
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm18.eq)(wallets.userId, userId));
          await tx.insert(transactions).values({
            walletId: wallet.id,
            userId,
            orderId,
            type: "cash_debt",
            amount,
            balanceBefore: wallet.cashOwed,
            balanceAfter: newCashOwed,
            description: description || `Cash debt from order`,
            status: "completed",
            createdAt: /* @__PURE__ */ new Date(),
            metadata: JSON.stringify({
              previousDebt: wallet.cashOwed,
              newDebt: newCashOwed,
              debtIncrease: amount,
              timestamp: (/* @__PURE__ */ new Date()).toISOString(),
              source: "cash_delivery_system"
            })
          });
          logger.warn(
            `\u{1F4B5} Cash debt updated: User ${userId} - $${(amount / 100).toFixed(2)} - Total debt: $${(newCashOwed / 100).toFixed(2)}`,
            {
              userId,
              amount,
              orderId,
              previousDebt: wallet.cashOwed,
              newDebt: newCashOwed
            }
          );
        });
      }
      // Atomic wallet update with validation and COMPLETE TRANSACTION LOGGING
      async updateWalletBalance(userId, amount, type, orderId, description, usePendingBalance = false) {
        return await db.transaction(async (tx) => {
          const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          const [user] = await tx.select().from(users5).where((0, import_drizzle_orm18.eq)(users5.id, userId)).limit(1);
          if (!user) {
            throw new Error(`User ${userId} not found`);
          }
          let [wallet] = await tx.select().from(wallets).where((0, import_drizzle_orm18.eq)(wallets.userId, userId)).limit(1);
          if (!wallet) {
            await tx.insert(wallets).values({
              userId,
              balance: 0,
              pendingBalance: 0,
              totalEarned: 0,
              totalWithdrawn: 0
            });
            [wallet] = await tx.select().from(wallets).where((0, import_drizzle_orm18.eq)(wallets.userId, userId)).limit(1);
          }
          if (!wallet) {
            throw new Error(`Failed to create wallet for user ${userId}`);
          }
          const targetBalance = usePendingBalance ? wallet.pendingBalance : wallet.balance;
          const newBalance = targetBalance + amount;
          if (newBalance < 0) {
            throw new Error(
              `Insufficient balance. Current: ${targetBalance}, Requested: ${amount}`
            );
          }
          if (usePendingBalance) {
            await tx.update(wallets).set({
              pendingBalance: newBalance,
              totalEarned: amount > 0 ? wallet.totalEarned + amount : wallet.totalEarned,
              updatedAt: /* @__PURE__ */ new Date()
            }).where((0, import_drizzle_orm18.eq)(wallets.userId, userId));
          } else {
            await tx.update(wallets).set({
              balance: newBalance,
              totalEarned: amount > 0 ? wallet.totalEarned + amount : wallet.totalEarned,
              updatedAt: /* @__PURE__ */ new Date()
            }).where((0, import_drizzle_orm18.eq)(wallets.userId, userId));
          }
          await tx.insert(transactions).values({
            walletId: wallet.id,
            userId,
            orderId,
            type,
            amount,
            balanceBefore: targetBalance,
            balanceAfter: newBalance,
            description: description || `${type} transaction`,
            status: "completed",
            createdAt: /* @__PURE__ */ new Date(),
            metadata: JSON.stringify({
              userType: user.role,
              userName: user.name,
              timestamp: (/* @__PURE__ */ new Date()).toISOString(),
              source: "unified_financial_service",
              isPending: usePendingBalance
            })
          });
          const balanceType = usePendingBalance ? "pending" : "available";
          logger.info(
            `\u{1F4B0} Wallet updated (${balanceType}): ${user.name} (${userId}) - ${type} - $${(amount / 100).toFixed(2)}`,
            {
              userId,
              type,
              amount,
              orderId,
              balanceBefore: targetBalance,
              balanceAfter: newBalance,
              isPending: usePendingBalance
            }
          );
        });
      }
      // Validate order total calculation
      validateOrderTotal(subtotal, deliveryFee, tax, total) {
        const calculatedTotal = subtotal + deliveryFee + tax;
        return calculatedTotal === total;
      }
      // Convert between pesos and centavos safely
      pesosTocentavos(pesos) {
        return Math.round(pesos * 100);
      }
      centavosToPesos(centavos) {
        return Math.round(centavos) / 100;
      }
      // Clear cache (for testing or admin updates)
      clearCache() {
        this.cachedRates = null;
        this.cacheExpiry = 0;
      }
      // Get or create wallet for any user
      async getWallet(userId) {
        let [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm18.eq)(wallets.userId, userId)).limit(1);
        if (!wallet) {
          await db.insert(wallets).values({
            userId,
            balance: 0,
            pendingBalance: 0,
            totalEarned: 0,
            totalWithdrawn: 0,
            cashOwed: 0
          });
          [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm18.eq)(wallets.userId, userId)).limit(1);
        }
        return wallet;
      }
      // Check if user can withdraw
      async canUserWithdraw(userId, userRole) {
        if (!["business_owner", "delivery_driver", "admin"].includes(userRole)) {
          return {
            allowed: false,
            reason: "Solo negocios, repartidores y administradores pueden retirar"
          };
        }
        const wallet = await this.getWallet(userId);
        const MINIMUM_WITHDRAWAL2 = 5e3;
        const availableBalance = wallet.balance - (wallet.cashOwed || 0);
        if (availableBalance < MINIMUM_WITHDRAWAL2) {
          return {
            allowed: false,
            reason: `Saldo m\xEDnimo para retiro: $${MINIMUM_WITHDRAWAL2 / 100} MXN`
          };
        }
        if (wallet.cashOwed > 0) {
          return {
            allowed: false,
            reason: "Debes liquidar tu efectivo pendiente antes de retirar"
          };
        }
        return { allowed: true };
      }
      // Get available payment methods by role
      async getPaymentMethods(userId, userRole) {
        const methods = [];
        methods.push({
          id: "card",
          name: "Tarjeta",
          icon: "card-outline",
          available: true
        });
        methods.push({
          id: "cash",
          name: "Efectivo",
          icon: "cash-outline",
          available: true
        });
        const wallet = await this.getWallet(userId);
        methods.push({
          id: "wallet",
          name: "Billetera ComeYa",
          icon: "wallet-outline",
          available: wallet.balance > 0,
          balance: wallet.balance
        });
        return methods;
      }
      // Universal payment processor
      async processPayment(options) {
        const { userId, orderId, amount, method, businessId, driverId } = options;
        try {
          switch (method) {
            case "card":
              const { createPaymentIntent: createPaymentIntent2 } = await Promise.resolve().then(() => (init_paymentService(), paymentService_exports));
              return await createPaymentIntent2({
                orderId,
                amount,
                customerId: userId,
                businessId,
                driverId
              });
            case "cash":
              const { processCashPayment: processCashPayment2 } = await Promise.resolve().then(() => (init_cashPaymentService(), cashPaymentService_exports));
              return await processCashPayment2({
                orderId,
                customerId: userId,
                businessId,
                cashReceived: amount,
                orderTotal: amount
              });
            case "wallet":
              return await this.processWalletPayment(
                userId,
                orderId,
                amount,
                businessId,
                driverId
              );
            default:
              return { success: false, error: "M\xE9todo de pago no soportado" };
          }
        } catch (error) {
          return { success: false, error: error.message };
        }
      }
      // Process wallet payment
      async processWalletPayment(userId, orderId, amount, businessId, driverId) {
        return await db.transaction(async (tx) => {
          const [wallet] = await tx.select().from(wallets).where((0, import_drizzle_orm18.eq)(wallets.userId, userId)).limit(1);
          if (!wallet || wallet.balance < amount) {
            throw new Error("Saldo insuficiente en billetera");
          }
          await tx.update(wallets).set({
            balance: wallet.balance - amount,
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm18.eq)(wallets.userId, userId));
          const { payments: payments2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          await tx.insert(payments2).values({
            orderId,
            customerId: userId,
            businessId,
            driverId,
            amount,
            currency: "MXN",
            status: "succeeded",
            paymentMethod: "wallet",
            processedAt: /* @__PURE__ */ new Date()
          });
          await tx.insert(transactions).values({
            walletId: wallet.id,
            userId,
            orderId,
            type: "wallet_payment",
            amount: -amount,
            balanceBefore: wallet.balance,
            balanceAfter: wallet.balance - amount,
            description: `Pago con billetera - Pedido #${orderId.slice(-6)}`,
            status: "completed"
          });
          const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          await tx.update(orders2).set({
            status: "paid",
            paymentMethod: "wallet",
            paidAt: /* @__PURE__ */ new Date(),
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm18.eq)(orders2.id, orderId));
          return { success: true, message: "Pago procesado con billetera" };
        });
      }
      async getBusinessCommissionRate(ownerId) {
        if (!ownerId) return 0.15;
        try {
          const discount = await SubscriptionService.getBusinessCommissionDiscount(ownerId);
          return discount.commissionRate;
        } catch {
          return 0.15;
        }
      }
    };
    financialService = UnifiedFinancialService.getInstance();
  }
});

// server/systemSettingsService.ts
var systemSettingsService_exports = {};
__export(systemSettingsService_exports, {
  createSetting: () => createSetting,
  deleteSetting: () => deleteSetting,
  getAllSettings: () => getAllSettings,
  getPublicSettings: () => getPublicSettings,
  getSetting: () => getSetting,
  getSettingValue: () => getSettingValue,
  getSettingsByCategory: () => getSettingsByCategory,
  initializeDefaultSettings: () => initializeDefaultSettings,
  updateSetting: () => updateSetting
});
async function initializeDefaultSettings() {
  try {
    for (const setting of DEFAULT_SETTINGS) {
      const [existing] = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.key, setting.key)).limit(1);
      if (!existing) {
        await db.insert(systemSettings).values(setting);
      }
    }
    console.log("\u2705 Default settings initialized");
    return { success: true };
  } catch (error) {
    console.error("Error initializing settings:", error);
    return { success: false, error: error.message };
  }
}
async function getAllSettings() {
  try {
    const settings = await db.select().from(systemSettings);
    return { success: true, settings };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
async function getSettingsByCategory(category) {
  try {
    const settings = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.category, category));
    return { success: true, settings };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
async function getPublicSettings() {
  try {
    const settings = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.isPublic, true));
    return { success: true, settings };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
async function getSetting(key) {
  try {
    const [setting] = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.key, key)).limit(1);
    if (!setting) {
      return { success: false, error: "Setting not found" };
    }
    return { success: true, setting };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
async function updateSetting(params) {
  try {
    const [existing] = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.key, params.key)).limit(1);
    if (!existing) {
      return { success: false, error: "Setting not found" };
    }
    if (existing.type === "number" && isNaN(parseFloat(params.value))) {
      return { success: false, error: "Invalid number value" };
    }
    if (existing.type === "boolean" && !["true", "false"].includes(params.value)) {
      return { success: false, error: "Invalid boolean value" };
    }
    await db.update(systemSettings).set({
      value: params.value,
      updatedBy: params.updatedBy,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm21.eq)(systemSettings.key, params.key));
    await db.insert(auditLogs).values({
      userId: params.updatedBy,
      action: "update_setting",
      entityType: "system_setting",
      entityId: params.key,
      changes: JSON.stringify({
        key: params.key,
        oldValue: existing.value,
        newValue: params.value
      })
    });
    return { success: true };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
async function createSetting(params) {
  try {
    const [existing] = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.key, params.key)).limit(1);
    if (existing) {
      return { success: false, error: "Setting already exists" };
    }
    await db.insert(systemSettings).values({
      key: params.key,
      value: params.value,
      type: params.type,
      category: params.category,
      description: params.description,
      isPublic: params.isPublic || false,
      updatedBy: params.createdBy
    });
    await db.insert(auditLogs).values({
      userId: params.createdBy,
      action: "create_setting",
      entityType: "system_setting",
      entityId: params.key,
      changes: JSON.stringify(params)
    });
    return { success: true };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
async function deleteSetting(key, deletedBy) {
  try {
    const [existing] = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.key, key)).limit(1);
    if (!existing) {
      return { success: false, error: "Setting not found" };
    }
    await db.delete(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.key, key));
    await db.insert(auditLogs).values({
      userId: deletedBy,
      action: "delete_setting",
      entityType: "system_setting",
      entityId: key,
      changes: JSON.stringify(existing)
    });
    return { success: true };
  } catch (error) {
    return { success: false, error: error.message };
  }
}
async function getSettingValue(key, defaultValue = null) {
  try {
    const [setting] = await db.select().from(systemSettings).where((0, import_drizzle_orm21.eq)(systemSettings.key, key)).limit(1);
    if (!setting) return defaultValue;
    switch (setting.type) {
      case "number":
        return parseFloat(setting.value);
      case "boolean":
        return setting.value === "true";
      case "json":
        return JSON.parse(setting.value);
      default:
        return setting.value;
    }
  } catch (error) {
    return defaultValue;
  }
}
var import_drizzle_orm21, DEFAULT_SETTINGS;
var init_systemSettingsService = __esm({
  "server/systemSettingsService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm21 = require("drizzle-orm");
    DEFAULT_SETTINGS = [
      // Commissions
      {
        key: "platform_commission_rate",
        value: "0.15",
        type: "number",
        category: "commissions",
        description: "Markup de la plataforma sobre productos (15%)",
        isPublic: false
      },
      {
        key: "business_commission_rate",
        value: "1.00",
        type: "number",
        category: "commissions",
        description: "Porcentaje del precio base de productos que recibe el negocio (100%)",
        isPublic: false
      },
      {
        key: "driver_commission_rate",
        value: "1.00",
        type: "number",
        category: "commissions",
        description: "Porcentaje de la tarifa de entrega que recibe el repartidor (100%)",
        isPublic: false
      },
      // Payments
      {
        key: "min_withdrawal_amount",
        value: "10000",
        type: "number",
        category: "payments",
        description: "Monto m\xEDnimo de retiro (centavos)",
        isPublic: false
      },
      {
        key: "fund_hold_duration_hours",
        value: "0",
        type: "number",
        category: "payments",
        description: "Horas de retenci\xF3n de fondos (0 = pago inmediato)",
        isPublic: false
      },
      {
        key: "max_daily_transactions",
        value: "100",
        type: "number",
        category: "payments",
        description: "M\xE1ximo de transacciones diarias por usuario",
        isPublic: false
      },
      {
        key: "max_transaction_amount",
        value: "1000000",
        type: "number",
        category: "payments",
        description: "Monto m\xE1ximo por transacci\xF3n (centavos)",
        isPublic: false
      },
      // Operations
      {
        key: "delivery_base_fee",
        value: "300",
        type: "number",
        category: "operations",
        description: "Tarifa base de entrega (centavos de euro)",
        isPublic: true
      },
      {
        key: "delivery_fee_per_km",
        value: "50",
        type: "number",
        category: "operations",
        description: "Tarifa por kil\xF3metro (centavos de euro)",
        isPublic: true
      },
      {
        key: "delivery_min_fee",
        value: "300",
        type: "number",
        category: "operations",
        description: "Tarifa m\xEDnima de entrega (centavos de euro)",
        isPublic: true
      },
      {
        key: "delivery_max_fee",
        value: "500",
        type: "number",
        category: "operations",
        description: "Tarifa m\xE1xima de entrega (centavos de euro)",
        isPublic: true
      },
      {
        key: "business_default_delivery_fee",
        value: "300",
        type: "number",
        category: "operations",
        description: "Tarifa de entrega por defecto para nuevos negocios (centavos de euro)",
        isPublic: true
      },
      {
        key: "business_default_min_order",
        value: "1000",
        type: "number",
        category: "operations",
        description: "Pedido m\xEDnimo por defecto para nuevos negocios (centavos de euro)",
        isPublic: true
      },
      {
        key: "max_delivery_radius_km",
        value: "10",
        type: "number",
        category: "operations",
        description: "Radio m\xE1ximo de entrega (km)",
        isPublic: true
      },
      {
        key: "delivery_average_speed_kmh",
        value: "20",
        type: "number",
        category: "operations",
        description: "Velocidad promedio de repartidores (km/h)",
        isPublic: false
      },
      {
        key: "delivery_stop_time_minutes",
        value: "10",
        type: "number",
        category: "operations",
        description: "Tiempo promedio por parada de entrega (minutos)",
        isPublic: false
      },
      {
        key: "order_regret_period_seconds",
        value: "60",
        type: "number",
        category: "operations",
        description: "Per\xEDodo de arrepentimiento (segundos)",
        isPublic: true
      },
      {
        key: "pending_order_call_minutes",
        value: "3",
        type: "number",
        category: "operations",
        description: "Minutos antes de llamar al negocio",
        isPublic: false
      },
      {
        key: "max_simultaneous_orders",
        value: "10",
        type: "number",
        category: "operations",
        description: "M\xE1ximo de pedidos simult\xE1neos por negocio",
        isPublic: false
      },
      // Security
      {
        key: "max_login_attempts",
        value: "5",
        type: "number",
        category: "security",
        description: "Intentos m\xE1ximos de login",
        isPublic: false
      },
      {
        key: "rate_limit_requests_per_minute",
        value: "60",
        type: "number",
        category: "security",
        description: "L\xEDmite de requests por minuto",
        isPublic: false
      },
      {
        key: "driver_max_strikes",
        value: "3",
        type: "number",
        category: "security",
        description: "Strikes m\xE1ximos antes de bloqueo",
        isPublic: false
      },
      // App Settings
      {
        key: "app_maintenance_mode",
        value: "false",
        type: "boolean",
        category: "app",
        description: "Modo mantenimiento",
        isPublic: true
      },
      {
        key: "app_version_required",
        value: "1.0.0",
        type: "string",
        category: "app",
        description: "Versi\xF3n m\xEDnima requerida",
        isPublic: true
      },
      {
        key: "support_phone",
        value: "+34612345678",
        type: "string",
        category: "app",
        description: "Tel\xE9fono de soporte",
        isPublic: true
      },
      {
        key: "support_email",
        value: "soporte@comeya.es",
        type: "string",
        category: "app",
        description: "Email de soporte",
        isPublic: true
      },
      {
        key: "privacy_contact_email",
        value: "privacidad@comeya.es",
        type: "string",
        category: "app",
        description: "Email de contacto para solicitudes de privacidad (RGPD)",
        isPublic: true
      },
      {
        key: "privacy_policy_url",
        value: "https://app.comeya.es/privacy-policy",
        type: "string",
        category: "app",
        description: "URL p\xFAblica de la Pol\xEDtica de Privacidad",
        isPublic: true
      },
      {
        key: "company_name",
        value: "ComeYa",
        type: "string",
        category: "app",
        description: "Nombre de la empresa/plataforma",
        isPublic: true
      },
      {
        key: "app_store_url_android",
        value: "",
        type: "string",
        category: "app",
        description: "URL de la app en Google Play Store",
        isPublic: true
      },
      // Twilio Configuration
      {
        key: "twilio_phone_number",
        value: process.env.TWILIO_PHONE_NUMBER || "",
        type: "string",
        category: "twilio",
        description: "N\xFAmero de tel\xE9fono Twilio",
        isPublic: false
      },
      {
        key: "twilio_studio_flow_sid",
        value: process.env.TWILIO_STUDIO_FLOW_SID || "",
        type: "string",
        category: "twilio",
        description: "Twilio Studio Flow SID para llamadas",
        isPublic: false
      }
    ];
  }
});

// server/utils/distance.ts
var distance_exports = {};
__export(distance_exports, {
  calculateDeliveryFee: () => calculateDeliveryFee,
  calculateDeliveryFeeSync: () => calculateDeliveryFeeSync,
  calculateDistance: () => calculateDistance,
  estimateDeliveryTime: () => estimateDeliveryTime,
  isInCoverageArea: () => isInCoverageArea,
  isInCoverageAreaSync: () => isInCoverageAreaSync
});
function calculateDistance(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const dLat = toRad(lat2 - lat1);
  const dLon = toRad(lon2 - lon1);
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
function toRad(deg) {
  return deg * (Math.PI / 180);
}
async function calculateDeliveryFee(distance) {
  return calculateDeliveryFeeSync(distance);
}
function calculateDeliveryFeeSync(distance) {
  let euros;
  if (distance <= 2) {
    euros = 2.5;
  } else if (distance <= 3) {
    euros = 4;
  } else if (distance <= 4) {
    euros = 5;
  } else {
    euros = 5 + Math.ceil(distance - 4);
  }
  return Math.round(euros * 100);
}
function estimateDeliveryTime(distance, prepTime = 20) {
  const SPEED_KM_PER_MIN = 0.5;
  const travelTime = distance / SPEED_KM_PER_MIN;
  return Math.ceil(prepTime + travelTime);
}
async function isInCoverageArea(latitude, longitude) {
  const CENTER_LAT = 41.7636;
  const CENTER_LNG = -2.4677;
  const { getSettingValue: getSettingValue2 } = await Promise.resolve().then(() => (init_systemSettingsService(), systemSettingsService_exports));
  const maxRadius = await getSettingValue2("max_delivery_radius_km", 10);
  const distance = calculateDistance(
    latitude,
    longitude,
    CENTER_LAT,
    CENTER_LNG
  );
  return distance <= maxRadius;
}
function isInCoverageAreaSync(latitude, longitude, maxRadius = 10) {
  const CENTER_LAT = 41.7636;
  const CENTER_LNG = -2.4677;
  const distance = calculateDistance(
    latitude,
    longitude,
    CENTER_LAT,
    CENTER_LNG
  );
  return distance <= maxRadius;
}
var init_distance = __esm({
  "server/utils/distance.ts"() {
    "use strict";
  }
});

// server/geocodeService.ts
var geocodeService_exports = {};
__export(geocodeService_exports, {
  geocodeAddress: () => geocodeAddress2,
  geocodeMissingCoordinatesJob: () => geocodeMissingCoordinatesJob,
  getMapsServerKey: () => getMapsServerKey
});
function getMapsServerKey() {
  return process.env.GOOGLE_MAPS_API_KEY || process.env.EXPO_PUBLIC_GOOGLE_MAPS_API_KEY || "";
}
async function geocodeAddress2(address) {
  if (!address?.trim()) return null;
  const base = address.trim();
  const query = base.toLowerCase().includes("soria") ? base : `${base}, Soria, Espa\xF1a`;
  const result = await googleMapsService.geocodeAddress(query);
  return result ? { lat: result.lat, lng: result.lng } : null;
}
async function geocodeMissingCoordinatesJob() {
  try {
    const key = getMapsServerKey();
    if (!key) {
      logger.warn("[geocode] Sin GOOGLE_MAPS_API_KEY: job omitido");
      return;
    }
    const bizMissing = await db.select().from(businesses).limit(10);
    const bizToFix = bizMissing.filter(
      (b) => b.address && (!b.latitude || !b.longitude || b.latitude === "" || b.longitude === "")
    );
    let bizFixed = 0;
    for (const b of bizToFix) {
      const geo = await geocodeAddress2(b.address);
      if (geo) {
        await db.update(businesses).set({ latitude: String(geo.lat), longitude: String(geo.lng) }).where((0, import_drizzle_orm22.eq)(businesses.id, b.id));
        bizFixed++;
      }
    }
    const addrMissing = await db.select().from(addresses).limit(30);
    const addrToFix = addrMissing.filter(
      (a) => !a.latitude || !a.longitude || a.latitude === "" || a.longitude === ""
    );
    let addrFixed = 0;
    for (const a of addrToFix) {
      const geo = await geocodeAddress2(`${a.street}, ${a.city || "Soria"}`);
      if (geo) {
        await db.update(addresses).set({ latitude: String(geo.lat), longitude: String(geo.lng) }).where((0, import_drizzle_orm22.eq)(addresses.id, a.id));
        addrFixed++;
      }
    }
    const SORIA_CENTER = { lat: 41.7636, lng: -2.4677 };
    const addrRows = await db.select().from(addresses).limit(60);
    const addrCenter = addrRows.filter(
      (a) => a.latitude && a.longitude && Math.abs(Number(a.latitude) - SORIA_CENTER.lat) < 1e-4 && Math.abs(Number(a.longitude) - SORIA_CENTER.lng) < 1e-4 && !/plaza\s+mayor/i.test(a.street || "")
    );
    let addrCenterFixed = 0;
    for (const a of addrCenter) {
      const geo = await geocodeAddress2(`${a.street}, ${a.city || "Soria"}`);
      if (geo) {
        await db.update(addresses).set({ latitude: String(geo.lat), longitude: String(geo.lng) }).where((0, import_drizzle_orm22.eq)(addresses.id, a.id));
        addrCenterFixed++;
      }
    }
    addrFixed += addrCenterFixed;
    const { and: and47, ne: ne3 } = await import("drizzle-orm");
    const orderRows = await db.select().from(orders).where(
      and47(ne3(orders.status, "delivered"), ne3(orders.status, "cancelled"))
    ).limit(40);
    const ordersToFix = orderRows.filter(
      (o) => !o.deliveryLatitude || !o.deliveryLongitude || o.deliveryLatitude === "" || o.deliveryLongitude === ""
    );
    let ordersFixed = 0;
    for (const o of ordersToFix.slice(0, 20)) {
      const addressText = typeof o.deliveryAddress === "string" ? o.deliveryAddress : o.deliveryAddress?.street || null;
      if (!addressText) continue;
      const geo = await geocodeAddress2(addressText);
      if (geo) {
        await db.update(orders).set({
          deliveryLatitude: String(geo.lat),
          deliveryLongitude: String(geo.lng)
        }).where((0, import_drizzle_orm22.eq)(orders.id, o.id));
        ordersFixed++;
      }
    }
    if (bizFixed > 0 || addrFixed > 0 || ordersFixed > 0) {
      logger.info(
        `[geocode] Job completado: ${bizFixed} negocios, ${addrFixed} direcciones y ${ordersFixed} pedidos geocodificados`
      );
    }
  } catch (error) {
    logger.error("[geocode] Job fall\xF3", error);
  }
}
var import_drizzle_orm22;
var init_geocodeService = __esm({
  "server/geocodeService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm22 = require("drizzle-orm");
    init_logger();
    init_googleMapsService();
  }
});

// server/pickupService.ts
var pickupService_exports = {};
__export(pickupService_exports, {
  pickupService: () => pickupService
});
function generatePickupCode() {
  return Math.floor(1e5 + Math.random() * 9e5).toString();
}
function generateQRData(orderId, pickupCode) {
  return JSON.stringify({
    orderId,
    pickupCode,
    timestamp: Date.now(),
    type: "pickup"
  });
}
var import_drizzle_orm23, pickupService;
var init_pickupService = __esm({
  "server/pickupService.ts"() {
    "use strict";
    import_drizzle_orm23 = require("drizzle-orm");
    init_db();
    init_schema_mysql();
    init_enhancedPushService();
    init_orderNumberService();
    pickupService = {
      // Crear pedido pickup con código
      async createPickupOrder(orderId, estimatedMinutes) {
        const pickupCode = generatePickupCode();
        const qrData = generateQRData(orderId, pickupCode);
        await db.update(orders).set({
          pickupCode,
          pickupQrCode: qrData,
          estimatedPickupTime: estimatedMinutes
        }).where((0, import_drizzle_orm23.eq)(orders.id, orderId));
        return { pickupCode, qrData };
      },
      /** Garantiza que un pedido pickup tenga código y QR. Los pedidos creados
       *  antes de esta lógica llegaban con pickupCode/pickupQrCode a NULL y el
       *  QR del cliente crasheaba ("Invalid data"). */
      async ensurePickupCodes(order) {
        if (order.pickupCode && order.pickupQrCode) {
          return {
            pickupCode: order.pickupCode,
            pickupQrCode: order.pickupQrCode
          };
        }
        const pickupCode = order.pickupCode || generatePickupCode();
        const pickupQrCode = generateQRData(order.id, pickupCode);
        await db.update(orders).set({ pickupCode, pickupQrCode }).where((0, import_drizzle_orm23.eq)(orders.id, order.id));
        console.log(
          `[pickup] C\xF3digos regenerados para el pedido #${order.id.slice(-6)}`
        );
        return { pickupCode, pickupQrCode };
      },
      // Calcular tiempo restante
      getTimeRemaining(order) {
        if (!order.estimatedPickupTime || !order.createdAt) return null;
        const createdAt = new Date(order.createdAt).getTime();
        const now = Date.now();
        const estimatedMs = order.estimatedPickupTime * 60 * 1e3;
        const elapsed = now - createdAt;
        const remaining = Math.max(0, estimatedMs - elapsed);
        return Math.ceil(remaining / 6e4);
      },
      // Calcular progreso (0-100)
      getProgress(order) {
        if (!order.estimatedPickupTime || !order.createdAt) return 0;
        const createdAt = new Date(order.createdAt).getTime();
        const now = Date.now();
        const estimatedMs = order.estimatedPickupTime * 60 * 1e3;
        const elapsed = now - createdAt;
        return Math.min(100, Math.round(elapsed / estimatedMs * 100));
      },
      // Enviar notificación según progreso
      async sendProgressNotification(orderId, userId, progress) {
        let title = "";
        let body = "";
        if (progress >= 25 && progress < 30) {
          title = "\u{1F373} Preparando tu pedido";
          body = "Tu pedido est\xE1 en preparaci\xF3n. Quedan aprox. 15 min";
        } else if (progress >= 50 && progress < 55) {
          title = "\u23F1\uFE0F A mitad de camino";
          body = "Tu pedido va por la mitad. Quedan aprox. 10 min";
        } else if (progress >= 75 && progress < 80) {
          title = "\u{1F525} \xA1Casi listo!";
          body = "Tu pedido est\xE1 casi listo. Quedan aprox. 5 min";
        } else {
          return;
        }
        await sendPushToUser(userId, {
          title,
          body,
          data: { orderId, screen: "OrderTracking" }
        });
      },
      // Marcar como listo para recoger
      async markReadyForPickup(orderId, userId) {
        await db.update(orders).set({
          status: "ready",
          pickupReadyAt: /* @__PURE__ */ new Date()
        }).where((0, import_drizzle_orm23.eq)(orders.id, orderId));
        await sendPushToUser(userId, {
          title: "\u2705 \xA1Tu pedido est\xE1 listo!",
          body: "Puedes venir a recogerlo cuando quieras",
          data: { orderId, screen: "OrderTracking" },
          sound: "default",
          priority: "high"
        });
      },
      // Cliente avisa que llegó
      async customerArrived(orderId, businessOwnerId) {
        await db.update(orders).set({
          customerArrivedAt: /* @__PURE__ */ new Date()
        }).where((0, import_drizzle_orm23.eq)(orders.id, orderId));
        await sendPushToUser(businessOwnerId, {
          title: "\u{1F6B6} Cliente en el local",
          body: `El cliente lleg\xF3 a recoger el pedido ${await orderRefFromId(orderId)}`,
          data: { orderId, screen: "BusinessOrders" }
        });
      },
      // Validar código de pickup
      async validatePickupCode(orderId, code) {
        const [order] = await db.select().from(orders).where((0, import_drizzle_orm23.eq)(orders.id, orderId)).limit(1);
        return order?.pickupCode === code;
      },
      // Obtener estadísticas de tiempos por negocio
      async getBusinessAverageTime(businessId) {
        const result = await db.select().from(orders).where((0, import_drizzle_orm23.eq)(orders.businessId, businessId));
        const pickupOrders = result.filter(
          (o) => o.orderType === "pickup" && o.pickupReadyAt && o.createdAt
        );
        if (pickupOrders.length === 0) return 20;
        const totalMinutes = pickupOrders.reduce((sum3, order) => {
          const created = new Date(order.createdAt).getTime();
          const ready = new Date(order.pickupReadyAt).getTime();
          return sum3 + (ready - created) / 6e4;
        }, 0);
        return Math.round(totalMinutes / pickupOrders.length);
      },
      // Contar pedidos pendientes del negocio (zona de espera)
      async getPendingOrdersCount(businessId, beforeOrderId) {
        const allOrders = await db.select().from(orders).where((0, import_drizzle_orm23.eq)(orders.businessId, businessId));
        const targetOrder = allOrders.find((o) => o.id === beforeOrderId);
        if (!targetOrder) return 0;
        const targetCreatedAt = new Date(targetOrder.createdAt).getTime();
        return allOrders.filter(
          (o) => o.orderType === "pickup" && ["accepted", "preparing"].includes(o.status) && new Date(o.createdAt).getTime() < targetCreatedAt
        ).length;
      }
    };
  }
});

// server/orderCancellationService.ts
var orderCancellationService_exports = {};
__export(orderCancellationService_exports, {
  cancelOrder: () => cancelOrder,
  checkCancellationEligibility: () => checkCancellationEligibility,
  computeCancelPolicy: () => computeCancelPolicy,
  getCancelPreview: () => getCancelPreview,
  isCancellable: () => isCancellable
});
function getPhase(status) {
  return PHASE_BY_STATUS[status] ?? null;
}
function isCancellable(status) {
  const phase = getPhase(status);
  return phase !== null && phase !== "done";
}
function computeCancelPolicy(order, actorRole, opts = {}) {
  const total = order.total || 0;
  const products3 = order.productosBase || order.subtotal || 0;
  const phase = getPhase(order.status) || "pre_accept";
  const withinRegret = order.regretPeriodEndsAt && new Date(order.regretPeriodEndsAt) > /* @__PURE__ */ new Date();
  const full = (liableParty, explanation) => ({
    refundAmount: total,
    penaltyAmount: 0,
    businessKeepsEarnings: false,
    driverKeepsEarnings: false,
    liableParty,
    explanation
  });
  if (actorRole === "admin" || actorRole === "super_admin") {
    const refundAmount = typeof opts.refundOverride === "number" ? Math.max(0, Math.min(opts.refundOverride, total)) : total;
    return {
      refundAmount,
      penaltyAmount: total - refundAmount,
      businessKeepsEarnings: refundAmount < total,
      driverKeepsEarnings: refundAmount < total,
      liableParty: opts.liableParty || "platform",
      explanation: refundAmount === total ? "Cancelaci\xF3n por administraci\xF3n con devoluci\xF3n completa." : `Cancelaci\xF3n por administraci\xF3n con devoluci\xF3n parcial de ${(refundAmount / 100).toFixed(2)} \u20AC.`
    };
  }
  if (actorRole === "system") {
    return full(
      "platform",
      "Cancelado por el sistema al no encontrar repartidor: se devuelve el importe completo."
    );
  }
  if (actorRole === "business_owner") {
    const driverKeeps = phase === "in_transit";
    return {
      refundAmount: total,
      penaltyAmount: 0,
      businessKeepsEarnings: false,
      driverKeepsEarnings: driverKeeps,
      liableParty: "business",
      explanation: driverKeeps ? "Cancelado por el negocio con el pedido ya en camino: el cliente recupera todo y el repartidor mantiene su tarifa." : "Cancelado por el negocio: el cliente recupera el importe completo."
    };
  }
  if (actorRole === "delivery_driver") {
    return full(
      "driver",
      "Cancelado por el repartidor: el cliente recupera el importe completo."
    );
  }
  if (withinRegret || phase === "pre_accept" || phase === "accepted") {
    return full(
      "platform",
      withinRegret ? "Cancelaci\xF3n dentro del periodo de arrepentimiento: devoluci\xF3n completa sin cargos." : "El negocio a\xFAn no hab\xEDa empezado a preparar el pedido: devoluci\xF3n completa."
    );
  }
  if (phase === "preparing") {
    const refundAmount = Math.max(0, total - products3);
    return {
      refundAmount,
      penaltyAmount: products3,
      businessKeepsEarnings: true,
      driverKeepsEarnings: false,
      liableParty: "platform",
      explanation: `El negocio ya hab\xEDa empezado a preparar el pedido: se descuentan ${(products3 / 100).toFixed(2)} \u20AC de comida y se devuelven ${(refundAmount / 100).toFixed(2)} \u20AC.`
    };
  }
  return {
    refundAmount: 0,
    penaltyAmount: total,
    businessKeepsEarnings: true,
    driverKeepsEarnings: true,
    liableParty: "platform",
    explanation: "El pedido ya estaba listo o en camino: no procede devoluci\xF3n. Si hubo un problema con la entrega, abre una incidencia."
  };
}
async function getCancelPreview(orderId, actorRole = "admin", opts = {}) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm24.eq)(orders.id, orderId)).limit(1);
  if (!order) return { success: false, message: "Pedido no encontrado" };
  return {
    success: true,
    order,
    cancellable: isCancellable(order.status),
    policy: computeCancelPolicy(order, actorRole, opts)
  };
}
async function voidPendingPayouts(orderId, recipientTypes, reason) {
  if (recipientTypes.length === 0) return;
  try {
    const rows = await db.select().from(payouts).where(
      (0, import_drizzle_orm24.and)(
        (0, import_drizzle_orm24.eq)(payouts.orderId, orderId),
        (0, import_drizzle_orm24.eq)(payouts.status, "pending"),
        (0, import_drizzle_orm24.inArray)(payouts.recipientType, recipientTypes)
      )
    );
    for (const p of rows) {
      await db.update(payouts).set({
        amount: 0,
        adjustmentAmount: (p.adjustmentAmount || 0) + p.amount,
        adjustmentReason: reason,
        status: "cancelled"
      }).where((0, import_drizzle_orm24.eq)(payouts.id, p.id));
    }
    if (rows.length > 0) {
      logger.info(`${rows.length} payout(s) anulados por cancelaci\xF3n`, {
        orderId
      });
    }
  } catch (err) {
    logger.error(`Error anulando payouts: ${err.message}`, { orderId });
  }
}
async function notifyCancellation(order, actorRole, policy, reason) {
  const short = orderRef(order);
  const who = actorRole === "customer" ? "el cliente" : actorRole === "business_owner" ? "el negocio" : actorRole === "delivery_driver" ? "el repartidor" : actorRole === "system" ? "el sistema" : "administraci\xF3n";
  if (actorRole !== "customer") {
    await sendPushToUser(order.userId, {
      title: "\u274C Pedido cancelado",
      body: `Pedido ${short} cancelado por ${who}. ${policy.refundAmount > 0 ? `Se devolver\xE1n ${(policy.refundAmount / 100).toFixed(2)} \u20AC.` : reason}`,
      data: {
        orderId: order.id,
        screen: "OrderTracking",
        type: "order_cancelled"
      }
    }).catch(() => {
    });
  }
  if (actorRole !== "business_owner") {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [biz] = await db.select({ ownerId: businesses3.ownerId }).from(businesses3).where((0, import_drizzle_orm24.eq)(businesses3.id, order.businessId)).limit(1);
      if (biz?.ownerId) {
        await sendPushToUser(biz.ownerId, {
          title: "\u274C Pedido cancelado",
          body: `Pedido ${short} cancelado por ${who}.${policy.businessKeepsEarnings ? " Mantienes el importe de los productos." : ""}`,
          data: {
            orderId: order.id,
            screen: "BusinessOrders",
            type: "order_cancelled"
          }
        }).catch(() => {
        });
      }
    } catch {
    }
  }
  if (order.deliveryPersonId && actorRole !== "delivery_driver") {
    await sendPushToUser(order.deliveryPersonId, {
      title: "\u274C Pedido cancelado",
      body: `El pedido ${short} se ha cancelado.${policy.driverKeepsEarnings ? " Se te abonar\xE1 la tarifa de env\xEDo." : ""}`,
      data: {
        orderId: order.id,
        screen: "DriverMyDeliveries",
        type: "order_cancelled"
      }
    }).catch(() => {
    });
  }
  if (actorRole !== "admin" && actorRole !== "super_admin" && policy.refundAmount > 0) {
    try {
      const admins = await db.select({ id: users.id }).from(users).where((0, import_drizzle_orm24.inArray)(users.role, ["admin", "super_admin"]));
      for (const admin of admins) {
        await sendPushToUser(admin.id, {
          title: "\u{1F4B6} Cancelaci\xF3n con devoluci\xF3n",
          body: `Pedido ${short} cancelado por ${who}: ${(policy.refundAmount / 100).toFixed(2)} \u20AC a devolver.`,
          data: {
            orderId: order.id,
            screen: "AdminDashboard",
            section: "finance_refunds",
            type: "cancellation_refund"
          }
        }).catch(() => {
        });
      }
    } catch {
    }
  }
}
async function resolveActorRole(actorId, order, hint) {
  if (hint) return hint;
  if (actorId === "system") return "system";
  try {
    const [u] = await db.select({ role: users.role }).from(users).where((0, import_drizzle_orm24.eq)(users.id, actorId)).limit(1);
    if (u?.role) return u.role;
  } catch {
  }
  return order.userId === actorId ? "customer" : "system";
}
async function cancelOrder(orderId, actorId, reason, opts = {}) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm24.eq)(orders.id, orderId)).limit(1);
  if (!order) {
    return {
      success: false,
      message: "Pedido no encontrado",
      error: "not_found"
    };
  }
  if (order.status === "cancelled") {
    return {
      success: false,
      message: "Este pedido ya estaba cancelado",
      error: "already_cancelled"
    };
  }
  if (!isCancellable(order.status)) {
    return {
      success: false,
      message: order.status === "delivered" ? "El pedido ya se entreg\xF3. Para un problema con la entrega, abre una incidencia." : `Un pedido en estado "${order.status}" no se puede cancelar`,
      error: "not_cancellable"
    };
  }
  const actorRole = await resolveActorRole(actorId, order, opts.actorRole);
  if (actorRole === "customer" && order.userId !== actorId) {
    return { success: false, message: "No autorizado", error: "forbidden" };
  }
  const policy = computeCancelPolicy(order, actorRole, opts);
  await db.update(orders).set({
    status: "cancelled",
    cancelledAt: /* @__PURE__ */ new Date(),
    cancelledBy: actorId,
    cancellationReason: reason,
    refundAmount: policy.refundAmount,
    penaltyAmount: policy.penaltyAmount,
    refundStatus: policy.refundAmount > 0 ? "pending" : null,
    updatedAt: /* @__PURE__ */ new Date()
  }).where((0, import_drizzle_orm24.eq)(orders.id, orderId));
  const toVoid = [];
  if (!policy.businessKeepsEarnings) toVoid.push("business");
  if (!policy.driverKeepsEarnings) toVoid.push("driver");
  await voidPendingPayouts(orderId, toVoid, `Pedido cancelado: ${reason}`);
  let refundInfo;
  if (policy.refundAmount > 0) {
    const result = await createRefund({
      orderId,
      amount: policy.refundAmount,
      type: "cancellation",
      reason,
      liableParty: opts.liableParty || policy.liableParty,
      requestedBy: actorRole === "admin" || actorRole === "super_admin" ? actorId : void 0,
      notes: policy.explanation,
      notifyCustomer: false
      // se avisa una sola vez en notifyCancellation
    });
    refundInfo = {
      refundId: result.refundId,
      method: result.method,
      status: result.status,
      amount: result.amount,
      message: result.message
    };
  }
  await notifyCancellation(order, actorRole, policy, reason);
  logger.info(
    `Pedido ${orderId} cancelado por ${actorRole} \u2014 devoluci\xF3n ${policy.refundAmount} centavos`,
    { orderId }
  );
  return {
    success: true,
    orderId,
    message: "Pedido cancelado",
    policy,
    refund: refundInfo
  };
}
async function checkCancellationEligibility(orderId) {
  const preview = await getCancelPreview(orderId, "customer");
  if (!preview.success) {
    return { success: false, canCancel: false, error: preview.message };
  }
  return {
    success: true,
    canCancel: preview.cancellable,
    refundAmount: preview.policy?.refundAmount ?? 0,
    penalty: preview.policy?.penaltyAmount ?? 0,
    explanation: preview.policy?.explanation
  };
}
var import_drizzle_orm24, PHASE_BY_STATUS;
var init_orderCancellationService = __esm({
  "server/orderCancellationService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm24 = require("drizzle-orm");
    init_logger();
    init_enhancedPushService();
    init_orderNumberService();
    init_refundService();
    PHASE_BY_STATUS = {
      pending: "pre_accept",
      confirmed: "pre_accept",
      // Estados de pago que dejaban el pedido "incancelable" (getPhase → null):
      // el cron de limpieza los necesita para reembolsar y cerrar
      payment_failed: "pre_accept",
      paid: "pre_accept",
      accepted: "accepted",
      assigned_driver: "accepted",
      preparing: "preparing",
      ready: "ready",
      picked_up: "in_transit",
      on_the_way: "in_transit",
      in_transit: "in_transit",
      arriving: "in_transit",
      delivered: "done"
    };
  }
});

// server/autoVerificationService.ts
var autoVerificationService_exports = {};
__export(autoVerificationService_exports, {
  AutoVerificationService: () => AutoVerificationService,
  autoVerificationService: () => autoVerificationService
});
var import_drizzle_orm27, AutoVerificationService, autoVerificationService;
var init_autoVerificationService = __esm({
  "server/autoVerificationService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm27 = require("drizzle-orm");
    init_logger();
    AutoVerificationService = class _AutoVerificationService {
      static instance;
      // Configuración de seguridad
      MIN_CONFIDENCE = 0.75;
      // 75% confianza mínima para auto-aprobar
      MAX_RISK_SCORE = 0.3;
      // 30% riesgo máximo
      MIN_SUCCESSFUL_ORDERS = 3;
      // Mínimo 3 pedidos exitosos
      MAX_DISPUTE_RATE = 0.1;
      // Máximo 10% de disputas
      MAX_AMOUNT_TOLERANCE = 500;
      // ±5 Bs de tolerancia
      MIN_ACCOUNT_AGE_DAYS = 7;
      // Cuenta mínima 7 días
      MAX_ORDERS_PER_HOUR = 5;
      // Máximo 5 pedidos por hora
      MAX_ORDERS_PER_DAY = 20;
      // Máximo 20 pedidos por día
      constructor() {
      }
      static getInstance() {
        if (!_AutoVerificationService.instance) {
          _AutoVerificationService.instance = new _AutoVerificationService();
        }
        return _AutoVerificationService.instance;
      }
      /**
       * Verifica si un comprobante debe ser auto-aprobado
       */
      async shouldAutoApprove(proofId) {
        try {
          const [proof] = await db.select().from(paymentProofs).where((0, import_drizzle_orm27.eq)(paymentProofs.id, proofId)).limit(1);
          if (!proof) {
            throw new Error("Comprobante no encontrado");
          }
          const [order] = await db.select().from(orders).where((0, import_drizzle_orm27.eq)(orders.id, proof.orderId)).limit(1);
          if (!order) {
            throw new Error("Orden no encontrada");
          }
          const [user] = await db.select().from(users).where((0, import_drizzle_orm27.eq)(users.id, proof.userId)).limit(1);
          if (!user) {
            throw new Error("Usuario no encontrado");
          }
          const checks = {
            referenceFormat: await this.checkReferenceFormat(proof.referenceNumber),
            amountMatch: await this.checkAmountMatch(proof.amount, order.total),
            userTrustworthy: await this.checkUserTrustworthiness(proof.userId),
            timeValid: await this.checkTimeValidity(proof.submittedAt),
            imageValid: await this.checkImageValidity(proof.proofImageUrl),
            duplicateCheck: await this.checkDuplicateReference(
              proof.referenceNumber,
              proof.id
            ),
            velocityCheck: await this.checkVelocity(proof.userId)
          };
          const { confidence, riskScore, reason } = await this.calculateConfidenceAndRisk(checks, proof, order, user);
          const autoApprove = confidence >= this.MIN_CONFIDENCE && riskScore <= this.MAX_RISK_SCORE && Object.values(checks).every((check) => check === true);
          logger.info(`\u{1F916} Auto-verification result for proof ${proofId}`, {
            proofId,
            orderId: order.id,
            userId: user.id,
            autoApprove,
            confidence,
            riskScore,
            checks
          });
          return {
            autoApprove,
            reason,
            confidence,
            riskScore,
            checks
          };
        } catch (error) {
          logger.error("Error in auto-verification:", error);
          return {
            autoApprove: false,
            reason: `Error en verificaci\xF3n: ${error.message}`,
            confidence: 0,
            riskScore: 1,
            checks: {
              referenceFormat: false,
              amountMatch: false,
              userTrustworthy: false,
              timeValid: false,
              imageValid: false,
              duplicateCheck: false,
              velocityCheck: false
            }
          };
        }
      }
      /**
       * 1. Verificar formato de referencia
       * Debe ser 8-10 dígitos numéricos
       */
      async checkReferenceFormat(reference) {
        if (!reference) return false;
        const isValid = /^\d{8,10}$/.test(reference);
        const isSequential = /^(\d)\1+$/.test(reference) || reference === "12345678" || reference === "87654321";
        return isValid && !isSequential;
      }
      /**
       * 2. Verificar que el monto coincida
       * Tolerancia de ±5 Bs
       */
      async checkAmountMatch(proofAmount, orderTotal) {
        const difference = Math.abs(proofAmount - orderTotal);
        return difference <= this.MAX_AMOUNT_TOLERANCE;
      }
      /**
       * 3. Verificar confiabilidad del usuario
       */
      async checkUserTrustworthiness(userId) {
        const stats = await this.getUserStats(userId);
        if (stats.accountAge < this.MIN_ACCOUNT_AGE_DAYS) {
          return false;
        }
        if (stats.totalOrders === 0) {
          return false;
        }
        if (stats.successfulOrders < this.MIN_SUCCESSFUL_ORDERS) {
          return false;
        }
        if (stats.disputeRate > this.MAX_DISPUTE_RATE) {
          return false;
        }
        return true;
      }
      /**
       * 4. Verificar validez temporal
       * No más de 48 horas desde el envío
       * No en horario sospechoso (2am-6am)
       */
      async checkTimeValidity(submittedAt) {
        const now = /* @__PURE__ */ new Date();
        const hoursSinceSubmit = (now.getTime() - new Date(submittedAt).getTime()) / (1e3 * 60 * 60);
        if (hoursSinceSubmit > 48) {
          return false;
        }
        const hour = new Date(submittedAt).getHours();
        if (hour >= 2 && hour <= 6) {
          return false;
        }
        return true;
      }
      /**
       * 5. Verificar validez de la imagen
       * Debe existir y tener URL válida
       */
      async checkImageValidity(imageUrl) {
        if (!imageUrl) return false;
        try {
          new URL(imageUrl);
          return true;
        } catch {
          return false;
        }
      }
      /**
       * 6. Verificar que la referencia no esté duplicada
       * CRÍTICO para prevenir fraude
       */
      async checkDuplicateReference(reference, currentProofId) {
        const duplicates = await db.select().from(paymentProofs).where(
          (0, import_drizzle_orm27.and)(
            (0, import_drizzle_orm27.eq)(paymentProofs.referenceNumber, reference),
            import_drizzle_orm27.sql`${paymentProofs.id} != ${currentProofId}`
          )
        ).limit(1);
        return duplicates.length === 0;
      }
      /**
       * 7. Verificar velocidad de pedidos (Velocity Check)
       * Detecta comportamiento anormal
       */
      async checkVelocity(userId) {
        const now = /* @__PURE__ */ new Date();
        const oneHourAgo = new Date(now.getTime() - 60 * 60 * 1e3);
        const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1e3);
        const ordersLastHour = await db.select({ count: import_drizzle_orm27.sql`count(*)` }).from(orders).where((0, import_drizzle_orm27.and)((0, import_drizzle_orm27.eq)(orders.userId, userId), (0, import_drizzle_orm27.gte)(orders.createdAt, oneHourAgo)));
        const countLastHour = Number(ordersLastHour[0]?.count || 0);
        if (countLastHour > this.MAX_ORDERS_PER_HOUR) {
          logger.warn(
            `\u26A0\uFE0F Velocity check failed: ${countLastHour} orders in last hour`,
            { userId }
          );
          return false;
        }
        const ordersLastDay = await db.select({ count: import_drizzle_orm27.sql`count(*)` }).from(orders).where((0, import_drizzle_orm27.and)((0, import_drizzle_orm27.eq)(orders.userId, userId), (0, import_drizzle_orm27.gte)(orders.createdAt, oneDayAgo)));
        const countLastDay = Number(ordersLastDay[0]?.count || 0);
        if (countLastDay > this.MAX_ORDERS_PER_DAY) {
          logger.warn(
            `\u26A0\uFE0F Velocity check failed: ${countLastDay} orders in last day`,
            { userId }
          );
          return false;
        }
        return true;
      }
      /**
       * Obtener estadísticas del usuario
       */
      async getUserStats(userId) {
        const [user] = await db.select().from(users).where((0, import_drizzle_orm27.eq)(users.id, userId)).limit(1);
        if (!user) {
          return {
            totalOrders: 0,
            successfulOrders: 0,
            disputedOrders: 0,
            disputeRate: 0,
            avgOrderValue: 0,
            accountAge: 0,
            lastOrderDate: null
          };
        }
        const userOrders = await db.select().from(orders).where((0, import_drizzle_orm27.eq)(orders.userId, userId)).orderBy((0, import_drizzle_orm27.desc)(orders.createdAt));
        const totalOrders = userOrders.length;
        const successfulOrders = userOrders.filter(
          (o) => o.status === "completed"
        ).length;
        const disputedOrders = userOrders.filter(
          (o) => o.status === "disputed"
        ).length;
        const disputeRate = totalOrders > 0 ? disputedOrders / totalOrders : 0;
        const totalValue = userOrders.reduce((sum3, o) => sum3 + o.total, 0);
        const avgOrderValue = totalOrders > 0 ? totalValue / totalOrders : 0;
        const accountAge = Math.floor(
          (Date.now() - new Date(user.createdAt).getTime()) / (1e3 * 60 * 60 * 24)
        );
        const lastOrderDate = userOrders.length > 0 ? userOrders[0].createdAt : null;
        return {
          totalOrders,
          successfulOrders,
          disputedOrders,
          disputeRate,
          avgOrderValue,
          accountAge,
          lastOrderDate
        };
      }
      /**
       * Calcular confianza y riesgo
       */
      async calculateConfidenceAndRisk(checks, proof, order, user) {
        let confidence = 1;
        let riskScore = 0;
        const reasons = [];
        if (!checks.referenceFormat) {
          confidence -= 0.3;
          riskScore += 0.4;
          reasons.push("Formato de referencia inv\xE1lido");
        }
        if (!checks.amountMatch) {
          confidence -= 0.4;
          riskScore += 0.5;
          reasons.push("Monto no coincide");
        }
        if (!checks.userTrustworthy) {
          confidence -= 0.3;
          riskScore += 0.3;
          reasons.push("Usuario no confiable");
        }
        if (!checks.timeValid) {
          confidence -= 0.2;
          riskScore += 0.2;
          reasons.push("Tiempo inv\xE1lido o hora sospechosa");
        }
        if (!checks.imageValid) {
          confidence -= 0.2;
          riskScore += 0.2;
          reasons.push("Imagen inv\xE1lida");
        }
        if (!checks.duplicateCheck) {
          confidence -= 0.5;
          riskScore += 0.8;
          reasons.push("\u26A0\uFE0F REFERENCIA DUPLICADA - POSIBLE FRAUDE");
        }
        if (!checks.velocityCheck) {
          confidence -= 0.3;
          riskScore += 0.4;
          reasons.push("\u26A0\uFE0F VELOCIDAD ANORMAL - POSIBLE FRAUDE");
        }
        const stats = await this.getUserStats(user.id);
        if (stats.successfulOrders >= 10 && stats.disputeRate === 0) {
          confidence += 0.1;
          riskScore -= 0.1;
          reasons.push("\u2705 Usuario con excelente historial");
        }
        confidence = Math.max(0, Math.min(1, confidence));
        riskScore = Math.max(0, Math.min(1, riskScore));
        const reason = reasons.length > 0 ? reasons.join(", ") : "Todas las validaciones pasaron correctamente";
        return { confidence, riskScore, reason };
      }
      /**
       * Registrar intento de fraude
       */
      async logFraudAttempt(userId, proofId, reason) {
        logger.error(`\u{1F6A8} FRAUD ATTEMPT DETECTED`, {
          userId,
          proofId,
          reason,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        });
        const { auditLogs: auditLogs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
        await db.insert(auditLogs2).values({
          userId,
          action: "fraud_attempt",
          entityType: "payment_proof",
          entityId: proofId,
          changes: JSON.stringify({ reason })
        });
        try {
          const { notifyAdminFraud: notifyAdminFraud2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
          const [user] = await db.select({ name: users.name }).from(users).where((0, import_drizzle_orm27.eq)(users.id, userId)).limit(1);
          notifyAdminFraud2({
            userId,
            userName: user?.name ?? "Usuario desconocido",
            proofId,
            reason
          });
        } catch {
        }
        const since = new Date(Date.now() - 7 * 24 * 60 * 60 * 1e3);
        const recentFraud = await db.select({ count: import_drizzle_orm27.sql`count(*)` }).from(auditLogs2).where(
          (0, import_drizzle_orm27.and)(
            (0, import_drizzle_orm27.eq)(auditLogs2.userId, userId),
            (0, import_drizzle_orm27.eq)(auditLogs2.action, "fraud_attempt"),
            (0, import_drizzle_orm27.gte)(auditLogs2.createdAt, since)
          )
        );
        const fraudCount = Number(recentFraud[0]?.count || 0);
        if (fraudCount >= 3) {
          await db.update(users).set({ isActive: false }).where((0, import_drizzle_orm27.eq)(users.id, userId));
          logger.error(
            `\u{1F512} User ${userId} BLOCKED after ${fraudCount} fraud attempts`
          );
        }
      }
    };
    autoVerificationService = AutoVerificationService.getInstance();
  }
});

// server/utils/paymentMethods.ts
var paymentMethods_exports = {};
__export(paymentMethods_exports, {
  MANUAL_PROVIDERS: () => MANUAL_PROVIDERS,
  canonicalizeProvider: () => canonicalizeProvider,
  mergePaymentMethods: () => mergePaymentMethods
});
function canonicalizeProvider(provider) {
  const k = String(provider || "").toLowerCase().replace(/[\s_-]+/g, "");
  if (!k) return null;
  if (k.includes("sepa") || k.includes("transferencia") || k.includes("banktransfer")) {
    return "sepa";
  }
  if (k === "bizum" || k.includes("bizummanual")) return "bizum_manual";
  if (k === "paypal") return "paypal";
  return null;
}
function mergePaymentMethods(rows, receiving) {
  const out = /* @__PURE__ */ new Map();
  for (const row of rows) {
    if (row && row.isActive === false) continue;
    const canon = canonicalizeProvider(row.provider);
    const key = canon ?? String(row.provider || "").toLowerCase();
    if (out.has(key)) continue;
    if (canon) {
      out.set(key, {
        ...row,
        provider: canon,
        requiresManualVerification: true,
        displayName: MANUAL_LABELS[canon] || row.displayName
      });
    } else {
      out.set(key, row);
    }
  }
  const addManual = (key, instructions) => {
    const existing = out.get(key);
    if (existing) {
      if (!existing.instructions) existing.instructions = instructions;
      return;
    }
    out.set(key, {
      id: key,
      name: key,
      provider: key,
      displayName: MANUAL_LABELS[key] ?? key,
      isActive: true,
      requiresManualVerification: true,
      instructions
    });
  };
  if (receiving?.bizum) addManual("bizum_manual", `Env\xEDa al ${receiving.bizum}`);
  if (receiving?.iban) addManual("sepa", `IBAN: ${receiving.iban}`);
  if (receiving?.paypalEmail) {
    addManual("paypal", `Env\xEDa a ${receiving.paypalEmail}`);
  }
  return [...out.values()];
}
var MANUAL_LABELS, MANUAL_PROVIDERS;
var init_paymentMethods = __esm({
  "server/utils/paymentMethods.ts"() {
    "use strict";
    MANUAL_LABELS = {
      bizum_manual: "Bizum (manual)",
      sepa: "Transferencia SEPA",
      paypal: "PayPal"
    };
    MANUAL_PROVIDERS = ["bizum_manual", "sepa", "paypal"];
  }
});

// server/digitalPaymentService.ts
var digitalPaymentService_exports = {};
__export(digitalPaymentService_exports, {
  DigitalPaymentService: () => DigitalPaymentService,
  digitalPaymentService: () => digitalPaymentService
});
var import_drizzle_orm28, DigitalPaymentService, digitalPaymentService;
var init_digitalPaymentService = __esm({
  "server/digitalPaymentService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm28 = require("drizzle-orm");
    init_unifiedFinancialService();
    init_autoVerificationService();
    init_logger();
    init_enhancedPushService();
    init_orderNumberService();
    init_cloudinaryService();
    DigitalPaymentService = class _DigitalPaymentService {
      static instance;
      constructor() {
      }
      static getInstance() {
        if (!_DigitalPaymentService.instance) {
          _DigitalPaymentService.instance = new _DigitalPaymentService();
        }
        return _DigitalPaymentService.instance;
      }
      // Get active payment methods
      async getActivePaymentMethods() {
        try {
          const [tableRows, receiving] = await Promise.all([
            db.select().from(paymentMethods).where((0, import_drizzle_orm28.eq)(paymentMethods.isActive, true)),
            this.getReceivingAccounts()
          ]);
          const { mergePaymentMethods: mergePaymentMethods2 } = await Promise.resolve().then(() => (init_paymentMethods(), paymentMethods_exports));
          return mergePaymentMethods2(tableRows, receiving);
        } catch (error) {
          logger.error("Error getting active payment methods:", error);
          return [];
        }
      }
      // Cuentas receptoras de ComeYa (bizum/IBAN/PayPal) — misma fuente que
      // GET /api/payments/info, con el mismo fallback a configuración
      async getReceivingAccounts() {
        try {
          const [rows] = await db.execute(
            "SELECT provider, account_data FROM payment_receiving_accounts WHERE is_active = TRUE"
          );
          const out = { bizum: "", iban: "", paypalEmail: "" };
          for (const row of rows || []) {
            const d = row.account_data || {};
            if (row.provider === "bizum") out.bizum = d.phone || "";
            if (row.provider === "transferencia") out.iban = d.iban || "";
            if (row.provider === "paypal") out.paypalEmail = d.email || "";
          }
          return out;
        } catch {
          const { CONFIG: CONFIG2 } = await Promise.resolve().then(() => (init_config(), config_exports));
          return {
            bizum: await CONFIG2.bizumPhone(),
            iban: await CONFIG2.iban(),
            paypalEmail: await CONFIG2.paypalEmail()
          };
        }
      }
      // Submit payment proof (Pago Móvil, Binance Pay, Zinli, Zelle)
      async submitPaymentProof(data) {
        try {
          const [order] = await db.select().from(orders).where((0, import_drizzle_orm28.eq)(orders.id, data.orderId)).limit(1);
          if (!order) {
            return { success: false, message: "Pedido no encontrado" };
          }
          if (order.status !== "pending") {
            return { success: false, message: "El pedido ya fue procesado" };
          }
          const [method] = await db.select().from(paymentMethods).where(
            (0, import_drizzle_orm28.and)(
              (0, import_drizzle_orm28.eq)(paymentMethods.provider, data.paymentProvider),
              (0, import_drizzle_orm28.eq)(paymentMethods.isActive, true)
            )
          ).limit(1);
          if (!method) {
            return { success: false, message: "M\xE9todo de pago no disponible" };
          }
          if (!method.requiresManualVerification) {
            return {
              success: false,
              message: "Este m\xE9todo no requiere comprobante"
            };
          }
          let imageUrl = data.proofImageUrl;
          if (imageUrl && imageUrl.startsWith("data:image/")) {
            imageUrl = await CloudinaryService.uploadImage(
              imageUrl,
              "comprobantes",
              `proof-${data.orderId}`
            );
          }
          const proofId = crypto.randomUUID();
          await db.insert(paymentProofs).values({
            id: proofId,
            orderId: data.orderId,
            userId: data.userId,
            paymentProvider: data.paymentProvider,
            referenceNumber: data.referenceNumber,
            proofImageUrl: imageUrl,
            amount: data.amount,
            status: "pending",
            submittedAt: /* @__PURE__ */ new Date()
          });
          const [proof] = await db.select().from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.id, proofId)).limit(1);
          logger.info(
            `\u{1F4B3} Payment proof submitted: Order ${data.orderId} - ${data.paymentProvider}`,
            {
              orderId: data.orderId,
              provider: data.paymentProvider,
              reference: data.referenceNumber
            }
          );
          const SKIP_AUTO_VERIFICATION = process.env.NODE_ENV === "development";
          if (!SKIP_AUTO_VERIFICATION) {
            const autoVerification = await autoVerificationService.shouldAutoApprove(proof.id);
            if (autoVerification.autoApprove) {
              logger.info(`\u{1F916} Auto-approving payment proof ${proof.id}`, {
                proofId: proof.id,
                confidence: autoVerification.confidence,
                riskScore: autoVerification.riskScore
              });
              const verificationResult = await this.verifyPaymentProof(
                proof.id,
                "SYSTEM_AUTO",
                true,
                `Auto-aprobado (Confianza: ${(autoVerification.confidence * 100).toFixed(0)}%, Riesgo: ${(autoVerification.riskScore * 100).toFixed(0)}%)`
              );
              if (verificationResult.success) {
                return {
                  success: true,
                  proofId: proof.id,
                  message: "\xA1Pago verificado autom\xE1ticamente! Tu pedido est\xE1 confirmado."
                };
              }
            } else {
              logger.warn(
                `\u26A0\uFE0F Payment proof ${proof.id} requires manual verification`,
                {
                  proofId: proof.id,
                  reason: autoVerification.reason,
                  confidence: autoVerification.confidence,
                  riskScore: autoVerification.riskScore
                }
              );
              if (autoVerification.riskScore > 0.7) {
                await autoVerificationService.logFraudAttempt(
                  data.userId,
                  proof.id,
                  autoVerification.reason
                );
              }
            }
          } else {
            logger.info(`\u23ED\uFE0F Skipping auto-verification in development mode`);
          }
          await db.update(orders).set({
            paymentMethod: data.paymentProvider,
            paymentProvider: data.paymentProvider,
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm28.eq)(orders.id, data.orderId));
          try {
            const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
            const { inArray: inArray12 } = await import("drizzle-orm");
            const admins = await db.select({ id: users5.id }).from(users5).where(inArray12(users5.role, ["admin", "super_admin"]));
            const { orderRef: orderRef2 } = await Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports));
            const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
            const ref = orderRef2(order);
            for (const admin of admins) {
              await sendPushToUser2(admin.id, {
                title: "\u{1F9FE} Comprobante pendiente de verificaci\xF3n",
                body: `Pedido ${ref} \u2014 ${data.paymentProvider} (${(data.amount / 100).toFixed(2)} \u20AC). Verif\xEDcalo para activar el pedido.`,
                data: { orderId: data.orderId, screen: "AdminFinance" }
              });
            }
            const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
            notifyAdmins2({
              type: "payment_proof_pending",
              orderId: data.orderId,
              proofId: proof.id,
              provider: data.paymentProvider,
              amount: data.amount
            });
          } catch (notifyError) {
            logger.error("Failed to notify admins of payment proof", notifyError);
          }
          return {
            success: true,
            proofId: proof.id,
            message: "Comprobante enviado. Ser\xE1 verificado en breve."
          };
        } catch (error) {
          logger.error("Error submitting payment proof:", error);
          return { success: false, message: error.message };
        }
      }
      // Verify payment proof (Admin only)
      async verifyPaymentProof(proofId, adminId, approved, notes) {
        try {
          const [proof] = await db.select().from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.id, proofId)).limit(1);
          if (!proof) {
            return { success: false, message: "Comprobante no encontrado" };
          }
          if (proof.status !== "pending") {
            return { success: false, message: "Este comprobante ya fue procesado" };
          }
          const [order] = await db.select().from(orders).where((0, import_drizzle_orm28.eq)(orders.id, proof.orderId)).limit(1);
          if (!order) {
            return { success: false, message: "Pedido no encontrado" };
          }
          if (approved) {
            await db.transaction(async (tx) => {
              await tx.update(paymentProofs).set({
                status: "approved",
                verifiedBy: adminId,
                verifiedAt: /* @__PURE__ */ new Date(),
                verificationNotes: notes
              }).where((0, import_drizzle_orm28.eq)(paymentProofs.id, proofId));
              await tx.update(orders).set({
                status: "accepted",
                paidAt: /* @__PURE__ */ new Date(),
                updatedAt: /* @__PURE__ */ new Date()
              }).where((0, import_drizzle_orm28.eq)(orders.id, proof.orderId));
            });
            logger.info(`\u2705 Payment verified: Order ${order.id}`, {
              orderId: order.id,
              provider: proof.paymentProvider
            });
            await notifyPagoMovilStatus(proof.userId, "verified", order.id);
            const [biz] = await db.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm28.eq)(businesses.id, order.businessId)).limit(1);
            if (biz?.ownerId) {
              await sendPushToUser(biz.ownerId, {
                title: "\u{1F4B3} Pago confirmado \u2014 \xA1A preparar!",
                body: `Pedido ${orderRef(order)} pagado. Empieza la preparaci\xF3n.`,
                data: { orderId: order.id, screen: "BusinessOrders" }
              });
            }
            try {
              const { notifyPaymentVerified: notifyPaymentVerified2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
              notifyPaymentVerified2(order.businessId, order.id);
            } catch {
            }
            return {
              success: true,
              message: "Pago verificado y pedido activado",
              orderId: order.id
            };
          } else {
            await db.transaction(async (tx) => {
              await tx.update(paymentProofs).set({
                status: "rejected",
                verifiedBy: adminId,
                verifiedAt: /* @__PURE__ */ new Date(),
                verificationNotes: notes || "Comprobante rechazado"
              }).where((0, import_drizzle_orm28.eq)(paymentProofs.id, proofId));
              await tx.update(orders).set({
                status: "payment_failed",
                updatedAt: /* @__PURE__ */ new Date()
              }).where((0, import_drizzle_orm28.eq)(orders.id, proof.orderId));
            });
            logger.warn(`\u274C Payment proof rejected: Order ${order.id}`, {
              orderId: order.id,
              provider: proof.paymentProvider,
              reason: notes
            });
            await notifyPagoMovilStatus(proof.userId, "rejected", order.id, notes);
            return {
              success: true,
              message: "Comprobante rechazado",
              orderId: order.id
            };
          }
        } catch (error) {
          logger.error("Error verifying payment proof:", error);
          return { success: false, message: error.message };
        }
      }
      // Process PayPal payment (automatic)
      async processPayPalPayment(orderId, paypalTransactionId) {
        try {
          const [order] = await db.select().from(orders).where((0, import_drizzle_orm28.eq)(orders.id, orderId)).limit(1);
          if (!order) {
            return { success: false, message: "Pedido no encontrado" };
          }
          const settings = await db.select().from(systemSettings).where((0, import_drizzle_orm28.eq)(systemSettings.category, "payment_providers"));
          const paypalClientId = settings.find(
            (s) => s.key === "paypal_client_id"
          )?.value;
          const paypalSecret = settings.find(
            (s) => s.key === "paypal_secret"
          )?.value;
          if (!paypalClientId || !paypalSecret) {
            return { success: false, message: "PayPal no configurado" };
          }
          await db.transaction(async (tx) => {
            await tx.update(orders).set({
              status: "confirmed",
              paymentMethod: "paypal",
              paymentProvider: "paypal",
              paidAt: /* @__PURE__ */ new Date(),
              updatedAt: /* @__PURE__ */ new Date()
            }).where((0, import_drizzle_orm28.eq)(orders.id, orderId));
            const commissions = await financialService.calculateCommissions(
              order.totalAmount,
              order.deliveryFee || 0
            );
            const [paypalMethod] = await tx.select().from(paymentMethods).where((0, import_drizzle_orm28.eq)(paymentMethods.provider, "paypal")).limit(1);
            const paypalFee = paypalMethod ? Math.round(
              order.totalAmount * (paypalMethod.commissionPercentage / 100)
            ) : 0;
            const [business] = await tx.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm28.eq)(businesses.id, order.businessId)).limit(1);
            if (!business?.ownerId) {
              throw new Error(
                `Business owner not found for business ${order.businessId}`
              );
            }
            await financialService.updateWalletBalance(
              business.ownerId,
              commissions.business - paypalFee,
              "order_payment",
              order.id,
              `Pago de pedido ${orderRef(order)} - PayPal (${paypalMethod?.commissionPercentage}% fee)`
            );
            if (order.driverId) {
              await financialService.updateWalletBalance(
                order.driverId,
                commissions.driver,
                "delivery_payment",
                order.id,
                `Entrega de pedido ${orderRef(order)}`
              );
            }
            const [adminUser] = await tx.select().from(await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports)).then((m) => m.users)).where(
              (0, import_drizzle_orm28.eq)(
                (await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports)).then((m) => m.users)).role,
                "admin"
              )
            ).limit(1);
            if (adminUser) {
              await financialService.updateWalletBalance(
                adminUser.id,
                commissions.platform + paypalFee,
                "platform_commission",
                order.id,
                `Comisi\xF3n ComeYa + PayPal fee - Pedido ${orderRef(order)}`
              );
            }
            logger.info(`\u2705 PayPal payment processed: Order ${order.id}`, {
              orderId: order.id,
              transactionId: paypalTransactionId,
              commissions,
              paypalFee
            });
          });
          return {
            success: true,
            message: "Pago con PayPal procesado exitosamente",
            orderId: order.id
          };
        } catch (error) {
          logger.error("Error processing PayPal payment:", error);
          return { success: false, message: error.message };
        }
      }
      // Get pending payment proofs (Admin)
      async getPendingPaymentProofs() {
        const proofs = await db.select().from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.status, "pending")).orderBy(paymentProofs.submittedAt);
        const backendUrl = process.env.BACKEND_URL || `http://localhost:${process.env.PORT || 5e3}`;
        return proofs.map((proof) => ({
          ...proof,
          proofImageUrl: proof.proofImageUrl?.startsWith("http") ? proof.proofImageUrl : `${backendUrl}${proof.proofImageUrl}`
        }));
      }
      // Métricas genéricas de pagos (todos los métodos)
      async getPaymentMetrics() {
        const today = /* @__PURE__ */ new Date();
        today.setHours(0, 0, 0, 0);
        const [all, pending, approved, rejected, todayApproved, todayRejected] = await Promise.all([
          db.select({ count: (0, import_drizzle_orm28.count)() }).from(paymentProofs),
          db.select({ count: (0, import_drizzle_orm28.count)() }).from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.status, "pending")),
          db.select({ count: (0, import_drizzle_orm28.count)(), total: (0, import_drizzle_orm28.sum)(paymentProofs.amount) }).from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.status, "approved")),
          db.select({ count: (0, import_drizzle_orm28.count)() }).from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.status, "rejected")),
          db.select({ count: (0, import_drizzle_orm28.count)(), total: (0, import_drizzle_orm28.sum)(paymentProofs.amount) }).from(paymentProofs).where(
            (0, import_drizzle_orm28.and)(
              (0, import_drizzle_orm28.eq)(paymentProofs.status, "approved"),
              (0, import_drizzle_orm28.gte)(paymentProofs.verifiedAt, today)
            )
          ),
          db.select({ count: (0, import_drizzle_orm28.count)() }).from(paymentProofs).where(
            (0, import_drizzle_orm28.and)(
              (0, import_drizzle_orm28.eq)(paymentProofs.status, "rejected"),
              (0, import_drizzle_orm28.gte)(paymentProofs.verifiedAt, today)
            )
          )
        ]);
        const byProvider = await db.select({
          provider: paymentProofs.paymentProvider,
          count: (0, import_drizzle_orm28.count)(),
          total: (0, import_drizzle_orm28.sum)(paymentProofs.amount)
        }).from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.status, "approved")).groupBy(paymentProofs.paymentProvider);
        const totalByMethod = {};
        byProvider.forEach((item) => {
          totalByMethod[item.provider] = {
            count: item.count,
            amount: item.total || 0
          };
        });
        return {
          totalByMethod,
          pendingProofs: pending[0].count,
          approvedToday: todayApproved[0].count,
          rejectedToday: todayRejected[0].count,
          totalRevenue: approved[0].total || 0
        };
      }
      // Get payment proof by order ID
      async getPaymentProofByOrderId(orderId) {
        const [proof] = await db.select().from(paymentProofs).where((0, import_drizzle_orm28.eq)(paymentProofs.orderId, orderId)).limit(1);
        return proof;
      }
    };
    digitalPaymentService = DigitalPaymentService.getInstance();
  }
});

// server/webhookHandlers.ts
var webhookHandlers_exports = {};
__export(webhookHandlers_exports, {
  handleStripeWebhook: () => handleStripeWebhook
});
async function getWebhookSecret() {
  try {
    const { sql: sql23 } = await import("drizzle-orm");
    const [rows] = await db.execute(
      sql23`SELECT value FROM app_settings WHERE \`key\` = 'stripe_webhook_secret' LIMIT 1`
    );
    if (rows?.[0]?.value) return String(rows[0].value);
  } catch (err) {
    console.error("getWebhookSecret: error leyendo app_settings:", err);
  }
  return ENV_WEBHOOK_SECRET || null;
}
function logWebhookEvent(context, message, data) {
  console.log(
    `[WEBHOOK ${context.eventId}] ${context.eventType} - ${message}`,
    {
      timestamp: context.timestamp,
      accountId: context.accountId,
      data
    }
  );
}
function logWebhookError(context, error, details) {
  console.error(`[WEBHOOK ${context.eventId}] ERROR - ${error}`, {
    eventType: context.eventType,
    timestamp: context.timestamp,
    accountId: context.accountId,
    details
  });
}
async function handleStripeWebhook(req, res) {
  const sig = req.headers["stripe-signature"];
  if (!sig) {
    console.error("Missing Stripe signature header");
    return res.status(400).json({ error: "Missing signature" });
  }
  const webhookSecret = await getWebhookSecret();
  if (!webhookSecret) {
    console.error("STRIPE_WEBHOOK_SECRET not configured");
    return res.status(500).json({ error: "Webhook not configured" });
  }
  let event;
  try {
    const stripe2 = getStripe2();
    const rawBody = Buffer.isBuffer(req.body) ? req.body : typeof req.body === "string" ? Buffer.from(req.body) : Buffer.from(JSON.stringify(req.body));
    event = stripe2.webhooks.constructEvent(rawBody, sig, webhookSecret);
  } catch (err) {
    console.error("Webhook signature verification failed:", err.message);
    return res.status(400).json({ error: "Invalid signature" });
  }
  const context = {
    eventId: event.id,
    eventType: event.type,
    timestamp: new Date(event.created * 1e3),
    accountId: event.account || void 0
  };
  logWebhookEvent(context, "Received webhook event");
  try {
    switch (event.type) {
      case "payment_intent.succeeded":
        await handlePaymentIntentSucceeded(
          event.data.object,
          context
        );
        break;
      case "payment_intent.payment_failed":
        await handlePaymentIntentFailed(
          event.data.object,
          context
        );
        break;
      case "account.updated":
        await handleAccountUpdated(
          event.data.object,
          context
        );
        break;
      case "transfer.created":
        await handleTransferCreated(
          event.data.object,
          context
        );
        break;
      case "payout.paid":
        await handlePayoutPaid(event.data.object, context);
        break;
      case "payout.failed":
        await handlePayoutFailed(event.data.object, context);
        break;
      case "charge.refunded":
        await handleChargeRefunded(
          event.data.object,
          context
        );
        break;
      case "refund.updated": {
        const refund = event.data.object;
        if (refund.status === "failed") {
          await handleRefundFailed(refund, context);
        }
        break;
      }
      case "charge.dispute.created":
        await handleDisputeCreated(
          event.data.object,
          context
        );
        break;
      default:
        logWebhookEvent(context, `Unhandled event type: ${event.type}`);
    }
    logWebhookEvent(context, "Webhook processed successfully");
    res.status(200).json({ received: true });
  } catch (error) {
    logWebhookError(context, "Failed to process webhook", {
      error: error.message,
      stack: error.stack
    });
    if (error.message.includes("Order not found") || error.message.includes("Business not found")) {
      res.status(200).json({ received: true, warning: "Resource not found" });
    } else {
      res.status(500).json({ error: "Internal server error" });
    }
  }
}
async function handlePaymentIntentSucceeded(paymentIntent, context) {
  if (paymentIntent.metadata?.isDelta === "true") {
    const { applyDeltaPaymentFromWebhook: applyDeltaPaymentFromWebhook2 } = await Promise.resolve().then(() => (init_substitutionService(), substitutionService_exports));
    const deltaResult = await applyDeltaPaymentFromWebhook2(paymentIntent);
    logWebhookEvent(
      context,
      `Substitution delta payment: ${deltaResult.message}`
    );
    return;
  }
  const orderId = paymentIntent.metadata?.orderId;
  if (!orderId) {
    logWebhookError(context, "Missing orderId in payment intent metadata");
    return;
  }
  logWebhookEvent(
    context,
    `Processing successful payment for order ${orderId}`,
    {
      amount: paymentIntent.amount,
      currency: paymentIntent.currency
    }
  );
  try {
    const { confirmPaidOrder: confirmPaidOrder2 } = await Promise.resolve().then(() => (init_paymentConfirmationService(), paymentConfirmationService_exports));
    const result = await confirmPaidOrder2(orderId, paymentIntent, "webhook");
    if (!result.success) {
      throw new Error(result.message);
    }
    logWebhookEvent(
      context,
      `Order ${orderId} marked as accepted and transaction recorded (${result.message})`
    );
  } catch (error) {
    logWebhookError(context, `Failed to update order ${orderId}`, error);
    throw error;
  }
}
async function handlePaymentIntentFailed(paymentIntent, context) {
  const orderId = paymentIntent.metadata?.orderId;
  if (!orderId) {
    logWebhookError(
      context,
      "Missing orderId in failed payment intent metadata"
    );
    return;
  }
  logWebhookEvent(context, `Processing failed payment for order ${orderId}`, {
    lastPaymentError: paymentIntent.last_payment_error
  });
  try {
    await db.update(orders).set({
      status: "payment_failed",
      stripePaymentIntentId: paymentIntent.id,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm29.eq)(orders.id, orderId));
    logWebhookEvent(context, `Order ${orderId} marked as payment failed`);
  } catch (error) {
    logWebhookError(
      context,
      `Failed to update failed payment for order ${orderId}`,
      error
    );
    throw error;
  }
}
async function handleAccountUpdated(account, context) {
  logWebhookEvent(context, `Account updated: ${account.id}`, {
    chargesEnabled: account.charges_enabled,
    payoutsEnabled: account.payouts_enabled,
    detailsSubmitted: account.details_submitted
  });
  try {
    await db.update(businesses).set({
      stripeAccountStatus: account.charges_enabled && account.payouts_enabled ? "active" : "pending",
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm29.eq)(businesses.stripeAccountId, account.id));
    logWebhookEvent(
      context,
      `Business account status updated for Stripe account ${account.id}`
    );
  } catch (error) {
    logWebhookError(
      context,
      `Failed to update business for account ${account.id}`,
      error
    );
    throw error;
  }
}
async function handleTransferCreated(transfer, context) {
  logWebhookEvent(context, `Transfer created: ${transfer.id}`, {
    amount: transfer.amount,
    destination: transfer.destination,
    currency: transfer.currency
  });
  const orderId = transfer.metadata?.orderId;
  if (orderId) {
    try {
      await db.insert(transactions).values({
        id: `txn_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,
        orderId,
        businessId: transfer.metadata?.businessId || "",
        userId: transfer.metadata?.userId || "",
        amount: transfer.amount,
        type: "transfer",
        status: "completed",
        stripeTransferId: transfer.id,
        metadata: JSON.stringify({
          destination: transfer.destination,
          currency: transfer.currency
        }),
        createdAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      });
      logWebhookEvent(
        context,
        `Transfer transaction recorded for order ${orderId}`
      );
    } catch (error) {
      logWebhookError(context, `Failed to record transfer transaction`, error);
    }
  }
}
async function handlePayoutPaid(payout, context) {
  logWebhookEvent(context, `Payout paid: ${payout.id}`, {
    amount: payout.amount,
    currency: payout.currency,
    method: payout.method
  });
}
async function handlePayoutFailed(payout, context) {
  logWebhookError(context, `Payout failed: ${payout.id}`, {
    amount: payout.amount,
    currency: payout.currency,
    failureCode: payout.failure_code,
    failureMessage: payout.failure_message
  });
}
async function handleChargeRefunded(charge, context) {
  const paymentIntentId = typeof charge.payment_intent === "string" ? charge.payment_intent : charge.payment_intent?.id;
  if (!paymentIntentId) return;
  const { refunds: refunds2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
  const rows = await db.select().from(refunds2).where((0, import_drizzle_orm29.eq)(refunds2.stripePaymentIntentId, paymentIntentId));
  for (const row of rows) {
    if (row.status !== "completed") {
      await db.update(refunds2).set({ status: "completed", processedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm29.eq)(refunds2.id, row.id));
      logWebhookEvent(context, `Refund ${row.id} confirmada por Stripe`);
    }
  }
}
async function handleRefundFailed(refund, context) {
  const paymentIntentId = typeof refund.payment_intent === "string" ? refund.payment_intent : refund.payment_intent?.id;
  if (!paymentIntentId) return;
  const { refunds: refunds2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
  await db.update(refunds2).set({
    status: "failed",
    failureReason: refund.reason || "Stripe report\xF3 fallo en la devoluci\xF3n"
  }).where((0, import_drizzle_orm29.eq)(refunds2.stripePaymentIntentId, paymentIntentId));
  logWebhookError(context, `Refund failed para PI ${paymentIntentId}`);
}
async function handleDisputeCreated(dispute, context) {
  const paymentIntentId = typeof dispute.payment_intent === "string" ? dispute.payment_intent : dispute.payment_intent?.id;
  if (!paymentIntentId) return;
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm29.eq)(orders.stripePaymentIntentId, paymentIntentId)).limit(1);
  logWebhookError(
    context,
    `Disputa abierta por el banco (chargeback) \u2014 PI ${paymentIntentId}`,
    { orderId: order?.id, amount: dispute.amount, reason: dispute.reason }
  );
  if (order) {
    try {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { inArray: inArray12 } = await import("drizzle-orm");
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      const admins = await db.select({ id: users5.id }).from(users5).where(inArray12(users5.role, ["admin", "super_admin"]));
      for (const admin of admins) {
        await sendPushToUser2(admin.id, {
          title: "\u{1F6A8} Chargeback abierto",
          body: `Pedido ${orderRef(order)}: el cliente ha reclamado ${(dispute.amount / 100).toFixed(2)} \u20AC a su banco. Responde en Stripe antes del plazo.`,
          data: {
            orderId: order.id,
            screen: "AdminDashboard",
            section: "finance_refunds",
            type: "chargeback"
          }
        }).catch(() => {
        });
      }
    } catch {
    }
  }
}
var import_drizzle_orm29, ENV_WEBHOOK_SECRET;
var init_webhookHandlers = __esm({
  "server/webhookHandlers.ts"() {
    "use strict";
    init_stripeClient();
    init_db();
    init_schema_mysql();
    import_drizzle_orm29 = require("drizzle-orm");
    init_orderNumberService();
    ENV_WEBHOOK_SECRET = process.env.STRIPE_WEBHOOK_SECRET;
  }
});

// server/strikeService.ts
var strikeService_exports = {};
__export(strikeService_exports, {
  addStrike: () => addStrike,
  removeStrike: () => removeStrike,
  unblockDriver: () => unblockDriver
});
async function addStrike(driverId, reason, orderId) {
  const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm32.eq)(deliveryDrivers.userId, driverId)).limit(1);
  if (!driver) return;
  const newStrikes = driver.strikes + 1;
  const shouldBlock = newStrikes >= 3;
  await db.update(deliveryDrivers).set({
    strikes: newStrikes,
    isBlocked: shouldBlock,
    blockedReason: shouldBlock ? `3 strikes: ${reason}` : null,
    blockedUntil: shouldBlock ? new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3) : null
  }).where((0, import_drizzle_orm32.eq)(deliveryDrivers.userId, driverId));
  logger.security(`Strike added to driver`, {
    driverId,
    strikes: newStrikes,
    reason,
    orderId,
    blocked: shouldBlock
  });
  if (shouldBlock) {
    await sendPushToUser(driverId, {
      title: "\u26D4 Cuenta suspendida",
      body: "Recibiste 3 strikes. Tu cuenta est\xE1 suspendida por 7 d\xEDas.",
      data: { screen: "DriverProfile" }
    });
  } else {
    await sendPushToUser(driverId, {
      title: `\u26A0\uFE0F Strike ${newStrikes}/3`,
      body: `${reason}. Al llegar a 3 strikes tu cuenta ser\xE1 suspendida.`,
      data: { screen: "DriverProfile" }
    });
  }
}
async function removeStrike(driverId) {
  const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm32.eq)(deliveryDrivers.userId, driverId)).limit(1);
  if (!driver || driver.strikes === 0) return;
  await db.update(deliveryDrivers).set({
    strikes: Math.max(0, driver.strikes - 1)
  }).where((0, import_drizzle_orm32.eq)(deliveryDrivers.userId, driverId));
  logger.info("Strike removed from driver", { driverId });
}
async function unblockDriver(driverId) {
  await db.update(deliveryDrivers).set({
    isBlocked: false,
    blockedReason: null,
    blockedUntil: null,
    strikes: 0
  }).where((0, import_drizzle_orm32.eq)(deliveryDrivers.userId, driverId));
  logger.info("Driver unblocked", { driverId });
}
var import_drizzle_orm32;
var init_strikeService = __esm({
  "server/strikeService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm32 = require("drizzle-orm");
    init_logger();
    init_enhancedPushService();
  }
});

// server/giftCardService.ts
var giftCardService_exports = {};
__export(giftCardService_exports, {
  GiftCardService: () => GiftCardService
});
var import_drizzle_orm33, GiftCardService;
var init_giftCardService = __esm({
  "server/giftCardService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm33 = require("drizzle-orm");
    GiftCardService = class {
      static EXPIRY_DAYS = 30;
      static generateCode() {
        const chars = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
        let code = "";
        for (let i = 0; i < 16; i++) {
          if (i > 0 && i % 4 === 0) code += "-";
          code += chars.charAt(Math.floor(Math.random() * chars.length));
        }
        return code;
      }
      // Crear gift card tras pago confirmado (llamado desde webhook Stripe o aprobación manual)
      static async purchaseGiftCard(data) {
        const {
          purchasedBy,
          amount,
          recipientEmail,
          recipientPhone,
          message,
          design = "default",
          activateImmediately = false
        } = data;
        if (amount < 1e3) return { success: false, error: "Monto m\xEDnimo: \u20AC10" };
        const code = this.generateCode();
        const giftCardId = crypto.randomUUID();
        const expiresAt = activateImmediately ? new Date(Date.now() + this.EXPIRY_DAYS * 24 * 60 * 60 * 1e3) : null;
        await db.insert(giftCards).values({
          id: giftCardId,
          code,
          amount,
          balance: activateImmediately ? amount : 0,
          status: activateImmediately ? "active" : "pending_payment",
          purchasedBy,
          recipientEmail: recipientEmail || null,
          recipientPhone: recipientPhone || null,
          message: message || null,
          design,
          expiresAt
        });
        if (activateImmediately) {
          await db.insert(giftCardTransactions).values({
            id: crypto.randomUUID(),
            giftCardId,
            amount,
            balanceAfter: amount
          });
        }
        return {
          success: true,
          giftCard: {
            id: giftCardId,
            code,
            amount: amount / 100,
            status: activateImmediately ? "active" : "pending_payment",
            expiresAt
          }
        };
      }
      // Subir comprobante de pago para una gift card
      static async submitPaymentProof(data) {
        const {
          giftCardId,
          userId,
          paymentProvider,
          proofImageUrl,
          referenceNumber,
          amount
        } = data;
        const [gc] = await db.select().from(giftCards).where((0, import_drizzle_orm33.eq)(giftCards.id, giftCardId)).limit(1);
        if (!gc) return { success: false, error: "Gift card no encontrada" };
        if (gc.purchasedBy !== userId)
          return { success: false, error: "No autorizado" };
        if (gc.status !== "pending_payment")
          return {
            success: false,
            error: "Esta gift card ya tiene pago registrado"
          };
        await db.insert(paymentProofs).values({
          id: crypto.randomUUID(),
          orderId: null,
          giftCardId,
          userId,
          paymentProvider,
          proofImageUrl,
          referenceNumber: referenceNumber || null,
          amount,
          status: "pending"
        });
        return {
          success: true,
          message: "Comprobante enviado. El admin lo verificar\xE1 pronto."
        };
      }
      // Admin: obtener gift cards pendientes de activación
      static async getPendingGiftCards() {
        const pending = await db.select().from(giftCards).where(
          (0, import_drizzle_orm33.or)(
            (0, import_drizzle_orm33.eq)(giftCards.status, "pending_payment"),
            (0, import_drizzle_orm33.eq)(giftCards.status, "pending_verification")
          )
        );
        return {
          success: true,
          giftCards: pending.map((gc) => ({ ...gc, amount: gc.amount / 100 }))
        };
      }
      // Admin: activar gift card tras verificar pago
      static async activateGiftCard(giftCardId, adminId) {
        const [gc] = await db.select().from(giftCards).where((0, import_drizzle_orm33.eq)(giftCards.id, giftCardId)).limit(1);
        if (!gc) return { success: false, error: "Gift card no encontrada" };
        const expiresAt = new Date(
          Date.now() + this.EXPIRY_DAYS * 24 * 60 * 60 * 1e3
        );
        await db.update(giftCards).set({
          status: "active",
          balance: gc.amount,
          expiresAt
        }).where((0, import_drizzle_orm33.eq)(giftCards.id, giftCardId));
        await db.update(paymentProofs).set({
          status: "approved",
          verifiedBy: adminId,
          verifiedAt: /* @__PURE__ */ new Date()
        }).where(
          (0, import_drizzle_orm33.and)(
            (0, import_drizzle_orm33.eq)(paymentProofs.giftCardId, giftCardId),
            (0, import_drizzle_orm33.eq)(paymentProofs.status, "pending")
          )
        );
        await db.insert(giftCardTransactions).values({
          id: crypto.randomUUID(),
          giftCardId,
          amount: gc.amount,
          balanceAfter: gc.amount
        });
        try {
          const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
          if (gc.purchasedBy) {
            await sendPushToUser2(gc.purchasedBy, {
              title: "\u{1F381} Gift Card activada",
              body: `Tu tarjeta de ${(gc.amount / 100).toFixed(2)} \u20AC ya est\xE1 lista para usar o regalar.`,
              data: { screen: "GiftCards" }
            });
          }
        } catch (err) {
          console.error("Error notifying gift card activation:", err);
        }
        return { success: true, message: "Gift card activada", expiresAt };
      }
      // Admin: rechazar gift card
      static async rejectGiftCard(giftCardId, adminId, reason) {
        await db.update(giftCards).set({ status: "rejected" }).where((0, import_drizzle_orm33.eq)(giftCards.id, giftCardId));
        await db.update(paymentProofs).set({
          status: "rejected",
          verifiedBy: adminId,
          verifiedAt: /* @__PURE__ */ new Date(),
          verificationNotes: reason
        }).where(
          (0, import_drizzle_orm33.and)(
            (0, import_drizzle_orm33.eq)(paymentProofs.giftCardId, giftCardId),
            (0, import_drizzle_orm33.eq)(paymentProofs.status, "pending")
          )
        );
        return { success: true, message: "Gift card rechazada" };
      }
      // Validar gift card en checkout
      static async validateGiftCard(code) {
        const [gc] = await db.select().from(giftCards).where((0, import_drizzle_orm33.eq)(giftCards.code, code.toUpperCase())).limit(1);
        if (!gc) return { success: false, error: "Tarjeta no encontrada" };
        if (gc.status !== "active")
          return {
            success: false,
            error: gc.status === "pending_payment" ? "Tarjeta pendiente de activaci\xF3n" : "Tarjeta no activa"
          };
        if (gc.expiresAt && /* @__PURE__ */ new Date() > new Date(gc.expiresAt)) {
          await db.update(giftCards).set({ status: "expired" }).where((0, import_drizzle_orm33.eq)(giftCards.id, gc.id));
          return { success: false, error: "Tarjeta expirada" };
        }
        if (gc.balance <= 0) return { success: false, error: "Tarjeta sin saldo" };
        return {
          success: true,
          giftCard: {
            id: gc.id,
            code: gc.code,
            balance: gc.balance / 100,
            amount: gc.amount / 100,
            expiresAt: gc.expiresAt
          }
        };
      }
      // Canjear gift card en pedido
      static async redeemGiftCard(data) {
        const { code, orderId, amountToUse } = data;
        const validation = await this.validateGiftCard(code);
        if (!validation.success) return validation;
        const [gc] = await db.select().from(giftCards).where((0, import_drizzle_orm33.eq)(giftCards.code, code.toUpperCase())).limit(1);
        if (!gc || amountToUse > gc.balance)
          return { success: false, error: "Saldo insuficiente" };
        const newBalance = gc.balance - amountToUse;
        await db.update(giftCards).set({
          balance: newBalance,
          redeemedAt: /* @__PURE__ */ new Date(),
          status: newBalance === 0 ? "redeemed" : "active"
        }).where((0, import_drizzle_orm33.eq)(giftCards.id, gc.id));
        await db.insert(giftCardTransactions).values({
          id: crypto.randomUUID(),
          giftCardId: gc.id,
          orderId,
          amount: -amountToUse,
          balanceAfter: newBalance
        });
        return {
          success: true,
          amountRedeemed: amountToUse / 100,
          remainingBalance: newBalance / 100
        };
      }
      // Obtener gift cards del usuario
      static async getUserGiftCards(userId) {
        try {
          const purchased = await db.select().from(giftCards).where((0, import_drizzle_orm33.eq)(giftCards.purchasedBy, userId));
          return {
            success: true,
            purchased: purchased.map((gc) => ({
              ...gc,
              amount: gc.amount / 100,
              balance: gc.balance / 100
            })),
            redeemed: []
          };
        } catch (error) {
          return { success: true, purchased: [], redeemed: [] };
        }
      }
      static async getDesigns() {
        try {
          const designs = await db.select().from(giftCardDesigns).where((0, import_drizzle_orm33.eq)(giftCardDesigns.isActive, true)).orderBy(giftCardDesigns.displayOrder);
          return { success: true, designs };
        } catch {
          return { success: true, designs: [] };
        }
      }
      static async getTransactionHistory(giftCardId) {
        const transactions4 = await db.select().from(giftCardTransactions).where((0, import_drizzle_orm33.eq)(giftCardTransactions.giftCardId, giftCardId));
        return {
          success: true,
          transactions: transactions4.map((t) => ({
            ...t,
            amount: t.amount / 100,
            balanceAfter: t.balanceAfter / 100
          }))
        };
      }
    };
  }
});

// server/payoutService.ts
var payoutService_exports = {};
__export(payoutService_exports, {
  createPayoutsForOrder: () => createPayoutsForOrder,
  deletePaymentAccount: () => deletePaymentAccount,
  getPayoutHistory: () => getPayoutHistory,
  getPendingPayouts: () => getPendingPayouts,
  getUserPaymentAccounts: () => getUserPaymentAccounts,
  markPayoutPaid: () => markPayoutPaid,
  savePaymentAccount: () => savePaymentAccount,
  settleWithdrawalWallet: () => settleWithdrawalWallet
});
async function createPayoutsForOrder(orderId) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm35.eq)(orders.id, orderId)).limit(1);
  if (!order) throw new Error("Pedido no encontrado");
  const existing = await db.select().from(payouts).where((0, import_drizzle_orm35.eq)(payouts.orderId, orderId));
  if (existing.length > 0) return;
  const inserts = [];
  if (order.businessEarnings && order.businessEarnings > 0) {
    let account = null;
    if (order.businessId) {
      const [business] = await db.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm35.eq)(businesses.id, order.businessId)).limit(1);
      if (business?.ownerId) {
        account = await getDefaultAccount(business.ownerId);
      }
    }
    inserts.push({
      orderId,
      recipientId: order.businessId,
      recipientType: "business",
      amount: order.businessEarnings,
      method: account?.method || null,
      accountSnapshot: account ? JSON.stringify(account) : null,
      status: "pending"
    });
  }
  if (order.deliveryPersonId && order.deliveryEarnings && order.deliveryEarnings > 0) {
    const account = await getDefaultAccount(order.deliveryPersonId);
    inserts.push({
      orderId,
      recipientId: order.deliveryPersonId,
      recipientType: "driver",
      amount: order.deliveryEarnings,
      method: account?.method || null,
      accountSnapshot: account ? JSON.stringify(account) : null,
      status: "pending"
    });
  }
  if (inserts.length > 0) {
    await db.insert(payouts).values(inserts);
    logger.info(`\u{1F4B0} Payouts created for order ${orderId}`, {
      count: inserts.length
    });
    try {
      const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      notifyAdmins2({
        type: "new_payouts",
        orderId,
        count: inserts.length,
        message: "Nuevos pagos pendientes de liberaci\xF3n manual"
      });
    } catch (notifyError) {
      console.error("Error notifying admins of payouts:", notifyError);
    }
  }
}
async function markPayoutPaid(payoutId, adminId, notes) {
  const [payout] = await db.select().from(payouts).where((0, import_drizzle_orm35.eq)(payouts.id, payoutId)).limit(1);
  if (!payout) throw new Error("Payout no encontrado");
  if (payout.status === "paid") throw new Error("Ya fue marcado como pagado");
  await db.update(payouts).set({
    status: "paid",
    paidBy: adminId,
    paidAt: /* @__PURE__ */ new Date(),
    notes: notes || null
  }).where((0, import_drizzle_orm35.eq)(payouts.id, payoutId));
  if ((payout.orderId || "").startsWith(WITHDRAWAL_ORDER_PREFIX)) {
    await settleWithdrawalWallet(payout.recipientId, payout.amount);
  }
  await db.insert(transactions).values({
    orderId: payout.orderId,
    userId: payout.recipientId,
    type: "payout_paid",
    amount: payout.amount,
    description: `Pago enviado por admin - Pedido #${payout.orderId.slice(-6)}`,
    status: "completed",
    metadata: JSON.stringify({
      payoutId,
      adminId,
      method: payout.method,
      notes
    })
  });
  logger.info(`\u2705 Payout ${payoutId} marked as paid by admin ${adminId}`);
}
async function settleWithdrawalWallet(userId, amount) {
  const [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm35.eq)(wallets.userId, userId)).limit(1);
  if (!wallet) return;
  await db.update(wallets).set({
    pendingBalance: Math.max(0, (wallet.pendingBalance || 0) - amount),
    totalWithdrawn: (wallet.totalWithdrawn || 0) + amount
  }).where((0, import_drizzle_orm35.eq)(wallets.userId, userId));
}
async function getPendingPayouts() {
  return db.select().from(payouts).where((0, import_drizzle_orm35.eq)(payouts.status, "pending"));
}
async function getPayoutHistory(recipientId) {
  return db.select().from(payouts).where((0, import_drizzle_orm35.eq)(payouts.recipientId, recipientId));
}
async function getDefaultAccount(userId) {
  const [account] = await db.select().from(paymentAccounts).where(
    (0, import_drizzle_orm35.and)(
      (0, import_drizzle_orm35.eq)(paymentAccounts.userId, userId),
      (0, import_drizzle_orm35.eq)(paymentAccounts.isDefault, true)
    )
  ).limit(1);
  return account || null;
}
async function savePaymentAccount(userId, data) {
  if (data.isDefault) {
    await db.update(paymentAccounts).set({ isDefault: false }).where(
      (0, import_drizzle_orm35.and)(
        (0, import_drizzle_orm35.eq)(paymentAccounts.userId, userId),
        (0, import_drizzle_orm35.eq)(paymentAccounts.method, data.method)
      )
    );
  }
  await db.insert(paymentAccounts).values({
    userId,
    method: data.method,
    isDefault: data.isDefault ?? false,
    label: data.label || null,
    pagoMovilPhone: data.pagoMovilPhone || null,
    binanceId: data.binanceId || null,
    zelleEmail: data.zelleEmail || null,
    zinliEmail: data.zinliEmail || null,
    zellePhone: data.zellePhone || null,
    // No usados en España
    pagoMovilBank: null,
    pagoMovilCedula: null,
    binanceEmail: null
  });
}
async function getUserPaymentAccounts(userId) {
  return db.select().from(paymentAccounts).where((0, import_drizzle_orm35.eq)(paymentAccounts.userId, userId));
}
async function deletePaymentAccount(accountId, userId) {
  await db.delete(paymentAccounts).where(
    (0, import_drizzle_orm35.and)(
      (0, import_drizzle_orm35.eq)(paymentAccounts.id, accountId),
      (0, import_drizzle_orm35.eq)(paymentAccounts.userId, userId)
    )
  );
}
var import_drizzle_orm35, WITHDRAWAL_ORDER_PREFIX;
var init_payoutService = __esm({
  "server/payoutService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm35 = require("drizzle-orm");
    init_logger();
    WITHDRAWAL_ORDER_PREFIX = "wdr-";
  }
});

// server/exchangeRateService.ts
var exchangeRateService_exports = {};
__export(exchangeRateService_exports, {
  ExchangeRateService: () => ExchangeRateService,
  exchangeRateService: () => exchangeRateService
});
var import_drizzle_orm36, ExchangeRateService, exchangeRateService;
var init_exchangeRateService = __esm({
  "server/exchangeRateService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm36 = require("drizzle-orm");
    init_logger();
    ExchangeRateService = class _ExchangeRateService {
      static instance;
      cachedRate = null;
      lastFetch = null;
      CACHE_DURATION_MS = 5 * 60 * 1e3;
      // 5 minutos
      FALLBACK_RATE = 36.5;
      constructor() {
      }
      static getInstance() {
        if (!_ExchangeRateService.instance) {
          _ExchangeRateService.instance = new _ExchangeRateService();
        }
        return _ExchangeRateService.instance;
      }
      /**
       * Obtiene la tasa USDT desde alcambio.app
       * Scraping del HTML ya que no tienen API pública
       */
      async fetchFromAlCambio() {
        try {
          logger.info("\u{1F4CA} Fetching USDT rate from alcambio.app...");
          const response = await fetch("https://alcambio.app/tasas", {
            headers: {
              "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
            }
          });
          if (!response.ok) {
            logger.warn(`\u274C AlCambio returned status ${response.status}`);
            return null;
          }
          const html = await response.text();
          const usdtMatch = html.match(
            /Tasa USDT[\s\S]*?Promedio[\s\S]*?(\d{1,3}(?:,\d{3})*(?:\.\d{2})?)\s*Bs/i
          );
          if (usdtMatch && usdtMatch[1]) {
            const rateStr = usdtMatch[1].replace(/,/g, "");
            const rate = parseFloat(rateStr);
            if (!isNaN(rate) && rate > 0) {
              logger.info(`\u2705 USDT rate from AlCambio: ${rate} Bs`);
              return rate;
            }
          }
          logger.warn("\u26A0\uFE0F Could not parse USDT rate from AlCambio HTML");
          return null;
        } catch (error) {
          logger.error("\u274C Error fetching from AlCambio:", error.message);
          return null;
        }
      }
      /**
       * Obtiene la tasa manual configurada por el admin
       */
      async getManualRate() {
        try {
          const [setting] = await db.select().from(systemSettings).where((0, import_drizzle_orm36.eq)(systemSettings.key, "usd_exchange_rate")).limit(1);
          if (setting?.value) {
            const rate = parseFloat(setting.value);
            if (!isNaN(rate) && rate > 0) {
              return rate;
            }
          }
          return null;
        } catch (error) {
          logger.error("Error getting manual rate:", error);
          return null;
        }
      }
      /**
       * Verifica si el admin ha deshabilitado la actualización automática
       */
      async isAutoUpdateEnabled() {
        try {
          const [setting] = await db.select().from(systemSettings).where((0, import_drizzle_orm36.eq)(systemSettings.key, "exchange_rate_auto_update")).limit(1);
          return setting?.value !== "false";
        } catch (error) {
          return true;
        }
      }
      /**
       * Obtiene la tasa de cambio con la siguiente prioridad:
       * 1. Si auto-update está deshabilitado → tasa manual
       * 2. Si está habilitado → alcambio.app (con cache de 5 min)
       * 3. Fallback → tasa manual
       * 4. Fallback final → 36.50
       */
      async getCurrentRate() {
        const autoUpdateEnabled = await this.isAutoUpdateEnabled();
        if (!autoUpdateEnabled) {
          const manualRate2 = await this.getManualRate();
          if (manualRate2) {
            return {
              success: true,
              rate: manualRate2,
              source: "manual"
            };
          }
          return {
            success: true,
            rate: this.FALLBACK_RATE,
            source: "fallback"
          };
        }
        const now = /* @__PURE__ */ new Date();
        if (this.cachedRate && this.lastFetch && now.getTime() - this.lastFetch.getTime() < this.CACHE_DURATION_MS) {
          return {
            success: true,
            rate: this.cachedRate,
            source: "alcambio",
            lastUpdated: this.lastFetch
          };
        }
        const alcambioRate = await this.fetchFromAlCambio();
        if (alcambioRate) {
          this.cachedRate = alcambioRate;
          this.lastFetch = now;
          await this.saveRateToHistory(alcambioRate, "alcambio");
          return {
            success: true,
            rate: alcambioRate,
            source: "alcambio",
            lastUpdated: now
          };
        }
        const manualRate = await this.getManualRate();
        if (manualRate) {
          return {
            success: true,
            rate: manualRate,
            source: "manual"
          };
        }
        return {
          success: true,
          rate: this.FALLBACK_RATE,
          source: "fallback"
        };
      }
      /**
       * Actualiza la tasa manual (solo admin)
       */
      async updateManualRate(rate, adminId) {
        try {
          if (rate <= 0) {
            return { success: false, message: "La tasa debe ser mayor a 0" };
          }
          const [existing] = await db.select().from(systemSettings).where((0, import_drizzle_orm36.eq)(systemSettings.key, "usd_exchange_rate")).limit(1);
          if (existing) {
            await db.update(systemSettings).set({
              value: rate.toString(),
              updatedAt: /* @__PURE__ */ new Date()
            }).where((0, import_drizzle_orm36.eq)(systemSettings.key, "usd_exchange_rate"));
          } else {
            await db.insert(systemSettings).values({
              category: "currency",
              key: "usd_exchange_rate",
              value: rate.toString(),
              description: "Tasa de cambio BsD/USD manual"
            });
          }
          await this.saveRateToHistory(rate, "manual", adminId);
          this.cachedRate = null;
          this.lastFetch = null;
          logger.info(
            `\u{1F4B0} Exchange rate updated manually by ${adminId}: ${rate} Bs/USD`
          );
          return {
            success: true,
            message: `Tasa actualizada a ${rate} Bs/USD`
          };
        } catch (error) {
          logger.error("Error updating manual rate:", error);
          return { success: false, message: error.message };
        }
      }
      /**
       * Habilita/deshabilita la actualización automática
       */
      async toggleAutoUpdate(enabled, adminId) {
        try {
          const [existing] = await db.select().from(systemSettings).where((0, import_drizzle_orm36.eq)(systemSettings.key, "exchange_rate_auto_update")).limit(1);
          if (existing) {
            await db.update(systemSettings).set({
              value: enabled ? "true" : "false",
              updatedAt: /* @__PURE__ */ new Date()
            }).where((0, import_drizzle_orm36.eq)(systemSettings.key, "exchange_rate_auto_update"));
          } else {
            await db.insert(systemSettings).values({
              category: "currency",
              key: "exchange_rate_auto_update",
              value: enabled ? "true" : "false",
              description: "Actualizaci\xF3n autom\xE1tica de tasa desde alcambio.app"
            });
          }
          logger.info(
            `\u{1F504} Auto-update ${enabled ? "enabled" : "disabled"} by ${adminId}`
          );
          return {
            success: true,
            message: `Actualizaci\xF3n autom\xE1tica ${enabled ? "habilitada" : "deshabilitada"}`
          };
        } catch (error) {
          logger.error("Error toggling auto-update:", error);
          return { success: false, message: error.message };
        }
      }
      /**
       * Guarda la tasa en un historial (opcional, para analytics)
       */
      async saveRateToHistory(rate, source, adminId) {
        try {
          logger.info(
            `\u{1F4CA} Rate saved to history: ${rate} Bs/USD (source: ${source})`
          );
        } catch (error) {
        }
      }
      /**
       * Fuerza una actualización inmediata desde alcambio.app
       */
      async forceUpdate() {
        this.cachedRate = null;
        this.lastFetch = null;
        return await this.getCurrentRate();
      }
    };
    exchangeRateService = ExchangeRateService.getInstance();
  }
});

// server/cashSettlementService.ts
var cashSettlementService_exports = {};
__export(cashSettlementService_exports, {
  CashSettlementService: () => CashSettlementService,
  cashSettlementService: () => cashSettlementService
});
var import_drizzle_orm37, CashSettlementService, cashSettlementService;
var init_cashSettlementService = __esm({
  "server/cashSettlementService.ts"() {
    "use strict";
    init_db();
    init_orderNumberService();
    init_schema_mysql();
    import_drizzle_orm37 = require("drizzle-orm");
    init_unifiedFinancialService();
    CashSettlementService = class {
      // Registrar deuda de efectivo al entregar pedido
      async registerCashDebt(orderId, driverId, businessId, total, deliveryFee) {
        const commissions = await financialService.calculateCommissions(
          total,
          deliveryFee
        );
        const [business] = await db.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm37.eq)(businesses.id, businessId)).limit(1);
        const businessOwnerId = business?.ownerId || businessId;
        await db.transaction(async (tx) => {
          let [driverWallet] = await tx.select().from(wallets).where((0, import_drizzle_orm37.eq)(wallets.userId, driverId)).limit(1);
          if (!driverWallet) {
            await tx.insert(wallets).values({
              userId: driverId,
              balance: 0,
              pendingBalance: 0,
              cashOwed: 0,
              cashPending: 0,
              totalEarned: 0,
              totalWithdrawn: 0
            });
            [driverWallet] = await tx.select().from(wallets).where((0, import_drizzle_orm37.eq)(wallets.userId, driverId)).limit(1);
          }
          const debtAmount = commissions.business + commissions.platform;
          await tx.update(wallets).set({
            balance: driverWallet.balance + commissions.driver,
            cashOwed: driverWallet.cashOwed + debtAmount,
            totalEarned: driverWallet.totalEarned + commissions.driver,
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm37.eq)(wallets.userId, driverId));
          let [businessWallet] = await tx.select().from(wallets).where((0, import_drizzle_orm37.eq)(wallets.userId, businessOwnerId)).limit(1);
          if (!businessWallet) {
            await tx.insert(wallets).values({
              userId: businessOwnerId,
              balance: 0,
              pendingBalance: 0,
              cashOwed: 0,
              cashPending: 0,
              totalEarned: 0,
              totalWithdrawn: 0
            });
            [businessWallet] = await tx.select().from(wallets).where((0, import_drizzle_orm37.eq)(wallets.userId, businessOwnerId)).limit(1);
          }
          await tx.update(wallets).set({
            cashPending: businessWallet.cashPending + commissions.business,
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm37.eq)(wallets.userId, businessOwnerId));
          await tx.insert(transactions).values([
            {
              walletId: driverWallet.id,
              userId: driverId,
              orderId,
              type: "delivery_payment",
              amount: commissions.driver,
              balanceBefore: driverWallet.balance,
              balanceAfter: driverWallet.balance + commissions.driver,
              description: `Delivery en efectivo - Pedido #${orderId.slice(-8)}`,
              status: "completed"
            },
            {
              walletId: driverWallet.id,
              userId: driverId,
              orderId,
              type: "cash_debt",
              amount: -debtAmount,
              balanceBefore: driverWallet.cashOwed,
              balanceAfter: driverWallet.cashOwed + debtAmount,
              description: `Efectivo a liquidar - Pedido #${orderId.slice(-8)}`,
              status: "pending"
            }
          ]);
        });
      }
      // Descuento automático de futuras ganancias con tarjeta
      async autoDeductCashDebt(driverId, orderId, earnings) {
        const [driverWallet] = await db.select().from(wallets).where((0, import_drizzle_orm37.eq)(wallets.userId, driverId)).limit(1);
        if (!driverWallet || driverWallet.cashOwed === 0) {
          return { netEarnings: earnings, debtPaid: 0 };
        }
        const debtPayment = Math.min(earnings, driverWallet.cashOwed);
        const netEarnings = earnings - debtPayment;
        await db.transaction(async (tx) => {
          await tx.update(wallets).set({
            balance: driverWallet.balance + netEarnings,
            cashOwed: driverWallet.cashOwed - debtPayment,
            totalEarned: driverWallet.totalEarned + earnings,
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm37.eq)(wallets.userId, driverId));
          await tx.insert(transactions).values({
            walletId: driverWallet.id,
            userId: driverId,
            orderId,
            type: "cash_debt_payment",
            amount: -debtPayment,
            balanceBefore: driverWallet.cashOwed,
            balanceAfter: driverWallet.cashOwed - debtPayment,
            description: `Descuento autom\xE1tico de deuda - Pedido ${await orderRefFromId(orderId)}`,
            status: "completed"
          });
          if (debtPayment > 0) {
            await this.settleCashToBusinesses(tx, driverId, debtPayment);
          }
        });
        return { netEarnings, debtPaid: debtPayment };
      }
      // Liberar fondos al negocio cuando se liquida efectivo
      async settleCashToBusinesses(tx, driverId, amount) {
        const cashOrders = await tx.select().from(orders).where(
          (0, import_drizzle_orm37.and)(
            (0, import_drizzle_orm37.eq)(orders.deliveryPersonId, driverId),
            (0, import_drizzle_orm37.eq)(orders.paymentMethod, "cash"),
            (0, import_drizzle_orm37.eq)(orders.status, "delivered"),
            (0, import_drizzle_orm37.eq)(orders.cashSettled, false)
          )
        ).orderBy(orders.deliveredAt);
        let remainingAmount = amount;
        for (const order of cashOrders) {
          if (remainingAmount <= 0) break;
          const commissions = await financialService.calculateCommissions(
            order.total,
            order.deliveryFee
          );
          const orderDebt = commissions.business + commissions.platform;
          if (remainingAmount >= orderDebt) {
            const [business] = await tx.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm37.eq)(businesses.id, order.businessId)).limit(1);
            const businessOwnerId = business?.ownerId || order.businessId;
            const [businessWallet] = await tx.select().from(wallets).where((0, import_drizzle_orm37.eq)(wallets.userId, businessOwnerId)).limit(1);
            if (businessWallet) {
              await tx.update(wallets).set({
                cashPending: businessWallet.cashPending - commissions.business,
                updatedAt: /* @__PURE__ */ new Date()
              }).where((0, import_drizzle_orm37.eq)(wallets.userId, businessOwnerId));
              await tx.insert(transactions).values({
                walletId: businessWallet.id,
                userId: businessOwnerId,
                orderId: order.id,
                type: "cash_settlement",
                amount: commissions.business,
                balanceBefore: businessWallet.balance,
                balanceAfter: businessWallet.balance,
                description: `Efectivo liquidado - Pedido #${order.id.slice(-8)}`,
                status: "completed"
              });
            }
            await tx.update(orders).set({
              cashSettled: true,
              cashSettledAt: /* @__PURE__ */ new Date()
            }).where((0, import_drizzle_orm37.eq)(orders.id, order.id));
            remainingAmount -= orderDebt;
          }
        }
      }
      // Obtener deuda pendiente del repartidor
      async getDriverDebt(driverId) {
        const [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm37.eq)(wallets.userId, driverId)).limit(1);
        if (!wallet || wallet.cashOwed === 0) {
          return { totalDebt: 0, pendingOrders: [] };
        }
        const cashOrders = await db.select().from(orders).where(
          (0, import_drizzle_orm37.and)(
            (0, import_drizzle_orm37.eq)(orders.deliveryPersonId, driverId),
            (0, import_drizzle_orm37.eq)(orders.paymentMethod, "cash"),
            (0, import_drizzle_orm37.eq)(orders.status, "delivered"),
            (0, import_drizzle_orm37.eq)(orders.cashSettled, false)
          )
        ).orderBy(orders.deliveredAt);
        const pendingOrders = await Promise.all(
          cashOrders.map(async (order) => {
            const commissions = await financialService.calculateCommissions(
              order.total,
              order.deliveryFee
            );
            return {
              orderId: order.id,
              amount: commissions.business + commissions.platform,
              deliveredAt: order.deliveredAt,
              businessName: order.businessName
            };
          })
        );
        return {
          totalDebt: wallet.cashOwed,
          pendingOrders
        };
      }
    };
    cashSettlementService = new CashSettlementService();
  }
});

// server/withdrawalService.ts
var withdrawalService_exports = {};
__export(withdrawalService_exports, {
  WithdrawalService: () => WithdrawalService,
  cancelWithdrawal: () => cancelWithdrawal,
  getWalletBalance: () => getWalletBalance,
  getWithdrawalHistory: () => getWithdrawalHistory,
  requestWithdrawal: () => requestWithdrawal,
  withdrawalService: () => withdrawalService
});
async function getWalletBalance(userId) {
  try {
    const [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm38.eq)(wallets.userId, userId)).limit(1);
    if (!wallet) {
      await db.insert(wallets).values({
        userId,
        balance: 0,
        pendingBalance: 0,
        totalEarned: 0,
        totalWithdrawn: 0,
        cashOwed: 0
      });
      return {
        success: true,
        balance: 0,
        pendingBalance: 0,
        totalEarned: 0,
        totalWithdrawn: 0,
        cashOwed: 0,
        availableForWithdrawal: 0
      };
    }
    const availableForWithdrawal = Math.max(
      0,
      wallet.balance - (wallet.cashOwed || 0)
    );
    return {
      success: true,
      balance: wallet.balance,
      pendingBalance: wallet.pendingBalance || 0,
      totalEarned: wallet.totalEarned || 0,
      totalWithdrawn: wallet.totalWithdrawn || 0,
      cashOwed: wallet.cashOwed || 0,
      availableForWithdrawal
    };
  } catch (error) {
    console.error("Error getting wallet balance:", error);
    throw new Error("Error al obtener balance de wallet");
  }
}
async function getWithdrawalHistory(userId) {
  return withdrawalService.getWithdrawalHistory(userId);
}
async function requestWithdrawal(request) {
  return await withdrawalService.requestWithdrawal(request);
}
async function cancelWithdrawal(withdrawalId, userId) {
  try {
    const [withdrawal] = await db.select().from(payouts).where(
      (0, import_drizzle_orm38.and)(
        (0, import_drizzle_orm38.eq)(payouts.id, withdrawalId),
        (0, import_drizzle_orm38.eq)(payouts.recipientId, userId),
        (0, import_drizzle_orm38.eq)(payouts.status, "pending")
      )
    ).limit(1);
    if (!withdrawal) {
      throw new Error(
        "Solicitud de retiro no encontrada o no se puede cancelar"
      );
    }
    await db.update(payouts).set({ status: "cancelled", paidAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm38.eq)(payouts.id, withdrawalId));
    const [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm38.eq)(wallets.userId, userId)).limit(1);
    if (wallet) {
      await db.update(wallets).set({
        balance: wallet.balance + withdrawal.amount,
        pendingBalance: Math.max(
          0,
          (wallet.pendingBalance || 0) - withdrawal.amount
        )
      }).where((0, import_drizzle_orm38.eq)(wallets.userId, userId));
    }
    try {
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      await sendPushToUser2(userId, {
        title: "\u21A9\uFE0F Retiro cancelado",
        body: `Tu solicitud de retiro de \u20AC${(withdrawal.amount / 100).toFixed(2)} fue cancelada y el saldo fue devuelto a tu wallet.`,
        data: { screen: "Wallet", payoutId: withdrawal.id }
      });
    } catch (notifyError) {
      console.error("Error notifying withdrawal cancellation:", notifyError);
    }
    return {
      success: true,
      message: "Solicitud de retiro cancelada"
    };
  } catch (error) {
    console.error("Error cancelling withdrawal:", error);
    throw new Error(error.message || "Error al cancelar retiro");
  }
}
var import_drizzle_orm38, MINIMUM_WITHDRAWAL, WITHDRAWAL_ORDER_PREFIX2, WithdrawalService, withdrawalService;
var init_withdrawalService = __esm({
  "server/withdrawalService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm38 = require("drizzle-orm");
    MINIMUM_WITHDRAWAL = 5e3;
    WITHDRAWAL_ORDER_PREFIX2 = "wdr-";
    WithdrawalService = class {
      async requestWithdrawal(request) {
        const { financialService: financialService2 } = await Promise.resolve().then(() => (init_unifiedFinancialService(), unifiedFinancialService_exports));
        const [user] = await db.select().from(users).where((0, import_drizzle_orm38.eq)(users.id, request.userId)).limit(1);
        if (!user) {
          throw new Error("Usuario no encontrado");
        }
        const canWithdraw = await financialService2.canUserWithdraw(
          request.userId,
          user.role
        );
        if (!canWithdraw.allowed) {
          throw new Error(canWithdraw.reason || "No puedes retirar en este momento");
        }
        if (request.amount < MINIMUM_WITHDRAWAL) {
          throw new Error("El monto m\xEDnimo de retiro es 50 \u20AC");
        }
        const wallet = await financialService2.getWallet(request.userId);
        const availableBalance = wallet.balance - (wallet.cashOwed || 0);
        if (request.amount > availableBalance) {
          throw new Error("Saldo insuficiente");
        }
        let bankAccount = request.bankAccount;
        let pagoMovilPhone = request.pagoMovilPhone;
        if (!bankAccount && !pagoMovilPhone) {
          const accounts = await db.select().from(paymentAccounts).where((0, import_drizzle_orm38.eq)(paymentAccounts.userId, request.userId));
          const account = accounts.find((a) => a.isDefault) || accounts[0];
          if (!account) {
            throw new Error(
              "Configura primero tu cuenta de pago en tu perfil (M\xE9todos de pago)"
            );
          }
          if (account.method === "bizum" && account.pagoMovilPhone) {
            pagoMovilPhone = account.pagoMovilPhone;
          } else {
            bankAccount = {
              accountNumber: account.binanceId || account.zelleEmail || account.zinliEmail || "",
              bankName: account.method === "paypal" ? "PayPal" : account.method === "bizum" ? "Bizum" : "Transferencia SEPA",
              accountHolder: account.zelleEmail || "",
              accountType: account.method
            };
          }
        }
        const snapshot = bankAccount ? JSON.stringify(bankAccount) : JSON.stringify({ pagoMovilPhone: pagoMovilPhone || null });
        const payoutId = crypto.randomUUID();
        await db.insert(payouts).values({
          id: payoutId,
          orderId: `${WITHDRAWAL_ORDER_PREFIX2}${payoutId}`,
          recipientId: request.userId,
          recipientType: user.role === "business_owner" ? "business" : "driver",
          amount: request.amount,
          method: request.method || "bank_transfer",
          accountSnapshot: snapshot,
          status: "pending",
          notes: JSON.stringify({ type: "withdrawal" })
        });
        await db.update(wallets).set({
          balance: Math.max(0, wallet.balance - request.amount),
          pendingBalance: (wallet.pendingBalance || 0) + request.amount
        }).where((0, import_drizzle_orm38.eq)(wallets.userId, request.userId));
        try {
          const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
          notifyAdmins2({
            type: "new_withdrawal",
            payoutId,
            userId: request.userId,
            amount: request.amount,
            message: "Nueva solicitud de retiro pendiente de aprobaci\xF3n"
          });
          const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
          const pendingAdmins = await db.select({ id: users.id }).from(users).where((0, import_drizzle_orm38.eq)(users.role, "admin"));
          for (const admin of pendingAdmins) {
            await sendPushToUser2(admin.id, {
              title: "\u{1F4B0} Retiro pendiente",
              body: `Un usuario solicit\xF3 retirar ${(request.amount / 100).toFixed(2)} \u20AC. Revisa Finanzas.`,
              data: { screen: "AdminFinance" }
            });
          }
        } catch (notifyError) {
          console.error("Error notifying admins of withdrawal:", notifyError);
        }
        return {
          success: true,
          id: payoutId,
          status: "pending",
          message: "Solicitud enviada. El admin la aprobar\xE1 pronto."
        };
      }
      async getWithdrawalHistory(userId) {
        const all = await db.select().from(payouts).where((0, import_drizzle_orm38.eq)(payouts.recipientId, userId)).orderBy((0, import_drizzle_orm38.desc)(payouts.createdAt));
        return all.filter(
          (p) => (p.orderId || "").startsWith(WITHDRAWAL_ORDER_PREFIX2)
        );
      }
      // Admin: Aprobar un retiro (marcar pagado + liquidar el saldo retenido)
      async approveWithdrawal(withdrawalId, adminId) {
        const [withdrawal] = await db.select().from(payouts).where((0, import_drizzle_orm38.eq)(payouts.id, withdrawalId)).limit(1);
        if (!withdrawal || withdrawal.status !== "pending") {
          throw new Error("Solicitud no v\xE1lida");
        }
        await db.update(payouts).set({
          status: "paid",
          paidBy: adminId,
          paidAt: /* @__PURE__ */ new Date()
        }).where((0, import_drizzle_orm38.eq)(payouts.id, withdrawalId));
        const [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm38.eq)(wallets.userId, withdrawal.recipientId)).limit(1);
        if (wallet) {
          await db.update(wallets).set({
            pendingBalance: Math.max(
              0,
              (wallet.pendingBalance || 0) - withdrawal.amount
            ),
            totalWithdrawn: (wallet.totalWithdrawn || 0) + withdrawal.amount
          }).where((0, import_drizzle_orm38.eq)(wallets.userId, withdrawal.recipientId));
        }
        try {
          const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
          await sendPushToUser2(withdrawal.recipientId, {
            title: "\u{1F4B0} Retiro enviado",
            body: `ComeYa ha enviado tu retiro de \u20AC${(withdrawal.amount / 100).toFixed(2)}. Rev\xEDsalo en tu cuenta.`,
            data: { screen: "Wallet", payoutId: withdrawal.id }
          });
        } catch (notifyError) {
          console.error("Error notifying withdrawal approval:", notifyError);
        }
        return { success: true };
      }
    };
    withdrawalService = new WithdrawalService();
  }
});

// server/weeklySettlementService.ts
var import_drizzle_orm45, WeeklySettlementService;
var init_weeklySettlementService = __esm({
  "server/weeklySettlementService.ts"() {
    "use strict";
    init_db();
    import_drizzle_orm45 = require("drizzle-orm");
    init_logger();
    WeeklySettlementService = class {
      static getRows(result) {
        if (Array.isArray(result)) {
          return Array.isArray(result[0]) ? result[0] : result;
        }
        return result?.rows || [];
      }
      /**
       * FLUJO RESTRICTIVO VIERNES: Cierra la semana y crea liquidaciones
       * Se ejecuta cada viernes a las 11:59 PM
       * BLOQUEA inmediatamente a drivers con deuda para forzar liquidación
       */
      static async closeWeek() {
        const today = /* @__PURE__ */ new Date();
        const weekStart = new Date(today);
        weekStart.setDate(today.getDate() - 7);
        const weekEnd = new Date(today);
        const driversWithDebt = await db.execute(import_drizzle_orm45.sql`
      SELECT w.user_id, w.cash_owed, u.name, u.phone
      FROM wallets w
      JOIN users u ON w.user_id = u.id
      WHERE w.cash_owed > 0
    `);
        let settlementsCreated = 0;
        const driverRows = this.getRows(driversWithDebt);
        for (const driver of driverRows) {
          await db.execute(import_drizzle_orm45.sql`
        INSERT INTO weekly_settlements 
        (id, driver_id, week_start, week_end, amount_owed, status, created_at, deadline)
        VALUES (UUID(), ${driver.user_id}, ${weekStart.toISOString().split("T")[0]}, 
                ${weekEnd.toISOString().split("T")[0]}, ${driver.cash_owed}, 'pending', NOW(), 
                DATE_ADD(NOW(), INTERVAL 48 HOUR))
      `);
          await db.execute(import_drizzle_orm45.sql`
        UPDATE users 
        SET is_active = 0, blocked_reason = CONCAT('Deuda semanal: $', ${(driver.cash_owed / 100).toFixed(2)}, '. Liquida antes del lunes.')
        WHERE id = ${driver.user_id}
      `);
          await db.execute(import_drizzle_orm45.sql`
        UPDATE delivery_drivers 
        SET is_available = 0 
        WHERE user_id = ${driver.user_id}
      `);
          settlementsCreated++;
          logger.info(
            `\u{1F6AB} Driver ${driver.name} bloqueado por deuda: $${(driver.cash_owed / 100).toFixed(2)}`
          );
        }
        await db.execute(import_drizzle_orm45.sql`
      INSERT INTO audit_logs (user_id, action, entity_type, changes, created_at)
      VALUES ('system', 'weekly_close', 'settlement',
        ${JSON.stringify({ settlements_created: settlementsCreated, drivers_blocked: settlementsCreated })},
        NOW())
    `);
        console.log(
          `\u2705 FLUJO RESTRICTIVO: Semana cerrada. ${settlementsCreated} drivers bloqueados hasta liquidar.`
        );
        return {
          success: true,
          count: settlementsCreated,
          blocked: settlementsCreated
        };
      }
      /**
       * BLOQUEO LUNES: Bloquea drivers que no pagaron en 48 horas
       * Se ejecuta cada lunes a las 12:00 AM
       * Garantiza que cuentas se bloqueen si no liquidan
       */
      static async blockUnpaidDrivers() {
        const now = /* @__PURE__ */ new Date();
        const overdueSettlements = await db.execute(import_drizzle_orm45.sql`
      SELECT DISTINCT ws.driver_id, ws.amount_owed, u.name, u.phone
      FROM weekly_settlements ws
      JOIN users u ON ws.driver_id = u.id
      WHERE ws.status = 'pending' 
      AND ws.deadline < NOW()
      AND u.is_active = 1
    `);
        let driversBlocked = 0;
        const overdueRows = this.getRows(overdueSettlements);
        for (const settlement of overdueRows) {
          await db.execute(import_drizzle_orm45.sql`
        UPDATE users 
        SET is_active = 0, 
            blocked_reason = CONCAT('Deuda vencida: $', ${(settlement.amount_owed / 100).toFixed(2)}, '. Contacta soporte para reactivar.'),
            blocked_at = NOW()
        WHERE id = ${settlement.driver_id}
      `);
          await db.execute(import_drizzle_orm45.sql`
        UPDATE delivery_drivers 
        SET is_available = 0, 
            blocked_reason = 'Deuda vencida sin liquidar'
        WHERE user_id = ${settlement.driver_id}
      `);
          await db.execute(import_drizzle_orm45.sql`
        UPDATE weekly_settlements 
        SET status = 'overdue', 
            notes = 'Driver bloqueado por falta de pago'
        WHERE driver_id = ${settlement.driver_id} AND status = 'pending'
      `);
          driversBlocked++;
          logger.error(
            `\u{1F6AB} Driver ${settlement.name} BLOQUEADO por deuda vencida: $${(settlement.amount_owed / 100).toFixed(2)}`
          );
        }
        await db.execute(import_drizzle_orm45.sql`
      INSERT INTO audit_logs (user_id, action, entity_type, changes, created_at)
      VALUES ('system', 'monday_block', 'settlement',
        ${JSON.stringify({ drivers_blocked: driversBlocked })},
        NOW())
    `);
        console.log(
          `\u{1F6AB} BLOQUEO LUNES: ${driversBlocked} drivers bloqueados por falta de pago.`
        );
        return { success: true, blocked: driversBlocked };
      }
      /**
       * Obtener liquidación pendiente del driver
       */
      static async getDriverPendingSettlement(driverId) {
        const result = await db.execute(import_drizzle_orm45.sql`
      SELECT * FROM weekly_settlements 
      WHERE driver_id = ${driverId} 
      AND status IN ('pending', 'submitted')
      ORDER BY created_at DESC 
      LIMIT 1
    `);
        const rows = this.getRows(result);
        return rows[0] || null;
      }
      /**
       * Driver sube comprobante de pago
       */
      static async submitPaymentProof(settlementId, proofUrl) {
        await db.execute(import_drizzle_orm45.sql`
      UPDATE weekly_settlements 
      SET status = 'submitted', 
          payment_proof_url = ${proofUrl},
          submitted_at = NOW()
      WHERE id = ${settlementId}
    `);
        return { success: true };
      }
      /**
       * Admin aprueba liquidación - marca payout como pagado y desbloquea driver
       */
      static async approveSettlement(settlementId, adminId) {
        const result = await db.execute(import_drizzle_orm45.sql`
      SELECT ws.driver_id, ws.amount_owed, u.name
      FROM weekly_settlements ws
      JOIN users u ON ws.driver_id = u.id
      WHERE ws.id = ${settlementId}
    `);
        const rows = this.getRows(result);
        const settlement = rows[0];
        if (!settlement) throw new Error("Liquidaci\xF3n no encontrada");
        await db.execute(import_drizzle_orm45.sql`
      UPDATE weekly_settlements 
      SET status = 'approved', approved_at = NOW(), approved_by = ${adminId}
      WHERE id = ${settlementId}
    `);
        await db.execute(import_drizzle_orm45.sql`
      UPDATE wallets 
      SET cash_owed = GREATEST(0, cash_owed - ${settlement.amount_owed})
      WHERE user_id = ${settlement.driver_id}
    `);
        await db.execute(import_drizzle_orm45.sql`
      UPDATE users SET is_active = 1, blocked_reason = NULL WHERE id = ${settlement.driver_id}
    `);
        await db.execute(import_drizzle_orm45.sql`
      UPDATE delivery_drivers SET is_available = 1, blocked_reason = NULL WHERE user_id = ${settlement.driver_id}
    `);
        logger.info(
          `\u2705 Liquidaci\xF3n aprobada: ${settlement.name} - $${(settlement.amount_owed / 100).toFixed(2)}`
        );
        return { success: true };
      }
      /**
       * Admin rechaza liquidación
       */
      static async rejectSettlement(settlementId, adminId, notes) {
        await db.execute(import_drizzle_orm45.sql`
      UPDATE weekly_settlements 
      SET status = 'rejected', 
          approved_by = ${adminId},
          notes = ${notes},
          approved_at = NOW()
      WHERE id = ${settlementId}
    `);
        return { success: true };
      }
      /**
       * Obtener todas las liquidaciones pendientes (Admin)
       */
      static async getAllPendingSettlements() {
        const result = await db.execute(import_drizzle_orm45.sql`
      SELECT ws.*, u.name as driver_name, u.phone as driver_phone
      FROM weekly_settlements ws
      JOIN users u ON ws.driver_id = u.id
      WHERE ws.status IN ('pending', 'submitted')
      ORDER BY ws.created_at DESC
    `);
        return this.getRows(result);
      }
      /**
       * Calcular ganancias semanales del driver para transferencia Stripe
       */
      static async calculateDriverWeeklyEarnings(driverId) {
        const weekAgo = /* @__PURE__ */ new Date();
        weekAgo.setDate(weekAgo.getDate() - 7);
        const result = await db.execute(import_drizzle_orm45.sql`
      SELECT COALESCE(SUM(delivery_earnings), 0) as total_earnings
      FROM orders 
      WHERE delivery_person_id = ${driverId}
      AND status = 'delivered'
      AND delivered_at >= ${weekAgo.toISOString()}
      AND payment_method = 'card'
    `);
        const earnings = result.rows[0]?.total_earnings || 0;
        return Math.max(0, earnings);
      }
      /**
       * Obtener historial completo de transacciones para auditoría
       */
      static async getTransactionHistory(driverId, limit = 100) {
        let query = import_drizzle_orm45.sql`
      SELECT 
        t.id,
        t.type,
        t.amount,
        t.description,
        t.status,
        t.created_at,
        t.order_id,
        u.name as user_name,
        o.total as order_total,
        o.payment_method
      FROM transactions t
      JOIN users u ON t.user_id = u.id
      LEFT JOIN orders o ON t.order_id = o.id
    `;
        if (driverId) {
          query = import_drizzle_orm45.sql`${query} WHERE t.user_id = ${driverId}`;
        }
        query = import_drizzle_orm45.sql`${query} ORDER BY t.created_at DESC LIMIT ${limit}`;
        const result = await db.execute(query);
        return result.rows;
      }
    };
  }
});

// server/utils/locationFilter.ts
function haversineM(lat1, lng1, lat2, lng2) {
  const R = 6371e3;
  const toRad3 = (deg) => deg * Math.PI / 180;
  const dLat = toRad3(lat2 - lat1);
  const dLng = toRad3(lng2 - lng1);
  const a = Math.sin(dLat / 2) ** 2 + Math.cos(toRad3(lat1)) * Math.cos(toRad3(lat2)) * Math.sin(dLng / 2) ** 2;
  return R * 2 * Math.asin(Math.sqrt(a));
}
function evaluateDriverFix(states, driverId, rawLat, rawLng, opts) {
  const lat = Number(rawLat);
  const lng = Number(rawLng);
  if (!Number.isFinite(lat) || !Number.isFinite(lng)) {
    return { accept: false, reason: "not_finite" };
  }
  if (lat === 0 && lng === 0) return { accept: false, reason: "zero" };
  const accuracy = Number(opts?.accuracy);
  if (Number.isFinite(accuracy) && accuracy > MAX_ACCURACY_M) {
    return { accept: false, reason: "low_accuracy" };
  }
  const now = opts?.now ?? Date.now();
  const at = Number.isFinite(Number(opts?.timestamp)) && opts.timestamp > 0 ? Number(opts.timestamp) : now;
  const prev = states.get(driverId);
  if (prev) {
    if (at <= prev.at) return { accept: false, reason: "stale" };
    const dt = (at - prev.at) / 1e3;
    const dist = haversineM(prev.lat, prev.lng, lat, lng);
    if (dt > 0 && dist / dt > MAX_SPEED_MPS) {
      return { accept: false, reason: "impossible_jump" };
    }
  }
  states.set(driverId, { lat, lng, at });
  return { accept: true };
}
var MAX_ACCURACY_M, MAX_SPEED_MPS;
var init_locationFilter = __esm({
  "server/utils/locationFilter.ts"() {
    "use strict";
    MAX_ACCURACY_M = 100;
    MAX_SPEED_MPS = 35;
  }
});

// server/enhancedTrackingService.ts
var enhancedTrackingService_exports = {};
__export(enhancedTrackingService_exports, {
  EnhancedTrackingService: () => EnhancedTrackingService
});
var import_drizzle_orm48, ETA_CACHE_TTL_MS, etaCache, EnhancedTrackingService;
var init_enhancedTrackingService = __esm({
  "server/enhancedTrackingService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm48 = require("drizzle-orm");
    init_enhancedPushService();
    init_orderNumberService();
    ETA_CACHE_TTL_MS = 6e4;
    etaCache = /* @__PURE__ */ new Map();
    EnhancedTrackingService = class {
      // Calcular distancia entre dos puntos (Haversine)
      static calculateDistance(loc1, loc2) {
        const R = 6371;
        const dLat = this.toRad(loc2.latitude - loc1.latitude);
        const dLon = this.toRad(loc2.longitude - loc1.longitude);
        const lat1 = this.toRad(loc1.latitude);
        const lat2 = this.toRad(loc2.latitude);
        const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.sin(dLon / 2) * Math.sin(dLon / 2) * Math.cos(lat1) * Math.cos(lat2);
        const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
        return R * c;
      }
      static toRad(degrees) {
        return degrees * (Math.PI / 180);
      }
      // Actualizar ubicación del repartidor y verificar proximidad
      static async updateDriverLocation(driverId, latitude, longitude, heading, speed) {
        const { handleDriverLocationUpdate: handleDriverLocationUpdate2 } = await Promise.resolve().then(() => (init_trackingPipeline(), trackingPipeline_exports));
        const result = await handleDriverLocationUpdate2(
          driverId,
          latitude,
          longitude,
          { heading, speed }
        );
        return {
          success: result.success,
          location: { latitude, longitude, heading, speed }
        };
      }
      // Verificar y enviar alertas de tiempo (5 min, 2 min)
      static async checkTimeAlerts(orderId, etaMinutes) {
        const [order] = await db.select().from(orders).where((0, import_drizzle_orm48.eq)(orders.id, orderId)).limit(1);
        if (!order || !order.deliveryPersonId) return;
        const timeAlerts = [
          {
            type: "eta_5min",
            threshold: 5,
            message: "\xA1Tu pedido llega en 5 minutos!"
          },
          {
            type: "eta_2min",
            threshold: 2,
            message: "\xA1Tu pedido llega en 2 minutos!"
          }
        ];
        for (const alert of timeAlerts) {
          if (etaMinutes <= alert.threshold) {
            const [existing] = await db.select().from(proximityAlerts).where(
              (0, import_drizzle_orm48.and)(
                (0, import_drizzle_orm48.eq)(proximityAlerts.orderId, orderId),
                (0, import_drizzle_orm48.eq)(proximityAlerts.alertType, alert.type)
              )
            ).limit(1);
            if (!existing) {
              await db.insert(proximityAlerts).values({
                orderId,
                driverId: order.deliveryPersonId,
                alertType: alert.type,
                distance: 0,
                destinationType: "customer",
                notificationSent: true
              });
              await sendPushToUser(order.userId, {
                title: alert.message,
                body: `Pedido ${orderRef(order)}`,
                data: { orderId, screen: "OrderTracking", type: alert.type }
              });
            }
          }
        }
      }
      // Obtener ubicación actual del repartidor
      static async getDriverLocation(orderId) {
        const [order] = await db.select().from(orders).where((0, import_drizzle_orm48.eq)(orders.id, orderId)).limit(1);
        if (!order || !order.deliveryPersonId) {
          return { success: false, error: "Pedido sin repartidor asignado" };
        }
        const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm48.eq)(deliveryDrivers.userId, order.deliveryPersonId)).limit(1);
        if (!driver || !driver.currentLatitude || !driver.currentLongitude) {
          return {
            success: false,
            error: "Ubicaci\xF3n del repartidor no disponible"
          };
        }
        return {
          success: true,
          location: {
            latitude: driver.currentLatitude,
            longitude: driver.currentLongitude,
            lastUpdate: driver.lastLocationUpdate
          }
        };
      }
      // Calcular ETA dinámico
      static async calculateDynamicETA(orderId) {
        const [order] = await db.select().from(orders).where((0, import_drizzle_orm48.eq)(orders.id, orderId)).limit(1);
        if (!order || !order.deliveryPersonId) {
          return { success: false, eta: null };
        }
        const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm48.eq)(deliveryDrivers.userId, order.deliveryPersonId)).limit(1);
        if (!driver || !driver.currentLatitude || !driver.currentLongitude || !order.deliveryLatitude || !order.deliveryLongitude) {
          return { success: false, eta: null };
        }
        const driverLocation = {
          latitude: parseFloat(driver.currentLatitude),
          longitude: parseFloat(driver.currentLongitude)
        };
        const customerLocation = {
          latitude: parseFloat(order.deliveryLatitude),
          longitude: parseFloat(order.deliveryLongitude)
        };
        const distance = this.calculateDistance(driverLocation, customerLocation);
        let etaMinutes = null;
        const etaCacheKey = `eta:${orderId}`;
        const cachedETA = etaCache.get(etaCacheKey);
        if (cachedETA && Date.now() - cachedETA.at < ETA_CACHE_TTL_MS) {
          etaMinutes = cachedETA.minutes;
        } else if (["picked_up", "on_the_way", "in_transit", "arriving"].includes(
          order.status
        ) && distance > 1) {
          try {
            const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
            const dirs = await googleMapsService2.getDirections(
              driverLocation.latitude,
              driverLocation.longitude,
              customerLocation.latitude,
              customerLocation.longitude
            );
            if (dirs?.duration?.value) {
              etaMinutes = Math.max(1, Math.ceil(dirs.duration.value / 60));
              etaCache.set(etaCacheKey, { at: Date.now(), minutes: etaMinutes });
            }
          } catch (e) {
            console.error("ETA directions fallback:", e);
          }
        }
        if (etaMinutes == null) {
          const avgSpeed = 25;
          etaMinutes = Math.ceil(distance / avgSpeed * 60);
        }
        const totalETA = Math.max(1, etaMinutes);
        const etaDate = new Date(Date.now() + totalETA * 60 * 1e3);
        await this.checkTimeAlerts(orderId, totalETA);
        return {
          success: true,
          eta: {
            minutes: totalETA,
            timestamp: etaDate,
            distance: Math.round(distance * 1e3),
            // en metros
            confidence: distance < 5 ? 95 : distance < 10 ? 85 : 75
          }
        };
      }
      // Obtener hitos del pedido
      static async getOrderMilestones(orderId) {
        const [order] = await db.select().from(orders).where((0, import_drizzle_orm48.eq)(orders.id, orderId)).limit(1);
        if (!order) {
          return { success: false, milestones: null };
        }
        return {
          success: true,
          milestones: {
            orderPlaced: order.createdAt,
            restaurantConfirmed: order.businessResponseAt,
            preparationStarted: order.businessResponseAt,
            driverAssigned: order.assignedAt,
            pickedUp: order.driverPickedUpAt,
            onTheWay: order.driverPickedUpAt,
            delivered: order.deliveredAt
          }
        };
      }
    };
  }
});

// server/arrivingStatusService.ts
var arrivingStatusService_exports = {};
__export(arrivingStatusService_exports, {
  checkAllActiveOrdersForArriving: () => checkAllActiveOrdersForArriving,
  checkAndUpdateArrivingStatus: () => checkAndUpdateArrivingStatus
});
function calculateDistance2(lat1, lon1, lat2, lon2) {
  const R = 6371;
  const dLat = toRad2(lat2 - lat1);
  const dLon = toRad2(lon2 - lon1);
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(toRad2(lat1)) * Math.cos(toRad2(lat2)) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
function toRad2(degrees) {
  return degrees * (Math.PI / 180);
}
async function checkAndUpdateArrivingStatus(orderId, driverLat, driverLng) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm49.eq)(orders.id, orderId)).limit(1);
  if (!order || !order.deliveryLatitude || !order.deliveryLongitude) {
    return false;
  }
  if (!["picked_up", "on_the_way", "in_transit"].includes(order.status)) {
    return false;
  }
  const deliveryLat = parseFloat(order.deliveryLatitude);
  const deliveryLng = parseFloat(order.deliveryLongitude);
  const distance = calculateDistance2(
    driverLat,
    driverLng,
    deliveryLat,
    deliveryLng
  );
  if (distance <= 0.5) {
    await db.update(orders).set({ status: "arriving" }).where((0, import_drizzle_orm49.eq)(orders.id, orderId));
    return true;
  }
  return false;
}
async function checkAllActiveOrdersForArriving() {
  const activeOrders = await db.select().from(orders).where(
    (0, import_drizzle_orm49.and)(
      (0, import_drizzle_orm49.inArray)(orders.status, ["picked_up", "on_the_way", "in_transit"]),
      (0, import_drizzle_orm49.isNotNull)(orders.deliveryPersonId)
      // Tiene driver asignado
    )
  );
  for (const order of activeOrders) {
    if (!order.deliveryPersonId) continue;
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm49.eq)(deliveryDrivers.userId, order.deliveryPersonId)).limit(1);
    if (driver && driver.currentLatitude && driver.currentLongitude && order.deliveryLatitude && order.deliveryLongitude) {
      await checkAndUpdateArrivingStatus(
        order.id,
        parseFloat(driver.currentLatitude),
        parseFloat(driver.currentLongitude)
      );
    }
  }
}
var import_drizzle_orm49;
var init_arrivingStatusService = __esm({
  "server/arrivingStatusService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm49 = require("drizzle-orm");
  }
});

// server/trackingPipeline.ts
var trackingPipeline_exports = {};
__export(trackingPipeline_exports, {
  handleDriverLocationUpdate: () => handleDriverLocationUpdate,
  haversineMeters: () => haversineMeters
});
function haversineMeters(lat1, lng1, lat2, lng2) {
  const R = 6371e3;
  const toRad3 = (deg) => deg * Math.PI / 180;
  const dLat = toRad3(lat2 - lat1);
  const dLng = toRad3(lng2 - lng1);
  const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(toRad3(lat1)) * Math.cos(toRad3(lat2)) * Math.sin(dLng / 2) * Math.sin(dLng / 2);
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
async function checkProximityAlerts(order, driverId, distanceMeters) {
  for (const alert of PROXIMITY_ALERTS) {
    if (distanceMeters > alert.distance) continue;
    const [existing] = await db.select().from(proximityAlerts).where(
      (0, import_drizzle_orm50.and)(
        (0, import_drizzle_orm50.eq)(proximityAlerts.orderId, order.id),
        (0, import_drizzle_orm50.eq)(proximityAlerts.alertType, alert.type)
      )
    ).limit(1);
    if (!existing) {
      await db.insert(proximityAlerts).values({
        orderId: order.id,
        driverId,
        alertType: alert.type,
        distance: Math.round(distanceMeters),
        destinationType: "customer",
        notificationSent: true
      });
      await sendPushToUser(order.userId, {
        title: alert.title,
        body: `${alert.body} \xB7 Pedido ${await orderRefFromId(order.id)}`,
        data: { orderId: order.id, screen: "OrderTracking", type: alert.type }
      }).catch(() => {
      });
    }
  }
}
async function checkGeofenceMarkers(order, driverLat, driverLng, distanceToCustomerMeters) {
  if (["accepted", "preparing", "ready"].includes(order.status)) {
    const [biz] = await db.select({
      latitude: businesses.latitude,
      longitude: businesses.longitude,
      ownerId: businesses.ownerId,
      name: businesses.name
    }).from(businesses).where((0, import_drizzle_orm50.eq)(businesses.id, order.businessId)).limit(1);
    if (biz?.latitude && biz?.longitude) {
      const d = haversineMeters(
        driverLat,
        driverLng,
        parseFloat(biz.latitude),
        parseFloat(biz.longitude)
      );
      if (d <= PICKUP_RADIUS_M) {
        const [existing] = await db.select().from(proximityAlerts).where(
          (0, import_drizzle_orm50.and)(
            (0, import_drizzle_orm50.eq)(proximityAlerts.orderId, order.id),
            (0, import_drizzle_orm50.eq)(proximityAlerts.destinationType, "business"),
            (0, import_drizzle_orm50.eq)(proximityAlerts.alertType, "arrived")
          )
        ).limit(1);
        if (!existing && biz.ownerId) {
          await db.insert(proximityAlerts).values({
            orderId: order.id,
            driverId: order.deliveryPersonId,
            alertType: "arrived",
            distance: Math.round(d),
            destinationType: "business",
            notificationSent: true
          });
          await sendPushToUser(biz.ownerId, {
            title: "\u{1F3EA} El repartidor lleg\xF3 a tu local",
            body: `Pedido ${await orderRefFromId(order.id)} \xB7 ${biz.name}`,
            data: { orderId: order.id, screen: "BusinessOrders", type: "driver_arrived" }
          }).catch(() => {
          });
        }
      }
    }
  }
  if (["picked_up", "on_the_way", "in_transit"].includes(order.status) && !order.driverArrivedAt && distanceToCustomerMeters <= ARRIVED_RADIUS_M) {
    await db.update(orders).set({ driverArrivedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm50.eq)(orders.id, order.id));
  }
}
async function runOrderChecks(order, driverLat, driverLng) {
  if (!order.deliveryLatitude || !order.deliveryLongitude) return;
  const distanceToCustomer = haversineMeters(
    driverLat,
    driverLng,
    parseFloat(order.deliveryLatitude),
    parseFloat(order.deliveryLongitude)
  );
  await checkProximityAlerts(order, order.deliveryPersonId, distanceToCustomer);
  try {
    const { EnhancedTrackingService: EnhancedTrackingService2 } = await Promise.resolve().then(() => (init_enhancedTrackingService(), enhancedTrackingService_exports));
    await EnhancedTrackingService2.calculateDynamicETA(order.id);
    const { checkAndUpdateArrivingStatus: checkAndUpdateArrivingStatus2 } = await Promise.resolve().then(() => (init_arrivingStatusService(), arrivingStatusService_exports));
    await checkAndUpdateArrivingStatus2(order.id, driverLat, driverLng);
  } catch (e) {
    console.error("trackingPipeline checks error:", e);
  }
  await checkGeofenceMarkers(order, driverLat, driverLng, distanceToCustomer);
}
async function handleDriverLocationUpdate(userId, rawLatitude, rawLongitude, extra) {
  const verdict = evaluateDriverFix(driverFixStates, userId, rawLatitude, rawLongitude, {
    accuracy: extra?.accuracy,
    timestamp: extra?.timestamp
  });
  if (!verdict.accept) {
    return { success: false, error: `Posici\xF3n descartada (${verdict.reason})` };
  }
  const latitude = Number(rawLatitude);
  const longitude = Number(rawLongitude);
  await db.update(deliveryDrivers).set({
    currentLatitude: latitude.toString(),
    currentLongitude: longitude.toString(),
    lastLocationUpdate: /* @__PURE__ */ new Date()
  }).where((0, import_drizzle_orm50.eq)(deliveryDrivers.userId, userId));
  const activeOrders = await db.select().from(orders).where(
    (0, import_drizzle_orm50.and)(
      (0, import_drizzle_orm50.eq)(orders.deliveryPersonId, userId),
      (0, import_drizzle_orm50.inArray)(orders.status, [
        "accepted",
        "preparing",
        "ready",
        "picked_up",
        "on_the_way",
        "in_transit",
        "arriving"
      ])
    )
  );
  const now = Date.now();
  for (const order of activeOrders) {
    const lastEmit = lastEmitAt.get(order.id) ?? 0;
    if (now - lastEmit >= EMIT_INTERVAL_MS) {
      lastEmitAt.set(order.id, now);
      const payload = {
        orderId: order.id,
        driverId: userId,
        businessId: order.businessId,
        status: order.status,
        latitude,
        longitude,
        heading: extra?.heading ?? null,
        speed: extra?.speed ?? null,
        lastUpdate: (/* @__PURE__ */ new Date()).toISOString()
      };
      try {
        const io2 = getIO();
        io2.to(`order:${order.id}`).emit("driver_location", payload);
        io2.to(`business:${order.businessId}`).emit("driver_location", payload);
        io2.to("admins").emit("driver_location", payload);
      } catch {
      }
    }
    const lastCheck = lastCheckAt.get(order.id) ?? 0;
    if (now - lastCheck < CHECK_INTERVAL_MS) continue;
    lastCheckAt.set(order.id, now);
    try {
      await runOrderChecks(order, latitude, longitude);
    } catch (e) {
      console.error(`trackingPipeline error for order ${order.id}:`, e);
    }
  }
  return { success: true, activeOrders: activeOrders.length };
}
var import_drizzle_orm50, CHECK_INTERVAL_MS, EMIT_INTERVAL_MS, PICKUP_RADIUS_M, ARRIVED_RADIUS_M, lastCheckAt, lastEmitAt, PROXIMITY_ALERTS, driverFixStates;
var init_trackingPipeline = __esm({
  "server/trackingPipeline.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm50 = require("drizzle-orm");
    init_enhancedPushService();
    init_orderNumberService();
    init_websocket();
    init_locationFilter();
    CHECK_INTERVAL_MS = 3e4;
    EMIT_INTERVAL_MS = 2e3;
    PICKUP_RADIUS_M = 200;
    ARRIVED_RADIUS_M = 200;
    lastCheckAt = /* @__PURE__ */ new Map();
    lastEmitAt = /* @__PURE__ */ new Map();
    PROXIMITY_ALERTS = [
      {
        type: "nearby",
        distance: 500,
        title: "Tu repartidor est\xE1 a 500m",
        body: "Tu pedido est\xE1 muy cerca"
      },
      {
        type: "approaching",
        distance: 200,
        title: "Tu repartidor est\xE1 llegando (200m)",
        body: "Prep\xE1rate para recibir tu pedido"
      },
      {
        type: "arrived",
        distance: 50,
        title: "\xA1Tu repartidor ha llegado!",
        body: "Tu pedido est\xE1 siendo entregado"
      }
    ];
    driverFixStates = /* @__PURE__ */ new Map();
  }
});

// server/metricsService.ts
var metricsService_exports = {};
__export(metricsService_exports, {
  calculateETAAccuracy: () => calculateETAAccuracy,
  updateBusinessPrepTimeMetrics: () => updateBusinessPrepTimeMetrics,
  updateDriverSpeedMetrics: () => updateDriverSpeedMetrics
});
async function updateBusinessPrepTimeMetrics(businessId) {
  const completedOrders = await db.select().from(orders).where(
    import_drizzle_orm51.sql`business_id = ${businessId} AND status = 'delivered' AND actual_prep_time IS NOT NULL`
  ).limit(50);
  if (completedOrders.length === 0) return;
  const avgPrepTime = Math.round(
    completedOrders.reduce((sum3, o) => sum3 + (o.actualPrepTime || 0), 0) / completedOrders.length
  );
  await db.update(businesses).set({ avgPrepTime }).where((0, import_drizzle_orm51.eq)(businesses.id, businessId));
  console.log(
    `\u{1F4CA} Business ${businessId} avg prep time updated: ${avgPrepTime} min`
  );
}
async function updateDriverSpeedMetrics(driverId) {
  const completedDeliveries = await db.select().from(orders).where(
    import_drizzle_orm51.sql`delivery_person_id = ${driverId} AND status = 'delivered' AND actual_delivery_time IS NOT NULL`
  ).limit(50);
  if (completedDeliveries.length === 0) return;
  let totalSpeed = 0;
  let validDeliveries = 0;
  for (const order of completedDeliveries) {
    if (order.actualDeliveryTime && order.actualDeliveryTime > 0) {
      const estimatedDistance = 3;
      const timeInHours = order.actualDeliveryTime / 60;
      const speed = estimatedDistance / timeInHours;
      if (speed > 5 && speed < 60) {
        totalSpeed += speed;
        validDeliveries++;
      }
    }
  }
  if (validDeliveries > 0) {
    const avgSpeed = totalSpeed / validDeliveries;
    await db.update(deliveryDrivers).set({ avgSpeed: Math.round(avgSpeed * 100) / 100 }).where((0, import_drizzle_orm51.eq)(deliveryDrivers.userId, driverId));
    console.log(
      `\u{1F4CA} Driver ${driverId} avg speed updated: ${avgSpeed.toFixed(2)} km/h`
    );
  }
}
async function calculateETAAccuracy(orderId) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm51.eq)(orders.id, orderId)).limit(1);
  if (!order || !order.estimatedTotalTime || !order.deliveredAt || !order.createdAt) {
    return 0;
  }
  const actualTime = Math.floor(
    (new Date(order.deliveredAt).getTime() - new Date(order.createdAt).getTime()) / 6e4
  );
  const estimatedTime = order.estimatedTotalTime;
  const difference = Math.abs(actualTime - estimatedTime);
  const accuracy = Math.max(0, 100 - difference / estimatedTime * 100);
  return Math.round(accuracy);
}
var import_drizzle_orm51;
var init_metricsService = __esm({
  "server/metricsService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm51 = require("drizzle-orm");
  }
});

// server/fundReleaseService.ts
var fundReleaseService_exports = {};
__export(fundReleaseService_exports, {
  FundReleaseService: () => FundReleaseService,
  fundReleaseService: () => fundReleaseService
});
async function getStripe4() {
  const key = process.env.STRIPE_SECRET_KEY;
  if (!key) return null;
  try {
    const StripeModule = await import("stripe");
    const StripeClass = StripeModule.default || StripeModule;
    return new StripeClass(key);
  } catch {
    return null;
  }
}
async function stripeTransfer(stripe2, amount, stripeAccountId, orderId, description) {
  try {
    const transfer = await stripe2.transfers.create({
      amount,
      currency: "eur",
      destination: stripeAccountId,
      transfer_group: orderId,
      description
    });
    return transfer.id;
  } catch (err) {
    logger.error(`Stripe transfer failed: ${err.message}`, {
      orderId,
      stripeAccountId
    });
    return null;
  }
}
async function getBusinessStripeAccount(businessId) {
  const [biz] = await db.select({
    stripeAccountId: businesses.stripeAccountId,
    stripeAccountStatus: businesses.stripeAccountStatus,
    ownerId: businesses.ownerId
  }).from(businesses).where((0, import_drizzle_orm55.eq)(businesses.id, businessId)).limit(1);
  if (!biz) return { accountId: null, ownerId: null };
  const active = biz.stripeAccountStatus === "active" && biz.stripeAccountId;
  return {
    accountId: active ? biz.stripeAccountId : null,
    ownerId: biz.ownerId ?? null
  };
}
async function getDriverStripeAccount(driverId) {
  const [driver] = await db.select({
    stripeAccountId: deliveryDrivers.stripeAccountId,
    stripeAccountStatus: deliveryDrivers.stripeAccountStatus,
    userId: deliveryDrivers.userId
  }).from(deliveryDrivers).where((0, import_drizzle_orm55.eq)(deliveryDrivers.id, driverId)).limit(1);
  if (!driver) return { accountId: null, userId: null };
  const active = driver.stripeAccountStatus === "active" && driver.stripeAccountId;
  return {
    accountId: active ? driver.stripeAccountId : null,
    userId: driver.userId ?? null
  };
}
async function notifyAdminManualPayout(orderId, recipientName, amount, type) {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { sql: sql23 } = await import("drizzle-orm");
    const admins = await db.select({ id: users5.id }).from(users5).where(sql23`role IN ('admin', 'super_admin')`);
    const amountEur = (amount / 100).toFixed(2);
    const label = type === "business" ? "negocio" : "repartidor";
    for (const admin of admins) {
      await sendPushToUser(admin.id, {
        title: `\u{1F4B8} Payout pendiente \u2014 ${label}`,
        body: `${recipientName} debe recibir \u20AC${amountEur} por pedido ${await orderRefFromId(orderId)}`,
        data: { screen: "AdminPayouts", orderId }
      }).catch(() => {
      });
    }
  } catch (err) {
    logger.error("Error notifying admin of manual payout:", err);
  }
}
async function notifyRecipientPayout(userId, amount, orderId, method, type) {
  const amountEur = (amount / 100).toFixed(2);
  const msg = method === "stripe" ? `\u20AC${amountEur} transferidos autom\xE1ticamente a tu cuenta bancaria` : `\u20AC${amountEur} pendientes de transferencia por el pedido ${await orderRefFromId(orderId)}`;
  await sendPushToUser(userId, {
    title: method === "stripe" ? "\u2705 Pago recibido" : "\u23F3 Pago en proceso",
    body: msg,
    data: {
      screen: type === "business" ? "BusinessFinances" : "DriverEarnings",
      orderId
    }
  }).catch(() => {
  });
}
async function releaseFunds(order) {
  const stripe2 = await getStripe4();
  const existing = await db.select().from(payouts).where((0, import_drizzle_orm55.eq)(payouts.orderId, order.id));
  if (existing.length > 0) return;
  const customerPaidWithStripe = typeof order.paymentMethod === "string" && order.paymentMethod.startsWith("stripe_");
  const businessEarnings = order.businessEarnings ?? order.subtotal ?? 0;
  const deliveryEarnings = order.deliveryEarnings ?? order.deliveryFee ?? 0;
  const inserts = [];
  const orderUpdates = {
    fundsReleased: true,
    fundsReleasedAt: /* @__PURE__ */ new Date(),
    updatedAt: /* @__PURE__ */ new Date()
  };
  if (businessEarnings > 0) {
    const { accountId, ownerId } = await getBusinessStripeAccount(
      order.businessId
    );
    if (stripe2 && accountId && customerPaidWithStripe) {
      const transferId = await stripeTransfer(
        stripe2,
        businessEarnings,
        accountId,
        order.id,
        `Pago negocio pedido ${await orderRefFromId(order.id)}`
      );
      if (transferId) {
        orderUpdates.businessTransferId = transferId;
        inserts.push({
          orderId: order.id,
          recipientId: order.businessId,
          recipientType: "business",
          amount: businessEarnings,
          method: "stripe",
          status: "stripe_auto",
          stripeTransferId: transferId,
          paidAt: /* @__PURE__ */ new Date(),
          notes: `Transfer autom\xE1tico Stripe: ${transferId}`
        });
        if (ownerId)
          await notifyRecipientPayout(
            ownerId,
            businessEarnings,
            order.id,
            "stripe",
            "business"
          );
        logger.info(`\u2705 Stripe transfer to business: ${transferId}`, {
          orderId: order.id,
          amount: businessEarnings
        });
      } else {
        inserts.push({
          orderId: order.id,
          recipientId: order.businessId,
          recipientType: "business",
          amount: businessEarnings,
          method: "stripe_failed_manual",
          status: "pending"
        });
        if (ownerId) {
          await notifyAdminManualPayout(
            order.id,
            order.businessName || "Negocio",
            businessEarnings,
            "business"
          );
          await notifyRecipientPayout(
            ownerId,
            businessEarnings,
            order.id,
            "manual",
            "business"
          );
        }
      }
    } else {
      inserts.push({
        orderId: order.id,
        recipientId: order.businessId,
        recipientType: "business",
        amount: businessEarnings,
        method: "manual",
        status: "pending",
        notes: customerPaidWithStripe ? "Cliente pag\xF3 con Stripe pero el negocio no tiene Connect activo" : `Cliente pag\xF3 de forma manual (${order.paymentMethod || "manual"})`
      });
      if (ownerId) {
        await notifyAdminManualPayout(
          order.id,
          order.businessName || "Negocio",
          businessEarnings,
          "business"
        );
        await notifyRecipientPayout(
          ownerId,
          businessEarnings,
          order.id,
          "manual",
          "business"
        );
      }
    }
  }
  if (order.deliveryPersonId && deliveryEarnings > 0) {
    const { accountId, userId } = await getDriverStripeAccount(
      order.deliveryPersonId
    );
    if (stripe2 && accountId && customerPaidWithStripe) {
      const transferId = await stripeTransfer(
        stripe2,
        deliveryEarnings,
        accountId,
        order.id,
        `Pago repartidor pedido ${await orderRefFromId(order.id)}`
      );
      if (transferId) {
        orderUpdates.driverTransferId = transferId;
        inserts.push({
          orderId: order.id,
          recipientId: order.deliveryPersonId,
          recipientType: "driver",
          amount: deliveryEarnings,
          method: "stripe",
          status: "stripe_auto",
          stripeTransferId: transferId,
          paidAt: /* @__PURE__ */ new Date(),
          notes: `Transfer autom\xE1tico Stripe: ${transferId}`
        });
        if (userId)
          await notifyRecipientPayout(
            userId,
            deliveryEarnings,
            order.id,
            "stripe",
            "driver"
          );
        logger.info(`\u2705 Stripe transfer to driver: ${transferId}`, {
          orderId: order.id,
          amount: deliveryEarnings
        });
      } else {
        inserts.push({
          orderId: order.id,
          recipientId: order.deliveryPersonId,
          recipientType: "driver",
          amount: deliveryEarnings,
          method: "stripe_failed_manual",
          status: "pending"
        });
        if (userId) {
          await notifyAdminManualPayout(
            order.id,
            "Repartidor",
            deliveryEarnings,
            "driver"
          );
          await notifyRecipientPayout(
            userId,
            deliveryEarnings,
            order.id,
            "manual",
            "driver"
          );
        }
      }
    } else {
      inserts.push({
        orderId: order.id,
        recipientId: order.deliveryPersonId,
        recipientType: "driver",
        amount: deliveryEarnings,
        method: "manual",
        status: "pending",
        notes: customerPaidWithStripe ? "Cliente pag\xF3 con Stripe pero el repartidor no tiene Connect activo" : `Cliente pag\xF3 de forma manual (${order.paymentMethod || "manual"})`
      });
      if (userId) {
        await notifyAdminManualPayout(
          order.id,
          "Repartidor",
          deliveryEarnings,
          "driver"
        );
        await notifyRecipientPayout(
          userId,
          deliveryEarnings,
          order.id,
          "manual",
          "driver"
        );
      }
    }
  }
  if (inserts.length > 0) await db.insert(payouts).values(inserts);
  await db.update(orders).set(orderUpdates).where((0, import_drizzle_orm55.eq)(orders.id, order.id));
  logger.info(`\u{1F4B0} Funds released for order ${order.id}`, {
    businessEarnings,
    deliveryEarnings,
    payouts: inserts.map((i) => ({
      type: i.recipientType,
      method: i.method,
      status: i.status
    }))
  });
}
var import_drizzle_orm55, FundReleaseService, fundReleaseService;
var init_fundReleaseService = __esm({
  "server/fundReleaseService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm55 = require("drizzle-orm");
    init_logger();
    init_orderNumberService();
    init_enhancedPushService();
    FundReleaseService = class _FundReleaseService {
      static instance;
      constructor() {
      }
      static getInstance() {
        if (!_FundReleaseService.instance)
          _FundReleaseService.instance = new _FundReleaseService();
        return _FundReleaseService.instance;
      }
      async releaseOnCustomerConfirmation(orderId, customerId) {
        try {
          const [order] = await db.select().from(orders).where((0, import_drizzle_orm55.eq)(orders.id, orderId)).limit(1);
          if (!order) return { success: false, message: "Pedido no encontrado" };
          if (order.userId !== customerId)
            return { success: false, message: "No autorizado" };
          if (order.status !== "delivered")
            return {
              success: false,
              message: "El pedido a\xFAn no ha sido entregado"
            };
          if (order.fundsReleased)
            return {
              success: true,
              alreadyReleased: true,
              message: "Los fondos ya fueron liberados"
            };
          await db.update(orders).set({
            confirmedByCustomer: true,
            confirmedByCustomerAt: /* @__PURE__ */ new Date(),
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm55.eq)(orders.id, orderId));
          await releaseFunds(order);
          return {
            success: true,
            message: "Entrega confirmada. Los pagos han sido procesados.",
            orderId: order.id,
            amountReleased: order.total
          };
        } catch (error) {
          logger.error("Error releasing funds on customer confirmation:", error);
          return { success: false, message: error.message };
        }
      }
      async releaseOrderFunds(orderId) {
        try {
          const [order] = await db.select().from(orders).where((0, import_drizzle_orm55.eq)(orders.id, orderId)).limit(1);
          if (!order) return { success: false, message: "Pedido no encontrado" };
          if (order.fundsReleased)
            return { success: false, message: "Los fondos ya fueron liberados" };
          await releaseFunds(order);
          return {
            success: true,
            message: "Fondos liberados exitosamente",
            orderId: order.id,
            amountReleased: order.total
          };
        } catch (error) {
          logger.error("Error releasing order funds:", error);
          return { success: false, message: error.message };
        }
      }
      async autoReleaseFunds() {
        const deliveredAt24hAgo = new Date(Date.now() - 24 * 60 * 60 * 1e3);
        const ordersToRelease = await db.select().from(orders).where(
          (0, import_drizzle_orm55.and)(
            (0, import_drizzle_orm55.eq)(orders.status, "delivered"),
            (0, import_drizzle_orm55.eq)(orders.fundsReleased, false),
            (0, import_drizzle_orm55.lt)(orders.deliveredAt, deliveredAt24hAgo)
          )
        );
        let released = 0, failed = 0;
        for (const order of ordersToRelease) {
          try {
            await releaseFunds(order);
            released++;
            logger.info(`\u23F0 Auto-released funds: Order ${order.id}`);
          } catch (err) {
            failed++;
            logger.error(
              `Failed to auto-release funds for order ${order.id}:`,
              err
            );
          }
        }
        logger.info(
          `\u{1F504} Auto-release batch: ${released} released, ${failed} failed`
        );
        return { released, failed };
      }
      async disputeOrder(orderId, customerId, reason) {
        try {
          const [order] = await db.select().from(orders).where((0, import_drizzle_orm55.eq)(orders.id, orderId)).limit(1);
          if (!order) return { success: false, message: "Pedido no encontrado" };
          if (order.userId !== customerId)
            return { success: false, message: "No autorizado" };
          if (order.fundsReleased)
            return {
              success: false,
              message: "Los fondos ya fueron liberados. Contacta soporte."
            };
          await db.update(orders).set({
            status: "disputed",
            cancellationReason: reason,
            updatedAt: /* @__PURE__ */ new Date()
          }).where((0, import_drizzle_orm55.eq)(orders.id, orderId));
          logger.warn(`\u26A0\uFE0F Order disputed: ${orderId}`, { customerId, reason });
          return {
            success: true,
            message: "Disputa registrada. Un administrador revisar\xE1 tu caso.",
            orderId: order.id
          };
        } catch (error) {
          logger.error("Error disputing order:", error);
          return { success: false, message: error.message };
        }
      }
    };
    fundReleaseService = FundReleaseService.getInstance();
  }
});

// server/advancedCouponService.ts
var advancedCouponService_exports = {};
__export(advancedCouponService_exports, {
  AdvancedCouponService: () => AdvancedCouponService
});
var import_drizzle_orm57, AdvancedCouponService;
var init_advancedCouponService = __esm({
  "server/advancedCouponService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm57 = require("drizzle-orm");
    AdvancedCouponService = class {
      /**
       * Valida un cupón y calcula el descuento aplicable
       */
      static async validateCoupon(code, context) {
        try {
          const [coupon] = await db.select().from(coupons).where(
            (0, import_drizzle_orm57.and)((0, import_drizzle_orm57.eq)(coupons.code, code.toUpperCase()), (0, import_drizzle_orm57.eq)(coupons.isActive, true))
          ).limit(1);
          if (!coupon) {
            return { valid: false, discount: 0, message: "Cup\xF3n no v\xE1lido" };
          }
          if (coupon.expiresAt && new Date(coupon.expiresAt) < /* @__PURE__ */ new Date()) {
            return { valid: false, discount: 0, message: "Cup\xF3n expirado" };
          }
          if (coupon.maxUses && coupon.usedCount >= coupon.maxUses) {
            return { valid: false, discount: 0, message: "Cup\xF3n agotado" };
          }
          if (coupon.maxUsesPerUser) {
            const userUsageCount = await this.getUserCouponUsageCount(
              context.userId,
              coupon.id
            );
            if (userUsageCount >= coupon.maxUsesPerUser) {
              return {
                valid: false,
                discount: 0,
                message: `Ya usaste este cup\xF3n ${coupon.maxUsesPerUser} ${coupon.maxUsesPerUser === 1 ? "vez" : "veces"}`
              };
            }
          }
          if (coupon.minOrderAmount && context.orderTotal < coupon.minOrderAmount) {
            return {
              valid: false,
              discount: 0,
              message: `Pedido m\xEDnimo de Bs. ${(coupon.minOrderAmount / 100).toFixed(2)}`
            };
          }
          const newUsersOnly = coupon.newUsersOnly;
          if (newUsersOnly) {
            const isNewUser = await this.isNewUser(context.userId);
            if (!isNewUser) {
              return {
                valid: false,
                discount: 0,
                message: "Cup\xF3n solo para nuevos usuarios"
              };
            }
          }
          const firstOrderOnly = coupon.firstOrderOnly;
          if (firstOrderOnly && !context.isFirstOrder) {
            return {
              valid: false,
              discount: 0,
              message: "Cup\xF3n solo para primera compra"
            };
          }
          const validForBusinesses = coupon.validForBusinesses;
          if (validForBusinesses && context.businessId) {
            try {
              const businessIds = JSON.parse(validForBusinesses);
              if (Array.isArray(businessIds) && !businessIds.includes(context.businessId)) {
                return {
                  valid: false,
                  discount: 0,
                  message: "Cup\xF3n no v\xE1lido para este negocio"
                };
              }
            } catch (e) {
              console.error("Error parsing validForBusinesses:", e);
            }
          }
          const validForCategories = coupon.validForCategories;
          if (validForCategories && context.categories) {
            try {
              const allowedCategories = JSON.parse(validForCategories);
              if (Array.isArray(allowedCategories)) {
                const hasValidCategory = context.categories.some(
                  (cat) => allowedCategories.includes(cat)
                );
                if (!hasValidCategory) {
                  return {
                    valid: false,
                    discount: 0,
                    message: "Cup\xF3n no v\xE1lido para estas categor\xEDas"
                  };
                }
              }
            } catch (e) {
              console.error("Error parsing validForCategories:", e);
            }
          }
          const validForProducts = coupon.validForProducts;
          if (validForProducts && context.productIds) {
            try {
              const allowedProducts = JSON.parse(validForProducts);
              if (Array.isArray(allowedProducts)) {
                const hasValidProduct = context.productIds.some(
                  (pid) => allowedProducts.includes(pid)
                );
                if (!hasValidProduct) {
                  return {
                    valid: false,
                    discount: 0,
                    message: "Cup\xF3n no v\xE1lido para estos productos"
                  };
                }
              }
            } catch (e) {
              console.error("Error parsing validForProducts:", e);
            }
          }
          const dayOfWeek = coupon.dayOfWeek;
          if (dayOfWeek) {
            try {
              const allowedDays = JSON.parse(dayOfWeek);
              const currentDay = (context.currentTime || /* @__PURE__ */ new Date()).getDay();
              if (Array.isArray(allowedDays) && !allowedDays.includes(currentDay)) {
                const dayNames = [
                  "Domingo",
                  "Lunes",
                  "Martes",
                  "Mi\xE9rcoles",
                  "Jueves",
                  "Viernes",
                  "S\xE1bado"
                ];
                const validDays = allowedDays.map((d) => dayNames[d]).join(", ");
                return {
                  valid: false,
                  discount: 0,
                  message: `Cup\xF3n v\xE1lido solo: ${validDays}`
                };
              }
            } catch (e) {
              console.error("Error parsing dayOfWeek:", e);
            }
          }
          const timeRange = coupon.timeRange;
          if (timeRange) {
            try {
              const range = JSON.parse(timeRange);
              const currentTime = context.currentTime || /* @__PURE__ */ new Date();
              const currentHour = currentTime.getHours();
              const currentMinute = currentTime.getMinutes();
              const currentTimeStr = `${currentHour.toString().padStart(2, "0")}:${currentMinute.toString().padStart(2, "0")}`;
              if (range.start && range.end) {
                if (currentTimeStr < range.start || currentTimeStr > range.end) {
                  return {
                    valid: false,
                    discount: 0,
                    message: `Cup\xF3n v\xE1lido de ${range.start} a ${range.end}`
                  };
                }
              }
            } catch (e) {
              console.error("Error parsing timeRange:", e);
            }
          }
          const discount = this.calculateDiscount(coupon, context.orderTotal);
          return {
            valid: true,
            discount,
            coupon,
            message: `Descuento de Bs. ${(discount / 100).toFixed(2)} aplicado`
          };
        } catch (error) {
          console.error("Error validating coupon:", error);
          return { valid: false, discount: 0, message: "Error al validar cup\xF3n" };
        }
      }
      /**
       * Calcula el descuento basado en el tipo de cupón
       */
      static calculateDiscount(coupon, orderTotal) {
        const type = coupon.type || coupon.discountType;
        switch (type) {
          case "percentage":
            let discount = Math.round(orderTotal * coupon.discountValue / 100);
            if (coupon.maxDiscount && discount > coupon.maxDiscount) {
              discount = coupon.maxDiscount;
            }
            return discount;
          case "fixed":
            return Math.min(coupon.discountValue, orderTotal);
          case "free_delivery":
            return coupon.discountValue || 2500;
          case "bogo":
            return Math.round(orderTotal * 0.5);
          case "first_order":
            return Math.round(orderTotal * coupon.discountValue / 100);
          default:
            return 0;
        }
      }
      /**
       * Obtiene el número de veces que un usuario ha usado un cupón
       */
      static async getUserCouponUsageCount(userId, couponId) {
        try {
          const result = await db.execute(
            `SELECT COUNT(*) as count FROM coupon_usage WHERE user_id = ? AND coupon_id = ?`,
            [userId, couponId]
          );
          return result[0]?.count || 0;
        } catch (error) {
          console.error("Error getting coupon usage count:", error);
          return 0;
        }
      }
      /**
       * Verifica si un usuario es nuevo (sin pedidos completados)
       */
      static async isNewUser(userId) {
        try {
          const result = await db.execute(
            `SELECT COUNT(*) as count FROM orders WHERE user_id = ? AND status = 'delivered'`,
            [userId]
          );
          return (result[0]?.count || 0) === 0;
        } catch (error) {
          console.error("Error checking if new user:", error);
          return false;
        }
      }
      /**
       * Registra el uso de un cupón
       */
      static async recordCouponUsage(couponId, userId, orderId, discountApplied) {
        try {
          await db.execute(
            `INSERT INTO coupon_usage (id, coupon_id, user_id, order_id, discount_applied) 
         VALUES (UUID(), ?, ?, ?, ?)`,
            [couponId, userId, orderId, discountApplied]
          );
          await db.execute(
            `UPDATE coupons SET used_count = used_count + 1 WHERE id = ?`,
            [couponId]
          );
        } catch (error) {
          console.error("Error recording coupon usage:", error);
          throw error;
        }
      }
      /**
       * Obtiene cupones disponibles para un usuario
       */
      static async getAvailableCoupons(userId) {
        try {
          const now = /* @__PURE__ */ new Date();
          const availableCoupons = await db.select().from(coupons).where(
            (0, import_drizzle_orm57.and)(
              (0, import_drizzle_orm57.eq)(coupons.isActive, true),
              (0, import_drizzle_orm57.or)((0, import_drizzle_orm57.isNull)(coupons.expiresAt), (0, import_drizzle_orm57.gte)(coupons.expiresAt, now))
            )
          );
          const filtered = [];
          for (const coupon of availableCoupons) {
            if (coupon.maxUsesPerUser) {
              const usageCount = await this.getUserCouponUsageCount(
                userId,
                coupon.id
              );
              if (usageCount >= coupon.maxUsesPerUser) {
                continue;
              }
            }
            if (coupon.maxUses && coupon.usedCount >= coupon.maxUses) {
              continue;
            }
            filtered.push(coupon);
          }
          return filtered;
        } catch (error) {
          console.error("Error getting available coupons:", error);
          return [];
        }
      }
    };
  }
});

// server/scheduledOrdersService.ts
var scheduledOrdersService_exports = {};
__export(scheduledOrdersService_exports, {
  ScheduledOrdersService: () => ScheduledOrdersService
});
var import_drizzle_orm61, ScheduledOrdersService;
var init_scheduledOrdersService = __esm({
  "server/scheduledOrdersService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm61 = require("drizzle-orm");
    ScheduledOrdersService = class {
      // Crear pedido programado
      static async createScheduledOrder(data) {
        const itemsRaw = Array.isArray(data.items) ? JSON.stringify(data.items) : typeof data.items === "string" ? data.items : "[]";
        const scheduledFor = data.scheduledFor || data.scheduledDate;
        const [scheduled] = await db.insert(scheduledOrders).values({
          userId: data.userId,
          businessId: data.businessId,
          items: itemsRaw,
          scheduledFor: new Date(scheduledFor),
          deliveryAddress: data.deliveryAddress || "Pendiente de confirmar",
          deliveryLatitude: data.deliveryLatitude || null,
          deliveryLongitude: data.deliveryLongitude || null,
          paymentMethod: data.paymentMethod || "card",
          notes: data.notes || null,
          status: "pending"
        });
        return { success: true, scheduledOrderId: scheduled.insertId };
      }
      // Obtener pedidos programados del usuario
      static async getUserScheduledOrders(userId) {
        const scheduled = await db.select().from(scheduledOrders).where(
          (0, import_drizzle_orm61.and)(
            (0, import_drizzle_orm61.eq)(scheduledOrders.userId, userId),
            (0, import_drizzle_orm61.eq)(scheduledOrders.status, "pending")
          )
        );
        return scheduled;
      }
      // Cancelar pedido programado
      static async cancelScheduledOrder(scheduledOrderId, userId) {
        await db.update(scheduledOrders).set({ status: "cancelled" }).where(
          (0, import_drizzle_orm61.and)(
            (0, import_drizzle_orm61.eq)(scheduledOrders.id, scheduledOrderId),
            (0, import_drizzle_orm61.eq)(scheduledOrders.userId, userId)
          )
        );
        return { success: true };
      }
      // Ejecutar pedidos programados (cron job)
      static async executeScheduledOrders() {
        const now = /* @__PURE__ */ new Date();
        const pending = await db.select().from(scheduledOrders).where(
          (0, import_drizzle_orm61.and)(
            (0, import_drizzle_orm61.eq)(scheduledOrders.status, "pending"),
            (0, import_drizzle_orm61.lte)(scheduledOrders.scheduledFor, now)
          )
        );
        const results = [];
        for (const scheduled of pending) {
          try {
            let items = [];
            try {
              items = typeof scheduled.items === "string" ? JSON.parse(scheduled.items) : scheduled.items || [];
              if (!Array.isArray(items)) items = [];
            } catch {
              items = [];
            }
            const subtotal = items.reduce(
              (sum3, it) => sum3 + Math.round((Number(it.price) || 0) * 100 * (Number(it.quantity) || 1)),
              0
            );
            const productosBase = Math.round(subtotal / 1.15);
            const nemyCommission = subtotal - productosBase;
            const [business] = await db.select({
              name: businesses.name,
              image: businesses.image,
              deliveryFee: businesses.deliveryFee,
              ownerId: businesses.ownerId
            }).from(businesses).where((0, import_drizzle_orm61.eq)(businesses.id, scheduled.businessId)).limit(1);
            const deliveryFee = Number(business?.deliveryFee) || 0;
            const total = subtotal + deliveryFee;
            const { nextOrderNumber: nextOrderNumber2 } = await Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports));
            const seqNumber = await nextOrderNumber2();
            const [order] = await db.insert(orders).values({
              ...seqNumber != null ? { orderNumber: seqNumber } : {},
              userId: scheduled.userId,
              businessId: scheduled.businessId,
              businessName: business?.name || "Negocio",
              businessImage: business?.image || null,
              items: scheduled.items,
              status: "pending",
              subtotal,
              productosBase,
              nemyCommission,
              deliveryFee,
              total,
              paymentMethod: scheduled.paymentMethod,
              orderType: "delivery",
              deliveryAddress: scheduled.deliveryAddress,
              notes: scheduled.notes || null,
              // LAS COORDENADAS se copian del pedido programado: antes el pedido
              // materializado nacía SIEMPRE sin ellas y no tenía ruta real hasta
              // que un job lo arreglaba (hasta 30 min después)
              deliveryLatitude: scheduled.deliveryLatitude || null,
              deliveryLongitude: scheduled.deliveryLongitude || null,
              // La fecha programada queda registrada en el pedido para el
              // seguimiento del cliente y la facturación
              scheduledFor: scheduled.scheduledFor ?? null
            });
            await db.update(scheduledOrders).set({
              status: "executed",
              orderId: order.insertId
            }).where((0, import_drizzle_orm61.eq)(scheduledOrders.id, scheduled.id));
            try {
              const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
              if (business?.ownerId) {
                await sendPushToUser2(business.ownerId, {
                  title: "\u{1F514} Pedido programado",
                  body: `Se ha activado el pedido programado de un cliente. Rev\xEDsalo y conf\xEDrmalo.`,
                  data: { orderId: String(order.insertId), screen: "BusinessOrders" }
                });
              }
              await sendPushToUser2(scheduled.userId, {
                title: "\u23F0 Pedido programado activado",
                body: `Tu pedido programado en ${business?.name || "el negocio"} ya se ha enviado al local.`,
                data: { orderId: String(order.insertId), screen: "OrderTracking" }
              });
            } catch (notifyError) {
              console.error("Error notifying scheduled order execution:", notifyError);
            }
            results.push({
              scheduledOrderId: scheduled.id,
              orderId: order.insertId,
              success: true
            });
          } catch (error) {
            console.error("Error executing scheduled order:", error);
            await db.update(scheduledOrders).set({ status: "failed" }).where((0, import_drizzle_orm61.eq)(scheduledOrders.id, scheduled.id));
            results.push({ scheduledOrderId: scheduled.id, success: false, error });
          }
        }
        return results;
      }
    };
  }
});

// server/gamificationService.ts
var gamificationService_exports = {};
__export(gamificationService_exports, {
  GamificationService: () => GamificationService
});
var import_drizzle_orm66, GamificationService;
var init_gamificationService = __esm({
  "server/gamificationService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm66 = require("drizzle-orm");
    GamificationService = class {
      // Inicializar puntos de usuario
      static async initializeUserPoints(userId) {
        const [existing] = await db.select().from(loyaltyPoints).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId)).limit(1);
        if (!existing) {
          await db.insert(loyaltyPoints).values({
            id: crypto.randomUUID(),
            userId,
            currentPoints: 0,
            totalEarned: 0,
            totalRedeemed: 0,
            tier: "bronze",
            pointsToNextTier: 1e3
          });
        }
      }
      // Otorgar puntos
      static async awardPoints(data) {
        const { userId, points, type, description, orderId } = data;
        await this.initializeUserPoints(userId);
        const [userPoints] = await db.select().from(loyaltyPoints).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId)).limit(1);
        if (!userPoints) return { success: false, error: "Usuario no encontrado" };
        const newPoints = userPoints.currentPoints + points;
        const newTotalEarned = userPoints.totalEarned + points;
        await db.update(loyaltyPoints).set({
          currentPoints: newPoints,
          totalEarned: newTotalEarned
        }).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId));
        await db.insert(loyaltyTransactions).values({
          id: crypto.randomUUID(),
          userId,
          type,
          points,
          description,
          orderId: orderId || null
        });
        await this.checkTierUpgrade(userId, newTotalEarned);
        await this.checkAchievements(userId);
        return { success: true, newPoints, pointsAwarded: points };
      }
      // Verificar upgrade de tier
      static async checkTierUpgrade(userId, totalEarned) {
        let newTier = "bronze";
        let pointsToNext = 1e3;
        if (totalEarned >= 1e4) {
          newTier = "platinum";
          pointsToNext = 0;
        } else if (totalEarned >= 5e3) {
          newTier = "gold";
          pointsToNext = 1e4 - totalEarned;
        } else if (totalEarned >= 2e3) {
          newTier = "silver";
          pointsToNext = 5e3 - totalEarned;
        } else {
          pointsToNext = 2e3 - totalEarned;
        }
        await db.update(loyaltyPoints).set({
          tier: newTier,
          pointsToNextTier: pointsToNext,
          tierUpdatedAt: /* @__PURE__ */ new Date()
        }).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId));
      }
      // Verificar y desbloquear achievements
      static async checkAchievements(userId) {
        const userOrders = await db.select().from(orders).where((0, import_drizzle_orm66.eq)(orders.userId, userId));
        const completedOrders = userOrders.filter((o) => o.status === "delivered");
        const orderCount = completedOrders.length;
        const totalSpent = completedOrders.reduce(
          (sum3, o) => sum3 + (o.total || 0),
          0
        );
        const allAchievements = await db.select().from(achievements).where((0, import_drizzle_orm66.eq)(achievements.isActive, true));
        for (const achievement of allAchievements) {
          let qualifies = false;
          if (achievement.requirementType === "order_count")
            qualifies = orderCount >= achievement.requirementValue;
          if (achievement.requirementType === "total_spent")
            qualifies = totalSpent >= achievement.requirementValue;
          if (qualifies) await this.unlockAchievement(userId, achievement.id);
        }
      }
      // Desbloquear achievement por ID
      static async unlockAchievement(userId, achievementId) {
        const [achievement] = await db.select().from(achievements).where((0, import_drizzle_orm66.eq)(achievements.id, achievementId)).limit(1);
        if (!achievement) return;
        const [existing] = await db.select().from(userAchievements).where(
          (0, import_drizzle_orm66.and)(
            (0, import_drizzle_orm66.eq)(userAchievements.userId, userId),
            (0, import_drizzle_orm66.eq)(userAchievements.achievementId, achievementId)
          )
        ).limit(1);
        if (existing) return;
        await db.insert(userAchievements).values({
          id: crypto.randomUUID(),
          userId,
          achievementId
        });
        if (achievement.rewardPoints > 0) {
          await this.awardPoints({
            userId,
            points: achievement.rewardPoints,
            type: "achievement",
            description: `Logro desbloqueado: ${achievement.name}`
          });
        }
      }
      // Obtener puntos del usuario
      static async getUserPoints(userId) {
        await this.initializeUserPoints(userId);
        const [points] = await db.select().from(loyaltyPoints).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId)).limit(1);
        return { success: true, points };
      }
      // Obtener leaderboard
      static async getLeaderboard(limit = 50) {
        const topUsers = await db.select({
          userId: loyaltyPoints.userId,
          currentPoints: loyaltyPoints.currentPoints,
          totalEarned: loyaltyPoints.totalEarned,
          tier: loyaltyPoints.tier,
          userName: users.name,
          userImage: users.profileImage
        }).from(loyaltyPoints).leftJoin(users, (0, import_drizzle_orm66.eq)(loyaltyPoints.userId, users.id)).orderBy((0, import_drizzle_orm66.desc)(loyaltyPoints.totalEarned)).limit(limit);
        return { success: true, leaderboard: topUsers };
      }
      // Obtener achievements del usuario
      static async getUserAchievements(userId) {
        const unlocked = await db.select({
          achievement: achievements,
          unlockedAt: userAchievements.unlockedAt
        }).from(userAchievements).leftJoin(
          achievements,
          (0, import_drizzle_orm66.eq)(userAchievements.achievementId, achievements.id)
        ).where((0, import_drizzle_orm66.eq)(userAchievements.userId, userId));
        const allAchievements = await db.select().from(achievements).where((0, import_drizzle_orm66.eq)(achievements.isActive, true));
        const unlockedIds = unlocked.map((u) => u.achievement?.id);
        const locked = allAchievements.filter((a) => !unlockedIds.includes(a.id));
        return {
          success: true,
          unlocked: unlocked.map((u) => ({
            ...u.achievement,
            unlockedAt: u.unlockedAt
          })),
          locked
        };
      }
      // Canjear recompensa
      static async redeemReward(userId, rewardId) {
        const [reward] = await db.select().from(loyaltyRewards).where((0, import_drizzle_orm66.eq)(loyaltyRewards.id, rewardId)).limit(1);
        if (!reward) {
          return { success: false, error: "Recompensa no encontrada" };
        }
        if (!reward.isAvailable) {
          return { success: false, error: "Recompensa no disponible" };
        }
        const [userPoints] = await db.select().from(loyaltyPoints).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId)).limit(1);
        if (!userPoints || userPoints.currentPoints < reward.pointsCost) {
          return { success: false, error: "Puntos insuficientes" };
        }
        await db.update(loyaltyPoints).set({
          currentPoints: userPoints.currentPoints - reward.pointsCost,
          totalRedeemed: userPoints.totalRedeemed + reward.pointsCost
        }).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId));
        await db.insert(loyaltyTransactions).values({
          id: crypto.randomUUID(),
          userId,
          type: "redemption",
          points: -reward.pointsCost,
          description: `Canjeado: ${reward.title}`,
          rewardId
        });
        const redemptionId = crypto.randomUUID();
        await db.insert(loyaltyRedemptions).values({
          id: redemptionId,
          userId,
          rewardId,
          pointsSpent: reward.pointsCost,
          status: "active",
          expiresAt: reward.expiresAt || null
        });
        return { success: true, redemptionId };
      }
      // Obtener recompensas disponibles
      static async getAvailableRewards(userId) {
        const [userPoints] = await db.select().from(loyaltyPoints).where((0, import_drizzle_orm66.eq)(loyaltyPoints.userId, userId)).limit(1);
        const rewards = await db.select().from(loyaltyRewards).where((0, import_drizzle_orm66.eq)(loyaltyRewards.isAvailable, true));
        return {
          success: true,
          rewards: rewards.map((r) => ({
            ...r,
            canAfford: userPoints ? userPoints.currentPoints >= r.pointsCost : false
          })),
          userPoints: userPoints?.currentPoints || 0
        };
      }
    };
  }
});

// server/businessHoursCron.ts
var businessHoursCron_exports = {};
__export(businessHoursCron_exports, {
  startBusinessHoursCron: () => startBusinessHoursCron
});
function startBusinessHoursCron() {
  const INTERVAL = 5 * 60 * 1e3;
  console.log("\u{1F550} Business hours cron started - checking every 5 minutes");
  BusinessHoursService.updateAllBusinessStatuses().catch(console.error);
  setInterval(() => {
    BusinessHoursService.updateAllBusinessStatuses().catch(console.error);
  }, INTERVAL);
}
var init_businessHoursCron = __esm({
  "server/businessHoursCron.ts"() {
    "use strict";
    init_businessHoursService();
  }
});

// server/weeklySettlementCron.ts
var weeklySettlementCron_exports = {};
__export(weeklySettlementCron_exports, {
  WeeklySettlementCron: () => WeeklySettlementCron
});
var import_node_cron, WeeklySettlementCron;
var init_weeklySettlementCron = __esm({
  "server/weeklySettlementCron.ts"() {
    "use strict";
    import_node_cron = __toESM(require("node-cron"));
    init_weeklySettlementService();
    WeeklySettlementCron = class {
      /**
       * Iniciar todos los cron jobs
       */
      static start() {
        import_node_cron.default.schedule("59 23 * * 5", async () => {
          console.log(
            "\u{1F550} FLUJO RESTRICTIVO VIERNES: Cerrando semana y bloqueando drivers con deuda..."
          );
          try {
            const result = await WeeklySettlementService.closeWeek();
            console.log(
              `\u2705 Semana cerrada: ${result.count} liquidaciones creadas, ${result.blocked} drivers bloqueados`
            );
          } catch (error) {
            console.error("\u274C Error al cerrar semana:", error);
          }
        });
        import_node_cron.default.schedule("0 0 * * 1", async () => {
          console.log("\u{1F550} Ejecutando bloqueo de drivers...");
          try {
            const result = await WeeklySettlementService.blockUnpaidDrivers();
            console.log(`\u{1F6AB} ${result.blocked} drivers bloqueados`);
          } catch (error) {
            console.error("\u274C Error al bloquear drivers:", error);
          }
        });
        console.log("\u23F0 Cron jobs de liquidaci\xF3n semanal iniciados:");
        console.log(
          "   - Viernes 11:59 PM: FLUJO RESTRICTIVO - Cierre y bloqueo inmediato"
        );
        console.log("   - Lunes 12:00 AM: Bloqueo definitivo de drivers sin pago");
      }
    };
  }
});

// server/autoConfirmDeliveryCron.ts
var autoConfirmDeliveryCron_exports = {};
__export(autoConfirmDeliveryCron_exports, {
  startAutoConfirmCron: () => startAutoConfirmCron
});
function startAutoConfirmCron() {
  import_node_cron2.default.schedule("0 * * * *", async () => {
    try {
      console.log("\u{1F504} Running auto-confirm delivery cron...");
      const twelveHoursAgo = new Date(Date.now() - 12 * 60 * 60 * 1e3);
      const ordersToConfirm = await db.select().from(orders).where(
        (0, import_drizzle_orm84.and)(
          (0, import_drizzle_orm84.eq)(orders.status, "delivered"),
          (0, import_drizzle_orm84.or)(
            (0, import_drizzle_orm84.isNull)(orders.confirmedByCustomer),
            (0, import_drizzle_orm84.eq)(orders.confirmedByCustomer, false)
          ),
          (0, import_drizzle_orm84.lt)(orders.deliveredAt, twelveHoursAgo)
        )
      );
      console.log(`\u{1F4E6} Found ${ordersToConfirm.length} orders to auto-confirm`);
      for (const order of ordersToConfirm) {
        try {
          const { financialService: financialService2 } = await Promise.resolve().then(() => (init_unifiedFinancialService(), unifiedFinancialService_exports));
          const commissions = await financialService2.calculateCommissions(
            order.total,
            order.deliveryFee,
            order.productosBase || order.subtotal,
            order.nemyCommission || void 0
          );
          await db.update(orders).set({
            confirmedByCustomer: true,
            confirmedByCustomerAt: /* @__PURE__ */ new Date(),
            platformFee: commissions.platform,
            businessEarnings: commissions.business,
            deliveryEarnings: commissions.driver
          }).where((0, import_drizzle_orm84.eq)(orders.id, order.id));
          const [business] = await db.select({ ownerId: businesses.ownerId }).from(businesses).where((0, import_drizzle_orm84.eq)(businesses.id, order.businessId)).limit(1);
          const businessOwnerId = business?.ownerId || order.businessId;
          if (order.paymentMethod === "cash") {
            const { cashSettlementService: cashSettlementService2 } = await Promise.resolve().then(() => (init_cashSettlementService(), cashSettlementService_exports));
            await cashSettlementService2.registerCashDebt(
              order.id,
              order.deliveryPersonId,
              order.businessId,
              order.total,
              order.deliveryFee
            );
          } else {
            const [businessWallet] = await db.select().from(wallets).where((0, import_drizzle_orm84.eq)(wallets.userId, businessOwnerId)).limit(1);
            if (businessWallet) {
              await db.update(wallets).set({
                balance: businessWallet.balance + commissions.business,
                totalEarned: businessWallet.totalEarned + commissions.business
              }).where((0, import_drizzle_orm84.eq)(wallets.userId, businessOwnerId));
            } else {
              await db.insert(wallets).values({
                userId: businessOwnerId,
                balance: commissions.business,
                pendingBalance: 0,
                totalEarned: commissions.business,
                totalWithdrawn: 0
              });
            }
            if (order.deliveryPersonId) {
              const [driverWallet] = await db.select().from(wallets).where((0, import_drizzle_orm84.eq)(wallets.userId, order.deliveryPersonId)).limit(1);
              if (driverWallet) {
                await db.update(wallets).set({
                  balance: driverWallet.balance + commissions.driver,
                  totalEarned: driverWallet.totalEarned + commissions.driver
                }).where((0, import_drizzle_orm84.eq)(wallets.userId, order.deliveryPersonId));
              } else {
                await db.insert(wallets).values({
                  userId: order.deliveryPersonId,
                  balance: commissions.driver,
                  pendingBalance: 0,
                  totalEarned: commissions.driver,
                  totalWithdrawn: 0
                });
              }
            }
            await db.insert(transactions).values([
              {
                userId: businessOwnerId,
                type: "order_payment",
                amount: commissions.business,
                status: "completed",
                description: `Pago autom\xE1tico por pedido #${order.id.slice(-8)}`,
                orderId: order.id
              },
              {
                userId: order.deliveryPersonId,
                type: "delivery_payment",
                amount: commissions.driver,
                status: "completed",
                description: `Pago autom\xE1tico entrega #${order.id.slice(-8)}`,
                orderId: order.id
              }
            ]);
          }
          console.log(`\u2705 Auto-confirmed order ${order.id.slice(-8)}`);
        } catch (error) {
          console.error(`\u274C Error auto-confirming order ${order.id}:`, error);
        }
      }
      console.log("\u2705 Auto-confirm cron completed");
    } catch (error) {
      console.error("\u274C Auto-confirm cron error:", error);
    }
  });
  console.log("\u23F0 Auto-confirm delivery cron started (runs every hour)");
}
var import_node_cron2, import_drizzle_orm84;
var init_autoConfirmDeliveryCron = __esm({
  "server/autoConfirmDeliveryCron.ts"() {
    "use strict";
    import_node_cron2 = __toESM(require("node-cron"));
    init_db();
    init_schema_mysql();
    import_drizzle_orm84 = require("drizzle-orm");
  }
});

// server/pickupNotificationCron.ts
var pickupNotificationCron_exports = {};
__export(pickupNotificationCron_exports, {
  startPickupNotificationCron: () => startPickupNotificationCron
});
function startPickupNotificationCron() {
  import_node_cron3.default.schedule("*/5 * * * *", async () => {
    try {
      console.log("\u{1F514} Running pickup notification cron...");
      const activeOrders = await db.select().from(orders);
      for (const order of activeOrders) {
        if (order.orderType !== "pickup" || !["accepted", "preparing"].includes(order.status)) {
          continue;
        }
        const progress = pickupService.getProgress(order);
        if (!sentNotifications.has(order.id)) {
          sentNotifications.set(order.id, /* @__PURE__ */ new Set());
        }
        const sent = sentNotifications.get(order.id);
        if (progress >= 25 && progress < 30 && !sent.has(25)) {
          await pickupService.sendProgressNotification(
            order.id,
            order.userId,
            25
          );
          sent.add(25);
          console.log(
            `\u{1F4F2} Sent 25% notification for order ${order.id.slice(-6)}`
          );
        } else if (progress >= 50 && progress < 55 && !sent.has(50)) {
          await pickupService.sendProgressNotification(
            order.id,
            order.userId,
            50
          );
          sent.add(50);
          console.log(
            `\u{1F4F2} Sent 50% notification for order ${order.id.slice(-6)}`
          );
        } else if (progress >= 75 && progress < 80 && !sent.has(75)) {
          await pickupService.sendProgressNotification(
            order.id,
            order.userId,
            75
          );
          sent.add(75);
          console.log(
            `\u{1F4F2} Sent 75% notification for order ${order.id.slice(-6)}`
          );
        }
      }
      const twoHoursAgo = Date.now() - 2 * 60 * 60 * 1e3;
      for (const [orderId, _] of sentNotifications.entries()) {
        const order = activeOrders.find((o) => o.id === orderId);
        if (!order || new Date(order.createdAt).getTime() < twoHoursAgo) {
          sentNotifications.delete(orderId);
        }
      }
    } catch (error) {
      console.error("\u274C Pickup notification cron error:", error);
    }
  });
  console.log("\u2705 Pickup notification cron started (every 5 minutes)");
}
var import_node_cron3, sentNotifications;
var init_pickupNotificationCron = __esm({
  "server/pickupNotificationCron.ts"() {
    "use strict";
    import_node_cron3 = __toESM(require("node-cron"));
    init_db();
    init_schema_mysql();
    init_pickupService();
    sentNotifications = /* @__PURE__ */ new Map();
  }
});

// server/staleOrdersCron.ts
var staleOrdersCron_exports = {};
__export(staleOrdersCron_exports, {
  NO_ACCEPTANCE_MINUTES: () => NO_ACCEPTANCE_MINUTES,
  NO_DRIVER_MINUTES: () => NO_DRIVER_MINUTES,
  PROOF_PENDING_MAX_MINUTES: () => PROOF_PENDING_MAX_MINUTES,
  checkStaleOrders: () => checkStaleOrders,
  checkUnacceptedOrders: () => checkUnacceptedOrders,
  runOrderCleanup: () => runOrderCleanup,
  startStaleOrdersCron: () => startStaleOrdersCron,
  stopStaleOrdersCron: () => stopStaleOrdersCron
});
async function rescueStripePayment2(order) {
  const { rescueStripePayment: rescue } = await Promise.resolve().then(() => (init_paymentConfirmationService(), paymentConfirmationService_exports));
  return rescue(order);
}
async function checkUnacceptedOrders() {
  const cutoff = new Date(Date.now() - NO_ACCEPTANCE_MINUTES * 60 * 1e3);
  const stale = await db.select().from(orders).where(
    (0, import_drizzle_orm85.and)(
      (0, import_drizzle_orm85.inArray)(orders.status, ["pending", "payment_failed"]),
      (0, import_drizzle_orm85.isNull)(orders.businessResponseAt),
      (0, import_drizzle_orm85.lt)(orders.createdAt, cutoff)
    )
  ).limit(50);
  const { pendingProofOrderIds: pendingProofOrderIds2 } = await Promise.resolve().then(() => (init_paymentState(), paymentState_exports));
  const proofPending = await pendingProofOrderIds2(stale.map((o) => o.id));
  let cancelled = 0;
  for (const order of stale) {
    try {
      if (proofPending.has(order.id)) continue;
      if (await rescueStripePayment2(order)) continue;
      const result = await cancelOrder(
        order.id,
        "system",
        `Sin pago confirmado ni aceptaci\xF3n del negocio en ${NO_ACCEPTANCE_MINUTES} minutos \u2014 cancelaci\xF3n autom\xE1tica con reembolso del 100%`,
        { actorRole: "system" }
      );
      if (result.success) {
        cancelled++;
        console.log(
          `\u23F0 ${orderRef(order)} cancelado por falta de aceptaci\xF3n \u2014 devoluci\xF3n: ${result.refund?.message ?? "no aplica"}`
        );
      } else {
        console.error(
          `\u23F0 ${orderRef(order)} (sin aceptar) no se pudo cancelar: ${result.message}`
        );
      }
    } catch (error) {
      console.error(`Error cancelling unaccepted order ${order.id}:`, error);
    }
  }
  return cancelled;
}
async function checkStuckPaymentProofs() {
  const cutoff = new Date(
    Date.now() - PROOF_PENDING_MAX_MINUTES * 60 * 1e3
  );
  try {
    const { paymentProofs: paymentProofs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const stuckProofs = await db.select({ orderId: paymentProofs2.orderId }).from(paymentProofs2).where(
      (0, import_drizzle_orm85.and)(
        (0, import_drizzle_orm85.eq)(paymentProofs2.status, "pending"),
        (0, import_drizzle_orm85.lt)(paymentProofs2.submittedAt, cutoff)
      )
    ).limit(50);
    const ids = stuckProofs.map((p) => p.orderId).filter((id) => Boolean(id));
    if (!ids.length) return 0;
    const stuck = await db.select().from(orders).where((0, import_drizzle_orm85.and)((0, import_drizzle_orm85.inArray)(orders.id, ids), (0, import_drizzle_orm85.inArray)(orders.status, ["pending"])));
    let cancelled = 0;
    for (const order of stuck) {
      try {
        const result = await cancelOrder(
          order.id,
          "system",
          `El comprobante de pago no se verific\xF3 en ${PROOF_PENDING_MAX_MINUTES / 60} horas \u2014 cancelaci\xF3n autom\xE1tica con reembolso del 100%`,
          { actorRole: "system" }
        );
        if (result.success) {
          cancelled++;
          console.log(
            `\u23F0 ${orderRef(order)} cancelado: comprobante sin verificar`
          );
        }
      } catch (error) {
        console.error(`Error cancelling proof-stuck order ${order.id}:`, error);
      }
    }
    return cancelled;
  } catch {
    return 0;
  }
}
async function checkStaleOrders() {
  const cutoff = new Date(Date.now() - NO_DRIVER_MINUTES * 60 * 1e3);
  const stale = await db.select().from(orders).where(
    (0, import_drizzle_orm85.and)(
      (0, import_drizzle_orm85.inArray)(orders.status, ["accepted", "preparing", "ready"]),
      (0, import_drizzle_orm85.isNull)(orders.deliveryPersonId),
      (0, import_drizzle_orm85.lt)(orders.createdAt, cutoff),
      // Los pedidos de recogida en local no llevan repartidor: no deben
      // cancelarse por "falta de asignación"
      (0, import_drizzle_orm85.ne)(orders.orderType, "pickup")
    )
  ).limit(50);
  for (const order of stale) {
    try {
      const result = await cancelOrder(
        order.id,
        "system",
        `Pedido estancado: sin repartidor tras ${NO_DRIVER_MINUTES} minutos`,
        { actorRole: "system" }
      );
      if (result.success) {
        console.log(
          `\u23F0 Stale order ${order.id} cancelado \u2014 devoluci\xF3n: ${result.refund?.message ?? "no aplica"}`
        );
      } else {
        console.error(
          `\u23F0 Stale order ${order.id} no se pudo cancelar: ${result.message}`
        );
      }
    } catch (error) {
      console.error(`Error cancelling stale order ${order.id}:`, error);
    }
  }
  return stale.length;
}
async function retryFailedRefunds() {
  try {
    const { refunds: refunds2, orders: ordersTable } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const svc = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    if (typeof svc.retryRefund !== "function") return 0;
    const failed = await db.select({ id: refunds2.id, orderId: refunds2.orderId }).from(refunds2).where(eq79(refunds2.status, "failed")).limit(10);
    let handled = 0;
    for (const r of failed) {
      try {
        const [order] = await db.select({
          paymentMethod: ordersTable.paymentMethod,
          paidAt: ordersTable.paidAt
        }).from(ordersTable).where(eq79(ordersTable.id, r.orderId)).limit(1);
        if (order && String(order.paymentMethod || "").startsWith("stripe_") && !order.paidAt) {
          await db.update(refunds2).set({
            status: "completed",
            processedAt: /* @__PURE__ */ new Date(),
            amount: 0
          }).where(eq79(refunds2.id, r.id));
          await db.update(ordersTable).set({ refundStatus: "processed" }).where(eq79(ordersTable.id, r.orderId));
          handled++;
          continue;
        }
        await svc.retryRefund(r.id, "system");
        handled++;
      } catch {
      }
    }
    if (handled) console.log(`\u{1F4B8} Reembolsos procesados: ${handled}`);
    return handled;
  } catch {
    return 0;
  }
}
async function runOrderCleanup() {
  const [unaccepted, stale, proofStuck] = await Promise.all([
    checkUnacceptedOrders(),
    checkStaleOrders(),
    checkStuckPaymentProofs()
  ]);
  retryFailedRefunds().catch(() => {
  });
  return { unaccepted, stale, proofStuck };
}
function startStaleOrdersCron() {
  if (interval) return;
  if (process.env.NODE_ENV === "development") {
    console.log("\u23F0 Stale orders cron: solo producci\xF3n (omitido en desarrollo)");
    return;
  }
  console.log(
    `\u23F0 Stale orders cron iniciado (cada 2 min \xB7 sin aceptar: ${NO_ACCEPTANCE_MINUTES} min \xB7 sin repartidor: ${NO_DRIVER_MINUTES} min \xB7 comprobante sin verificar: ${PROOF_PENDING_MAX_MINUTES} min)`
  );
  runOrderCleanup().catch(console.error);
  interval = setInterval(
    () => {
      runOrderCleanup().catch(console.error);
    },
    2 * 60 * 1e3
  );
}
function stopStaleOrdersCron() {
  if (interval) {
    clearInterval(interval);
    interval = null;
  }
}
var import_drizzle_orm85, NO_ACCEPTANCE_MINUTES, NO_DRIVER_MINUTES, PROOF_PENDING_MAX_MINUTES, interval;
var init_staleOrdersCron = __esm({
  "server/staleOrdersCron.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm85 = require("drizzle-orm");
    init_orderCancellationService();
    init_orderNumberService();
    NO_ACCEPTANCE_MINUTES = 10;
    NO_DRIVER_MINUTES = 30;
    PROOF_PENDING_MAX_MINUTES = 120;
    interval = null;
  }
});

// server/stripeWebhookRegistration.ts
var stripeWebhookRegistration_exports = {};
__export(stripeWebhookRegistration_exports, {
  ensureStripeWebhook: () => ensureStripeWebhook
});
async function storedSecret() {
  try {
    const [rows] = await db.execute(
      import_drizzle_orm86.sql`SELECT value FROM app_settings WHERE \`key\` = ${SETTINGS_KEY} LIMIT 1`
    );
    return rows?.[0]?.value ? String(rows[0].value) : null;
  } catch {
    return null;
  }
}
async function persistSecret(secret) {
  await db.execute(
    import_drizzle_orm86.sql`INSERT INTO app_settings (\`key\`, value) VALUES (${SETTINGS_KEY}, ${secret})
        ON DUPLICATE KEY UPDATE value = VALUES(value)`
  );
}
async function createEndpoint(stripe2, url) {
  const created = await stripe2.webhookEndpoints.create({
    url,
    enabled_events: WEBHOOK_EVENTS,
    // Misma versión de API que el webhook de Connect de la cuenta
    api_version: "2026-03-25.dahlia"
  });
  console.log(`\u2705 Webhook de pagos Stripe creado: ${created.id}`);
  return created.secret;
}
async function ensureStripeWebhook() {
  try {
    if (!process.env.STRIPE_SECRET_KEY) return;
    const base = (process.env.BACKEND_URL || "").replace(/\/+$/, "");
    if (!base) {
      console.warn("ensureStripeWebhook: BACKEND_URL no definido, se omite");
      return;
    }
    const url = `${base}${WEBHOOK_PATH}`;
    const stripe2 = getStripe2();
    const endpoints = await stripe2.webhookEndpoints.list({ limit: 100 });
    const existing = endpoints.data.find((e) => e.url === url);
    if (existing && existing.status === "enabled") {
      const hasAll = WEBHOOK_EVENTS.every(
        (ev) => existing.enabled_events.includes(ev)
      );
      if (hasAll && await storedSecret()) {
        console.log("\u2705 Webhook de pagos Stripe verificado (ya activo)");
        return;
      }
      if (hasAll && process.env.STRIPE_WEBHOOK_SECRET) {
        console.log("\u2705 Webhook de pagos Stripe activo (secreto desde entorno)");
        return;
      }
      console.warn("ensureStripeWebhook: recreando endpoint (sin secreto conocido)");
      await stripe2.webhookEndpoints.del(existing.id);
      const secret2 = await createEndpoint(stripe2, url);
      await persistSecret(secret2);
      return;
    }
    if (existing) {
      console.warn(`ensureStripeWebhook: recreando endpoint (status ${existing.status})`);
      await stripe2.webhookEndpoints.del(existing.id);
    }
    const secret = await createEndpoint(stripe2, url);
    await persistSecret(secret);
  } catch (err) {
    console.error("ensureStripeWebhook error:", err?.message ?? err);
  }
}
var import_drizzle_orm86, WEBHOOK_PATH, SETTINGS_KEY, WEBHOOK_EVENTS;
var init_stripeWebhookRegistration = __esm({
  "server/stripeWebhookRegistration.ts"() {
    "use strict";
    init_stripeClient();
    init_db();
    import_drizzle_orm86 = require("drizzle-orm");
    WEBHOOK_PATH = "/api/payments/webhook/stripe";
    SETTINGS_KEY = "stripe_webhook_secret";
    WEBHOOK_EVENTS = [
      "payment_intent.succeeded",
      "payment_intent.payment_failed",
      "payment_intent.canceled",
      "charge.refunded",
      "refund.updated",
      "charge.dispute.created",
      "payout.paid",
      "payout.failed",
      "transfer.created"
    ];
  }
});

// server/commissionService.ts
async function releasePendingFunds(orderId) {
  const [order] = await db.select().from(orders).where((0, import_drizzle_orm87.eq)(orders.id, orderId)).limit(1);
  if (!order || !order.businessEarnings) return;
  logger.payment("Funds released immediately", {
    orderId,
    amount: order.businessEarnings
  });
}
var import_drizzle_orm87;
var init_commissionService = __esm({
  "server/commissionService.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm87 = require("drizzle-orm");
    init_logger();
    init_errors();
    init_unifiedFinancialService();
  }
});

// server/backgroundJobs.ts
var backgroundJobs_exports = {};
__export(backgroundJobs_exports, {
  cleanupAuditLogsJob: () => cleanupAuditLogsJob,
  cleanupStrikesJob: () => cleanupStrikesJob,
  deactivateInactiveDriversJob: () => deactivateInactiveDriversJob,
  executeScheduledOrdersJob: () => executeScheduledOrdersJob,
  processPendingWithdrawalsJob: () => processPendingWithdrawalsJob,
  releaseFundsJob: () => releaseFundsJob,
  startBackgroundJobs: () => startBackgroundJobs,
  stopBackgroundJobs: () => stopBackgroundJobs,
  unblockExpiredDriversJob: () => unblockExpiredDriversJob,
  updateBusinessStatsJob: () => updateBusinessStatsJob
});
async function releaseFundsJob() {
  try {
    logger.info("Running release funds job");
    const { getSettingValue: getSettingValue2 } = await Promise.resolve().then(() => (init_systemSettingsService(), systemSettingsService_exports));
    const holdHours = await getSettingValue2("fund_hold_duration_hours", 0);
    if (holdHours === 0) {
      logger.info("Immediate payment enabled");
      return;
    }
    const holdTime = new Date(Date.now() - holdHours * 60 * 60 * 1e3);
    const ordersToRelease = await db.select().from(orders).where(
      (0, import_drizzle_orm88.and)((0, import_drizzle_orm88.eq)(orders.status, "delivered"), (0, import_drizzle_orm88.lte)(orders.deliveredAt, holdTime))
    );
    logger.info(`Releasing funds for ${ordersToRelease.length} orders`);
    for (const order of ordersToRelease) {
      try {
        await releasePendingFunds(order.id);
      } catch (error) {
        logger.error("Failed to release funds", error, { orderId: order.id });
      }
    }
  } catch (error) {
    logger.error("Release funds job failed", error);
  }
}
async function cleanupStrikesJob() {
  try {
    logger.info("Running cleanup strikes job");
    const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1e3);
    await db.update(deliveryDrivers).set({ strikes: 0 }).where(
      (0, import_drizzle_orm88.and)(
        (0, import_drizzle_orm88.lte)(deliveryDrivers.lastLocationUpdate, thirtyDaysAgo),
        (0, import_drizzle_orm88.eq)(deliveryDrivers.strikes, 1)
      )
    );
    logger.info("Strikes cleaned up");
  } catch (error) {
    logger.error("Cleanup strikes job failed", error);
  }
}
async function deactivateInactiveDriversJob() {
  try {
    logger.info("Running deactivate inactive drivers job");
    const sixtyDaysAgo = new Date(Date.now() - 60 * 24 * 60 * 60 * 1e3);
    await db.update(deliveryDrivers).set({ isAvailable: false }).where(
      (0, import_drizzle_orm88.and)(
        (0, import_drizzle_orm88.eq)(deliveryDrivers.isAvailable, true),
        (0, import_drizzle_orm88.lte)(deliveryDrivers.lastLocationUpdate, sixtyDaysAgo)
      )
    );
    logger.info("Inactive drivers deactivated");
  } catch (error) {
    logger.error("Deactivate drivers job failed", error);
  }
}
async function processPendingWithdrawalsJob() {
  try {
    const pending = await db.select({ id: withdrawals.id }).from(withdrawals).where((0, import_drizzle_orm88.eq)(withdrawals.status, "pending")).limit(1);
    if (pending.length > 0) {
      logger.info(
        `Hay ${pending.length} retiro(s) pendiente(s) de aprobaci\xF3n manual`
      );
    }
  } catch (error) {
    logger.error("Process withdrawals job failed", error);
  }
}
async function cleanupAuditLogsJob() {
  try {
    logger.info("Running cleanup audit logs job");
    const ninetyDaysAgo = new Date(Date.now() - 90 * 24 * 60 * 60 * 1e3);
    const { auditLogs: auditLogs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    await db.delete(auditLogs2).where((0, import_drizzle_orm88.lt)(auditLogs2.createdAt, ninetyDaysAgo));
    logger.info("Audit logs cleaned up");
  } catch (error) {
    logger.error("Cleanup audit logs job failed", error);
  }
}
async function updateBusinessStatsJob() {
  try {
    logger.info("Running update business stats job");
    logger.info("Business stats updated");
  } catch (error) {
    logger.error("Update business stats job failed", error);
  }
}
async function executeScheduledOrdersJob() {
  try {
    const { ScheduledOrdersService: ScheduledOrdersService2 } = await Promise.resolve().then(() => (init_scheduledOrdersService(), scheduledOrdersService_exports));
    const results = await ScheduledOrdersService2.executeScheduledOrders();
    const executed = results.filter((r) => r.success).length;
    if (executed > 0) {
      logger.info(`Executed ${executed} scheduled orders`);
    }
  } catch (error) {
    logger.error("Execute scheduled orders job failed", error);
  }
}
async function unblockExpiredDriversJob() {
  try {
    const now = /* @__PURE__ */ new Date();
    await db.update(deliveryDrivers).set({ isBlocked: false }).where(
      (0, import_drizzle_orm88.and)(
        (0, import_drizzle_orm88.eq)(deliveryDrivers.isBlocked, true),
        (0, import_drizzle_orm88.lt)(deliveryDrivers.blockedUntil, now)
      )
    );
    logger.info("Unblocked expired driver blocks checked");
  } catch (error) {
    logger.error("Unblock expired drivers job failed", error);
  }
}
function startBackgroundJobs() {
  if (process.env.NODE_ENV !== "production") {
    logger.warn("Background jobs disabled in development");
    return;
  }
  logger.info("Starting background jobs");
  setInterval(releaseFundsJob, 60 * 60 * 1e3);
  setInterval(cleanupStrikesJob, 24 * 60 * 60 * 1e3);
  setInterval(deactivateInactiveDriversJob, 24 * 60 * 60 * 1e3);
  setInterval(processPendingWithdrawalsJob, 5 * 60 * 1e3);
  setInterval(cleanupAuditLogsJob, 7 * 24 * 60 * 60 * 1e3);
  setInterval(updateBusinessStatsJob, 6 * 60 * 60 * 1e3);
  setInterval(executeScheduledOrdersJob, 5 * 60 * 1e3);
  setInterval(unblockExpiredDriversJob, 60 * 60 * 1e3);
  setInterval(geocodeMissingCoordinatesJob, 30 * 60 * 1e3);
  releaseFundsJob();
  processPendingWithdrawalsJob();
  executeScheduledOrdersJob();
  unblockExpiredDriversJob();
  geocodeMissingCoordinatesJob();
  logger.info("Background jobs started");
}
function stopBackgroundJobs() {
  logger.info("Stopping background jobs");
}
var import_drizzle_orm88;
var init_backgroundJobs = __esm({
  "server/backgroundJobs.ts"() {
    "use strict";
    init_db();
    init_schema_mysql();
    import_drizzle_orm88 = require("drizzle-orm");
    init_commissionService();
    init_logger();
    init_geocodeService();
  }
});

// server/server.ts
var import_express63 = __toESM(require("express"));
var import_cors = __toESM(require("cors"));
var import_helmet = __toESM(require("helmet"));
var import_express_rate_limit2 = __toESM(require("express-rate-limit"));
var import_path2 = __toESM(require("path"));
var import_fs2 = __toESM(require("fs"));
var import_dotenv2 = require("dotenv");
init_env();
var import_http = require("http");
init_websocket();

// server/routes.ts
var import_express62 = __toESM(require("express"));

// server/authMiddleware.ts
var import_jsonwebtoken2 = __toESM(require("jsonwebtoken"));
init_db();
init_schema_mysql();
var import_drizzle_orm3 = require("drizzle-orm");
async function authenticateToken(req, res, next) {
  try {
    const authHeader = req.headers["authorization"];
    const token = authHeader && authHeader.split(" ")[1];
    if (!token) {
      return res.status(401).json({ error: "Token requerido" });
    }
    const decoded = import_jsonwebtoken2.default.verify(
      token,
      process.env.JWT_SECRET || "comeya_local_secret_key"
    );
    const [user] = await db.select().from(users).where((0, import_drizzle_orm3.eq)(users.id, decoded.id)).limit(1);
    if (!user) {
      return res.status(401).json({ error: "Usuario no encontrado" });
    }
    req.user = {
      userId: user.id,
      // Add userId field for compatibility
      id: user.id,
      email: user.email || void 0,
      name: user.name,
      phone: user.phone,
      role: user.role,
      phoneVerified: user.phoneVerified,
      verificationStatus: user.verificationStatus ?? void 0,
      isActive: user.isActive
    };
    next();
  } catch (error) {
    console.error("\u274C Auth error:", error);
    return res.status(401).json({ error: "Token inv\xE1lido" });
  }
}
function requireRole(...allowedRoles) {
  return (req, res, next) => {
    if (!req.user) {
      return res.status(401).json({ error: "No autenticado" });
    }
    if (!allowedRoles.includes(req.user.role)) {
      return res.status(403).json({
        error: "No tienes permisos para esta acci\xF3n",
        requiredRole: allowedRoles,
        yourRole: req.user.role
      });
    }
    next();
  };
}
function requireApprovedDriver(req, res, next) {
  if (!req.user) {
    return res.status(401).json({ error: "No autenticado" });
  }
  if (req.user.role !== "delivery_driver") {
    return res.status(403).json({
      error: "Debes tener un perfil de repartidor",
      code: "driver_role_required"
    });
  }
  if (req.user.verificationStatus !== "verified") {
    return res.status(403).json({
      error: "Tu perfil de repartidor est\xE1 pendiente de aprobaci\xF3n por el administrador",
      code: "driver_not_verified",
      verificationStatus: req.user.verificationStatus || "pending"
    });
  }
  if (req.user.isActive === false) {
    return res.status(403).json({
      error: "Tu cuenta de repartidor est\xE1 desactivada",
      code: "driver_inactive"
    });
  }
  next();
}
function requireAdmin(req, res, next) {
  if (!req.user) {
    return res.status(401).json({ error: "No autenticado" });
  }
  if (req.user.role !== "admin" && req.user.role !== "super_admin") {
    return res.status(403).json({
      error: "Solo administradores pueden acceder",
      yourRole: req.user.role
    });
  }
  next();
}
function auditAction(action, entityType) {
  return async (req, res, next) => {
    if (req.user) {
      try {
        await db.insert(auditLogs).values({
          userId: req.user.id,
          action,
          entityType,
          entityId: req.params.id || req.body.id,
          changes: JSON.stringify({
            method: req.method,
            path: req.path,
            body: req.body,
            query: req.query
          }),
          ipAddress: req.ip,
          userAgent: req.headers["user-agent"]
        });
      } catch (error) {
        console.error("Audit log error:", error);
      }
    }
    next();
  };
}

// server/routes/auth.ts
var import_express = __toESM(require("express"));
var import_drizzle_orm7 = require("drizzle-orm");
var import_jsonwebtoken3 = __toESM(require("jsonwebtoken"));
var router = import_express.default.Router();
var signToken = (userId) => import_jsonwebtoken3.default.sign(
  { id: userId },
  process.env.JWT_SECRET || "comeya_local_secret_key",
  { expiresIn: "7d" }
);
router.post("/send-code", async (req, res) => {
  try {
    const { phone } = req.body;
    if (!phone) return res.status(400).json({ error: "Tel\xE9fono requerido" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.phone, phone)).limit(1);
    if (!user) return res.json({ userNotFound: true });
    const { sendVerificationCode: sendVerificationCode2 } = await Promise.resolve().then(() => (init_smsService(), smsService_exports));
    const sent = await sendVerificationCode2(phone, "123456");
    if (!sent) {
      return res.status(500).json({ error: "No se pudo enviar el c\xF3digo SMS" });
    }
    res.json({ success: true, requiresVerification: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/phone-login", async (req, res) => {
  try {
    const { phone, code, signupData } = req.body;
    if (!phone || !code)
      return res.status(400).json({ error: "Tel\xE9fono y c\xF3digo requeridos" });
    const { verifyCode: verifyCode2 } = await Promise.resolve().then(() => (init_smsService(), smsService_exports));
    const isValid = await verifyCode2(phone, code);
    if (!isValid) return res.status(400).json({ error: "C\xF3digo inv\xE1lido" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    let [user] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.phone, phone)).limit(1);
    if (!user) {
      if (signupData) {
        let hashedPassword = null;
        if (signupData.password) {
          const bcrypt = await import("bcryptjs");
          hashedPassword = await bcrypt.hash(signupData.password, 10);
        }
        const newUser = {
          id: crypto.randomUUID(),
          phone,
          name: signupData.name,
          email: signupData.email || null,
          password: hashedPassword,
          role: signupData.role || "customer",
          isActive: signupData.role === "customer" ? true : false,
          // Solo clientes activos inmediatamente
          phoneVerified: true,
          createdAt: /* @__PURE__ */ new Date()
        };
        if (signupData.referralCode) {
          try {
            const { ReferralService: ReferralService2 } = await Promise.resolve().then(() => (init_referralService(), referralService_exports));
            const inviterId = await ReferralService2.resolveReferralCode(
              signupData.referralCode
            );
            if (inviterId) newUser.referredBy = inviterId;
          } catch {
          }
        }
        await db2.insert(users5).values(newUser);
        user = newUser;
      } else {
        const newUser = {
          id: crypto.randomUUID(),
          phone,
          name: `Usuario ${phone.slice(-4)}`,
          role: "customer",
          isActive: true,
          phoneVerified: true,
          createdAt: /* @__PURE__ */ new Date()
        };
        await db2.insert(users5).values(newUser);
        user = newUser;
      }
    } else {
      await db2.update(users5).set({ phoneVerified: true, isActive: true }).where((0, import_drizzle_orm7.eq)(users5.id, user.id));
      user.phoneVerified = true;
      user.isActive = true;
    }
    const token = signToken(user.id);
    res.json({
      success: true,
      token,
      user: {
        id: user.id,
        name: user.name,
        phone: user.phone,
        role: user.role,
        isActive: user.isActive,
        verificationStatus: user.verificationStatus ?? "pending",
        phoneVerified: user.phoneVerified,
        profileImage: user.profileImage || null
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/phone-signup", async (req, res) => {
  try {
    const { name, role, phone, email, password } = req.body;
    if (!phone || !name)
      return res.status(400).json({ error: "Nombre y tel\xE9fono requeridos" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [existing] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.phone, phone)).limit(1);
    if (existing) {
      if (!existing.phoneVerified && !existing.isActive) {
        console.log(`\u{1F504} Re-registro permitido para tel\xE9fono no verificado: ${phone} (userId: ${existing.id})`);
        await db2.update(users5).set({
          email: null,
          password: null,
          role: "customer",
          updatedAt: /* @__PURE__ */ new Date()
        }).where((0, import_drizzle_orm7.eq)(users5.id, existing.id));
      } else {
        return res.status(400).json({ error: "El tel\xE9fono ya est\xE1 registrado" });
      }
    }
    let userId = existing?.id;
    if (!userId) {
      let hashedPassword = null;
      if (password) {
        const bcrypt = await import("bcryptjs");
        hashedPassword = await bcrypt.hash(password, 10);
      }
      const tempUser = {
        id: crypto.randomUUID(),
        phone,
        name,
        email: email || null,
        password: hashedPassword,
        role: role || "customer",
        isActive: false,
        phoneVerified: false,
        createdAt: /* @__PURE__ */ new Date()
      };
      await db2.insert(users5).values(tempUser);
      userId = tempUser.id;
    } else {
      let hashedPassword = null;
      if (password) {
        const bcrypt = await import("bcryptjs");
        hashedPassword = await bcrypt.hash(password, 10);
      }
      await db2.update(users5).set({
        name,
        email: email || null,
        password: hashedPassword,
        role: role || "customer",
        isActive: false,
        phoneVerified: false,
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm7.eq)(users5.id, userId));
    }
    const { sendVerificationCode: sendVerificationCode2 } = await Promise.resolve().then(() => (init_smsService(), smsService_exports));
    const sent = await sendVerificationCode2(phone, "123456");
    if (!sent) {
      return res.status(500).json({ error: "No se pudo enviar el c\xF3digo SMS" });
    }
    res.json({ success: true, requiresVerification: true, userId });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/dev-login", async (req, res) => {
  if (process.env.NODE_ENV === "production") {
    return res.status(404).json({ error: "Not found" });
  }
  try {
    const { userId } = req.body;
    if (!userId)
      return res.status(400).json({ error: "userId requerido" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.id, userId)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    const token = signToken(user.id);
    res.json({
      success: true,
      token,
      user: {
        id: user.id,
        name: user.name,
        phone: user.phone,
        email: user.email,
        role: user.role,
        isActive: user.isActive,
        verificationStatus: user.verificationStatus ?? "pending",
        phoneVerified: user.phoneVerified,
        profileImage: user.profileImage || null
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/dev-email-login", async (req, res) => {
  try {
    const { email, password } = req.body;
    if (!email || !password)
      return res.status(400).json({ error: "Email y contrase\xF1a requeridos" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.email, email)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    if (user.password) {
      const bcrypt = await import("bcryptjs");
      const isValid = await bcrypt.compare(password, user.password);
      if (!isValid)
        return res.status(401).json({
          error: "Credenciales incorrectas. Verifica tu correo y contrase\xF1a."
        });
    } else {
      return res.status(401).json({
        error: "Esta cuenta no tiene contrase\xF1a configurada. Inicia sesi\xF3n con c\xF3digo SMS."
      });
    }
    const token = signToken(user.id);
    res.json({
      success: true,
      token,
      user: {
        id: user.id,
        name: user.name,
        phone: user.phone,
        email: user.email,
        role: user.role,
        isActive: user.isActive,
        verificationStatus: user.verificationStatus ?? "pending",
        phoneVerified: user.phoneVerified,
        profileImage: user.profileImage || null
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/biometric-login", async (req, res) => {
  try {
    const { phone } = req.body;
    if (!phone) return res.status(400).json({ error: "Tel\xE9fono requerido" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.phone, phone)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    const token = signToken(user.id);
    res.json({
      success: true,
      token,
      user: {
        id: user.id,
        name: user.name,
        phone: user.phone,
        role: user.role,
        isActive: user.isActive,
        verificationStatus: user.verificationStatus ?? "pending",
        phoneVerified: user.phoneVerified,
        biometricEnabled: user.biometricEnabled,
        profileImage: user.profileImage || null
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/enable-biometric", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    await db2.update(users5).set({ biometricEnabled: true }).where((0, import_drizzle_orm7.eq)(users5.id, req.user.id));
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/disable-biometric", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    await db2.update(users5).set({ biometricEnabled: false }).where((0, import_drizzle_orm7.eq)(users5.id, req.user.id));
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.put("/change-password", authenticateToken, async (req, res) => {
  try {
    const { currentPassword, newPassword } = req.body;
    if (!currentPassword || !newPassword)
      return res.status(400).json({ error: "Faltan campos" });
    if (newPassword.length < 8)
      return res.status(400).json({ error: "La contrase\xF1a debe tener al menos 8 caracteres" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const bcrypt = await import("bcryptjs");
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.id, req.user.id)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    if (user.password) {
      const valid = await bcrypt.compare(currentPassword, user.password);
      if (!valid)
        return res.status(401).json({ success: false, error: "Contrase\xF1a actual incorrecta" });
    }
    const hashed = await bcrypt.hash(newPassword, 10);
    await db2.update(users5).set({ password: hashed }).where((0, import_drizzle_orm7.eq)(users5.id, req.user.id));
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.put("/change-phone", authenticateToken, async (req, res) => {
  try {
    const { newPhone, code } = req.body;
    if (!newPhone || !code)
      return res.status(400).json({ error: "Faltan campos" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [existing] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.phone, newPhone)).limit(1);
    if (existing && existing.id !== req.user.id)
      return res.status(400).json({ error: "Ese tel\xE9fono ya est\xE1 en uso" });
    const { verifyCode: verifyCode2 } = await Promise.resolve().then(() => (init_smsService(), smsService_exports));
    const isValid = await verifyCode2(newPhone, code);
    if (!isValid) return res.status(400).json({ error: "C\xF3digo inv\xE1lido" });
    await db2.update(users5).set({ phone: newPhone }).where((0, import_drizzle_orm7.eq)(users5.id, req.user.id));
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router.post("/logout", authenticateToken, async (req, res) => {
  res.json({ success: true, message: "Sesi\xF3n cerrada" });
});
router.delete("/account", authenticateToken, async (req, res) => {
  try {
    const { users: users5, orders: orders2, addresses: addresses2, favorites: favorites2, businesses: businesses3, deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const userId = req.user.id;
    const [user] = await db2.select().from(users5).where(eq79(users5.id, userId)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    if (user.role === "business_owner") {
      const ownerBusinesses = await db2.select().from(businesses3).where(eq79(businesses3.ownerId, userId));
      for (const biz of ownerBusinesses) {
        await db2.update(businesses3).set({ isActive: false, isOpen: false }).where(eq79(businesses3.id, biz.id));
      }
    }
    if (user.role === "delivery_driver") {
      await db2.delete(deliveryDrivers3).where(eq79(deliveryDrivers3.userId, userId)).catch(() => {
      });
    }
    await db2.update(orders2).set({
      deliveryAddress: null,
      notes: "[Usuario eliminado]"
    }).where(eq79(orders2.userId, userId)).catch(() => {
    });
    await db2.delete(addresses2).where(eq79(addresses2.userId, userId)).catch(() => {
    });
    await db2.delete(favorites2).where(eq79(favorites2.userId, userId)).catch(() => {
    });
    await db2.update(users5).set({
      isActive: false,
      name: "[Cuenta eliminada]",
      email: null,
      phone: `deleted_${userId}`,
      profileImage: null,
      address: null,
      dni: null,
      password: null,
      pushToken: null,
      biometricEnabled: false,
      updatedAt: /* @__PURE__ */ new Date()
    }).where(eq79(users5.id, userId));
    res.json({
      success: true,
      message: "Cuenta eliminada correctamente. Todos tus datos personales han sido eliminados."
    });
  } catch (error) {
    console.error("Delete account error:", error);
    res.status(500).json({ error: error.message });
  }
});
router.get("/me", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm7.eq)(users5.id, req.user.id)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    res.json({
      success: true,
      user: {
        id: user.id,
        name: user.name,
        phone: user.phone,
        role: user.role,
        isActive: user.isActive,
        verificationStatus: user.verificationStatus ?? "pending",
        profileImage: user.profileImage
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var auth_default = router;

// server/routes/business.ts
var import_express2 = __toESM(require("express"));
var import_drizzle_orm14 = require("drizzle-orm");

// server/partnerLevelService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm8 = require("drizzle-orm");
var LEVELS = [
  {
    level: "platinum",
    minOrders: 200,
    minRevenue: 5e7,
    commission: 0.12,
    badge: "\u{1F48E}"
  },
  {
    level: "gold",
    minOrders: 100,
    minRevenue: 2e7,
    commission: 0.13,
    badge: "\u{1F947}"
  },
  {
    level: "silver",
    minOrders: 50,
    minRevenue: 8e6,
    commission: 0.14,
    badge: "\u{1F948}"
  },
  {
    level: "bronze",
    minOrders: 0,
    minRevenue: 0,
    commission: 0.15,
    badge: "\u{1F949}"
  }
];
function getLevelInfo(level) {
  return LEVELS.find((l) => l.level === level) || LEVELS[3];
}
async function updatePartnerLevel(businessId) {
  const since = new Date(Date.now() - 30 * 24 * 60 * 60 * 1e3);
  const [stats] = await db.select({ orderCount: (0, import_drizzle_orm8.count)(), revenue: (0, import_drizzle_orm8.sum)(orders.total) }).from(orders).where(
    (0, import_drizzle_orm8.and)((0, import_drizzle_orm8.eq)(orders.businessId, businessId), (0, import_drizzle_orm8.eq)(orders.status, "delivered"))
  );
  const orderCount = stats?.orderCount || 0;
  const revenue = Number(stats?.revenue) || 0;
  const newLevel = LEVELS.find((l) => orderCount >= l.minOrders && revenue >= l.minRevenue)?.level || "bronze";
  await db.update(businesses).set({
    partnerLevel: newLevel,
    partnerLevelUpdatedAt: /* @__PURE__ */ new Date(),
    totalOrdersCompleted: orderCount,
    totalRevenueGenerated: revenue
  }).where((0, import_drizzle_orm8.eq)(businesses.id, businessId));
  return newLevel;
}
async function getPartnerStatus(businessId) {
  const [business] = await db.select({
    partnerLevel: businesses.partnerLevel,
    totalOrdersCompleted: businesses.totalOrdersCompleted,
    totalRevenueGenerated: businesses.totalRevenueGenerated,
    partnerLevelUpdatedAt: businesses.partnerLevelUpdatedAt
  }).from(businesses).where((0, import_drizzle_orm8.eq)(businesses.id, businessId)).limit(1);
  if (!business) throw new Error("Negocio no encontrado");
  const level = business.partnerLevel || "bronze";
  const info = getLevelInfo(level);
  const nextLevel = LEVELS[LEVELS.findIndex((l) => l.level === level) - 1];
  return {
    level,
    badge: info.badge,
    commission: info.commission,
    totalOrders: business.totalOrdersCompleted || 0,
    totalRevenue: business.totalRevenueGenerated || 0,
    updatedAt: business.partnerLevelUpdatedAt,
    nextLevel: nextLevel ? {
      level: nextLevel.level,
      badge: nextLevel.badge,
      ordersNeeded: Math.max(
        0,
        nextLevel.minOrders - (business.totalOrdersCompleted || 0)
      ),
      revenueNeeded: Math.max(
        0,
        nextLevel.minRevenue - (business.totalRevenueGenerated || 0)
      )
    } : null
  };
}

// server/routes/business.ts
init_config();

// server/rateLimiters.ts
var import_express_rate_limit = __toESM(require("express-rate-limit"));
init_errors();
init_logger();
var generalLimiter = (0, import_express_rate_limit.default)({
  windowMs: 15 * 60 * 1e3,
  max: 100,
  message: "Too many requests from this IP",
  standardHeaders: true,
  legacyHeaders: false,
  handler: (req, res) => {
    logger.security("Rate limit exceeded", {
      ip: req.ip,
      path: req.path
    });
    throw new RateLimitError();
  }
});
var authLimiter = (0, import_express_rate_limit.default)({
  windowMs: 15 * 60 * 1e3,
  max: 5,
  skipSuccessfulRequests: true,
  message: "Too many authentication attempts",
  handler: (req, res) => {
    logger.security("Auth rate limit exceeded", {
      ip: req.ip,
      path: req.path
    });
    throw new RateLimitError(
      "Too many authentication attempts. Try again later."
    );
  }
});
var orderLimiter = (0, import_express_rate_limit.default)({
  windowMs: 60 * 1e3,
  max: 10,
  message: "Too many orders created",
  handler: (req, res) => {
    logger.security("Order rate limit exceeded", {
      ip: req.ip,
      userId: req.user?.id
    });
    throw new RateLimitError("Too many orders. Please wait a moment.");
  }
});
var webhookLimiter = (0, import_express_rate_limit.default)({
  windowMs: 60 * 1e3,
  max: 100,
  skipFailedRequests: true
});
var geocodingLimiter = (0, import_express_rate_limit.default)({
  windowMs: 60 * 1e3,
  // 1 minuto
  max: 5,
  // máximo 5 geocodificaciones por minuto por IP
  message: "Demasiadas solicitudes de geocodificaci\xF3n. Espera un momento.",
  standardHeaders: true,
  legacyHeaders: false,
  handler: (req, res) => {
    logger.security("Geocoding rate limit exceeded", {
      ip: req.ip,
      userId: req.user?.id
    });
    throw new RateLimitError(
      "Demasiadas solicitudes de geocodificaci\xF3n. Espera un momento."
    );
  }
});
var gpsLimiter = (0, import_express_rate_limit.default)({
  windowMs: 60 * 1e3,
  // 1 minuto
  max: 120,
  // defensa adicional; los límites reales de Google viven en googleMapsService
  message: "Demasiadas solicitudes de mapas. Espera un momento.",
  standardHeaders: true,
  legacyHeaders: false,
  handler: (req, res) => {
    logger.security("GPS rate limit exceeded", {
      ip: req.ip,
      userId: req.user?.id,
      path: req.path
    });
    throw new RateLimitError(
      "Demasiadas solicitudes de mapas. Espera un momento."
    );
  }
});

// server/routes/business.ts
var router2 = import_express2.default.Router();
var geocodeCache = /* @__PURE__ */ new Map();
var GEOCODE_CACHE_TTL = 30 * 60 * 1e3;
function getGeocodeCacheKey(address) {
  return `geo:${address.toLowerCase().trim()}`;
}
function getCachedGeocode(address) {
  const key = getGeocodeCacheKey(address);
  const cached = geocodeCache.get(key);
  if (cached && cached.expires > Date.now()) {
    return { lat: cached.lat, lng: cached.lng };
  }
  if (cached) geocodeCache.delete(key);
  return null;
}
function setGeocodeCache(address, lat, lng) {
  const key = getGeocodeCacheKey(address);
  geocodeCache.set(key, { lat, lng, expires: Date.now() + GEOCODE_CACHE_TTL });
}
router2.get(
  "/partner-level",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const status = await getPartnerStatus(business.id);
      res.json({ success: true, ...status });
    } catch (err) {
      res.status(500).json({ error: err.message });
    }
  }
);
router2.post(
  "/partner-level/recalculate",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId } = req.body;
      const newLevel = await updatePartnerLevel(businessId);
      res.json({ success: true, level: newLevel });
    } catch (err) {
      res.status(500).json({ error: err.message });
    }
  }
);
router2.get("/featured", async (req, res) => {
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { sql: sql23 } = await import("drizzle-orm");
    const [rows] = await db2.execute(
      sql23`SELECT * FROM businesses WHERE is_featured = 1 AND is_active = 1 AND verification_status = 'verified' LIMIT 10`
    );
    res.json({ success: true, businesses: rows });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router2.get("/nearby", async (req, res) => {
  try {
    const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const allBusinesses = await db2.select().from(businesses3).where(
      (0, import_drizzle_orm14.and)(
        (0, import_drizzle_orm14.eq)(businesses3.isActive, true),
        (0, import_drizzle_orm14.eq)(businesses3.verificationStatus, "verified")
      )
    );
    res.json({ success: true, businesses: allBusinesses });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router2.get(
  "/dashboard",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId } = req.query;
      const { businesses: businesses3, orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = businessId ? await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1) : await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const businessOrders = await db2.select().from(orders2).where((0, import_drizzle_orm14.eq)(orders2.businessId, business.id)).orderBy((0, import_drizzle_orm14.desc)(orders2.createdAt));
      const today = /* @__PURE__ */ new Date();
      const todayOrders = businessOrders.filter(
        (o) => new Date(o.createdAt).toDateString() === today.toDateString()
      );
      const pendingOrders = businessOrders.filter(
        (o) => o.status === "pending"
      );
      const todayRevenue = todayOrders.filter((o) => o.status === "delivered").reduce((sum3, o) => sum3 + (o.subtotal || 0), 0);
      res.json({
        success: true,
        dashboard: {
          business,
          isOpen: business.isOpen || false,
          pendingOrders: pendingOrders.length,
          todayOrders: todayOrders.length,
          // Céntimos exactos: el cliente formatea ("59,80 €"), sin redondeos
          todayRevenue,
          totalOrders: businessOrders.length,
          recentOrders: businessOrders.slice(0, 10)
        }
      });
    } catch (error) {
      console.error("Dashboard error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/stats",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId } = req.query;
      const { businesses: businesses3, orders: orders2, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      const [business] = businessId ? await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1) : await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const businessOrders = await db2.select().from(orders2).where((0, import_drizzle_orm14.eq)(orders2.businessId, business.id));
      const today = /* @__PURE__ */ new Date();
      const thisWeek = new Date(today.getTime() - 7 * 24 * 60 * 60 * 1e3);
      const thisMonth = new Date(today.getFullYear(), today.getMonth(), 1);
      const revenue = (list) => list.filter((o) => o.status === "delivered").reduce((s, o) => s + (o.subtotal || 0), 0);
      const todayOrders = businessOrders.filter(
        (o) => new Date(o.createdAt).toDateString() === today.toDateString()
      );
      const weekOrders = businessOrders.filter(
        (o) => new Date(o.createdAt) >= thisWeek
      );
      const monthOrders = businessOrders.filter(
        (o) => new Date(o.createdAt) >= thisMonth
      );
      const completedOrders = businessOrders.filter(
        (o) => o.status === "delivered"
      );
      const cancelledOrders = businessOrders.filter(
        (o) => o.status === "cancelled"
      );
      const totalRevenue = revenue(businessOrders);
      const avgValue = completedOrders.length > 0 ? Math.round(totalRevenue / completedOrders.length) : 0;
      const [topProductRows] = await db2.execute(sql23`
      SELECT 
        p.name,
        SUM(JSON_EXTRACT(oi.value, '$.quantity')) as quantity,
        SUM(JSON_EXTRACT(oi.value, '$.quantity') * JSON_EXTRACT(oi.value, '$.price')) as revenue
      FROM orders o
      CROSS JOIN JSON_TABLE(
        o.items,
        '$[*]' COLUMNS(
          value JSON PATH '$'
        )
      ) oi
      JOIN products p ON p.id = JSON_EXTRACT(oi.value, '$.productId')
      WHERE o.business_id = ${business.id}
        AND o.status = 'delivered'
      GROUP BY p.id, p.name
      ORDER BY quantity DESC
      LIMIT 5
    `);
      const topProducts = topProductRows.map((row) => ({
        name: row.name,
        quantity: parseInt(row.quantity) || 0,
        revenue: (parseInt(row.revenue) || 0) / 100
      }));
      res.json({
        success: true,
        // Céntimos exactos: el cliente formatea ("59,80 €"), sin redondeos
        revenue: {
          today: revenue(todayOrders),
          week: revenue(weekOrders),
          month: revenue(monthOrders),
          total: totalRevenue
        },
        orders: {
          total: businessOrders.length,
          completed: completedOrders.length,
          cancelled: cancelledOrders.length,
          avgValue
        },
        topProducts
      });
    } catch (error) {
      console.error("Stats error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/limits",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    res.json({
      success: true,
      limits: {
        maxProducts: 100,
        maxCategories: 20,
        maxImages: 10,
        maxOrdersPerHour: 50,
        maxDeliveryRadius: 10,
        minOrderAmount: 1e3,
        maxOrderAmount: 1e5
      }
    });
  }
);
router2.get(
  "/active-deliveries",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3, orders: orders2, users: users5, deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23, inArray: inArray12 } = await import("drizzle-orm");
      const ownerBusinesses = await db2.select({
        id: businesses3.id,
        name: businesses3.name,
        latitude: businesses3.latitude,
        longitude: businesses3.longitude
      }).from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id));
      if (!ownerBusinesses.length)
        return res.json({ success: true, deliveries: [], stats: {} });
      const businessIds = ownerBusinesses.map((b) => b.id);
      const [rows] = await db2.execute(sql23`
      SELECT
        o.id, o.status, o.subtotal, o.delivery_fee, o.total, o.created_at,
        o.order_number,
        o.business_id, o.delivery_address, o.payment_method,
        o.delivery_latitude  AS customer_lat,
        o.delivery_longitude AS customer_lng,
        u.name  AS customer_name,  u.phone AS customer_phone,
        dd.id   AS driver_row_id,  dd.user_id AS driver_user_id,
        dd.vehicle_type, dd.rating AS driver_rating,
        dd.current_latitude AS driver_lat, dd.current_longitude AS driver_lng,
        dd.last_location_update AS driver_last_update,
        du.name AS driver_name,    du.phone AS driver_phone,
        b.name  AS business_name
      FROM orders o
      LEFT JOIN users u   ON o.user_id = u.id
      LEFT JOIN delivery_drivers dd ON o.delivery_person_id = dd.user_id
      LEFT JOIN users du  ON dd.user_id = du.id
      LEFT JOIN businesses b ON o.business_id = b.id
      WHERE o.business_id IN (${sql23.join(
        businessIds.map((id) => sql23`${id}`),
        sql23`, `
      )})
        AND o.status IN ('accepted','confirmed','preparing','ready','picked_up','on_the_way','in_transit','arriving')
        AND o.deleted_at IS NULL
      ORDER BY o.created_at DESC
    `);
      const deliveries = rows.map((r) => {
        let address = null;
        try {
          address = typeof r.delivery_address === "string" ? JSON.parse(r.delivery_address) : r.delivery_address;
        } catch {
        }
        const minutesActive = Math.round(
          (Date.now() - new Date(r.created_at).getTime()) / 6e4
        );
        return {
          orderId: r.id,
          orderNumber: r.order_number ?? null,
          status: r.status,
          subtotal: r.subtotal,
          deliveryFee: r.delivery_fee,
          total: r.total,
          paymentMethod: r.payment_method,
          minutesActive,
          businessId: r.business_id,
          businessName: r.business_name,
          customer: {
            name: r.customer_name,
            phone: r.customer_phone,
            lat: r.customer_lat ? parseFloat(r.customer_lat) : null,
            lng: r.customer_lng ? parseFloat(r.customer_lng) : null,
            address: r.delivery_address || null
          },
          driver: r.driver_user_id ? {
            id: r.driver_user_id,
            rowId: r.driver_row_id,
            name: r.driver_name,
            phone: r.driver_phone,
            lat: r.driver_lat ? parseFloat(r.driver_lat) : null,
            lng: r.driver_lng ? parseFloat(r.driver_lng) : null,
            lastUpdate: r.driver_last_update || null,
            vehicleType: r.vehicle_type,
            rating: r.driver_rating ? (r.driver_rating / 10).toFixed(1) : null
          } : null
        };
      });
      const stats = {
        totalActive: deliveries.length,
        pending: deliveries.filter(
          (d) => ["accepted", "confirmed"].includes(d.status)
        ).length,
        preparing: deliveries.filter(
          (d) => ["preparing", "ready"].includes(d.status)
        ).length,
        onTheWay: deliveries.filter(
          (d) => ["picked_up", "on_the_way", "in_transit", "arriving"].includes(
            d.status
          )
        ).length,
        avgMinutes: deliveries.length ? Math.round(
          deliveries.reduce((s, d) => s + d.minutesActive, 0) / deliveries.length
        ) : 0,
        pendingRevenue: deliveries.reduce(
          (s, d) => s + (d.subtotal || 0),
          0
        )
      };
      res.json({
        success: true,
        deliveries,
        businesses: ownerBusinesses,
        stats
      });
    } catch (err) {
      console.error("active-deliveries error:", err);
      res.status(500).json({ error: err.message });
    }
  }
);
router2.get(
  "/my-businesses",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businesses: businesses3, orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { inArray: inArray12 } = await import("drizzle-orm");
      const ownerBusinesses = await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id));
      const businessIds = ownerBusinesses.map((b) => b.id);
      const allOrders = businessIds.length > 0 ? await db2.select().from(orders2).where(inArray12(orders2.businessId, businessIds)) : [];
      const result = ownerBusinesses.map((business) => {
        const bOrders = allOrders.filter((o) => o.businessId === business.id);
        const deliveredOrders = bOrders.filter((o) => o.status === "delivered");
        const pendingOrders = bOrders.filter(
          (o) => ["pending", "accepted", "preparing"].includes(o.status)
        );
        return {
          ...business,
          stats: {
            pendingOrders: pendingOrders.length,
            totalOrders: deliveredOrders.length,
            totalRevenue: deliveredOrders.reduce(
              (s, o) => s + (o.subtotal || 0),
              0
            )
          }
        };
      });
      res.json({ success: true, businesses: result });
    } catch (error) {
      console.error("My businesses error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/orders",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId } = req.query;
      const { businesses: businesses3, orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      const [business] = businessId ? await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1) : await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const [orderRows] = await db2.execute(sql23`
      SELECT o.*, u.name as customer_name, u.phone as customer_phone
      FROM orders o
      LEFT JOIN users u ON o.user_id = u.id
      WHERE o.business_id = ${business.id}
        AND o.deleted_at IS NULL
      ORDER BY o.created_at DESC
    `);
      const { classifyPaymentState: classifyPaymentState2, pendingProofOrderIds: pendingProofOrderIds2 } = await Promise.resolve().then(() => (init_paymentState(), paymentState_exports));
      const proofIds = await pendingProofOrderIds2(
        orderRows.filter((r) => r.status === "pending").map((r) => r.id)
      );
      for (const row of orderRows) {
        if (row.status === "pending" && String(row.payment_method || "").startsWith("stripe_") && row.stripe_payment_intent_id && !proofIds.has(row.id)) {
          const { rescueStripePayment: rescueStripePayment3 } = await Promise.resolve().then(() => (init_paymentConfirmationService(), paymentConfirmationService_exports));
          rescueStripePayment3({
            id: row.id,
            paymentMethod: row.payment_method,
            stripePaymentIntentId: row.stripe_payment_intent_id
          }).catch(() => {
          });
        }
      }
      const { collectSubstituteIds: collectSubstituteIds2 } = await Promise.resolve().then(() => (init_substitution(), substitution_exports));
      const substituteIds = new Set(collectSubstituteIds2(orderRows));
      const productNames = /* @__PURE__ */ new Map();
      const productDetails = /* @__PURE__ */ new Map();
      if (substituteIds.size) {
        const { products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
        const idList = [...substituteIds];
        const [prodRows] = await db2.execute(sql23`
          SELECT id, name, image, price FROM products
          WHERE id IN (${sql23.join(idList.map((id) => sql23`${id}`), sql23`, `)})
        `);
        (prodRows || []).forEach((p) => {
          productNames.set(p.id, p.name);
          productDetails.set(p.id, {
            name: p.name,
            image: p.image || null,
            price: p.price ?? null
          });
        });
      }
      const subsByOrder = /* @__PURE__ */ new Map();
      if (orderRows.length) {
        try {
          const { substitutions: substitutions2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          const { inArray: inArray12 } = await import("drizzle-orm");
          const subRows = await db2.select().from(substitutions2).where(
            inArray12(
              substitutions2.orderId,
              orderRows.map((r) => r.id)
            )
          ).limit(200);
          for (const s of subRows) {
            if (!subsByOrder.has(s.orderId)) subsByOrder.set(s.orderId, []);
            subsByOrder.get(s.orderId).push(s);
          }
        } catch {
        }
      }
      const formattedOrders = orderRows.map((row) => {
        let addressObj = null;
        if (row.delivery_address) {
          try {
            addressObj = JSON.parse(row.delivery_address);
          } catch {
            addressObj = { street: row.delivery_address };
          }
        }
        return {
          id: row.id,
          orderNumber: row.order_number ?? null,
          userId: row.user_id,
          businessId: row.business_id,
          businessName: row.business_name,
          businessImage: row.business_image,
          items: row.items,
          status: row.status,
          paymentState: classifyPaymentState2(
            {
              id: row.id,
              status: row.status,
              paymentMethod: row.payment_method,
              paidAt: row.paid_at
            },
            proofIds
          ),
          // orderType es imprescindible en el panel: los pedidos de recogida
          // no deben avisar a repartidores ni pedir tiempo de preparación
          orderType: row.order_type || "delivery",
          subtotal: row.subtotal,
          deliveryFee: row.delivery_fee,
          total: row.total,
          paymentMethod: row.payment_method,
          deliveryAddress: row.delivery_address,
          notes: row.notes,
          createdAt: row.created_at,
          // Sustitución: preferencia global, preferencias por ítem, IDs de
          // sustitutos y sus NOMBRES resueltos (para la UI del negocio)
          substitutionPreference: row.substitution_preference ?? null,
          itemSubstitutionPreferences: row.item_substitution_preferences ?? null,
          substituteProductIds: row.substitute_product_ids ?? null,
          substituteProductNames: Object.fromEntries(productNames),
          substituteProducts: Object.fromEntries(productDetails),
          substitutions: subsByOrder.get(row.id) || [],
          customer: row.customer_name ? {
            name: row.customer_name,
            phone: row.customer_phone
          } : null,
          address: addressObj
        };
      });
      res.json({ success: true, orders: formattedOrders });
    } catch (error) {
      console.error("Business orders error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/products",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId } = req.query;
      const { businesses: businesses3, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = businessId ? await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1) : await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const businessProducts = await db2.select().from(products3).where((0, import_drizzle_orm14.eq)(products3.businessId, business.id));
      res.json({ success: true, products: businessProducts });
    } catch (error) {
      console.error("Business products error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/payouts",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId } = req.query;
      const { businesses: businesses3, payouts: payouts2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, inArray: inArray12, or: or8, desc: desc19 } = await import("drizzle-orm");
      const ownerBusinesses = await db2.select({ id: businesses3.id }).from(businesses3).where(
        businessId ? (0, import_drizzle_orm14.and)(
          eq79(businesses3.id, businessId),
          eq79(businesses3.ownerId, req.user.id)
        ) : eq79(businesses3.ownerId, req.user.id)
      );
      if (ownerBusinesses.length === 0) {
        return res.json({ success: true, payouts: [] });
      }
      const businessIds = ownerBusinesses.map((b) => b.id);
      const businessPayouts = await db2.select().from(payouts2).where(
        or8(
          inArray12(payouts2.recipientId, businessIds),
          // retiros del dueño (recipientId = id del usuario)
          eq79(payouts2.recipientId, req.user.id)
        )
      ).orderBy(desc19(payouts2.createdAt));
      res.json({ success: true, payouts: businessPayouts });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/finances",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId, period = "week" } = req.query;
      const { businesses: businesses3, orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      const [business] = businessId ? await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1) : await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const today = /* @__PURE__ */ new Date();
      let startDate;
      switch (period) {
        case "week":
          startDate = new Date(today.getTime() - 7 * 24 * 60 * 60 * 1e3);
          break;
        case "month":
          startDate = new Date(today.getFullYear(), today.getMonth(), 1);
          break;
        case "all":
        default:
          startDate = /* @__PURE__ */ new Date(0);
          break;
      }
      const [transactionRows] = await db2.execute(sql23`
      SELECT 
        o.id,
        o.subtotal,
        o.delivery_fee,
        o.total,
        o.status,
        o.payment_method,
        o.delivery_address,
        o.notes,
        o.created_at,
        u.name as customer_name,
        u.phone as customer_phone
      FROM orders o
      LEFT JOIN users u ON o.user_id = u.id
      WHERE o.business_id = ${business.id}
        AND o.created_at >= ${startDate.toISOString()}
      ORDER BY o.created_at DESC
    `);
      const transactions4 = transactionRows.map((row) => {
        let address = null;
        if (row.delivery_address) {
          try {
            address = JSON.parse(row.delivery_address);
          } catch {
            address = { street: row.delivery_address };
          }
        }
        return {
          id: row.id,
          orderId: row.id,
          subtotal: row.subtotal || 0,
          deliveryFee: row.delivery_fee || 0,
          total: row.total || 0,
          status: row.status,
          paymentMethod: row.payment_method,
          deliveryAddress: address,
          notes: row.notes,
          createdAt: row.created_at,
          customerName: row.customer_name,
          customerPhone: row.customer_phone
        };
      });
      const completedOrders = transactions4.filter(
        (t) => t.status === "delivered"
      );
      const pendingOrders = transactions4.filter(
        (t) => ["pending", "accepted", "preparing", "on_the_way"].includes(t.status)
      );
      const completedAmount = completedOrders.reduce(
        (sum3, t) => sum3 + (t.subtotal || 0),
        0
      );
      const pendingAmount = pendingOrders.reduce(
        (sum3, t) => sum3 + (t.subtotal || 0),
        0
      );
      res.json({
        success: true,
        transactions: transactions4,
        summary: {
          totalEarnings: completedAmount + pendingAmount,
          completedAmount,
          pendingAmount,
          transactionCount: transactions4.length
        }
      });
    } catch (error) {
      console.error("Business finances error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.put(
  "/verification-status",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { verificationStatus } = req.body;
      if (!verificationStatus || !["pending", "verified", "rejected"].includes(verificationStatus)) {
        return res.status(400).json({ error: "verificationStatus debe ser pending, verified o rejected" });
      }
      if (req.user.role === "admin" || req.user.role === "super_admin") {
        const { userId } = req.body;
        if (!userId) return res.status(400).json({ error: "userId requerido" });
        await db2.update(businesses3).set({
          verificationStatus,
          isActive: verificationStatus === "verified" ? true : false
        }).where(eq79(businesses3.ownerId, userId));
        try {
          const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
          const body = verificationStatus === "verified" ? "\xA1Tu negocio fue verificado! Ya puedes recibir pedidos \u{1F389}" : verificationStatus === "rejected" ? "Tu solicitud de negocio fue rechazada. Revisa tus datos." : "Tu negocio volvi\xF3 a estado de revisi\xF3n.";
          await sendPushToUser2(userId, {
            title: verificationStatus === "verified" ? "\u2705 Negocio verificado" : verificationStatus === "rejected" ? "\u274C Negocio rechazado" : "\u{1F550} Negocio en revisi\xF3n",
            body,
            data: { screen: "MyBusinesses" }
          });
        } catch (err) {
          console.error("Error sending verification push:", err);
        }
      } else {
        if (verificationStatus !== "pending") {
          return res.status(403).json({ error: "Solo un administrador puede verificar negocios" });
        }
        await db2.update(businesses3).set({ verificationStatus, isActive: false }).where(eq79(businesses3.ownerId, req.user.id));
      }
      res.json({ success: true, message: "Estado de verificaci\xF3n actualizado" });
    } catch (error) {
      console.error("Update verification status error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/hours",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId } = req.query;
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = businessId ? await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1) : await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const defaultHours = {
        monday: { open: "09:00", close: "22:00", closed: false },
        tuesday: { open: "09:00", close: "22:00", closed: false },
        wednesday: { open: "09:00", close: "22:00", closed: false },
        thursday: { open: "09:00", close: "22:00", closed: false },
        friday: { open: "09:00", close: "22:00", closed: false },
        saturday: { open: "09:00", close: "22:00", closed: false },
        sunday: { open: "09:00", close: "22:00", closed: false }
      };
      const hours = business.openingHours ? JSON.parse(business.openingHours) : defaultHours;
      res.json({ success: true, hours });
    } catch (error) {
      console.error("Business hours error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.put(
  "/hours",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId, hours } = req.body;
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = businessId ? await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1) : await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      await db2.update(businesses3).set({ openingHours: JSON.stringify(hours), isOpen: true }).where((0, import_drizzle_orm14.eq)(businesses3.id, business.id));
      try {
        const { BusinessHoursService: BusinessHoursService2 } = await Promise.resolve().then(() => (init_businessHoursService(), businessHoursService_exports));
        const shouldBeOpen = await BusinessHoursService2.isBusinessOpen(business.id);
        await db2.update(businesses3).set({ isOpen: shouldBeOpen }).where((0, import_drizzle_orm14.eq)(businesses3.id, business.id));
        console.log(`\u2705 ${business.name}: isOpen=${shouldBeOpen} actualizado tras guardar horarios`);
      } catch (recalcErr) {
        console.error("Error recalculating isOpen:", recalcErr);
      }
      res.json({ success: true, message: "Horarios actualizados" });
    } catch (error) {
      console.error("Update hours error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.post(
  "/products",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
      const { name, description, price, image } = req.body;
      const [business] = await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      let imageUrl = null;
      if (image && image.startsWith("data:image/")) {
        const productId = crypto.randomUUID();
        imageUrl = await CloudinaryService2.uploadImage(
          image,
          "products",
          `product-${productId}`
        );
      }
      const newProduct = {
        id: crypto.randomUUID(),
        businessId: business.id,
        name,
        description: description || null,
        price,
        image: imageUrl || image,
        isAvailable: true,
        createdAt: /* @__PURE__ */ new Date()
      };
      await db2.insert(products3).values(newProduct);
      res.json({ success: true, product: newProduct });
    } catch (error) {
      console.error("Create product error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.put(
  "/products/:id",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
      const [product] = await db2.select().from(products3).where((0, import_drizzle_orm14.eq)(products3.id, req.params.id)).limit(1);
      if (!product)
        return res.status(404).json({ error: "Producto no encontrado" });
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, product.businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business) return res.status(403).json({ error: "No autorizado" });
      const updates = {};
      if (req.body.name !== void 0) updates.name = req.body.name;
      if (req.body.description !== void 0)
        updates.description = req.body.description;
      if (req.body.price !== void 0) updates.price = req.body.price;
      if (req.body.image !== void 0) {
        if (req.body.image && req.body.image.startsWith("data:image/")) {
          if (product.image && product.image.includes("cloudinary")) {
            const oldPublicId = CloudinaryService2.extractPublicId(
              product.image
            );
            if (oldPublicId)
              await CloudinaryService2.deleteImage(oldPublicId).catch(() => {
              });
          }
          updates.image = await CloudinaryService2.uploadImage(
            req.body.image,
            "products",
            `product-${req.params.id}`
          );
        } else {
          updates.image = req.body.image;
        }
      }
      await db2.update(products3).set(updates).where((0, import_drizzle_orm14.eq)(products3.id, req.params.id));
      res.json({ success: true, message: "Producto actualizado" });
    } catch (error) {
      console.error("Update product error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.put(
  "/products/:id/availability",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { isAvailable } = req.body;
      const [product] = await db2.select().from(products3).where((0, import_drizzle_orm14.eq)(products3.id, req.params.id)).limit(1);
      if (!product)
        return res.status(404).json({ error: "Producto no encontrado" });
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, product.businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business) return res.status(403).json({ error: "No autorizado" });
      await db2.update(products3).set({ isAvailable }).where((0, import_drizzle_orm14.eq)(products3.id, req.params.id));
      res.json({ success: true, message: "Disponibilidad actualizada" });
    } catch (error) {
      console.error("Update availability error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.delete(
  "/products/:id",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [product] = await db2.select().from(products3).where((0, import_drizzle_orm14.eq)(products3.id, req.params.id)).limit(1);
      if (!product)
        return res.status(404).json({ error: "Producto no encontrado" });
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, product.businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business) return res.status(403).json({ error: "No autorizado" });
      await db2.delete(products3).where((0, import_drizzle_orm14.eq)(products3.id, req.params.id));
      res.json({ success: true, message: "Producto eliminado" });
    } catch (error) {
      console.error("Delete product error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.put(
  "/orders/:id/status",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { status } = req.body;
      const { businesses: businesses3, orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const validStatuses = [
        "pending",
        "accepted",
        "preparing",
        "ready",
        "picked_up",
        "on_the_way",
        "in_transit",
        "arriving",
        "delivered",
        "cancelled"
      ];
      if (!status || !validStatuses.includes(status)) {
        return res.status(400).json({ error: "Estado inv\xE1lido" });
      }
      const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm14.eq)(orders2.id, req.params.id)).limit(1);
      if (!order)
        return res.status(404).json({ error: "Pedido no encontrado" });
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, order.businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business && req.user.role !== "admin" && req.user.role !== "super_admin") {
        return res.status(403).json({ error: "No autorizado" });
      }
      if (status === "accepted" && order.status === "pending") {
        const { paymentStateOf: paymentStateOf2, acceptanceBlockedMessage: acceptanceBlockedMessage2 } = await Promise.resolve().then(() => (init_paymentState(), paymentState_exports));
        let paymentState = await paymentStateOf2(order);
        if (paymentState === "awaiting_payment") {
          const { rescueStripePayment: rescueStripePayment3 } = await Promise.resolve().then(() => (init_paymentConfirmationService(), paymentConfirmationService_exports));
          if (await rescueStripePayment3(order)) paymentState = "paid";
        }
        const blocked = acceptanceBlockedMessage2(paymentState);
        if (blocked) {
          return res.status(400).json({ error: blocked });
        }
      }
      const updates = { status, updatedAt: /* @__PURE__ */ new Date() };
      if (status === "accepted") updates.businessResponseAt = /* @__PURE__ */ new Date();
      if (status === "preparing") updates.assignedAt = /* @__PURE__ */ new Date();
      const { estimatedPrepMinutes, estimatedPrepRange } = req.body;
      if (status === "preparing" && (estimatedPrepMinutes || estimatedPrepRange)) {
        if (estimatedPrepMinutes)
          updates.estimatedPrepMinutes = parseInt(String(estimatedPrepMinutes));
        if (estimatedPrepRange)
          updates.estimatedPrepRange = String(estimatedPrepRange);
      }
      await db2.update(orders2).set(updates).where((0, import_drizzle_orm14.eq)(orders2.id, req.params.id));
      try {
        const { sendOrderStatusNotification: sendOrderStatusNotification2, sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        const { orderRef: orderRef2 } = await Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports));
        await sendOrderStatusNotification2(order.id, order.userId, status);
        const isPickupOrder = order.orderType === "pickup";
        if (status === "preparing" && !isPickupOrder) {
          const rangeText = estimatedPrepRange || "unos minutos";
          if (order.deliveryPersonId) {
            await sendPushToUser2(order.deliveryPersonId, {
              title: "\u{1F468}\u200D\u{1F373} Pedido en preparaci\xF3n",
              body: `${order.businessName} \u2014 Pedido ${orderRef2(order)} listo en ${rangeText}`,
              data: { orderId: order.id, screen: "DriverActiveOrder" }
            });
          } else {
            const { DeliveryNotificationService: DeliveryNotificationService2 } = await Promise.resolve().then(() => (init_deliveryNotificationService(), deliveryNotificationService_exports));
            await DeliveryNotificationService2.broadcastToDrivers(
              "\u{1F468}\u200D\u{1F373} Pedido en preparaci\xF3n",
              `${order.businessName} \u2014 listo en ${rangeText}. Ac\xE9rcate al local.`,
              { orderId: order.id, screen: "DriverAvailable" }
            );
          }
        }
        if (status === "ready" && !isPickupOrder) {
          if (order.deliveryPersonId) {
            await sendPushToUser2(order.deliveryPersonId, {
              title: "\u{1F4E6} Pedido listo para recoger",
              body: `${order.businessName} \u2014 Pedido ${orderRef2(order)} listo`,
              data: { orderId: order.id, screen: "DriverActiveOrder" }
            });
          } else {
            const { DeliveryNotificationService: DeliveryNotificationService2 } = await Promise.resolve().then(() => (init_deliveryNotificationService(), deliveryNotificationService_exports));
            await DeliveryNotificationService2.broadcastToDrivers(
              "\u{1F6F5} \xA1Nuevo pedido disponible!",
              `${order.businessName} \u2014 recoge el pedido ${orderRef2(order)}`,
              { orderId: order.id, screen: "DriverAvailable" }
            );
          }
        }
        if (status === "cancelled" && order.deliveryPersonId) {
          await sendPushToUser2(order.deliveryPersonId, {
            title: "\u274C Pedido cancelado",
            body: `El pedido ${orderRef2(order)} fue cancelado por el negocio`,
            data: { orderId: order.id, screen: "DriverAvailable" }
          });
        }
      } catch (err) {
        console.error("Error sending order status push:", err);
      }
      res.json({ success: true, message: "Estado actualizado" });
    } catch (error) {
      console.error("Update order status error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.post(
  "/orders/:orderId/substitutions",
  authenticateToken,
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm14.eq)(orders2.id, req.params.orderId)).limit(1);
      if (!order) {
        return res.status(404).json({ error: "Pedido no encontrado" });
      }
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, order.businessId),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business && req.user.role !== "admin" && req.user.role !== "super_admin") {
        return res.status(403).json({ error: "No autorizado" });
      }
      const { itemProductId, substituteProductId } = req.body || {};
      if (!itemProductId || !substituteProductId) {
        return res.status(400).json({
          error: "itemProductId y substituteProductId son obligatorios"
        });
      }
      const { proposeSubstitution: proposeSubstitution2 } = await Promise.resolve().then(() => (init_substitutionService(), substitutionService_exports));
      const result = await proposeSubstitution2(
        order,
        String(itemProductId),
        String(substituteProductId),
        req.user.id
      );
      if (!result.success) {
        return res.status(400).json({ error: result.message });
      }
      res.json(result);
    } catch (error) {
      console.error("Propose substitution error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.post(
  "/create",
  authenticateToken,
  requireRole("business_owner"),
  geocodingLimiter,
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
      const {
        name,
        description,
        type,
        image,
        address,
        phone,
        categories,
        latitude,
        longitude
      } = req.body;
      if (!name)
        return res.status(400).json({ error: "El nombre del negocio es requerido" });
      let imageUrl = null;
      if (image && image.startsWith("data:image/")) {
        const businessId = crypto.randomUUID();
        imageUrl = await CloudinaryService2.uploadImage(
          image,
          "businesses",
          `business-${businessId}`
        );
      } else if (image) {
        imageUrl = image;
      }
      const newBusiness = {
        id: crypto.randomUUID(),
        ownerId: req.user.id,
        name,
        description: description || null,
        type: type || "restaurant",
        image: imageUrl,
        address: address || null,
        phone: phone || null,
        categories: categories || null,
        latitude: latitude || null,
        longitude: longitude || null,
        isActive: true,
        isOpen: false,
        rating: 0,
        totalRatings: 0,
        deliveryTime: CONFIG.DEFAULT_DELIVERY_TIME,
        deliveryFee: CONFIG.DEFAULT_DELIVERY_FEE,
        minOrder: 1e3,
        createdAt: /* @__PURE__ */ new Date()
      };
      await db2.insert(businesses3).values(newBusiness);
      if (address && (!latitude || !longitude)) {
        const cached = getCachedGeocode(`${address}, Soria, Espa\xF1a`);
        if (cached) {
          await db2.update(businesses3).set({ latitude: String(cached.lat), longitude: String(cached.lng) }).where((0, import_drizzle_orm14.eq)(businesses3.id, newBusiness.id));
          newBusiness.latitude = String(cached.lat);
          newBusiness.longitude = String(cached.lng);
        } else {
          try {
            const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
            const geo = await googleMapsService2.geocodeAddress(
              `${address}, Soria, Espa\xF1a`
            );
            if (geo) {
              setGeocodeCache(`${address}, Soria, Espa\xF1a`, geo.lat, geo.lng);
              await db2.update(businesses3).set({ latitude: String(geo.lat), longitude: String(geo.lng) }).where((0, import_drizzle_orm14.eq)(businesses3.id, newBusiness.id));
              newBusiness.latitude = String(geo.lat);
              newBusiness.longitude = String(geo.lng);
            }
          } catch {
          }
        }
      }
      if (!newBusiness.latitude || !newBusiness.longitude) {
        await db2.delete(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.id, newBusiness.id));
        return res.status(400).json({
          error: "No hemos podido ubicar el negocio en el mapa. Revisa la direcci\xF3n o elige el punto en el mapa para poder darlo de alta."
        });
      }
      try {
        const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
        const { inArray: inArray12 } = await import("drizzle-orm");
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        const admins = await db2.select({ id: users5.id }).from(users5).where(inArray12(users5.role, ["admin", "super_admin"]));
        for (const admin of admins) {
          await sendPushToUser2(admin.id, {
            title: "\u{1F3EA} Negocio pendiente de aprobaci\xF3n",
            body: `El negocio "${name}" se registr\xF3 y requiere verificaci\xF3n`,
            data: { screen: "AdminBusinesses" }
          });
        }
      } catch (err) {
        console.error("Error notifying admins of new business:", err);
      }
      res.status(201).json({ success: true, business: newBusiness });
    } catch (error) {
      console.error("Create business error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get("/", async (req, res) => {
  try {
    const { queryWithRetry: queryWithRetry2 } = await Promise.resolve().then(() => (init_dbHelper(), dbHelper_exports));
    const { BusinessHoursService: BusinessHoursService2 } = await Promise.resolve().then(() => (init_businessHoursService(), businessHoursService_exports));
    console.log("\u{1F4CD} GET /api/businesses called");
    const rows = await queryWithRetry2(
      "SELECT * FROM businesses WHERE is_active = 1 AND verification_status = 'verified'"
    );
    const businessesWithRealOpenStatus = await Promise.all(
      rows.map(async (biz) => {
        try {
          const realIsOpen = await BusinessHoursService2.isBusinessOpen(biz.id);
          return { ...biz, isOpen: realIsOpen, is_open: realIsOpen ? 1 : 0 };
        } catch {
          return biz;
        }
      })
    );
    console.log(`\u2705 Found ${businessesWithRealOpenStatus.length} active & verified businesses`);
    res.setHeader("Cache-Control", "no-store, no-cache, must-revalidate");
    res.setHeader("Pragma", "no-cache");
    res.setHeader("Expires", "0");
    res.json({ success: true, businesses: businessesWithRealOpenStatus });
  } catch (error) {
    console.error("\u274C Error in /api/businesses:", error);
    res.status(500).json({
      error: "Failed to fetch businesses",
      details: error.message,
      code: error.code
    });
  }
});
router2.post(
  "/",
  authenticateToken,
  requireRole("business_owner"),
  geocodingLimiter,
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
      const {
        name,
        description,
        type,
        image,
        address,
        phone,
        categories,
        latitude,
        longitude
      } = req.body;
      if (!name)
        return res.status(400).json({ error: "El nombre del negocio es requerido" });
      let imageUrl = null;
      if (image && image.startsWith("data:image/")) {
        const businessId = crypto.randomUUID();
        imageUrl = await CloudinaryService2.uploadImage(
          image,
          "businesses",
          `business-${businessId}`
        );
      } else if (image) {
        imageUrl = image;
      }
      const newBusiness = {
        id: crypto.randomUUID(),
        ownerId: req.user.id,
        name,
        description: description || null,
        type: type || "restaurant",
        image: imageUrl,
        address: address || null,
        phone: phone || null,
        categories: categories || null,
        latitude: latitude || null,
        longitude: longitude || null,
        isActive: true,
        isOpen: false,
        rating: 0,
        totalRatings: 0,
        deliveryTime: CONFIG.DEFAULT_DELIVERY_TIME,
        deliveryFee: CONFIG.DEFAULT_DELIVERY_FEE,
        minOrder: 1e3,
        createdAt: /* @__PURE__ */ new Date()
      };
      await db2.insert(businesses3).values(newBusiness);
      if (address && (!latitude || !longitude)) {
        const cached = getCachedGeocode(`${address}, Soria, Espa\xF1a`);
        if (cached) {
          await db2.update(businesses3).set({ latitude: String(cached.lat), longitude: String(cached.lng) }).where((0, import_drizzle_orm14.eq)(businesses3.id, newBusiness.id));
          newBusiness.latitude = String(cached.lat);
          newBusiness.longitude = String(cached.lng);
        } else {
          try {
            const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
            const geo = await googleMapsService2.geocodeAddress(
              `${address}, Soria, Espa\xF1a`
            );
            if (geo) {
              setGeocodeCache(`${address}, Soria, Espa\xF1a`, geo.lat, geo.lng);
              await db2.update(businesses3).set({ latitude: String(geo.lat), longitude: String(geo.lng) }).where((0, import_drizzle_orm14.eq)(businesses3.id, newBusiness.id));
              newBusiness.latitude = String(geo.lat);
              newBusiness.longitude = String(geo.lng);
            }
          } catch {
          }
        }
      }
      if (!newBusiness.latitude || !newBusiness.longitude) {
        await db2.delete(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.id, newBusiness.id));
        return res.status(400).json({
          error: "No hemos podido ubicar el negocio en el mapa. Revisa la direcci\xF3n o elige el punto en el mapa para poder darlo de alta."
        });
      }
      res.json({ success: true, business: newBusiness });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router2.delete(
  "/:id",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, req.params.id),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      await db2.delete(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.id, req.params.id));
      res.json({ success: true, message: "Negocio eliminado" });
    } catch (error) {
      console.error("Delete business error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/:id/stats",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3, orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, req.params.id),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const businessOrders = await db2.select().from(orders2).where((0, import_drizzle_orm14.eq)(orders2.businessId, business.id));
      const today = /* @__PURE__ */ new Date();
      const todayOrders = businessOrders.filter(
        (o) => new Date(o.createdAt).toDateString() === today.toDateString()
      );
      const pendingOrders = businessOrders.filter(
        (o) => ["pending", "accepted", "preparing"].includes(o.status)
      );
      const todayRevenue = todayOrders.filter((o) => o.status === "delivered").reduce((sum3, o) => sum3 + (o.subtotal || 0), 0);
      res.json({
        success: true,
        pendingOrders: pendingOrders.length,
        todayOrders: todayOrders.length,
        // Céntimos exactos: el cliente formatea (evita redondeos tipo 59,80 -> 60)
        todayRevenue
      });
    } catch (error) {
      console.error("Business stats error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get("/:id", async (req, res) => {
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { sql: sql23 } = await import("drizzle-orm");
    console.log(`\u{1F4CD} GET /api/business/${req.params.id} called`);
    const [bizRows] = await db2.execute(sql23`
      SELECT * FROM businesses WHERE id = ${req.params.id} LIMIT 1
    `);
    const business = bizRows[0];
    if (!business) {
      console.log(`\u274C Business ${req.params.id} not found`);
      return res.status(404).json({ error: "Negocio no encontrado" });
    }
    if (!business.is_active) {
      console.log(`\u274C Business ${req.params.id} is not active`);
      return res.status(404).json({ error: "Negocio no encontrado" });
    }
    console.log(`\u2705 Found business: ${business.name}`);
    if ((!business.latitude || !business.longitude) && business.address) {
      try {
        const base = String(business.address);
        const query = base.toLowerCase().includes("soria") ? base : `${base}, Soria, Espa\xF1a`;
        const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
        const geo = await googleMapsService2.geocodeAddress(query);
        if (geo) {
          business.latitude = String(geo.lat);
          business.longitude = String(geo.lng);
          await db2.execute(
            sql23`UPDATE businesses SET latitude = ${String(geo.lat)}, longitude = ${String(geo.lng)} WHERE id = ${business.id}`
          );
          console.log(`\u{1F5FA}\uFE0F Negocio ${business.name} geocodificado`);
        }
      } catch (geoErr) {
        console.error("Geocodificaci\xF3n del negocio fall\xF3:", geoErr.message);
      }
    }
    let productRows = [];
    try {
      const [rows] = await db2.execute(sql23`
        SELECT * FROM products
        WHERE business_id = ${req.params.id}
          AND is_available = 1
      `);
      productRows = rows || [];
      console.log(`\u{1F4E6} Found ${productRows.length} products for ${business.name}`);
    } catch (prodErr) {
      console.error(`\u274C Error fetching products for ${business.name}:`, prodErr.message);
      productRows = [];
    }
    let calculatedIsOpen = business.is_open === 1 || business.is_open === true;
    let parsedOpeningHours = null;
    try {
      const { BusinessHoursService: BusinessHoursService2 } = await Promise.resolve().then(() => (init_businessHoursService(), businessHoursService_exports));
      calculatedIsOpen = await BusinessHoursService2.isBusinessOpen(business.id);
      if (business.opening_hours) {
        parsedOpeningHours = JSON.parse(business.opening_hours);
      }
    } catch (hoursErr) {
      console.error(`\u274C Error calculating hours for ${business.name}:`, hoursErr.message);
    }
    res.json({
      success: true,
      business: {
        ...business,
        products: productRows,
        isOpen: calculatedIsOpen,
        openingHours: parsedOpeningHours
      }
    });
  } catch (error) {
    console.error(`\u274C Error in GET /api/business/:id:`, error.message);
    res.status(500).json({ error: error.message });
  }
});
router2.put(
  "/:id",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
      const { isValidLatLng: isValidLatLng2 } = await Promise.resolve().then(() => (init_coordinates(), coordinates_exports));
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, req.params.id),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const allowed = [
        "name",
        "description",
        "type",
        "image",
        "address",
        "phone",
        "categories",
        "isOpen",
        "deliveryTime",
        "deliveryFee",
        "minOrder"
      ];
      const updates = {};
      for (const field of allowed) {
        if (req.body[field] !== void 0) {
          if (field === "image" && req.body[field] && req.body[field].startsWith("data:image/")) {
            if (business.image && business.image.includes("cloudinary")) {
              const oldPublicId = CloudinaryService2.extractPublicId(
                business.image
              );
              if (oldPublicId)
                await CloudinaryService2.deleteImage(oldPublicId).catch(
                  () => {
                  }
                );
            }
            updates.image = await CloudinaryService2.uploadImage(
              req.body[field],
              "businesses",
              `business-${req.params.id}`
            );
          } else {
            updates[field] = req.body[field];
          }
        }
      }
      let newLat = null;
      let newLng = null;
      if (req.body.latitude !== void 0 || req.body.longitude !== void 0) {
        const la = Number(req.body.latitude);
        const ln = Number(req.body.longitude);
        const valid = isValidLatLng2(la, ln);
        if (req.body.latitude !== null && req.body.longitude !== null && !valid) {
          return res.status(400).json({ error: "Coordenadas inv\xE1lidas" });
        }
        if (valid) {
          newLat = String(la);
          newLng = String(ln);
        }
      }
      await db2.update(businesses3).set(updates).where((0, import_drizzle_orm14.eq)(businesses3.id, req.params.id));
      if (newLat && newLng) {
        await db2.update(businesses3).set({ latitude: newLat, longitude: newLng }).where((0, import_drizzle_orm14.eq)(businesses3.id, req.params.id));
      }
      if (req.body.address && req.body.address !== business.address && !newLat && !newLng) {
        const cached = getCachedGeocode(`${req.body.address}, Soria, Espa\xF1a`);
        if (cached) {
          await db2.update(businesses3).set({ latitude: String(cached.lat), longitude: String(cached.lng) }).where((0, import_drizzle_orm14.eq)(businesses3.id, req.params.id));
        } else {
          try {
            const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
            const geo = await googleMapsService2.geocodeAddress(
              `${req.body.address}, Soria, Espa\xF1a`
            );
            if (geo) {
              setGeocodeCache(
                `${req.body.address}, Soria, Espa\xF1a`,
                geo.lat,
                geo.lng
              );
              await db2.update(businesses3).set({ latitude: String(geo.lat), longitude: String(geo.lng) }).where((0, import_drizzle_orm14.eq)(businesses3.id, req.params.id));
            }
          } catch {
          }
        }
      }
      res.json({ success: true, message: "Negocio actualizado" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router2.post(
  "/:id/geocode",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, req.params.id),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      if (!business.address)
        return res.status(400).json({ error: "El negocio no tiene direcci\xF3n" });
      const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
      const geo = await googleMapsService2.geocodeAddress(
        `${business.address}, Soria, Espa\xF1a`
      );
      if (!geo) {
        return res.status(400).json({ error: "No se pudo geocodificar la direcci\xF3n" });
      }
      const { lat, lng } = geo;
      await db2.update(businesses3).set({ latitude: String(lat), longitude: String(lng) }).where((0, import_drizzle_orm14.eq)(businesses3.id, req.params.id));
      res.json({ success: true, latitude: lat, longitude: lng });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router2.put(
  "/:id/toggle-status",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = await db2.select().from(businesses3).where(
        (0, import_drizzle_orm14.and)(
          (0, import_drizzle_orm14.eq)(businesses3.id, req.params.id),
          (0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)
        )
      ).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      const newStatus = !business.isOpen;
      await db2.update(businesses3).set({ isOpen: newStatus }).where((0, import_drizzle_orm14.eq)(businesses3.id, req.params.id));
      res.json({
        success: true,
        isOpen: newStatus,
        message: newStatus ? "Negocio abierto" : "Negocio cerrado"
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router2.post(
  "/stripe/connect",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { getStripe: getStripe5 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
      const stripe2 = getStripe5();
      const [business] = await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      if (business.stripeAccountId) {
        const baseUrl2 = process.env.EXPO_PUBLIC_BACKEND_URL || process.env.BACKEND_URL || `http://localhost:${process.env.PORT || 3e3}`;
        const accountLink2 = await stripe2.accountLinks.create({
          account: business.stripeAccountId,
          refresh_url: `${baseUrl2}/api/business/stripe/refresh`,
          return_url: `${baseUrl2}/api/business/stripe/return`,
          type: "account_onboarding"
        });
        return res.json({
          success: true,
          onboardingUrl: accountLink2.url,
          accountId: business.stripeAccountId
        });
      }
      const account = await stripe2.accounts.create({
        type: "express",
        country: "ES",
        email: req.user.email || void 0,
        capabilities: {
          card_payments: { requested: true },
          transfers: { requested: true }
        },
        business_type: "individual",
        business_profile: {
          name: business.name,
          product_description: business.description || "Negocio local"
        }
      });
      await db2.update(businesses3).set({ stripeAccountId: account.id }).where((0, import_drizzle_orm14.eq)(businesses3.id, business.id));
      const baseUrl = process.env.EXPO_PUBLIC_BACKEND_URL || process.env.BACKEND_URL || `http://localhost:${process.env.PORT || 3e3}`;
      const accountLink = await stripe2.accountLinks.create({
        account: account.id,
        refresh_url: `${baseUrl}/api/business/stripe/refresh`,
        return_url: `${baseUrl}/api/business/stripe/return`,
        type: "account_onboarding"
      });
      res.json({
        success: true,
        onboardingUrl: accountLink.url,
        accountId: account.id
      });
    } catch (error) {
      const rawMessage = error?.raw?.message || error?.message || "Error al conectar con Stripe";
      const friendly = String(rawMessage).split("\n")[0].trim();
      console.error("Stripe connect error:", friendly);
      res.status(502).json({
        error: friendly,
        connectNotActivated: /signed up for Connect/i.test(
          String(rawMessage)
        )
      });
    }
  }
);
router2.get("/stripe/return", (req, res) => {
  res.redirect("comeya://stripe-connect-complete");
});
router2.get("/stripe/refresh", async (req, res) => {
  try {
    const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { getStripe: getStripe5 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
    const stripe2 = getStripe5();
    const accountId = req.query.accountId || null;
    if (!accountId) return res.redirect("comeya://stripe-connect-refresh");
    const accountLink = await stripe2.accountLinks.create({
      account: accountId,
      refresh_url: `${process.env.EXPO_PUBLIC_BACKEND_URL || process.env.BACKEND_URL}/api/business/stripe/refresh?accountId=${accountId}`,
      return_url: `${process.env.EXPO_PUBLIC_BACKEND_URL || process.env.BACKEND_URL}/api/business/stripe/return`,
      type: "account_onboarding"
    });
    res.redirect(accountLink.url);
  } catch (error) {
    res.redirect("comeya://stripe-connect-refresh");
  }
});
router2.get(
  "/stripe/status",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { getStripe: getStripe5 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
      const stripe2 = getStripe5();
      const [business] = await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business || !business.stripeAccountId) {
        return res.json({
          success: true,
          connected: false,
          chargesEnabled: false,
          payoutsEnabled: false,
          detailsSubmitted: false
        });
      }
      const account = await stripe2.accounts.retrieve(business.stripeAccountId);
      const { mapStripeRequirements: mapStripeRequirements2 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
      res.json({
        success: true,
        connected: true,
        chargesEnabled: account.charges_enabled,
        payoutsEnabled: account.payouts_enabled,
        detailsSubmitted: account.details_submitted,
        accountId: account.id,
        requirements: mapStripeRequirements2(
          account.requirements?.currently_due || []
        )
      });
    } catch (error) {
      console.error("Stripe status error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.get(
  "/stripe/dashboard-link",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { getStripe: getStripe5 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
      const stripe2 = getStripe5();
      const [business] = await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business?.stripeAccountId) {
        return res.status(404).json({ error: "Cuenta de Stripe no conectada" });
      }
      const loginLink = await stripe2.accounts.createLoginLink(
        business.stripeAccountId
      );
      res.json({
        success: true,
        url: loginLink.url
      });
    } catch (error) {
      console.error("Stripe dashboard link error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router2.delete(
  "/stripe/disconnect",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [business] = await db2.select().from(businesses3).where((0, import_drizzle_orm14.eq)(businesses3.ownerId, req.user.id)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Negocio no encontrado" });
      await db2.update(businesses3).set({ stripeAccountId: null }).where((0, import_drizzle_orm14.eq)(businesses3.id, business.id));
      res.json({
        success: true,
        message: "Cuenta de Stripe desconectada"
      });
    } catch (error) {
      console.error("Stripe disconnect error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var business_default = router2;

// server/routes/orderRoutes.ts
var import_express3 = __toESM(require("express"));
init_orderNumberService();

// server/financialIntegrity.ts
init_db();
init_schema_mysql();
var import_drizzle_orm19 = require("drizzle-orm");
init_unifiedFinancialService();
var ROLE_WITHDRAWAL_LIMITS = {
  business_owner: {
    minAmount: 1e4,
    // $100 MXN
    maxDaily: 5e6,
    // $50,000 MXN
    maxPerTransaction: 5e6
  },
  delivery_driver: {
    minAmount: 5e3,
    // $50 MXN
    maxDaily: 1e6,
    // $10,000 MXN
    maxPerTransaction: 5e5
    // $5,000 MXN
  },
  customer: {
    minAmount: 0,
    maxDaily: 0,
    maxPerTransaction: 0
  }
};
var FinancialIntegrity = class {
  // Validar pedido completo
  static async validateOrder(order) {
    const subtotalTotal = order.subtotal + order.deliveryFee;
    const hasMarkup = order.productosBase !== void 0 && order.productosBase !== null && order.nemyCommission !== void 0 && order.nemyCommission !== null;
    const markupTotal = hasMarkup ? (order.productosBase || 0) + (order.nemyCommission || 0) + order.deliveryFee : subtotalTotal;
    if (order.total !== subtotalTotal && order.total !== markupTotal) {
      return {
        valid: false,
        error: "Total del pedido inv\xE1lido",
        details: {
          expected: hasMarkup ? markupTotal : subtotalTotal,
          received: order.total,
          subtotal: order.subtotal,
          productosBase: order.productosBase,
          nemyCommission: order.nemyCommission,
          deliveryFee: order.deliveryFee
        }
      };
    }
    if (order.platformFee !== void 0 && order.businessEarnings !== void 0 && order.deliveryEarnings !== void 0) {
      const commissionTotal = order.platformFee + order.businessEarnings + order.deliveryEarnings;
      if (commissionTotal !== order.total) {
        return {
          valid: false,
          error: "Comisiones no suman el total del pedido",
          details: {
            platform: order.platformFee,
            business: order.businessEarnings,
            driver: order.deliveryEarnings,
            commissionTotal,
            orderTotal: order.total,
            difference: order.total - commissionTotal
          }
        };
      }
      const expectedCommissions = await financialService.calculateCommissions(
        order.total,
        order.deliveryFee || 0,
        order.productosBase || void 0,
        order.nemyCommission || void 0
      );
      if (order.platformFee !== expectedCommissions.platform || order.businessEarnings !== expectedCommissions.business || order.deliveryEarnings !== expectedCommissions.driver) {
        return {
          valid: false,
          error: "Comisiones no coinciden con rates del sistema",
          details: {
            expected: expectedCommissions,
            received: {
              platform: order.platformFee,
              business: order.businessEarnings,
              driver: order.deliveryEarnings
            }
          }
        };
      }
    }
    return { valid: true };
  }
  // Validar transacción de wallet
  static async validateWalletTransaction(userId, amount, type) {
    const [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm19.eq)(wallets.userId, userId)).limit(1);
    if (!wallet) {
      return { valid: false, error: "Wallet no existe para este usuario" };
    }
    if (amount < 0) {
      const newBalance = wallet.balance + amount;
      if (newBalance < 0) {
        return {
          valid: false,
          error: "Balance insuficiente",
          details: {
            currentBalance: wallet.balance,
            requestedAmount: Math.abs(amount),
            shortfall: Math.abs(newBalance)
          }
        };
      }
    }
    if (type === "withdrawal") {
      const [user] = await db.select({ role: users.role }).from(users).where((0, import_drizzle_orm19.eq)(users.id, userId)).limit(1);
      if (!user) {
        return { valid: false, error: "Usuario no encontrado" };
      }
      const limits = ROLE_WITHDRAWAL_LIMITS[user.role] || ROLE_WITHDRAWAL_LIMITS.customer;
      const withdrawalAmount = Math.abs(amount);
      if (withdrawalAmount < limits.minAmount) {
        return {
          valid: false,
          error: `Monto m\xEDnimo de retiro: $${(limits.minAmount / 100).toFixed(2)} MXN`,
          details: { minAmount: limits.minAmount, requested: withdrawalAmount }
        };
      }
      if (withdrawalAmount > limits.maxPerTransaction) {
        return {
          valid: false,
          error: `Monto m\xE1ximo por transacci\xF3n: $${(limits.maxPerTransaction / 100).toFixed(2)} MXN`,
          details: {
            maxAmount: limits.maxPerTransaction,
            requested: withdrawalAmount
          }
        };
      }
    }
    return { valid: true };
  }
  // Validar que comisiones del sistema sumen 100%
  static async validateSystemCommissionRates() {
    try {
      const rates = await financialService.getCommissionRates();
      const total = rates.platform + rates.business + rates.driver;
      if (Math.abs(total - 1) > 1e-3) {
        return {
          valid: false,
          error: "Comisiones del sistema no suman 100%",
          details: {
            platform: `${(rates.platform * 100).toFixed(2)}%`,
            business: `${(rates.business * 100).toFixed(2)}%`,
            driver: `${(rates.driver * 100).toFixed(2)}%`,
            total: `${(total * 100).toFixed(2)}%`
          }
        };
      }
      return { valid: true, details: rates };
    } catch (error) {
      return { valid: false, error: error.message };
    }
  }
  // Reconciliar pedido - verificar integridad financiera
  static async reconcileOrder(orderId) {
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm19.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      return { valid: false, error: "Pedido no encontrado" };
    }
    const orderForValidation = order.status === "delivered" ? order : {
      ...order,
      platformFee: void 0,
      businessEarnings: void 0,
      deliveryEarnings: void 0
    };
    const orderValidation = await this.validateOrder(orderForValidation);
    if (!orderValidation.valid) {
      return orderValidation;
    }
    if (order.status === "delivered") {
      if (!order.platformFee || !order.businessEarnings || !order.deliveryEarnings) {
        return {
          valid: false,
          error: "Pedido entregado sin comisiones calculadas",
          details: { orderId, status: order.status }
        };
      }
    }
    return { valid: true };
  }
  // Obtener límites de retiro para un usuario
  static async getWithdrawalLimits(userId) {
    const [user] = await db.select({ role: users.role }).from(users).where((0, import_drizzle_orm19.eq)(users.id, userId)).limit(1);
    if (!user) {
      return ROLE_WITHDRAWAL_LIMITS.customer;
    }
    return ROLE_WITHDRAWAL_LIMITS[user.role] || ROLE_WITHDRAWAL_LIMITS.customer;
  }
};

// server/financialMiddleware.ts
init_unifiedFinancialService();
async function validateOrderFinancials(req, res, next) {
  try {
    const {
      subtotal,
      deliveryFee,
      total,
      productosBase,
      nemyCommission,
      couponDiscount
    } = req.body;
    if (subtotal === void 0 || deliveryFee === void 0 || total === void 0) {
      return res.status(400).json({
        error: "Campos financieros requeridos: subtotal, deliveryFee, total"
      });
    }
    if (subtotal < 0 || deliveryFee < 0 || total < 0) {
      return res.status(400).json({
        error: "Los montos deben ser positivos"
      });
    }
    const baseSubtotal = productosBase ?? subtotal;
    const platformCommission = typeof nemyCommission === "number" && nemyCommission > 0 ? nemyCommission : Math.round(baseSubtotal * 0.15);
    const discount = couponDiscount || 0;
    const calculatedTotal = baseSubtotal + platformCommission + deliveryFee - discount;
    if (Math.abs(calculatedTotal - total) > 1) {
      return res.status(400).json({
        error: "Total inv\xE1lido",
        expected: calculatedTotal,
        received: total,
        breakdown: { baseSubtotal, platformCommission, deliveryFee, discount }
      });
    }
    next();
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
}
async function validateOrderCompletion(req, res, next) {
  try {
    const paramId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
    const paramOrderId = Array.isArray(req.params.orderId) ? req.params.orderId[0] : req.params.orderId;
    const orderId = paramId || paramOrderId;
    const validation = await FinancialIntegrity.reconcileOrder(orderId);
    if (!validation.valid) {
      if (validation.error === "Comisiones no suman el total del pedido" || validation.error === "Pedido entregado sin comisiones calculadas") {
        return next();
      }
      return res.status(400).json({
        error: "El pedido tiene problemas de integridad financiera",
        details: validation.error
      });
    }
    next();
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
}

// server/validateOwnership.ts
init_db();
init_schema_mysql();
var import_drizzle_orm20 = require("drizzle-orm");
async function validateDriverOrderOwnership(req, res, next) {
  try {
    const orderId = req.params.id || req.params.orderId;
    const userId = req.user.id;
    if (req.user.role === "admin" || req.user.role === "super_admin") {
      return next();
    }
    const [order] = await db.select({ deliveryPersonId: orders.deliveryPersonId }).from(orders).where((0, import_drizzle_orm20.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Order not found" });
    }
    if (order.deliveryPersonId !== userId) {
      return res.status(403).json({
        error: "This order is not assigned to you"
      });
    }
    next();
  } catch (error) {
    console.error("Error validating driver order ownership:", error);
    res.status(500).json({ error: "Internal server error" });
  }
}
async function validateCustomerOrderOwnership(req, res, next) {
  try {
    const orderId = req.params.id || req.params.orderId;
    const userId = req.user.id;
    const role = req.user.role;
    if (role === "admin" || role === "super_admin") return next();
    const [order] = await db.select({
      userId: orders.userId,
      deliveryPersonId: orders.deliveryPersonId
    }).from(orders).where((0, import_drizzle_orm20.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Order not found" });
    }
    if (order.userId !== userId && role !== "business_owner") {
      return res.status(403).json({
        error: "Solo el cliente puede realizar esta acci\xF3n"
      });
    }
    next();
  } catch (error) {
    console.error("Error validating customer order ownership:", error);
    res.status(500).json({ error: "Internal server error" });
  }
}

// server/routes/orderRoutes.ts
init_distance();
init_enhancedPushService();
init_loyaltyService();
var router3 = import_express3.default.Router();
router3.post("/calculate-delivery", authenticateToken, async (req, res) => {
  try {
    const { businessLat, businessLng, deliveryLat, deliveryLng } = req.body;
    if (!businessLat || !businessLng || !deliveryLat || !deliveryLng) {
      return res.status(400).json({ error: "Missing coordinates" });
    }
    const distance = calculateDistance(
      businessLat,
      businessLng,
      deliveryLat,
      deliveryLng
    );
    const deliveryFee = await calculateDeliveryFee(distance);
    const estimatedTime = estimateDeliveryTime(distance);
    res.json({
      success: true,
      distance: Math.round(distance * 100) / 100,
      deliveryFee,
      estimatedTime
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router3.post(
  "/",
  authenticateToken,
  validateOrderFinancials,
  async (req, res) => {
    try {
      const { orders: orders2, businesses: businesses3, addresses: addresses2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, desc: desc19 } = await import("drizzle-orm");
      let deliveryFee = req.body.deliveryFee;
      let estimatedDeliveryTime = req.body.estimatedDeliveryTime;
      if (req.body.deliveryAddressId && req.body.businessId) {
        const [business] = await db2.select().from(businesses3).where(eq79(businesses3.id, req.body.businessId)).limit(1);
        const [address] = await db2.select().from(addresses2).where(eq79(addresses2.id, req.body.deliveryAddressId)).limit(1);
        if (business && address && business.latitude && business.longitude && address.latitude && address.longitude) {
          const distance = calculateDistance(
            business.latitude,
            business.longitude,
            address.latitude,
            address.longitude
          );
          deliveryFee = await calculateDeliveryFee(distance);
          estimatedDeliveryTime = estimateDeliveryTime(
            distance,
            business.prepTime || 20
          );
        } else {
          deliveryFee = Math.max(deliveryFee || 0, 250);
        }
      }
      let deliveryLatitude = null;
      let deliveryLongitude = null;
      const rawLat = Number(req.body.deliveryLatitude ?? req.body.deliveryLat);
      const rawLng = Number(
        req.body.deliveryLongitude ?? req.body.deliveryLng
      );
      if (Number.isFinite(rawLat) && Number.isFinite(rawLng) && rawLat !== 0 && rawLng !== 0 && Math.abs(rawLat) <= 90 && Math.abs(rawLng) <= 180) {
        deliveryLatitude = String(rawLat);
        deliveryLongitude = String(rawLng);
      }
      if (!deliveryLatitude || !deliveryLongitude) {
        if (req.body.deliveryAddressId) {
          try {
            const [addr] = await db2.select().from(addresses2).where(eq79(addresses2.id, req.body.deliveryAddressId)).limit(1);
            if (addr?.latitude && addr?.longitude) {
              deliveryLatitude = String(addr.latitude);
              deliveryLongitude = String(addr.longitude);
            }
          } catch (addrErr) {
            console.error("Error reading address coordinates:", addrErr);
          }
        }
      }
      if ((!deliveryLatitude || !deliveryLongitude) && req.body.orderType !== "pickup") {
        const addressText = typeof req.body.deliveryAddress === "string" ? req.body.deliveryAddress : req.body.deliveryAddress?.street || req.body.deliveryAddress?.fullAddress || null;
        if (addressText) {
          try {
            const { geocodeAddress: geocodeAddress4 } = await Promise.resolve().then(() => (init_geocodeService(), geocodeService_exports));
            const geo = await geocodeAddress4(addressText);
            if (geo?.lat && geo?.lng) {
              deliveryLatitude = String(geo.lat);
              deliveryLongitude = String(geo.lng);
            }
          } catch (geoErr) {
            console.error("Error geocoding delivery address:", geoErr);
          }
        }
      }
      const subtotal = req.body.subtotal;
      const couponDiscount = req.body.couponDiscount || 0;
      const { SubscriptionService: SubscriptionService2 } = await Promise.resolve().then(() => (init_subscriptionService(), subscriptionService_exports));
      const rawDeliveryFee = req.body.orderType === "pickup" ? 0 : deliveryFee || 0;
      const subBenefits = await SubscriptionService2.applySubscriptionBenefits(
        req.user.id,
        req.body.subtotal || 0,
        rawDeliveryFee
      );
      const subDiscount = subBenefits.discount;
      const subDeliveryFee = subBenefits.deliveryFee;
      const finalDeliveryFee = req.body.orderType === "pickup" ? 0 : subDeliveryFee;
      let productosBase = req.body.productosBase || null;
      let nemyCommission = req.body.nemyCommission || null;
      try {
        const { SubscriptionService: SubscriptionService3 } = await Promise.resolve().then(() => (init_subscriptionService(), subscriptionService_exports));
        const rate = await SubscriptionService3.getBusinessCommissionRate(
          req.body.businessId
        );
        if (rate !== 0.15 && subtotal > 0) {
          productosBase = Math.round(subtotal / (1 + rate));
          nemyCommission = subtotal - productosBase;
        }
      } catch (err) {
        console.error("Error applying business commission rate:", err);
      }
      const calculatedTotal = Math.max(
        0,
        subtotal + finalDeliveryFee - couponDiscount - subDiscount
      );
      const orderData = {
        userId: req.user.id,
        businessId: req.body.businessId,
        businessName: req.body.businessName,
        businessImage: req.body.businessImage,
        items: req.body.items,
        status: req.body.status || "pending",
        subtotal,
        productosBase,
        nemyCommission,
        deliveryFee: finalDeliveryFee,
        total: calculatedTotal,
        paymentMethod: req.body.paymentMethod,
        orderType: req.body.orderType === "pickup" ? "pickup" : "delivery",
        deliveryAddress: req.body.deliveryAddress,
        deliveryLatitude,
        deliveryLongitude,
        notes: req.body.notes,
        substitutionPreference: req.body.substitutionPreference,
        itemSubstitutionPreferences: req.body.itemSubstitutionPreferences,
        substituteProductIds: req.body.substituteProductIds || null,
        cashPaymentAmount: req.body.cashPaymentAmount,
        cashChangeAmount: req.body.cashChangeAmount,
        estimatedDeliveryTime
      };
      const { nextOrderNumber: nextOrderNumber2, formatOrderNumber: formatOrderNumber2 } = await Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports));
      const seqNumber = await nextOrderNumber2();
      if (seqNumber != null) {
        orderData.orderNumber = seqNumber;
      }
      await db2.insert(orders2).values(orderData);
      const createdOrder = await db2.select().from(orders2).where(eq79(orders2.userId, req.user.id)).orderBy(desc19(orders2.createdAt)).limit(1);
      const orderId = createdOrder[0].id;
      if (createdOrder[0].orderType === "pickup") {
        try {
          const { pickupService: pickupService2 } = await Promise.resolve().then(() => (init_pickupService(), pickupService_exports));
          await pickupService2.createPickupOrder(orderId, 15);
        } catch (err) {
          console.error("Error generating pickup codes:", err);
        }
      }
      res.json({
        success: true,
        id: orderId,
        orderId,
        order: { id: orderId },
        orderNumber: seqNumber,
        orderNumberFormatted: formatOrderNumber2(seqNumber),
        deliveryFee,
        estimatedDeliveryTime
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router3.get("/", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, reviews: reviews2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, inArray: inArray12, isNull: isNull9, and: and47 } = await import("drizzle-orm");
    let userOrders;
    if (req.query.status === "active") {
      userOrders = await db2.select().from(orders2).where(
        // Pedidos ocultados por el admin: fuera de la lista del cliente
        and47(eq79(orders2.userId, req.user.id), isNull9(orders2.deletedAt))
      );
      userOrders = userOrders.filter(
        (o) => ["pending", "accepted", "preparing", "ready", "on_the_way"].includes(
          o.status
        )
      );
    } else {
      userOrders = await db2.select().from(orders2).where(
        and47(eq79(orders2.userId, req.user.id), isNull9(orders2.deletedAt))
      );
    }
    const reviewedOrderIds = /* @__PURE__ */ new Set();
    if (userOrders.length > 0) {
      const revs = await db2.select({ orderId: reviews2.orderId }).from(reviews2).where(
        inArray12(
          reviews2.orderId,
          userOrders.map((o) => o.id)
        )
      );
      for (const r of revs) reviewedOrderIds.add(r.orderId);
    }
    const ordersWithFlag = userOrders.map((o) => ({
      ...o,
      hasReview: reviewedOrderIds.has(o.id)
    }));
    res.json({ success: true, orders: ordersWithFlag });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router3.get("/:id", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, users: users5, deliveryDrivers: deliveryDrivers3, businesses: businesses3, reviews: reviews2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, sql: sql23 } = await import("drizzle-orm");
    const orderId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Order not found" });
    }
    const { id: userId, role } = req.user;
    const isCustomer = order.userId === userId;
    const isDriver = order.deliveryPersonId === userId;
    const isAdmin = role === "admin" || role === "super_admin";
    if (!isCustomer && !isDriver && !isAdmin) {
      return res.status(403).json({ error: "No autorizado" });
    }
    let driverInfo = null;
    if (order.deliveryPersonId) {
      try {
        const [driverData] = await db2.select({
          id: users5.id,
          name: users5.name,
          phone: users5.phone,
          profileImage: users5.profileImage,
          vehicleType: deliveryDrivers3.vehicleType,
          vehiclePlate: deliveryDrivers3.vehiclePlate,
          vehicleBrand: deliveryDrivers3.vehicleBrand,
          vehicleModel: deliveryDrivers3.vehicleModel,
          vehicleColor: deliveryDrivers3.vehicleColor,
          vehiclePhoto: deliveryDrivers3.vehiclePhoto
        }).from(users5).leftJoin(deliveryDrivers3, eq79(users5.id, deliveryDrivers3.userId)).where(eq79(users5.id, order.deliveryPersonId)).limit(1);
        if (driverData) {
          driverInfo = {
            id: driverData.id,
            name: driverData.name,
            phone: driverData.phone,
            profilePhoto: driverData.profileImage,
            vehicleType: driverData.vehicleType,
            vehiclePlate: driverData.vehiclePlate,
            vehicleBrand: driverData.vehicleBrand,
            vehicleModel: driverData.vehicleModel,
            vehicleColor: driverData.vehicleColor,
            vehiclePhoto: driverData.vehiclePhoto
          };
        }
      } catch (err) {
        console.error("Error fetching driver info:", err);
      }
    }
    let businessInfo = null;
    if (order.businessId) {
      try {
        const [bizData] = await db2.select({
          type: businesses3.type,
          categories: businesses3.categories
        }).from(businesses3).where(eq79(businesses3.id, order.businessId)).limit(1);
        if (bizData) businessInfo = bizData;
      } catch {
      }
    }
    let hasReview = false;
    try {
      const [existingReview] = await db2.select({ id: reviews2.id }).from(reviews2).where(eq79(reviews2.orderId, orderId)).limit(1);
      hasReview = !!existingReview;
    } catch {
    }
    let substitutions2 = [];
    try {
      const { listSubstitutions: listSubstitutions2 } = await Promise.resolve().then(() => (init_substitutionService(), substitutionService_exports));
      substitutions2 = await listSubstitutions2(orderId);
    } catch {
    }
    res.json({
      success: true,
      order: {
        ...order,
        driverInfo,
        businessType: businessInfo?.type ?? null,
        businessCategories: businessInfo?.categories ?? null,
        hasReview,
        substitutions: substitutions2
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router3.post("/:id/substitutions/:subId/approve", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, req.params.id)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    const { decideSubstitution: decideSubstitution2 } = await Promise.resolve().then(() => (init_substitutionService(), substitutionService_exports));
    const result = await decideSubstitution2(
      order,
      req.params.subId,
      true,
      req.user.id
    );
    if (!result.success) {
      return res.status(400).json({ error: result.message });
    }
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router3.post("/:id/substitutions/:subId/reject", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, req.params.id)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    const { decideSubstitution: decideSubstitution2 } = await Promise.resolve().then(() => (init_substitutionService(), substitutionService_exports));
    const result = await decideSubstitution2(
      order,
      req.params.subId,
      false,
      req.user.id
    );
    if (!result.success) {
      return res.status(400).json({ error: result.message });
    }
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router3.post("/:id/substitutions/:subId/confirm-payment", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, req.params.id)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    const { confirmDeltaPayment: confirmDeltaPayment2 } = await Promise.resolve().then(() => (init_substitutionService(), substitutionService_exports));
    const result = await confirmDeltaPayment2(
      order,
      req.params.subId,
      String(req.body?.paymentIntentId || ""),
      req.user.id
    );
    if (!result.success) {
      return res.status(400).json({ error: result.message });
    }
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router3.post(
  "/:id/assign-driver",
  authenticateToken,
  async (req, res) => {
    try {
      if (req.user.role !== "admin" && req.user.role !== "super_admin") {
        return res.status(403).json({
          error: "Solo un administrador puede asignar repartidores"
        });
      }
      const { orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, and: and47 } = await import("drizzle-orm");
      const orderId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (!order) {
        return res.status(404).json({ error: "Pedido no encontrado" });
      }
      if (order.deliveryPersonId) {
        return res.status(400).json({ error: "El pedido ya tiene repartidor asignado" });
      }
      const availableDrivers = await db2.select().from(users5).where(
        and47(eq79(users5.role, "delivery_driver"), eq79(users5.isActive, true))
      ).limit(10);
      if (availableDrivers.length === 0) {
        return res.json({
          success: false,
          message: "No hay repartidores disponibles"
        });
      }
      const driver = availableDrivers[0];
      await db2.update(orders2).set({
        deliveryPersonId: driver.id,
        assignedAt: /* @__PURE__ */ new Date()
      }).where(eq79(orders2.id, orderId));
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      await sendPushToUser2(driver.id, {
        title: "\u{1F4E6} Pedido asignado",
        body: `Se te asign\xF3 el pedido ${orderRef(order)}`,
        data: { orderId, screen: "DriverActiveOrder" }
      });
      res.json({
        success: true,
        driver: {
          id: driver.id,
          name: driver.name,
          phone: driver.phone
        }
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router3.post(
  "/:id/cancel-regret",
  authenticateToken,
  validateCustomerOrderOwnership,
  async (req, res) => {
    try {
      const { cancelOrder: cancelOrder2 } = await Promise.resolve().then(() => (init_orderCancellationService(), orderCancellationService_exports));
      const orderId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
      const result = await cancelOrder2(
        orderId,
        req.user.id,
        req.body?.reason || "Cancelado por el cliente en el periodo de arrepentimiento",
        { actorRole: "customer" }
      );
      if (!result.success) {
        return res.status(result.error === "not_found" ? 404 : 400).json({
          error: result.message
        });
      }
      res.json({
        success: true,
        message: "Pedido cancelado",
        refund: result.refund,
        policy: result.policy
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router3.post(
  "/:id/confirm",
  authenticateToken,
  validateCustomerOrderOwnership,
  async (req, res) => {
    try {
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const orderId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (!order) {
        return res.status(404).json({ error: "Order not found" });
      }
      await db2.update(orders2).set({
        regretPeriodConfirmed: true,
        regretPeriodConfirmedAt: /* @__PURE__ */ new Date(),
        confirmedToBusinessAt: /* @__PURE__ */ new Date()
      }).where(eq79(orders2.id, orderId));
      try {
        const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
        const [biz] = await db2.select({ ownerId: businesses3.ownerId }).from(businesses3).where(eq79(businesses3.id, order.businessId)).limit(1);
        if (biz?.ownerId) {
          await sendPushToUser(biz.ownerId, {
            title: "\u{1F514} Pedido confirmado",
            body: `El pedido ${orderRef(order)} fue confirmado por el cliente.`,
            data: { orderId, screen: "BusinessOrders" }
          });
        }
        const { notifyNewOrder: notifyNewOrder2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
        notifyNewOrder2(order.businessId, order);
      } catch (notifyError) {
        console.error("Error notifying business:", notifyError);
      }
      res.json({ success: true, message: "Pedido confirmado" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router3.post(
  "/:id/complete-delivery",
  authenticateToken,
  requireRole("delivery_driver"),
  requireApprovedDriver,
  validateDriverOrderOwnership,
  async (req, res) => {
    try {
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { calculateDistance: calculateDistance4 } = await Promise.resolve().then(() => (init_distance(), distance_exports));
      const orderId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (!order) {
        return res.status(404).json({ error: "Order not found" });
      }
      if (!["ready", "picked_up", "on_the_way", "in_transit", "arriving"].includes(
        order.status
      )) {
        return res.status(400).json({
          error: "El pedido debe estar recogido y en camino para marcarlo como entregado"
        });
      }
      const driverLat = req.body.latitude ?? req.body.lat;
      const driverLng = req.body.longitude ?? req.body.lng;
      const hasDriverCoords = typeof driverLat === "number" && typeof driverLng === "number";
      const confirmWithoutGps = req.body.confirmWithoutGps === true;
      if (process.env.NODE_ENV === "production") {
        if (!hasDriverCoords) {
          if (!confirmWithoutGps) {
            return res.status(400).json({ error: "Ubicaci\xF3n requerida para marcar entregado" });
          }
          console.warn(
            `[complete-delivery] Pedido ${orderId} marcado entregado sin GPS (confirmaci\xF3n expl\xEDcita del repartidor)`
          );
        }
        const deliveryLatRaw = order.deliveryLatitude ?? order.deliveryLat ?? order.latitude;
        const deliveryLngRaw = order.deliveryLongitude ?? order.deliveryLng ?? order.longitude;
        const toNum = (v) => v === null || v === void 0 || v === "" ? NaN : Number(v);
        const deliveryLatNum = toNum(deliveryLatRaw);
        const deliveryLngNum = toNum(deliveryLngRaw);
        const hasDeliveryCoords = !Number.isNaN(deliveryLatNum) && !Number.isNaN(deliveryLngNum);
        if (hasDriverCoords && hasDeliveryCoords) {
          const distanceKm = calculateDistance4(
            Number(driverLat),
            Number(driverLng),
            deliveryLatNum,
            deliveryLngNum
          );
          const maxDistanceMeters = 200;
          if (distanceKm * 1e3 > maxDistanceMeters) {
            return res.status(400).json({
              error: "Debes estar m\xE1s cerca del cliente para marcar entregado",
              distanceMeters: Math.round(distanceKm * 1e3),
              maxDistanceMeters
            });
          }
        }
      }
      await db2.update(orders2).set({
        status: "delivered",
        deliveredAt: /* @__PURE__ */ new Date(),
        driverArrivedAt: /* @__PURE__ */ new Date()
      }).where(eq79(orders2.id, orderId));
      if (req.body.deliveryPhoto) {
        try {
          const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
          const { deliveryProofs: deliveryProofs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          const photoUrl = await CloudinaryService2.uploadImage(
            req.body.deliveryPhoto,
            "delivery-proofs",
            `proof-${orderId}-${Date.now()}`
          );
          await db2.insert(deliveryProofs2).values({
            id: crypto.randomUUID(),
            orderId,
            driverId: req.user.id,
            photoUrl,
            // Nunca "0,0": una coordenada basura rompe el mapa de pruebas de
            // entrega. Si el repartidor no envió GPS, se guarda null.
            latitude: driverLat ? String(driverLat) : null,
            longitude: driverLng ? String(driverLng) : null,
            timestamp: /* @__PURE__ */ new Date()
          });
          await db2.update(orders2).set({ deliveryProofPhoto: photoUrl }).where(eq79(orders2.id, orderId));
        } catch (photoErr) {
          console.error("Error uploading delivery photo:", photoErr);
        }
      }
      await sendOrderStatusNotification(orderId, order.userId, "delivered");
      res.json({
        success: true,
        message: "Pedido marcado como entregado. Esperando confirmaci\xF3n del cliente."
      });
    } catch (error) {
      console.error("Complete delivery error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router3.post(
  "/:id/confirm-receipt",
  authenticateToken,
  validateCustomerOrderOwnership,
  validateOrderCompletion,
  async (req, res) => {
    try {
      if (req.user.role !== "customer" && req.user.role !== "admin" && req.user.role !== "super_admin") {
        return res.status(403).json({
          error: "Solo el cliente puede confirmar la recepci\xF3n del pedido"
        });
      }
      const { orders: orders2, businesses: businesses3, payouts: payouts2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const orderId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (!order) return res.status(404).json({ error: "Order not found" });
      if (order.status !== "delivered")
        return res.status(400).json({ error: "El pedido debe estar entregado primero" });
      if (order.confirmedByCustomer)
        return res.status(400).json({ error: "Ya confirmaste este pedido" });
      try {
        await LoyaltyService.awardPointsForOrder(
          order.userId,
          order.id,
          order.total
        );
      } catch (e) {
        console.error("Error awarding loyalty points:", e);
      }
      const productosBase = order.productosBase && order.productosBase > 0 ? order.productosBase : Math.round((order.subtotal || order.total) / 1.15);
      const businessAmount = productosBase;
      const driverAmount = order.deliveryFee || 0;
      const platformAmount = order.nemyCommission || Math.round(productosBase * 0.15);
      await db2.update(orders2).set({
        confirmedByCustomer: true,
        confirmedByCustomerAt: /* @__PURE__ */ new Date(),
        fundsReleased: true,
        fundsReleasedAt: /* @__PURE__ */ new Date(),
        platformFee: platformAmount,
        businessEarnings: businessAmount,
        deliveryEarnings: driverAmount
      }).where(eq79(orders2.id, orderId));
      const [business] = await db2.select({ ownerId: businesses3.ownerId }).from(businesses3).where(eq79(businesses3.id, order.businessId)).limit(1);
      const businessOwnerId = business?.ownerId || order.businessId;
      const payoutInserts = [];
      if (businessAmount > 0) {
        payoutInserts.push({
          orderId: order.id,
          recipientId: businessOwnerId,
          recipientType: "business",
          amount: businessAmount,
          status: "pending"
        });
      }
      if (order.deliveryPersonId && driverAmount > 0) {
        payoutInserts.push({
          orderId: order.id,
          recipientId: order.deliveryPersonId,
          recipientType: "driver",
          amount: driverAmount,
          status: "pending"
        });
      }
      if (payoutInserts.length > 0) {
        await db2.insert(payouts2).values(payoutInserts);
      }
      res.json({
        success: true,
        message: "Pedido confirmado. Payouts creados para negocio y repartidor."
      });
      if (businessOwnerId) {
        await sendPushToUser(businessOwnerId, {
          title: "\u{1F4B0} Pago liberado",
          body: `El cliente confirm\xF3 la entrega del pedido ${orderRef(order)}`,
          data: { orderId: order.id, screen: "BusinessEarnings" }
        });
      }
      if (order.deliveryPersonId) {
        await sendPushToUser(order.deliveryPersonId, {
          title: "\u{1F4B0} Pago liberado",
          body: `Pedido ${orderRef(order)} confirmado. Tu pago est\xE1 disponible.`,
          data: { orderId: order.id, screen: "DriverEarnings" }
        });
      }
    } catch (error) {
      console.error("Confirm receipt error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router3.post(
  "/:id/pickup",
  authenticateToken,
  requireRole("delivery_driver"),
  requireApprovedDriver,
  validateDriverOrderOwnership,
  async (req, res) => {
    try {
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const orderId = Array.isArray(req.params.id) ? req.params.id[0] : req.params.id;
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (!order) {
        return res.status(404).json({ error: "Pedido no encontrado" });
      }
      if (!["ready", "picked_up", "preparing"].includes(order.status)) {
        return res.status(400).json({
          error: `No puedes recoger este pedido. Estado actual: ${order.status}`
        });
      }
      if (order.deliveryPersonId !== req.user.id) {
        return res.status(403).json({ error: "Este pedido no est\xE1 asignado a ti" });
      }
      await db2.update(orders2).set({
        status: "picked_up",
        driverPickedUpAt: /* @__PURE__ */ new Date()
      }).where(eq79(orders2.id, orderId));
      await sendOrderStatusNotification(orderId, order.userId, "picked_up");
      try {
        const { notifyOrderStatusChange: notifyOrderStatusChange2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
        notifyOrderStatusChange2(order.userId, orderId, "picked_up");
      } catch {
      }
      res.json({
        success: true,
        message: "Pedido recogido. Inicia la entrega cuando salgas del local.",
        order: {
          id: order.id,
          status: "picked_up",
          driverPickedUpAt: /* @__PURE__ */ new Date()
        }
      });
    } catch (error) {
      console.error("Pickup order error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var orderRoutes_default = router3;

// server/routes/users.ts
var import_express4 = __toESM(require("express"));
var import_drizzle_orm25 = require("drizzle-orm");
init_db();
var router4 = import_express4.default.Router();
router4.get("/me/issues", authenticateToken, async (req, res) => {
  try {
    const { orderIssues: orderIssues2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { desc: desc19 } = await import("drizzle-orm");
    const issues = await db.select().from(orderIssues2).where((0, import_drizzle_orm25.eq)(orderIssues2.reportedBy, String(req.user.id))).orderBy(desc19(orderIssues2.createdAt));
    res.json({
      success: true,
      issues: issues.map((i) => ({
        ...i,
        photos: i.photos ? JSON.parse(i.photos) : [],
        internalNote: void 0
        // nota interna solo para admins
      }))
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.get("/:id/issues", authenticateToken, async (req, res) => {
  try {
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    if (!isAdmin && req.params.id !== req.user.id) {
      return res.status(403).json({ error: "No autorizado" });
    }
    const { orderIssues: orderIssues2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const issues = await db.select().from(orderIssues2).where((0, import_drizzle_orm25.eq)(orderIssues2.reportedBy, String(req.params.id))).orderBy((0, import_drizzle_orm25.desc)(orderIssues2.createdAt));
    res.json({
      success: true,
      issues: issues.map((i) => ({
        ...i,
        photos: i.photos ? JSON.parse(i.photos) : [],
        internalNote: isAdmin ? i.internalNote : void 0
      }))
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.put("/push-token", authenticateToken, async (req, res) => {
  try {
    const { token } = req.body;
    if (!token || !token.startsWith("ExponentPushToken[")) {
      return res.status(400).json({ error: "Token inv\xE1lido" });
    }
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    await db2.update(users5).set({ pushToken: token, updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.get("/notification-preferences", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const [user] = await db.select({ notificationPreferences: users5.notificationPreferences }).from(users5).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id)).limit(1);
    if (!user)
      return res.status(404).json({ error: "Usuario no encontrado" });
    let prefs = { promotions: true, news: true };
    if (user.notificationPreferences) {
      try {
        const parsed = JSON.parse(user.notificationPreferences);
        prefs = {
          promotions: typeof parsed.promotions === "boolean" ? parsed.promotions : true,
          news: typeof parsed.news === "boolean" ? parsed.news : true
        };
      } catch {
      }
    }
    res.json({ success: true, preferences: prefs });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.put("/notification-preferences", authenticateToken, async (req, res) => {
  try {
    const { promotions, news } = req.body || {};
    const prefs = {
      promotions: promotions !== false,
      news: news !== false
    };
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    await db.update(users5).set({ notificationPreferences: JSON.stringify(prefs) }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    res.json({ success: true, preferences: prefs });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.get("/profile/full", authenticateToken, async (req, res) => {
  try {
    const { users: users5, deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    let vehicleType = null, vehiclePlate = null, vehiclePhoto = null;
    let vehicleBrand = null, vehicleModel = null, vehicleColor = null, vehicleYear = null;
    let vehiclePlatePhoto = null, vehicleItvPhoto = null, vehicleInsurancePhoto = null, vehicleLicensePhoto = null;
    if (user.role === "delivery_driver") {
      const [dd] = await db2.select().from(deliveryDrivers3).where((0, import_drizzle_orm25.eq)(deliveryDrivers3.userId, user.id)).limit(1);
      vehicleType = dd?.vehicleType ?? null;
      vehiclePlate = dd?.vehiclePlate ?? null;
      vehiclePhoto = dd?.vehiclePhoto ?? null;
      vehicleBrand = dd?.vehicleBrand ?? null;
      vehicleModel = dd?.vehicleModel ?? null;
      vehicleColor = dd?.vehicleColor ?? null;
      vehicleYear = dd?.vehicleYear ?? null;
      vehiclePlatePhoto = dd?.vehiclePlatePhoto ?? null;
      vehicleItvPhoto = dd?.vehicleItvPhoto ?? null;
      vehicleInsurancePhoto = dd?.vehicleInsurancePhoto ?? null;
      vehicleLicensePhoto = dd?.vehicleLicensePhoto ?? null;
    }
    res.json({
      success: true,
      dni: user.dni,
      address: user.address,
      verificationStatus: user.verificationStatus,
      isActive: user.isActive,
      vehicleType,
      vehiclePlate,
      vehiclePhoto,
      vehicleBrand,
      vehicleModel,
      vehicleColor,
      vehicleYear,
      vehiclePlatePhoto,
      vehicleItvPhoto,
      vehicleInsurancePhoto,
      vehicleLicensePhoto,
      idDocumentUrl: user.idDocumentUrl,
      idDocumentBackUrl: user.idDocumentBackUrl,
      autonomoDocumentUrl: user.autonomoDocumentUrl
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.post("/profile-image", authenticateToken, async (req, res) => {
  try {
    const { image } = req.body;
    if (!image || !image.startsWith("data:image/")) {
      return res.status(400).json({ error: "Imagen en base64 requerida (data:image/...)" });
    }
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const profileImageUrl = await CloudinaryService2.uploadImage(
      image,
      "profiles",
      `user-${req.user.id}`
    );
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    await db.update(users5).set({ profileImage: profileImageUrl, updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    res.json({
      success: true,
      profileImage: profileImageUrl,
      message: "Foto de perfil actualizada correctamente"
    });
  } catch (error) {
    console.error("Profile image upload error:", error);
    res.status(500).json({ error: error.message });
  }
});
router4.put("/vehicle", authenticateToken, async (req, res) => {
  try {
    const { deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const {
      vehicleType,
      vehiclePlate,
      vehicleBrand,
      vehicleModel,
      vehicleColor,
      vehicleYear,
      vehiclePhoto,
      vehiclePlatePhoto,
      vehicleItvPhoto,
      vehicleInsurancePhoto,
      vehicleLicensePhoto,
      deleteVehiclePhoto,
      deleteVehiclePlatePhoto,
      deleteVehicleItvPhoto,
      deleteVehicleInsurancePhoto,
      deleteVehicleLicensePhoto
    } = req.body;
    const updates = {};
    if (vehicleType !== void 0) updates.vehicleType = vehicleType || null;
    if (vehiclePlate !== void 0) updates.vehiclePlate = vehiclePlate || null;
    if (vehicleBrand !== void 0) updates.vehicleBrand = vehicleBrand || null;
    if (vehicleModel !== void 0) updates.vehicleModel = vehicleModel || null;
    if (vehicleColor !== void 0) updates.vehicleColor = vehicleColor || null;
    if (vehicleYear !== void 0) updates.vehicleYear = vehicleYear || null;
    const uploadDoc = async (b64, key) => {
      if (!b64) return void 0;
      if (b64.startsWith("data:image/")) {
        return await CloudinaryService2.uploadImage(
          b64,
          "profiles",
          `driver-${req.user.id}-${key}`
        );
      }
      return b64;
    };
    const photoUrl = await uploadDoc(vehiclePhoto, "photo");
    const plateUrl = await uploadDoc(vehiclePlatePhoto, "plate");
    const itvUrl = await uploadDoc(vehicleItvPhoto, "itv");
    const insuranceUrl = await uploadDoc(vehicleInsurancePhoto, "insurance");
    const licenseUrl = await uploadDoc(vehicleLicensePhoto, "license");
    if (deleteVehiclePhoto === true) updates.vehiclePhoto = null;
    else if (photoUrl !== void 0) updates.vehiclePhoto = photoUrl;
    if (deleteVehiclePlatePhoto === true) updates.vehiclePlatePhoto = null;
    else if (plateUrl !== void 0) updates.vehiclePlatePhoto = plateUrl;
    if (deleteVehicleItvPhoto === true) updates.vehicleItvPhoto = null;
    else if (itvUrl !== void 0) updates.vehicleItvPhoto = itvUrl;
    if (deleteVehicleInsurancePhoto === true)
      updates.vehicleInsurancePhoto = null;
    else if (insuranceUrl !== void 0)
      updates.vehicleInsurancePhoto = insuranceUrl;
    if (deleteVehicleLicensePhoto === true) updates.vehicleLicensePhoto = null;
    else if (licenseUrl !== void 0) updates.vehicleLicensePhoto = licenseUrl;
    const [existing] = await db2.select().from(deliveryDrivers3).where((0, import_drizzle_orm25.eq)(deliveryDrivers3.userId, req.user.id)).limit(1);
    if (existing) {
      await db2.update(deliveryDrivers3).set(updates).where((0, import_drizzle_orm25.eq)(deliveryDrivers3.userId, req.user.id));
    } else {
      await db2.insert(deliveryDrivers3).values({ id: crypto.randomUUID(), userId: req.user.id, ...updates });
    }
    const docsChanged = photoUrl !== void 0 || plateUrl !== void 0 || itvUrl !== void 0 || insuranceUrl !== void 0 || licenseUrl !== void 0 || deleteVehiclePhoto === true || deleteVehiclePlatePhoto === true || deleteVehicleItvPhoto === true || deleteVehicleInsurancePhoto === true || deleteVehicleLicensePhoto === true;
    const infoChanged = !!existing && (updates.vehicleType !== void 0 && updates.vehicleType !== existing.vehicleType || updates.vehiclePlate !== void 0 && updates.vehiclePlate !== existing.vehiclePlate || updates.vehicleBrand !== void 0 && updates.vehicleBrand !== existing.vehicleBrand || updates.vehicleModel !== void 0 && updates.vehicleModel !== existing.vehicleModel || updates.vehicleColor !== void 0 && updates.vehicleColor !== existing.vehicleColor);
    if (docsChanged || infoChanged) {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      await db2.update(users5).set({ verificationStatus: "pending" }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
      try {
        const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
        notifyAdmins2({
          type: "driver_document_updated",
          userId: req.user.id,
          document: "vehicle"
        });
      } catch {
      }
    }
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.get("/profile", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id)).limit(1);
    if (!user) {
      return res.status(404).json({ error: "Usuario no encontrado" });
    }
    res.json({
      success: true,
      user: {
        id: user.id,
        name: user.name,
        phone: user.phone,
        role: user.role,
        isActive: user.isActive,
        profileImage: user.profileImage,
        createdAt: user.createdAt
      }
    });
  } catch (error) {
    console.error("Get profile error:", error);
    res.status(500).json({ error: error.message });
  }
});
router4.get("/:userId", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select().from(users5).where((0, import_drizzle_orm25.eq)(users5.id, req.params.userId)).limit(1);
    if (!user) {
      return res.status(404).json({ error: "Usuario no encontrado" });
    }
    res.json({
      success: true,
      user: {
        id: user.id,
        name: user.name,
        phone: user.phone,
        role: user.role,
        profilePicture: user.profileImage,
        createdAt: user.createdAt
      }
    });
  } catch (error) {
    console.error("Get user error:", error);
    res.status(500).json({ error: error.message });
  }
});
router4.put("/profile", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { name, email, profileImage, dni, address, phone } = req.body;
    const updates = {};
    if (name) updates.name = name;
    if (phone !== void 0) updates.phone = phone || null;
    if (email !== void 0) updates.email = email || null;
    if (profileImage) updates.profileImage = profileImage;
    if (dni !== void 0) updates.dni = dni || null;
    if (address !== void 0) updates.address = address || null;
    if (Object.keys(updates).length === 0) {
      return res.status(400).json({ error: "No hay datos para actualizar" });
    }
    updates.updatedAt = /* @__PURE__ */ new Date();
    await db2.update(users5).set(updates).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    if (address) {
      const { addresses: addresses2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const existing = await db2.select().from(addresses2).where((0, import_drizzle_orm25.eq)(addresses2.userId, req.user.id)).limit(10);
      const defaultAddr = existing.find((a) => a.isDefault) || existing[0];
      if (defaultAddr) {
        let lat = defaultAddr.latitude;
        let lng = defaultAddr.longitude;
        try {
          const { geocodeAddress: geocodeAddressFn } = await Promise.resolve().then(() => (init_geocodeService(), geocodeService_exports));
          const coords = await geocodeAddressFn(address);
          if (coords) {
            lat = String(coords.lat);
            lng = String(coords.lng);
          }
        } catch {
        }
        await db2.update(addresses2).set({ street: address, latitude: lat, longitude: lng }).where((0, import_drizzle_orm25.eq)(addresses2.id, defaultAddr.id));
      } else {
        let lat = null;
        let lng = null;
        try {
          const { geocodeAddress: geocodeAddressFn } = await Promise.resolve().then(() => (init_geocodeService(), geocodeService_exports));
          const coords = await geocodeAddressFn(address);
          if (coords) {
            lat = String(coords.lat);
            lng = String(coords.lng);
          }
        } catch {
        }
        if (lat && lng) {
          await db2.insert(addresses2).values({
            id: crypto.randomUUID(),
            userId: req.user.id,
            label: "Casa",
            street: address,
            city: "Soria",
            state: "Castilla y Le\xF3n",
            isDefault: true,
            latitude: lat,
            longitude: lng
          });
        } else {
          console.warn(
            `\u{1F4CD} Perfil: direcci\xF3n no ubicable, no se crea la fila de direcci\xF3n (user ${req.user.id})`
          );
        }
      }
    }
    res.json({ success: true, message: "Perfil actualizado" });
  } catch (error) {
    console.error("Update profile error:", error);
    res.status(500).json({ error: error.message });
  }
});
router4.post("/profile-image", authenticateToken, async (req, res) => {
  try {
    const { image } = req.body;
    if (!image) return res.status(400).json({ error: "Imagen requerida" });
    if (!image.startsWith("data:image/"))
      return res.status(400).json({ error: "Formato de imagen inv\xE1lido" });
    const estimatedBytes = Math.ceil(image.length * 0.75);
    if (estimatedBytes > 2 * 1024 * 1024) {
      return res.status(400).json({ error: "La imagen es muy pesada. M\xE1ximo 2MB" });
    }
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const [currentUser] = await db2.select().from(users5).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id)).limit(1);
    if (currentUser?.profileImage && currentUser.profileImage.includes("cloudinary")) {
      const oldPublicId = CloudinaryService2.extractPublicId(
        currentUser.profileImage
      );
      if (oldPublicId)
        await CloudinaryService2.deleteImage(oldPublicId).catch(() => {
        });
    }
    const imageUrl = await CloudinaryService2.uploadImage(
      image,
      "profiles",
      `user-${req.user.id}`
    );
    await db2.update(users5).set({ profileImage: imageUrl }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    res.json({
      success: true,
      profileImage: imageUrl,
      message: "Imagen actualizada"
    });
  } catch (error) {
    console.error("Upload image error:", error);
    res.status(500).json({ error: error.message });
  }
});
var getAddresses = async (req, res) => {
  try {
    const { addresses: addresses2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const userId = req.params.id || req.user.id;
    const role = req.user.role;
    if (userId !== req.user.id && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado para ver estas direcciones" });
    }
    const list = await db2.select().from(addresses2).where((0, import_drizzle_orm25.eq)(addresses2.userId, userId));
    res.json({ success: true, addresses: list });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};
router4.get("/addresses", authenticateToken, getAddresses);
router4.get("/:id/addresses", authenticateToken, getAddresses);
var postAddress = async (req, res) => {
  try {
    const { addresses: addresses2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { isValidLatLng: isValidLatLng2 } = await Promise.resolve().then(() => (init_coordinates(), coordinates_exports));
    const userId = req.params.id || req.user.id;
    if (!userId || userId === "undefined" || userId === "null") {
      return res.status(400).json({ error: "Usuario no autenticado" });
    }
    const role = req.user.role;
    if (userId !== req.user.id && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado para crear direcciones de otro usuario" });
    }
    const {
      label,
      street,
      city,
      state,
      zipCode,
      latitude,
      longitude,
      isDefault
    } = req.body;
    if (!label || !street)
      return res.status(400).json({ error: "label y street son requeridos" });
    let finalLat = latitude !== void 0 && latitude !== null && latitude !== "" ? String(latitude) : null;
    let finalLng = longitude !== void 0 && longitude !== null && longitude !== "" ? String(longitude) : null;
    if (finalLat && finalLng && !isValidLatLng2(finalLat, finalLng)) {
      return res.status(400).json({ error: "Coordenadas inv\xE1lidas" });
    }
    if (!finalLat || !finalLng) {
      try {
        const { geocodeAddress: geocodeAddressFn } = await Promise.resolve().then(() => (init_geocodeService(), geocodeService_exports));
        const coords = await geocodeAddressFn(
          `${street}, ${city || "Soria"}`
        );
        if (coords) {
          finalLat = String(coords.lat);
          finalLng = String(coords.lng);
        }
      } catch {
      }
    }
    if (!finalLat || !finalLng) {
      return res.status(400).json({
        error: "No hemos podido ubicar la direcci\xF3n en el mapa. Elige el punto en el mapa o revisa la direcci\xF3n."
      });
    }
    if (isDefault) {
      await db2.update(addresses2).set({ isDefault: false }).where((0, import_drizzle_orm25.eq)(addresses2.userId, userId));
    }
    const id = crypto.randomUUID();
    await db2.insert(addresses2).values({
      id,
      userId,
      label,
      street,
      city: city || "Soria",
      state: state || "Castilla y Le\xF3n",
      zipCode: zipCode || null,
      latitude: finalLat,
      longitude: finalLng,
      isDefault: isDefault || false
    });
    const [saved] = await db2.select().from(addresses2).where((0, import_drizzle_orm25.eq)(addresses2.id, id)).limit(1);
    res.json({ success: true, address: saved });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};
router4.post("/addresses", authenticateToken, postAddress);
router4.post("/:id/addresses", authenticateToken, postAddress);
router4.put("/:id/addresses/:addressId", authenticateToken, async (req, res) => {
  try {
    const { addresses: addresses2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { isValidLatLng: isValidLatLng2 } = await Promise.resolve().then(() => (init_coordinates(), coordinates_exports));
    const role = req.user.role;
    const [existing] = await db2.select().from(addresses2).where((0, import_drizzle_orm25.eq)(addresses2.id, req.params.addressId)).limit(1);
    if (!existing) {
      return res.status(404).json({ error: "Direcci\xF3n no encontrada" });
    }
    if (existing.userId !== req.user.id && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado para editar esta direcci\xF3n" });
    }
    const {
      label,
      street,
      city,
      state,
      zipCode,
      latitude,
      longitude,
      isDefault
    } = req.body;
    const updates = {};
    if (label) updates.label = label;
    if (street) updates.street = street;
    if (city) updates.city = city;
    if (state) updates.state = state;
    if (zipCode !== void 0) updates.zipCode = zipCode;
    if (latitude !== void 0) updates.latitude = String(latitude);
    if (longitude !== void 0) updates.longitude = String(longitude);
    if (isDefault !== void 0) updates.isDefault = isDefault;
    if (updates.latitude || updates.longitude) {
      if (!isValidLatLng2(updates.latitude, updates.longitude)) {
        return res.status(400).json({ error: "Coordenadas inv\xE1lidas" });
      }
    } else if (updates.street) {
      try {
        const { geocodeAddress: geocodeAddressFn } = await Promise.resolve().then(() => (init_geocodeService(), geocodeService_exports));
        const coords = await geocodeAddressFn(
          `${updates.street}, ${updates.city || existing.city || "Soria"}`
        );
        if (coords) {
          updates.latitude = String(coords.lat);
          updates.longitude = String(coords.lng);
        }
      } catch {
      }
      if (!updates.latitude || !updates.longitude) {
        delete updates.latitude;
        delete updates.longitude;
      }
    }
    await db2.update(addresses2).set(updates).where((0, import_drizzle_orm25.eq)(addresses2.id, req.params.addressId));
    const [updated] = await db2.select().from(addresses2).where((0, import_drizzle_orm25.eq)(addresses2.id, req.params.addressId)).limit(1);
    res.json({ success: true, address: updated });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.delete(
  "/:id/addresses/:addressId",
  authenticateToken,
  async (req, res) => {
    try {
      const { addresses: addresses2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const role = req.user.role;
      const [existing] = await db2.select().from(addresses2).where((0, import_drizzle_orm25.eq)(addresses2.id, req.params.addressId)).limit(1);
      if (!existing) {
        return res.status(404).json({ error: "Direcci\xF3n no encontrada" });
      }
      if (existing.userId !== req.user.id && role !== "admin" && role !== "super_admin") {
        return res.status(403).json({ error: "No autorizado para eliminar esta direcci\xF3n" });
      }
      await db2.delete(addresses2).where((0, import_drizzle_orm25.eq)(addresses2.id, req.params.addressId));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router4.get(
  "/stats",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      const [userCount] = await db2.execute(
        sql23`SELECT COUNT(*) as count FROM users`
      );
      const [orderCount] = await db2.execute(
        sql23`SELECT COUNT(*) as count FROM orders`
      );
      const [activeUsers] = await db2.execute(sql23`
      SELECT COUNT(DISTINCT userId) as count 
      FROM orders 
      WHERE createdAt >= DATE_SUB(NOW(), INTERVAL 30 DAY)
    `);
      res.json({
        success: true,
        stats: {
          totalUsers: userCount.count,
          totalOrders: orderCount.count,
          activeUsers: activeUsers.count
        }
      });
    } catch (error) {
      console.error("Get user stats error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router4.post(
  "/:id/verification-documents",
  authenticateToken,
  async (req, res) => {
    try {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const userId = req.params.id;
      const { idDocumentUrl, autonomoDocumentUrl } = req.body;
      const updates = { verificationStatus: "pending" };
      if (idDocumentUrl) updates.idDocumentUrl = idDocumentUrl;
      if (autonomoDocumentUrl)
        updates.autonomoDocumentUrl = autonomoDocumentUrl;
      await db2.update(users5).set(updates).where((0, import_drizzle_orm25.eq)(users5.id, userId));
      res.json({
        success: true,
        message: "Documentos recibidos. Tu cuenta est\xE1 en revisi\xF3n."
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router4.get("/:id/verification-status", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [user] = await db2.select({
      verificationStatus: users5.verificationStatus,
      verificationNotes: users5.verificationNotes
    }).from(users5).where((0, import_drizzle_orm25.eq)(users5.id, req.params.id)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    res.json({ success: true, ...user });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.post("/:id/reset-verification", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const userId = req.params.id;
    if (req.user.id !== userId && req.user.role !== "admin" && req.user.role !== "super_admin") {
      return res.status(403).json({ error: "No tienes permiso para hacer esto" });
    }
    const [user] = await db2.select().from(users5).where(eq79(users5.id, userId)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    if (user.verificationStatus === "verified" && user.isActive) {
      return res.status(400).json({
        error: "Tu cuenta ya est\xE1 verificada. Contacta soporte para modificar tus documentos."
      });
    }
    await db2.update(users5).set({
      verificationStatus: "pending",
      verificationNotes: null,
      updatedAt: /* @__PURE__ */ new Date()
    }).where(eq79(users5.id, userId));
    res.json({
      success: true,
      message: "Verificaci\xF3n reiniciada. Por favor, env\xEDa tus documentos nuevamente."
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.post("/change-phone", authenticateToken, async (req, res) => {
  try {
    const { newPhone } = req.body;
    if (!newPhone) return res.status(400).json({ error: "Tel\xE9fono requerido" });
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [existing] = await db2.select().from(users5).where((0, import_drizzle_orm25.eq)(users5.phone, newPhone)).limit(1);
    if (existing && existing.id !== req.user.id) {
      return res.status(400).json({ error: "Este tel\xE9fono ya est\xE1 registrado" });
    }
    const TwilioModule = await import("twilio");
    const TwilioClass = TwilioModule.default || TwilioModule;
    const twilio2 = TwilioClass(
      process.env.TWILIO_ACCOUNT_SID,
      process.env.TWILIO_AUTH_TOKEN
    );
    await twilio2.verify.v2.services(process.env.TWILIO_VERIFY_SERVICE_SID).verifications.create({
      to: newPhone,
      channel: "sms"
    });
    res.json({ success: true, message: "C\xF3digo enviado al nuevo tel\xE9fono" });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.post("/verify-phone-change", authenticateToken, async (req, res) => {
  try {
    const { newPhone, code } = req.body;
    if (!newPhone || !code)
      return res.status(400).json({ error: "Tel\xE9fono y c\xF3digo requeridos" });
    const TwilioModule = await import("twilio");
    const TwilioClass = TwilioModule.default || TwilioModule;
    const twilio2 = TwilioClass(
      process.env.TWILIO_ACCOUNT_SID,
      process.env.TWILIO_AUTH_TOKEN
    );
    const check = await twilio2.verify.v2.services(process.env.TWILIO_VERIFY_SERVICE_SID).verificationChecks.create({
      to: newPhone,
      code
    });
    if (check.status !== "approved") {
      return res.status(400).json({ error: "C\xF3digo incorrecto" });
    }
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    await db2.update(users5).set({ phone: newPhone, updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    res.json({ success: true, message: "Tel\xE9fono actualizado correctamente" });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router4.post("/verification-document", authenticateToken, async (req, res) => {
  try {
    const { key, image } = req.body;
    if (!key || !image)
      return res.status(400).json({ error: "key e image requeridos" });
    if (!image.startsWith("data:image/"))
      return res.status(400).json({ error: "Formato de imagen inv\xE1lido" });
    const estimatedBytes = Math.ceil(image.length * 0.75);
    if (estimatedBytes > 5 * 1024 * 1024) {
      return res.status(400).json({ error: "El archivo es muy pesado. M\xE1ximo 5MB" });
    }
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const url = await CloudinaryService2.uploadImage(
      image,
      "verification-docs",
      `driver-${req.user.id}-${key}`
    );
    const fieldMap = {
      idDocument: "idDocumentUrl",
      idDocumentBack: "idDocumentBackUrl",
      autonomo: "autonomoDocumentUrl",
      vehicleLicense: "vehicleLicensePhoto",
      vehiclePlate: "vehiclePlatePhoto",
      vehicleItv: "vehicleItvPhoto",
      vehicleInsurance: "vehicleInsurancePhoto",
      vehiclePhoto: "vehiclePhoto"
    };
    const field = fieldMap[key];
    if (!field) {
      return res.status(400).json({ error: "Tipo de documento inv\xE1lido" });
    }
    const isVehicleDocument = [
      "vehicleLicense",
      "vehiclePlate",
      "vehicleItv",
      "vehicleInsurance",
      "vehiclePhoto"
    ].includes(key);
    if (isVehicleDocument) {
      const { deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [existing] = await db2.select().from(deliveryDrivers3).where((0, import_drizzle_orm25.eq)(deliveryDrivers3.userId, req.user.id)).limit(1);
      console.log("DEBUG existing driver:", !!existing, "field:", field);
      if (existing) {
        const updateQuery = db2.update(deliveryDrivers3).set({ [field]: url, updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm25.eq)(deliveryDrivers3.userId, req.user.id));
        await updateQuery;
      } else {
        await db2.insert(deliveryDrivers3).values({
          id: crypto.randomUUID(),
          userId: req.user.id,
          [field]: url
        });
      }
    } else {
      await db2.update(users5).set({
        [field]: url,
        verificationStatus: "pending",
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    }
    if (isVehicleDocument) {
      await db2.update(users5).set({ verificationStatus: "pending" }).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    }
    try {
      const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      notifyAdmins2({
        type: "driver_document_updated",
        userId: req.user.id,
        document: key
      });
    } catch {
    }
    res.json({
      success: true,
      url,
      message: "Documento guardado correctamente"
    });
  } catch (error) {
    console.error("Verification document upload error:", error);
    res.status(500).json({ error: error.message });
  }
});
router4.put("/personal-docs", authenticateToken, async (req, res) => {
  try {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const {
      idDocumentUrl,
      idDocumentBackUrl,
      autonomoDocumentUrl,
      deleteIdDocumentUrl,
      deleteIdDocumentBackUrl,
      deleteAutonomoDocumentUrl
    } = req.body;
    const updates = {
      verificationStatus: "pending",
      updatedAt: /* @__PURE__ */ new Date()
    };
    if (deleteIdDocumentUrl === true) updates.idDocumentUrl = null;
    else if (idDocumentUrl) updates.idDocumentUrl = idDocumentUrl;
    if (deleteIdDocumentBackUrl === true) updates.idDocumentBackUrl = null;
    else if (idDocumentBackUrl) updates.idDocumentBackUrl = idDocumentBackUrl;
    if (deleteAutonomoDocumentUrl === true) updates.autonomoDocumentUrl = null;
    else if (autonomoDocumentUrl)
      updates.autonomoDocumentUrl = autonomoDocumentUrl;
    await db2.update(users5).set(updates).where((0, import_drizzle_orm25.eq)(users5.id, req.user.id));
    try {
      const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      notifyAdmins2({
        type: "driver_document_updated",
        userId: req.user.id,
        document: "personal"
      });
    } catch {
    }
    res.json({
      success: true,
      message: "Documentos guardados. Tu cuenta ser\xE1 revisada nuevamente."
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var users_default = router4;

// server/routes/delivery.ts
var import_express5 = __toESM(require("express"));
var import_drizzle_orm26 = require("drizzle-orm");
init_orderNumberService();
var router5 = import_express5.default.Router();
router5.get("/config", (req, res) => {
  res.json({
    success: true,
    config: { tier1: 2.5, tier2: 4, tier3: 5, extraPerKm: 1 }
  });
});
router5.get(
  "/active-order",
  authenticateToken,
  requireRole("delivery_driver"),
  async (req, res) => {
    try {
      const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const activeOrders = await db2.select().from(orders2).where(
        (0, import_drizzle_orm26.and)(
          (0, import_drizzle_orm26.eq)(orders2.deliveryPersonId, req.user.id),
          // "arriving"/"in_transit" deben estar: el pipeline los usa al
          // final de la entrega y el pedido desaparecía del mapa del driver
          (0, import_drizzle_orm26.inArray)(orders2.status, [
            "accepted",
            "preparing",
            "ready",
            "assigned",
            "picked_up",
            "on_the_way",
            "in_transit",
            "arriving"
          ])
        )
      ).limit(1);
      if (!activeOrders.length) return res.json({ success: true, order: null });
      const order = activeOrders[0];
      const [biz] = await db2.select({ name: businesses3.name, address: businesses3.address }).from(businesses3).where((0, import_drizzle_orm26.eq)(businesses3.id, order.businessId)).limit(1);
      res.json({
        success: true,
        order: {
          id: order.id,
          status: order.status,
          businessName: biz?.name ?? order.businessName ?? "Negocio",
          deliveryAddress: order.deliveryAddress,
          deliveryLatitude: order.deliveryLatitude,
          deliveryLongitude: order.deliveryLongitude
        }
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router5.get(
  "/drivers",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const drivers3 = await db2.select({
        id: users5.id,
        name: users5.name,
        phone: users5.phone,
        isActive: users5.isActive,
        createdAt: users5.createdAt
      }).from(users5).where(
        (0, import_drizzle_orm26.and)((0, import_drizzle_orm26.eq)(users5.role, "delivery_driver"), (0, import_drizzle_orm26.eq)(users5.isActive, true))
      );
      res.json({ success: true, drivers: drivers3 });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router5.post(
  "/assign",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { orderId, driverId } = req.body;
      if (!orderId || !driverId)
        return res.status(400).json({ error: "ID de pedido y conductor requeridos" });
      const { orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm26.eq)(orders2.id, orderId)).limit(1);
      if (!order)
        return res.status(404).json({ error: "Pedido no encontrado" });
      const [driver] = await db2.select().from(users5).where(
        (0, import_drizzle_orm26.and)(
          (0, import_drizzle_orm26.eq)(users5.id, driverId),
          (0, import_drizzle_orm26.eq)(users5.role, "delivery_driver"),
          (0, import_drizzle_orm26.eq)(users5.isActive, true)
        )
      ).limit(1);
      if (!driver)
        return res.status(404).json({ error: "Conductor no encontrado" });
      await db2.update(orders2).set({
        deliveryPersonId: driverId,
        status: "assigned",
        assignedAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm26.eq)(orders2.id, orderId));
      const { notifyDriverAssigned: notifyDriverAssigned2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      notifyDriverAssigned2(driverId, {
        orderId,
        status: "assigned",
        assignedAt: (/* @__PURE__ */ new Date()).toISOString()
      });
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      sendPushToUser2(driverId, {
        title: "\u{1F680} Nuevo pedido asignado",
        body: `Te han asignado el pedido ${await orderRefFromId(orderId)}`,
        data: { orderId, screen: "DriverMap" }
      }).catch(() => {
      });
      res.json({ success: true, message: "Conductor asignado" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var delivery_default = router5;

// server/routes/payments.ts
var import_express6 = __toESM(require("express"));
var import_drizzle_orm30 = require("drizzle-orm");
init_stripeClient();
var router6 = import_express6.default.Router();
router6.get("/info", authenticateToken, async (req, res) => {
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [rows] = await db2.execute(
      "SELECT provider, account_data FROM payment_receiving_accounts WHERE is_active = TRUE"
    );
    const data = {
      success: true,
      bizum: "",
      iban: "",
      paypalEmail: "",
      titular: "ComeYa S.L.",
      banco: ""
    };
    rows.forEach((row) => {
      const d = row.account_data;
      if (row.provider === "bizum") {
        data.bizum = d.phone || "";
        data.titular = d.name || data.titular;
      }
      if (row.provider === "transferencia") {
        data.iban = d.iban || "";
        data.titular = d.titular || data.titular;
        data.banco = d.banco || "";
      }
      if (row.provider === "paypal") {
        data.paypalEmail = d.email || "";
      }
    });
    res.json(data);
  } catch (error) {
    const { CONFIG: CONFIG2 } = await Promise.resolve().then(() => (init_config(), config_exports));
    res.json({
      success: true,
      bizum: await CONFIG2.bizumPhone(),
      iban: await CONFIG2.iban(),
      paypalEmail: await CONFIG2.paypalEmail(),
      titular: await CONFIG2.titular(),
      banco: await CONFIG2.banco()
    });
  }
});
router6.post("/upload-proof-image", authenticateToken, async (req, res) => {
  try {
    const multer = (await import("multer")).default;
    const cloudinary2 = (await import("cloudinary")).v2;
    cloudinary2.config({
      cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
      api_key: process.env.CLOUDINARY_API_KEY,
      api_secret: process.env.CLOUDINARY_API_SECRET
    });
    const upload = multer({ storage: multer.memoryStorage() }).single("file");
    upload(req, res, async (err) => {
      if (err)
        return res.status(400).json({ success: false, error: err.message });
      if (!req.file)
        return res.status(400).json({ success: false, error: "No se recibi\xF3 archivo" });
      const result = await new Promise((resolve, reject) => {
        const stream = cloudinary2.uploader.upload_stream(
          { folder: "payment_proofs", resource_type: "image" },
          (error, result2) => error ? reject(error) : resolve(result2)
        );
        stream.end(req.file.buffer);
      });
      res.json({ success: true, url: result.secure_url });
    });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router6.post("/submit-proof", authenticateToken, async (req, res) => {
  try {
    const {
      orderId,
      imageUrl,
      referenceNumber,
      senderName,
      amount,
      paymentMethod
    } = req.body;
    const userId = req.user?.id;
    if (!orderId || !imageUrl || !referenceNumber) {
      return res.status(400).json({ error: "orderId, imageUrl y referenceNumber son requeridos" });
    }
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { orders: orders2, paymentProofs: paymentProofs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { randomUUID: randomUUID5 } = await import("crypto");
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm30.eq)(orders2.id, orderId)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    const { sql: drizzleSql } = await import("drizzle-orm");
    const [existing] = await db2.select().from(paymentProofs2).where(
      drizzleSql`reference_number = ${referenceNumber.trim()} AND id != 'none'`
    ).limit(1);
    if (existing) {
      return res.status(409).json({ error: "Este comprobante ya fue enviado anteriormente" });
    }
    const proofId = randomUUID5();
    await db2.insert(paymentProofs2).values({
      id: proofId,
      orderId,
      userId,
      paymentProvider: paymentMethod || order.paymentMethod || "bizum",
      proofImageUrl: imageUrl,
      referenceNumber: referenceNumber.trim(),
      amount: amount || order.total,
      status: "pending",
      submittedAt: /* @__PURE__ */ new Date()
    });
    if (senderName) {
      await db2.execute(
        drizzleSql`UPDATE payment_proofs SET verification_notes = ${`Remitente: ${senderName}`} WHERE id = ${proofId}`
      );
    }
    try {
      const { autoVerificationService: autoVerificationService2 } = await Promise.resolve().then(() => (init_autoVerificationService(), autoVerificationService_exports));
      const result = await autoVerificationService2.shouldAutoApprove(proofId);
      if (result.autoApprove) {
        await db2.update(orders2).set({
          status: "confirmed",
          paidAt: /* @__PURE__ */ new Date(),
          updatedAt: /* @__PURE__ */ new Date()
        }).where((0, import_drizzle_orm30.eq)(orders2.id, orderId));
        await db2.execute(
          drizzleSql`UPDATE payment_proofs SET status = 'approved', verified_at = NOW() WHERE id = ${proofId}`
        );
        return res.json({
          success: true,
          proofId,
          autoApproved: true,
          message: "Pago verificado autom\xE1ticamente"
        });
      }
    } catch {
    }
    try {
      const { notifyAdminNewProof: notifyAdminNewProof2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [u] = await db2.select({ name: users5.name }).from(users5).where((0, import_drizzle_orm30.eq)(users5.id, userId)).limit(1);
      notifyAdminNewProof2({
        proofId,
        orderId,
        userName: u?.name ?? "Cliente",
        amount: amount || order.total,
        method: paymentMethod || order.paymentMethod || "bizum"
      });
    } catch {
    }
    res.json({
      success: true,
      proofId,
      autoApproved: false,
      message: "Comprobante recibido. Ser\xE1 verificado en breve."
    });
  } catch (error) {
    console.error("Submit proof error:", error);
    res.status(500).json({ error: error.message });
  }
});
router6.get("/proofs/pending", authenticateToken, async (req, res) => {
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { sql: drizzleSql } = await import("drizzle-orm");
    const [rows] = await db2.execute(
      drizzleSql`
        SELECT pp.*, o.business_name, o.total as order_total, u.name as user_name, u.phone as user_phone
        FROM payment_proofs pp
        LEFT JOIN orders o ON pp.order_id = o.id
        LEFT JOIN users u ON pp.user_id = u.id
        WHERE pp.status = 'pending'
        ORDER BY pp.submitted_at DESC
      `
    );
    res.json({ success: true, proofs: rows });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router6.post(
  "/proofs/:proofId/approve",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { digitalPaymentService: digitalPaymentService2 } = await Promise.resolve().then(() => (init_digitalPaymentService(), digitalPaymentService_exports));
      const result = await digitalPaymentService2.verifyPaymentProof(
        String(req.params.proofId),
        req.user.id,
        true
      );
      if (!result.success) {
        return res.status(400).json({ error: result.message });
      }
      res.json({ success: true, message: "Comprobante aprobado" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router6.post(
  "/proofs/:proofId/reject",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { reason } = req.body;
      const { digitalPaymentService: digitalPaymentService2 } = await Promise.resolve().then(() => (init_digitalPaymentService(), digitalPaymentService_exports));
      const result = await digitalPaymentService2.verifyPaymentProof(
        String(req.params.proofId),
        req.user.id,
        false,
        reason || "Rechazado por administraci\xF3n"
      );
      if (!result.success) {
        return res.status(400).json({ error: result.message });
      }
      res.json({ success: true, message: "Comprobante rechazado" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router6.post("/analyze-proof", authenticateToken, async (req, res) => {
  try {
    const { imageBase64, expectedAmount, paymentMethod } = req.body;
    if (!imageBase64)
      return res.status(400).json({ error: "imageBase64 requerido" });
    const { GoogleGenerativeAI: GoogleGenerativeAI2 } = await import("@google/generative-ai");
    const genAI = new GoogleGenerativeAI2(process.env.GEMINI_API_KEY);
    const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash" });
    const prompt = `Analiza este comprobante de pago bancario/transferencia y extrae:
1. referenceNumber: n\xFAmero de referencia, localizador o ID de transacci\xF3n
2. amount: importe en euros (solo n\xFAmero)
3. senderName: nombre del remitente
4. date: fecha de la transacci\xF3n
5. bankName: nombre del banco
6. isValid: true si parece un comprobante leg\xEDtimo

Responde SOLO con JSON v\xE1lido sin markdown. Ejemplo:
{"referenceNumber":"123456","amount":25.50,"senderName":"Juan Garc\xEDa","date":"2024-04-22","bankName":"Santander","isValid":true}`;
    const result = await model.generateContent([
      { inlineData: { mimeType: "image/jpeg", data: imageBase64 } },
      prompt
    ]);
    const text2 = result.response.text().replace(/```json|```/g, "").trim();
    const extracted = JSON.parse(text2);
    res.json({ success: true, extracted });
  } catch (error) {
    console.error("OCR analyze error:", error);
    res.json({ success: false, error: error.message });
  }
});
router6.post("/create-session", authenticateToken, async (req, res) => {
  try {
    const { orderId, amount, provider, isSubscription, subscriptionId } = req.body;
    if (!orderId || !amount || !provider) {
      return res.status(400).json({ error: "orderId, amount y provider son requeridos" });
    }
    const amountEur = amount / 100;
    switch (provider) {
      case "stripe_card":
      case "stripe_bizum": {
        const stripe2 = getStripe2();
        const payment_method_types = provider === "stripe_bizum" ? ["bizum"] : ["card"];
        let productName = `Pedido ComeYa #${orderId.slice(-6)}`;
        if (isSubscription) {
          productName = subscriptionId ? `Suscripci\xF3n ComeYa ${subscriptionId.slice(-6)}` : `Suscripci\xF3n ComeYa Premium/Business`;
        }
        const session = await stripe2.checkout.sessions.create({
          payment_method_types,
          line_items: [
            {
              price_data: {
                currency: "eur",
                product_data: { name: productName },
                unit_amount: amount
              },
              quantity: 1
            }
          ],
          mode: "payment",
          success_url: `${process.env.BACKEND_URL}/api/payments/success?orderId=${orderId}&provider=${provider}&isSubscription=${!!isSubscription}&subscriptionId=${subscriptionId || ""}`,
          cancel_url: `${process.env.BACKEND_URL}/api/payments/cancel?orderId=${orderId}&isSubscription=${!!isSubscription}`,
          metadata: {
            orderId,
            provider,
            isSubscription: isSubscription ? "true" : "false",
            subscriptionId: subscriptionId || ""
          }
        });
        return res.json({ url: session.url, sessionId: session.id });
      }
      case "paypal": {
        const base = process.env.PAYPAL_MODE === "live" ? "https://api-m.paypal.com" : "https://api-m.sandbox.paypal.com";
        const tokenRes = await fetch(`${base}/v1/oauth2/token`, {
          method: "POST",
          headers: {
            "Content-Type": "application/x-www-form-urlencoded",
            Authorization: `Basic ${Buffer.from(`${process.env.PAYPAL_CLIENT_ID}:${process.env.PAYPAL_SECRET}`).toString("base64")}`
          },
          body: "grant_type=client_credentials"
        });
        const tokenData = await tokenRes.json();
        const orderRes = await fetch(`${base}/v2/checkout/orders`, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${tokenData.access_token}`
          },
          body: JSON.stringify({
            intent: "CAPTURE",
            purchase_units: [
              {
                reference_id: orderId,
                amount: { currency_code: "EUR", value: amountEur.toFixed(2) }
              }
            ],
            application_context: {
              return_url: `${process.env.BACKEND_URL}/api/payments/success?orderId=${orderId}&provider=paypal`,
              cancel_url: `${process.env.BACKEND_URL}/api/payments/cancel?orderId=${orderId}`
            }
          })
        });
        const paypalOrder = await orderRes.json();
        const approveLink = paypalOrder.links?.find(
          (l) => l.rel === "approve"
        )?.href;
        return res.json({ url: approveLink, paypalOrderId: paypalOrder.id });
      }
      case "binance": {
        const crypto4 = await import("crypto");
        const nonce = crypto4.randomBytes(16).toString("hex");
        const timestamp2 = Date.now();
        const merchantId = process.env.BINANCE_MERCHANT_ID;
        const apiKey = process.env.BINANCE_API_KEY;
        const secretKey = process.env.BINANCE_SECRET_KEY;
        const body = JSON.stringify({
          env: { terminalType: "APP" },
          merchantTradeNo: orderId.replace(/-/g, "").slice(0, 32),
          orderAmount: amountEur.toFixed(2),
          currency: "EUR",
          goods: {
            goodsType: "01",
            goodsCategory: "Z000",
            referenceGoodsId: orderId,
            goodsName: `Pedido ComeYa #${orderId.slice(-6)}`
          },
          returnUrl: `${process.env.BACKEND_URL}/api/payments/success?orderId=${orderId}&provider=binance`,
          cancelUrl: `${process.env.BACKEND_URL}/api/payments/cancel?orderId=${orderId}`
        });
        const payload = `${timestamp2}
${nonce}
${body}
`;
        const signature = crypto4.createHmac("sha512", secretKey).update(payload).digest("hex").toUpperCase();
        const binanceRes = await fetch(
          "https://bpay.binanceapi.com/binancepay/openapi/v2/order",
          {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
              "BinancePay-Timestamp": String(timestamp2),
              "BinancePay-Nonce": nonce,
              "BinancePay-Certificate-SN": apiKey,
              "BinancePay-Signature": signature
            },
            body
          }
        );
        const binanceData = await binanceRes.json();
        return res.json({
          url: binanceData.data?.checkoutUrl,
          binanceOrderId: binanceData.data?.prepayId
        });
      }
      default:
        return res.status(400).json({ error: "Provider no soportado" });
    }
  } catch (error) {
    console.error("Create payment session error:", error);
    res.status(500).json({ error: error.message });
  }
});
router6.get("/success", async (req, res) => {
  try {
    const { orderId, provider, token } = req.query;
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    if (!orderId) {
      return res.status(400).send("<h2>Error: orderId no encontrado</h2>");
    }
    if (provider === "paypal" && token) {
      const base = process.env.PAYPAL_MODE === "live" ? "https://api-m.paypal.com" : "https://api-m.sandbox.paypal.com";
      const tokenRes = await fetch(`${base}/v1/oauth2/token`, {
        method: "POST",
        headers: {
          "Content-Type": "application/x-www-form-urlencoded",
          Authorization: `Basic ${Buffer.from(`${process.env.PAYPAL_CLIENT_ID}:${process.env.PAYPAL_SECRET}`).toString("base64")}`
        },
        body: "grant_type=client_credentials"
      });
      const tokenData = await tokenRes.json();
      await fetch(`${base}/v2/checkout/orders/${token}/capture`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${tokenData.access_token}`
        }
      });
    }
    await db2.update(orders2).set({ status: "accepted", paidAt: /* @__PURE__ */ new Date(), updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm30.eq)(orders2.id, orderId));
    const deepLink = `comeya://order-confirmed?orderId=${orderId}`;
    res.send(`
      <!DOCTYPE html>
      <html lang="es">
      <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Pago completado - ComeYa</title>
        <style>
          body { font-family: system-ui, sans-serif; display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 100vh; margin: 0; background: #F7F7F7; padding: 24px; text-align: center; }
          .card { background: white; border-radius: 20px; padding: 40px 32px; max-width: 400px; width: 100%; box-shadow: 0 4px 20px rgba(0,0,0,0.1); }
          .icon { font-size: 64px; margin-bottom: 16px; }
          h1 { color: #1A1A1A; font-size: 24px; margin-bottom: 8px; }
          p { color: #555; font-size: 16px; margin-bottom: 24px; }
          .btn { display: inline-block; background: #FF6B35; color: white; padding: 14px 28px; border-radius: 50px; text-decoration: none; font-weight: 700; font-size: 16px; }
        </style>
        <script>
          // Intentar abrir la app automaticamente
          window.location.href = '${deepLink}';
          // Si no abre en 2 segundos, mostrar boton
          setTimeout(() => {
            document.getElementById('btn').style.display = 'inline-block';
          }, 2000);
        </script>
      </head>
      <body>
        <div class="card">
          <div class="icon">&#x2705;</div>
          <h1>Pago completado</h1>
          <p>Tu pedido ha sido confirmado. Vuelve a la app para seguir tu pedido.</p>
          <a id="btn" href="${deepLink}" class="btn" style="display:none">Volver a ComeYa</a>
        </div>
      </body>
      </html>
    `);
  } catch (error) {
    console.error("Payment success error:", error);
    res.send(`
      <!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><title>Error - ComeYa</title>
      <style>body{font-family:system-ui,sans-serif;display:flex;align-items:center;justify-content:center;min-height:100vh;margin:0;background:#F7F7F7;padding:24px;text-align:center;}</style></head>
      <body><div style="background:white;border-radius:20px;padding:40px;max-width:400px;box-shadow:0 4px 20px rgba(0,0,0,.1)">
      <div style="font-size:64px">&#x26A0;&#xFE0F;</div>
      <h1 style="color:#1A1A1A">Hubo un problema</h1>
      <p style="color:#555">El pago fue procesado pero hubo un error. Contacta soporte con tu pedido.</p>
      <a href="comeya://" style="display:inline-block;background:#FF6B35;color:white;padding:14px 28px;border-radius:50px;text-decoration:none;font-weight:700">Volver a ComeYa</a>
      </div></body></html>
    `);
  }
});
router6.get("/cancel", async (req, res) => {
  const { orderId } = req.query;
  res.send(`
    <!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><title>Pago cancelado - ComeYa</title>
    <style>body{font-family:system-ui,sans-serif;display:flex;align-items:center;justify-content:center;min-height:100vh;margin:0;background:#F7F7F7;padding:24px;text-align:center;}</style>
    <script>window.location.href='comeya://order-cancelled?orderId=${orderId}';</script>
    </head>
    <body><div style="background:white;border-radius:20px;padding:40px;max-width:400px;box-shadow:0 4px 20px rgba(0,0,0,.1)">
    <div style="font-size:64px">&#x274C;</div>
    <h1 style="color:#1A1A1A">Pago cancelado</h1>
    <p style="color:#555">No se realizo ningun cargo. Puedes intentarlo de nuevo.</p>
    <a href="comeya://" style="display:inline-block;background:#FF6B35;color:white;padding:14px 28px;border-radius:50px;text-decoration:none;font-weight:700">Volver a ComeYa</a>
    </div></body></html>
  `);
});
router6.post(
  "/webhook/stripe",
  import_express6.default.raw({ type: "application/json" }),
  async (req, res) => {
    try {
      const { handleStripeWebhook: handleStripeWebhook2 } = await Promise.resolve().then(() => (init_webhookHandlers(), webhookHandlers_exports));
      return handleStripeWebhook2(req, res);
    } catch (error) {
      res.status(400).json({ error: error.message });
    }
  }
);
router6.post("/webhook/binance", async (req, res) => {
  try {
    const { bizType, data } = req.body;
    if (bizType === "PAY" && data?.merchantTradeNo) {
      const orderId = data.merchantTradeNo;
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      await db2.update(orders2).set({ status: "confirmed", updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm30.eq)(orders2.id, orderId));
    }
    res.json({ returnCode: "SUCCESS", returnMessage: null });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var payments_default = router6;

// server/routes/wallet.ts
var import_express7 = __toESM(require("express"));
var import_drizzle_orm31 = require("drizzle-orm");
var router7 = import_express7.default.Router();
router7.get("/", authenticateToken, async (req, res) => {
  try {
    const { wallets: wallets2, transactions: transactions4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    let [wallet] = await db2.select().from(wallets2).where((0, import_drizzle_orm31.eq)(wallets2.userId, req.user.id)).limit(1);
    if (!wallet) {
      const newWallet = {
        id: crypto.randomUUID(),
        userId: req.user.id,
        balance: 0,
        pendingBalance: 0,
        totalEarned: 0,
        createdAt: /* @__PURE__ */ new Date()
      };
      await db2.insert(wallets2).values(newWallet);
      wallet = newWallet;
    }
    const recentTransactions = await db2.select().from(transactions4).where((0, import_drizzle_orm31.eq)(transactions4.userId, req.user.id)).orderBy((0, import_drizzle_orm31.desc)(transactions4.createdAt)).limit(10);
    res.json({
      success: true,
      wallet: {
        ...wallet,
        balancePesos: wallet.balance / 100,
        pendingBalancePesos: wallet.pendingBalance / 100,
        totalEarnedPesos: wallet.totalEarned / 100
      },
      transactions: recentTransactions.map((t) => ({
        ...t,
        amountPesos: t.amount / 100
      }))
    });
  } catch (error) {
    console.error("Get wallet error:", error);
    res.status(500).json({ error: error.message });
  }
});
router7.get("/transactions", authenticateToken, async (req, res) => {
  try {
    const { transactions: transactions4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const userTransactions = await db2.select().from(transactions4).where((0, import_drizzle_orm31.eq)(transactions4.userId, req.user.id)).orderBy((0, import_drizzle_orm31.desc)(transactions4.createdAt));
    res.json({
      success: true,
      transactions: userTransactions.map((t) => ({
        ...t,
        amountPesos: t.amount / 100
      }))
    });
  } catch (error) {
    console.error("Get transactions error:", error);
    res.status(500).json({ error: error.message });
  }
});
router7.get("/balance", authenticateToken, async (req, res) => {
  try {
    const { wallets: wallets2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [wallet] = await db2.select().from(wallets2).where((0, import_drizzle_orm31.eq)(wallets2.userId, req.user.id)).limit(1);
    if (!wallet) {
      return res.json({
        success: true,
        balance: 0,
        balancePesos: 0,
        pendingBalance: 0,
        pendingBalancePesos: 0
      });
    }
    res.json({
      success: true,
      balance: wallet.balance,
      balancePesos: wallet.balance / 100,
      pendingBalance: wallet.pendingBalance,
      pendingBalancePesos: wallet.pendingBalance / 100
    });
  } catch (error) {
    console.error("Get balance error:", error);
    res.status(500).json({ error: error.message });
  }
});
router7.get("/withdrawals", authenticateToken, async (req, res) => {
  try {
    const { transactions: transactions4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const withdrawals2 = await db2.select().from(transactions4).where((0, import_drizzle_orm31.eq)(transactions4.userId, req.user.id)).orderBy((0, import_drizzle_orm31.desc)(transactions4.createdAt));
    const withdrawalTransactions = withdrawals2.filter(
      (t) => t.type === "withdrawal"
    );
    res.json({
      success: true,
      withdrawals: withdrawalTransactions.map((t) => ({
        ...t,
        amountPesos: Math.abs(t.amount) / 100
      }))
    });
  } catch (error) {
    console.error("Get withdrawals error:", error);
    res.status(500).json({ error: error.message });
  }
});
var wallet_default = router7;

// server/routes/adminRoutes.ts
var import_express8 = __toESM(require("express"));
var import_drizzle_orm34 = require("drizzle-orm");
var router8 = import_express8.default.Router();
router8.get(
  "/dashboard/metrics",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [[todayRow], [activeRow], [avgRow], [driverRow], [bizRow]] = await Promise.all([
        db2.execute(import_drizzle_orm34.sql`
              SELECT
                COUNT(*) AS orders_today,
                SUM(status = 'cancelled') AS cancelled_today,
                SUM(status = 'delivered') AS delivered_today,
                COALESCE(SUM(CASE WHEN status = 'delivered' THEN total ELSE 0 END), 0) AS revenue_today
              FROM orders WHERE created_at >= CURDATE()
            `).then((r) => r[0]),
        db2.execute(import_drizzle_orm34.sql`
              SELECT COUNT(*) AS active_orders FROM orders
              WHERE status IN (
                'pending','accepted','preparing','ready',
                'assigned_driver','picked_up','on_the_way','in_transit','arriving'
              )
            `).then((r) => r[0]),
        db2.execute(import_drizzle_orm34.sql`
              SELECT
                AVG(TIMESTAMPDIFF(MINUTE, created_at, delivered_at)) AS avg_total,
                AVG(TIMESTAMPDIFF(MINUTE, driver_picked_up_at, delivered_at)) AS avg_delivery
              FROM orders
              WHERE status = 'delivered' AND delivered_at IS NOT NULL
                AND delivered_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
                AND TIMESTAMPDIFF(MINUTE, created_at, delivered_at) BETWEEN 1 AND 240
            `).then((r) => r[0]),
        db2.execute(import_drizzle_orm34.sql`
              SELECT
                COUNT(*) AS total,
                SUM(
                  is_available = 1
                  AND last_location_update IS NOT NULL
                  AND last_location_update >= DATE_SUB(NOW(), INTERVAL 10 MINUTE)
                ) AS online
              FROM delivery_drivers
            `).then((r) => r[0]),
        db2.execute(import_drizzle_orm34.sql`
              SELECT
                COUNT(*) AS total,
                SUM(is_paused = 1 OR is_active = 0) AS paused
              FROM businesses
            `).then((r) => r[0])
      ]);
      const today = todayRow[0] || {};
      const active = activeRow[0] || {};
      const avg = avgRow[0] || {};
      const drv = driverRow[0] || {};
      const biz = bizRow[0] || {};
      const countSafe = async (query) => {
        try {
          const [result] = await db2.execute(query);
          const rows = result;
          return Number(rows?.[0]?.cnt) || 0;
        } catch {
          return 0;
        }
      };
      const [openIssues, openTickets, pendingPayments, pendingVerifications] = await Promise.all([
        countSafe(import_drizzle_orm34.sql`SELECT COUNT(*) AS cnt FROM order_issues WHERE status IN ('open', 'in_review')`),
        countSafe(import_drizzle_orm34.sql`SELECT COUNT(*) AS cnt FROM support_tickets WHERE status = 'open'`),
        countSafe(import_drizzle_orm34.sql`SELECT COUNT(*) AS cnt FROM payment_proofs WHERE status = 'pending'`),
        countSafe(import_drizzle_orm34.sql`SELECT COUNT(*) AS cnt FROM delivery_drivers WHERE verification_status = 'pending'`)
      ]);
      const ordersToday = Number(today.orders_today) || 0;
      const cancelledToday = Number(today.cancelled_today) || 0;
      const onlineDrivers = Number(drv.online) || 0;
      const avgDeliveryTime = avg.avg_delivery != null ? Math.round(Number(avg.avg_delivery)) : avg.avg_total != null ? Math.round(Number(avg.avg_total)) : null;
      res.json({
        activeOrders: Number(active.active_orders) || 0,
        ordersToday,
        onlineDrivers,
        todayOrders: ordersToday,
        todayRevenue: Number(today.revenue_today) || 0,
        deliveredToday: Number(today.delivered_today) || 0,
        cancelledToday,
        cancellationRate: ordersToday > 0 ? (cancelledToday / ordersToday * 100).toFixed(1) + "%" : "0%",
        avgDeliveryTime,
        avgTotalTime: avg.avg_total != null ? Math.round(Number(avg.avg_total)) : null,
        driversOnline: onlineDrivers,
        totalDrivers: Number(drv.total) || 0,
        pausedBusinesses: Number(biz.paused) || 0,
        totalBusinesses: Number(biz.total) || 0,
        // Badges del sidebar (antes siempre a 0: nadie los calculaba)
        pendingOrders: Number(active.active_orders) || 0,
        openIssues,
        openTickets,
        pendingPayments,
        pendingVerifications,
        timeframe: "hoy",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/dashboard/active-orders",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [rows] = await db2.execute(import_drizzle_orm34.sql`
        SELECT
          o.id, o.status, o.total, o.created_at, o.payment_method, o.order_type,
          o.delivery_latitude, o.delivery_longitude, o.delivery_address,
          o.delivery_person_id,
          u.id AS customer_id, u.name AS customer_name, u.phone AS customer_phone,
          b.id AS business_id, b.name AS business_name,
          b.latitude AS business_lat, b.longitude AS business_lng,
          du.name AS driver_name,
          dd.vehicle_type AS driver_vehicle,
          dd.is_available AS driver_available,
          dd.current_latitude AS driver_lat,
          dd.current_longitude AS driver_lng,
          dd.last_location_update AS driver_last_update
        FROM orders o
        LEFT JOIN users u ON o.user_id = u.id
        LEFT JOIN businesses b ON o.business_id = b.id
        LEFT JOIN delivery_drivers dd ON o.delivery_person_id = dd.user_id
        LEFT JOIN users du ON dd.user_id = du.id
        WHERE o.status IN (
          'pending','accepted','preparing','ready',
          'assigned_driver','picked_up','on_the_way','in_transit','arriving'
        )
        ORDER BY o.created_at DESC
        LIMIT 200
      `);
      const ordersWithDetails = rows.map((r) => ({
        id: r.id,
        status: r.status,
        total: Number(r.total) || 0,
        createdAt: r.created_at,
        paymentMethod: r.payment_method,
        orderType: r.order_type,
        customer: {
          id: r.customer_id || "",
          name: r.customer_name || "Cliente",
          phone: r.customer_phone || null
        },
        business: {
          id: r.business_id || "",
          name: r.business_name || "Negocio",
          latitude: r.business_lat,
          longitude: r.business_lng
        },
        deliveryAddress: {
          latitude: r.delivery_latitude,
          longitude: r.delivery_longitude,
          address: r.delivery_address || "Direcci\xF3n no disponible"
        },
        driver: r.delivery_person_id ? {
          id: r.delivery_person_id,
          name: r.driver_name || "Repartidor",
          isOnline: !!r.driver_available,
          vehicleType: r.driver_vehicle ?? null,
          currentLatitude: r.driver_lat ?? null,
          currentLongitude: r.driver_lng ?? null,
          lastLocationUpdate: r.driver_last_update ?? null
        } : null
      }));
      res.json({ orders: ordersWithDetails });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/dashboard/online-drivers",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const onlyOnline = req.query.onlyOnline === "true";
      const [rows] = await db2.execute(import_drizzle_orm34.sql`
        SELECT
          dd.user_id AS id, u.name, u.phone,
          dd.vehicle_type, dd.vehicle_plate, dd.rating, dd.total_deliveries,
          dd.is_available, dd.is_blocked,
          dd.current_latitude, dd.current_longitude, dd.last_location_update,
          u.last_active_at,
          (
            SELECT o.id FROM orders o
            WHERE o.delivery_person_id = dd.user_id
              AND o.status IN ('assigned_driver','picked_up','on_the_way','in_transit','arriving','ready')
            ORDER BY o.created_at DESC LIMIT 1
          ) AS active_order_id
        FROM delivery_drivers dd
        LEFT JOIN users u ON dd.user_id = u.id
        ORDER BY dd.is_available DESC, dd.last_location_update DESC
      `);
      const drivers3 = rows.map((r) => {
        const lastUpdate = r.last_location_update ? new Date(r.last_location_update).getTime() : null;
        const lastUpdateMinutes = lastUpdate != null ? Math.max(0, Math.round((Date.now() - lastUpdate) / 6e4)) : null;
        const isOnline = !!r.is_available && lastUpdateMinutes != null && lastUpdateMinutes < 10;
        return {
          id: r.id,
          name: r.name || "Repartidor",
          phone: r.phone || null,
          isOnline,
          isAvailable: !!r.is_available,
          isBlocked: !!r.is_blocked,
          lastActiveAt: r.last_active_at,
          // Formato plano (el mapa nativo lo consume así) + objeto anidado
          // por compatibilidad con consumidores antiguos
          currentLatitude: r.current_latitude ?? null,
          currentLongitude: r.current_longitude ?? null,
          location: r.current_latitude && r.current_longitude ? {
            latitude: r.current_latitude,
            longitude: r.current_longitude
          } : null,
          lastLocationUpdate: r.last_location_update ?? null,
          lastUpdateMinutes,
          staleGps: lastUpdateMinutes == null || lastUpdateMinutes >= 10,
          vehicleType: r.vehicle_type ?? null,
          vehiclePlate: r.vehicle_plate ?? null,
          rating: r.rating ? (r.rating / 10).toFixed(1) : null,
          totalDeliveries: Number(r.total_deliveries) || 0,
          activeOrder: r.active_order_id || null
        };
      });
      res.json({
        drivers: onlyOnline ? drivers3.filter((d) => d.isOnline) : drivers3
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/users",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const allUsers = await db2.select().from(users5);
      res.json({ success: true, users: allUsers });
    } catch (error) {
      console.error("Users endpoint error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.put(
  "/users/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { name, email, phone, role, isActive } = req.body;
      const userId = req.params.id;
      const updates = {};
      if (name !== void 0) updates.name = name;
      if (email !== void 0) updates.email = email;
      if (phone !== void 0) updates.phone = phone;
      if (role !== void 0) updates.role = role;
      if (isActive !== void 0) updates.isActive = isActive;
      await db2.update(users5).set(updates).where(eq79(users5.id, userId));
      res.json({ success: true, message: "Usuario actualizado correctamente" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/orders",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { orders: orders2, businesses: businesses3, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, desc: desc19, inArray: inArray12 } = await import("drizzle-orm");
      const allOrders = await db2.select().from(orders2).orderBy(desc19(orders2.createdAt)).limit(500);
      const businessIds = [
        ...new Set(allOrders.map((o) => o.businessId).filter(Boolean))
      ];
      const userIds = [
        ...new Set(allOrders.map((o) => o.userId).filter(Boolean))
      ];
      const businessRows = businessIds.length ? await db2.select({ id: businesses3.id, name: businesses3.name }).from(businesses3).where(inArray12(businesses3.id, businessIds)) : [];
      const userRows = userIds.length ? await db2.select({ id: users5.id, name: users5.name, phone: users5.phone }).from(users5).where(inArray12(users5.id, userIds)) : [];
      const businessMap = new Map(businessRows.map((b) => [b.id, b.name]));
      const userMap = new Map(userRows.map((u) => [u.id, u]));
      const enrichedOrders = allOrders.map((order) => ({
        id: order.id,
        userId: order.userId,
        businessId: order.businessId,
        businessName: businessMap.get(order.businessId) || order.businessName || "Negocio",
        businessImage: order.businessImage,
        customerName: userMap.get(order.userId)?.name || "Cliente",
        customerPhone: userMap.get(order.userId)?.phone || "",
        status: order.status,
        subtotal: order.subtotal,
        deliveryFee: order.deliveryFee,
        total: order.total,
        paymentMethod: order.paymentMethod,
        deliveryAddress: order.deliveryAddress,
        items: order.items,
        notes: order.notes,
        createdAt: order.createdAt,
        deliveredAt: order.deliveredAt,
        deliveryPersonId: order.deliveryPersonId
      }));
      res.json({ success: true, orders: enrichedOrders });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/businesses/:id/products",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const businessProducts = await db2.select().from(products3).where(eq79(products3.businessId, req.params.id));
      res.json({ success: true, products: businessProducts });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/businesses",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, desc: desc19 } = await import("drizzle-orm");
      const rows = await db2.select({
        business: businesses3,
        owner: { id: users5.id, name: users5.name }
      }).from(businesses3).leftJoin(users5, eq79(businesses3.ownerId, users5.id)).orderBy(desc19(businesses3.createdAt));
      const all = rows.map((r) => ({
        ...r.business,
        ownerName: r.owner?.name ?? null
      }));
      res.json({ success: true, businesses: all });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router8.put(
  "/businesses/:id/commission",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { customCommission } = req.body;
      const val = customCommission === null || customCommission === void 0 ? null : parseInt(String(customCommission));
      if (val !== null && (isNaN(val) || val < 0 || val > 100)) {
        return res.status(400).json({ success: false, error: "Comisi\xF3n debe ser entre 0 y 100" });
      }
      await db2.update(businesses3).set({ customCommission: val }).where(eq79(businesses3.id, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router8.put(
  "/businesses/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { name, address, phone, type, isActive, customCommission } = req.body;
      await db2.update(businesses3).set({
        ...name !== void 0 && { name },
        ...address !== void 0 && { address },
        ...phone !== void 0 && { phone },
        ...type !== void 0 && { type },
        ...isActive !== void 0 && { isActive },
        // null = usa comision global, numero = comision especifica
        ...customCommission !== void 0 && {
          customCommission: customCommission === null ? null : parseInt(customCommission)
        }
      }).where(eq79(businesses3.id, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router8.get(
  "/zones",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { deliveryZones: deliveryZones2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const zones = await db2.select().from(deliveryZones2);
      res.json({ success: true, zones });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.put(
  "/zones/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { deliveryZones: deliveryZones2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { name, description, isActive } = req.body;
      const updates = {};
      if (name !== void 0) updates.name = name;
      if (description !== void 0) updates.description = description;
      if (isActive !== void 0) updates.isActive = isActive;
      await db2.update(deliveryZones2).set(updates).where(eq79(deliveryZones2.id, req.params.id));
      res.json({ success: true, message: "Zona actualizada" });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router8.get(
  "/delivery-zones",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { deliveryZones: deliveryZones2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const zones = await db2.select().from(deliveryZones2);
      res.json({
        success: true,
        zones
      });
    } catch (error) {
      console.error("Delivery zones error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/drivers",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, deliveryDrivers: deliveryDrivers3, payouts: payouts2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, and: and47 } = await import("drizzle-orm");
      const driverUsers = await db2.select().from(users5).where(eq79(users5.role, "delivery_driver"));
      const enriched = await Promise.all(
        driverUsers.map(async (u) => {
          const [dd] = await db2.select().from(deliveryDrivers3).where(eq79(deliveryDrivers3.userId, u.id)).limit(1);
          const pendingPays = await db2.select().from(payouts2).where(
            and47(
              eq79(payouts2.recipientId, u.id),
              eq79(payouts2.status, "pending")
            )
          );
          const pendingAmount = pendingPays.reduce(
            (s, p) => s + Number(p.amount || 0),
            0
          );
          return {
            id: u.id,
            name: u.name,
            email: u.email,
            phone: u.phone,
            isOnline: u.isOnline,
            isActive: u.isActive,
            isBlocked: dd?.isBlocked ?? false,
            blockedReason: dd?.blockedReason ?? null,
            strikes: dd?.strikes ?? 0,
            totalDeliveries: dd?.totalDeliveries ?? 0,
            rating: dd?.rating ?? null,
            vehicleType: dd?.vehicleType ?? null,
            vehiclePlate: dd?.vehiclePlate ?? null,
            currentLatitude: dd?.currentLatitude ?? null,
            currentLongitude: dd?.currentLongitude ?? null,
            createdAt: u.createdAt,
            lastActiveAt: u.lastActiveAt,
            pendingPayouts: pendingPays.length,
            pendingAmount
          };
        })
      );
      res.json({ success: true, drivers: enriched });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.post(
  "/drivers/:id/block",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { reason } = req.body;
      await db2.update(deliveryDrivers3).set({
        isBlocked: true,
        blockedReason: reason ?? "Bloqueado por admin"
      }).where(eq79(deliveryDrivers3.userId, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.post(
  "/drivers/:id/unblock",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      await db2.update(deliveryDrivers3).set({ isBlocked: false, blockedReason: null }).where(eq79(deliveryDrivers3.userId, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.post(
  "/drivers/:id/strike",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { reason } = req.body;
      if (!reason?.trim())
        return res.status(400).json({ error: "La raz\xF3n del strike es requerida" });
      const { addStrike: addStrike2 } = await Promise.resolve().then(() => (init_strikeService(), strikeService_exports));
      await addStrike2(req.params.id, reason.trim());
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.delete(
  "/drivers/:id/strike",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { removeStrike: removeStrike2 } = await Promise.resolve().then(() => (init_strikeService(), strikeService_exports));
      await removeStrike2(req.params.id);
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get("/debug/wallets-noauth", async (req, res) => {
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const result = await db2.execute(import_drizzle_orm34.sql`
      SELECT 
        w.id, w.user_id, w.balance, w.pending_balance, w.total_earned, w.total_withdrawn,
        u.name, u.email, u.role, u.phone
      FROM wallets w 
      LEFT JOIN users u ON w.user_id = u.id 
      ORDER BY w.total_earned DESC
    `);
    res.json({ success: true, wallets: result });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router8.get(
  "/debug/wallets",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const result = await db2.execute(import_drizzle_orm34.sql`
      SELECT 
        w.id, w.userId, w.balance, w.pendingBalance, w.totalEarned, w.totalWithdrawn,
        u.name, u.email, u.role, u.phone
      FROM wallets w 
      LEFT JOIN users u ON w.userId = u.id 
      ORDER BY w.totalEarned DESC
    `);
      res.json({ success: true, wallets: result });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/wallets",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const result = await db2.execute(import_drizzle_orm34.sql`
      SELECT 
        w.id, w.user_id as userId, w.balance, w.pending_balance as pendingBalance, 
        w.total_earned as totalEarned, w.total_withdrawn as totalWithdrawn,
        u.id as user_id, u.name as user_name, u.phone as user_phone, u.role as user_role
      FROM wallets w 
      LEFT JOIN users u ON w.user_id = u.id
    `);
      const rows = Array.isArray(result[0]) ? result[0] : result;
      const walletsWithUsers = rows.map((row) => ({
        id: row.id,
        userId: row.userId,
        balance: row.balance || 0,
        pendingBalance: row.pendingBalance || 0,
        totalEarned: row.totalEarned || 0,
        totalWithdrawn: row.totalWithdrawn || 0,
        user: row.user_name ? {
          id: row.user_id,
          name: row.user_name,
          phone: row.user_phone,
          role: row.user_role
        } : null
      }));
      res.json({ success: true, wallets: walletsWithUsers });
    } catch (error) {
      console.error("Wallets error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.post(
  "/wallets/:walletId/release",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { wallets: wallets2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const [wallet] = await db2.select().from(wallets2).where(eq79(wallets2.id, req.params.walletId)).limit(1);
      if (!wallet) {
        return res.status(404).json({ error: "Wallet not found" });
      }
      if (wallet.pendingBalance <= 0) {
        return res.status(400).json({ error: "No pending balance to release" });
      }
      await db2.update(wallets2).set({
        balance: wallet.balance + wallet.pendingBalance,
        pendingBalance: 0
      }).where(eq79(wallets2.id, req.params.walletId));
      res.json({ success: true, message: "Balance released successfully" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/finance",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { transactions: transactions4, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, desc: desc19 } = await import("drizzle-orm");
      const allTransactions = await db2.select().from(transactions4).orderBy(desc19(transactions4.createdAt));
      const enrichedTransactions = [];
      for (const transaction of allTransactions) {
        const user = await db2.select({
          id: users5.id,
          name: users5.name,
          email: users5.email,
          role: users5.role
        }).from(users5).where(eq79(users5.id, transaction.userId)).limit(1);
        enrichedTransactions.push({
          id: transaction.id,
          type: transaction.type,
          amount: transaction.amount,
          description: transaction.description,
          createdAt: transaction.createdAt,
          userId: transaction.userId,
          userName: user[0]?.name || "Usuario desconocido",
          userEmail: user[0]?.email || "",
          userRole: user[0]?.role || ""
        });
      }
      res.json({
        success: true,
        transactions: enrichedTransactions
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/coupons",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      res.json({ success: true, coupons: [] });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/support/tickets",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { supportTickets: supportTickets2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { desc: desc19, eq: eq79 } = await import("drizzle-orm");
      const tickets = await db2.select({
        id: supportTickets2.id,
        userId: supportTickets2.userId,
        orderId: supportTickets2.orderId,
        subject: supportTickets2.subject,
        category: supportTickets2.category,
        priority: supportTickets2.priority,
        status: supportTickets2.status,
        createdAt: supportTickets2.createdAt,
        userName: users5.name,
        userPhone: users5.phone
      }).from(supportTickets2).leftJoin(users5, eq79(users5.id, supportTickets2.userId)).orderBy(desc19(supportTickets2.createdAt)).limit(200);
      res.json({ success: true, tickets });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/support",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { supportTickets: supportTickets2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { desc: desc19, eq: eq79 } = await import("drizzle-orm");
      const tickets = await db2.select({
        id: supportTickets2.id,
        userId: supportTickets2.userId,
        orderId: supportTickets2.orderId,
        subject: supportTickets2.subject,
        category: supportTickets2.category,
        priority: supportTickets2.priority,
        status: supportTickets2.status,
        createdAt: supportTickets2.createdAt,
        userName: users5.name,
        userPhone: users5.phone
      }).from(supportTickets2).leftJoin(users5, eq79(users5.id, supportTickets2.userId)).orderBy(desc19(supportTickets2.createdAt)).limit(200);
      res.json({ success: true, tickets });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/logs",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const result = await db2.execute(import_drizzle_orm34.sql`
      SELECT 
        id,
        user_id as userId,
        action,
        entity_type as entityType,
        entity_id as entityId,
        changes,
        ip_address as ipAddress,
        user_agent as userAgent,
        created_at as createdAt
      FROM audit_logs
      ORDER BY created_at DESC
      LIMIT 100
    `);
      const rows = Array.isArray(result[0]) ? result[0] : result;
      res.json({ success: true, logs: rows });
    } catch (error) {
      console.error("Admin logs error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/settings",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { systemSettings: systemSettings2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const settings = await db2.select().from(systemSettings2);
      res.json({ success: true, settings });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.put(
  "/settings",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { systemSettings: systemSettings2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { key, value } = req.body;
      if (!key || value === void 0) {
        return res.status(400).json({ error: "Key and value are required" });
      }
      const [existing] = await db2.select().from(systemSettings2).where(eq79(systemSettings2.key, key)).limit(1);
      if (existing) {
        await db2.update(systemSettings2).set({ value: String(value), updatedBy: req.user.id }).where(eq79(systemSettings2.key, key));
      } else {
        await db2.insert(systemSettings2).values({
          key,
          value: String(value),
          type: typeof value === "number" ? "number" : "string",
          category: "operations",
          updatedBy: req.user.id
        });
      }
      res.json({ success: true, message: "Setting updated successfully" });
    } catch (error) {
      console.error("Settings update error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var adminRoutes_default = router8;
router8.get(
  "/verifications/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, deliveryDrivers: deliveryDrivers3, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { inArray: inArray12, eq: eq79 } = await import("drizzle-orm");
      const pending = await db2.select().from(users5).where(
        inArray12(users5.role, ["delivery_driver", "business_owner"])
      );
      const enriched = await Promise.all(
        pending.map(async (user) => {
          let deliveryDriver = null;
          let business = null;
          if (user.role === "delivery_driver") {
            const [dd] = await db2.select().from(deliveryDrivers3).where(eq79(deliveryDrivers3.userId, user.id)).limit(1);
            deliveryDriver = dd || null;
          }
          if (user.role === "business_owner") {
            const [biz] = await db2.select().from(businesses3).where(eq79(businesses3.ownerId, user.id)).limit(1);
            business = biz || null;
          }
          return {
            ...user,
            deliveryDriver,
            business
          };
        })
      );
      res.json({ success: true, users: enriched });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.put(
  "/verifications/:userId",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, auditLogs: auditLogs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { action, notes } = req.body;
      const userId = req.params.userId;
      if (!action || !["approve", "reject"].includes(action)) {
        return res.status(400).json({ error: "action debe ser 'approve' o 'reject'" });
      }
      const verificationStatus = action === "approve" ? "verified" : "rejected";
      const isActive = action === "approve";
      if (action === "approve") {
        const [targetUser] = await db2.select().from(users5).where(eq79(users5.id, userId)).limit(1);
        const missing = [];
        if (!targetUser?.idDocumentUrl)
          missing.push("DNI/NIE (anverso)");
        if (!targetUser?.idDocumentBackUrl)
          missing.push("DNI/NIE (reverso)");
        if (targetUser?.role === "delivery_driver") {
          const { deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
          const [dd] = await db2.select().from(deliveryDrivers3).where(eq79(deliveryDrivers3.userId, userId)).limit(1);
          if (!dd?.vehicleLicensePhoto)
            missing.push("Permiso de circulaci\xF3n");
          if (!dd?.vehiclePhoto)
            missing.push("Foto del veh\xEDculo");
        }
        if (targetUser?.role === "business_owner") {
          if (!targetUser?.autonomoDocumentUrl)
            missing.push("Documento de aut\xF3nomo/empresa");
        }
        if (missing.length > 0) {
          return res.status(400).json({
            error: "El usuario no ha subido todos los documentos requeridos: " + missing.join(", ")
          });
        }
      }
      await db2.update(users5).set({
        verificationStatus,
        verificationNotes: notes || null,
        isActive
      }).where(eq79(users5.id, userId));
      await db2.insert(auditLogs2).values({
        userId: req.user.id,
        action: action === "approve" ? "verify_user_approved" : "verify_user_rejected",
        entityType: "user",
        entityId: userId,
        changes: JSON.stringify({ verificationStatus, notes })
      });
      try {
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        await sendPushToUser2(userId, {
          title: action === "approve" ? "\u2705 Cuenta verificada" : "\u274C Verificaci\xF3n rechazada",
          body: action === "approve" ? "Tu cuenta ha sido verificada. Ya puedes usar ComeYa." : `Tu verificaci\xF3n fue rechazada. ${notes || "Contacta con soporte para m\xE1s informaci\xF3n."}`,
          data: { screen: "Profile" }
        });
      } catch {
      }
      res.json({ success: true, verificationStatus });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/profile/stats",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const adminId = req.user.id;
      const result = await db2.execute(import_drizzle_orm34.sql`
      SELECT
        COUNT(*) as totalActions,
        SUM(action LIKE 'verify%') as verificationsProcessed,
        SUM(entity_type = 'payout') as payoutsProcessed,
        SUM(entity_type = 'payment_proof') as proofsReviewed,
        MAX(created_at) as lastActionAt
      FROM audit_logs
      WHERE user_id = ${adminId}
    `);
      const rows = Array.isArray(result[0]) ? result[0] : result;
      const stats = rows[0] || {};
      const pending = await db2.execute(import_drizzle_orm34.sql`
      SELECT
        (SELECT COUNT(*) FROM payment_proofs WHERE status = 'pending') as pendingProofs,
        (SELECT COUNT(*) FROM payouts WHERE status = 'pending') as pendingPayouts,
        (SELECT COUNT(*) FROM orders WHERE status IN ('pending','confirmed','preparing','on_the_way')) as activeOrders
    `);
      const pendingRows = Array.isArray(pending[0]) ? pending[0] : pending;
      const pendingData = pendingRows[0] || {};
      res.json({
        success: true,
        stats: {
          totalActions: Number(stats.totalActions) || 0,
          verificationsProcessed: Number(stats.verificationsProcessed) || 0,
          payoutsProcessed: Number(stats.payoutsProcessed) || 0,
          proofsReviewed: Number(stats.proofsReviewed) || 0,
          lastActionAt: stats.lastActionAt || null
        },
        pending: {
          proofs: Number(pendingData.pendingProofs) || 0,
          payouts: Number(pendingData.pendingPayouts) || 0,
          orders: Number(pendingData.activeOrders) || 0
        }
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/bank-account",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      res.json({
        success: true,
        bankAccount: null,
        message: "No bank account configured"
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/gift-cards/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { GiftCardService: GiftCardService2 } = await Promise.resolve().then(() => (init_giftCardService(), giftCardService_exports));
      res.json(await GiftCardService2.getPendingGiftCards());
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router8.post(
  "/gift-cards/:id/activate",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { GiftCardService: GiftCardService2 } = await Promise.resolve().then(() => (init_giftCardService(), giftCardService_exports));
      res.json(
        await GiftCardService2.activateGiftCard(
          req.params.id,
          req.user.id
        )
      );
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router8.post(
  "/gift-cards/:id/reject",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { reason } = req.body;
      if (!reason)
        return res.status(400).json({ success: false, error: "Raz\xF3n requerida" });
      const { GiftCardService: GiftCardService2 } = await Promise.resolve().then(() => (init_giftCardService(), giftCardService_exports));
      res.json(
        await GiftCardService2.rejectGiftCard(
          req.params.id,
          req.user.id,
          reason
        )
      );
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router8.get(
  "/delivery-verifications/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, or: or8, asc } = await import("drizzle-orm");
      const pendingDrivers = await db2.select().from(users5).where(
        or8(
          eq79(users5.role, "delivery_driver"),
          eq79(users5.verificationStatus, "pending")
        )
      ).orderBy(asc(users5.createdAt));
      const enriched = await Promise.all(
        pendingDrivers.map(async (user) => {
          const [dd] = await db2.select().from(deliveryDrivers3).where(eq79(deliveryDrivers3.userId, user.id)).limit(1);
          return {
            id: user.id,
            name: user.name,
            phone: user.phone,
            email: user.email,
            verificationStatus: user.verificationStatus,
            isActive: user.isActive,
            createdAt: user.createdAt,
            vehicleInfo: {
              vehicleType: dd?.vehicleType || null,
              vehiclePlate: dd?.vehiclePlate || null,
              vehicleBrand: dd?.vehicleBrand || null,
              vehicleModel: dd?.vehicleModel || null,
              vehicleColor: dd?.vehicleColor || null
            },
            documents: {
              idDocumentUrl: !!user.idDocumentUrl,
              idDocumentBackUrl: !!user.idDocumentBackUrl,
              autonomoDocumentUrl: !!user.autonomoDocumentUrl,
              vehiclePhoto: !!dd?.vehiclePhoto,
              vehiclePlatePhoto: !!dd?.vehiclePlatePhoto,
              vehicleItvPhoto: !!dd?.vehicleItvPhoto,
              vehicleInsurancePhoto: !!dd?.vehicleInsurancePhoto,
              vehicleLicensePhoto: !!dd?.vehicleLicensePhoto
            }
          };
        })
      );
      res.json({ success: true, drivers: enriched });
    } catch (error) {
      console.error("Error getting pending deliveries:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/delivery-verifications/:userId",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, deliveryDrivers: deliveryDrivers3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const userId = req.params.userId;
      const [user] = await db2.select().from(users5).where(eq79(users5.id, userId)).limit(1);
      if (!user) {
        return res.status(404).json({ error: "Usuario no encontrado" });
      }
      const [driver] = await db2.select().from(deliveryDrivers3).where(eq79(deliveryDrivers3.userId, user.id)).limit(1);
      res.json({
        success: true,
        driver: {
          id: user.id,
          name: user.name,
          phone: user.phone,
          email: user.email,
          verificationStatus: user.verificationStatus,
          verificationNotes: user.verificationNotes,
          isActive: user.isActive,
          createdAt: user.createdAt,
          vehicleInfo: {
            vehicleType: driver?.vehicleType || null,
            vehiclePlate: driver?.vehiclePlate || null,
            vehicleBrand: driver?.vehicleBrand || null,
            vehicleModel: driver?.vehicleModel || null,
            vehicleColor: driver?.vehicleColor || null,
            vehicleYear: driver?.vehicleYear || null
          },
          documents: {
            idDocumentUrl: user.idDocumentUrl,
            idDocumentBackUrl: user.idDocumentBackUrl,
            autonomoDocumentUrl: user.autonomoDocumentUrl,
            vehiclePhoto: driver?.vehiclePhoto || null,
            vehiclePlatePhoto: driver?.vehiclePlatePhoto || null,
            vehicleItvPhoto: driver?.vehicleItvPhoto || null,
            vehicleInsurancePhoto: driver?.vehicleInsurancePhoto || null,
            vehicleLicensePhoto: driver?.vehicleLicensePhoto || null
          }
        }
      });
    } catch (error) {
      console.error("Error getting delivery details:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.post(
  "/delivery-verifications/:userId/approve",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, auditLogs: auditLogs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const userId = req.params.userId;
      const adminId = req.user.id;
      const [user] = await db2.select().from(users5).where(eq79(users5.id, userId)).limit(1);
      if (!user) {
        return res.status(404).json({ error: "Usuario no encontrado" });
      }
      await db2.update(users5).set({
        verificationStatus: "verified",
        isActive: true,
        updatedAt: /* @__PURE__ */ new Date()
      }).where(eq79(users5.id, userId));
      await db2.insert(auditLogs2).values({
        id: crypto.randomUUID(),
        userId: adminId,
        action: "delivery_approved",
        entityType: "user",
        entityId: userId,
        changes: JSON.stringify({
          verificationStatus: "verified",
          isActive: true
        }),
        createdAt: /* @__PURE__ */ new Date()
      });
      try {
        const { deliveryNotificationService: deliveryNotificationService2 } = await Promise.resolve().then(() => (init_deliveryNotificationService(), deliveryNotificationService_exports));
        await deliveryNotificationService2.notifyApproved(userId);
      } catch {
      }
      res.json({
        success: true,
        message: `Repartidor ${user.name} aprobado correctamente`
      });
    } catch (error) {
      console.error("Error approving delivery:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.post(
  "/delivery-verifications/:userId/reject",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5, auditLogs: auditLogs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const userId = req.params.userId;
      const adminId = req.user.id;
      const { reason } = req.body;
      if (!reason?.trim()) {
        return res.status(400).json({ error: "Raz\xF3n de rechazo requerida" });
      }
      const [user] = await db2.select().from(users5).where(eq79(users5.id, userId)).limit(1);
      if (!user) {
        return res.status(404).json({ error: "Usuario no encontrado" });
      }
      await db2.update(users5).set({
        verificationStatus: "rejected",
        verificationNotes: reason.trim(),
        isActive: false,
        updatedAt: /* @__PURE__ */ new Date()
      }).where(eq79(users5.id, userId));
      await db2.insert(auditLogs2).values({
        id: crypto.randomUUID(),
        userId: adminId,
        action: "delivery_rejected",
        entityType: "user",
        entityId: userId,
        changes: JSON.stringify({
          verificationStatus: "rejected",
          rejectionReason: reason.trim()
        }),
        createdAt: /* @__PURE__ */ new Date()
      });
      try {
        const { deliveryNotificationService: deliveryNotificationService2 } = await Promise.resolve().then(() => (init_deliveryNotificationService(), deliveryNotificationService_exports));
        await deliveryNotificationService2.notifyRejected(userId, reason.trim());
      } catch {
      }
      res.json({
        success: true,
        message: `Repartidor ${user.name} rechazado`
      });
    } catch (error) {
      console.error("Error rejecting delivery:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.post(
  "/delivery-verifications/:userId/reset",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const userId = req.params.userId;
      const [user] = await db2.select().from(users5).where(eq79(users5.id, userId)).limit(1);
      if (!user) {
        return res.status(404).json({ error: "Usuario no encontrado" });
      }
      await db2.update(users5).set({
        verificationStatus: "pending",
        verificationNotes: null,
        isActive: false,
        updatedAt: /* @__PURE__ */ new Date()
      }).where(eq79(users5.id, userId));
      res.json({
        success: true,
        message: "Verificaci\xF3n reiniciada. El repartidor puede enviar documentos nuevamente."
      });
    } catch (error) {
      console.error("Error resetting verification:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router8.get(
  "/delivery-verifications/stats",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, and: and47 } = await import("drizzle-orm");
      const [pendingCount] = await db2.select({ count: users5.id }).from(users5).where(
        and47(
          eq79(users5.role, "delivery_driver"),
          eq79(users5.verificationStatus, "pending")
        )
      );
      const [verifiedCount] = await db2.select({ count: users5.id }).from(users5).where(
        and47(
          eq79(users5.role, "delivery_driver"),
          eq79(users5.verificationStatus, "verified")
        )
      );
      const [rejectedCount] = await db2.select({ count: users5.id }).from(users5).where(
        and47(
          eq79(users5.role, "delivery_driver"),
          eq79(users5.verificationStatus, "rejected")
        )
      );
      const [activeDrivers] = await db2.select({ count: users5.id }).from(users5).where(
        and47(eq79(users5.role, "delivery_driver"), eq79(users5.isActive, true))
      );
      res.json({
        success: true,
        stats: {
          pending: Number(pendingCount?.count || 0),
          verified: Number(verifiedCount?.count || 0),
          rejected: Number(rejectedCount?.count || 0),
          active: Number(activeDrivers?.count || 0)
        }
      });
    } catch (error) {
      console.error("Error getting verification stats:", error);
      res.status(500).json({ error: error.message });
    }
  }
);

// server/routes/adminFinanceRoutes.ts
var import_express9 = __toESM(require("express"));
init_orderNumberService();
var router9 = import_express9.default.Router();
router9.get(
  "/platform-earnings",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { orders: orders2, transactions: transactions4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const allOrders = await db2.select().from(orders2);
      const today = /* @__PURE__ */ new Date();
      today.setHours(0, 0, 0, 0);
      const weekAgo = new Date(today);
      weekAgo.setDate(weekAgo.getDate() - 7);
      const monthStart = new Date(today.getFullYear(), today.getMonth(), 1);
      const deliveredOrders = allOrders.filter((o) => o.status === "delivered");
      const todayEarnings = deliveredOrders.filter((o) => new Date(o.createdAt) >= today).reduce((sum3, o) => sum3 + (o.nemyCommission || 0), 0);
      const weekEarnings = deliveredOrders.filter((o) => new Date(o.createdAt) >= weekAgo).reduce((sum3, o) => sum3 + (o.nemyCommission || 0), 0);
      const monthEarnings = deliveredOrders.filter((o) => new Date(o.createdAt) >= monthStart).reduce((sum3, o) => sum3 + (o.nemyCommission || 0), 0);
      const totalEarnings = deliveredOrders.reduce(
        (sum3, o) => sum3 + (o.nemyCommission || 0),
        0
      );
      const allTransactions = await db2.select().from(transactions4);
      const penalties = allTransactions.filter((t) => t.type === "penalty").reduce((sum3, t) => sum3 + t.amount, 0);
      const couponsApplied = allTransactions.filter((t) => t.type === "coupon_discount").reduce((sum3, t) => sum3 + Math.abs(t.amount), 0);
      const recentTransactions = deliveredOrders.sort(
        (a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime()
      ).slice(0, 50).map((order) => ({
        id: order.id,
        orderId: order.id,
        date: order.createdAt,
        amount: order.nemyCommission || 0,
        type: "commission",
        businessName: order.businessName,
        status: order.status
      }));
      res.json({
        success: true,
        earnings: {
          today: todayEarnings,
          week: weekEarnings,
          month: monthEarnings,
          total: totalEarnings
        },
        breakdown: {
          productMarkup: totalEarnings,
          deliveryCommission: 0,
          // ComeYa no cobra comisión de delivery
          businessCommission: 0,
          // ComeYa no cobra comisión a negocios
          penalties,
          couponsApplied: -couponsApplied,
          netTotal: totalEarnings + penalties - couponsApplied
        },
        transactions: recentTransactions,
        stats: {
          totalOrders: deliveredOrders.length,
          avgCommissionPerOrder: deliveredOrders.length > 0 ? Math.round(totalEarnings / deliveredOrders.length) : 0,
          conversionRate: allOrders.length > 0 ? (deliveredOrders.length / allOrders.length * 100).toFixed(1) : "0.0"
        }
      });
    } catch (error) {
      console.error("Platform earnings error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router9.get(
  "/stripe-status",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const stripeConfigured = !!process.env.STRIPE_SECRET_KEY;
      if (!stripeConfigured) {
        return res.json({
          success: true,
          status: {
            isConnected: false,
            accountId: null,
            chargesEnabled: false,
            payoutsEnabled: false,
            requirements: ["Configurar Stripe Secret Key"],
            lastSync: null,
            balance: {
              available: 0,
              pending: 0
            }
          }
        });
      }
      try {
        const { getStripe: getStripe5 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
        const stripe2 = getStripe5();
        const account = await stripe2.accounts.retrieve();
        let balance = { available: 0, pending: 0 };
        try {
          const b = await stripe2.balance.retrieve();
          balance = {
            available: b.available?.[0]?.amount || 0,
            pending: b.pending?.[0]?.amount || 0
          };
        } catch (balanceError) {
          console.error("Stripe balance error:", balanceError);
        }
        res.json({
          success: true,
          status: {
            isConnected: true,
            accountId: account.id || null,
            accountEmail: account.email || null,
            accountName: account.settings?.dashboard?.display_name || account.business_profile?.name || "ComeYa",
            chargesEnabled: account.charges_enabled ?? true,
            payoutsEnabled: account.payouts_enabled ?? true,
            requirements: [],
            lastSync: (/* @__PURE__ */ new Date()).toISOString(),
            balance
          }
        });
      } catch (stripeError) {
        console.error("Stripe API error:", stripeError);
        res.json({
          success: true,
          status: {
            isConnected: !!process.env.STRIPE_SECRET_KEY,
            accountId: null,
            chargesEnabled: false,
            payoutsEnabled: false,
            requirements: ["No se pudo conectar con la API de Stripe"],
            lastSync: (/* @__PURE__ */ new Date()).toISOString(),
            balance: { available: 0, pending: 0 },
            error: stripeError?.message || "No se pudo conectar con Stripe API"
          }
        });
      }
    } catch (error) {
      console.error("Stripe status error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router9.get(
  "/top-businesses",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const allOrders = await db2.select().from(orders2);
      const deliveredOrders = allOrders.filter((o) => o.status === "delivered");
      const businessEarnings = /* @__PURE__ */ new Map();
      for (const order of deliveredOrders) {
        const current = businessEarnings.get(order.businessId) || {
          name: order.businessName,
          total: 0,
          orders: 0
        };
        current.total += order.nemyCommission || 0;
        current.orders += 1;
        businessEarnings.set(order.businessId, current);
      }
      const topBusinesses = Array.from(businessEarnings.entries()).map(([id, data]) => ({
        businessId: id,
        businessName: data.name,
        totalCommissions: data.total,
        totalOrders: data.orders,
        avgCommissionPerOrder: Math.round(data.total / data.orders)
      })).sort((a, b) => b.totalCommissions - a.totalCommissions).slice(0, 10);
      res.json({
        success: true,
        topBusinesses
      });
    } catch (error) {
      console.error("Top businesses error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router9.get(
  "/earnings-chart",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const days = parseInt(req.query.days) || 30;
      const allOrders = await db2.select().from(orders2);
      const deliveredOrders = allOrders.filter((o) => o.status === "delivered");
      const startDate = /* @__PURE__ */ new Date();
      startDate.setDate(startDate.getDate() - days);
      const dailyEarnings = /* @__PURE__ */ new Map();
      for (const order of deliveredOrders) {
        const orderDate = new Date(order.createdAt);
        if (orderDate >= startDate) {
          const dateKey = orderDate.toISOString().split("T")[0];
          const current = dailyEarnings.get(dateKey) || 0;
          dailyEarnings.set(dateKey, current + (order.nemyCommission || 0));
        }
      }
      const chartData = Array.from(dailyEarnings.entries()).map(([date, amount]) => ({ date, amount })).sort((a, b) => a.date.localeCompare(b.date));
      res.json({
        success: true,
        chartData
      });
    } catch (error) {
      console.error("Earnings chart error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router9.post(
  "/export-csv",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { startDate, endDate } = req.body;
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const allOrders = await db2.select().from(orders2);
      let filteredOrders = allOrders.filter((o) => o.status === "delivered");
      if (startDate) {
        filteredOrders = filteredOrders.filter(
          (o) => new Date(o.createdAt) >= new Date(startDate)
        );
      }
      if (endDate) {
        filteredOrders = filteredOrders.filter(
          (o) => new Date(o.createdAt) <= new Date(endDate)
        );
      }
      const csvHeader = "Fecha,Pedido ID,Negocio,Comisi\xF3n (EUR),Estado\n";
      const csvRows = filteredOrders.map((order) => {
        const date = new Date(order.createdAt).toLocaleDateString("es-VE");
        const commission = ((order.nemyCommission || 0) / 100).toFixed(2);
        return `${date},${order.id},${order.businessName},${commission},${order.status}`;
      }).join("\n");
      const csv = csvHeader + csvRows;
      res.setHeader("Content-Type", "text/csv");
      res.setHeader(
        "Content-Disposition",
        `attachment; filename=comisiones_${Date.now()}.csv`
      );
      res.send(csv);
    } catch (error) {
      console.error("Export CSV error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router9.get(
  "/payouts/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { payouts: payouts2, users: users5, businesses: businesses3, orders: orders2, paymentAccounts: paymentAccounts2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const pending = await db2.select().from(payouts2).where(eq79(payouts2.status, "pending"));
      const enriched = await Promise.all(
        pending.map(async (p) => {
          let recipientName = "";
          let recipientUserId = p.recipientId;
          let businessName = null;
          const isWithdrawal = (p.orderId || "").startsWith("wdr-");
          if (p.recipientType === "business") {
            const [biz] = await db2.select({ name: businesses3.name, ownerId: businesses3.ownerId }).from(businesses3).where(eq79(businesses3.id, p.recipientId)).limit(1);
            if (biz) {
              recipientName = biz.name ?? "";
              businessName = biz.name ?? null;
              if (biz.ownerId) recipientUserId = biz.ownerId;
            } else if (isWithdrawal) {
              const [usr] = await db2.select({ name: users5.name }).from(users5).where(eq79(users5.id, p.recipientId)).limit(1);
              recipientName = usr?.name ?? "";
            }
          } else {
            const [usr] = await db2.select({ name: users5.name }).from(users5).where(eq79(users5.id, p.recipientId)).limit(1);
            recipientName = usr?.name ?? "";
          }
          const accounts = await db2.select().from(paymentAccounts2).where(eq79(paymentAccounts2.userId, recipientUserId));
          const [order] = isWithdrawal ? [null] : await db2.select({
            paymentMethod: orders2.paymentMethod,
            assignedAt: orders2.assignedAt,
            deliveredAt: orders2.deliveredAt,
            total: orders2.total,
            businessName: orders2.businessName
          }).from(orders2).where(eq79(orders2.id, p.orderId)).limit(1);
          let deliveryMinutes = null;
          if (order?.assignedAt && order?.deliveredAt) {
            deliveryMinutes = Math.round(
              (new Date(order.deliveredAt).getTime() - new Date(order.assignedAt).getTime()) / 6e4
            );
          }
          return {
            ...p,
            recipientName,
            isWithdrawal,
            paymentAccounts: accounts,
            paymentMethod: order?.paymentMethod ?? null,
            deliveryMinutes,
            orderTotal: order?.total ?? null,
            businessName: order?.businessName ?? businessName ?? null
          };
        })
      );
      res.json({ success: true, payouts: enriched });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router9.post(
  "/payouts/fix-amounts",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { payouts: payouts2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const all = await db2.select().from(payouts2);
      const fixed = 0;
      res.json({ success: true, fixed, total: all.length });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router9.post(
  "/payouts/backfill",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { orders: orders2, payouts: payouts2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, and: and47 } = await import("drizzle-orm");
      const deliveredOrders = await db2.select().from(orders2).where(
        and47(
          eq79(orders2.status, "delivered"),
          eq79(orders2.confirmedByCustomer, true)
        )
      );
      const existingPayouts = await db2.select({ orderId: payouts2.orderId }).from(payouts2);
      const existingOrderIds = new Set(existingPayouts.map((p) => p.orderId));
      const toProcess = deliveredOrders.filter(
        (o) => !existingOrderIds.has(o.id)
      );
      let created = 0;
      for (const order of toProcess) {
        const inserts = [];
        const [biz] = await db2.select({ ownerId: businesses3.ownerId }).from(businesses3).where(eq79(businesses3.id, order.businessId)).limit(1);
        const businessOwnerId = biz?.ownerId || order.businessId;
        const businessAmount = order.businessEarnings || order.subtotal || 0;
        if (businessAmount > 0) {
          inserts.push({
            orderId: order.id,
            recipientId: businessOwnerId,
            recipientType: "business",
            amount: businessAmount,
            status: "pending"
          });
        }
        const driverAmount = order.deliveryEarnings || order.deliveryFee || 0;
        if (order.deliveryPersonId && driverAmount > 0) {
          inserts.push({
            orderId: order.id,
            recipientId: order.deliveryPersonId,
            recipientType: "driver",
            amount: driverAmount,
            status: "pending"
          });
        }
        if (inserts.length > 0) {
          await db2.insert(payouts2).values(inserts);
          created += inserts.length;
        }
      }
      res.json({ success: true, processed: toProcess.length, created });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router9.post(
  "/payout-proof-upload",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { image } = req.body;
      if (!image || typeof image !== "string" || !image.startsWith("data:image/")) {
        return res.status(400).json({ success: false, error: "Imagen requerida (data:image/...)" });
      }
      const estimatedBytes = Math.ceil(image.length * 0.75);
      if (estimatedBytes > 5 * 1024 * 1024) {
        return res.status(400).json({ success: false, error: "El archivo es muy pesado. M\xE1ximo 5MB" });
      }
      const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
      const url = await CloudinaryService2.uploadImage(
        image,
        "comprobantes",
        `payout-proof-${req.user.id}-${Date.now()}`
      );
      res.json({ success: true, url });
    } catch (error) {
      console.error("Payout proof upload error:", error);
      res.status(500).json({ success: false, error: "Error al subir el comprobante" });
    }
  }
);
router9.post(
  "/payouts/:id/mark-paid",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { payouts: payouts2, users: users5, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { notes, proofUrl, method } = req.body;
      const [payout] = await db2.select().from(payouts2).where(eq79(payouts2.id, req.params.id)).limit(1);
      if (!payout)
        return res.status(404).json({ error: "Payout no encontrado" });
      if (payout.status === "paid" || payout.status === "stripe_auto")
        return res.status(400).json({ error: "Este pago ya fue procesado" });
      const cleanNotes = (notes || "").trim();
      if (!cleanNotes) {
        return res.status(400).json({
          error: "Indica la referencia / nota de la transferencia realizada"
        });
      }
      if (!proofUrl) {
        return res.status(400).json({
          error: "Adjunta la captura del comprobante de la transferencia"
        });
      }
      await db2.update(payouts2).set({
        status: "paid",
        paidBy: req.user.id,
        paidAt: /* @__PURE__ */ new Date(),
        method: method || payout.method || "transferencia",
        notes: cleanNotes,
        proofUrl
      }).where(eq79(payouts2.id, req.params.id));
      if ((payout.orderId || "").startsWith("wdr-")) {
        try {
          const { settleWithdrawalWallet: settleWithdrawalWallet2 } = await Promise.resolve().then(() => (init_payoutService(), payoutService_exports));
          await settleWithdrawalWallet2(payout.recipientId, payout.amount);
        } catch (settleError) {
          console.error("Error settling withdrawal wallet:", settleError);
        }
      }
      try {
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        let recipientUserId = payout.recipientId;
        if (payout.recipientType === "business") {
          const [biz] = await db2.select({ ownerId: businesses3.ownerId }).from(businesses3).where(eq79(businesses3.id, payout.recipientId)).limit(1);
          if (biz?.ownerId) recipientUserId = biz.ownerId;
        }
        const amountEur = `\u20AC${(payout.amount / 100).toFixed(2)}`;
        const methodLabel = method === "bizum" ? "Bizum" : method === "transferencia" ? "Transferencia IBAN" : method === "paypal" ? "PayPal" : "transferencia";
        await sendPushToUser2(recipientUserId, {
          title: "\u{1F4B0} Pago recibido",
          body: `ComeYa te ha enviado ${amountEur} v\xEDa ${methodLabel}. Pedido ${await orderRefFromId(payout.orderId)}.`,
          data: {
            screen: payout.recipientType === "business" ? "BusinessFinances" : "DriverEarnings",
            payoutId: payout.id
          }
        });
      } catch (pushErr) {
        console.error("Push notification error:", pushErr);
      }
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router9.get(
  "/payouts/history",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { payouts: payouts2, users: users5, businesses: businesses3, orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { inArray: inArray12, eq: eq79, desc: desc19 } = await import("drizzle-orm");
      const paid = await db2.select().from(payouts2).where(inArray12(payouts2.status, ["paid", "stripe_auto"])).orderBy(desc19(payouts2.paidAt));
      console.log(`[Finance] Found ${paid.length} paid payouts`);
      const enriched = await Promise.all(
        paid.map(async (p) => {
          let recipientName = "";
          let businessName = null;
          if (p.recipientType === "business") {
            const [biz] = await db2.select({ name: businesses3.name }).from(businesses3).where(eq79(businesses3.id, p.recipientId)).limit(1);
            if (biz) {
              recipientName = biz.name ?? "";
            } else {
              const [usr] = await db2.select({ name: users5.name }).from(users5).where(eq79(users5.id, p.recipientId)).limit(1);
              recipientName = usr?.name ?? "";
            }
          } else {
            const [usr] = await db2.select({ name: users5.name }).from(users5).where(eq79(users5.id, p.recipientId)).limit(1);
            recipientName = usr?.name ?? "";
            const [order] = await db2.select({ businessName: orders2.businessName }).from(orders2).where(eq79(orders2.id, p.orderId)).limit(1);
            businessName = order?.businessName ?? null;
          }
          let proofUrl = p.proofUrl ?? null;
          if (!proofUrl && p.accountSnapshot) {
            try {
              const snap = JSON.parse(p.accountSnapshot);
              proofUrl = snap?.proofUrl ?? null;
            } catch {
            }
          }
          return {
            ...p,
            recipientName,
            businessName,
            proofUrl
          };
        })
      );
      res.json({ success: true, payouts: enriched });
    } catch (error) {
      console.error("[Finance] History error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var adminFinanceRoutes_default = router9;

// server/routes/adminIssues.ts
var import_express10 = __toESM(require("express"));

// shared/orderIssues.ts
var ISSUE_LABELS = {
  missing_items: "Art\xEDculos faltantes",
  wrong_items: "Art\xEDculos incorrectos",
  incomplete: "Pedido incompleto",
  damaged: "Producto da\xF1ado",
  quality: "Mala calidad",
  late_delivery: "Entrega tard\xEDa",
  never_arrived: "El pedido nunca lleg\xF3",
  driver_issue: "Problema con el repartidor",
  other: "Otro problema"
};
var RESOLUTION_LABELS = {
  refund_full: "Devoluci\xF3n total",
  refund_partial: "Devoluci\xF3n parcial",
  redelivery: "Reenv\xEDo del pedido",
  rejected: "Denegada"
};
var REFUND_METHOD_LABELS = {
  stripe: "Devoluci\xF3n autom\xE1tica (Stripe)",
  manual_transfer: "Transferencia manual",
  cash_none: "Sin cargo previo"
};

// server/routes/adminIssues.ts
var router10 = import_express10.default.Router();
router10.use(authenticateToken, requireRole("admin", "super_admin"));
var parseJson = (value) => {
  if (!value) return null;
  try {
    return JSON.parse(value);
  } catch {
    return null;
  }
};
router10.get("/", async (req, res) => {
  try {
    const { orderIssues: orderIssues2, orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, and: and47, desc: desc19, gte: gte10, lte: lte6, like: like2, or: or8, sql: sql23 } = await import("drizzle-orm");
    const {
      status,
      issueType,
      priority,
      liableParty,
      search,
      dateFrom,
      dateTo
    } = req.query;
    const limit = Math.min(Number(req.query.limit) || 50, 200);
    const offset = Math.max(Number(req.query.offset) || 0, 0);
    const filters = [];
    if (status && status !== "all") filters.push(eq79(orderIssues2.status, status));
    if (issueType && issueType !== "all")
      filters.push(eq79(orderIssues2.issueType, issueType));
    if (priority && priority !== "all")
      filters.push(eq79(orderIssues2.priority, priority));
    if (liableParty && liableParty !== "all")
      filters.push(eq79(orderIssues2.liableParty, liableParty));
    if (dateFrom) filters.push(gte10(orderIssues2.createdAt, new Date(dateFrom)));
    if (dateTo) filters.push(lte6(orderIssues2.createdAt, new Date(dateTo)));
    if (search?.trim()) {
      const q = `%${search.trim()}%`;
      filters.push(
        or8(
          like2(orderIssues2.orderId, q),
          like2(orderIssues2.description, q),
          like2(users5.name, q)
        )
      );
    }
    const where = filters.length > 0 ? and47(...filters) : void 0;
    const rows = await db2.select({
      id: orderIssues2.id,
      orderId: orderIssues2.orderId,
      ticketId: orderIssues2.ticketId,
      reportedBy: orderIssues2.reportedBy,
      reporterRole: orderIssues2.reporterRole,
      issueType: orderIssues2.issueType,
      description: orderIssues2.description,
      photos: orderIssues2.photos,
      status: orderIssues2.status,
      priority: orderIssues2.priority,
      resolutionType: orderIssues2.resolutionType,
      resolutionAmount: orderIssues2.resolutionAmount,
      liableParty: orderIssues2.liableParty,
      assignedTo: orderIssues2.assignedTo,
      resolvedAt: orderIssues2.resolvedAt,
      createdAt: orderIssues2.createdAt,
      customerName: users5.name,
      customerPhone: users5.phone,
      orderTotal: orders2.total,
      orderStatus: orders2.status,
      paymentMethod: orders2.paymentMethod,
      businessName: orders2.businessName,
      cashCollected: orders2.cashCollected,
      stripePaymentIntentId: orders2.stripePaymentIntentId
    }).from(orderIssues2).leftJoin(orders2, eq79(orderIssues2.orderId, orders2.id)).leftJoin(users5, eq79(orderIssues2.reportedBy, users5.id)).where(where).orderBy(desc19(orderIssues2.createdAt)).limit(limit).offset(offset);
    const counts = await db2.select({
      status: orderIssues2.status,
      count: sql23`COUNT(*)`
    }).from(orderIssues2).groupBy(orderIssues2.status);
    const byStatus = {
      open: 0,
      in_review: 0,
      resolved: 0,
      rejected: 0
    };
    for (const c of counts) byStatus[c.status] = Number(c.count);
    res.json({
      success: true,
      issues: rows.map((r) => ({
        ...r,
        photos: parseJson(r.photos) || [],
        issueLabel: ISSUE_LABELS[r.issueType] || r.issueType
      })),
      counts: {
        ...byStatus,
        total: Object.values(byStatus).reduce((a, b) => a + b, 0)
      },
      pagination: { limit, offset, returned: rows.length }
    });
  } catch (error) {
    console.error("Admin list issues error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router10.get("/:id", async (req, res) => {
  try {
    const { orderIssues: orderIssues2, orders: orders2, users: users5, ticketMessages: ticketMessages2, refunds: refunds2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, and: and47, ne: ne3, desc: desc19, sql: sql23 } = await import("drizzle-orm");
    const { describePaymentForRefund: describePaymentForRefund2, getRefundableAmount: getRefundableAmount2, defaultLiableParty: defaultLiableParty2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const [issue] = await db2.select().from(orderIssues2).where(eq79(orderIssues2.id, req.params.id)).limit(1);
    if (!issue) {
      return res.status(404).json({ success: false, error: "Incidencia no encontrada" });
    }
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, issue.orderId)).limit(1);
    const [customer] = await db2.select({
      id: users5.id,
      name: users5.name,
      email: users5.email,
      phone: users5.phone,
      createdAt: users5.createdAt
    }).from(users5).where(eq79(users5.id, issue.reportedBy)).limit(1);
    const messages = issue.ticketId ? await db2.select().from(ticketMessages2).where(eq79(ticketMessages2.ticketId, issue.ticketId)).orderBy(ticketMessages2.createdAt) : [];
    const orderRefunds = await db2.select().from(refunds2).where(eq79(refunds2.orderId, issue.orderId)).orderBy(desc19(refunds2.createdAt));
    const history = await db2.select({
      id: orderIssues2.id,
      orderId: orderIssues2.orderId,
      issueType: orderIssues2.issueType,
      status: orderIssues2.status,
      resolutionType: orderIssues2.resolutionType,
      resolutionAmount: orderIssues2.resolutionAmount,
      createdAt: orderIssues2.createdAt
    }).from(orderIssues2).where(
      and47(
        eq79(orderIssues2.reportedBy, issue.reportedBy),
        ne3(orderIssues2.id, issue.id)
      )
    ).orderBy(desc19(orderIssues2.createdAt)).limit(20);
    const [totals] = await db2.select({
      orders: sql23`COUNT(*)`
    }).from(orders2).where(eq79(orders2.userId, issue.reportedBy));
    const refundedToCustomer = await db2.select({ amount: refunds2.amount, status: refunds2.status }).from(refunds2).where(eq79(refunds2.customerId, issue.reportedBy));
    let businessInfo = null;
    if (order?.businessId) {
      const [biz] = await db2.select({
        id: businesses3.id,
        name: businesses3.name,
        phone: businesses3.phone,
        ownerId: businesses3.ownerId
      }).from(businesses3).where(eq79(businesses3.id, order.businessId)).limit(1);
      businessInfo = biz || null;
    }
    let driverInfo = null;
    if (order?.deliveryPersonId) {
      const [drv] = await db2.select({ id: users5.id, name: users5.name, phone: users5.phone }).from(users5).where(eq79(users5.id, order.deliveryPersonId)).limit(1);
      driverInfo = drv || null;
    }
    const payment = order ? await describePaymentForRefund2(order) : null;
    const refundable = order ? await getRefundableAmount2(order) : 0;
    res.json({
      success: true,
      issue: {
        ...issue,
        photos: parseJson(issue.photos) || [],
        affectedItems: parseJson(issue.affectedItems),
        issueLabel: ISSUE_LABELS[issue.issueType] || issue.issueType,
        resolutionLabel: issue.resolutionType ? RESOLUTION_LABELS[issue.resolutionType] : null,
        suggestedLiableParty: issue.liableParty || defaultLiableParty2(issue.issueType)
      },
      order: order ? { ...order, items: parseJson(order.items) || order.items } : null,
      customer,
      business: businessInfo,
      driver: driverInfo,
      messages,
      refunds: orderRefunds,
      payment,
      refundableAmount: refundable,
      customerHistory: {
        issues: history,
        totalIssues: history.length + 1,
        totalOrders: Number(totals?.orders || 0),
        totalRefunded: refundedToCustomer.filter((r) => r.status === "completed").reduce((s, r) => s + r.amount, 0)
      }
    });
  } catch (error) {
    console.error("Admin get issue error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router10.post("/:id/assign", async (req, res) => {
  try {
    const { orderIssues: orderIssues2, supportTickets: supportTickets2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [issue] = await db2.select().from(orderIssues2).where(eq79(orderIssues2.id, req.params.id)).limit(1);
    if (!issue) {
      return res.status(404).json({ success: false, error: "Incidencia no encontrada" });
    }
    await db2.update(orderIssues2).set({
      assignedTo: req.user.id,
      status: issue.status === "open" ? "in_review" : issue.status,
      updatedAt: /* @__PURE__ */ new Date()
    }).where(eq79(orderIssues2.id, req.params.id));
    if (issue.ticketId) {
      await db2.update(supportTickets2).set({
        assignedTo: req.user.id,
        status: "in_progress",
        updatedAt: /* @__PURE__ */ new Date()
      }).where(eq79(supportTickets2.id, issue.ticketId));
    }
    res.json({ success: true, message: "Incidencia asignada" });
  } catch (error) {
    console.error("Admin assign issue error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router10.post("/:id/messages", async (req, res) => {
  try {
    const { message } = req.body;
    if (!message?.trim()) {
      return res.status(400).json({ success: false, error: "El mensaje no puede estar vac\xEDo" });
    }
    const { orderIssues: orderIssues2, supportTickets: supportTickets2, ticketMessages: ticketMessages2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const { randomUUID: randomUUID5 } = await import("crypto");
    const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
    const [issue] = await db2.select().from(orderIssues2).where(eq79(orderIssues2.id, req.params.id)).limit(1);
    if (!issue) {
      return res.status(404).json({ success: false, error: "Incidencia no encontrada" });
    }
    let ticketId = issue.ticketId;
    if (!ticketId) {
      ticketId = randomUUID5();
      await db2.insert(supportTickets2).values({
        id: ticketId,
        userId: issue.reportedBy,
        orderId: issue.orderId,
        subject: `[Pedido #${issue.orderId.slice(-6)}] ${ISSUE_LABELS[issue.issueType] || issue.issueType}`.slice(
          0,
          255
        ),
        category: "order_issue",
        priority: issue.priority,
        status: "in_progress"
      });
      await db2.update(orderIssues2).set({ ticketId }).where(eq79(orderIssues2.id, issue.id));
    }
    await db2.insert(ticketMessages2).values({
      ticketId,
      senderId: req.user.id,
      senderType: "admin",
      message: message.trim()
    });
    await db2.update(orderIssues2).set({
      status: issue.status === "open" ? "in_review" : issue.status,
      assignedTo: issue.assignedTo || req.user.id,
      updatedAt: /* @__PURE__ */ new Date()
    }).where(eq79(orderIssues2.id, issue.id));
    await sendPushToUser2(issue.reportedBy, {
      title: "Respuesta a tu incidencia",
      body: message.trim().substring(0, 120),
      data: {
        type: "issue_reply",
        issueId: issue.id,
        orderId: issue.orderId,
        ticketId,
        screen: "TicketDetail"
      }
    }).catch(() => {
    });
    res.json({ success: true, message: "Respuesta enviada", ticketId });
  } catch (error) {
    console.error("Admin issue message error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router10.post("/:id/resolve", async (req, res) => {
  try {
    const {
      resolutionType,
      amount,
      liableParty,
      customerMessage,
      internalNote
    } = req.body;
    const VALID = ["refund_full", "refund_partial", "redelivery"];
    if (!VALID.includes(resolutionType)) {
      return res.status(400).json({
        success: false,
        error: `Tipo de resoluci\xF3n inv\xE1lido. Opciones: ${VALID.join(", ")}`
      });
    }
    const { orderIssues: orderIssues2, orders: orders2, supportTickets: supportTickets2, ticketMessages: ticketMessages2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
    const { createRefund: createRefund2, getRefundableAmount: getRefundableAmount2, defaultLiableParty: defaultLiableParty2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const [issue] = await db2.select().from(orderIssues2).where(eq79(orderIssues2.id, req.params.id)).limit(1);
    if (!issue) {
      return res.status(404).json({ success: false, error: "Incidencia no encontrada" });
    }
    if (issue.status === "resolved") {
      return res.status(400).json({ success: false, error: "Esta incidencia ya est\xE1 resuelta" });
    }
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, issue.orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ success: false, error: "Pedido asociado no encontrado" });
    }
    const responsible = liableParty || issue.liableParty || defaultLiableParty2(issue.issueType);
    let refundAmount = 0;
    if (resolutionType === "refund_full") {
      refundAmount = await getRefundableAmount2(order);
    } else if (resolutionType === "refund_partial") {
      refundAmount = Math.round(Number(amount) || 0);
      if (refundAmount <= 0) {
        return res.status(400).json({
          success: false,
          error: "Indica el importe a devolver (en c\xE9ntimos)"
        });
      }
    }
    let refundResult = null;
    if (refundAmount > 0) {
      refundResult = await createRefund2({
        orderId: issue.orderId,
        amount: refundAmount,
        type: "issue",
        reason: `${ISSUE_LABELS[issue.issueType] || issue.issueType}: ${issue.description.slice(0, 180)}`,
        issueId: issue.id,
        liableParty: responsible,
        requestedBy: req.user.id,
        notes: internalNote,
        notifyCustomer: false
        // el mensaje de resolución ya lo informa
      });
      if (!refundResult.success) {
        return res.status(400).json({
          success: false,
          error: refundResult.message,
          refund: refundResult
        });
      }
    }
    await db2.update(orderIssues2).set({
      status: "resolved",
      resolutionType,
      resolutionAmount: refundResult?.amount ?? 0,
      liableParty: responsible,
      customerMessage: customerMessage || null,
      internalNote: internalNote || issue.internalNote,
      assignedTo: issue.assignedTo || req.user.id,
      resolvedBy: req.user.id,
      resolvedAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where(eq79(orderIssues2.id, issue.id));
    if (issue.ticketId) {
      const summary = [
        `Resoluci\xF3n: ${RESOLUTION_LABELS[resolutionType] || resolutionType}`,
        refundResult?.amount ? `Importe: ${(refundResult.amount / 100).toFixed(2)} \u20AC` : null,
        refundResult?.message,
        customerMessage
      ].filter(Boolean).join("\n");
      await db2.insert(ticketMessages2).values({
        ticketId: issue.ticketId,
        senderId: req.user.id,
        senderType: "admin",
        message: summary
      });
      await db2.update(supportTickets2).set({
        status: "resolved",
        assignedTo: req.user.id,
        resolvedAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      }).where(eq79(supportTickets2.id, issue.ticketId));
    }
    const bodyText = customerMessage?.trim() || (refundResult?.amount ? `Hemos resuelto tu incidencia. ${refundResult.message}` : resolutionType === "redelivery" ? "Vamos a reenviarte el pedido." : "Hemos resuelto tu incidencia.");
    await sendPushToUser2(issue.reportedBy, {
      title: "\u2705 Incidencia resuelta",
      body: bodyText.substring(0, 150),
      data: {
        type: "issue_resolved",
        issueId: issue.id,
        orderId: issue.orderId,
        screen: "OrderTracking"
      }
    }).catch(() => {
    });
    res.json({
      success: true,
      message: refundResult ? refundResult.message : "Incidencia resuelta sin devoluci\xF3n",
      refund: refundResult,
      liableParty: responsible
    });
  } catch (error) {
    console.error("Admin resolve issue error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router10.post("/:id/reject", async (req, res) => {
  try {
    const { reason, customerMessage } = req.body;
    if (!reason?.trim()) {
      return res.status(400).json({ success: false, error: "Indica el motivo de la denegaci\xF3n" });
    }
    const { orderIssues: orderIssues2, supportTickets: supportTickets2, ticketMessages: ticketMessages2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
    const [issue] = await db2.select().from(orderIssues2).where(eq79(orderIssues2.id, req.params.id)).limit(1);
    if (!issue) {
      return res.status(404).json({ success: false, error: "Incidencia no encontrada" });
    }
    if (issue.status === "resolved") {
      return res.status(400).json({
        success: false,
        error: "Esta incidencia ya se resolvi\xF3 con devoluci\xF3n"
      });
    }
    await db2.update(orderIssues2).set({
      status: "rejected",
      resolutionType: "rejected",
      resolutionAmount: 0,
      customerMessage: customerMessage || null,
      internalNote: reason.trim(),
      assignedTo: issue.assignedTo || req.user.id,
      resolvedBy: req.user.id,
      resolvedAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where(eq79(orderIssues2.id, issue.id));
    if (issue.ticketId) {
      await db2.insert(ticketMessages2).values({
        ticketId: issue.ticketId,
        senderId: req.user.id,
        senderType: "admin",
        message: customerMessage?.trim() || "Hemos revisado tu incidencia y no procede compensaci\xF3n en este caso."
      });
      await db2.update(supportTickets2).set({
        status: "closed",
        assignedTo: req.user.id,
        resolvedAt: /* @__PURE__ */ new Date(),
        updatedAt: /* @__PURE__ */ new Date()
      }).where(eq79(supportTickets2.id, issue.ticketId));
    }
    await sendPushToUser2(issue.reportedBy, {
      title: "Incidencia revisada",
      body: (customerMessage?.trim() || "Hemos revisado tu incidencia y no procede compensaci\xF3n en este caso.").substring(0, 150),
      data: {
        type: "issue_rejected",
        issueId: issue.id,
        orderId: issue.orderId,
        screen: "OrderTracking"
      }
    }).catch(() => {
    });
    res.json({ success: true, message: "Incidencia denegada" });
  } catch (error) {
    console.error("Admin reject issue error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
var adminIssues_default = router10;

// server/routes/adminRefunds.ts
var import_express11 = __toESM(require("express"));
var router11 = import_express11.default.Router();
router11.use(authenticateToken, requireRole("admin", "super_admin"));
router11.get("/", async (req, res) => {
  try {
    const { refunds: refunds2, orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, and: and47, desc: desc19, gte: gte10, lte: lte6, like: like2, or: or8, sql: sql23 } = await import("drizzle-orm");
    const { status, method, type, liableParty, search, dateFrom, dateTo } = req.query;
    const limit = Math.min(Number(req.query.limit) || 100, 300);
    const offset = Math.max(Number(req.query.offset) || 0, 0);
    const filters = [];
    if (status && status !== "all") filters.push(eq79(refunds2.status, status));
    if (method && method !== "all") filters.push(eq79(refunds2.method, method));
    if (type && type !== "all") filters.push(eq79(refunds2.type, type));
    if (liableParty && liableParty !== "all")
      filters.push(eq79(refunds2.liableParty, liableParty));
    if (dateFrom) filters.push(gte10(refunds2.createdAt, new Date(dateFrom)));
    if (dateTo) filters.push(lte6(refunds2.createdAt, new Date(dateTo)));
    if (search?.trim()) {
      const q = `%${search.trim()}%`;
      filters.push(or8(like2(refunds2.orderId, q), like2(users5.name, q)));
    }
    const where = filters.length > 0 ? and47(...filters) : void 0;
    const rows = await db2.select({
      id: refunds2.id,
      orderId: refunds2.orderId,
      issueId: refunds2.issueId,
      customerId: refunds2.customerId,
      amount: refunds2.amount,
      type: refunds2.type,
      reason: refunds2.reason,
      method: refunds2.method,
      status: refunds2.status,
      stripeRefundId: refunds2.stripeRefundId,
      liableParty: refunds2.liableParty,
      businessDeduction: refunds2.businessDeduction,
      driverDeduction: refunds2.driverDeduction,
      platformCost: refunds2.platformCost,
      payoutAdjusted: refunds2.payoutAdjusted,
      failureReason: refunds2.failureReason,
      proofUrl: refunds2.proofUrl,
      notes: refunds2.notes,
      processedAt: refunds2.processedAt,
      createdAt: refunds2.createdAt,
      customerName: users5.name,
      customerPhone: users5.phone,
      businessName: orders2.businessName,
      orderTotal: orders2.total,
      paymentMethod: orders2.paymentMethod
    }).from(refunds2).leftJoin(orders2, eq79(refunds2.orderId, orders2.id)).leftJoin(users5, eq79(refunds2.customerId, users5.id)).where(where).orderBy(desc19(refunds2.createdAt)).limit(limit).offset(offset);
    const counts = await db2.select({ status: refunds2.status, count: sql23`COUNT(*)` }).from(refunds2).groupBy(refunds2.status);
    const byStatus = {
      pending: 0,
      processing: 0,
      completed: 0,
      failed: 0
    };
    for (const c of counts) byStatus[c.status] = Number(c.count);
    res.json({
      success: true,
      refunds: rows.map((r) => ({
        ...r,
        methodLabel: REFUND_METHOD_LABELS[r.method] || r.method
      })),
      counts: {
        ...byStatus,
        total: Object.values(byStatus).reduce((a, b) => a + b, 0)
      },
      pagination: { limit, offset, returned: rows.length }
    });
  } catch (error) {
    console.error("Admin list refunds error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router11.get("/summary", async (req, res) => {
  try {
    const { refunds: refunds2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const all = await db2.select().from(refunds2);
    const today = /* @__PURE__ */ new Date();
    today.setHours(0, 0, 0, 0);
    const weekAgo = new Date(today);
    weekAgo.setDate(weekAgo.getDate() - 7);
    const monthStart = new Date(today.getFullYear(), today.getMonth(), 1);
    const completed = all.filter((r) => r.status === "completed");
    const sum3 = (rows) => rows.reduce((s, r) => s + r.amount, 0);
    const since = (d) => completed.filter((r) => r.createdAt && new Date(r.createdAt) >= d);
    const byMethod = {};
    for (const r of completed) {
      byMethod[r.method] = byMethod[r.method] || { count: 0, amount: 0 };
      byMethod[r.method].count += 1;
      byMethod[r.method].amount += r.amount;
    }
    const byLiableParty = {};
    for (const r of completed) {
      const key = r.liableParty || "platform";
      byLiableParty[key] = byLiableParty[key] || { count: 0, amount: 0 };
      byLiableParty[key].count += 1;
      byLiableParty[key].amount += r.amount;
    }
    const pendingManual = all.filter(
      (r) => r.status === "pending" && r.method === "manual_transfer"
    );
    const failed = all.filter((r) => r.status === "failed");
    res.json({
      success: true,
      refunded: {
        today: sum3(since(today)),
        week: sum3(since(weekAgo)),
        month: sum3(since(monthStart)),
        total: sum3(completed)
      },
      cost: {
        business: completed.reduce((s, r) => s + (r.businessDeduction || 0), 0),
        driver: completed.reduce((s, r) => s + (r.driverDeduction || 0), 0),
        platform: completed.reduce((s, r) => s + (r.platformCost || 0), 0)
      },
      byMethod,
      byLiableParty,
      pendingManual: {
        count: pendingManual.length,
        amount: sum3(pendingManual)
      },
      failed: { count: failed.length, amount: sum3(failed) },
      stats: {
        totalRefunds: all.length,
        avgRefund: completed.length > 0 ? Math.round(sum3(completed) / completed.length) : 0
      }
    });
  } catch (error) {
    console.error("Admin refunds summary error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router11.post("/", async (req, res) => {
  try {
    const { orderId, amount, reason, liableParty, notes } = req.body;
    if (!orderId || !reason?.trim()) {
      return res.status(400).json({ success: false, error: "Pedido y motivo son obligatorios" });
    }
    const { createRefund: createRefund2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const result = await createRefund2({
      orderId,
      amount: Math.round(Number(amount) || 0),
      type: "manual",
      reason: reason.trim(),
      liableParty,
      requestedBy: req.user.id,
      notes
    });
    if (!result.success) {
      return res.status(400).json({ success: false, error: result.message, refund: result });
    }
    res.json({ success: true, message: result.message, refund: result });
  } catch (error) {
    console.error("Admin create refund error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router11.get("/order/:orderId", async (req, res) => {
  try {
    const { orders: orders2, refunds: refunds2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, desc: desc19 } = await import("drizzle-orm");
    const { describePaymentForRefund: describePaymentForRefund2, getRefundableAmount: getRefundableAmount2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, req.params.orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ success: false, error: "Pedido no encontrado" });
    }
    const history = await db2.select().from(refunds2).where(eq79(refunds2.orderId, order.id)).orderBy(desc19(refunds2.createdAt));
    res.json({
      success: true,
      order: {
        id: order.id,
        total: order.total,
        status: order.status,
        paymentMethod: order.paymentMethod,
        businessName: order.businessName
      },
      payment: await describePaymentForRefund2(order),
      refundableAmount: await getRefundableAmount2(order),
      refunds: history
    });
  } catch (error) {
    console.error("Admin refund order context error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router11.post("/:id/mark-paid", async (req, res) => {
  try {
    const { proofUrl, notes } = req.body;
    if (!proofUrl && !notes) {
      return res.status(400).json({
        success: false,
        error: "Adjunta el comprobante o deja una nota del pago"
      });
    }
    const { markRefundPaid: markRefundPaid2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const result = await markRefundPaid2(req.params.id, req.user.id, {
      proofUrl,
      notes
    });
    if (!result.success) {
      return res.status(400).json({ success: false, error: result.message });
    }
    res.json({ success: true, message: result.message });
  } catch (error) {
    console.error("Admin mark refund paid error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router11.post("/:id/retry", async (req, res) => {
  try {
    const { retryRefund: retryRefund2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const result = await retryRefund2(req.params.id, req.user.id);
    if (!result.success) {
      return res.status(400).json({ success: false, error: result.message, refund: result });
    }
    res.json({ success: true, message: result.message, refund: result });
  } catch (error) {
    console.error("Admin retry refund error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
var adminRefunds_default = router11;

// server/routes/adminOrderActions.ts
var import_express12 = __toESM(require("express"));
var router12 = import_express12.default.Router();
router12.use(authenticateToken, requireRole("admin", "super_admin"));
router12.get("/:id/cancel-preview", async (req, res) => {
  try {
    const { getCancelPreview: getCancelPreview2 } = await Promise.resolve().then(() => (init_orderCancellationService(), orderCancellationService_exports));
    const { describePaymentForRefund: describePaymentForRefund2, getRefundableAmount: getRefundableAmount2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const refundOverride = req.query.refundAmount !== void 0 ? Number(req.query.refundAmount) : void 0;
    const preview = await getCancelPreview2(req.params.id, "admin", {
      actorRole: "admin",
      refundOverride,
      liableParty: req.query.liableParty
    });
    if (!preview.success || !preview.order) {
      return res.status(404).json({ success: false, error: preview.message || "Pedido no encontrado" });
    }
    res.json({
      success: true,
      cancellable: preview.cancellable,
      policy: preview.policy,
      payment: await describePaymentForRefund2(preview.order),
      refundableAmount: await getRefundableAmount2(preview.order),
      order: {
        id: preview.order.id,
        status: preview.order.status,
        total: preview.order.total,
        productosBase: preview.order.productosBase,
        deliveryFee: preview.order.deliveryFee,
        paymentMethod: preview.order.paymentMethod,
        businessName: preview.order.businessName
      }
    });
  } catch (error) {
    console.error("Admin cancel preview error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router12.post("/:id/cancel", async (req, res) => {
  try {
    const { reason, refundAmount, liableParty } = req.body;
    if (!reason?.trim()) {
      return res.status(400).json({ success: false, error: "Indica el motivo de la cancelaci\xF3n" });
    }
    const { cancelOrder: cancelOrder2 } = await Promise.resolve().then(() => (init_orderCancellationService(), orderCancellationService_exports));
    const result = await cancelOrder2(req.params.id, req.user.id, reason.trim(), {
      actorRole: req.user.role,
      refundOverride: refundAmount !== void 0 && refundAmount !== null ? Math.round(Number(refundAmount)) : void 0,
      liableParty
    });
    if (!result.success) {
      return res.status(result.error === "not_found" ? 404 : 400).json({ success: false, error: result.message });
    }
    res.json({
      success: true,
      message: result.message,
      policy: result.policy,
      refund: result.refund
    });
  } catch (error) {
    console.error("Admin cancel order error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router12.post("/:id/refund", async (req, res) => {
  try {
    const { amount, reason, liableParty, notes } = req.body;
    if (!reason?.trim()) {
      return res.status(400).json({ success: false, error: "Indica el motivo de la devoluci\xF3n" });
    }
    const { createRefund: createRefund2 } = await Promise.resolve().then(() => (init_refundService(), refundService_exports));
    const result = await createRefund2({
      orderId: req.params.id,
      amount: Math.round(Number(amount) || 0),
      type: "manual",
      reason: reason.trim(),
      liableParty,
      requestedBy: req.user.id,
      notes
    });
    if (!result.success) {
      return res.status(400).json({ success: false, error: result.message, refund: result });
    }
    res.json({ success: true, message: result.message, refund: result });
  } catch (error) {
    console.error("Admin order refund error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router12.post("/:id/hide", async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [order] = await db2.select().from(orders2).where(eq79(orders2.id, req.params.id)).limit(1);
    if (!order) return res.status(404).json({ success: false, error: "Pedido no encontrado" });
    if (!["cancelled", "refunded", "completed", "delivered", "payment_failed"].includes(order.status)) {
      return res.status(400).json({
        success: false,
        error: "Solo se pueden ocultar pedidos cancelados, reembolsados o finalizados"
      });
    }
    await db2.update(orders2).set({ deletedAt: /* @__PURE__ */ new Date() }).where(eq79(orders2.id, req.params.id));
    res.json({ success: true, message: "Pedido ocultado de las listas" });
  } catch (error) {
    console.error("Admin hide order error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router12.post("/:id/unhide", async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    await db2.update(orders2).set({ deletedAt: null }).where(eq79(orders2.id, req.params.id));
    res.json({ success: true, message: "Pedido visible de nuevo" });
  } catch (error) {
    console.error("Admin unhide order error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
var adminOrderActions_default = router12;

// server/routes/adminExchangeRate.ts
var import_express13 = __toESM(require("express"));
init_exchangeRateService();
var router13 = import_express13.default.Router();
router13.get(
  "/exchange-rate",
  authenticateToken,
  requireAdmin,
  async (req, res) => {
    try {
      const result = await exchangeRateService.getCurrentRate();
      res.json(result);
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router13.post(
  "/exchange-rate",
  authenticateToken,
  requireAdmin,
  async (req, res) => {
    try {
      const { rate } = req.body;
      if (!rate || typeof rate !== "number") {
        return res.status(400).json({ success: false, message: "Tasa inv\xE1lida" });
      }
      const result = await exchangeRateService.updateManualRate(
        rate,
        req.user.id
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router13.post(
  "/exchange-rate/auto-update",
  authenticateToken,
  requireAdmin,
  async (req, res) => {
    try {
      const { enabled } = req.body;
      if (typeof enabled !== "boolean") {
        return res.status(400).json({ success: false, message: "Valor inv\xE1lido" });
      }
      const result = await exchangeRateService.toggleAutoUpdate(
        enabled,
        req.user.id
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router13.post(
  "/exchange-rate/force-update",
  authenticateToken,
  requireAdmin,
  async (req, res) => {
    try {
      const result = await exchangeRateService.forceUpdate();
      res.json(result);
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var adminExchangeRate_default = router13;

// server/routes/walletRoutes.ts
var import_express14 = __toESM(require("express"));
var router14 = import_express14.default.Router();
router14.get("/balance", authenticateToken, async (req, res) => {
  try {
    const { wallets: wallets2, transactions: transactions4, orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const { cashSettlementService: cashSettlementService2 } = await Promise.resolve().then(() => (init_cashSettlementService(), cashSettlementService_exports));
    const [wallet] = await db2.select().from(wallets2).where(eq79(wallets2.userId, req.user.id)).limit(1);
    if (!wallet) {
      await db2.insert(wallets2).values({
        userId: req.user.id,
        balance: 0,
        pendingBalance: 0,
        cashOwed: 0,
        cashPending: 0,
        totalEarned: 0,
        totalWithdrawn: 0
      });
      const [createdWallet] = await db2.select().from(wallets2).where(eq79(wallets2.userId, req.user.id)).limit(1);
      if (!createdWallet) {
        return res.status(500).json({ error: "Failed to create wallet" });
      }
      return res.json({
        success: true,
        wallet: {
          id: createdWallet.id,
          balance: createdWallet.balance,
          pendingBalance: createdWallet.pendingBalance,
          cashOwed: createdWallet.cashOwed || 0,
          cashPending: createdWallet.cashPending || 0,
          availableBalance: createdWallet.balance - (createdWallet.cashOwed || 0),
          availableForWithdrawal: createdWallet.balance - (createdWallet.cashOwed || 0),
          totalEarned: createdWallet.totalEarned,
          totalWithdrawn: createdWallet.totalWithdrawn,
          pendingCashOrders: []
        }
      });
    }
    let pendingCashOrders = [];
    if (req.user.role === "delivery_driver" && wallet.cashOwed > 0) {
      const debtInfo = await cashSettlementService2.getDriverDebt(req.user.id);
      pendingCashOrders = debtInfo.pendingOrders;
    }
    res.json({
      success: true,
      wallet: {
        id: wallet.id,
        balance: wallet.balance,
        pendingBalance: wallet.pendingBalance,
        cashOwed: wallet.cashOwed || 0,
        cashPending: wallet.cashPending || 0,
        availableBalance: wallet.balance - (wallet.cashOwed || 0),
        availableForWithdrawal: wallet.balance - (wallet.cashOwed || 0),
        totalEarned: wallet.totalEarned,
        totalWithdrawn: wallet.totalWithdrawn,
        pendingCashOrders
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router14.get("/transactions", authenticateToken, async (req, res) => {
  try {
    const { transactions: transactions4, businesses: businesses3, wallets: wallets2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, desc: desc19 } = await import("drizzle-orm");
    if (req.user.role === "business_owner" || req.user.role === "business") {
      const ownerBusinesses = await db2.select({ id: businesses3.id }).from(businesses3).where(eq79(businesses3.ownerId, req.user.id));
      for (const business of ownerBusinesses) {
        const [legacyWallet] = await db2.select().from(wallets2).where(eq79(wallets2.userId, business.id)).limit(1);
        if (legacyWallet && (legacyWallet.balance !== 0 || legacyWallet.totalEarned !== 0)) {
          const [ownerWallet] = await db2.select().from(wallets2).where(eq79(wallets2.userId, req.user.id)).limit(1);
          if (ownerWallet) {
            await db2.update(wallets2).set({
              balance: ownerWallet.balance + legacyWallet.balance,
              totalEarned: ownerWallet.totalEarned + legacyWallet.totalEarned
            }).where(eq79(wallets2.userId, req.user.id));
          } else {
            await db2.insert(wallets2).values({
              userId: req.user.id,
              balance: legacyWallet.balance,
              pendingBalance: 0,
              totalEarned: legacyWallet.totalEarned,
              totalWithdrawn: 0,
              cashOwed: 0,
              cashPending: 0
            });
          }
          await db2.update(wallets2).set({
            balance: 0,
            totalEarned: 0
          }).where(eq79(wallets2.userId, business.id));
        }
        await db2.update(transactions4).set({ userId: req.user.id }).where(eq79(transactions4.userId, business.id));
      }
    }
    const walletTransactions2 = await db2.select().from(transactions4).where(eq79(transactions4.userId, req.user.id)).orderBy(desc19(transactions4.createdAt)).limit(50);
    res.json({ success: true, transactions: walletTransactions2 });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router14.get("/payouts", authenticateToken, async (req, res) => {
  try {
    const { payouts: payouts2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, inArray: inArray12, or: or8, desc: desc19 } = await import("drizzle-orm");
    const userId = req.user.id;
    const isBusinessOwner = req.user.role === "business_owner" || req.user.role === "business";
    let rows = [];
    if (isBusinessOwner) {
      const ownerBusinesses = await db2.select({ id: businesses3.id }).from(businesses3).where(eq79(businesses3.ownerId, userId));
      const businessIds = ownerBusinesses.map((b) => b.id);
      if (businessIds.length > 0) {
        rows = await db2.select().from(payouts2).where(
          or8(
            inArray12(payouts2.recipientId, businessIds),
            eq79(payouts2.recipientId, userId)
          )
        ).orderBy(desc19(payouts2.createdAt));
      } else {
        rows = await db2.select().from(payouts2).where(eq79(payouts2.recipientId, userId)).orderBy(desc19(payouts2.createdAt));
      }
    } else {
      rows = await db2.select().from(payouts2).where(eq79(payouts2.recipientId, userId)).orderBy(desc19(payouts2.createdAt));
    }
    res.json({ success: true, payouts: rows });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router14.post(
  "/withdraw",
  authenticateToken,
  auditAction("request_withdrawal", "withdrawal"),
  async (req, res) => {
    try {
      const { financialService: financialService2 } = await Promise.resolve().then(() => (init_unifiedFinancialService(), unifiedFinancialService_exports));
      const canWithdraw = await financialService2.canUserWithdraw(
        req.user.id,
        req.user.role
      );
      if (!canWithdraw.allowed) {
        return res.status(403).json({ error: canWithdraw.reason });
      }
      const { requestWithdrawal: requestWithdrawal2 } = await Promise.resolve().then(() => (init_withdrawalService(), withdrawalService_exports));
      const result = await requestWithdrawal2({
        userId: req.user.id,
        amount: req.body.amount,
        method: req.body.method || "bank_transfer",
        bankAccount: req.body.bankAccount
      });
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router14.get("/withdrawals", authenticateToken, async (req, res) => {
  try {
    const { getWithdrawalHistory: getWithdrawalHistory2 } = await Promise.resolve().then(() => (init_withdrawalService(), withdrawalService_exports));
    const result = await getWithdrawalHistory2(req.user.id);
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router14.post(
  "/withdrawals/:id/cancel",
  authenticateToken,
  auditAction("cancel_withdrawal", "withdrawal"),
  async (req, res) => {
    try {
      const { cancelWithdrawal: cancelWithdrawal2 } = await Promise.resolve().then(() => (init_withdrawalService(), withdrawalService_exports));
      const result = await cancelWithdrawal2(req.params.id, req.user.id);
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var walletRoutes_default = router14;

// server/routes/bankAccountRoutes.ts
var import_express15 = __toESM(require("express"));
init_db();
init_schema_mysql();
var import_drizzle_orm39 = require("drizzle-orm");
var router15 = import_express15.default.Router();
router15.get("/", authenticateToken, async (req, res) => {
  try {
    const [user] = await db.select({ bankAccount: users.bankAccount }).from(users).where((0, import_drizzle_orm39.eq)(users.id, req.user.id)).limit(1);
    let bankAccount = null;
    if (user?.bankAccount) {
      try {
        bankAccount = JSON.parse(user.bankAccount);
      } catch {
        bankAccount = user.bankAccount;
      }
    }
    if (!bankAccount) {
      try {
        const accounts = await db.select().from(paymentAccounts).where((0, import_drizzle_orm39.eq)(paymentAccounts.userId, req.user.id));
        const account = accounts.find((a) => a.isDefault) || accounts[0];
        if (account) {
          const numero = account.binanceId || account.pagoMovilPhone || "";
          bankAccount = {
            iban: account.method === "transferencia" ? numero : null,
            clabe: account.method === "transferencia" ? null : numero,
            bankName: account.method === "paypal" ? "PayPal" : account.zelleEmail || "Transferencia SEPA",
            accountHolder: account.zelleEmail || ""
          };
        }
      } catch {
      }
    }
    res.json({ success: true, bankAccount });
  } catch (error) {
    console.error("Error loading bank account:", error);
    res.status(500).json({ error: "Error al cargar la cuenta bancaria" });
  }
});
router15.post("/", authenticateToken, async (req, res) => {
  try {
    const { iban, clabe, bankName, accountHolder } = req.body || {};
    const numero = `${iban || clabe || ""}`.trim().replace(/\s+/g, "");
    if (numero.length !== 24 && numero.length !== 18) {
      return res.status(400).json({ error: "IBAN (24 caracteres) o CLABE (18 d\xEDgitos) requerido" });
    }
    if (!bankName || !accountHolder) {
      return res.status(400).json({ error: "Banco y titular son requeridos" });
    }
    const bankAccount = {
      iban: numero.length === 24 ? numero : null,
      clabe: numero.length === 18 ? numero : null,
      bankName: `${bankName}`.trim(),
      accountHolder: `${accountHolder}`.trim()
    };
    await db.update(users).set({ bankAccount: JSON.stringify(bankAccount) }).where((0, import_drizzle_orm39.eq)(users.id, req.user.id));
    try {
      await db.insert(paymentAccounts).values({
        id: crypto.randomUUID(),
        userId: req.user.id,
        method: "transferencia",
        isDefault: true,
        binanceId: numero,
        zelleEmail: `${accountHolder}`.trim(),
        label: `${bankName}`.trim()
      });
    } catch {
    }
    res.json({ success: true, bankAccount });
  } catch (error) {
    console.error("Error saving bank account:", error);
    res.status(500).json({ error: "Error al guardar la cuenta bancaria" });
  }
});
var bankAccountRoutes_default = router15;

// server/routes/deliveryConfigRoutes.ts
var import_express16 = __toESM(require("express"));

// server/services/deliveryConfigService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm40 = require("drizzle-orm");
var cachedConfig = null;
var lastFetch = 0;
var CACHE_TTL2 = 6e4;
async function getDeliveryConfig() {
  const now = Date.now();
  if (cachedConfig && now - lastFetch < CACHE_TTL2) return cachedConfig;
  const settings = await db.select().from(systemSettings).where((0, import_drizzle_orm40.eq)(systemSettings.category, "delivery"));
  const config3 = {
    tier1: 250,
    // 2.50€
    tier2: 400,
    // 4.00€
    tier3: 500,
    // 5.00€
    extraPerKm: 100,
    // 1.00€/km
    speedKmPerMin: 0.5,
    defaultPrepTime: 20
  };
  settings.forEach((s) => {
    const v = parseFloat(s.value);
    switch (s.key) {
      case "delivery_tier1":
        config3.tier1 = Math.round(v * 100);
        break;
      case "delivery_tier2":
        config3.tier2 = Math.round(v * 100);
        break;
      case "delivery_tier3":
        config3.tier3 = Math.round(v * 100);
        break;
      case "delivery_extra_per_km":
        config3.extraPerKm = Math.round(v * 100);
        break;
      case "delivery_speed":
        config3.speedKmPerMin = v;
        break;
      case "delivery_prep_time":
        config3.defaultPrepTime = v;
        break;
    }
  });
  cachedConfig = config3;
  lastFetch = now;
  return config3;
}
function clearDeliveryConfigCache() {
  cachedConfig = null;
}

// server/routes/deliveryConfigRoutes.ts
init_db();
init_schema_mysql();
var import_drizzle_orm41 = require("drizzle-orm");
var router16 = import_express16.default.Router();
router16.get("/config", async (req, res) => {
  try {
    const config3 = await getDeliveryConfig();
    res.json({
      success: true,
      config: {
        tier1: config3.tier1 / 100,
        tier2: config3.tier2 / 100,
        tier3: config3.tier3 / 100,
        extraPerKm: config3.extraPerKm / 100,
        speedKmPerMin: config3.speedKmPerMin,
        defaultPrepTime: config3.defaultPrepTime
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router16.put(
  "/config",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { tier1, tier2, tier3, extraPerKm } = req.body;
      const updates = [
        {
          key: "delivery_tier1",
          value: tier1?.toString(),
          category: "delivery",
          description: "Tarifa 1-2 km (EUR)"
        },
        {
          key: "delivery_tier2",
          value: tier2?.toString(),
          category: "delivery",
          description: "Tarifa 2-3 km (EUR)"
        },
        {
          key: "delivery_tier3",
          value: tier3?.toString(),
          category: "delivery",
          description: "Tarifa 3-4 km (EUR)"
        },
        {
          key: "delivery_extra_per_km",
          value: extraPerKm?.toString(),
          category: "delivery",
          description: "Extra por km >4 km (EUR)"
        }
      ];
      for (const u of updates) {
        if (u.value !== void 0) {
          const existing = await db.select().from(systemSettings).where((0, import_drizzle_orm41.eq)(systemSettings.key, u.key)).limit(1);
          if (existing.length > 0) {
            await db.update(systemSettings).set({ value: u.value, updatedBy: req.user.id }).where((0, import_drizzle_orm41.eq)(systemSettings.key, u.key));
          } else {
            await db.insert(systemSettings).values({
              key: u.key,
              value: u.value,
              type: "number",
              category: u.category,
              description: u.description,
              isPublic: false
            });
          }
        }
      }
      clearDeliveryConfigCache();
      const newConfig = await getDeliveryConfig();
      res.json({
        success: true,
        config: {
          tier1: newConfig.tier1 / 100,
          tier2: newConfig.tier2 / 100,
          tier3: newConfig.tier3 / 100,
          extraPerKm: newConfig.extraPerKm / 100
        }
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var deliveryConfigRoutes_default = router16;

// server/routes/businessVerificationRoutes.ts
var import_express17 = __toESM(require("express"));
init_db();
init_schema_mysql();
var import_drizzle_orm42 = require("drizzle-orm");
var router17 = import_express17.default.Router();
router17.post(
  "/send-code",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId, phone } = req.body;
      if (!phone || !/^\+?[1-9]\d{9,14}$/.test(phone.replace(/\s/g, ""))) {
        return res.status(400).json({ error: "N\xFAmero de tel\xE9fono inv\xE1lido" });
      }
      const [business] = await db.select().from(businesses).where((0, import_drizzle_orm42.eq)(businesses.id, businessId)).limit(1);
      if (!business || business.ownerId !== req.user.id) {
        return res.status(404).json({ error: "Negocio no encontrado" });
      }
      const code = Math.floor(1e5 + Math.random() * 9e5).toString();
      const expires = new Date(Date.now() + 10 * 60 * 1e3);
      await db.update(businesses).set({
        phone,
        verificationCode: code,
        verificationExpires: expires
      }).where((0, import_drizzle_orm42.eq)(businesses.id, businessId));
      if (process.env.TWILIO_ACCOUNT_SID) {
        const TwilioModule = await import("twilio");
        const TwilioClass = TwilioModule.default || TwilioModule;
        const client = TwilioClass(
          process.env.TWILIO_ACCOUNT_SID,
          process.env.TWILIO_AUTH_TOKEN
        );
        await client.messages.create({
          body: `Tu c\xF3digo de verificaci\xF3n ComeYa es: ${code}`,
          from: process.env.TWILIO_PHONE_NUMBER,
          to: phone
        });
      } else {
        console.log(`[DEV] C\xF3digo de verificaci\xF3n para ${phone}: ${code}`);
      }
      res.json({ success: true, message: "C\xF3digo enviado" });
    } catch (error) {
      console.error("Error sending verification code:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router17.post(
  "/verify-code",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId, code } = req.body;
      const [business] = await db.select().from(businesses).where((0, import_drizzle_orm42.eq)(businesses.id, businessId)).limit(1);
      if (!business || business.ownerId !== req.user.id) {
        return res.status(404).json({ error: "Negocio no encontrado" });
      }
      if (!business.verificationCode || !business.verificationExpires) {
        return res.status(400).json({ error: "No hay c\xF3digo pendiente" });
      }
      if (/* @__PURE__ */ new Date() > business.verificationExpires) {
        return res.status(400).json({ error: "C\xF3digo expirado" });
      }
      if (business.verificationCode !== code) {
        return res.status(400).json({ error: "C\xF3digo incorrecto" });
      }
      await db.update(businesses).set({
        phoneVerified: true,
        verificationCode: null,
        verificationExpires: null
      }).where((0, import_drizzle_orm42.eq)(businesses.id, businessId));
      res.json({ success: true, message: "Tel\xE9fono verificado" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var businessVerificationRoutes_default = router17;

// server/supportRoutes.ts
var import_express18 = __toESM(require("express"));
var router18 = import_express18.default.Router();
async function generateAIResponse(message, history) {
  const fallbackResponse = `Gracias por tu mensaje. Un agente de soporte revisar\xE1 tu consulta pronto.

\xBFNecesitas ayuda con:
\u2022 Realizar un pedido
\u2022 Seguimiento de entregas
\u2022 Informaci\xF3n de negocios
\u2022 Problemas con pagos
\u2022 Otra consulta

Por favor, describe tu consulta con m\xE1s detalle.`;
  try {
    const openaiKey = process.env.OPENAI_API_KEY;
    const geminiKey = process.env.AI_INTEGRATIONS_GEMINI_API_KEY || process.env.GEMINI_API_KEY;
    if (openaiKey) {
      const response = await fetch(
        "https://api.openai.com/v1/chat/completions",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${openaiKey}`
          },
          body: JSON.stringify({
            model: "gpt-3.5-turbo",
            messages: [
              {
                role: "system",
                content: `Eres un asistente de soporte para ComeYa, una plataforma de delivery de comida.

INFORMACI\xD3N CLAVE:
- Conectamos negocios locales, clientes y repartidores
- Comisiones: 15% del producto para ComeYa, 100% del producto para el negocio, 100% del delivery para el repartidor
- Pagos con tarjeta (Stripe) o m\xE9todos manuales con comprobante
- Autenticaci\xF3n solo por tel\xE9fono con SMS

Responde de manera amigable, profesional y concisa en espa\xF1ol.`
              },
              { role: "user", content: message }
            ],
            max_tokens: 500,
            temperature: 0.7
          })
        }
      );
      if (response.ok) {
        const data = await response.json();
        return data.choices?.[0]?.message?.content || fallbackResponse;
      }
    }
    if (geminiKey) {
      const listResponse = await fetch(
        `https://generativelanguage.googleapis.com/v1beta/models?key=${geminiKey}`,
        { method: "GET", headers: { "Content-Type": "application/json" } }
      );
      if (listResponse.ok) {
        const models = await listResponse.json();
        console.log("Available models:", JSON.stringify(models, null, 2));
      }
      const contents = [
        {
          role: "user",
          parts: [
            {
              text: `Eres un asistente de soporte para ComeYa. Responde en espa\xF1ol de forma amigable y concisa.

Usuario: ${message}`
            }
          ]
        }
      ];
      const response = await fetch(
        `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${geminiKey}`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ contents })
        }
      );
      if (response.ok) {
        const data = await response.json();
        console.log("Gemini response:", JSON.stringify(data));
        return data.candidates?.[0]?.content?.parts?.[0]?.text || fallbackResponse;
      } else {
        console.log("Gemini error:", response.status, await response.text());
      }
    }
    return fallbackResponse;
  } catch (error) {
    console.error("AI Error:", error);
    return fallbackResponse;
  }
}
router18.get("/tickets/:userId", authenticateToken, async (req, res) => {
  try {
    const { supportChats: supportChats2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, desc: desc19 } = await import("drizzle-orm");
    const tickets = await db2.select().from(supportChats2).where(eq79(supportChats2.userId, req.params.userId)).orderBy(desc19(supportChats2.createdAt));
    res.json({ success: true, tickets });
  } catch (error) {
    console.error("Error fetching user tickets:", error);
    res.status(500).json({ error: error.message });
  }
});
router18.post("/chat", authenticateToken, async (req, res) => {
  try {
    const { message, history } = req.body;
    if (!message) {
      return res.status(400).json({ error: "Message is required" });
    }
    const response = await generateAIResponse(message, history || []);
    res.json({ success: true, response });
  } catch (error) {
    console.error("AI chat error:", error);
    res.status(500).json({ error: error.message });
  }
});
router18.post("/tickets", authenticateToken, async (req, res) => {
  try {
    const { supportChats: supportChats2, supportMessages: supportMessages2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { randomUUID: randomUUID5 } = await import("crypto");
    const { message, subject, priority } = req.body;
    const chatId = randomUUID5();
    await db2.insert(supportChats2).values({
      id: chatId,
      userId: req.user.id,
      status: "open"
    });
    await db2.insert(supportMessages2).values({
      id: randomUUID5(),
      chatId,
      userId: req.user.id,
      message: subject || message,
      isBot: false
    });
    res.json({ success: true, chatId });
  } catch (error) {
    console.error("Error creating ticket:", error);
    res.status(500).json({ error: error.message });
  }
});
router18.get("/tickets/:id/messages", authenticateToken, async (req, res) => {
  try {
    const { supportMessages: supportMessages2, supportChats: supportChats2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79, and: and47 } = await import("drizzle-orm");
    const [ticket] = await db2.select().from(supportChats2).where(eq79(supportChats2.id, req.params.id)).limit(1);
    if (!ticket || ticket.userId !== req.user.id && req.user.role !== "admin" && req.user.role !== "super_admin") {
      return res.status(403).json({ error: "No tienes acceso a este ticket" });
    }
    const messages = await db2.select().from(supportMessages2).where(eq79(supportMessages2.chatId, req.params.id)).orderBy(supportMessages2.createdAt);
    res.json({ success: true, messages });
  } catch (error) {
    console.error("Error fetching messages:", error);
    res.status(500).json({ error: error.message });
  }
});
router18.post("/tickets/:id/messages", authenticateToken, async (req, res) => {
  try {
    const { supportMessages: supportMessages2, supportChats: supportChats2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { randomUUID: randomUUID5 } = await import("crypto");
    const { eq: eq79 } = await import("drizzle-orm");
    const [ticket] = await db2.select().from(supportChats2).where(eq79(supportChats2.id, req.params.id)).limit(1);
    if (!ticket || ticket.userId !== req.user.id && req.user.role !== "admin" && req.user.role !== "super_admin") {
      return res.status(403).json({ error: "No tienes acceso a este ticket" });
    }
    const { message } = req.body;
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    const newMessage = {
      id: randomUUID5(),
      chatId: req.params.id,
      userId: req.user.id,
      message,
      isBot: false,
      isAdmin
    };
    await db2.insert(supportMessages2).values(newMessage);
    res.json({ success: true, message: newMessage });
  } catch (error) {
    console.error("Error sending message:", error);
    res.status(500).json({ error: error.message });
  }
});
router18.post("/create-ticket", authenticateToken, async (req, res) => {
  try {
    const { supportChats: supportChats2, supportMessages: supportMessages2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { randomUUID: randomUUID5 } = await import("crypto");
    const { message, subject, category } = req.body;
    const chatId = randomUUID5();
    await db2.insert(supportChats2).values({
      id: chatId,
      userId: req.user.id,
      subject: subject || "Consulta de soporte",
      category: category || "general",
      status: "open",
      priority: "medium"
    });
    await db2.insert(supportMessages2).values({
      id: randomUUID5(),
      chatId,
      userId: req.user.id,
      message,
      isBot: false,
      isAdmin: false
    });
    res.json({ success: true, chatId });
  } catch (error) {
    console.error("Error creating support chat:", error);
    res.status(500).json({ error: error.message });
  }
});
router18.get(
  "/admin/tickets",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { supportChats: supportChats2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, desc: desc19 } = await import("drizzle-orm");
      const tickets = await db2.select().from(supportChats2).orderBy(desc19(supportChats2.createdAt));
      const enrichedTickets = await Promise.all(
        tickets.map(async (ticket) => {
          const [user] = await db2.select({
            id: users5.id,
            name: users5.name,
            email: users5.email,
            phone: users5.phone
          }).from(users5).where(eq79(users5.id, ticket.userId)).limit(1);
          return {
            id: ticket.id,
            userId: ticket.userId,
            userName: user?.name || "Usuario",
            userEmail: user?.email || "",
            subject: ticket.subject || "Sin asunto",
            status: ticket.status || "open",
            priority: ticket.priority || "medium",
            category: ticket.category || "general",
            createdAt: ticket.createdAt,
            updatedAt: ticket.updatedAt,
            lastMessageAt: ticket.updatedAt,
            messageCount: 0
          };
        })
      );
      res.json({ success: true, tickets: enrichedTickets });
    } catch (error) {
      console.error("Error fetching tickets:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router18.put(
  "/tickets/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { supportChats: supportChats2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { status, priority } = req.body;
      await db2.update(supportChats2).set({
        ...status && { status },
        ...priority && { priority },
        updatedAt: /* @__PURE__ */ new Date()
      }).where(eq79(supportChats2.id, req.params.id));
      res.json({ success: true });
    } catch (error) {
      console.error("Error updating ticket:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var supportRoutes_default = router18;

// server/withdrawalRoutes.ts
var import_express19 = require("express");
init_withdrawalService();
init_db();
init_schema_mysql();
var import_drizzle_orm43 = require("drizzle-orm");
var router19 = (0, import_express19.Router)();
router19.post("/request", authenticateToken, async (req, res) => {
  try {
    const { userId, amount, method, bankAccount } = req.body;
    if (!userId || !amount || !method) {
      return res.status(400).json({ error: "Faltan datos requeridos" });
    }
    const isStaff = req.user?.role === "admin" || req.user?.role === "super_admin";
    if (!isStaff && userId !== req.user?.id) {
      return res.status(403).json({ error: "No autorizado" });
    }
    if (amount < 5e3) {
      return res.status(400).json({ error: "El monto m\xEDnimo de retiro es 50 \u20AC" });
    }
    if (method === "bank_transfer" && !bankAccount) {
    }
    const result = await withdrawalService.requestWithdrawal({
      userId,
      amount,
      method,
      bankAccount
    });
    res.json(result);
  } catch (error) {
    console.error("Error requesting withdrawal:", error);
    res.status(400).json({ error: error.message });
  }
});
router19.get(
  "/history/:userId",
  authenticateToken,
  async (req, res) => {
    try {
      const { userId } = req.params;
      const role = req.user?.role;
      if (userId !== req.user?.id && role !== "admin" && role !== "super_admin") {
        return res.status(403).json({ error: "No autorizado" });
      }
      const history = await withdrawalService.getWithdrawalHistory(userId);
      res.json(history);
    } catch (error) {
      console.error("Error fetching withdrawal history:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router19.get(
  "/admin/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const pending = await db.select().from(withdrawalRequests).where((0, import_drizzle_orm43.eq)(withdrawalRequests.status, "pending")).orderBy(withdrawalRequests.createdAt);
      res.json(pending);
    } catch (error) {
      console.error("Error fetching pending withdrawals:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router19.post(
  "/admin/approve/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { id } = req.params;
      const adminId = req.user?.id || req.body?.adminId;
      if (!adminId) {
        return res.status(400).json({ error: "Se requiere ID de administrador" });
      }
      const result = await withdrawalService.approveWithdrawal(id, adminId);
      res.json(result);
    } catch (error) {
      console.error("Error approving withdrawal:", error);
      res.status(400).json({ error: error.message });
    }
  }
);
var withdrawalRoutes_default = router19;

// server/cashSettlementRoutes.ts
var import_express20 = require("express");
init_db();
init_schema_mysql();
var import_drizzle_orm44 = require("drizzle-orm");
init_orderNumberService();
var router20 = (0, import_express20.Router)();
router20.get("/pending", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const ownerBusinesses = await db.select().from(businesses3).where((0, import_drizzle_orm44.eq)(businesses3.ownerId, userId));
    if (ownerBusinesses.length === 0) {
      return res.json({ success: true, orders: [], total: 0 });
    }
    const businessIds = ownerBusinesses.map((b) => b.id);
    const { inArray: inArray12, sql: sql23 } = await import("drizzle-orm");
    const allOrders = await db.select().from(orders);
    const pendingOrders = allOrders.filter(
      (o) => businessIds.includes(o.businessId) && o.paymentMethod === "cash" && o.status === "delivered" && o.cashSettled === false
    );
    const { financialService: financialService2 } = await Promise.resolve().then(() => (init_unifiedFinancialService(), unifiedFinancialService_exports));
    let total = 0;
    for (const order of pendingOrders) {
      if (order.businessEarnings) {
        total += order.businessEarnings;
      } else {
        const businessShare = order.subtotal;
        total += businessShare;
      }
    }
    res.json({
      success: true,
      orders: pendingOrders,
      total,
      count: pendingOrders.length
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router20.post("/settle/:orderId", authenticateToken, async (req, res) => {
  try {
    const { orderId } = req.params;
    const userId = req.user.id;
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm44.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const [business] = await db.select().from(businesses3).where(
      (0, import_drizzle_orm44.and)(
        (0, import_drizzle_orm44.eq)(businesses3.id, order.businessId),
        (0, import_drizzle_orm44.eq)(businesses3.ownerId, userId)
      )
    ).limit(1);
    if (!business) {
      return res.status(403).json({ error: "No tienes permiso" });
    }
    if (order.paymentMethod !== "cash") {
      return res.status(400).json({ error: "El pedido no es en efectivo" });
    }
    if (order.cashSettled) {
      return res.status(400).json({ error: "Ya est\xE1 liquidado" });
    }
    const recentSettlement = await db.select().from(transactions).where(
      (0, import_drizzle_orm44.and)(
        (0, import_drizzle_orm44.eq)(transactions.orderId, orderId),
        (0, import_drizzle_orm44.eq)(transactions.type, "cash_settlement"),
        (0, import_drizzle_orm44.eq)(transactions.status, "completed")
      )
    ).limit(1);
    if (recentSettlement.length > 0) {
      return res.status(400).json({ error: "Este pedido ya fue liquidado" });
    }
    const { financialService: financialService2 } = await Promise.resolve().then(() => (init_unifiedFinancialService(), unifiedFinancialService_exports));
    const commissions = await financialService2.calculateCommissions(
      order.total,
      order.deliveryFee || 0
    );
    const totalDebtForOrder = commissions.business + commissions.platform;
    await db.update(orders).set({
      cashSettled: 1,
      cashSettledAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm44.eq)(orders.id, orderId));
    if (order.deliveryPersonId) {
      const [driverWallet] = await db.select().from(wallets).where((0, import_drizzle_orm44.eq)(wallets.userId, order.deliveryPersonId)).limit(1);
      if (driverWallet) {
        await db.update(wallets).set({
          cashOwed: Math.max(0, driverWallet.cashOwed - totalDebtForOrder)
        }).where((0, import_drizzle_orm44.eq)(wallets.userId, order.deliveryPersonId));
        await db.insert(transactions).values({
          walletId: driverWallet.id,
          userId: order.deliveryPersonId,
          orderId: order.id,
          type: "cash_debt_payment",
          amount: -totalDebtForOrder,
          balanceBefore: driverWallet.cashOwed,
          balanceAfter: Math.max(0, driverWallet.cashOwed - totalDebtForOrder),
          description: `Deuda liquidada por negocio - Pedido ${orderRef(order)}`,
          status: "completed"
        });
      }
    }
    res.json({
      success: true,
      message: "Liquidaci\xF3n registrada",
      settled: (totalDebtForOrder / 100).toFixed(2),
      breakdown: {
        business: (commissions.business / 100).toFixed(2),
        platform: (commissions.platform / 100).toFixed(2),
        total: (totalDebtForOrder / 100).toFixed(2)
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var cashSettlementRoutes_default = router20;

// server/weeklySettlementRoutes.ts
var import_express21 = require("express");
init_weeklySettlementService();
init_db();
var import_drizzle_orm46 = require("drizzle-orm");
var router21 = (0, import_express21.Router)();
router21.get("/driver/pending", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const settlement = await WeeklySettlementService.getDriverPendingSettlement(userId);
    const bankResult = await db.execute(import_drizzle_orm46.sql`
      SELECT * FROM platform_bank_account WHERE is_active = 1 LIMIT 1
    `);
    const bankRows = Array.isArray(bankResult) ? Array.isArray(bankResult[0]) ? bankResult[0] : bankResult : bankResult?.rows || [];
    res.json({
      success: true,
      settlement,
      bankAccount: bankRows[0] || null
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router21.post("/driver/submit-proof", authenticateToken, async (req, res) => {
  try {
    const { settlementId, proofUrl } = req.body;
    await WeeklySettlementService.submitPaymentProof(settlementId, proofUrl);
    res.json({ success: true, message: "Comprobante enviado" });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router21.get(
  "/admin/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const settlements = await WeeklySettlementService.getAllPendingSettlements();
      res.json({ success: true, settlements });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router21.post(
  "/admin/approve/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { id } = req.params;
      const adminId = req.user.id;
      await WeeklySettlementService.approveSettlement(id, adminId);
      res.json({ success: true, message: "Liquidaci\xF3n aprobada" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router21.post(
  "/admin/reject/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { id } = req.params;
      const { notes } = req.body;
      const adminId = req.user.id;
      await WeeklySettlementService.rejectSettlement(id, adminId, notes);
      res.json({ success: true, message: "Liquidaci\xF3n rechazada" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router21.post(
  "/admin/bank-account",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { bankName, accountHolder, clabe, accountNumber, notes } = req.body;
      await db.execute(import_drizzle_orm46.sql`UPDATE platform_bank_account SET is_active = 0`);
      await db.execute(import_drizzle_orm46.sql`
      INSERT INTO platform_bank_account 
      (bank_name, account_holder, clabe, account_number, notes, is_active)
      VALUES (${bankName}, ${accountHolder}, ${clabe}, ${accountNumber || ""}, ${notes || ""}, 1)
    `);
      res.json({ success: true, message: "Cuenta bancaria configurada" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router21.get(
  "/admin/bank-account",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await db.execute(import_drizzle_orm46.sql`
      SELECT * FROM platform_bank_account WHERE is_active = 1 LIMIT 1
    `);
      const rows = Array.isArray(result) ? Array.isArray(result[0]) ? result[0] : result : result?.rows || [];
      res.json({ success: true, bankAccount: rows[0] || null });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
function requireCronAccess(req, res, next) {
  const role = req.user?.role;
  if (role === "admin" || role === "super_admin") return next();
  const secret = process.env.CRON_SECRET;
  if (secret && req.headers["x-cron-secret"] === secret) return next();
  return res.status(403).json({ error: "No autorizado" });
}
router21.post(
  "/cron/close-week",
  authenticateToken,
  requireCronAccess,
  async (req, res) => {
    try {
      const result = await WeeklySettlementService.closeWeek();
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router21.post(
  "/cron/block-unpaid",
  authenticateToken,
  requireCronAccess,
  async (req, res) => {
    try {
      const result = await WeeklySettlementService.blockUnpaidDrivers();
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var weeklySettlementRoutes_default = router21;

// server/financialAuditRoutes.ts
var import_express22 = require("express");

// server/financialAuditService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm47 = require("drizzle-orm");
init_unifiedFinancialService();
var FinancialAuditService = class {
  // REGLA 1: Comisiones suman 100%
  async auditCommissionRates() {
    try {
      const rates = await financialService.getCommissionRates();
      const total = rates.platform + rates.business + rates.driver;
      const passed = Math.abs(total - 1) < 1e-3;
      return {
        passed,
        rule: "Commission Rates Sum to 100%",
        details: passed ? `\u2713 Rates valid: Platform ${(rates.platform * 100).toFixed(1)}% + Business ${(rates.business * 100).toFixed(1)}% + Driver ${(rates.driver * 100).toFixed(1)}% = 100%` : `\u2717 Rates invalid: Total = ${(total * 100).toFixed(2)}%`,
        severity: passed ? "info" : "critical",
        expectedValue: 1,
        actualValue: total
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Commission Rates Sum to 100%",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // REGLA 2: Total pedido = subtotal + deliveryFee + tax
  async auditOrderTotals() {
    try {
      const allOrders = await db.select().from(orders);
      const invalidOrders = [];
      for (const order of allOrders) {
        const nemyCommission = Math.round(order.subtotal * 0.15);
        const expectedTotal = order.subtotal + nemyCommission + order.deliveryFee;
        if (order.total !== expectedTotal) {
          invalidOrders.push(
            `${order.id.slice(-6)}: expected ${expectedTotal}, got ${order.total}`
          );
        }
      }
      return {
        passed: invalidOrders.length === 0,
        rule: "Order Totals Match Calculation",
        details: invalidOrders.length === 0 ? `\u2713 All ${allOrders.length} orders have correct totals` : `\u2717 ${invalidOrders.length}/${allOrders.length} orders with incorrect totals`,
        severity: invalidOrders.length === 0 ? "info" : "critical",
        affectedEntities: invalidOrders.slice(0, 10)
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Order Totals Match Calculation",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // REGLA 3: Comisiones distribuidas = total pedido
  async auditCommissionDistribution() {
    try {
      const deliveredOrders = await db.select().from(orders).where((0, import_drizzle_orm47.eq)(orders.status, "delivered"));
      const invalidOrders = [];
      for (const order of deliveredOrders) {
        if (order.platformFee && order.businessEarnings && order.deliveryEarnings) {
          const distributed = order.platformFee + order.businessEarnings + order.deliveryEarnings;
          if (distributed !== order.total) {
            invalidOrders.push(
              `${order.id.slice(-6)}: total ${order.total}, distributed ${distributed}`
            );
          }
        }
      }
      return {
        passed: invalidOrders.length === 0,
        rule: "Commission Distribution Equals Order Total",
        details: invalidOrders.length === 0 ? `\u2713 All ${deliveredOrders.length} delivered orders correctly distributed` : `\u2717 ${invalidOrders.length}/${deliveredOrders.length} orders with distribution errors`,
        severity: invalidOrders.length === 0 ? "info" : "critical",
        affectedEntities: invalidOrders.slice(0, 10)
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Commission Distribution Equals Order Total",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // REGLA 4: Balance wallet = suma transacciones
  async auditWalletBalances() {
    try {
      const allWallets = await db.select().from(wallets);
      const invalidWallets = [];
      for (const wallet of allWallets) {
        const txs = await db.select().from(transactions).where((0, import_drizzle_orm47.eq)(transactions.walletId, wallet.id));
        const calculatedBalance = txs.reduce((sum3, tx) => sum3 + tx.amount, 0);
        if (wallet.balance !== calculatedBalance) {
          invalidWallets.push(
            `${wallet.userId.slice(-6)}: expected ${calculatedBalance}, got ${wallet.balance}`
          );
        }
      }
      return {
        passed: invalidWallets.length === 0,
        rule: "Wallet Balances Match Transaction History",
        details: invalidWallets.length === 0 ? `\u2713 All ${allWallets.length} wallets have correct balances` : `\u2717 ${invalidWallets.length}/${allWallets.length} wallets with balance mismatches`,
        severity: invalidWallets.length === 0 ? "info" : "critical",
        affectedEntities: invalidWallets.slice(0, 10)
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Wallet Balances Match Transaction History",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // REGLA 5: Transacciones tienen balanceBefore/After consistentes
  async auditTransactionChain() {
    try {
      const allWallets = await db.select().from(wallets);
      const invalidChains = [];
      for (const wallet of allWallets) {
        const txs = await db.select().from(transactions).where((0, import_drizzle_orm47.eq)(transactions.walletId, wallet.id)).orderBy(transactions.createdAt);
        for (let i = 0; i < txs.length; i++) {
          const tx = txs[i];
          const expectedAfter = (tx.balanceBefore || 0) + tx.amount;
          if (tx.balanceAfter !== expectedAfter) {
            invalidChains.push(
              `Wallet ${wallet.userId.slice(-6)}, tx ${i + 1}: chain broken`
            );
            break;
          }
        }
      }
      return {
        passed: invalidChains.length === 0,
        rule: "Transaction Chains Are Consistent",
        details: invalidChains.length === 0 ? `\u2713 All wallet transaction chains are valid` : `\u2717 ${invalidChains.length} wallets with broken transaction chains`,
        severity: invalidChains.length === 0 ? "info" : "critical",
        affectedEntities: invalidChains.slice(0, 10)
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Transaction Chains Are Consistent",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // REGLA 6: Pagos Stripe coinciden con orders
  async auditStripePayments() {
    try {
      const allPayments = await db.select().from(payments);
      const invalidPayments = [];
      for (const payment of allPayments) {
        const [order] = await db.select().from(orders).where((0, import_drizzle_orm47.eq)(orders.id, payment.orderId)).limit(1);
        if (!order) {
          invalidPayments.push(
            `Payment ${payment.id.slice(-6)}: order not found`
          );
          continue;
        }
        if (payment.amount !== order.total) {
          invalidPayments.push(
            `Payment ${payment.id.slice(-6)}: amount ${payment.amount} != order ${order.total}`
          );
        }
      }
      return {
        passed: invalidPayments.length === 0,
        rule: "Stripe Payments Match Order Totals",
        details: invalidPayments.length === 0 ? `\u2713 All ${allPayments.length} payments match their orders` : `\u2717 ${invalidPayments.length}/${allPayments.length} payments with mismatches`,
        severity: invalidPayments.length === 0 ? "info" : "warning",
        affectedEntities: invalidPayments.slice(0, 10)
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Stripe Payments Match Order Totals",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // REGLA 7: Transacciones coinciden con comisiones del pedido
  async auditTransactionAmounts() {
    try {
      const deliveredOrders = await db.select().from(orders).where((0, import_drizzle_orm47.eq)(orders.status, "delivered"));
      const invalidTransactions = [];
      for (const order of deliveredOrders) {
        if (!order.deliveryPersonId || !order.deliveryEarnings) continue;
        const driverTxs = await db.select().from(transactions).where((0, import_drizzle_orm47.eq)(transactions.orderId, order.id));
        const driverTx = driverTxs.find(
          (tx) => tx.userId === order.deliveryPersonId
        );
        if (driverTx && driverTx.amount !== order.deliveryEarnings) {
          invalidTransactions.push(
            `Order ${order.id.slice(-6)}: driver tx ${driverTx.amount} != expected ${order.deliveryEarnings}`
          );
        }
      }
      return {
        passed: invalidTransactions.length === 0,
        rule: "Transaction Amounts Match Order Commissions",
        details: invalidTransactions.length === 0 ? `\u2713 All transactions match their order commissions` : `\u2717 ${invalidTransactions.length} transactions with incorrect amounts`,
        severity: invalidTransactions.length === 0 ? "info" : "critical",
        affectedEntities: invalidTransactions.slice(0, 10)
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Transaction Amounts Match Order Commissions",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // REGLA 8: Ganancias del repartidor = deliveryEarnings (no deliveryFee)
  async auditDriverEarningsCalculation() {
    try {
      const deliveredOrders = await db.select().from(orders).where((0, import_drizzle_orm47.eq)(orders.status, "delivered"));
      const invalidEarnings = [];
      const rates = await financialService.getCommissionRates();
      for (const order of deliveredOrders) {
        if (!order.deliveryPersonId) continue;
        const expectedEarnings = Math.round(order.deliveryFee * rates.driver);
        if (order.deliveryEarnings !== expectedEarnings) {
          invalidEarnings.push(
            `Order ${order.id.slice(-6)}: deliveryEarnings ${order.deliveryEarnings} != expected ${expectedEarnings} (${(rates.driver * 100).toFixed(0)}% of ${order.deliveryFee})`
          );
        }
      }
      return {
        passed: invalidEarnings.length === 0,
        rule: "Driver Earnings = 15% of Delivery Fee",
        details: invalidEarnings.length === 0 ? `\u2713 All ${deliveredOrders.length} orders have correct driver earnings (15% of deliveryFee)` : `\u2717 ${invalidEarnings.length}/${deliveredOrders.length} orders with incorrect driver earnings`,
        severity: invalidEarnings.length === 0 ? "info" : "critical",
        affectedEntities: invalidEarnings.slice(0, 10)
      };
    } catch (error) {
      return {
        passed: false,
        rule: "Driver Earnings = 15% of Delivery Fee",
        details: `\u2717 Error: ${error.message}`,
        severity: "critical"
      };
    }
  }
  // Ejecutar auditoría completa
  async runFullAudit() {
    const results = [];
    results.push(await this.auditCommissionRates());
    results.push(await this.auditOrderTotals());
    results.push(await this.auditCommissionDistribution());
    results.push(await this.auditWalletBalances());
    results.push(await this.auditTransactionChain());
    results.push(await this.auditStripePayments());
    results.push(await this.auditTransactionAmounts());
    results.push(await this.auditDriverEarningsCalculation());
    const passed = results.filter((r) => r.passed).length;
    const failed = results.filter(
      (r) => !r.passed && r.severity === "critical"
    ).length;
    const warnings = results.filter(
      (r) => !r.passed && r.severity === "warning"
    ).length;
    let systemHealth = "healthy";
    if (failed > 0) systemHealth = "critical";
    else if (warnings > 0) systemHealth = "warning";
    return {
      timestamp: /* @__PURE__ */ new Date(),
      totalChecks: results.length,
      passed,
      failed,
      warnings,
      results,
      systemHealth
    };
  }
};
var financialAuditService = new FinancialAuditService();

// server/financialAuditRoutes.ts
var router22 = (0, import_express22.Router)();
router22.get(
  "/full",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const report = await financialAuditService.runFullAudit();
      res.json(report);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router22.get(
  "/commission-rates",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await financialAuditService.auditCommissionRates();
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router22.get(
  "/order-totals",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await financialAuditService.auditOrderTotals();
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router22.get(
  "/wallet-balances",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await financialAuditService.auditWalletBalances();
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var financialAuditRoutes_default = router22;

// server/favoritesRoutes.ts
var import_express23 = __toESM(require("express"));
var router23 = import_express23.default.Router();
console.log("\u{1F527} Favorites routes loaded");
router23.get("/:userId", authenticateToken, async (req, res) => {
  try {
    const userId = req.params.userId;
    const role = req.user?.role;
    if (userId !== req.user?.id && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    const { favorites: favorites2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const rows = await db2.select({
      id: favorites2.id,
      userId: favorites2.userId,
      businessId: favorites2.businessId,
      productId: favorites2.productId,
      businessName: businesses3.name,
      businessImage: businesses3.image,
      businessType: businesses3.type,
      businessRating: businesses3.rating
    }).from(favorites2).leftJoin(businesses3, eq79(favorites2.businessId, businesses3.id)).where(eq79(favorites2.userId, userId));
    const mapped = rows.map((row) => ({
      id: row.id,
      userId: row.userId,
      businessId: row.businessId,
      productId: row.productId,
      business: row.businessId ? {
        id: row.businessId,
        name: row.businessName,
        image: row.businessImage,
        type: row.businessType,
        rating: ((row.businessRating || 0) / 10).toFixed(1)
      } : null
    }));
    res.json(mapped);
  } catch (error) {
    console.error("\u274C ERROR:", error);
    res.status(500).json({ error: error.message });
  }
});
router23.post("/", authenticateToken, async (req, res) => {
  try {
    const { favorites: favorites2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { v4: uuidv4 } = await import("uuid");
    const { and: and47, eq: eq79 } = await import("drizzle-orm");
    const { userId, businessId, productId } = req.body;
    const role = req.user?.role;
    if (userId !== req.user?.id && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    const existing = await db2.select().from(favorites2).where(
      and47(
        eq79(favorites2.userId, userId),
        businessId ? eq79(favorites2.businessId, businessId) : eq79(favorites2.productId, productId)
      )
    ).limit(1);
    if (existing.length > 0) {
      return res.json({ success: true, favorite: existing[0] });
    }
    const newFavorite = {
      id: uuidv4(),
      userId,
      businessId: businessId || null,
      productId: productId || null
    };
    await db2.insert(favorites2).values(newFavorite);
    res.json({ success: true, favorite: newFavorite });
  } catch (error) {
    console.error("Error adding favorite:", error);
    res.status(500).json({ error: error.message });
  }
});
router23.delete("/:id", authenticateToken, async (req, res) => {
  try {
    const { favorites: favorites2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [existing] = await db2.select().from(favorites2).where(eq79(favorites2.id, req.params.id)).limit(1);
    const role = req.user?.role;
    if (!existing || existing.userId !== req.user?.id && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    await db2.delete(favorites2).where(eq79(favorites2.id, req.params.id));
    res.json({ success: true });
  } catch (error) {
    console.error("Error removing favorite:", error);
    res.status(500).json({ error: error.message });
  }
});
router23.get("/check/:userId/:itemId", authenticateToken, async (req, res) => {
  try {
    const { favorites: favorites2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { and: and47, eq: eq79, or: or8 } = await import("drizzle-orm");
    const { userId, itemId } = req.params;
    const [favorite] = await db2.select().from(favorites2).where(
      and47(
        eq79(favorites2.userId, userId),
        or8(eq79(favorites2.businessId, itemId), eq79(favorites2.productId, itemId))
      )
    ).limit(1);
    res.json({ isFavorite: !!favorite, favoriteId: favorite?.id });
  } catch (error) {
    console.error("Error checking favorite:", error);
    res.status(500).json({ error: error.message });
  }
});
var favoritesRoutes_default = router23;

// server/deliveryRoutes.ts
var import_express24 = require("express");
init_db();
init_schema_mysql();
var import_drizzle_orm52 = require("drizzle-orm");
init_errors();
init_logger();
init_orderNumberService();
init_distance();
init_enhancedPushService();
var router24 = (0, import_express24.Router)();
var DELIVERY_RADIUS_KM = 0.2;
router24.post(
  "/register",
  authenticateToken,
  asyncHandler(async (req, res) => {
    const { userId, vehicleType, vehiclePlate, bankClabe, bankName } = req.body;
    if (!vehicleType || !vehiclePlate) {
      throw new ValidationError("Vehicle type and plate are required");
    }
    if (![
      "bike",
      "bicycle",
      "ebike",
      "scooter",
      "moped",
      "motorcycle",
      "car"
    ].includes(vehicleType)) {
      throw new ValidationError("Invalid vehicle type");
    }
    const [existing] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    if (existing) {
      await db.update(deliveryDrivers).set({
        vehicleType,
        vehiclePlate: vehiclePlate.toUpperCase()
      }).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId));
    } else {
      await db.insert(deliveryDrivers).values({
        userId,
        vehicleType,
        vehiclePlate: vehiclePlate.toUpperCase(),
        isAvailable: false,
        totalDeliveries: 0,
        rating: 0,
        totalRatings: 0,
        strikes: 0,
        isBlocked: false
      });
    }
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    const [existingWallet] = await db.select({ id: wallets.id }).from(wallets).where((0, import_drizzle_orm52.eq)(wallets.userId, userId)).limit(1);
    if (!existingWallet) {
      await db.insert(wallets).values({
        userId,
        balance: 0,
        pendingBalance: 0,
        totalEarned: 0,
        totalWithdrawn: 0
      });
    }
    logger.delivery("Driver registered", { userId, driverId: driver.id });
    if (bankClabe) {
      try {
        const { paymentAccounts: paymentAccounts2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
        await db.insert(paymentAccounts2).values({
          id: crypto.randomUUID(),
          userId,
          method: "transferencia",
          isDefault: true,
          binanceId: bankClabe,
          zelleEmail: bankName || "Transferencia SEPA",
          label: "Cuenta principal"
        });
      } catch (accountError) {
        console.error("Error saving driver bank account:", accountError);
      }
    }
    try {
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      const pendingAdmins = await db.select({ id: users.id }).from(users).where((0, import_drizzle_orm52.inArray)(users.role, ["admin", "super_admin"]));
      for (const admin of pendingAdmins) {
        await sendPushToUser2(admin.id, {
          title: "\u{1F6F5} Repartidor pendiente de aprobaci\xF3n",
          body: "Un nuevo repartidor se registr\xF3 y requiere verificaci\xF3n",
          data: { screen: "AdminVerifications" }
        });
      }
    } catch (err) {
      console.error("Error notifying admins of new driver:", err);
    }
    res.json({ driver, message: "Driver registered" });
  })
);
router24.post(
  "/location",
  authenticateToken,
  requireApprovedDriver,
  asyncHandler(async (req, res) => {
    const userId = req.user.id;
    const { latitude, longitude, accuracy, timestamp: timestamp2 } = req.body;
    if (!latitude || !longitude) {
      throw new ValidationError("Latitude and longitude required");
    }
    const { handleDriverLocationUpdate: handleDriverLocationUpdate2 } = await Promise.resolve().then(() => (init_trackingPipeline(), trackingPipeline_exports));
    await handleDriverLocationUpdate2(userId, latitude, longitude, {
      accuracy: Number(accuracy) || void 0,
      timestamp: Number(timestamp2) || void 0
    });
    res.json({ success: true });
  })
);
router24.get(
  "/status",
  authenticateToken,
  asyncHandler(async (req, res) => {
    const userId = req.user.id;
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    let totalDeliveries = 0;
    let rating = 0;
    try {
      const { sql: sql23 } = await import("drizzle-orm");
      const [deliveries] = await db.execute(sql23`
        SELECT
          (SELECT COUNT(*) FROM orders o
            WHERE o.delivery_person_id = ${userId}
              AND o.status IN ('delivered', 'completed')) AS total,
          (SELECT AVG(delivery_person_rating) FROM reviews r
            WHERE r.delivery_person_id = ${userId}
              AND r.delivery_person_rating IS NOT NULL) AS avgRating
      `);
      const row = deliveries[0];
      totalDeliveries = Number(row?.total) || 0;
      rating = Number(row?.avgRating) || 0;
    } catch (statsError) {
      console.error("Error loading driver stats in /status:", statsError);
    }
    res.json({
      success: true,
      isOnline: driver?.isAvailable ?? false,
      verificationStatus: req.user?.verificationStatus ?? "pending",
      strikes: driver?.strikes ?? 0,
      isBlocked: driver?.isBlocked ?? false,
      blockedUntil: driver?.blockedUntil ?? null,
      rating,
      totalDeliveries
    });
  })
);
router24.get(
  "/stats",
  authenticateToken,
  asyncHandler(async (req, res) => {
    const userId = req.user.id;
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    if (!driver) {
      return res.json({
        success: true,
        stats: {
          totalDeliveries: 0,
          rating: 0,
          totalRatings: 0,
          completionRate: 100,
          todayEarnings: 0,
          weekEarnings: 0,
          monthEarnings: 0,
          totalEarnings: 0,
          balance: 0,
          avgDeliveryTime: 0,
          cashOwed: 0,
          availableToWithdraw: 0,
          pendingCashOrders: []
        }
      });
    }
    const [wallet] = await db.select().from(wallets).where((0, import_drizzle_orm52.eq)(wallets.userId, userId)).limit(1);
    const { or: orOp } = await import("drizzle-orm");
    const completedOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm52.and)(
        (0, import_drizzle_orm52.eq)(orders.deliveryPersonId, userId),
        orOp((0, import_drizzle_orm52.eq)(orders.status, "delivered"), (0, import_drizzle_orm52.eq)(orders.status, "completed"))
      )
    );
    const now = /* @__PURE__ */ new Date();
    const todayStart = new Date(now);
    todayStart.setHours(0, 0, 0, 0);
    const weekStart = new Date(now);
    weekStart.setDate(now.getDate() - 7);
    weekStart.setHours(0, 0, 0, 0);
    const monthStart = new Date(now);
    monthStart.setDate(now.getDate() - 30);
    monthStart.setHours(0, 0, 0, 0);
    const todayOrders = completedOrders.filter((o) => {
      if (!o.deliveredAt) return false;
      const deliveredDate = new Date(o.deliveredAt);
      return deliveredDate >= todayStart;
    });
    const weekOrders = completedOrders.filter((o) => {
      if (!o.deliveredAt) return false;
      const deliveredDate = new Date(o.deliveredAt);
      return deliveredDate >= weekStart;
    });
    const monthOrders = completedOrders.filter((o) => {
      if (!o.deliveredAt) return false;
      const deliveredDate = new Date(o.deliveredAt);
      return deliveredDate >= monthStart;
    });
    const todayEarnings = todayOrders.reduce(
      (sum3, o) => sum3 + (o.deliveryFee || 0),
      0
    );
    const weekEarnings = weekOrders.reduce(
      (sum3, o) => sum3 + (o.deliveryFee || 0),
      0
    );
    const monthEarnings = monthOrders.reduce(
      (sum3, o) => sum3 + (o.deliveryFee || 0),
      0
    );
    const totalEarnings = completedOrders.reduce(
      (sum3, o) => sum3 + (o.deliveryFee || 0),
      0
    );
    const avgTimeMinutes = completedOrders.length > 0 ? completedOrders.reduce((sum3, order) => {
      if (order.deliveredAt && order.createdAt) {
        const diff = new Date(order.deliveredAt).getTime() - new Date(order.createdAt).getTime();
        return sum3 + Math.floor(diff / 6e4);
      }
      return sum3;
    }, 0) / completedOrders.length : 0;
    const { cashSettlementService: cashSettlementService2 } = await Promise.resolve().then(() => (init_cashSettlementService(), cashSettlementService_exports));
    const cashSummary = await cashSettlementService2.getDriverDebt(userId);
    const canWithdraw = Math.max(
      0,
      (wallet?.balance || 0) - (wallet?.cashOwed || 0)
    );
    res.json({
      success: true,
      stats: {
        totalDeliveries: completedOrders.length,
        rating: driver.rating,
        totalRatings: driver.totalRatings,
        completionRate: 100,
        todayEarnings,
        weekEarnings,
        monthEarnings,
        totalEarnings,
        balance: wallet?.balance || 0,
        avgDeliveryTime: Math.round(avgTimeMinutes),
        // Info de efectivo
        cashOwed: wallet?.cashOwed || 0,
        availableToWithdraw: canWithdraw,
        pendingCashOrders: cashSummary.pendingOrders || []
      },
      // Historial de entregas para mostrar en pantalla
      deliveries: completedOrders.map((o) => ({
        id: o.id,
        businessName: o.businessName,
        deliveryFee: o.deliveryFee || 0,
        deliveryEarnings: o.deliveryEarnings || o.deliveryFee || 0,
        deliveredAt: o.deliveredAt,
        createdAt: o.createdAt,
        paymentMethod: o.paymentMethod
      })).sort(
        (a, b) => new Date(b.deliveredAt || b.createdAt).getTime() - new Date(a.deliveredAt || a.createdAt).getTime()
      )
    });
  })
);
router24.post(
  "/toggle-status",
  authenticateToken,
  requireApprovedDriver,
  asyncHandler(async (req, res) => {
    const userId = req.user.id;
    let [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    if (!driver) {
      await db.insert(deliveryDrivers).values({
        userId,
        vehicleType: "motorcycle",
        vehiclePlate: "PENDIENTE",
        isAvailable: false,
        totalDeliveries: 0,
        rating: 0,
        totalRatings: 0,
        strikes: 0,
        isBlocked: false
      });
      [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    }
    const newStatus = !driver.isAvailable;
    await db.update(deliveryDrivers).set({
      isAvailable: newStatus,
      lastLocationUpdate: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId));
    logger.delivery(`Driver ${newStatus ? "online" : "offline"}`, { userId });
    res.json({ success: true, isOnline: newStatus });
  })
);
router24.get(
  "/orders",
  authenticateToken,
  requireApprovedDriver,
  asyncHandler(async (req, res) => {
    const userId = req.user.id;
    const myOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm52.and)(
        (0, import_drizzle_orm52.eq)(orders.deliveryPersonId, userId),
        (0, import_drizzle_orm52.inArray)(orders.status, ["ready", "picked_up", "delivered"])
      )
    );
    const { isNull: isNullOp, ne: neOp } = await import("drizzle-orm");
    const availableOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm52.and)(
        (0, import_drizzle_orm52.eq)(orders.status, "ready"),
        (0, import_drizzle_orm52.isNull)(orders.deliveryPersonId),
        (0, import_drizzle_orm52.isNull)(orders.deletedAt),
        neOp(orders.orderType, "pickup")
      )
    ).limit(10);
    res.json({ orders: myOrders, availableOrders });
  })
);
router24.get(
  "/my-orders",
  authenticateToken,
  requireApprovedDriver,
  asyncHandler(async (req, res) => {
    const userId = req.user.id;
    const rows = await db.select({
      order: orders,
      customerPhone: users.phone,
      customerName: users.name,
      businessLatitude: businesses.latitude,
      businessLongitude: businesses.longitude,
      businessAddress: businesses.address
    }).from(orders).leftJoin(users, (0, import_drizzle_orm52.eq)(users.id, orders.userId)).leftJoin(businesses, (0, import_drizzle_orm52.eq)(businesses.id, orders.businessId)).where(
      (0, import_drizzle_orm52.and)((0, import_drizzle_orm52.eq)(orders.deliveryPersonId, userId), (0, import_drizzle_orm52.isNull)(orders.deletedAt))
    ).orderBy(import_drizzle_orm52.sql`created_at DESC`).limit(50);
    res.json({
      success: true,
      orders: rows.map((r) => ({
        ...r.order,
        customerPhone: r.customerPhone,
        customerName: r.customerName,
        businessLatitude: r.businessLatitude,
        businessLongitude: r.businessLongitude,
        businessAddress: r.businessAddress
      }))
    });
  })
);
router24.get(
  "/available-orders",
  authenticateToken,
  requireApprovedDriver,
  asyncHandler(async (req, res) => {
    const userId = req.user.id;
    let [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    if (!driver) {
      await db.insert(deliveryDrivers).values({
        userId,
        vehicleType: "motorcycle",
        vehiclePlate: "PENDIENTE",
        isAvailable: false,
        totalDeliveries: 0,
        rating: 0,
        totalRatings: 0,
        strikes: 0,
        isBlocked: false
      });
      [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    }
    const { isNull: isNull9, desc: desc19, ne: ne3 } = await import("drizzle-orm");
    const availableOrders = await db.select({
      id: orders.id,
      orderNumber: orders.orderNumber,
      businessId: orders.businessId,
      businessName: orders.businessName,
      businessImage: orders.businessImage,
      businessLatitude: businesses.latitude,
      businessLongitude: businesses.longitude,
      businessAddress: businesses.address,
      status: orders.status,
      items: orders.items,
      // la lista del repartidor muestra el nº de productos
      subtotal: orders.subtotal,
      deliveryFee: orders.deliveryFee,
      total: orders.total,
      paymentMethod: orders.paymentMethod,
      deliveryAddress: orders.deliveryAddress,
      createdAt: orders.createdAt,
      estimatedPrepMinutes: orders.estimatedPrepMinutes,
      estimatedPrepRange: orders.estimatedPrepRange,
      featured: businesses.isFeatured
    }).from(orders).leftJoin(businesses, (0, import_drizzle_orm52.eq)(businesses.id, orders.businessId)).where(
      (0, import_drizzle_orm52.and)(
        (0, import_drizzle_orm52.eq)(orders.status, "ready"),
        isNull9(orders.deliveryPersonId),
        isNull9(orders.deletedAt),
        ne3(orders.orderType, "pickup")
      )
    ).orderBy(desc19(businesses.isFeatured), desc19(orders.createdAt)).limit(100);
    res.json({ success: true, orders: availableOrders });
  })
);
router24.post(
  "/accept/:orderId",
  authenticateToken,
  requireApprovedDriver,
  asyncHandler(async (req, res) => {
    const { orderId } = req.params;
    const userId = req.user.id;
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm52.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      throw new NotFoundError("Order");
    }
    if (order.deliveryPersonId) {
      throw new ValidationError("Order already assigned");
    }
    if (order.status !== "ready") {
      throw new ValidationError("Order not ready for pickup");
    }
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId)).limit(1);
    if (!driver || !driver.isAvailable) {
      throw new AuthorizationError("Driver not available");
    }
    if (driver.isBlocked) {
      if (driver.blockedUntil && new Date(driver.blockedUntil) > /* @__PURE__ */ new Date()) {
        throw new AuthorizationError(
          `Repartidor bloqueado hasta ${driver.blockedUntil.toISOString()}`
        );
      }
      throw new AuthorizationError("Repartidor bloqueado");
    }
    await db.update(orders).set({
      deliveryPersonId: userId,
      status: "ready",
      assignedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm52.eq)(orders.id, orderId));
    const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
    if (order.userId) {
      await sendPushToUser2(order.userId, {
        title: "\u{1F6F5} Repartidor asignado",
        body: `Un repartidor est\xE1 en camino a recoger tu pedido ${orderRef(order)}`,
        data: { orderId, screen: "OrderTracking" }
      });
    }
    if (order.businessId) {
      const [business] = await db.select().from(businesses).where((0, import_drizzle_orm52.eq)(businesses.id, order.businessId)).limit(1);
      if (business?.ownerId) {
        await sendPushToUser2(business.ownerId, {
          title: "\u{1F6F5} Repartidor asignado",
          body: `Un repartidor acept\xF3 el pedido ${orderRef(order)} y va en camino a recogerlo`,
          data: { orderId, screen: "BusinessOrders" }
        });
      }
    }
    logger.delivery("Order accepted", { orderId, driverId: userId });
    res.json({ success: true, order });
  })
);
router24.post(
  "/pickup/:orderId",
  authenticateToken,
  requireApprovedDriver,
  asyncHandler(async (req, res) => {
    const { orderId } = req.params;
    const userId = req.user.id;
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm52.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      throw new NotFoundError("Order");
    }
    if (order.deliveryPersonId !== userId) {
      throw new AuthorizationError("Not your order");
    }
    if (order.status !== "ready" && order.status !== "assigned") {
      throw new ValidationError("Order not ready for pickup");
    }
    await db.update(orders).set({
      status: "picked_up",
      pickedUpAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm52.eq)(orders.id, orderId));
    logger.delivery("Order picked up", { orderId, driverId: userId });
    res.json({ success: true });
  })
);
router24.put(
  "/orders/:orderId/status",
  authenticateToken,
  asyncHandler(async (req, res) => {
    const { orderId } = req.params;
    const { status } = req.body;
    const userId = req.user.id;
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm52.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      throw new NotFoundError("Order");
    }
    if (order.deliveryPersonId !== userId) {
      throw new AuthorizationError("Not your order");
    }
    const allowed = {
      on_the_way: ["picked_up", "preparing", "ready"],
      in_transit: ["on_the_way", "picked_up"],
      arriving: ["in_transit", "on_the_way"]
    };
    const prev = String(status);
    if (allowed[prev] && !allowed[prev].includes(order.status)) {
      throw new ValidationError(
        `No se puede pasar de ${order.status} a ${prev}`
      );
    }
    await db.update(orders).set({ status: prev }).where((0, import_drizzle_orm52.eq)(orders.id, orderId));
    logger.delivery(`Order status updated to ${prev}`, {
      orderId,
      driverId: userId
    });
    if (prev === "on_the_way") {
      await sendOrderStatusNotification(orderId, order.userId, "on_the_way");
      try {
        const { notifyOrderStatusChange: notifyOrderStatusChange2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
        notifyOrderStatusChange2(order.userId, orderId, "on_the_way");
      } catch {
      }
    }
    res.json({ success: true });
  })
);
router24.post(
  "/deliver/:orderId",
  authenticateToken,
  asyncHandler(async (req, res) => {
    const { orderId } = req.params;
    const userId = req.user.id;
    const { latitude, longitude } = req.body;
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm52.eq)(orders.id, orderId)).limit(1);
    if (!order) {
      throw new NotFoundError("Order");
    }
    if (order.deliveryPersonId !== userId) {
      throw new AuthorizationError("Not your order");
    }
    if (order.status === "delivered") {
      return res.status(400).json({ error: "Order already delivered" });
    }
    if (order.status !== "picked_up" && order.status !== "on_the_way" && order.status !== "in_transit" && order.status !== "arriving") {
      throw new ValidationError("Order must be picked up or on the way first");
    }
    if (latitude === void 0 || longitude === void 0) {
      throw new ValidationError(
        "Se requiere la ubicaci\xF3n GPS para finalizar la entrega"
      );
    }
    const deliveryLat = order.deliveryLatitude ?? order.deliveryLat ?? order.latitude;
    const deliveryLng = order.deliveryLongitude ?? order.deliveryLng ?? order.longitude;
    if (!deliveryLat || !deliveryLng) {
      throw new ValidationError(
        "No hay coordenadas de entrega registradas para validar la entrega"
      );
    }
    const driverLat = Number(latitude);
    const driverLng = Number(longitude);
    const deliveryLatNum = Number(deliveryLat);
    const deliveryLngNum = Number(deliveryLng);
    if ([driverLat, driverLng, deliveryLatNum, deliveryLngNum].some(
      (value) => Number.isNaN(value)
    )) {
      throw new ValidationError(
        "Coordenadas inv\xE1lidas para validar la entrega"
      );
    }
    const distanceKm = calculateDistance(
      driverLat,
      driverLng,
      deliveryLatNum,
      deliveryLngNum
    );
    if (distanceKm > DELIVERY_RADIUS_KM) {
      return res.status(400).json({
        error: `Debes estar dentro de ${Math.round(DELIVERY_RADIUS_KM * 1e3)}m del punto de entrega para finalizar`,
        distanceKm
      });
    }
    const deliveredAt = /* @__PURE__ */ new Date();
    const actualDeliveryTime = order.pickedUpAt ? Math.floor(
      (deliveredAt.getTime() - new Date(order.pickedUpAt).getTime()) / 6e4
    ) : null;
    const actualPrepTime = order.pickedUpAt && order.createdAt ? Math.floor(
      (new Date(order.pickedUpAt).getTime() - new Date(order.createdAt).getTime()) / 6e4
    ) : null;
    await db.update(orders).set({
      status: "delivered",
      deliveredAt,
      // deliveryLatitude/Longitude guardan la dirección del cliente; no
      // se pisan con la posición GPS del repartidor en el momento de
      // entregar (rompía la navegación y el tracking de pedidos antiguos).
      actualDeliveryTime,
      actualPrepTime
    }).where((0, import_drizzle_orm52.eq)(orders.id, orderId));
    if (order.paymentMethod === "cash") {
      const { cashSettlementService: cashSettlementService2 } = await Promise.resolve().then(() => (init_cashSettlementService(), cashSettlementService_exports));
      await cashSettlementService2.registerCashDebt(
        orderId,
        userId,
        order.businessId,
        order.total,
        order.deliveryFee
      );
      logger.delivery("Cash order completed - debt registered", {
        orderId,
        driverId: userId,
        total: order.total
      });
    }
    await db.update(deliveryDrivers).set({
      totalDeliveries: import_drizzle_orm52.sql`total_deliveries + 1`,
      isAvailable: true
    }).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, userId));
    const { updateBusinessPrepTimeMetrics: updateBusinessPrepTimeMetrics2, updateDriverSpeedMetrics: updateDriverSpeedMetrics2 } = await Promise.resolve().then(() => (init_metricsService(), metricsService_exports));
    updateBusinessPrepTimeMetrics2(order.businessId).catch(console.error);
    updateDriverSpeedMetrics2(userId).catch(console.error);
    const { sendOrderStatusNotification: sendOrderStatusNotification2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
    await sendOrderStatusNotification2(orderId, order.userId, "delivered");
    try {
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [biz] = await db.select({ ownerId: businesses3.ownerId, name: businesses3.name }).from(businesses3).where((0, import_drizzle_orm52.eq)(businesses3.id, order.businessId)).limit(1);
      if (biz?.ownerId) {
        await sendPushToUser2(biz.ownerId, {
          title: "\u2705 Pedido entregado",
          body: `El pedido ${orderRef(order)} fue entregado al cliente`,
          data: { orderId, screen: "BusinessOrders", type: "delivered" }
        });
      }
    } catch (err) {
      console.error("Error notifying business of delivery:", err);
    }
    logger.delivery("Order delivered", { orderId, driverId: userId });
    res.json({ success: true, message: "Pedido entregado exitosamente" });
  })
);
router24.get(
  "/location/:orderId",
  authenticateToken,
  asyncHandler(async (req, res) => {
    const { orderId } = req.params;
    const requesterId = req.user.id;
    const [order] = await db.select().from(orders).where((0, import_drizzle_orm52.eq)(orders.id, orderId)).limit(1);
    if (!order || !order.deliveryPersonId) {
      return res.json({ location: null });
    }
    const role = req.user.role;
    let allowed = order.userId === requesterId || order.deliveryPersonId === requesterId;
    if (!allowed && order.businessId) {
      const [biz] = await db.select().from(businesses).where((0, import_drizzle_orm52.eq)(businesses.id, order.businessId)).limit(1);
      allowed = biz?.ownerId === requesterId;
    }
    if (!allowed && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, order.deliveryPersonId)).limit(1);
    if (!driver || !driver.currentLatitude || !driver.currentLongitude) {
      return res.json({ location: null });
    }
    const [driverUser] = await db.select({ profilePicture: users.profilePicture }).from(users).where((0, import_drizzle_orm52.eq)(users.id, order.deliveryPersonId)).limit(1);
    res.json({
      location: {
        latitude: driver.currentLatitude,
        longitude: driver.currentLongitude,
        lastUpdate: driver.lastLocationUpdate,
        vehicleType: driver.vehicleType,
        photo: driverUser?.profilePicture || null
      }
    });
  })
);
router24.get(
  "/location/driver/:deliveryPersonId",
  authenticateToken,
  asyncHandler(async (req, res) => {
    const { deliveryPersonId } = req.params;
    const requesterId = req.user.id;
    const role = req.user.role;
    if (deliveryPersonId !== requesterId && role !== "admin" && role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm52.eq)(deliveryDrivers.userId, deliveryPersonId)).limit(1);
    if (!driver || !driver.currentLatitude || !driver.currentLongitude) {
      return res.json({ location: null });
    }
    res.json({
      location: {
        latitude: parseFloat(driver.currentLatitude),
        longitude: parseFloat(driver.currentLongitude),
        lastUpdate: driver.lastLocationUpdate
      }
    });
  })
);
var deliveryRoutes_default = router24;

// server/gpsRoutes.ts
var import_express25 = require("express");
init_db();
init_schema_mysql();
var import_drizzle_orm53 = require("drizzle-orm");
init_googleMapsService();
var router25 = (0, import_express25.Router)();
router25.get(
  "/directions",
  authenticateToken,
  async (req, res) => {
    try {
      const { originLat, originLng, destLat, destLng, mode } = req.query;
      if (!originLat || !originLng || !destLat || !destLng) {
        return res.status(400).json({ error: "Missing coordinates" });
      }
      const oLat = Number(originLat);
      const oLng = Number(originLng);
      const dLat = Number(destLat);
      const dLng = Number(destLng);
      if (!Number.isFinite(oLat) || !Number.isFinite(oLng) || !Number.isFinite(dLat) || !Number.isFinite(dLng) || oLat === 0 || oLng === 0 || dLat === 0 || dLng === 0 || Math.abs(oLat) > 90 || Math.abs(oLng) > 180 || Math.abs(dLat) > 90 || Math.abs(dLng) > 180) {
        return res.status(400).json({ error: "Coordenadas inv\xE1lidas" });
      }
      const travelMode = mode === "walking" || mode === "walk" ? "walking" : "driving";
      const preferOsrm = req.query.provider === "osrm";
      const result = await googleMapsService.getDirections(
        parseFloat(originLat),
        parseFloat(originLng),
        parseFloat(destLat),
        parseFloat(destLng),
        travelMode,
        { preferOsrm }
      );
      if (!result) {
        const distanceKm = googleMapsService.calculateHaversineDistance(
          parseFloat(originLat),
          parseFloat(originLng),
          parseFloat(destLat),
          parseFloat(destLng)
        );
        const estimatedMinutes = googleMapsService.estimateDeliveryTimeMinutes(distanceKm);
        return res.json({
          success: true,
          fallback: true,
          polyline: "",
          distance: { text: `${distanceKm.toFixed(1)} km`, value: Math.round(distanceKm * 1e3) },
          duration: { text: `${estimatedMinutes} min`, value: estimatedMinutes * 60 },
          steps: []
        });
      }
      res.json({
        success: true,
        polyline: result.polyline,
        distance: result.distance,
        duration: result.duration,
        steps: result.steps
      });
    } catch (error) {
      console.error("Directions proxy error:", error);
      res.status(500).json({ error: "Failed to get directions" });
    }
  }
);
router25.get(
  "/places-autocomplete",
  authenticateToken,
  async (req, res) => {
    try {
      const input = req.query.input || "";
      const lat = parseFloat(req.query.lat);
      const lng = parseFloat(req.query.lng);
      const bias = !isNaN(lat) && !isNaN(lng) ? { lat, lng, radiusM: 3e4 } : void 0;
      const predictions = await googleMapsService.placesAutocomplete(input, bias);
      res.json({ success: true, predictions });
    } catch (error) {
      console.error("Places autocomplete error:", error);
      res.status(500).json({ error: "Failed to get place predictions" });
    }
  }
);
router25.post(
  "/geocode",
  authenticateToken,
  async (req, res) => {
    try {
      const { address } = req.body;
      if (!address || typeof address !== "string" || address.trim().length < 3) {
        return res.status(400).json({ error: "Direcci\xF3n requerida" });
      }
      const result = await googleMapsService.geocodeAddress(address.trim());
      if (!result) {
        return res.status(404).json({ error: "No se pudo geocodificar la direcci\xF3n" });
      }
      res.json({
        success: true,
        lat: result.lat,
        lng: result.lng,
        formattedAddress: result.formattedAddress
      });
    } catch (error) {
      console.error("Geocode proxy error:", error);
      res.status(500).json({ error: "Failed to geocode address" });
    }
  }
);
router25.get(
  "/maps-stats",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const stats = googleMapsService.getUsageStats();
      let pendingNoCoords = { orders: 0, businesses: 0, addresses: 0 };
      try {
        const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
        const { sql: sql23 } = await import("drizzle-orm");
        const [ord] = await db2.execute(sql23`
          SELECT COUNT(*) AS c FROM orders
          WHERE (delivery_latitude IS NULL OR delivery_longitude IS NULL
                 OR delivery_latitude = '' OR delivery_longitude = '')
            AND status NOT IN ('delivered','cancelled','refunded')
            AND order_type <> 'pickup'
        `);
        const [biz] = await db2.execute(sql23`
          SELECT COUNT(*) AS c FROM businesses
          WHERE latitude IS NULL OR longitude IS NULL OR latitude = '' OR longitude = ''
        `);
        const [addr] = await db2.execute(sql23`
          SELECT COUNT(*) AS c FROM addresses
          WHERE latitude IS NULL OR longitude IS NULL OR latitude = '' OR longitude = ''
        `);
        pendingNoCoords = {
          orders: Number(ord?.[0]?.c) || 0,
          businesses: Number(biz?.[0]?.c) || 0,
          addresses: Number(addr?.[0]?.c) || 0
        };
      } catch {
      }
      res.json({
        success: true,
        ...stats,
        routes: routeStats,
        pendingNoCoords
      });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
async function requireAssignedDriver(req, orderId) {
  const user = req.user;
  if (!user?.id) return false;
  if (user.role === "admin" || user.role === "super_admin") return true;
  const [order] = await db.select({ deliveryPersonId: orders.deliveryPersonId }).from(orders).where((0, import_drizzle_orm53.eq)(orders.id, orderId)).limit(1);
  return !!order && order.deliveryPersonId === user.id;
}
router25.post(
  "/geofence-event",
  authenticateToken,
  async (req, res) => {
    try {
      const { orderId, type, location, distance } = req.body;
      const userId = req.user?.id;
      if (!await requireAssignedDriver(req, orderId)) {
        return res.status(403).json({ error: "No autorizado para este pedido" });
      }
      console.log(
        `\u{1F4CD} Geofence event: ${type} for order ${orderId} at ${location} (${distance}m)`
      );
      if (type === "enter" && location === "business") {
        await db.update(orders).set({ driverPickedUpAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm53.eq)(orders.id, orderId));
      } else if (type === "enter" && location === "customer") {
        await db.update(orders).set({ driverArrivedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm53.eq)(orders.id, orderId));
      }
      res.json({ success: true });
    } catch (error) {
      console.error("Error handling geofence event:", error);
      res.status(500).json({ error: "Failed to process geofence event" });
    }
  }
);
router25.post(
  "/proximity-alert",
  authenticateToken,
  async (req, res) => {
    try {
      const { orderId, type, distance, destinationType, timestamp: timestamp2 } = req.body;
      const userId = req.user?.id;
      if (!await requireAssignedDriver(req, orderId)) {
        return res.status(403).json({ error: "No autorizado para este pedido" });
      }
      console.log(
        `\u{1F514} Proximity alert: ${type} for order ${orderId} (${distance}m from ${destinationType})`
      );
      await db.insert(proximityAlerts).values({
        orderId,
        driverId: userId,
        alertType: type,
        distance,
        destinationType,
        notificationSent: true
      });
      try {
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        const [order] = await db.select({ userId: orders.userId }).from(orders).where((0, import_drizzle_orm53.eq)(orders.id, orderId)).limit(1);
        if (order?.userId && distance <= 300) {
          await sendPushToUser2(order.userId, {
            title: "\u{1F6F5} Tu repartidor est\xE1 cerca",
            body: `Llega en aproximadamente ${Math.max(1, Math.round(distance / 200))} minuto(s)`,
            data: { orderId, screen: "OrderTracking" }
          });
        }
      } catch (err) {
        console.error("Error sending proximity push:", err);
      }
      res.json({ success: true });
    } catch (error) {
      console.error("Error handling proximity alert:", error);
      res.status(500).json({ error: "Failed to process proximity alert" });
    }
  }
);
router25.post(
  "/proof/:orderId",
  authenticateToken,
  async (req, res) => {
    try {
      const { orderId } = req.params;
      const { photoBase64, latitude, longitude, timestamp: timestamp2, accuracy, route } = req.body;
      const userId = req.user?.id;
      if (!await requireAssignedDriver(req, orderId)) {
        return res.status(403).json({ error: "No autorizado para este pedido" });
      }
      console.log(`\u{1F4F8} Delivery proof submitted for order ${orderId}`);
      let photoUrl = "";
      try {
        const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
        photoUrl = await CloudinaryService2.uploadImage(
          photoBase64,
          "delivery-proofs",
          `proof-${orderId}-${Date.now()}`
        );
      } catch (err) {
        console.error("Cloudinary upload failed for delivery proof:", err);
      }
      let routeDistance = 0;
      if (route && route.length > 1) {
        for (let i = 1; i < route.length; i++) {
          const prev = route[i - 1];
          const curr = route[i];
          routeDistance += calculateDistance3(
            prev.latitude,
            prev.longitude,
            curr.latitude,
            curr.longitude
          );
        }
      }
      await db.insert(deliveryProofs).values({
        orderId,
        driverId: userId,
        photoUrl,
        latitude: latitude.toString(),
        longitude: longitude.toString(),
        accuracy,
        route: JSON.stringify(route),
        routeDistance: Math.round(routeDistance),
        timestamp: new Date(timestamp2)
      });
      await db.update(orders).set({
        deliveryProofPhoto: photoUrl,
        deliveryProofPhotoTimestamp: new Date(timestamp2),
        deliveryRoute: JSON.stringify(route),
        deliveryDistance: Math.round(routeDistance),
        deliveryGpsAccuracy: accuracy,
        deliveryGpsValidated: accuracy ? accuracy < 50 : false
      }).where((0, import_drizzle_orm53.eq)(orders.id, orderId));
      await db.update(deliveryDrivers).set({
        totalDistanceTraveled: import_drizzle_orm53.sql`${deliveryDrivers.totalDistanceTraveled} + ${Math.round(routeDistance)}`
      }).where((0, import_drizzle_orm53.eq)(deliveryDrivers.userId, userId));
      res.json({ success: true, routeDistance: Math.round(routeDistance) });
    } catch (error) {
      console.error("Error submitting delivery proof:", error);
      res.status(500).json({ error: "Failed to submit delivery proof" });
    }
  }
);
router25.get(
  "/proof/:orderId",
  authenticateToken,
  async (req, res) => {
    try {
      const { orderId } = req.params;
      const proof = await db.select().from(deliveryProofs).where((0, import_drizzle_orm53.eq)(deliveryProofs.orderId, orderId)).limit(1);
      if (proof.length === 0) {
        return res.status(404).json({ error: "Delivery proof not found" });
      }
      res.json({ success: true, proof: proof[0] });
    } catch (error) {
      console.error("Error getting delivery proof:", error);
      res.status(500).json({ error: "Failed to get delivery proof" });
    }
  }
);
router25.get(
  "/heatmap",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const days = Math.min(
        365,
        Math.max(1, parseInt(req.query.days) || 30)
      );
      const [rows] = await db.execute(import_drizzle_orm53.sql`
        SELECT
          ROUND(delivery_latitude, 3) AS grid_lat,
          ROUND(delivery_longitude, 3) AS grid_lng,
          COUNT(*) AS order_count,
          COALESCE(SUM(total), 0) AS total_revenue,
          AVG(TIMESTAMPDIFF(MINUTE, created_at, delivered_at)) AS avg_minutes
        FROM orders
        WHERE status = 'delivered'
          AND delivery_latitude IS NOT NULL
          AND delivery_longitude IS NOT NULL
          AND delivered_at >= DATE_SUB(NOW(), INTERVAL ${days} DAY)
        GROUP BY grid_lat, grid_lng
        ORDER BY order_count DESC
        LIMIT 500
      `);
      const heatmap = rows.map((r) => ({
        latitude: parseFloat(r.grid_lat),
        longitude: parseFloat(r.grid_lng),
        orderCount: Number(r.order_count) || 0,
        totalRevenue: Number(r.total_revenue) || 0,
        avgMinutes: r.avg_minutes != null ? Math.round(Number(r.avg_minutes)) : null
      }));
      res.json({ success: true, days, heatmap });
    } catch (error) {
      console.error("Error getting heatmap:", error);
      res.status(500).json({ error: "Failed to get heatmap data" });
    }
  }
);
router25.post(
  "/tracking-token/:orderId",
  authenticateToken,
  async (req, res) => {
    try {
      const { orderId } = req.params;
      const userId = req.user?.id ?? req.user?.userId;
      const order = await db.select().from(orders).where((0, import_drizzle_orm53.eq)(orders.id, orderId)).limit(1);
      if (order.length === 0 || order[0].userId !== userId) {
        return res.status(403).json({ error: "Unauthorized" });
      }
      const token = generateTrackingToken();
      const expiresAt = new Date(Date.now() + 24 * 60 * 60 * 1e3);
      await db.update(orders).set({
        trackingToken: token,
        trackingTokenExpires: expiresAt
      }).where((0, import_drizzle_orm53.eq)(orders.id, orderId));
      const trackingUrl = `${process.env.FRONTEND_URL}/track/${token}`;
      res.json({ success: true, token, trackingUrl, expiresAt });
    } catch (error) {
      console.error("Error generating tracking token:", error);
      res.status(500).json({ error: "Failed to generate tracking token" });
    }
  }
);
router25.get("/track/:token", async (req, res) => {
  try {
    const { token } = req.params;
    const order = await db.select().from(orders).where(
      (0, import_drizzle_orm53.and)(
        (0, import_drizzle_orm53.eq)(orders.trackingToken, token),
        import_drizzle_orm53.sql`${orders.trackingTokenExpires} > NOW()`
      )
    ).limit(1);
    if (order.length === 0) {
      return res.status(404).json({ error: "Invalid or expired tracking link" });
    }
    let driverLocation = null;
    if (order[0].deliveryPersonId) {
      const driver = await db.select({
        latitude: deliveryDrivers.currentLatitude,
        longitude: deliveryDrivers.currentLongitude,
        lastUpdate: deliveryDrivers.lastLocationUpdate
      }).from(deliveryDrivers).where((0, import_drizzle_orm53.eq)(deliveryDrivers.userId, order[0].deliveryPersonId)).limit(1);
      if (driver.length > 0 && driver[0].latitude && driver[0].longitude) {
        driverLocation = {
          latitude: parseFloat(driver[0].latitude),
          longitude: parseFloat(driver[0].longitude),
          lastUpdate: driver[0].lastUpdate
        };
      }
    }
    res.json({
      success: true,
      order: {
        id: order[0].id,
        status: order[0].status,
        businessName: order[0].businessName,
        estimatedDelivery: order[0].estimatedDelivery,
        deliveryAddress: order[0].deliveryAddress,
        deliveryLatitude: order[0].deliveryLatitude,
        deliveryLongitude: order[0].deliveryLongitude
      },
      driverLocation
    });
  } catch (error) {
    console.error("Error tracking order:", error);
    res.status(500).json({ error: "Failed to track order" });
  }
});
function calculateDistance3(lat1, lon1, lat2, lon2) {
  const R = 6371e3;
  const \u03C61 = lat1 * Math.PI / 180;
  const \u03C62 = lat2 * Math.PI / 180;
  const \u0394\u03C6 = (lat2 - lat1) * Math.PI / 180;
  const \u0394\u03BB = (lon2 - lon1) * Math.PI / 180;
  const a = Math.sin(\u0394\u03C6 / 2) * Math.sin(\u0394\u03C6 / 2) + Math.cos(\u03C61) * Math.cos(\u03C62) * Math.sin(\u0394\u03BB / 2) * Math.sin(\u0394\u03BB / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
function generateTrackingToken() {
  return Math.random().toString(36).substring(2, 15) + Math.random().toString(36).substring(2, 15);
}
var gpsRoutes_default = router25;

// server/routes/digitalPayments.ts
var import_express26 = require("express");
init_digitalPaymentService();
var router26 = (0, import_express26.Router)();
router26.get("/methods", authenticateToken, async (req, res) => {
  try {
    const methods = await digitalPaymentService.getActivePaymentMethods();
    res.json({ success: true, methods });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router26.get(
  "/metrics",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const metrics = await digitalPaymentService.getPaymentMetrics();
      res.json({ success: true, ...metrics });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var digitalPayments_default = router26;

// server/routes/paymentAccounts.ts
var import_express27 = require("express");
var import_drizzle_orm54 = require("drizzle-orm");
init_db();
var router27 = (0, import_express27.Router)();
router27.get("/receiving-accounts", authenticateToken, async (req, res) => {
  try {
    const [rows] = await db.execute(
      "SELECT provider, account_data FROM payment_receiving_accounts WHERE is_active = TRUE"
    );
    const accounts = {};
    rows.forEach((row) => {
      accounts[row.provider] = row.account_data;
    });
    res.json({ success: true, accounts });
  } catch (error) {
    console.error("Error getting payment accounts:", error);
    res.status(500).json({ success: false, error: "Error al obtener cuentas de pago" });
  }
});
router27.get(
  "/admin/receiving-accounts",
  authenticateToken,
  requireAdmin,
  async (req, res) => {
    try {
      const [rows] = await db.execute(
        "SELECT id, provider, account_data, is_active, created_at, updated_at FROM payment_receiving_accounts ORDER BY provider"
      );
      const accounts = rows.map((row) => ({
        id: row.id,
        provider: row.provider,
        accountData: row.account_data,
        // Ya viene como objeto
        isActive: row.is_active,
        createdAt: row.created_at,
        updatedAt: row.updated_at
      }));
      res.json({ success: true, accounts });
    } catch (error) {
      console.error("Error getting admin payment accounts:", error);
      res.status(500).json({ success: false, error: "Error al obtener cuentas" });
    }
  }
);
router27.put(
  "/admin/receiving-accounts/:provider",
  authenticateToken,
  requireAdmin,
  async (req, res) => {
    try {
      const { provider } = req.params;
      const { accountData, isActive } = req.body;
      if (!provider || !accountData || typeof accountData !== "object") {
        return res.status(400).json({
          success: false,
          error: "provider y accountData son requeridos"
        });
      }
      await db.execute(import_drizzle_orm54.sql`
        INSERT INTO payment_receiving_accounts (provider, account_data, is_active)
        VALUES (${provider}, ${JSON.stringify(accountData)}, ${isActive ?? true})
        ON DUPLICATE KEY UPDATE
          account_data = VALUES(account_data),
          is_active = VALUES(is_active),
          updated_at = NOW()
      `);
      res.json({ success: true, message: "Cuenta actualizada correctamente" });
    } catch (error) {
      console.error("Error updating payment account:", error);
      res.status(500).json({ success: false, error: "Error al actualizar cuenta" });
    }
  }
);
var paymentAccounts_default = router27;

// server/routes/fundRelease.ts
var import_express28 = require("express");
init_fundReleaseService();
init_orderNumberService();
init_payoutService();
init_loyaltyService();
var router28 = (0, import_express28.Router)();
router28.post("/confirm-delivery", authenticateToken, async (req, res) => {
  try {
    if (req.user.role !== "customer" && req.user.role !== "admin" && req.user.role !== "super_admin") {
      return res.status(403).json({
        success: false,
        error: "Solo el cliente puede confirmar la entrega"
      });
    }
    const { orderId } = req.body;
    if (!orderId) {
      return res.status(400).json({
        success: false,
        error: "orderId es requerido"
      });
    }
    const result = await fundReleaseService.releaseOnCustomerConfirmation(
      orderId,
      req.user.id
    );
    if (!result.success) {
      return res.status(400).json(result);
    }
    await createPayoutsForOrder(orderId);
    try {
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (order?.userId) {
        await LoyaltyService.awardPointsForOrder(
          order.userId,
          orderId,
          order.total
        );
        console.log(
          `\u2705 Puntos de lealtad awarded para orden ${orderId.slice(-6)}`
        );
      }
    } catch (loyaltyError) {
      console.error("\u26A0\uFE0F Error agregando puntos de lealtad:", loyaltyError);
    }
    res.json(result);
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router28.post("/dispute", authenticateToken, async (req, res) => {
  try {
    const { orderId, reason } = req.body;
    if (!orderId || !reason) {
      return res.status(400).json({
        success: false,
        error: "orderId y reason son requeridos"
      });
    }
    const result = await fundReleaseService.disputeOrder(
      orderId,
      req.user.id,
      reason
    );
    if (!result.success) {
      return res.status(400).json(result);
    }
    try {
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (order?.deliveryPersonId) {
        const { addStrike: addStrike2 } = await Promise.resolve().then(() => (init_strikeService(), strikeService_exports));
        await addStrike2(
          order.deliveryPersonId,
          `Disputa del cliente: ${reason}`,
          orderId
        );
      }
    } catch (strikeErr) {
      console.error("Error adding strike on dispute:", strikeErr);
    }
    try {
      const { randomUUID: randomUUID5 } = await import("crypto");
      const schema = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { orderIssues: orderIssues2, supportTickets: supportTickets2, ticketMessages: ticketMessages2, users: users5, orders: orders2 } = schema;
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79, inArray: inArray12 } = await import("drizzle-orm");
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      const [order] = await db2.select().from(orders2).where(eq79(orders2.id, orderId)).limit(1);
      if (order) {
        const existing = await db2.select({ id: orderIssues2.id }).from(orderIssues2).where(eq79(orderIssues2.orderId, orderId));
        if (existing.length === 0) {
          const ticketId = randomUUID5();
          await db2.insert(supportTickets2).values({
            id: ticketId,
            userId: req.user.id,
            orderId,
            subject: `[Disputa ${await orderRefFromId(orderId)}] ${reason}`.slice(0, 255),
            category: "order_issue",
            priority: "high",
            status: "open"
          });
          await db2.insert(ticketMessages2).values({
            ticketId,
            senderId: req.user.id,
            senderType: "user",
            message: reason
          });
          const issueId = randomUUID5();
          await db2.insert(orderIssues2).values({
            id: issueId,
            orderId,
            ticketId,
            reportedBy: req.user.id,
            reporterRole: "customer",
            issueType: "never_arrived",
            description: `[Disputa de entrega] ${reason}`,
            status: "open",
            priority: "high"
          });
          const admins = await db2.select({ id: users5.id }).from(users5).where(inArray12(users5.role, ["admin", "super_admin"]));
          for (const admin of admins) {
            await sendPushToUser2(admin.id, {
              title: "\u26A0\uFE0F Disputa de entrega",
              body: `Pedido ${await orderRefFromId(orderId)}: ${reason.slice(0, 80)}`,
              data: {
                type: "order_issue",
                issueId,
                orderId,
                screen: "AdminDashboard",
                section: "support_issues"
              }
            }).catch(() => {
            });
          }
        }
      }
    } catch (issueErr) {
      console.error("Error creating issue from dispute:", issueErr);
    }
    res.json(result);
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router28.get(
  "/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const orders2 = await fundReleaseService.getPendingReleaseOrders();
      res.json({ success: true, orders: orders2 });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router28.post(
  "/auto-release",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await fundReleaseService.autoReleaseFunds();
      res.json({
        success: true,
        message: `Auto-release completado: ${result.released} liberados, ${result.failed} fallidos`,
        ...result
      });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var fundRelease_default = router28;

// server/payoutRoutes.ts
var import_express29 = require("express");
init_payoutService();
var router29 = (0, import_express29.Router)();
router29.get("/accounts", authenticateToken, async (req, res) => {
  try {
    const accounts = await getUserPaymentAccounts(req.user.id);
    res.json({ success: true, accounts });
  } catch (err) {
    res.status(500).json({ error: err.message });
  }
});
router29.post("/accounts", authenticateToken, async (req, res) => {
  try {
    await savePaymentAccount(req.user.id, req.body);
    res.json({ success: true });
  } catch (err) {
    res.status(400).json({ error: err.message });
  }
});
router29.delete("/accounts/:id", authenticateToken, async (req, res) => {
  try {
    await deletePaymentAccount(req.params.id, req.user.id);
    res.json({ success: true });
  } catch (err) {
    res.status(400).json({ error: err.message });
  }
});
router29.get("/history", authenticateToken, async (req, res) => {
  try {
    const history = await getPayoutHistory(req.user.id);
    res.json({ success: true, payouts: history });
  } catch (err) {
    res.status(500).json({ error: err.message });
  }
});
router29.get(
  "/pending",
  authenticateToken,
  requireRole("admin"),
  async (req, res) => {
    try {
      const pending = await getPendingPayouts();
      res.json({ success: true, payouts: pending });
    } catch (err) {
      res.status(500).json({ error: err.message });
    }
  }
);
router29.post(
  "/:id/paid",
  authenticateToken,
  requireRole("admin"),
  async (req, res) => {
    try {
      await markPayoutPaid(req.params.id, req.user.id, req.body.notes);
      res.json({ success: true });
    } catch (err) {
      res.status(400).json({ error: err.message });
    }
  }
);
var payoutRoutes_default = router29;

// server/routes/search.ts
var import_express30 = __toESM(require("express"));
init_db();
init_schema_mysql();
var import_drizzle_orm56 = require("drizzle-orm");
var router30 = import_express30.default.Router();
router30.get("/products", async (req, res) => {
  try {
    const query = (req.query.q || "").trim();
    if (!query || query.length < 2) {
      return res.json({ success: true, results: [] });
    }
    const searchTerms = query.toLowerCase().split(" ").filter((t) => t.length > 1);
    const results = await db.select({
      product: products,
      business: {
        id: businesses.id,
        name: businesses.name,
        image: businesses.image,
        isOpen: businesses.isOpen,
        deliveryFee: businesses.deliveryFee
      }
    }).from(products).leftJoin(businesses, (0, import_drizzle_orm56.eq)(products.businessId, businesses.id)).where(
      (0, import_drizzle_orm56.and)(
        (0, import_drizzle_orm56.eq)(products.isAvailable, true),
        (0, import_drizzle_orm56.eq)(businesses.isActive, true),
        (0, import_drizzle_orm56.or)(
          ...searchTerms.map((term) => (0, import_drizzle_orm56.like)(products.name, `%${term}%`)),
          ...searchTerms.map(
            (term) => (0, import_drizzle_orm56.like)(products.description, `%${term}%`)
          )
        )
      )
    ).limit(50);
    res.json({
      success: true,
      results: results.map((r) => ({
        ...r.product,
        business: r.business
      }))
    });
  } catch (error) {
    console.error("Product search error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
var search_default = router30;

// server/routes/coupons.ts
var import_express31 = __toESM(require("express"));
init_advancedCouponService();
init_db();
init_schema_mysql();
var import_drizzle_orm58 = require("drizzle-orm");
var router31 = import_express31.default.Router();
router31.get("/", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const availableCoupons = await AdvancedCouponService.getAvailableCoupons(userId);
    res.json({
      success: true,
      coupons: availableCoupons
    });
  } catch (error) {
    console.error("Error getting coupons:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router31.post("/validate", authenticateToken, async (req, res) => {
  try {
    const { code, orderTotal, businessId, productIds, categories } = req.body;
    const userId = req.user.id;
    if (!code || !orderTotal) {
      return res.status(400).json({
        success: false,
        error: "C\xF3digo de cup\xF3n y total del pedido son requeridos"
      });
    }
    const ordersResult = await db.execute(
      `SELECT COUNT(*) as count FROM orders WHERE user_id = ? AND status = 'delivered'`,
      [userId]
    );
    const isFirstOrder = (ordersResult[0]?.count || 0) === 0;
    const result = await AdvancedCouponService.validateCoupon(code, {
      userId,
      orderTotal,
      businessId,
      productIds,
      categories,
      isFirstOrder
    });
    res.json({
      success: result.valid,
      ...result
    });
  } catch (error) {
    console.error("Error validating coupon:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router31.post("/apply", authenticateToken, async (req, res) => {
  try {
    const { couponId, orderId, discountApplied } = req.body;
    const userId = req.user.id;
    if (!couponId || !orderId || discountApplied === void 0) {
      return res.status(400).json({
        success: false,
        error: "Datos incompletos"
      });
    }
    await AdvancedCouponService.recordCouponUsage(
      couponId,
      userId,
      orderId,
      discountApplied
    );
    res.json({
      success: true,
      message: "Cup\xF3n aplicado exitosamente"
    });
  } catch (error) {
    console.error("Error applying coupon:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router31.get("/my-usage", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const usage = await db.execute(
      `SELECT cu.*, c.code, c.description, o.created_at as used_at
       FROM coupon_usage cu
       JOIN coupons c ON cu.coupon_id = c.id
       JOIN orders o ON cu.order_id = o.id
       WHERE cu.user_id = ?
       ORDER BY cu.created_at DESC
       LIMIT 50`,
      [userId]
    );
    res.json({
      success: true,
      usage
    });
  } catch (error) {
    console.error("Error getting coupon usage:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router31.get(
  "/admin/all",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const allCoupons = await db.select().from(coupons);
      res.json({
        success: true,
        coupons: allCoupons
      });
    } catch (error) {
      console.error("Error getting all coupons:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router31.post(
  "/admin/create",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const {
        code,
        type,
        discountType,
        discountValue,
        minOrderAmount,
        maxDiscount,
        maxUses,
        maxUsesPerUser,
        description,
        validForBusinesses,
        validForCategories,
        validForProducts,
        newUsersOnly,
        firstOrderOnly,
        dayOfWeek,
        timeRange,
        expiresAt
      } = req.body;
      if (!code || !discountValue) {
        return res.status(400).json({
          success: false,
          error: "C\xF3digo y valor de descuento son requeridos"
        });
      }
      const [existing] = await db.select().from(coupons).where((0, import_drizzle_orm58.eq)(coupons.code, code.toUpperCase())).limit(1);
      if (existing) {
        return res.status(400).json({
          success: false,
          error: "Ya existe un cup\xF3n con ese c\xF3digo"
        });
      }
      await db.insert(coupons).values({
        code: code.toUpperCase(),
        discountType: discountType || "percentage",
        discountValue,
        minOrderAmount: minOrderAmount || 0,
        maxDiscount: maxDiscount || null,
        maxUses: maxUses || null,
        maxUsesPerUser: maxUsesPerUser || 1,
        description: description || "",
        type: type || "percentage",
        validForBusinesses: validForBusinesses ? JSON.stringify(validForBusinesses) : null,
        validForCategories: validForCategories ? JSON.stringify(validForCategories) : null,
        validForProducts: validForProducts ? JSON.stringify(validForProducts) : null,
        newUsersOnly: newUsersOnly || false,
        firstOrderOnly: firstOrderOnly || false,
        dayOfWeek: dayOfWeek ? JSON.stringify(dayOfWeek) : null,
        timeRange: timeRange ? JSON.stringify(timeRange) : null,
        expiresAt: expiresAt || null,
        isActive: true
      });
      res.json({
        success: true,
        message: "Cup\xF3n creado exitosamente"
      });
    } catch (error) {
      console.error("Error creating coupon:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router31.put(
  "/admin/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { id } = req.params;
      const {
        code,
        discountType,
        discountValue,
        minOrderAmount,
        maxUses,
        maxUsesPerUser,
        expiresAt,
        isActive
      } = req.body;
      const updates = {};
      if (code !== void 0) updates.code = String(code).toUpperCase();
      if (discountType !== void 0) updates.discountType = discountType;
      if (discountValue !== void 0)
        updates.discountValue = Number(discountValue);
      if (minOrderAmount !== void 0)
        updates.minOrderAmount = minOrderAmount === null ? null : Number(minOrderAmount);
      if (maxUses !== void 0)
        updates.maxUses = maxUses === null ? null : Number(maxUses);
      if (maxUsesPerUser !== void 0)
        updates.maxUsesPerUser = Number(maxUsesPerUser);
      if (expiresAt !== void 0)
        updates.expiresAt = expiresAt ? new Date(expiresAt) : null;
      if (isActive !== void 0) updates.isActive = Boolean(isActive);
      if (Object.keys(updates).length === 0) {
        return res.status(400).json({
          success: false,
          error: "No hay campos v\xE1lidos para actualizar"
        });
      }
      await db.update(coupons).set(updates).where((0, import_drizzle_orm58.eq)(coupons.id, id));
      const [updated] = await db.select().from(coupons).where((0, import_drizzle_orm58.eq)(coupons.id, id)).limit(1);
      res.json({ success: true, coupon: updated });
    } catch (error) {
      console.error("Error updating coupon:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router31.delete(
  "/admin/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { id } = req.params;
      await db.execute(`DELETE FROM coupons WHERE id = ?`, [id]);
      res.json({ success: true, message: "Cup\xF3n eliminado exitosamente" });
    } catch (error) {
      console.error("Error deleting coupon:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router31.get(
  "/admin/stats",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const stats = await db.execute(`
      SELECT 
        COUNT(*) as total_coupons,
        SUM(CASE WHEN is_active = 1 THEN 1 ELSE 0 END) as active_coupons,
        SUM(used_count) as total_uses,
        SUM(CASE WHEN expires_at < NOW() THEN 1 ELSE 0 END) as expired_coupons
      FROM coupons
    `);
      const topCoupons = await db.execute(`
      SELECT c.code, c.description, c.used_count, 
             COUNT(cu.id) as actual_uses,
             SUM(cu.discount_applied) as total_discount_given
      FROM coupons c
      LEFT JOIN coupon_usage cu ON c.id = cu.coupon_id
      GROUP BY c.id
      ORDER BY actual_uses DESC
      LIMIT 10
    `);
      res.json({
        success: true,
        stats: stats[0],
        topCoupons
      });
    } catch (error) {
      console.error("Error getting coupon stats:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var coupons_default = router31;

// server/routes/loyalty.ts
var import_express32 = __toESM(require("express"));
init_loyaltyService();
init_db();
init_schema_mysql();
var import_drizzle_orm59 = require("drizzle-orm");
var router32 = import_express32.default.Router();
router32.get("/points", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const points = await LoyaltyService.getOrCreateLoyaltyPoints(userId);
    res.json({
      success: true,
      points
    });
  } catch (error) {
    console.error("Error getting loyalty points:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router32.get("/history", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const limit = parseInt(req.query.limit) || 50;
    const history = await LoyaltyService.getTransactionHistory(userId, limit);
    res.json({
      success: true,
      history
    });
  } catch (error) {
    console.error("Error getting transaction history:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router32.get("/rewards", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const rewards = await LoyaltyService.getAvailableRewards(userId);
    res.json({
      success: true,
      rewards
    });
  } catch (error) {
    console.error("Error getting available rewards:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router32.post("/redeem", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const { rewardId } = req.body;
    if (!rewardId) {
      return res.status(400).json({
        success: false,
        error: "rewardId requerido"
      });
    }
    const result = await LoyaltyService.redeemReward(userId, rewardId);
    res.json({
      success: true,
      redemptionId: result.redemptionId,
      reward: result.reward,
      message: "Recompensa canjeada exitosamente"
    });
  } catch (error) {
    console.error("Error redeeming reward:", error);
    res.status(400).json({ success: false, error: error.message });
  }
});
router32.get("/challenges", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const challenges = await LoyaltyService.getUserChallenges(userId);
    res.json({
      success: true,
      challenges
    });
  } catch (error) {
    console.error("Error getting challenges:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router32.post("/challenges/:id/claim", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const challengeId = req.params.id;
    const pointsAwarded = await LoyaltyService.claimChallengeReward(
      userId,
      challengeId
    );
    res.json({
      success: true,
      pointsAwarded,
      message: `Has ganado ${pointsAwarded} puntos`
    });
  } catch (error) {
    console.error("Error claiming challenge reward:", error);
    res.status(400).json({ success: false, error: error.message });
  }
});
router32.get("/dashboard", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const [points, rewards, challenges, history] = await Promise.all([
      LoyaltyService.getOrCreateLoyaltyPoints(userId),
      LoyaltyService.getAvailableRewards(userId),
      LoyaltyService.getUserChallenges(userId),
      LoyaltyService.getTransactionHistory(userId, 10)
    ]);
    res.json({
      success: true,
      dashboard: {
        points,
        rewards,
        challenges,
        recentActivity: history
      }
    });
  } catch (error) {
    console.error("Error getting loyalty dashboard:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router32.get(
  "/admin/rewards",
  authenticateToken,
  requireRole(["admin", "super_admin"]),
  async (req, res) => {
    try {
      const allRewards = await db.select().from(loyaltyRewards);
      res.json({
        success: true,
        rewards: allRewards
      });
    } catch (error) {
      console.error("Error getting all rewards:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router32.post(
  "/admin/rewards",
  authenticateToken,
  requireRole(["admin", "super_admin"]),
  async (req, res) => {
    try {
      const {
        title,
        description,
        pointsCost,
        type,
        value,
        minTier,
        maxRedemptions,
        expiresAt,
        imageUrl,
        terms
      } = req.body;
      if (!title || !pointsCost || !type || !value) {
        return res.status(400).json({
          success: false,
          error: "Campos requeridos: title, pointsCost, type, value"
        });
      }
      const rewardId = crypto.randomUUID();
      await db.insert(loyaltyRewards).values({
        id: rewardId,
        title,
        description,
        pointsCost,
        type,
        value,
        minTier,
        maxRedemptions,
        expiresAt,
        imageUrl,
        terms,
        isAvailable: true,
        currentRedemptions: 0
      });
      res.json({
        success: true,
        rewardId,
        message: "Recompensa creada exitosamente"
      });
    } catch (error) {
      console.error("Error creating reward:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router32.put(
  "/admin/rewards/:id",
  authenticateToken,
  requireRole(["admin", "super_admin"]),
  async (req, res) => {
    try {
      const { id } = req.params;
      const updates = req.body;
      await db.update(loyaltyRewards).set(updates).where((0, import_drizzle_orm59.eq)(loyaltyRewards.id, id));
      res.json({
        success: true,
        message: "Recompensa actualizada exitosamente"
      });
    } catch (error) {
      console.error("Error updating reward:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router32.delete(
  "/admin/rewards/:id",
  authenticateToken,
  requireRole(["admin", "super_admin"]),
  async (req, res) => {
    try {
      const { id } = req.params;
      await db.update(loyaltyRewards).set({ isAvailable: false }).where((0, import_drizzle_orm59.eq)(loyaltyRewards.id, id));
      res.json({
        success: true,
        message: "Recompensa desactivada exitosamente"
      });
    } catch (error) {
      console.error("Error deleting reward:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router32.get(
  "/admin/challenges",
  authenticateToken,
  requireRole(["admin", "super_admin"]),
  async (req, res) => {
    try {
      const allChallenges = await db.select().from(loyaltyChallenges);
      res.json({
        success: true,
        challenges: allChallenges
      });
    } catch (error) {
      console.error("Error getting all challenges:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router32.post(
  "/admin/challenges",
  authenticateToken,
  requireRole(["admin", "super_admin"]),
  async (req, res) => {
    try {
      const {
        title,
        description,
        type,
        target,
        rewardPoints,
        startsAt,
        expiresAt
      } = req.body;
      if (!title || !type || !target || !rewardPoints) {
        return res.status(400).json({
          success: false,
          error: "Campos requeridos: title, type, target, rewardPoints"
        });
      }
      const challengeId = crypto.randomUUID();
      await db.insert(loyaltyChallenges).values({
        id: challengeId,
        title,
        description,
        type,
        target,
        rewardPoints,
        startsAt,
        expiresAt,
        isActive: true
      });
      res.json({
        success: true,
        challengeId,
        message: "Challenge creado exitosamente"
      });
    } catch (error) {
      console.error("Error creating challenge:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router32.get(
  "/admin/stats",
  authenticateToken,
  requireRole(["admin", "super_admin"]),
  async (req, res) => {
    try {
      const stats = await db.execute(`
      SELECT 
        COUNT(DISTINCT user_id) as total_users,
        SUM(current_points) as total_points_in_circulation,
        SUM(total_earned) as total_points_earned,
        SUM(total_redeemed) as total_points_redeemed,
        SUM(CASE WHEN tier = 'bronze' THEN 1 ELSE 0 END) as bronze_users,
        SUM(CASE WHEN tier = 'silver' THEN 1 ELSE 0 END) as silver_users,
        SUM(CASE WHEN tier = 'gold' THEN 1 ELSE 0 END) as gold_users,
        SUM(CASE WHEN tier = 'platinum' THEN 1 ELSE 0 END) as platinum_users,
        SUM(CASE WHEN tier = 'diamond' THEN 1 ELSE 0 END) as diamond_users
      FROM loyalty_points
    `);
      const rewardStats = await db.execute(`
      SELECT 
        COUNT(*) as total_rewards,
        SUM(CASE WHEN is_available = 1 THEN 1 ELSE 0 END) as active_rewards,
        SUM(current_redemptions) as total_redemptions
      FROM loyalty_rewards
    `);
      res.json({
        success: true,
        stats: {
          users: stats[0],
          rewards: rewardStats[0]
        }
      });
    } catch (error) {
      console.error("Error getting loyalty stats:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var loyalty_default = router32;

// server/routes/favorites.ts
var import_express33 = __toESM(require("express"));

// server/favoritesService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm60 = require("drizzle-orm");
var FavoritesService = class {
  // Agregar favorito
  static async addFavorite(userId, itemType, itemId) {
    try {
      await db.insert(userFavorites).values({
        userId,
        itemType,
        itemId
      });
      return { success: true };
    } catch (error) {
      if (error.code === "ER_DUP_ENTRY") {
        return { success: false, error: "Ya est\xE1 en favoritos" };
      }
      throw error;
    }
  }
  // Eliminar favorito
  static async removeFavorite(userId, itemType, itemId) {
    await db.delete(userFavorites).where(
      (0, import_drizzle_orm60.and)(
        (0, import_drizzle_orm60.eq)(userFavorites.userId, userId),
        (0, import_drizzle_orm60.eq)(userFavorites.itemType, itemType),
        (0, import_drizzle_orm60.eq)(userFavorites.itemId, itemId)
      )
    );
    return { success: true };
  }
  // Obtener favoritos del usuario
  static async getUserFavorites(userId) {
    const favorites2 = await db.select().from(userFavorites).where((0, import_drizzle_orm60.eq)(userFavorites.userId, userId));
    const businessIds = favorites2.filter((f) => f.itemType === "business").map((f) => f.itemId);
    const productIds = favorites2.filter((f) => f.itemType === "product").map((f) => f.itemId);
    const favoriteBusinesses = businessIds.length > 0 ? await db.select().from(businesses).where((0, import_drizzle_orm60.eq)(businesses.id, businessIds[0])) : [];
    const favoriteProducts = productIds.length > 0 ? await db.select().from(products).where((0, import_drizzle_orm60.eq)(products.id, productIds[0])) : [];
    return {
      businesses: favoriteBusinesses,
      products: favoriteProducts,
      total: favorites2.length
    };
  }
  // Verificar si es favorito
  static async isFavorite(userId, itemType, itemId) {
    const [favorite] = await db.select().from(userFavorites).where(
      (0, import_drizzle_orm60.and)(
        (0, import_drizzle_orm60.eq)(userFavorites.userId, userId),
        (0, import_drizzle_orm60.eq)(userFavorites.itemType, itemType),
        (0, import_drizzle_orm60.eq)(userFavorites.itemId, itemId)
      )
    ).limit(1);
    return !!favorite;
  }
};

// server/routes/favorites.ts
var router33 = import_express33.default.Router();
router33.post("/", authenticateToken, async (req, res) => {
  try {
    const { itemType, itemId } = req.body;
    const result = await FavoritesService.addFavorite(
      req.user.id,
      itemType,
      itemId
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router33.delete("/:itemType/:itemId", authenticateToken, async (req, res) => {
  try {
    const { itemType, itemId } = req.params;
    const result = await FavoritesService.removeFavorite(
      req.user.id,
      itemType,
      itemId
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router33.get("/", authenticateToken, async (req, res) => {
  try {
    const favorites2 = await FavoritesService.getUserFavorites(req.user.id);
    res.json({ success: true, favorites: favorites2 });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router33.get("/check/:itemType/:itemId", authenticateToken, async (req, res) => {
  try {
    const { itemType, itemId } = req.params;
    const isFavorite = await FavoritesService.isFavorite(
      req.user.id,
      itemType,
      itemId
    );
    res.json({ success: true, isFavorite });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var favorites_default = router33;

// server/routes/scheduledOrders.ts
var import_express34 = __toESM(require("express"));
init_scheduledOrdersService();
var router34 = import_express34.default.Router();
router34.post("/", authenticateToken, async (req, res) => {
  try {
    const result = await ScheduledOrdersService.createScheduledOrder({
      userId: req.user.id,
      ...req.body
    });
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router34.get("/", authenticateToken, async (req, res) => {
  try {
    const scheduled = await ScheduledOrdersService.getUserScheduledOrders(
      req.user.id
    );
    res.json({ success: true, scheduledOrders: scheduled });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router34.delete("/:id", authenticateToken, async (req, res) => {
  try {
    const result = await ScheduledOrdersService.cancelScheduledOrder(
      req.params.id,
      req.user.id
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router34.post("/execute", authenticateToken, async (req, res) => {
  try {
    if (req.user.role !== "admin" && req.user.role !== "super_admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    const results = await ScheduledOrdersService.executeScheduledOrders();
    res.json({ success: true, results });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var scheduledOrders_default = router34;

// server/routes/aiRecommendations.ts
var import_express35 = __toESM(require("express"));

// server/aiRecommendationsService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm62 = require("drizzle-orm");
var AIRecommendationsService = class {
  // Generar recomendaciones personalizadas
  static async generatePersonalizedRecommendations(userId) {
    const userOrders = await db.select().from(orders).where((0, import_drizzle_orm62.eq)(orders.userId, userId)).orderBy((0, import_drizzle_orm62.desc)(orders.createdAt)).limit(50);
    if (userOrders.length === 0) {
      return this.getDefaultRecommendations(userId);
    }
    const businessFrequency = this.analyzeBusinessFrequency(userOrders);
    const productFrequency = this.analyzeProductFrequency(userOrders);
    const timePatterns = this.analyzeTimePatterns(userOrders);
    const recommendations = [];
    for (const [businessId, count3] of Object.entries(businessFrequency).slice(
      0,
      3
    )) {
      const [business] = await db.select().from(businesses).where((0, import_drizzle_orm62.eq)(businesses.id, businessId)).limit(1);
      if (business) {
        recommendations.push({
          userId,
          recommendationType: "personalized",
          itemType: "business",
          itemId: businessId,
          confidenceScore: Math.min(95, 60 + count3 * 5),
          reason: `Has pedido aqu\xED ${count3} veces`,
          expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3)
        });
      }
    }
    for (const [productId, count3] of Object.entries(productFrequency).slice(
      0,
      3
    )) {
      recommendations.push({
        userId,
        recommendationType: "reorder",
        itemType: "product",
        itemId: productId,
        confidenceScore: Math.min(90, 50 + count3 * 10),
        reason: `Lo has pedido ${count3} veces`,
        expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3)
      });
    }
    await db.delete(aiRecommendations).where((0, import_drizzle_orm62.eq)(aiRecommendations.userId, userId));
    if (recommendations.length > 0) {
      await db.insert(aiRecommendations).values(recommendations);
    }
    return recommendations;
  }
  // Obtener recomendaciones trending
  static async getTrendingRecommendations() {
    const last24h = new Date(Date.now() - 24 * 60 * 60 * 1e3);
    const trending = await db.select({
      productId: import_drizzle_orm62.sql`JSON_EXTRACT(items, '$[*].product.id')`,
      count: import_drizzle_orm62.sql`COUNT(*)`
    }).from(orders).where((0, import_drizzle_orm62.gte)(orders.createdAt, last24h)).groupBy(import_drizzle_orm62.sql`JSON_EXTRACT(items, '$[*].product.id')`).orderBy((0, import_drizzle_orm62.desc)(import_drizzle_orm62.sql`COUNT(*)`)).limit(10);
    return trending;
  }
  // Obtener recomendaciones del usuario
  static async getUserRecommendations(userId) {
    const now = /* @__PURE__ */ new Date();
    const recommendations = await db.select().from(aiRecommendations).where(
      (0, import_drizzle_orm62.and)(
        (0, import_drizzle_orm62.eq)(aiRecommendations.userId, userId),
        (0, import_drizzle_orm62.gte)(aiRecommendations.expiresAt, now)
      )
    ).orderBy((0, import_drizzle_orm62.desc)(aiRecommendations.confidenceScore));
    const enriched = [];
    for (const rec of recommendations) {
      let itemData = null;
      if (rec.itemType === "business") {
        const [business] = await db.select().from(businesses).where((0, import_drizzle_orm62.eq)(businesses.id, rec.itemId)).limit(1);
        itemData = business;
      } else if (rec.itemType === "product") {
        const [product] = await db.select().from(products).where((0, import_drizzle_orm62.eq)(products.id, rec.itemId)).limit(1);
        itemData = product;
      }
      if (itemData) {
        enriched.push({
          ...rec,
          itemData
        });
      }
    }
    return enriched;
  }
  // Actualizar preferencias del usuario
  static async updateUserPreferences(userId, preferences) {
    const existing = await db.select().from(userPreferences).where((0, import_drizzle_orm62.eq)(userPreferences.userId, userId)).limit(1);
    if (existing.length > 0) {
      await db.update(userPreferences).set({
        cuisineTypes: preferences.cuisineTypes ? JSON.stringify(preferences.cuisineTypes) : null,
        priceRange: preferences.priceRange,
        dietaryRestrictions: preferences.dietaryRestrictions ? JSON.stringify(preferences.dietaryRestrictions) : null,
        preferredOrderTimes: preferences.preferredOrderTimes ? JSON.stringify(preferences.preferredOrderTimes) : null,
        favoriteCategories: preferences.favoriteCategories ? JSON.stringify(preferences.favoriteCategories) : null,
        spiceLevel: preferences.spiceLevel,
        healthScore: preferences.healthScore
      }).where((0, import_drizzle_orm62.eq)(userPreferences.userId, userId));
    } else {
      await db.insert(userPreferences).values({
        userId,
        cuisineTypes: preferences.cuisineTypes ? JSON.stringify(preferences.cuisineTypes) : null,
        priceRange: preferences.priceRange,
        dietaryRestrictions: preferences.dietaryRestrictions ? JSON.stringify(preferences.dietaryRestrictions) : null,
        preferredOrderTimes: preferences.preferredOrderTimes ? JSON.stringify(preferences.preferredOrderTimes) : null,
        favoriteCategories: preferences.favoriteCategories ? JSON.stringify(preferences.favoriteCategories) : null,
        spiceLevel: preferences.spiceLevel,
        healthScore: preferences.healthScore
      });
    }
    await this.generatePersonalizedRecommendations(userId);
    return { success: true };
  }
  // Analizar frecuencia de negocios
  static analyzeBusinessFrequency(orders2) {
    const frequency = {};
    for (const order of orders2) {
      frequency[order.businessId] = (frequency[order.businessId] || 0) + 1;
    }
    return Object.entries(frequency).sort(([, a], [, b]) => b - a).reduce((acc, [k, v]) => ({ ...acc, [k]: v }), {});
  }
  // Analizar frecuencia de productos
  static analyzeProductFrequency(orders2) {
    const frequency = {};
    for (const order of orders2) {
      try {
        const items = JSON.parse(order.items);
        for (const item of items) {
          const productId = item.product?.id || item.productId;
          if (productId) {
            frequency[productId] = (frequency[productId] || 0) + 1;
          }
        }
      } catch (e) {
      }
    }
    return Object.entries(frequency).sort(([, a], [, b]) => b - a).reduce((acc, [k, v]) => ({ ...acc, [k]: v }), {});
  }
  // Analizar patrones de tiempo
  static analyzeTimePatterns(orders2) {
    const hourFrequency = {};
    const dayFrequency = {};
    for (const order of orders2) {
      const date = new Date(order.createdAt);
      const hour = date.getHours();
      const day = date.getDay();
      hourFrequency[hour] = (hourFrequency[hour] || 0) + 1;
      dayFrequency[day] = (dayFrequency[day] || 0) + 1;
    }
    return { hourFrequency, dayFrequency };
  }
  // Recomendaciones por defecto para nuevos usuarios
  static async getDefaultRecommendations(userId) {
    const topBusinesses = await db.select().from(businesses).where((0, import_drizzle_orm62.eq)(businesses.isActive, true)).orderBy((0, import_drizzle_orm62.desc)(businesses.rating)).limit(5);
    const recommendations = topBusinesses.map((business, index) => ({
      userId,
      recommendationType: "trending",
      itemType: "business",
      itemId: business.id,
      confidenceScore: 80 - index * 5,
      reason: "Popular en tu \xE1rea",
      expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1e3)
    }));
    if (recommendations.length > 0) {
      await db.insert(aiRecommendations).values(recommendations);
    }
    return recommendations;
  }
};

// server/routes/aiRecommendations.ts
var router35 = import_express35.default.Router();
router35.get("/personalized", authenticateToken, async (req, res) => {
  try {
    const recommendations = await AIRecommendationsService.getUserRecommendations(req.user.id);
    res.json({ success: true, recommendations });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router35.get("/trending", authenticateToken, async (req, res) => {
  try {
    const trending = await AIRecommendationsService.getTrendingRecommendations();
    res.json({ success: true, trending });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router35.post("/generate", authenticateToken, async (req, res) => {
  try {
    const recommendations = await AIRecommendationsService.generatePersonalizedRecommendations(
      req.user.id
    );
    res.json({ success: true, recommendations });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router35.put("/preferences", authenticateToken, async (req, res) => {
  try {
    const result = await AIRecommendationsService.updateUserPreferences(
      req.user.id,
      req.body
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var aiRecommendations_default = router35;

// server/routes/support.ts
var import_express36 = __toESM(require("express"));

// server/supportService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm63 = require("drizzle-orm");
init_enhancedPushService();
var SupportService = class {
  static async createTicket(data) {
    const [result] = await db.insert(supportTickets).values({
      userId: data.userId,
      orderId: data.orderId,
      subject: data.subject,
      category: data.category,
      priority: data.priority || "medium",
      status: "open"
    });
    const ticketId = result.insertId;
    await db.insert(ticketMessages).values({
      ticketId,
      senderId: data.userId,
      senderType: "user",
      message: data.initialMessage || data.subject || "(sin mensaje)"
    });
    return { success: true, ticketId };
  }
  static async getUserTickets(userId) {
    return db.select().from(supportTickets).where((0, import_drizzle_orm63.eq)(supportTickets.userId, userId)).orderBy((0, import_drizzle_orm63.desc)(supportTickets.createdAt));
  }
  static async getTicket(ticketId, userId, isAdmin = false) {
    const [ticket] = await db.select().from(supportTickets).where(
      isAdmin ? (0, import_drizzle_orm63.eq)(supportTickets.id, ticketId) : (0, import_drizzle_orm63.and)((0, import_drizzle_orm63.eq)(supportTickets.id, ticketId), (0, import_drizzle_orm63.eq)(supportTickets.userId, userId))
    ).limit(1);
    if (!ticket) throw new Error("Ticket no encontrado");
    const messages = await db.select().from(ticketMessages).where((0, import_drizzle_orm63.eq)(ticketMessages.ticketId, ticketId)).orderBy(ticketMessages.createdAt);
    return { ticket, messages };
  }
  static async addMessage(data) {
    await db.insert(ticketMessages).values({
      ticketId: data.ticketId,
      senderId: data.senderId,
      senderType: data.senderType,
      message: data.message
    });
    await db.update(supportTickets).set({ updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm63.eq)(supportTickets.id, data.ticketId));
    if (data.senderType === "admin") {
      const [ticket] = await db.select().from(supportTickets).where((0, import_drizzle_orm63.eq)(supportTickets.id, data.ticketId)).limit(1);
      if (ticket) {
        await sendPushToUser(ticket.userId, {
          title: "Respuesta de Soporte",
          body: data.message.substring(0, 100),
          data: { ticketId: data.ticketId, screen: "TicketDetail" }
        });
      }
    }
    return { success: true };
  }
  static async updateTicketStatus(ticketId, status, adminId) {
    const updateData = { status, updatedAt: /* @__PURE__ */ new Date() };
    if (status === "resolved" || status === "closed")
      updateData.resolvedAt = /* @__PURE__ */ new Date();
    if (adminId) updateData.assignedTo = adminId;
    await db.update(supportTickets).set(updateData).where((0, import_drizzle_orm63.eq)(supportTickets.id, ticketId));
    return { success: true };
  }
  static async getPendingTickets() {
    return db.select().from(supportTickets).where((0, import_drizzle_orm63.eq)(supportTickets.status, "open")).orderBy((0, import_drizzle_orm63.desc)(supportTickets.createdAt));
  }
  /** Todos los tickets con nombre del usuario, para el panel admin. */
  static async getAllTickets(status) {
    const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const rows = await db.select({
      id: supportTickets.id,
      userId: supportTickets.userId,
      orderId: supportTickets.orderId,
      subject: supportTickets.subject,
      category: supportTickets.category,
      priority: supportTickets.priority,
      status: supportTickets.status,
      assignedTo: supportTickets.assignedTo,
      createdAt: supportTickets.createdAt,
      updatedAt: supportTickets.updatedAt,
      resolvedAt: supportTickets.resolvedAt,
      userName: users5.name,
      userEmail: users5.email
    }).from(supportTickets).leftJoin(users5, (0, import_drizzle_orm63.eq)(supportTickets.userId, users5.id)).orderBy((0, import_drizzle_orm63.desc)(supportTickets.createdAt));
    return status && status !== "all" ? rows.filter((t) => t.status === status) : rows;
  }
  static async assignTicket(ticketId, adminId) {
    await db.update(supportTickets).set({
      assignedTo: adminId,
      status: "in_progress",
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm63.eq)(supportTickets.id, ticketId));
    return { success: true };
  }
};

// server/routes/support.ts
var import_generative_ai = require("@google/generative-ai");
var router36 = import_express36.default.Router();
var SYSTEM_PROMPT = `Eres el asistente virtual de ComeYa, una plataforma de delivery en Soria, Espa\xF1a.
Respondes en espa\xF1ol, de forma amable, concisa y \xFAtil.
Solo respondes preguntas relacionadas con ComeYa: pedidos, pagos, entregas, negocios, repartidores, cuenta, etc.
Si la pregunta no tiene relaci\xF3n con ComeYa, redirige amablemente al tema.
M\xE9todos de pago aceptados: Bizum, transferencia IBAN, tarjeta, efectivo.
Pol\xEDtica de cancelaci\xF3n: gratis en los primeros 60 segundos, luego pueden aplicar cargos.
Tiempos de entrega estimados: 30-45 minutos seg\xFAn distancia y disponibilidad.
Si el usuario tiene un problema urgente con un pedido activo, sug\xEDere crear un ticket de soporte.`;
router36.post("/chat-public", async (req, res) => {
  try {
    const { message } = req.body;
    if (!message || typeof message !== "string") {
      return res.status(400).json({ error: "Mensaje requerido" });
    }
    const apiKey = process.env.GEMINI_API_KEY;
    if (!apiKey) {
      return res.status(500).json({ error: "IA no configurada" });
    }
    const genAI = new import_generative_ai.GoogleGenerativeAI(apiKey);
    const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash" });
    const chat = model.startChat({
      history: [
        { role: "user", parts: [{ text: SYSTEM_PROMPT }] },
        {
          role: "model",
          parts: [
            {
              text: "Entendido. Soy el asistente de ComeYa, listo para ayudar."
            }
          ]
        }
      ]
    });
    const result = await chat.sendMessage(message);
    const response = result.response.text();
    res.json({ success: true, response });
  } catch (error) {
    console.error("Support public chat error:", error);
    const errorMsg = error?.message || error?.toString() || "Error desconocido";
    res.json({
      success: true,
      response: "Lo siento, el asistente no est\xE1 disponible en este momento. Puedes contactarnos en support@comeya.es. (Error: " + errorMsg + ")"
    });
  }
});
router36.post("/chat", authenticateToken, async (req, res) => {
  try {
    const { message, history } = req.body;
    if (!message || typeof message !== "string") {
      return res.status(400).json({ error: "Mensaje requerido" });
    }
    const apiKey = process.env.GEMINI_API_KEY;
    if (!apiKey) {
      return res.status(500).json({ error: "IA no configurada" });
    }
    const genAI = new import_generative_ai.GoogleGenerativeAI(apiKey);
    const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash" });
    const chatHistory = (history || []).slice(-8).map((m) => ({
      role: m.role === "assistant" ? "model" : "user",
      parts: [{ text: m.content }]
    }));
    const chat = model.startChat({
      history: [
        { role: "user", parts: [{ text: SYSTEM_PROMPT }] },
        {
          role: "model",
          parts: [
            {
              text: "Entendido. Soy el asistente de ComeYa, listo para ayudar."
            }
          ]
        },
        ...chatHistory
      ]
    });
    const result = await chat.sendMessage(message);
    const response = result.response.text();
    res.json({ success: true, response });
  } catch (error) {
    console.error("Support chat error:", error);
    res.json({
      success: true,
      response: "Lo siento, el asistente no est\xE1 disponible en este momento. Por favor crea un ticket de soporte y te responderemos pronto."
    });
  }
});
router36.post("/tickets", authenticateToken, async (req, res) => {
  try {
    const result = await SupportService.createTicket({
      userId: req.user.id,
      ...req.body
    });
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router36.get("/tickets", authenticateToken, async (req, res) => {
  try {
    const tickets = await SupportService.getUserTickets(req.user.id);
    res.json({ success: true, tickets });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router36.get("/tickets/:id", authenticateToken, async (req, res) => {
  try {
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    const data = await SupportService.getTicket(
      String(req.params.id),
      String(req.user.id),
      isAdmin
    );
    res.json({ success: true, ...data });
  } catch (error) {
    res.status(404).json({ error: error.message });
  }
});
router36.get("/tickets/:id/messages", authenticateToken, async (req, res) => {
  try {
    const { ticketMessages: ticketMessages2, supportTickets: tickets } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [ticket] = await db2.select().from(tickets).where(eq79(tickets.id, String(req.params.id))).limit(1);
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    if (!ticket || !isAdmin && ticket.userId !== req.user.id) {
      return res.status(403).json({ error: "No tienes acceso a este ticket" });
    }
    const messages = await db2.select().from(ticketMessages2).where(eq79(ticketMessages2.ticketId, String(req.params.id))).orderBy(ticketMessages2.createdAt);
    res.json({
      success: true,
      messages: messages.map((m) => ({
        id: m.id,
        ticketId: m.ticketId,
        message: m.message,
        isBot: false,
        isAdmin: m.senderType === "admin",
        senderName: m.senderType === "admin" ? "Soporte" : "Cliente",
        createdAt: m.createdAt
      }))
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router36.post("/tickets/:id/messages", authenticateToken, async (req, res) => {
  try {
    const { supportTickets: tickets } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { eq: eq79 } = await import("drizzle-orm");
    const [ticket] = await db2.select({ userId: tickets.userId }).from(tickets).where(eq79(tickets.id, String(req.params.id))).limit(1);
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    if (!ticket || !isAdmin && ticket.userId !== req.user.id) {
      return res.status(403).json({ error: "No tienes acceso a este ticket" });
    }
    const result = await SupportService.addMessage({
      ticketId: String(req.params.id),
      senderId: req.user.id,
      senderType: isAdmin ? "admin" : "user",
      message: req.body.message
    });
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router36.patch(
  "/tickets/:id/status",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await SupportService.updateTicketStatus(
        String(req.params.id),
        req.body.status,
        req.user.id
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router36.get(
  "/admin/pending",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const tickets = await SupportService.getPendingTickets();
      res.json({ success: true, tickets });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router36.get(
  "/admin/tickets",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const tickets = await SupportService.getAllTickets(
        req.query.status
      );
      res.json({ success: true, tickets });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router36.post(
  "/tickets/:id/assign",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await SupportService.assignTicket(
        String(req.params.id),
        req.user.id
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var support_default = router36;

// server/routes/enhancedTracking.ts
var import_express37 = __toESM(require("express"));
init_enhancedTrackingService();
var router37 = import_express37.default.Router();
router37.post(
  "/location/update",
  authenticateToken,
  requireRole("delivery_driver"),
  async (req, res) => {
    try {
      const { latitude, longitude, heading, speed } = req.body;
      if (!latitude || !longitude) {
        return res.status(400).json({ error: "Latitud y longitud requeridas" });
      }
      const result = await EnhancedTrackingService.updateDriverLocation(
        req.user.id,
        latitude,
        longitude,
        heading,
        speed
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router37.get("/location/:orderId", authenticateToken, async (req, res) => {
  try {
    const result = await EnhancedTrackingService.getDriverLocation(
      req.params.orderId
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router37.get("/eta/:orderId", authenticateToken, async (req, res) => {
  try {
    const result = await EnhancedTrackingService.calculateDynamicETA(
      req.params.orderId
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router37.get("/milestones/:orderId", authenticateToken, async (req, res) => {
  try {
    const result = await EnhancedTrackingService.getOrderMilestones(
      req.params.orderId
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var enhancedTracking_default = router37;

// server/routes/subscriptions.ts
var import_express38 = __toESM(require("express"));
init_subscriptionService();
var import_drizzle_orm64 = require("drizzle-orm");
var router38 = import_express38.default.Router();
router38.get("/my-subscription", authenticateToken, async (req, res) => {
  try {
    const subscription = await SubscriptionService.getUserSubscription(req.user.id);
    res.json({ success: true, subscription });
  } catch (error) {
    if (error.message.includes("No se encontr\xF3")) {
      return res.status(404).json({ error: "No subscription found" });
    }
    res.status(500).json({ error: error.message });
  }
});
router38.get("/plans", async (req, res) => {
  try {
    const plans = await SubscriptionService.getPlansFromDB();
    res.json({ success: true, plans });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router38.get("/benefits-preview", authenticateToken, async (req, res) => {
  try {
    const { subtotal = 0, deliveryFee = 0 } = req.query;
    const benefits = await SubscriptionService.applySubscriptionBenefits(
      req.user.id,
      parseInt(subtotal),
      parseInt(deliveryFee)
    );
    const sub = await SubscriptionService.getUserSubscription(req.user.id);
    res.json({
      success: true,
      plan: sub.plan,
      isActive: sub.status === "active" && sub.plan !== "free",
      discount: benefits.discount,
      deliveryFee: benefits.deliveryFee,
      appliedBenefits: benefits.appliedBenefits
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router38.post("/subscribe", authenticateToken, async (req, res) => {
  try {
    const { plan, billingCycle } = req.body;
    const result = await SubscriptionService.initSubscription(
      req.user.id,
      plan,
      billingCycle || "monthly"
    );
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router38.post("/submit-proof", authenticateToken, async (req, res) => {
  try {
    const {
      subscriptionId,
      imageUrl,
      referenceNumber,
      senderName,
      amount,
      paymentMethod
    } = req.body;
    const userId = req.user.id;
    if (!subscriptionId || !imageUrl || !referenceNumber) {
      return res.status(400).json({
        error: "subscriptionId, imageUrl y referenceNumber son requeridos"
      });
    }
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { subscriptions: subscriptions2, paymentProofs: paymentProofs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { sql: drizzleSql } = await import("drizzle-orm");
    const { v4: uuidv4 } = await import("uuid");
    const [sub] = await db2.select().from(subscriptions2).where((0, import_drizzle_orm64.eq)(subscriptions2.id, subscriptionId)).limit(1);
    if (!sub || sub.userId !== userId) {
      return res.status(404).json({ error: "Suscripci\xF3n no encontrada" });
    }
    const [existing] = await db2.select().from(paymentProofs2).where(drizzleSql`reference_number = ${referenceNumber.trim()}`).limit(1);
    if (existing) {
      return res.status(409).json({ error: "Este comprobante ya fue enviado anteriormente" });
    }
    const proofId = uuidv4();
    await db2.insert(paymentProofs2).values({
      id: proofId,
      orderId: `sub_${subscriptionId}`,
      userId,
      paymentProvider: paymentMethod || "bizum",
      proofImageUrl: imageUrl,
      referenceNumber: referenceNumber.trim(),
      amount: amount || sub.price,
      status: "pending",
      submittedAt: /* @__PURE__ */ new Date()
    });
    if (senderName) {
      await db2.execute(
        drizzleSql`UPDATE payment_proofs SET verification_notes = ${`Remitente: ${senderName} | Suscripci\xF3n: ${sub.plan}`} WHERE id = ${proofId}`
      );
    }
    try {
      const { notifyAdminNewProof: notifyAdminNewProof2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      const { users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [u] = await db2.select({ name: users5.name }).from(users5).where((0, import_drizzle_orm64.eq)(users5.id, userId)).limit(1);
      notifyAdminNewProof2({
        proofId,
        orderId: `sub_${subscriptionId}`,
        userName: u?.name ?? "Cliente",
        amount: amount || sub.price,
        method: paymentMethod || "bizum"
      });
    } catch {
    }
    res.json({
      success: true,
      proofId,
      message: "Comprobante recibido. Tu suscripci\xF3n se activar\xE1 tras la verificaci\xF3n."
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router38.post(
  "/proofs/:proofId/approve",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { proofId } = req.params;
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { paymentProofs: paymentProofs2, subscriptions: subscriptions2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { sql: drizzleSql } = await import("drizzle-orm");
      const [proof] = await db2.select().from(paymentProofs2).where(drizzleSql`id = ${proofId}`).limit(1);
      if (!proof)
        return res.status(404).json({ error: "Comprobante no encontrado" });
      const subscriptionId = proof.orderId?.replace("sub_", "");
      if (!subscriptionId)
        return res.status(400).json({ error: "No es un comprobante de suscripci\xF3n" });
      await db2.execute(
        drizzleSql`UPDATE payment_proofs SET status = 'approved', verified_by = ${req.user.id}, verified_at = NOW() WHERE id = ${proofId}`
      );
      const now = /* @__PURE__ */ new Date();
      const [sub] = await db2.select().from(subscriptions2).where((0, import_drizzle_orm64.eq)(subscriptions2.id, subscriptionId)).limit(1);
      if (sub) {
        const periodEnd = new Date(now);
        sub.billingCycle === "yearly" ? periodEnd.setFullYear(periodEnd.getFullYear() + 1) : periodEnd.setMonth(periodEnd.getMonth() + 1);
        await db2.update(subscriptions2).set({
          status: "active",
          currentPeriodStart: now,
          currentPeriodEnd: periodEnd,
          autoRenew: true,
          cancelledAt: null
          // Limpiar cancelación previa si la hubiera
        }).where((0, import_drizzle_orm64.eq)(subscriptions2.id, subscriptionId));
      }
      const { SubscriptionService: SubscriptionService2 } = await Promise.resolve().then(() => (init_subscriptionService(), subscriptionService_exports));
      await SubscriptionService2.applyPlanActivationSideEffects(
        proof.userId,
        sub?.plan || ""
      ).catch(() => {
      });
      try {
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        await sendPushToUser2(proof.userId, {
          title: "\u2705 Suscripci\xF3n activada",
          body: `Tu plan ${sub?.plan || "Premium"} ya est\xE1 activo. \xA1Disfruta los beneficios!`,
          data: { screen: "Subscriptions" }
        });
      } catch {
      }
      res.json({ success: true, message: "Suscripci\xF3n activada" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router38.post(
  "/proofs/:proofId/reject",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { proofId } = req.params;
      const { reason } = req.body;
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { paymentProofs: paymentProofs2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { sql: drizzleSql } = await import("drizzle-orm");
      const [proof] = await db2.select().from(paymentProofs2).where(drizzleSql`id = ${proofId}`).limit(1);
      if (!proof)
        return res.status(404).json({ error: "Comprobante no encontrado" });
      await db2.execute(
        drizzleSql`UPDATE payment_proofs SET status = 'rejected', verified_by = ${req.user.id}, verified_at = NOW(), verification_notes = ${reason || "Rechazado por admin"} WHERE id = ${proofId}`
      );
      try {
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        await sendPushToUser2(proof.userId, {
          title: "\u274C Comprobante rechazado",
          body: reason || "Tu comprobante de suscripci\xF3n no pudo ser verificado. Contacta soporte.",
          data: { screen: "Subscriptions" }
        });
      } catch {
      }
      res.json({ success: true, message: "Comprobante rechazado" });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router38.post("/cancel-pending", authenticateToken, async (req, res) => {
  try {
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { subscriptions: subscriptions2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { eq: eq79, and: and47 } = await import("drizzle-orm");
    const { sql: drizzleSql } = await import("drizzle-orm");
    await db2.update(subscriptions2).set({ status: "cancelled", cancelledAt: /* @__PURE__ */ new Date() }).where(
      drizzleSql`user_id = ${req.user.id} AND status = 'pending_payment'`
    );
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router38.post("/cancel", authenticateToken, async (req, res) => {
  try {
    const result = await SubscriptionService.cancelSubscription(req.user.id);
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router38.get("/business-benefits", authenticateToken, async (req, res) => {
  try {
    const benefits = await SubscriptionService.getBusinessBenefits(
      req.user.id
    );
    const commission = await SubscriptionService.getBusinessCommissionDiscount(
      req.user.id
    );
    res.json({
      success: true,
      benefits,
      commission: {
        rate: commission.commissionRate,
        baseRate: commission.baseRate,
        discountPercent: commission.discountPercent,
        plan: commission.plan
      }
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var subscriptions_default = router38;

// server/routes/smartNotifications.ts
var import_express39 = __toESM(require("express"));

// server/smartNotificationService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm65 = require("drizzle-orm");
init_enhancedPushService();
var SmartNotificationService = class {
  // Segmentar usuarios
  static async segmentUsers(target) {
    const now = /* @__PURE__ */ new Date();
    const userIds = [];
    if (target.userSegment === "new") {
      const sevenDaysAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1e3);
      const newUsers = await db.select({ id: users.id }).from(users).where(
        (0, import_drizzle_orm65.and)((0, import_drizzle_orm65.gte)(users.createdAt, sevenDaysAgo), (0, import_drizzle_orm65.eq)(users.role, "customer"))
      );
      for (const user of newUsers) {
        const [order] = await db.select().from(orders).where((0, import_drizzle_orm65.eq)(orders.userId, user.id)).limit(1);
        if (!order) {
          userIds.push(user.id);
        }
      }
    } else if (target.userSegment === "inactive") {
      const thirtyDaysAgo = new Date(now.getTime() - 30 * 24 * 60 * 60 * 1e3);
      const allUsers = await db.select({ id: users.id }).from(users).where((0, import_drizzle_orm65.eq)(users.role, "customer"));
      for (const user of allUsers) {
        const [lastOrder] = await db.select().from(orders).where((0, import_drizzle_orm65.eq)(orders.userId, user.id)).orderBy((0, import_drizzle_orm65.desc)(orders.createdAt)).limit(1);
        if (lastOrder && lastOrder.createdAt < thirtyDaysAgo) {
          userIds.push(user.id);
        }
      }
    } else if (target.userSegment === "active") {
      const sevenDaysAgo = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1e3);
      const recentOrders = await db.select({ userId: orders.userId }).from(orders).where((0, import_drizzle_orm65.gte)(orders.createdAt, sevenDaysAgo)).groupBy(orders.userId);
      userIds.push(...recentOrders.map((o) => o.userId));
    } else if (target.userSegment === "vip") {
      const allUsers = await db.select({ id: users.id }).from(users).where((0, import_drizzle_orm65.eq)(users.role, "customer"));
      for (const user of allUsers) {
        const orderCount = await db.select({ count: import_drizzle_orm65.sql`COUNT(*)` }).from(orders).where((0, import_drizzle_orm65.eq)(orders.userId, user.id));
        if (orderCount[0].count >= 10) {
          userIds.push(user.id);
        }
      }
    }
    return [...new Set(userIds)];
  }
  // Enviar notificación de reactivación
  static async sendReactivationNotification() {
    const inactiveUsers = await this.segmentUsers({ userSegment: "inactive" });
    let sent = 0;
    for (const userId of inactiveUsers) {
      try {
        await sendPushToUser(userId, {
          title: "\xA1Te extra\xF1amos! \u{1F60A}",
          body: "Vuelve y disfruta de un 15% de descuento en tu pr\xF3ximo pedido",
          data: { screen: "Home", coupon: "COMEBACK15" }
        });
        sent++;
      } catch (error) {
        console.error(`Error sending to ${userId}:`, error);
      }
    }
    return { success: true, sent, total: inactiveUsers.length };
  }
  // Enviar notificación de promoción
  static async sendPromotionNotification(title, body, target, deepLink) {
    const targetUsers = await this.segmentUsers(target);
    let sent = 0;
    for (const userId of targetUsers) {
      try {
        await sendPushToUser(userId, {
          title,
          body,
          data: { screen: deepLink || "Home", type: "promotion" }
        });
        sent++;
      } catch (error) {
        console.error(`Error sending to ${userId}:`, error);
      }
    }
    return { success: true, sent, total: targetUsers.length };
  }
  // Enviar recordatorio inteligente (hora del almuerzo/cena)
  static async sendMealTimeReminder() {
    const hour = (/* @__PURE__ */ new Date()).getHours();
    let message = "";
    let targetSegment = "active";
    if (hour >= 12 && hour <= 14) {
      message = "\xBFHambre? \u{1F37D}\uFE0F Es hora del almuerzo. Pide ahora y recibe en 30 min";
    } else if (hour >= 19 && hour <= 21) {
      message = "\u{1F319} Hora de la cena. Tu restaurante favorito est\xE1 abierto";
      targetSegment = "vip";
    } else {
      return { success: false, message: "No es hora de comida" };
    }
    const targetUsers = await this.segmentUsers({ userSegment: targetSegment });
    let sent = 0;
    for (const userId of targetUsers) {
      try {
        await sendPushToUser(userId, {
          title: message,
          body: "Explora negocios cerca de ti",
          data: { screen: "Home", type: "reminder" }
        });
        sent++;
      } catch (error) {
        console.error(`Error sending to ${userId}:`, error);
      }
    }
    return { success: true, sent, total: targetUsers.length };
  }
  // Enviar notificación de nuevo negocio
  static async sendNewBusinessNotification(businessId, businessName) {
    const activeUsers = await this.segmentUsers({ userSegment: "active" });
    let sent = 0;
    for (const userId of activeUsers) {
      try {
        await sendPushToUser(userId, {
          title: `\u{1F389} Nuevo: ${businessName}`,
          body: "Descubre el nuevo restaurante en tu zona",
          data: { screen: "BusinessDetail", businessId, type: "new_business" }
        });
        sent++;
      } catch (error) {
        console.error(`Error sending to ${userId}:`, error);
      }
    }
    return { success: true, sent, total: activeUsers.length };
  }
  // Enviar notificación personalizada basada en favoritos
  static async sendFavoriteBusinessPromotion(businessId, promotion) {
    const { userFavorites: userFavorites2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const favorites2 = await db.select().from(userFavorites2).where(
      (0, import_drizzle_orm65.and)(
        (0, import_drizzle_orm65.eq)(userFavorites2.itemType, "business"),
        (0, import_drizzle_orm65.eq)(userFavorites2.itemId, businessId)
      )
    );
    let sent = 0;
    for (const fav of favorites2) {
      try {
        await sendPushToUser(fav.userId, {
          title: "\u{1F49D} Oferta en tu favorito",
          body: promotion,
          data: {
            screen: "BusinessDetail",
            businessId,
            type: "favorite_promo"
          }
        });
        sent++;
      } catch (error) {
        console.error(`Error sending to ${fav.userId}:`, error);
      }
    }
    return { success: true, sent, total: favorites2.length };
  }
};

// server/routes/smartNotifications.ts
var router39 = import_express39.default.Router();
router39.post(
  "/reactivation",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await SmartNotificationService.sendReactivationNotification();
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router39.post(
  "/promotion",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { title, body, target, deepLink } = req.body;
      if (!title || !body || !target) {
        return res.status(400).json({ error: "Faltan par\xE1metros requeridos" });
      }
      const result = await SmartNotificationService.sendPromotionNotification(
        title,
        body,
        target,
        deepLink
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router39.post(
  "/meal-reminder",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const result = await SmartNotificationService.sendMealTimeReminder();
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router39.post(
  "/new-business",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId, businessName } = req.body;
      if (!businessId || !businessName) {
        return res.status(400).json({ error: "businessId y businessName requeridos" });
      }
      const result = await SmartNotificationService.sendNewBusinessNotification(
        businessId,
        businessName
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router39.post(
  "/favorite-promo",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessId, promotion } = req.body;
      if (!businessId || !promotion) {
        return res.status(400).json({ error: "businessId y promotion requeridos" });
      }
      const result = await SmartNotificationService.sendFavoriteBusinessPromotion(
        businessId,
        promotion
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var smartNotifications_default = router39;

// server/routes/enhancedReviews.ts
var import_express40 = __toESM(require("express"));

// server/enhancedReviewService.ts
init_db();
init_orderNumberService();
init_schema_mysql();
var import_drizzle_orm67 = require("drizzle-orm");
init_cloudinaryService();
var EnhancedReviewService = class {
  // Crear review mejorada
  static async createReview(data) {
    const {
      userId,
      orderId,
      businessId,
      deliveryPersonId,
      foodRating,
      deliveryRating,
      packagingRating,
      driverRating,
      comment,
      tags,
      photos,
      tipAmount
    } = data;
    const [orderRow] = await db.select({ id: orders.id, userId: orders.userId }).from(orders).where((0, import_drizzle_orm67.eq)(orders.id, orderId)).limit(1);
    if (!orderRow) {
      return { success: false, error: "Pedido no encontrado" };
    }
    if (orderRow.userId !== userId) {
      return {
        success: false,
        error: "Solo puedes valorar tus propios pedidos"
      };
    }
    const [existingReview] = await db.select({ id: reviews.id }).from(reviews).where((0, import_drizzle_orm67.eq)(reviews.orderId, orderId)).limit(1);
    if (existingReview) {
      return {
        success: false,
        error: "Ya valoraste este pedido",
        alreadyReviewed: true
      };
    }
    let photoUrls = [];
    if (photos && photos.length > 0) {
      const uploaded = await Promise.all(
        photos.map(async (photo, index) => {
          if (!photo.startsWith("data:image/")) {
            return photo;
          }
          try {
            return await CloudinaryService.uploadImage(
              photo,
              "reviews",
              `review-${orderId}-${index}`
            );
          } catch (err) {
            console.error(`[reviews] No se pudo subir la foto ${index}:`, err);
            return null;
          }
        })
      );
      photoUrls = uploaded.filter((u) => !!u);
    }
    const ratings = [foodRating, deliveryRating, packagingRating].filter(
      (r) => r && r > 0
    );
    const averageRating = ratings.length > 0 ? Math.round(ratings.reduce((a, b) => a + b, 0) / ratings.length) : 5;
    const reviewId = crypto.randomUUID();
    await db.insert(reviews).values({
      id: reviewId,
      userId,
      orderId,
      businessId,
      deliveryPersonId: deliveryPersonId || null,
      rating: averageRating,
      foodRating: foodRating || null,
      deliveryRating: deliveryRating || null,
      packagingRating: packagingRating || null,
      deliveryPersonRating: driverRating || null,
      comment: comment || null,
      photos: photoUrls.length > 0 ? JSON.stringify(photoUrls) : null,
      tags: tags && tags.length > 0 ? JSON.stringify(tags) : null,
      approved: true,
      flagged: false,
      tipAmount: tipAmount || 0
    });
    await this.updateBusinessRating(businessId);
    if (deliveryPersonId && driverRating) {
      await this.updateDriverRating(deliveryPersonId);
    }
    if (tipAmount && tipAmount > 0 && deliveryPersonId && orderId) {
      try {
        const { wallets: wallets2, transactions: transactions4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
        const [driverWallet] = await db.select().from(wallets2).where((0, import_drizzle_orm67.eq)(wallets2.userId, deliveryPersonId)).limit(1);
        if (driverWallet) {
          await db.update(wallets2).set({
            balance: driverWallet.balance + tipAmount,
            totalEarned: driverWallet.totalEarned + tipAmount
          }).where((0, import_drizzle_orm67.eq)(wallets2.userId, deliveryPersonId));
        }
        const txId = crypto.randomUUID();
        await db.insert(transactions4).values({
          id: txId,
          userId: deliveryPersonId,
          orderId,
          type: "tip",
          amount: tipAmount,
          description: `Propina del cliente por pedido ${await orderRefFromId(orderId)}`,
          status: "completed"
        });
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        await sendPushToUser2(deliveryPersonId, {
          title: "\u{1F49D} \xA1Recibiste una propina!",
          body: `El cliente te dej\xF3 una propina de ${(tipAmount / 100).toFixed(2)} \u20AC`,
          data: { screen: "DriverEarnings" }
        });
      } catch (err) {
        console.error("Error processing review tip:", err);
      }
    }
    try {
      const { GamificationService: GamificationService2 } = await Promise.resolve().then(() => (init_gamificationService(), gamificationService_exports));
      const { count: countFn } = await import("drizzle-orm");
      const { achievements: achTable } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [{ value: reviewCount }] = await db.select({ value: countFn() }).from(reviews).where((0, import_drizzle_orm67.eq)(reviews.userId, userId));
      const reviewAchievements = await db.select().from(achTable).where((0, import_drizzle_orm67.eq)(achTable.requirementType, "review_count"));
      for (const ach of reviewAchievements) {
        if (Number(reviewCount) >= ach.requirementValue) {
          await GamificationService2.unlockAchievement(userId, ach.id);
        }
      }
    } catch {
    }
    return { success: true, reviewId };
  }
  // Actualizar rating promedio del negocio
  static async updateBusinessRating(businessId) {
    const businessReviews = await db.select().from(reviews).where(
      (0, import_drizzle_orm67.and)((0, import_drizzle_orm67.eq)(reviews.businessId, businessId), (0, import_drizzle_orm67.eq)(reviews.approved, true))
    );
    if (businessReviews.length === 0) return;
    const totalRating = businessReviews.reduce((sum3, r) => sum3 + r.rating, 0);
    const avgRating = Math.round(totalRating / businessReviews.length * 10);
    await db.update(businesses).set({
      rating: avgRating,
      totalRatings: businessReviews.length
    }).where((0, import_drizzle_orm67.eq)(businesses.id, businessId));
  }
  // Actualizar rating promedio del repartidor
  static async updateDriverRating(driverId) {
    const driverReviews = await db.select().from(reviews).where(
      (0, import_drizzle_orm67.and)((0, import_drizzle_orm67.eq)(reviews.deliveryPersonId, driverId), (0, import_drizzle_orm67.eq)(reviews.approved, true))
    );
    if (driverReviews.length === 0) return;
    const ratingsWithDriver = driverReviews.filter(
      (r) => r.deliveryPersonRating
    );
    if (ratingsWithDriver.length === 0) return;
    const totalRating = ratingsWithDriver.reduce(
      (sum3, r) => sum3 + (r.deliveryPersonRating || 0),
      0
    );
    const avgRating = Math.round(totalRating / ratingsWithDriver.length * 10);
    await db.update(deliveryDrivers).set({
      rating: avgRating,
      totalRatings: ratingsWithDriver.length
    }).where((0, import_drizzle_orm67.eq)(deliveryDrivers.userId, driverId));
  }
  // Obtener tags disponibles
  static async getTags() {
    const tags = await db.select().from(reviewTags).where((0, import_drizzle_orm67.eq)(reviewTags.isActive, true)).orderBy(reviewTags.displayOrder);
    return { success: true, tags };
  }
  // Obtener reviews de un negocio
  static async getBusinessReviews(businessId, limit = 20) {
    const businessReviews = await db.select().from(reviews).where(
      (0, import_drizzle_orm67.and)((0, import_drizzle_orm67.eq)(reviews.businessId, businessId), (0, import_drizzle_orm67.eq)(reviews.approved, true))
    ).orderBy((0, import_drizzle_orm67.desc)(reviews.createdAt)).limit(limit);
    const reviewsWithResponses = await Promise.all(
      businessReviews.map(async (review) => {
        const [response] = await db.select().from(reviewResponses).where((0, import_drizzle_orm67.eq)(reviewResponses.reviewId, review.id)).limit(1);
        return {
          ...review,
          photos: review.photos ? JSON.parse(review.photos) : [],
          tags: review.tags ? JSON.parse(review.tags) : [],
          response: response || null
        };
      })
    );
    return { success: true, reviews: reviewsWithResponses };
  }
  // Responder a una review (solo dueño del negocio)
  static async respondToReview(reviewId, businessId, respondedBy, responseText) {
    const [review] = await db.select().from(reviews).where((0, import_drizzle_orm67.and)((0, import_drizzle_orm67.eq)(reviews.id, reviewId), (0, import_drizzle_orm67.eq)(reviews.businessId, businessId))).limit(1);
    if (!review) {
      return { success: false, error: "Review no encontrada" };
    }
    const [existing] = await db.select().from(reviewResponses).where((0, import_drizzle_orm67.eq)(reviewResponses.reviewId, reviewId)).limit(1);
    if (existing) {
      await db.update(reviewResponses).set({ responseText, updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm67.eq)(reviewResponses.id, existing.id));
    } else {
      await db.insert(reviewResponses).values({
        id: crypto.randomUUID(),
        reviewId,
        businessId,
        responseText,
        respondedBy
      });
    }
    return { success: true };
  }
};

// server/routes/enhancedReviews.ts
var router40 = import_express40.default.Router();
router40.post("/", authenticateToken, async (req, res) => {
  try {
    const {
      orderId,
      businessId,
      deliveryPersonId,
      foodRating,
      deliveryRating,
      packagingRating,
      driverRating,
      comment,
      tags,
      photos,
      tipAmount
    } = req.body;
    if (!orderId || !businessId) {
      return res.status(400).json({ success: false, error: "Falta el pedido o el negocio" });
    }
    const validRating = (r) => r === void 0 || r === null || Number.isInteger(r) && r >= 1 && r <= 5;
    if (!validRating(foodRating) || !validRating(deliveryRating) || !validRating(packagingRating) || !validRating(driverRating)) {
      return res.status(400).json({ success: false, error: "Las puntuaciones deben ir de 1 a 5" });
    }
    const tipCents = Math.round(Number(tipAmount) || 0);
    if (!Number.isFinite(tipCents) || tipCents < 0 || tipCents > 5e4) {
      return res.status(400).json({ success: false, error: "Cantidad de propina no v\xE1lida" });
    }
    const result = await EnhancedReviewService.createReview({
      userId: req.user.id,
      orderId,
      businessId,
      deliveryPersonId,
      foodRating,
      deliveryRating,
      packagingRating,
      driverRating,
      comment,
      tags,
      photos,
      tipAmount: tipCents
    });
    res.json(result);
  } catch (error) {
    console.error("Create review error:", error);
    res.status(500).json({
      success: false,
      error: "No se pudo guardar la rese\xF1a. Int\xE9ntalo de nuevo."
    });
  }
});
router40.get("/tags", async (req, res) => {
  try {
    const result = await EnhancedReviewService.getTags();
    res.json(result);
  } catch (error) {
    console.error("Get tags error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router40.get("/business/:businessId", async (req, res) => {
  try {
    const { businessId } = req.params;
    const limit = parseInt(req.query.limit) || 20;
    const result = await EnhancedReviewService.getBusinessReviews(
      businessId,
      limit
    );
    res.json(result);
  } catch (error) {
    console.error("Get business reviews error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router40.post(
  "/:reviewId/respond",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { reviewId } = req.params;
      const { businessId, responseText } = req.body;
      if (!responseText || responseText.trim().length === 0) {
        return res.status(400).json({ success: false, error: "Respuesta requerida" });
      }
      const result = await EnhancedReviewService.respondToReview(
        reviewId,
        businessId,
        req.user.id,
        responseText
      );
      res.json(result);
    } catch (error) {
      console.error("Respond to review error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var enhancedReviews_default = router40;

// server/routes/businessAnalytics.ts
var import_express41 = __toESM(require("express"));

// server/businessAnalyticsService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm68 = require("drizzle-orm");
var BusinessAnalyticsService = class {
  // Dashboard principal con métricas en tiempo real
  static async getDashboard(businessId, period = "week") {
    const now = /* @__PURE__ */ new Date();
    let startDate = null;
    switch (period) {
      case "today":
        startDate = new Date(now.getFullYear(), now.getMonth(), now.getDate());
        break;
      case "week":
        startDate = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1e3);
        break;
      case "month":
        startDate = new Date(now.getFullYear(), now.getMonth(), 1);
        break;
      case "all":
        startDate = null;
        break;
    }
    const periodOrders = await db.select().from(orders).where(
      startDate ? (0, import_drizzle_orm68.and)(
        (0, import_drizzle_orm68.eq)(orders.businessId, businessId),
        (0, import_drizzle_orm68.gte)(orders.createdAt, startDate)
      ) : (0, import_drizzle_orm68.eq)(orders.businessId, businessId)
    );
    const periodLength = startDate ? now.getTime() - startDate.getTime() : 0;
    const previousStartDate = startDate ? new Date(startDate.getTime() - periodLength) : null;
    const previousOrders = startDate && previousStartDate ? await db.select().from(orders).where(
      (0, import_drizzle_orm68.and)(
        (0, import_drizzle_orm68.eq)(orders.businessId, businessId),
        (0, import_drizzle_orm68.gte)(orders.createdAt, previousStartDate),
        (0, import_drizzle_orm68.lte)(orders.createdAt, startDate)
      )
    ) : [];
    const allOrders = startDate ? await db.select().from(orders).where((0, import_drizzle_orm68.eq)(orders.businessId, businessId)) : periodOrders;
    const todayStart = new Date(
      now.getFullYear(),
      now.getMonth(),
      now.getDate()
    );
    const todayOrders = allOrders.filter(
      (o) => o.createdAt && new Date(o.createdAt) >= todayStart
    );
    const completedOrders = periodOrders.filter(
      (o) => o.status === "delivered"
    );
    const cancelledOrders = periodOrders.filter(
      (o) => o.status === "cancelled"
    );
    const getBusinessRevenue = (o) => o.businessEarnings ?? o.subtotal ?? 0;
    const totalRevenue = completedOrders.reduce(
      (sum3, o) => sum3 + getBusinessRevenue(o),
      0
    );
    const previousRevenue = previousOrders.filter((o) => o.status === "delivered").reduce((sum3, o) => sum3 + getBusinessRevenue(o), 0);
    const revenueChange = previousRevenue > 0 ? (totalRevenue - previousRevenue) / previousRevenue * 100 : 0;
    const avgOrderValue = completedOrders.length > 0 ? totalRevenue / completedOrders.length : 0;
    const totalOrders = periodOrders.length;
    const previousTotalOrders = previousOrders.length;
    const ordersChange = previousTotalOrders > 0 ? (totalOrders - previousTotalOrders) / previousTotalOrders * 100 : 0;
    const [business] = await db.select().from(businesses).where((0, import_drizzle_orm68.eq)(businesses.id, businessId)).limit(1);
    const rating = business?.rating ? business.rating / 10 : 0;
    return {
      success: true,
      dashboard: {
        period,
        totalOrders,
        ordersChange: Math.round(ordersChange * 10) / 10,
        totalRevenue,
        revenueChange: Math.round(revenueChange * 10) / 10,
        avgOrderValue,
        averageTicket: avgOrderValue,
        rating,
        averageRating: business?.rating || 0,
        totalReviews: business?.totalRatings || 0,
        // Campos que espera el frontend
        todayOrders: todayOrders.length,
        completedOrders: completedOrders.length,
        cancelledOrders: cancelledOrders.length,
        todayRevenue: todayOrders.filter((o) => o.status === "delivered").reduce((s, o) => s + getBusinessRevenue(o), 0),
        weekRevenue: totalRevenue,
        monthRevenue: totalRevenue
      }
    };
  }
  // Productos más vendidos
  static async getTopProducts(businessId, limit = 10) {
    const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1e3);
    const recentOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm68.and)(
        (0, import_drizzle_orm68.eq)(orders.businessId, businessId),
        (0, import_drizzle_orm68.eq)(orders.status, "delivered"),
        (0, import_drizzle_orm68.gte)(orders.createdAt, thirtyDaysAgo)
      )
    );
    const productCounts = {};
    for (const order of recentOrders) {
      try {
        const items = typeof order.items === "string" ? JSON.parse(order.items) : order.items || [];
        for (const item of items) {
          const productId = item.productId || item.product?.id || item.id || "unknown";
          const productName = item.product?.name || item.name || "Producto";
          const quantity = item.quantity || 1;
          const price = item.price || item.product?.price || 0;
          if (!productCounts[productId]) {
            productCounts[productId] = {
              name: productName,
              count: 0,
              revenue: 0
            };
          }
          productCounts[productId].count += quantity;
          productCounts[productId].revenue += price * quantity;
        }
      } catch {
      }
    }
    const topProducts = Object.entries(productCounts).map(([id, data]) => ({
      productId: id,
      name: data.name,
      unitsSold: data.count,
      revenue: data.revenue / 100
    })).sort((a, b) => b.unitsSold - a.unitsSold).slice(0, limit);
    return { success: true, topProducts };
  }
  // Horas pico
  static async getPeakHours(businessId) {
    const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1e3);
    const recentOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm68.and)(
        (0, import_drizzle_orm68.eq)(orders.businessId, businessId),
        (0, import_drizzle_orm68.gte)(orders.createdAt, thirtyDaysAgo)
      )
    );
    const hourCounts = {};
    for (let i = 0; i < 24; i++) {
      hourCounts[i] = 0;
    }
    for (const order of recentOrders) {
      const hour = new Date(order.createdAt).getHours();
      hourCounts[hour]++;
    }
    const peakHours = Object.entries(hourCounts).map(([hour, count3]) => ({
      hour: parseInt(hour),
      orderCount: count3,
      label: `${hour}:00 - ${parseInt(hour) + 1}:00`
    })).sort((a, b) => b.orderCount - a.orderCount);
    return { success: true, peakHours };
  }
  // Ventas por día (últimos 30 días)
  static async getSalesChart(businessId, days = 30) {
    const startDate = new Date(Date.now() - days * 24 * 60 * 60 * 1e3);
    const recentOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm68.and)(
        (0, import_drizzle_orm68.eq)(orders.businessId, businessId),
        (0, import_drizzle_orm68.eq)(orders.status, "delivered"),
        (0, import_drizzle_orm68.gte)(orders.createdAt, startDate)
      )
    );
    const dailySales = {};
    for (const order of recentOrders) {
      const date = new Date(order.createdAt).toISOString().split("T")[0];
      if (!dailySales[date]) {
        dailySales[date] = { orders: 0, revenue: 0 };
      }
      dailySales[date].orders++;
      dailySales[date].revenue += order.businessEarnings ?? order.subtotal ?? 0;
    }
    const chartData = Object.entries(dailySales).map(([date, data]) => ({
      date,
      orders: data.orders,
      revenue: data.revenue / 100
    })).sort((a, b) => a.date.localeCompare(b.date));
    return { success: true, chartData };
  }
  // Estadísticas de reviews
  static async getReviewStats(businessId) {
    const businessReviews = await db.select().from(reviews).where(
      (0, import_drizzle_orm68.and)((0, import_drizzle_orm68.eq)(reviews.businessId, businessId), (0, import_drizzle_orm68.eq)(reviews.approved, true))
    );
    const totalReviews = businessReviews.length;
    if (totalReviews === 0) {
      return {
        success: true,
        reviewStats: {
          totalReviews: 0,
          avgRating: 0,
          avgFoodRating: 0,
          avgPackagingRating: 0,
          ratingDistribution: { 5: 0, 4: 0, 3: 0, 2: 0, 1: 0 }
        }
      };
    }
    const avgRating = businessReviews.reduce((sum3, r) => sum3 + r.rating, 0) / totalReviews;
    const foodRatings = businessReviews.filter((r) => r.foodRating);
    const avgFoodRating = foodRatings.length > 0 ? foodRatings.reduce((sum3, r) => sum3 + (r.foodRating || 0), 0) / foodRatings.length : 0;
    const packagingRatings = businessReviews.filter((r) => r.packagingRating);
    const avgPackagingRating = packagingRatings.length > 0 ? packagingRatings.reduce(
      (sum3, r) => sum3 + (r.packagingRating || 0),
      0
    ) / packagingRatings.length : 0;
    const ratingDistribution = { 5: 0, 4: 0, 3: 0, 2: 0, 1: 0 };
    for (const review of businessReviews) {
      ratingDistribution[review.rating]++;
    }
    return {
      success: true,
      reviewStats: {
        totalReviews,
        avgRating: Math.round(avgRating * 10) / 10,
        avgFoodRating: Math.round(avgFoodRating * 10) / 10,
        avgPackagingRating: Math.round(avgPackagingRating * 10) / 10,
        ratingDistribution
      }
    };
  }
  // Comparativa semanal
  static async getWeeklyComparison(businessId) {
    const now = /* @__PURE__ */ new Date();
    const thisWeekStart = new Date(now.getTime() - 7 * 24 * 60 * 60 * 1e3);
    const lastWeekStart = new Date(now.getTime() - 14 * 24 * 60 * 60 * 1e3);
    const thisWeekOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm68.and)(
        (0, import_drizzle_orm68.eq)(orders.businessId, businessId),
        (0, import_drizzle_orm68.eq)(orders.status, "delivered"),
        (0, import_drizzle_orm68.gte)(orders.createdAt, thisWeekStart)
      )
    );
    const lastWeekOrders = await db.select().from(orders).where(
      (0, import_drizzle_orm68.and)(
        (0, import_drizzle_orm68.eq)(orders.businessId, businessId),
        (0, import_drizzle_orm68.eq)(orders.status, "delivered"),
        (0, import_drizzle_orm68.gte)(orders.createdAt, lastWeekStart),
        (0, import_drizzle_orm68.lte)(orders.createdAt, thisWeekStart)
      )
    );
    const thisWeekRevenue = thisWeekOrders.reduce(
      (sum3, o) => sum3 + (o.businessEarnings || 0),
      0
    );
    const lastWeekRevenue = lastWeekOrders.reduce(
      (sum3, o) => sum3 + (o.businessEarnings || 0),
      0
    );
    return {
      success: true,
      comparison: {
        thisWeek: {
          orders: thisWeekOrders.length,
          revenue: thisWeekRevenue / 100
        },
        lastWeek: {
          orders: lastWeekOrders.length,
          revenue: lastWeekRevenue / 100
        },
        ordersChange: lastWeekOrders.length > 0 ? (thisWeekOrders.length - lastWeekOrders.length) / lastWeekOrders.length * 100 : 0,
        revenueChange: lastWeekRevenue > 0 ? (thisWeekRevenue - lastWeekRevenue) / lastWeekRevenue * 100 : 0
      }
    };
  }
};

// server/routes/businessAnalytics.ts
var router41 = import_express41.default.Router();
router41.get(
  "/dashboard/:businessId",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId } = req.params;
      const period = req.query.period || "week";
      const result = await BusinessAnalyticsService.getDashboard(
        businessId,
        period
      );
      res.json(result);
    } catch (error) {
      console.error("Get dashboard error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router41.get(
  "/top-products/:businessId",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId } = req.params;
      const limit = parseInt(req.query.limit) || 10;
      const result = await BusinessAnalyticsService.getTopProducts(
        businessId,
        limit
      );
      res.json(result);
    } catch (error) {
      console.error("Get top products error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router41.get(
  "/peak-hours/:businessId",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId } = req.params;
      const result = await BusinessAnalyticsService.getPeakHours(businessId);
      res.json(result);
    } catch (error) {
      console.error("Get peak hours error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router41.get(
  "/sales-chart/:businessId",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId } = req.params;
      const days = parseInt(req.query.days) || 30;
      const result = await BusinessAnalyticsService.getSalesChart(
        businessId,
        days
      );
      res.json(result);
    } catch (error) {
      console.error("Get sales chart error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router41.get(
  "/reviews/:businessId",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId } = req.params;
      const result = await BusinessAnalyticsService.getReviewStats(businessId);
      res.json(result);
    } catch (error) {
      console.error("Get review stats error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router41.get(
  "/weekly-comparison/:businessId",
  authenticateToken,
  requireRole("business_owner"),
  async (req, res) => {
    try {
      const { businessId } = req.params;
      const result = await BusinessAnalyticsService.getWeeklyComparison(businessId);
      res.json(result);
    } catch (error) {
      console.error("Get weekly comparison error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var businessAnalytics_default = router41;

// server/routes/gamification.ts
var import_express42 = __toESM(require("express"));
init_gamificationService();
var router42 = import_express42.default.Router();
router42.get("/points", authenticateToken, async (req, res) => {
  try {
    const result = await GamificationService.getUserPoints(req.user.id);
    res.json(result);
  } catch (error) {
    console.error("Get points error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router42.get("/leaderboard", async (req, res) => {
  try {
    const limit = parseInt(req.query.limit) || 50;
    const result = await GamificationService.getLeaderboard(limit);
    res.json(result);
  } catch (error) {
    console.error("Get leaderboard error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router42.get("/achievements", authenticateToken, async (req, res) => {
  try {
    const result = await GamificationService.getUserAchievements(req.user.id);
    res.json(result);
  } catch (error) {
    console.error("Get achievements error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router42.get("/rewards", authenticateToken, async (req, res) => {
  try {
    const result = await GamificationService.getAvailableRewards(req.user.id);
    res.json(result);
  } catch (error) {
    console.error("Get rewards error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router42.post("/redeem/:rewardId", authenticateToken, async (req, res) => {
  try {
    const { rewardId } = req.params;
    const result = await GamificationService.redeemReward(
      req.user.id,
      rewardId
    );
    res.json(result);
  } catch (error) {
    console.error("Redeem reward error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
var gamification_default = router42;

// server/routes/giftCards.ts
var import_express43 = __toESM(require("express"));
init_giftCardService();
var router43 = import_express43.default.Router();
router43.post("/purchase", authenticateToken, async (req, res) => {
  try {
    const {
      amount,
      recipientEmail,
      recipientPhone,
      message,
      design,
      paymentMethod
    } = req.body;
    const result = await GiftCardService.purchaseGiftCard({
      purchasedBy: req.user.id,
      amount: Math.round(amount * 100),
      recipientEmail,
      recipientPhone,
      message,
      design,
      activateImmediately: false
      // siempre false aquí; Stripe activa via webhook
    });
    res.json(result);
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router43.post(
  "/:giftCardId/stripe-success",
  authenticateToken,
  async (req, res) => {
    try {
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { giftCards: giftCards2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { getStripe: getStripe5 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
      const [gc] = await db2.select().from(giftCards2).where(eq79(giftCards2.id, req.params.giftCardId)).limit(1);
      if (!gc)
        return res.status(404).json({ success: false, error: "Gift card no encontrada" });
      if (gc.purchasedBy !== req.user.id)
        return res.status(403).json({ success: false, error: "No autorizado" });
      if (gc.status === "active")
        return res.json({ success: true, message: "Ya estaba activa" });
      if (gc.status !== "pending_payment")
        return res.status(400).json({ success: false, error: "Estado inv\xE1lido" });
      const { paymentIntentId } = req.body;
      if (paymentIntentId && process.env.STRIPE_SECRET_KEY) {
        const stripe2 = getStripe5();
        const pi = await stripe2.paymentIntents.retrieve(paymentIntentId);
        if (pi.status !== "succeeded") {
          return res.status(400).json({ success: false, error: "El pago no se ha completado" });
        }
      }
      const result = await GiftCardService.activateGiftCard(
        req.params.giftCardId,
        req.user.id
      );
      res.json(result);
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router43.post(
  "/:giftCardId/payment-proof",
  authenticateToken,
  async (req, res) => {
    try {
      const { giftCardId } = req.params;
      const { paymentProvider, proofImageUrl, referenceNumber, amount } = req.body;
      if (!paymentProvider || !proofImageUrl) {
        return res.status(400).json({ success: false, error: "paymentProvider y proofImageUrl son requeridos" });
      }
      let amountCents = amount ? Math.round(amount * 100) : 0;
      if (!amountCents) {
        const { giftCards: giftCards2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
        const { eq: eq79 } = await import("drizzle-orm");
        const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
        const [gc] = await db2.select().from(giftCards2).where(eq79(giftCards2.id, giftCardId)).limit(1);
        amountCents = gc?.amount || 0;
      }
      const result = await GiftCardService.submitPaymentProof({
        giftCardId,
        userId: req.user.id,
        paymentProvider,
        proofImageUrl,
        referenceNumber,
        amount: amountCents
      });
      res.json(result);
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router43.post("/validate", authenticateToken, async (req, res) => {
  try {
    const { code } = req.body;
    if (!code)
      return res.status(400).json({ success: false, error: "C\xF3digo requerido" });
    res.json(await GiftCardService.validateGiftCard(code));
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router43.post("/redeem", authenticateToken, async (req, res) => {
  try {
    const { code, orderId, amountToUse } = req.body;
    if (!code || !orderId || !amountToUse)
      return res.status(400).json({ success: false, error: "Datos incompletos" });
    res.json(
      await GiftCardService.redeemGiftCard({
        code,
        orderId,
        userId: req.user.id,
        amountToUse: Math.round(amountToUse * 100)
      })
    );
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router43.get("/my-cards", authenticateToken, async (req, res) => {
  try {
    res.json(await GiftCardService.getUserGiftCards(req.user.id));
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router43.get("/designs", async (req, res) => {
  try {
    res.json(await GiftCardService.getDesigns());
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router43.get("/:giftCardId/history", authenticateToken, async (req, res) => {
  try {
    res.json(
      await GiftCardService.getTransactionHistory(req.params.giftCardId)
    );
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
var giftCards_default = router43;

// server/routes/groupOrders.ts
var import_express44 = __toESM(require("express"));

// server/groupOrderService.ts
init_db();
init_schema_mysql();
var import_drizzle_orm69 = require("drizzle-orm");
var GroupOrderService = class {
  static async createGroupOrder(data) {
    const {
      creatorId,
      businessId,
      deliveryAddressId = null,
      expiresInMinutes = 60
    } = data;
    const groupOrderId = crypto.randomUUID();
    const expiresAt = new Date(Date.now() + expiresInMinutes * 60 * 1e3);
    await db.insert(groupOrders).values({
      id: groupOrderId,
      hostUserId: creatorId,
      businessId,
      deliveryAddressId,
      splitMethod: "equal",
      status: "open",
      expiresAt
    });
    return {
      success: true,
      groupOrderId,
      // La tabla real no tiene share_token: usamos el propio id como token
      shareToken: groupOrderId,
      shareLink: `comeya://group-order/${groupOrderId}`
    };
  }
  static async joinGroupOrder(data) {
    const { shareToken, userId, items, subtotal } = data;
    const [group] = await db.select().from(groupOrders).where((0, import_drizzle_orm69.eq)(groupOrders.id, shareToken)).limit(1);
    if (!group) return { success: false, error: "Grupo no encontrado" };
    if (group.status !== "open") {
      return { success: false, error: "El grupo ya no acepta participantes" };
    }
    if (group.expiresAt && group.expiresAt < /* @__PURE__ */ new Date()) {
      return { success: false, error: "El grupo ha expirado" };
    }
    const [existing] = await db.select().from(groupOrderParticipants).where((0, import_drizzle_orm69.eq)(groupOrderParticipants.userId, userId)).limit(1);
    if (existing) {
      return { success: true, participantId: existing.id, alreadyJoined: true };
    }
    const participantId = crypto.randomUUID();
    await db.insert(groupOrderParticipants).values({
      id: participantId,
      groupOrderId: group.id,
      userId,
      items: JSON.stringify(items || []),
      subtotal: subtotal || 0,
      paid: false
    });
    return { success: true, participantId };
  }
  static async getGroupOrder(groupOrderId) {
    const [group] = await db.select().from(groupOrders).where((0, import_drizzle_orm69.eq)(groupOrders.id, groupOrderId)).limit(1);
    if (!group) return { success: false, error: "Grupo no encontrado" };
    const [business] = await db.select({
      id: businesses.id,
      name: businesses.name,
      image: businesses.image
    }).from(businesses).where((0, import_drizzle_orm69.eq)(businesses.id, group.businessId)).limit(1);
    const participants = await db.select().from(groupOrderParticipants).where((0, import_drizzle_orm69.eq)(groupOrderParticipants.groupOrderId, groupOrderId));
    const userIds = participants.map((p) => p.userId);
    let userNames = {};
    if (userIds.length) {
      const userRows = await db.select({ id: users.id, name: users.name }).from(users).where((0, import_drizzle_orm69.inArray)(users.id, userIds));
      userNames = Object.fromEntries(
        userRows.map((u) => [u.id, u.name || "Invitado"])
      );
    }
    return {
      success: true,
      group,
      business: business || null,
      participants: participants.map((p) => ({
        ...p,
        userName: userNames[p.userId] || "Invitado"
      }))
    };
  }
  static async markParticipantPaid(participantId, paymentProofUrl) {
    await db.update(groupOrderParticipants).set({ paid: true, updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm69.eq)(groupOrderParticipants.id, participantId));
    return { success: true };
  }
  static async lockGroupOrder(groupOrderId, userId) {
    const [group] = await db.select().from(groupOrders).where((0, import_drizzle_orm69.eq)(groupOrders.id, groupOrderId)).limit(1);
    if (!group) return { success: false, error: "Grupo no encontrado" };
    if (group.hostUserId !== userId) {
      return {
        success: false,
        error: "Solo el anfitri\xF3n puede bloquear el grupo"
      };
    }
    await db.update(groupOrders).set({ status: "locked", updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm69.eq)(groupOrders.id, groupOrderId));
    return { success: true };
  }
  static async getMyGroups(userId) {
    const hosted = await db.select().from(groupOrders).where((0, import_drizzle_orm69.eq)(groupOrders.hostUserId, userId));
    const participations = await db.select({ groupOrderId: groupOrderParticipants.groupOrderId }).from(groupOrderParticipants).where((0, import_drizzle_orm69.eq)(groupOrderParticipants.userId, userId));
    const ids = new Set(hosted.map((g) => g.id));
    for (const p of participations) ids.add(p.groupOrderId);
    if (!ids.size) return [];
    const groups = await db.select().from(groupOrders).where((0, import_drizzle_orm69.inArray)(groupOrders.id, [...ids]));
    return groups;
  }
};

// server/routes/groupOrders.ts
var router44 = import_express44.default.Router();
router44.post("/", authenticateToken, async (req, res) => {
  try {
    const {
      businessId,
      businessName,
      deliveryAddress,
      deliveryLatitude,
      deliveryLongitude,
      expiresInMinutes
    } = req.body;
    const result = await GroupOrderService.createGroupOrder({
      creatorId: req.user.id,
      businessId,
      businessName,
      deliveryAddress,
      deliveryLatitude,
      deliveryLongitude,
      expiresInMinutes
    });
    res.json(result);
  } catch (error) {
    console.error("Create group order error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router44.post("/join", authenticateToken, async (req, res) => {
  try {
    const { shareToken, items, subtotal } = req.body;
    const result = await GroupOrderService.joinGroupOrder({
      shareToken,
      userId: req.user.id,
      userName: req.user.name,
      items,
      subtotal
    });
    res.json(result);
  } catch (error) {
    console.error("Join group order error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router44.get("/:groupOrderId", authenticateToken, async (req, res) => {
  try {
    const { groupOrderId } = req.params;
    const result = await GroupOrderService.getGroupOrder(groupOrderId);
    res.json(result);
  } catch (error) {
    console.error("Get group order error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router44.get("/by-token/:shareToken", authenticateToken, async (req, res) => {
  try {
    const { shareToken } = req.params;
    const result = await GroupOrderService.getGroupOrder(shareToken);
    res.json(result);
  } catch (error) {
    console.error("Get group order by token error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router44.post("/:groupOrderId/lock", authenticateToken, async (req, res) => {
  try {
    const { groupOrderId } = req.params;
    const result = await GroupOrderService.lockGroupOrder(
      groupOrderId,
      req.user.id
    );
    res.json(result);
  } catch (error) {
    console.error("Lock group order error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
router44.post(
  "/participants/:participantId/pay",
  authenticateToken,
  async (req, res) => {
    try {
      const { participantId } = req.params;
      const { paymentProofUrl } = req.body;
      const result = await GroupOrderService.markParticipantPaid(
        participantId,
        paymentProofUrl
      );
      res.json(result);
    } catch (error) {
      console.error("Mark participant paid error:", error);
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router44.get("/user/my-groups", authenticateToken, async (req, res) => {
  try {
    const result = await GroupOrderService.getMyGroups(req.user.id);
    res.json(result);
  } catch (error) {
    console.error("Get user group orders error:", error);
    res.status(500).json({ success: false, error: error.message });
  }
});
var groupOrders_default = router44;

// server/routes/referrals.ts
var import_express45 = require("express");
init_referralService();
var router45 = (0, import_express45.Router)();
router45.get("/my-code", authenticateToken, async (req, res) => {
  try {
    const referralCode = await ReferralService.getOrCreateReferralCode(
      req.user.id
    );
    res.json({
      success: true,
      referralCode,
      shareLink: `https://app.comeya.es?ref=${referralCode}`
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router45.get("/summary", authenticateToken, async (req, res) => {
  try {
    const summary = await ReferralService.getReferralSummary(req.user.id);
    res.json({ success: true, ...summary });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var referrals_default = router45;

// server/routes/orderChat.ts
var import_express46 = __toESM(require("express"));
var import_drizzle_orm70 = require("drizzle-orm");
var router46 = import_express46.default.Router();
router46.get("/:orderId/chat", authenticateToken, async (req, res) => {
  try {
    const { orderId } = req.params;
    const { orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm70.eq)(orders2.id, orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    const isCustomer = order.userId === req.user.id;
    const isDriver = order.deliveryPersonId === req.user.id;
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    if (!isCustomer && !isDriver && !isAdmin) {
      return res.status(403).json({ error: "No autorizado" });
    }
    const chatMessages = order.chatMessages ? JSON.parse(order.chatMessages) : [];
    res.json({
      success: true,
      messages: chatMessages,
      orderId,
      order: {
        id: order.id,
        status: order.status,
        userId: order.userId,
        deliveryPersonId: order.deliveryPersonId,
        businessId: order.businessId
      }
    });
  } catch (error) {
    console.error("Get chat error:", error);
    res.status(500).json({ error: error.message });
  }
});
router46.post("/:orderId/chat", authenticateToken, async (req, res) => {
  try {
    const { orderId } = req.params;
    const { message, receiverId } = req.body;
    if (!message || !message.trim()) {
      return res.status(400).json({ error: "Mensaje vac\xEDo" });
    }
    const { orders: orders2, users: users5 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm70.eq)(orders2.id, orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    const isCustomer = order.userId === req.user.id;
    const isDriver = order.deliveryPersonId === req.user.id;
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    if (!isCustomer && !isDriver && !isAdmin) {
      return res.status(403).json({ error: "No autorizado" });
    }
    const [sender] = await db2.select().from(users5).where((0, import_drizzle_orm70.eq)(users5.id, req.user.id)).limit(1);
    const newMessage = {
      id: `msg-${Date.now()}`,
      orderId,
      senderId: req.user.id,
      senderName: sender?.name || "Usuario",
      receiverId: receiverId || (isCustomer ? order.deliveryPersonId : order.userId),
      message: message.trim(),
      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
      isRead: false
    };
    const chatMessages = order.chatMessages ? JSON.parse(order.chatMessages) : [];
    chatMessages.push(newMessage);
    await db2.update(orders2).set({
      chatMessages: JSON.stringify(chatMessages),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm70.eq)(orders2.id, orderId));
    try {
      const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
      const receiverName = sender?.name || "Cliente";
      const roleText = isDriver ? "Repartidor" : "Cliente";
      await sendPushToUser2(newMessage.receiverId, {
        title: `\u{1F4AC} Nuevo mensaje de ${roleText}`,
        body: message.substring(0, 50),
        data: { orderId, screen: "OrderChat" }
      });
    } catch (e) {
      console.error("Error sending push notification:", e);
    }
    res.json({
      success: true,
      message: newMessage,
      messages: chatMessages
    });
  } catch (error) {
    console.error("Send message error:", error);
    res.status(500).json({ error: error.message });
  }
});
router46.patch(
  "/:orderId/chat/:messageId/read",
  authenticateToken,
  async (req, res) => {
    try {
      const { orderId, messageId } = req.params;
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm70.eq)(orders2.id, orderId)).limit(1);
      if (!order) {
        return res.status(404).json({ error: "Pedido no encontrado" });
      }
      const chatMessages = order.chatMessages ? JSON.parse(order.chatMessages) : [];
      const messageIndex = chatMessages.findIndex(
        (m) => m.id === messageId
      );
      if (messageIndex === -1) {
        return res.status(404).json({ error: "Mensaje no encontrado" });
      }
      chatMessages[messageIndex].isRead = true;
      await db2.update(orders2).set({
        chatMessages: JSON.stringify(chatMessages),
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm70.eq)(orders2.id, orderId));
      res.json({ success: true, message: "Mensaje marcado como le\xEDdo" });
    } catch (error) {
      console.error("Mark read error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var orderChat_default = router46;

// server/routes/orders.ts
var import_express47 = __toESM(require("express"));
var import_drizzle_orm71 = require("drizzle-orm");
init_enhancedPushService();
init_websocket();
init_orderNumberService();
init_config();
var router47 = import_express47.default.Router();
router47.post("/test-ordertype", authenticateToken, async (req, res) => {
  const { orderType } = req.body;
  return res.json({
    received: orderType,
    type: typeof orderType,
    isPickup: orderType === "pickup",
    body: req.body
  });
});
router47.get("/", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const userOrders = await db2.select({
      order: orders2,
      business: {
        id: businesses3.id,
        name: businesses3.name,
        image: businesses3.image
      }
    }).from(orders2).leftJoin(businesses3, (0, import_drizzle_orm71.eq)(orders2.businessId, businesses3.id)).where((0, import_drizzle_orm71.eq)(orders2.userId, req.user.id)).orderBy((0, import_drizzle_orm71.desc)(orders2.createdAt));
    res.json({ success: true, orders: userOrders });
  } catch (error) {
    console.error("Get orders error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.get("/:id", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const orderId = req.params.id;
    const [order] = await db2.select({
      order: orders2,
      business: {
        id: businesses3.id,
        name: businesses3.name,
        image: businesses3.image,
        phone: businesses3.phone,
        address: businesses3.address
      }
    }).from(orders2).leftJoin(businesses3, (0, import_drizzle_orm71.eq)(orders2.businessId, businesses3.id)).where((0, import_drizzle_orm71.eq)(orders2.id, orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    if (order.order.userId !== req.user.id && req.user.role !== "admin" && req.user.role !== "delivery_driver") {
      return res.status(403).json({ error: "No autorizado" });
    }
    const searchingDriver = order.order.status === "confirmed" && !order.order.deliveryPersonId;
    res.json({
      success: true,
      order: {
        ...order.order,
        business: order.business,
        searchingDriver
      }
    });
  } catch (error) {
    console.error("Get order error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.post("/", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3, products: products3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const {
      businessId,
      businessName,
      businessImage,
      items: rawItems,
      deliveryAddress,
      deliveryAddressId,
      deliveryLatitude,
      deliveryLongitude,
      paymentMethod,
      notes,
      subtotal: clientSubtotal,
      productosBase,
      nemyCommission,
      deliveryFee: clientDeliveryFee,
      total: clientTotal,
      substitutionPreference,
      itemSubstitutionPreferences,
      substituteProductIds,
      cashPaymentAmount,
      cashChangeAmount,
      couponCode,
      couponDiscount,
      orderType
    } = req.body;
    const items = Array.isArray(rawItems) ? rawItems : typeof rawItems === "string" ? JSON.parse(rawItems) : [];
    if (!businessId || !items || items.length === 0) {
      return res.status(400).json({ error: "Datos del pedido incompletos" });
    }
    const [business] = await db2.select().from(businesses3).where((0, import_drizzle_orm71.and)((0, import_drizzle_orm71.eq)(businesses3.id, businessId), (0, import_drizzle_orm71.eq)(businesses3.isActive, true))).limit(1);
    if (!business) {
      return res.status(404).json({ error: "Negocio no encontrado" });
    }
    const productIds = items.map((item) => item.productId || item.product?.id).filter(Boolean);
    const orderProducts = await db2.select().from(products3).where((0, import_drizzle_orm71.inArray)(products3.id, productIds));
    let subtotal = 0;
    const validItems = [];
    for (const item of items) {
      const product = orderProducts.find(
        (p) => p.id === (item.productId || item.product?.id)
      );
      if (!product || !product.isAvailable) {
        return res.status(400).json({
          error: `Producto no disponible: ${item.productId || item.product?.id}`
        });
      }
      const itemTotal = product.price * item.quantity;
      subtotal += itemTotal;
      validItems.push({
        productId: item.productId,
        quantity: item.quantity,
        price: product.price,
        total: itemTotal,
        notes: item.notes || null
      });
    }
    const deliveryFee = orderType === "pickup" ? 0 : clientDeliveryFee ?? business.deliveryFee ?? await CONFIG.deliveryFee();
    const finalSubtotal = clientSubtotal ?? subtotal;
    const nemyCommissionCalc = nemyCommission !== void 0 && nemyCommission !== null ? nemyCommission : Math.round(finalSubtotal * await CONFIG.commission());
    const validOrderType = typeof orderType === "string" && orderType.toLowerCase() === "pickup" ? "pickup" : "delivery";
    const total = clientTotal;
    const businessEarnings = productosBase ?? finalSubtotal;
    let finalDeliveryLat = deliveryLatitude || null;
    let finalDeliveryLng = deliveryLongitude || null;
    if ((!finalDeliveryLat || !finalDeliveryLng) && deliveryAddress) {
      try {
        const addrObj = typeof deliveryAddress === "string" ? JSON.parse(deliveryAddress) : deliveryAddress;
        if (addrObj?.latitude) finalDeliveryLat = String(addrObj.latitude);
        if (addrObj?.longitude) finalDeliveryLng = String(addrObj.longitude);
      } catch {
      }
    }
    if ((!finalDeliveryLat || !finalDeliveryLng) && deliveryAddress) {
      try {
        const addrText = typeof deliveryAddress === "string" ? deliveryAddress : (deliveryAddress?.street || "") + ", Soria, Espa\xF1a";
        const query = addrText.includes("Soria") ? addrText : addrText + ", Soria, Espa\xF1a";
        const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
        const geo = await googleMapsService2.geocodeAddress(query);
        if (geo) {
          finalDeliveryLat = String(geo.lat);
          finalDeliveryLng = String(geo.lng);
        }
      } catch {
      }
    }
    const orderId = crypto.randomUUID();
    const { nextOrderNumber: nextOrderNumber2 } = await Promise.resolve().then(() => (init_orderNumberService(), orderNumberService_exports));
    const seqNumber = await nextOrderNumber2();
    const newOrder = {
      id: orderId,
      ...seqNumber != null ? { orderNumber: seqNumber } : {},
      userId: req.user.id,
      businessId,
      businessName: businessName || business.name,
      businessImage: businessImage || business.image || "",
      items: JSON.stringify(validItems),
      status: "pending",
      subtotal: finalSubtotal,
      productosBase: productosBase ?? finalSubtotal,
      nemyCommission: nemyCommissionCalc,
      deliveryFee,
      total,
      paymentMethod: paymentMethod || "cash",
      orderType: validOrderType,
      deliveryAddress: deliveryAddress || "",
      deliveryLatitude: finalDeliveryLat,
      deliveryLongitude: finalDeliveryLng,
      notes: notes || null,
      substitutionPreference: substitutionPreference || "refund",
      itemSubstitutionPreferences: itemSubstitutionPreferences || null,
      substituteProductIds: substituteProductIds || null,
      cashPaymentAmount: cashPaymentAmount || null,
      cashChangeAmount: cashChangeAmount || null,
      businessEarnings
    };
    await db2.insert(orders2).values(newOrder);
    if (orderType === "pickup") {
      const { pickupService: pickupService2 } = await Promise.resolve().then(() => (init_pickupService(), pickupService_exports));
      const estimatedMinutes = 20;
      await pickupService2.createPickupOrder(orderId, estimatedMinutes);
    }
    if (business.ownerId) {
      await sendPushToUser(business.ownerId, {
        title: "\u{1F6D2} Nuevo pedido recibido",
        body: `Pedido ${await orderRefFromId(orderId)} \u2014 \u20AC${(total / 100).toFixed(2)}`,
        data: { orderId, screen: "BusinessOrders" }
      });
    }
    notifyNewOrder(businessId, {
      id: orderId,
      businessName: business.name,
      total,
      items: validItems
    });
    res.json({
      success: true,
      orderId,
      order: newOrder
    });
  } catch (error) {
    console.error("Create order error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.post("/:id/confirm", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const confirmId = req.params.id;
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm71.eq)(orders2.id, confirmId)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    if (order.userId !== req.user.id && req.user.role !== "admin")
      return res.status(403).json({ error: "No autorizado" });
    const earlyStatuses = ["pending", "confirmed", "accepted"];
    if (!earlyStatuses.includes(order.status)) {
      return res.json({
        success: true,
        message: "El pedido ya avanz\xF3 de fase; no se requiere confirmaci\xF3n",
        status: order.status
      });
    }
    await db2.update(orders2).set({
      status: "accepted",
      confirmedToBusinessAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm71.eq)(orders2.id, confirmId));
    await sendOrderStatusNotification(confirmId, order.userId, "accepted");
    res.json({ success: true, message: "Pedido confirmado" });
  } catch (error) {
    console.error("Confirm order error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.post("/:id/cancel-regret", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const regretId = req.params.id;
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm71.eq)(orders2.id, regretId)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    if (order.userId !== req.user.id && req.user.role !== "admin")
      return res.status(403).json({ error: "No autorizado" });
    if (order.status !== "pending")
      return res.status(400).json({ error: "Solo se puede cancelar un pedido pendiente" });
    await db2.update(orders2).set({
      status: "cancelled",
      cancelledAt: /* @__PURE__ */ new Date(),
      cancelledBy: req.user.id,
      cancellationReason: "regret_period",
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm71.eq)(orders2.id, regretId));
    res.json({ success: true, message: "Pedido cancelado sin penalizaci\xF3n" });
  } catch (error) {
    console.error("Cancel regret error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.patch("/:id/status", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { status } = req.body;
    if (!status) {
      return res.status(400).json({ error: "Estado requerido" });
    }
    const validStatuses = [
      "pending",
      "accepted",
      "preparing",
      "ready",
      "assigned_driver",
      "picked_up",
      "on_the_way",
      "in_transit",
      "arriving",
      "delivered",
      "cancelled",
      "refunded"
    ];
    if (!validStatuses.includes(status)) {
      return res.status(400).json({ error: "Estado inv\xE1lido" });
    }
    const statusId = req.params.id;
    const [order] = await db2.select({
      order: orders2,
      business: businesses3
    }).from(orders2).leftJoin(businesses3, (0, import_drizzle_orm71.eq)(orders2.businessId, businesses3.id)).where((0, import_drizzle_orm71.eq)(orders2.id, statusId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    const isPickup = order.order.orderType === "pickup";
    if (isPickup && [
      "assigned_driver",
      "picked_up",
      "on_the_way",
      "in_transit",
      "arriving"
    ].includes(status)) {
      return res.status(400).json({
        error: "Un pedido de recogida no puede pasar a estados de reparto"
      });
    }
    const canUpdate = req.user.role === "admin" || req.user.role === "business_owner" && order.business?.ownerId === req.user.id || req.user.role === "delivery_driver" && ["picked_up", "on_the_way", "in_transit", "arriving"].includes(status);
    if (!canUpdate) {
      return res.status(403).json({ error: "No autorizado" });
    }
    if (status === "accepted" && order.order.status === "pending") {
      const { paymentStateOf: paymentStateOf2, acceptanceBlockedMessage: acceptanceBlockedMessage2 } = await Promise.resolve().then(() => (init_paymentState(), paymentState_exports));
      let paymentState = await paymentStateOf2(order.order);
      if (paymentState === "awaiting_payment") {
        const { rescueStripePayment: rescueStripePayment3 } = await Promise.resolve().then(() => (init_paymentConfirmationService(), paymentConfirmationService_exports));
        if (await rescueStripePayment3(order.order)) paymentState = "paid";
      }
      const blocked = acceptanceBlockedMessage2(paymentState);
      if (blocked) {
        return res.status(400).json({ error: blocked });
      }
    }
    await db2.update(orders2).set({
      status,
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm71.eq)(orders2.id, statusId));
    const o = order.order;
    const isPickupOrder = o.orderType === "pickup";
    if (status === "preparing") {
      await sendOrderStatusNotification(statusId, o.userId, "preparing");
    } else if (status === "ready") {
      await sendOrderStatusNotification(statusId, o.userId, "ready");
      if (o.deliveryPersonId) {
        await sendPushToUser(o.deliveryPersonId, {
          title: "\u{1F4E6} Pedido listo para recoger",
          body: `${o.businessName} \u2014 Pedido ${orderRef(o)} listo`,
          data: { orderId: o.id, screen: "DriverActiveOrder" }
        });
      } else if (!isPickupOrder) {
        try {
          const { DeliveryNotificationService: DeliveryNotificationService2 } = await Promise.resolve().then(() => (init_deliveryNotificationService(), deliveryNotificationService_exports));
          const notified = await DeliveryNotificationService2.broadcastToDrivers(
            "\u{1F6F5} \xA1Nuevo pedido disponible!",
            `${o.businessName} \u2014 Recoge en ${o.businessName || "negocio"} y gana \u20AC${((o.deliveryFee || 0) / 100).toFixed(2)}`,
            { orderId: o.id, screen: "DriverAvailable" }
          );
          console.log(`\u{1F4E2} Broadcast: nuevo pedido ${orderRef(o)} notificado a ${notified} drivers`);
        } catch (err) {
          console.error("Error broadcasting new order to drivers:", err);
        }
      }
    } else if (status === "cancelled") {
      await sendOrderStatusNotification(statusId, o.userId, "cancelled");
      if (order.business?.ownerId) {
        await sendPushToUser(order.business.ownerId, {
          title: "\u274C Pedido cancelado",
          body: `Pedido ${orderRef(o)} fue cancelado`,
          data: { orderId: o.id, screen: "BusinessOrders" }
        });
      }
      if (o.deliveryPersonId) {
        await sendPushToUser(o.deliveryPersonId, {
          title: "\u274C Pedido cancelado",
          body: `El pedido ${orderRef(o)} fue cancelado`,
          data: { orderId: o.id, screen: "DriverAvailable" }
        });
      }
    }
    res.json({ success: true, message: "Estado actualizado" });
  } catch (error) {
    console.error("Update order status error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.post("/:id/tip", authenticateToken, async (req, res) => {
  try {
    if (req.user.role !== "customer") {
      return res.status(403).json({ error: "Solo el cliente puede enviar propinas" });
    }
    const { orders: orders2, wallets: wallets2, transactions: transactions4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { amount, deliveryPersonId } = req.body;
    if (!amount || amount <= 0) {
      return res.status(400).json({ error: "Monto de propina inv\xE1lido" });
    }
    const tipOrderId = req.params.id;
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm71.eq)(orders2.id, tipOrderId)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    if (order.userId !== req.user.id)
      return res.status(403).json({ error: "No autorizado" });
    if (order.status !== "delivered")
      return res.status(400).json({ error: "El pedido debe estar entregado" });
    const driverId = deliveryPersonId || order.deliveryPersonId;
    if (!driverId)
      return res.status(400).json({ error: "No hay repartidor asignado" });
    const [driverWallet] = await db2.select().from(wallets2).where((0, import_drizzle_orm71.eq)(wallets2.userId, driverId)).limit(1);
    if (driverWallet) {
      await db2.update(wallets2).set({
        balance: driverWallet.balance + amount,
        totalEarned: driverWallet.totalEarned + amount
      }).where((0, import_drizzle_orm71.eq)(wallets2.userId, driverId));
    }
    await db2.insert(transactions4).values({
      userId: driverId,
      orderId: tipOrderId,
      type: "tip",
      amount,
      description: `Propina del cliente por pedido ${await orderRefFromId(tipOrderId)}`,
      status: "completed"
    });
    res.json({ success: true, message: "Propina enviada" });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router47.post("/:id/mark-picked-up", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const pickupId = req.params.id;
    const [order] = await db2.select({ order: orders2, business: businesses3 }).from(orders2).leftJoin(businesses3, (0, import_drizzle_orm71.eq)(orders2.businessId, businesses3.id)).where((0, import_drizzle_orm71.eq)(orders2.id, pickupId)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    const canMark = req.user.role === "admin" || req.user.role === "business_owner" && order.business?.ownerId === req.user.id;
    if (!canMark) {
      return res.status(403).json({ error: "No autorizado" });
    }
    if (order.order.orderType !== "pickup") {
      return res.status(400).json({ error: "Solo para pedidos de tipo pickup" });
    }
    const pickupTerminalStatuses = ["ready", "on_the_way", "picked_up"];
    if (!pickupTerminalStatuses.includes(order.order.status)) {
      return res.status(400).json({ error: "El pedido debe estar en estado 'listo'" });
    }
    const { code } = req.body;
    if (typeof code !== "string" || code.trim().length !== 6) {
      return res.status(400).json({ error: "C\xF3digo de recogida requerido (6 d\xEDgitos)" });
    }
    const { pickupService: pickupService2 } = await Promise.resolve().then(() => (init_pickupService(), pickupService_exports));
    await pickupService2.ensurePickupCodes(order.order);
    const codeValid = await pickupService2.validatePickupCode(
      pickupId,
      code.trim()
    );
    if (!codeValid) {
      return res.status(400).json({ error: "C\xF3digo de recogida inv\xE1lido" });
    }
    await db2.update(orders2).set({
      status: "delivered",
      deliveredAt: /* @__PURE__ */ new Date(),
      confirmedByCustomer: true,
      confirmedByCustomerAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm71.eq)(orders2.id, pickupId));
    await sendPushToUser(order.order.userId, {
      title: "\u{1F6CD}\uFE0F \xA1Pedido recogido!",
      body: `Gracias por recoger tu pedido en ${order.business?.name || order.order.businessName || "el negocio"}. \xA1Que lo disfrutes!`,
      data: {
        orderId: pickupId,
        screen: "OrderTracking",
        reviewDelayMs: 60 * 60 * 1e3
        // el cliente programa el recordatorio
      }
    });
    const { fundReleaseService: fundReleaseService2 } = await Promise.resolve().then(() => (init_fundReleaseService(), fundReleaseService_exports));
    await fundReleaseService2.releaseOrderFunds(pickupId);
    res.json({ success: true, message: "Pedido marcado como recogido" });
  } catch (error) {
    console.error("Mark picked up error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.put("/:id/complete", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const completeId = req.params.id;
    const [order] = await db2.select({ order: orders2, business: businesses3 }).from(orders2).leftJoin(businesses3, (0, import_drizzle_orm71.eq)(orders2.businessId, businesses3.id)).where((0, import_drizzle_orm71.eq)(orders2.id, completeId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    const canComplete = req.user.role === "admin" || req.user.role === "business_owner" && order.business?.ownerId === req.user.id;
    if (!canComplete) {
      return res.status(403).json({ error: "No autorizado" });
    }
    const validStatuses = [
      "ready",
      "picked_up",
      "on_the_way",
      "in_transit",
      "arriving"
    ];
    if (!validStatuses.includes(order.order.status)) {
      return res.status(400).json({
        error: `No se puede completar un pedido en estado '${order.order.status}'`
      });
    }
    await db2.update(orders2).set({
      status: "delivered",
      deliveredAt: /* @__PURE__ */ new Date(),
      confirmedByCustomer: true,
      confirmedByCustomerAt: /* @__PURE__ */ new Date(),
      fundsReleased: true,
      fundsReleasedAt: /* @__PURE__ */ new Date(),
      updatedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm71.eq)(orders2.id, completeId));
    await sendOrderStatusNotification(
      completeId,
      order.order.userId,
      "delivered"
    );
    try {
      const { createPayoutsForOrder: createPayoutsForOrder2 } = await Promise.resolve().then(() => (init_payoutService(), payoutService_exports));
      await createPayoutsForOrder2(completeId);
    } catch (e) {
      console.error(`Error creating payouts for order ${req.params.id}:`, e);
    }
    res.json({
      success: true,
      message: "Pedido completado exitosamente",
      orderId: order.order.id
    });
  } catch (error) {
    console.error("Complete order error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.patch("/:id/cancel", authenticateToken, async (req, res) => {
  try {
    const { cancelOrder: cancelOrder2 } = await Promise.resolve().then(() => (init_orderCancellationService(), orderCancellationService_exports));
    const cancelId = req.params.id;
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    const result = await cancelOrder2(
      cancelId,
      req.user.id,
      req.body?.reason || "Cancelado por el usuario",
      {
        actorRole: isAdmin ? req.user.role : void 0,
        refundOverride: isAdmin ? req.body?.refundAmount : void 0,
        liableParty: isAdmin ? req.body?.liableParty : void 0
      }
    );
    if (!result.success) {
      const code = result.error === "not_found" ? 404 : result.error === "forbidden" ? 403 : 400;
      return res.status(code).json({ error: result.message });
    }
    res.json({
      success: true,
      message: "Pedido cancelado",
      refund: result.refund,
      policy: result.policy
    });
  } catch (error) {
    console.error("Cancel order error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.post("/:id/report-issue", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, orderIssues: orderIssues2, supportTickets: supportTickets2, ticketMessages: ticketMessages2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { randomUUID: randomUUID5 } = await import("crypto");
    const { issueType, description, priority, photos, affectedItems } = req.body;
    if (!issueType || !description) {
      return res.status(400).json({ error: "Tipo de problema y descripci\xF3n son obligatorios" });
    }
    const issueOrderId = req.params.id;
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm71.eq)(orders2.id, issueOrderId)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    const role = req.user.role;
    if (role === "customer" && order.userId !== req.user.id) {
      return res.status(403).json({ error: "No autorizado" });
    }
    if (role === "delivery_driver" && order.deliveryPersonId !== req.user.id) {
      return res.status(403).json({ error: "No autorizado" });
    }
    if (role === "business_owner") {
      const { businesses: businesses4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [biz] = await db2.select({ ownerId: businesses4.ownerId }).from(businesses4).where((0, import_drizzle_orm71.eq)(businesses4.id, order.businessId)).limit(1);
      if (biz?.ownerId !== req.user.id) {
        return res.status(403).json({ error: "No autorizado" });
      }
    }
    const photoList = Array.isArray(photos) ? photos.filter((p) => typeof p === "string" && p.length > 0) : [];
    const ticketId = randomUUID5();
    const subject = `[Pedido ${orderRef(order)}] ${ISSUE_LABELS[issueType] || issueType}`.slice(
      0,
      255
    );
    await db2.insert(supportTickets2).values({
      id: ticketId,
      userId: req.user.id,
      orderId: issueOrderId,
      subject,
      category: "order_issue",
      priority: priority || "medium",
      status: "open"
    });
    await db2.insert(ticketMessages2).values({
      ticketId,
      senderId: req.user.id,
      senderType: "user",
      message: description
    });
    const issueId = randomUUID5();
    await db2.insert(orderIssues2).values({
      id: issueId,
      orderId: issueOrderId,
      ticketId,
      reportedBy: req.user.id,
      reporterRole: role,
      issueType,
      description,
      photos: photoList.length > 0 ? JSON.stringify(photoList) : null,
      affectedItems: affectedItems ? JSON.stringify(affectedItems) : null,
      status: "open",
      priority: priority || "medium"
    });
    const { users: users5, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const admins = await db2.select({ id: users5.id }).from(users5).where((0, import_drizzle_orm71.inArray)(users5.role, ["admin", "super_admin"]));
    try {
      for (const admin of admins) {
        await sendPushToUser(admin.id, {
          title: "\u26A0\uFE0F Incidencia reportada",
          body: `Pedido ${orderRef(order)}: ${ISSUE_LABELS[issueType] || issueType}`,
          data: {
            type: "order_issue",
            issueId,
            orderId: issueOrderId,
            screen: "AdminDashboard",
            section: "support_issues"
          }
        });
      }
    } catch (err) {
      console.error("Error notifying admins of issue:", err);
    }
    try {
      const { notifyAdminNewIssue: notifyAdminNewIssue2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      notifyAdminNewIssue2({
        issueId,
        orderId: issueOrderId,
        issueType,
        priority: priority || "medium"
      });
    } catch (err) {
      console.error("Error emitting admin issue event:", err);
    }
    try {
      const [biz] = await db2.select({ ownerId: businesses3.ownerId }).from(businesses3).where((0, import_drizzle_orm71.eq)(businesses3.id, order.businessId)).limit(1);
      if (biz?.ownerId && biz.ownerId !== req.user.id) {
        await sendPushToUser(biz.ownerId, {
          title: "\u26A0\uFE0F Incidencia en tu pedido",
          body: `Pedido ${orderRef(order)}: ${ISSUE_LABELS[issueType] || issueType}`,
          data: {
            orderId: issueOrderId,
            screen: "BusinessOrders",
            type: "order_issue"
          }
        });
      }
    } catch (err) {
      console.error("Error notifying business of issue:", err);
    }
    res.json({
      success: true,
      message: "Problema reportado. Nuestro equipo lo revisar\xE1 en breve.",
      issueId,
      ticketId
    });
  } catch (error) {
    console.error("Report issue error:", error);
    res.status(500).json({ error: error.message });
  }
});
router47.get("/:id/issues", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, orderIssues: orderIssues2, refunds: refunds2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const orderId = req.params.id;
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm71.eq)(orders2.id, orderId)).limit(1);
    if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
    const isAdmin = req.user.role === "admin" || req.user.role === "super_admin";
    if (!isAdmin && order.userId !== req.user.id) {
      return res.status(403).json({ error: "No autorizado" });
    }
    const issues = await db2.select().from(orderIssues2).where((0, import_drizzle_orm71.eq)(orderIssues2.orderId, orderId)).orderBy((0, import_drizzle_orm71.desc)(orderIssues2.createdAt));
    const orderRefunds = await db2.select().from(refunds2).where((0, import_drizzle_orm71.eq)(refunds2.orderId, orderId)).orderBy((0, import_drizzle_orm71.desc)(refunds2.createdAt));
    res.json({
      success: true,
      issues: issues.map((i) => ({
        ...i,
        photos: i.photos ? JSON.parse(i.photos) : [],
        affectedItems: i.affectedItems ? JSON.parse(i.affectedItems) : null,
        // La nota interna no sale del panel admin
        internalNote: isAdmin ? i.internalNote : void 0
      })),
      refunds: orderRefunds
    });
  } catch (error) {
    console.error("Get order issues error:", error);
    res.status(500).json({ error: error.message });
  }
});
var orders_default = router47;

// server/routes/addressRoutes.ts
var import_express48 = require("express");
init_db();
init_coordinates();
init_schema_mysql();
var import_drizzle_orm72 = require("drizzle-orm");
var import_crypto4 = __toESM(require("crypto"));
var router48 = (0, import_express48.Router)();
router48.get("/", authenticateToken, async (req, res) => {
  try {
    const list = await db.select().from(addresses).where((0, import_drizzle_orm72.eq)(addresses.userId, req.user.id));
    res.json({ success: true, addresses: list });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
async function geocodeAddress3(street, city) {
  const base = `${street}, ${city || "Soria"}`;
  const query = base.toLowerCase().includes("soria") ? base : `${base}, Soria, Espa\xF1a`;
  const { googleMapsService: googleMapsService2 } = await Promise.resolve().then(() => (init_googleMapsService(), googleMapsService_exports));
  const result = await googleMapsService2.geocodeAddress(query);
  return result ? { lat: String(result.lat), lng: String(result.lng) } : null;
}
router48.post("/", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const { label, street, city, state, zipCode, latitude, longitude, isDefault } = req.body;
    if (!label || !street) {
      return res.status(400).json({ error: "label y street son requeridos" });
    }
    if (isDefault) {
      await db.update(addresses).set({ isDefault: false }).where((0, import_drizzle_orm72.eq)(addresses.userId, userId));
    }
    let finalLat = latitude !== void 0 && latitude !== null && latitude !== "" ? String(latitude) : null;
    let finalLng = longitude !== void 0 && longitude !== null && longitude !== "" ? String(longitude) : null;
    if (finalLat && finalLng && !isValidLatLng(finalLat, finalLng)) {
      return res.status(400).json({ error: "Coordenadas inv\xE1lidas" });
    }
    if (!finalLat || !finalLng) {
      const geo = await geocodeAddress3(street, city || "");
      if (geo) {
        finalLat = geo.lat;
        finalLng = geo.lng;
      }
    }
    if (!finalLat || !finalLng) {
      return res.status(400).json({
        error: "No hemos podido ubicar la direcci\xF3n en el mapa. Elige el punto en el mapa o revisa la direcci\xF3n."
      });
    }
    const id = import_crypto4.default.randomUUID();
    await db.insert(addresses).values({
      id,
      userId,
      label,
      street,
      city: city || "",
      state: state || "",
      zipCode: zipCode || null,
      latitude: finalLat,
      longitude: finalLng,
      isDefault: isDefault || false
    });
    const [saved] = await db.select().from(addresses).where((0, import_drizzle_orm72.eq)(addresses.id, id)).limit(1);
    res.json({ success: true, address: saved });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router48.patch(
  "/:id/default",
  authenticateToken,
  async (req, res) => {
    try {
      const userId = req.user.id;
      const [existing] = await db.select().from(addresses).where((0, import_drizzle_orm72.eq)(addresses.id, req.params.id)).limit(1);
      if (!existing) {
        return res.status(404).json({ error: "Direcci\xF3n no encontrada" });
      }
      if (existing.userId !== userId) {
        return res.status(403).json({ error: "No autorizado" });
      }
      await db.update(addresses).set({ isDefault: false }).where((0, import_drizzle_orm72.eq)(addresses.userId, userId));
      await db.update(addresses).set({ isDefault: true }).where((0, import_drizzle_orm72.eq)(addresses.id, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router48.delete("/:id", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const [existing] = await db.select().from(addresses).where((0, import_drizzle_orm72.eq)(addresses.id, req.params.id)).limit(1);
    if (!existing) {
      return res.status(404).json({ error: "Direcci\xF3n no encontrada" });
    }
    if (existing.userId !== userId) {
      return res.status(403).json({ error: "No autorizado" });
    }
    await db.delete(addresses).where((0, import_drizzle_orm72.eq)(addresses.id, req.params.id));
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var addressRoutes_default = router48;

// server/routes/deliveryRequestRoutes.ts
var import_express49 = require("express");
init_db();
init_schema_mysql();
var import_drizzle_orm73 = require("drizzle-orm");
init_enhancedPushService();
var import_crypto5 = __toESM(require("crypto"));
var router49 = (0, import_express49.Router)();
var FLAT_FEE = 350;
router49.post(
  "/",
  authenticateToken,
  requireRole("business_owner", "admin", "super_admin"),
  async (req, res) => {
    try {
      const {
        businessId,
        pickupAddress,
        dropoffAddress,
        pickupLatitude,
        pickupLongitude,
        dropoffLatitude,
        dropoffLongitude,
        contactPhone,
        notes
      } = req.body;
      if (!businessId || !pickupAddress || !dropoffAddress) {
        return res.status(400).json({
          error: "businessId, pickupAddress y dropoffAddress son requeridos"
        });
      }
      const [business] = await db.select().from(businesses).where((0, import_drizzle_orm73.eq)(businesses.id, businessId)).limit(1);
      if (!business) {
        return res.status(404).json({ error: "Negocio no encontrado" });
      }
      if (business.ownerId !== req.user.id && req.user.role !== "admin" && req.user.role !== "super_admin") {
        return res.status(403).json({ error: "No autorizado" });
      }
      const id = import_crypto5.default.randomUUID();
      await db.insert(deliveryRequests).values({
        id,
        businessId,
        businessName: business.name,
        pickupAddress,
        pickupLatitude: pickupLatitude || null,
        pickupLongitude: pickupLongitude || null,
        dropoffAddress,
        dropoffLatitude: dropoffLatitude || null,
        dropoffLongitude: dropoffLongitude || null,
        contactPhone: contactPhone || null,
        fee: FLAT_FEE,
        notes: notes || null,
        status: "pending"
      });
      try {
        const { DeliveryNotificationService: DeliveryNotificationService2 } = await Promise.resolve().then(() => (init_deliveryNotificationService(), deliveryNotificationService_exports));
        await DeliveryNotificationService2.broadcastToDrivers(
          "\u{1F4E6} Recogida para comercio",
          `${business.name} solicita un repartidor: ${pickupAddress} \u2192 ${dropoffAddress} (3,50 \u20AC)`,
          { screen: "DriverAvailable" }
        );
      } catch {
      }
      res.json({ success: true, requestId: id });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router49.get("/driver-mine", authenticateToken, async (req, res) => {
  try {
    const list = await db.select().from(deliveryRequests).where((0, import_drizzle_orm73.eq)(deliveryRequests.driverId, req.user.id)).orderBy((0, import_drizzle_orm73.desc)(deliveryRequests.createdAt)).limit(50);
    res.json({ success: true, requests: list });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router49.get("/available", authenticateToken, async (req, res) => {
  try {
    const list = await db.select().from(deliveryRequests).where(
      (0, import_drizzle_orm73.and)(
        (0, import_drizzle_orm73.eq)(deliveryRequests.status, "pending"),
        (0, import_drizzle_orm73.isNull)(deliveryRequests.driverId)
      )
    ).orderBy((0, import_drizzle_orm73.desc)(deliveryRequests.createdAt)).limit(50);
    res.json({ success: true, requests: list });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router49.get("/mine", authenticateToken, async (req, res) => {
  try {
    const { businesses: bTable } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const ownerBusinesses = await db.select({ id: bTable.id }).from(bTable).where((0, import_drizzle_orm73.eq)(bTable.ownerId, req.user.id));
    const ids = ownerBusinesses.map((b) => b.id);
    if (!ids.length) return res.json({ success: true, requests: [] });
    const { inArray: inArray12 } = await import("drizzle-orm");
    const list = await db.select().from(deliveryRequests).where(inArray12(deliveryRequests.businessId, ids)).orderBy((0, import_drizzle_orm73.desc)(deliveryRequests.createdAt)).limit(50);
    res.json({ success: true, requests: list });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router49.post("/:id/accept", authenticateToken, async (req, res) => {
  try {
    const role = req.user.role;
    if (role !== "delivery_driver" && role !== "admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    const [request] = await db.select().from(deliveryRequests).where((0, import_drizzle_orm73.eq)(deliveryRequests.id, req.params.id)).limit(1);
    if (!request) return res.status(404).json({ error: "Solicitud no encontrada" });
    if (request.status !== "pending" || request.driverId) {
      return res.status(400).json({ error: "Solicitud ya asignada" });
    }
    await db.update(deliveryRequests).set({
      status: "accepted",
      driverId: req.user.id,
      acceptedAt: /* @__PURE__ */ new Date()
    }).where((0, import_drizzle_orm73.eq)(deliveryRequests.id, req.params.id));
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router49.post("/:id/complete", authenticateToken, async (req, res) => {
  try {
    const [request] = await db.select().from(deliveryRequests).where((0, import_drizzle_orm73.eq)(deliveryRequests.id, req.params.id)).limit(1);
    if (!request) return res.status(404).json({ error: "Solicitud no encontrada" });
    if (request.driverId !== req.user.id && req.user.role !== "admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    await db.update(deliveryRequests).set({ status: "delivered", deliveredAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm73.eq)(deliveryRequests.id, req.params.id));
    try {
      const { wallets: wallets2, transactions: transactions4 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const driverId = request.driverId;
      const [wallet] = await db.select().from(wallets2).where((0, import_drizzle_orm73.eq)(wallets2.userId, driverId)).limit(1);
      if (wallet) {
        await db.update(wallets2).set({
          balance: wallet.balance + request.fee,
          totalEarned: wallet.totalEarned + request.fee
        }).where((0, import_drizzle_orm73.eq)(wallets2.userId, driverId));
      }
      await db.insert(transactions4).values({
        id: import_crypto5.default.randomUUID(),
        userId: driverId,
        type: "logistics_delivery",
        amount: request.fee,
        status: "completed",
        description: `Entrega log\xEDstica #${request.id.slice(-6)} (Log\xEDstica Local)`
      });
      await sendPushToUser(driverId, {
        title: "\u2705 Entrega log\xEDstica completada",
        body: `Ganaste ${(request.fee / 100).toFixed(2)} \u20AC por la entrega de ${request.businessName}`,
        data: { screen: "DriverEarnings" }
      });
    } catch (err) {
      console.error("Error paying logistics fee:", err);
    }
    res.json({ success: true, fee: request.fee });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var deliveryRequestRoutes_default = router49;

// server/routes/stripePaymentRoutes.ts
var import_express50 = __toESM(require("express"));
init_db();
init_schema_mysql();
var import_drizzle_orm74 = require("drizzle-orm");
init_stripeClient();
var router50 = import_express50.default.Router();
function isMissingStripeCustomerError(error) {
  return error?.code === "resource_missing" || typeof error?.message === "string" && error.message.includes("No such customer");
}
async function loadUserForStripe(userId) {
  const { sql: sql23 } = await import("drizzle-orm");
  const [userRows] = await db.execute(
    sql23`SELECT id, name, stripe_customer_id FROM users WHERE id = ${userId} LIMIT 1`
  );
  return userRows[0];
}
async function createAndStoreStripeCustomer(userId, name) {
  const stripe2 = getStripe2();
  const customer = await stripe2.customers.create({
    metadata: { userId },
    ...name ? { name } : {}
  });
  const { sql: sql23 } = await import("drizzle-orm");
  await db.execute(
    sql23`UPDATE users SET stripe_customer_id = ${customer.id} WHERE id = ${userId}`
  );
  return customer.id;
}
async function withStripeCustomer(userId, op) {
  const user = await loadUserForStripe(userId);
  let customerId = user?.stripe_customer_id;
  if (!customerId) {
    customerId = await createAndStoreStripeCustomer(userId, user?.name);
  }
  try {
    return { result: await op(customerId), customerId };
  } catch (error) {
    if (isMissingStripeCustomerError(error)) {
      customerId = await createAndStoreStripeCustomer(userId, user?.name);
      return { result: await op(customerId), customerId };
    }
    throw error;
  }
}
function stripeErrorResponse(res, error, context) {
  console.error(`[stripe] ${context}`, {
    code: error?.code,
    type: error?.type,
    message: error?.message
  });
  res.status(500).json({
    error: "No se pudo procesar el pago. Int\xE9ntalo de nuevo en unos momentos."
  });
}
router50.get("/publishable-key", async (_req, res) => {
  try {
    if (!process.env.STRIPE_PUBLISHABLE_KEY) {
      return res.status(503).json({ error: "Stripe not configured" });
    }
    res.json({ publishableKey: process.env.STRIPE_PUBLISHABLE_KEY });
  } catch (error) {
    stripeErrorResponse(res, error, "publishable-key");
  }
});
router50.post("/create-payment-sheet", authenticateToken, async (req, res) => {
  try {
    if (!process.env.STRIPE_SECRET_KEY) {
      return res.status(503).json({ error: "Stripe no configurado" });
    }
    const {
      amount,
      businessId,
      orderId,
      giftCardId,
      isGiftCard,
      subtotal,
      deliveryFee
    } = req.body;
    if (!amount || amount <= 0) {
      return res.status(400).json({ error: "Datos de pago incompletos" });
    }
    if (isGiftCard && !giftCardId) {
      return res.status(400).json({ error: "Datos de gift card incompletos" });
    }
    if (!isGiftCard && !orderId) {
      return res.status(400).json({ error: "Datos de pago incompletos" });
    }
    if (!isGiftCard && !businessId) {
      return res.status(400).json({ error: "Datos de pago incompletos" });
    }
    const stripe2 = getStripe2();
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const subtotalCents = Math.round(subtotal || 0);
    const nemyCommission = Math.round(subtotalCents * 0.15);
    const amountCents = Math.round(amount);
    const { result: payment, customerId } = await withStripeCustomer(
      req.user.id,
      async (cid) => {
        const ephemeralKey = await stripe2.ephemeralKeys.create(
          { customer: cid },
          { apiVersion: "2024-06-20" }
        );
        const paymentIntent = await stripe2.paymentIntents.create({
          amount: amountCents,
          currency: "eur",
          customer: cid,
          automatic_payment_methods: { enabled: true },
          metadata: {
            userId: req.user.id,
            businessId: businessId || "",
            orderId: orderId || "",
            giftCardId: giftCardId || "",
            subtotal: subtotalCents.toString(),
            deliveryFee: (deliveryFee || 0).toString(),
            nemyCommission: nemyCommission.toString()
          }
        });
        return { ephemeralKeySecret: ephemeralKey.secret, paymentIntent };
      }
    );
    if (!isGiftCard && orderId) {
      await db.update(orders2).set({
        paymentIntentId: payment.paymentIntent.id,
        stripePaymentIntentId: payment.paymentIntent.id,
        productosBase: subtotalCents,
        nemyCommission,
        platformFee: nemyCommission,
        businessEarnings: subtotalCents,
        deliveryEarnings: deliveryFee || 0,
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm74.eq)(orders2.id, orderId));
    }
    res.json({
      paymentIntent: payment.paymentIntent.client_secret,
      ephemeralKey: payment.ephemeralKeySecret,
      customer: customerId,
      publishableKey: process.env.STRIPE_PUBLISHABLE_KEY
    });
  } catch (error) {
    stripeErrorResponse(res, error, "create-payment-sheet");
  }
});
router50.post("/create-payment-intent", authenticateToken, async (req, res) => {
  try {
    if (!process.env.STRIPE_SECRET_KEY || !process.env.STRIPE_PUBLISHABLE_KEY) {
      console.error("Stripe config missing", {
        hasSecret: !!process.env.STRIPE_SECRET_KEY,
        hasPublishable: !!process.env.STRIPE_PUBLISHABLE_KEY
      });
      return res.status(503).json({ message: "Stripe no est\xE1 configurado" });
    }
    const {
      amount,
      businessId,
      orderId,
      giftCardId,
      subtotal,
      deliveryFee,
      isGiftCard,
      isSubscription
    } = req.body;
    if (!amount || amount <= 0) {
      console.error("Invalid payment intent data", {
        amount,
        businessId,
        orderId,
        giftCardId,
        userId: req.user?.id
      });
      return res.status(400).json({ message: "Datos de pago incompletos" });
    }
    if (isGiftCard && !giftCardId) {
      return res.status(400).json({ message: "Datos de gift card incompletos" });
    }
    if (!isGiftCard && !isSubscription && !orderId) {
      return res.status(400).json({ message: "Datos de pago incompletos" });
    }
    if (!isGiftCard && !isSubscription && !businessId) {
      return res.status(400).json({ message: "Datos de pago incompletos" });
    }
    let business = null;
    if (businessId && !isGiftCard && !isSubscription) {
      const { businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [b] = await db.select().from(businesses3).where((0, import_drizzle_orm74.eq)(businesses3.id, businessId)).limit(1);
      if (!b) return res.status(404).json({ message: "Negocio no encontrado" });
      business = b;
    }
    const stripe2 = getStripe2();
    const amountInCents = Math.round(amount);
    const subtotalInCents = Math.round(subtotal || 0);
    const nemyCommission = Math.round(subtotalInCents * 0.15);
    const paymentIntent = await stripe2.paymentIntents.create({
      amount: amountInCents,
      currency: "eur",
      metadata: {
        userId: req.user.id,
        businessId: businessId || "",
        orderId: orderId || "",
        giftCardId: giftCardId || "",
        subtotal: subtotalInCents.toString(),
        deliveryFee: (deliveryFee || 0).toString(),
        nemyCommission: nemyCommission.toString()
      }
    });
    if (orderId && !isGiftCard && !isSubscription) {
      const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      await db.update(orders2).set({
        paymentIntentId: paymentIntent.id,
        stripePaymentIntentId: paymentIntent.id,
        productosBase: subtotalInCents,
        nemyCommission,
        platformFee: nemyCommission,
        businessEarnings: subtotalInCents,
        // Business gets 100% of products
        deliveryEarnings: deliveryFee || 0,
        // Delivery gets 100% of delivery fee
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm74.eq)(orders2.id, orderId));
    }
    res.json({ clientSecret: paymentIntent.client_secret });
  } catch (error) {
    console.error("Create payment intent error", {
      userId: req.user?.id,
      amount: req.body?.amount,
      code: error?.code,
      type: error?.type,
      message: error?.message
    });
    res.status(500).json({ message: "No se pudo crear el pago" });
  }
});
router50.get("/payment-method/:userId", authenticateToken, async (req, res) => {
  try {
    const [user] = await db.select().from(users).where((0, import_drizzle_orm74.eq)(users.id, req.params.userId)).limit(1);
    if (!user) {
      return res.status(404).json({ error: "User not found" });
    }
    if (user.role === "delivery_driver") {
      return res.json({ hasCard: false });
    }
    if (user.cardLast4 && user.cardBrand) {
      return res.json({
        hasCard: true,
        card: {
          last4: user.cardLast4,
          brand: user.cardBrand
        }
      });
    }
    res.json({ hasCard: false });
  } catch (error) {
    stripeErrorResponse(res, error, "payment-method");
  }
});
router50.post("/create-setup-intent", authenticateToken, async (req, res) => {
  try {
    if (!process.env.STRIPE_SECRET_KEY) {
      return res.status(503).json({ error: "Stripe not configured" });
    }
    const stripe2 = (await import("stripe")).default;
    const stripeClient = new stripe2(process.env.STRIPE_SECRET_KEY);
    const userId = req.body?.userId || req.user.id;
    const { result: setupIntent } = await withStripeCustomer(
      userId,
      async (cid) => stripeClient.setupIntents.create({
        customer: cid,
        payment_method_types: ["card"]
      })
    );
    res.json({ clientSecret: setupIntent.client_secret });
  } catch (error) {
    stripeErrorResponse(res, error, "create-setup-intent");
  }
});
router50.post("/save-payment-method", authenticateToken, async (req, res) => {
  try {
    if (!process.env.STRIPE_SECRET_KEY) {
      return res.status(503).json({ error: "Stripe not configured" });
    }
    const stripe2 = (await import("stripe")).default;
    const stripeClient = new stripe2(process.env.STRIPE_SECRET_KEY);
    const { userId, paymentMethodId } = req.body;
    const paymentMethod = await stripeClient.paymentMethods.retrieve(paymentMethodId);
    await db.update(users).set({
      stripePaymentMethodId: paymentMethodId,
      cardLast4: paymentMethod.card?.last4 || null,
      cardBrand: paymentMethod.card?.brand || null
    }).where((0, import_drizzle_orm74.eq)(users.id, userId));
    res.json({
      success: true,
      card: {
        last4: paymentMethod.card?.last4,
        brand: paymentMethod.card?.brand
      }
    });
  } catch (error) {
    stripeErrorResponse(res, error, "save-payment-method");
  }
});
router50.delete(
  "/payment-method/:userId",
  authenticateToken,
  async (req, res) => {
    try {
      await db.update(users).set({
        stripePaymentMethodId: null,
        cardLast4: null,
        cardBrand: null
      }).where((0, import_drizzle_orm74.eq)(users.id, req.params.userId));
      res.json({ success: true });
    } catch (error) {
      stripeErrorResponse(res, error, "delete-payment-method");
    }
  }
);
router50.post(
  "/confirm-delivery/:orderId",
  authenticateToken,
  async (req, res) => {
    try {
      if (!process.env.STRIPE_SECRET_KEY) {
        return res.status(503).json({ error: "Stripe not configured" });
      }
      const stripe2 = (await import("stripe")).default;
      const stripeClient = new stripe2(process.env.STRIPE_SECRET_KEY);
      const {
        orders: orders2,
        businesses: businesses3,
        users: usersTable
      } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [order] = await db.select().from(orders2).where((0, import_drizzle_orm74.eq)(orders2.id, req.params.orderId)).limit(1);
      if (!order) return res.status(404).json({ error: "Order not found" });
      if (order.userId !== req.user.id)
        return res.status(403).json({ error: "Not authorized" });
      if (order.status !== "delivered")
        return res.status(400).json({ error: "Order not delivered yet" });
      if (order.confirmedByCustomer)
        return res.status(400).json({ error: "Already confirmed" });
      if (!order.paymentIntentId)
        return res.status(400).json({ error: "No payment found" });
      const [business] = await db.select().from(businesses3).where((0, import_drizzle_orm74.eq)(businesses3.id, order.businessId)).limit(1);
      if (!business)
        return res.status(404).json({ error: "Business not found" });
      const hasStripeConnect = !!business.stripeAccountId;
      let driverAccount = null;
      if (order.deliveryPersonId) {
        const [driver] = await db.select().from(usersTable).where((0, import_drizzle_orm74.eq)(usersTable.id, order.deliveryPersonId)).limit(1);
        driverAccount = driver?.stripeAccountId;
      }
      const businessAmount = order.businessEarnings || order.productosBase || order.subtotal || 0;
      const deliveryAmount = order.deliveryEarnings || order.deliveryFee || 0;
      let businessTransferId = null;
      let driverTransferId = null;
      if (hasStripeConnect && order.paymentIntentId) {
        const paymentIntent = await stripeClient.paymentIntents.retrieve(
          order.paymentIntentId
        );
        const chargeId = paymentIntent.latest_charge;
        if (chargeId) {
          const businessTransfer = await stripeClient.transfers.create({
            amount: businessAmount,
            currency: "eur",
            destination: business.stripeAccountId,
            source_transaction: chargeId,
            metadata: { orderId: order.id, type: "business_payment" }
          });
          businessTransferId = businessTransfer.id;
          if (driverAccount && deliveryAmount > 0) {
            const driverTransfer = await stripeClient.transfers.create({
              amount: deliveryAmount,
              currency: "eur",
              destination: driverAccount,
              source_transaction: chargeId,
              metadata: { orderId: order.id, type: "delivery_payment" }
            });
            driverTransferId = driverTransfer.id;
          }
        }
      } else {
        const { createPayoutsForOrder: createPayoutsForOrder2 } = await Promise.resolve().then(() => (init_payoutService(), payoutService_exports));
        await createPayoutsForOrder2(order.id);
      }
      await db.update(orders2).set({
        confirmedByCustomer: true,
        confirmedByCustomerAt: /* @__PURE__ */ new Date(),
        fundsReleased: true,
        fundsReleasedAt: /* @__PURE__ */ new Date(),
        businessTransferId,
        driverTransferId,
        driverPaymentStatus: driverTransferId ? "completed" : "pending",
        driverPaidAt: driverTransferId ? /* @__PURE__ */ new Date() : null,
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm74.eq)(orders2.id, order.id));
      try {
        const { LoyaltyService: LoyaltyService2 } = await Promise.resolve().then(() => (init_loyaltyService(), loyaltyService_exports));
        await LoyaltyService2.awardPointsForOrder(
          order.userId,
          order.id,
          order.total
        );
        console.log(
          `\u2705 Puntos de lealtad awarded para orden ${order.id.slice(-6)}`
        );
      } catch (loyaltyError) {
        console.error("\u26A0\uFE0F Error agregando puntos de lealtad:", loyaltyError);
      }
      res.json({
        success: true,
        message: hasStripeConnect ? "Fondos transferidos automaticamente" : "Pedido confirmado. Pago pendiente de transferencia manual.",
        businessTransferId,
        driverTransferId,
        method: hasStripeConnect ? "stripe_connect" : "manual_payout"
      });
    } catch (error) {
      console.error("Confirm delivery error:", error);
      res.status(500).json({ error: "No se pudo confirmar la entrega. Int\xE9ntalo de nuevo." });
    }
  }
);
router50.post(
  "/create-subscription-payment-intent",
  authenticateToken,
  async (req, res) => {
    try {
      if (!process.env.STRIPE_SECRET_KEY) {
        return res.status(503).json({ error: "Stripe no configurado" });
      }
      const { subscriptionId, amount } = req.body;
      if (!subscriptionId || !amount || amount <= 0) {
        return res.status(400).json({ error: "subscriptionId y amount son requeridos" });
      }
      const { subscriptions: subscriptions2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const [sub] = await db.select().from(subscriptions2).where((0, import_drizzle_orm74.eq)(subscriptions2.id, subscriptionId)).limit(1);
      if (!sub || sub.userId !== req.user.id) {
        return res.status(404).json({ error: "Suscripci\xF3n no encontrada" });
      }
      const stripe2 = getStripe2();
      const paymentIntent = await stripe2.paymentIntents.create({
        amount: Math.round(amount),
        currency: "eur",
        automatic_payment_methods: { enabled: true },
        metadata: { userId: req.user.id, subscriptionId, plan: sub.plan }
      });
      res.json({ clientSecret: paymentIntent.client_secret });
    } catch (error) {
      stripeErrorResponse(res, error, "create-subscription-payment-intent");
    }
  }
);
router50.post(
  "/confirm-subscription/:subscriptionId",
  authenticateToken,
  async (req, res) => {
    try {
      const { subscriptionId } = req.params;
      const { subscriptions: subscriptions2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { sql: sql23 } = await import("drizzle-orm");
      let [sub] = await db.select().from(subscriptions2).where((0, import_drizzle_orm74.eq)(subscriptions2.id, subscriptionId)).limit(1);
      if (!sub || sub.userId !== req.user.id) {
        const [byUser] = await db.select().from(subscriptions2).where((0, import_drizzle_orm74.eq)(subscriptions2.userId, req.user.id)).limit(1);
        if (!byUser)
          return res.status(404).json({ error: "Suscripci\xF3n no encontrada" });
        sub = byUser;
      }
      const now = /* @__PURE__ */ new Date();
      const periodEnd = new Date(now);
      sub.billingCycle === "yearly" ? periodEnd.setFullYear(periodEnd.getFullYear() + 1) : periodEnd.setMonth(periodEnd.getMonth() + 1);
      await db.update(subscriptions2).set({
        status: "active",
        currentPeriodStart: now,
        currentPeriodEnd: periodEnd,
        autoRenew: true,
        cancelledAt: null
        // Limpiar cancelación previa si la hubiera
      }).where((0, import_drizzle_orm74.eq)(subscriptions2.userId, req.user.id));
      const { SubscriptionService: SubscriptionService2 } = await Promise.resolve().then(() => (init_subscriptionService(), subscriptionService_exports));
      await SubscriptionService2.applyPlanActivationSideEffects(
        sub.userId,
        sub.plan
      ).catch(() => {
      });
      try {
        const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
        await sendPushToUser2(sub.userId, {
          title: "\u2705 Suscripci\xF3n activada",
          body: `Tu plan ${sub.plan} ya est\xE1 activo. \xA1Disfruta los beneficios!`,
          data: { screen: "Subscriptions" }
        });
      } catch {
      }
      res.json({ success: true, plan: sub.plan });
    } catch (error) {
      stripeErrorResponse(res, error, "confirm-subscription");
    }
  }
);
router50.get("/cards", authenticateToken, async (req, res) => {
  try {
    const [user] = await db.select().from(users).where((0, import_drizzle_orm74.eq)(users.id, req.user.id)).limit(1);
    if (!user) return res.status(404).json({ error: "Usuario no encontrado" });
    if (!user.stripePaymentMethodId || !user.cardLast4) {
      return res.json({ cards: [] });
    }
    res.json({
      cards: [
        {
          id: user.stripePaymentMethodId,
          brand: user.cardBrand || "card",
          last4: user.cardLast4,
          expMonth: 12,
          expYear: 2026,
          isDefault: true
        }
      ]
    });
  } catch (error) {
    stripeErrorResponse(res, error, "cards");
  }
});
router50.get("/history", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { desc: desc19 } = await import("drizzle-orm");
    const userOrders = await db.select().from(orders2).where((0, import_drizzle_orm74.eq)(orders2.userId, req.user.id)).orderBy(desc19(orders2.createdAt)).limit(20);
    const payments2 = userOrders.filter((o) => o.stripePaymentIntentId || o.paymentMethod).map((o) => ({
      payment: {
        id: o.stripePaymentIntentId || o.id,
        amount: o.total || 0,
        method: o.paymentMethod || "card",
        status: ["delivered", "accepted", "confirmed"].includes(o.status) ? "completed" : "pending",
        createdAt: o.createdAt
      },
      order: {
        id: o.id,
        total: o.total || 0,
        status: o.status
      }
    }));
    res.json({ payments: payments2 });
  } catch (error) {
    stripeErrorResponse(res, error, "history");
  }
});
router50.put("/cards/:cardId/default", authenticateToken, async (req, res) => {
  res.json({ success: true });
});
router50.delete("/cards/:cardId", authenticateToken, async (req, res) => {
  try {
    await db.update(users).set({
      stripePaymentMethodId: null,
      cardLast4: null,
      cardBrand: null
    }).where((0, import_drizzle_orm74.eq)(users.id, req.user.id));
    res.json({ success: true });
  } catch (error) {
    stripeErrorResponse(res, error, "delete-card");
  }
});
var stripePaymentRoutes_default = router50;

// server/routes/stripeConnect.ts
var import_express51 = require("express");
init_db();
init_stripeClient();
var import_drizzle_orm75 = require("drizzle-orm");
init_logger();
var router51 = (0, import_express51.Router)();
async function getStripeAccountId(userId) {
  const [rows] = await db.execute(
    import_drizzle_orm75.sql`SELECT stripe_account_id FROM users WHERE id = ${userId} LIMIT 1`
  );
  return rows[0]?.stripe_account_id || null;
}
async function saveStripeAccountId(userId, accountId, role) {
  await db.execute(
    import_drizzle_orm75.sql`UPDATE users SET stripe_account_id = ${accountId} WHERE id = ${userId}`
  );
  if (role === "business_owner") {
    await db.execute(
      import_drizzle_orm75.sql`UPDATE businesses SET stripe_account_id = ${accountId}, stripe_account_status = 'pending' WHERE owner_id = ${userId}`
    );
  }
  if (role === "delivery_driver") {
    await db.execute(
      import_drizzle_orm75.sql`UPDATE delivery_drivers SET stripe_account_id = ${accountId}, stripe_account_status = 'pending' WHERE user_id = ${userId}`
    );
  }
}
async function markAccountActive(stripeAccountId) {
  await db.execute(
    import_drizzle_orm75.sql`UPDATE businesses SET stripe_account_status = 'active' WHERE stripe_account_id = ${stripeAccountId}`
  );
  await db.execute(
    import_drizzle_orm75.sql`UPDATE delivery_drivers SET stripe_account_status = 'active' WHERE stripe_account_id = ${stripeAccountId}`
  );
  logger.info(`\u2705 Stripe account marked active: ${stripeAccountId}`);
}
router51.post("/webhook", async (req, res) => {
  const sig = req.headers["stripe-signature"];
  const webhookSecret = process.env.STRIPE_WEBHOOK_SECRET;
  if (!webhookSecret) {
    logger.warn("STRIPE_WEBHOOK_SECRET not set, skipping verification");
    return res.json({ received: true });
  }
  let event;
  try {
    const stripe2 = getStripe2();
    event = stripe2.webhooks.constructEvent(req.body, sig, webhookSecret);
  } catch (err) {
    logger.error(`Webhook signature verification failed: ${err.message}`);
    return res.status(400).json({ error: `Webhook error: ${err.message}` });
  }
  try {
    switch (event.type) {
      case "account.updated":
      case "v2.core.account.updated":
      case "v2.core.account[configuration.recipient].capability_status_updated": {
        const account = event.data?.object || event.data;
        const accountId = account?.id || account?.account_id;
        if (!accountId) break;
        const stripe2 = getStripe2();
        const fullAccount = await stripe2.accounts.retrieve(accountId).catch(() => null);
        if (fullAccount?.payouts_enabled && fullAccount?.details_submitted) {
          await markAccountActive(accountId);
        }
        break;
      }
    }
  } catch (err) {
    logger.error(`Webhook handler error: ${err.message}`, {
      eventType: event.type
    });
  }
  res.json({ received: true });
});
router51.get("/status", authenticateToken, async (req, res) => {
  try {
    const stripe2 = getStripe2();
    const accountId = await getStripeAccountId(req.user.id);
    if (!accountId) {
      return res.json({
        hasAccount: false,
        onboardingComplete: false,
        canReceivePayments: false
      });
    }
    const account = await stripe2.accounts.retrieve(accountId);
    const { mapStripeRequirements: mapStripeRequirements2 } = await Promise.resolve().then(() => (init_stripeClient(), stripeClient_exports));
    res.json({
      hasAccount: true,
      accountId,
      onboardingComplete: account.details_submitted,
      canReceivePayments: account.charges_enabled && account.payouts_enabled,
      chargesEnabled: account.charges_enabled,
      payoutsEnabled: account.payouts_enabled,
      detailsSubmitted: account.details_submitted,
      requirements: mapStripeRequirements2(
        account.requirements?.currently_due || []
      )
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router51.post("/onboard", authenticateToken, async (req, res) => {
  try {
    if (!process.env.STRIPE_SECRET_KEY) {
      return res.status(503).json({ error: "Stripe no configurado" });
    }
    const stripe2 = getStripe2();
    let accountId = await getStripeAccountId(req.user.id);
    if (!accountId) {
      const [userRows] = await db.execute(
        import_drizzle_orm75.sql`SELECT name, email FROM users WHERE id = ${req.user.id} LIMIT 1`
      );
      const user = userRows[0];
      const account = await stripe2.accounts.create({
        type: "express",
        country: "ES",
        capabilities: { transfers: { requested: true } },
        business_type: req.user.role === "delivery_driver" ? "individual" : "company",
        ...user?.email && { email: user.email },
        metadata: { userId: req.user.id, role: req.user.role }
      });
      accountId = account.id;
      await saveStripeAccountId(req.user.id, accountId, req.user.role);
    }
    const baseUrl = process.env.EXPO_PUBLIC_BACKEND_URL || process.env.BACKEND_URL;
    const accountLink = await stripe2.accountLinks.create({
      account: accountId,
      refresh_url: `${baseUrl}/api/connect/refresh?accountId=${accountId}`,
      return_url: `${baseUrl}/api/connect/return?userId=${req.user.id}`,
      type: "account_onboarding"
    });
    res.json({ success: true, onboardingUrl: accountLink.url, accountId });
  } catch (error) {
    const rawMessage = error?.raw?.message || error?.message || "Error al conectar con Stripe";
    const friendly = String(rawMessage).split("\n")[0].trim();
    logger.error(`Stripe Connect onboard error: ${friendly}`);
    res.status(502).json({
      error: friendly,
      connectNotActivated: /signed up for Connect/i.test(String(rawMessage))
    });
  }
});
router51.post("/refresh-onboarding", authenticateToken, async (req, res) => {
  try {
    const stripe2 = getStripe2();
    const { accountId } = req.body;
    if (!accountId)
      return res.status(400).json({ error: "accountId requerido" });
    const baseUrl = process.env.EXPO_PUBLIC_BACKEND_URL || process.env.BACKEND_URL;
    const accountLink = await stripe2.accountLinks.create({
      account: accountId,
      refresh_url: `${baseUrl}/api/connect/refresh?accountId=${accountId}`,
      return_url: `${baseUrl}/api/connect/return?userId=${req.user.id}`,
      type: "account_onboarding"
    });
    res.json({ success: true, onboardingUrl: accountLink.url });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router51.get("/return", async (req, res) => {
  const { userId } = req.query;
  if (userId) {
    try {
      const stripe2 = getStripe2();
      const [userRows] = await db.execute(
        import_drizzle_orm75.sql`SELECT stripe_account_id FROM users WHERE id = ${userId} LIMIT 1`
      );
      const stripeAccountId = userRows[0]?.stripe_account_id;
      if (stripeAccountId) {
        const account = await stripe2.accounts.retrieve(stripeAccountId);
        if (account.details_submitted && account.payouts_enabled) {
          await markAccountActive(stripeAccountId);
        }
      }
    } catch (e) {
    }
  }
  const deepLink = `comeya://stripe-connect-complete`;
  res.send(`<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8">
    <title>Stripe configurado - ComeYa</title>
    <script>window.location.href='${deepLink}';</script>
    <style>body{font-family:system-ui,sans-serif;display:flex;align-items:center;justify-content:center;min-height:100vh;background:#F7F7F7;text-align:center;padding:24px;}</style>
    </head><body>
    <div style="background:white;border-radius:20px;padding:40px;max-width:400px;box-shadow:0 4px 20px rgba(0,0,0,.1)">
      <div style="font-size:64px">\u2705</div>
      <h1 style="color:#1A1A1A">\xA1Cuenta configurada!</h1>
      <p style="color:#555">Tu cuenta Stripe est\xE1 lista. Vuelve a la app para continuar.</p>
      <a href="${deepLink}" style="display:inline-block;background:#DC2626;color:white;padding:14px 28px;border-radius:50px;text-decoration:none;font-weight:700;margin-top:16px">Volver a ComeYa</a>
    </div></body></html>`);
});
router51.post("/disconnect", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    await db.execute(
      import_drizzle_orm75.sql`UPDATE users SET stripe_account_id = NULL WHERE id = ${userId}`
    );
    if (req.user.role === "business_owner") {
      await db.execute(
        import_drizzle_orm75.sql`UPDATE businesses SET stripe_account_id = NULL, stripe_account_status = 'not_connected' WHERE owner_id = ${userId}`
      );
    } else if (req.user.role === "delivery_driver") {
      await db.execute(
        import_drizzle_orm75.sql`UPDATE delivery_drivers SET stripe_account_id = NULL, stripe_account_status = 'not_connected' WHERE user_id = ${userId}`
      );
    }
    res.json({ success: true, message: "Cuenta Stripe desvinculada" });
  } catch (error) {
    const rawMessage = error?.raw?.message || error?.message || "Error al desvincular";
    res.status(500).json({ error: String(rawMessage).split("\n")[0].trim() });
  }
});
router51.get("/refresh", async (req, res) => {
  res.send(`<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8">
    <title>Enlace expirado - ComeYa</title>
    <style>body{font-family:system-ui,sans-serif;display:flex;align-items:center;justify-content:center;min-height:100vh;background:#F7F7F7;text-align:center;padding:24px;}</style>
    </head><body>
    <div style="background:white;border-radius:20px;padding:40px;max-width:400px;box-shadow:0 4px 20px rgba(0,0,0,.1)">
      <div style="font-size:64px">\u26A0\uFE0F</div>
      <h1 style="color:#1A1A1A">Enlace expirado</h1>
      <p style="color:#555">El enlace de configuraci\xF3n ha expirado. Vuelve a la app y genera uno nuevo.</p>
      <a href="comeya://" style="display:inline-block;background:#DC2626;color:white;padding:14px 28px;border-radius:50px;text-decoration:none;font-weight:700;margin-top:16px">Volver a ComeYa</a>
    </div></body></html>`);
});
router51.get("/dashboard-link", authenticateToken, async (req, res) => {
  try {
    const stripe2 = getStripe2();
    const accountId = await getStripeAccountId(req.user.id);
    if (!accountId) return res.status(404).json({ error: "Sin cuenta Stripe" });
    const loginLink = await stripe2.accounts.createLoginLink(accountId);
    res.json({ success: true, url: loginLink.url });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router51.delete("/disconnect", authenticateToken, async (req, res) => {
  try {
    await db.execute(
      import_drizzle_orm75.sql`UPDATE users SET stripe_account_id = NULL WHERE id = ${req.user.id}`
    );
    await db.execute(
      import_drizzle_orm75.sql`UPDATE businesses SET stripe_account_id = NULL, stripe_account_status = 'not_connected' WHERE owner_id = ${req.user.id}`
    );
    await db.execute(
      import_drizzle_orm75.sql`UPDATE delivery_drivers SET stripe_account_id = NULL, stripe_account_status = 'not_connected' WHERE user_id = ${req.user.id}`
    );
    res.json({ success: true });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
var stripeConnect_default = router51;

// server/routes/pickup.ts
var import_express52 = __toESM(require("express"));
var import_drizzle_orm76 = require("drizzle-orm");
init_pickupService();
var router52 = import_express52.default.Router();
router52.get("/:orderId/info", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [order] = await db2.select().from(orders2).where((0, import_drizzle_orm76.eq)(orders2.id, req.params.orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    if (order.orderType !== "pickup") {
      return res.status(400).json({ error: "Este pedido no es de tipo pickup" });
    }
    const codes = await pickupService.ensurePickupCodes(order);
    const timeRemaining = pickupService.getTimeRemaining(order);
    const progress = pickupService.getProgress(order);
    const pendingBefore = await pickupService.getPendingOrdersCount(
      order.businessId,
      order.id
    );
    res.json({
      success: true,
      pickup: {
        code: codes.pickupCode,
        qrCode: codes.pickupQrCode,
        estimatedMinutes: order.estimatedPickupTime,
        timeRemaining,
        progress,
        isReady: order.status === "ready",
        readyAt: order.pickupReadyAt,
        pendingBefore
      }
    });
  } catch (error) {
    console.error("Get pickup info error:", error);
    res.status(500).json({ error: error.message });
  }
});
router52.post("/:orderId/arrived", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const [order] = await db2.select({ order: orders2, business: businesses3 }).from(orders2).leftJoin(businesses3, (0, import_drizzle_orm76.eq)(orders2.businessId, businesses3.id)).where((0, import_drizzle_orm76.eq)(orders2.id, req.params.orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    if (order.order.userId !== req.user.id) {
      return res.status(403).json({ error: "No autorizado" });
    }
    if (order.order.orderType !== "pickup") {
      return res.status(400).json({ error: "Este pedido no es de tipo pickup" });
    }
    await pickupService.customerArrived(
      req.params.orderId,
      order.business?.ownerId
    );
    res.json({ success: true, message: "Negocio notificado de tu llegada" });
  } catch (error) {
    console.error("Customer arrived error:", error);
    res.status(500).json({ error: error.message });
  }
});
router52.post("/:orderId/update-time", authenticateToken, async (req, res) => {
  try {
    const { orders: orders2, businesses: businesses3 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { estimatedMinutes } = req.body;
    if (!estimatedMinutes || estimatedMinutes < 5 || estimatedMinutes > 120) {
      return res.status(400).json({ error: "Tiempo estimado inv\xE1lido (5-120 min)" });
    }
    const [order] = await db2.select({ order: orders2, business: businesses3 }).from(orders2).leftJoin(businesses3, (0, import_drizzle_orm76.eq)(orders2.businessId, businesses3.id)).where((0, import_drizzle_orm76.eq)(orders2.id, req.params.orderId)).limit(1);
    if (!order) {
      return res.status(404).json({ error: "Pedido no encontrado" });
    }
    if (order.business?.ownerId !== req.user.id && req.user.role !== "admin") {
      return res.status(403).json({ error: "No autorizado" });
    }
    await db2.update(orders2).set({
      estimatedPickupTime: estimatedMinutes
    }).where((0, import_drizzle_orm76.eq)(orders2.id, req.params.orderId));
    const { sendPushToUser: sendPushToUser2 } = await Promise.resolve().then(() => (init_enhancedPushService(), enhancedPushService_exports));
    await sendPushToUser2(order.order.userId, {
      title: "\u23F1\uFE0F Tiempo actualizado",
      body: `Tu pedido estar\xE1 listo en aprox. ${estimatedMinutes} min`,
      data: { orderId: order.order.id, screen: "OrderTracking" }
    });
    res.json({ success: true, message: "Tiempo actualizado" });
  } catch (error) {
    console.error("Update time error:", error);
    res.status(500).json({ error: error.message });
  }
});
router52.post("/:orderId/validate-code", authenticateToken, async (req, res) => {
  try {
    const { code } = req.body;
    if (!code || code.length !== 6) {
      return res.status(400).json({ error: "C\xF3digo inv\xE1lido" });
    }
    const isValid = await pickupService.validatePickupCode(
      req.params.orderId,
      code
    );
    res.json({ success: true, valid: isValid });
  } catch (error) {
    console.error("Validate code error:", error);
    res.status(500).json({ error: error.message });
  }
});
router52.get("/business/:businessId/average-time", async (req, res) => {
  try {
    const avgTime = await pickupService.getBusinessAverageTime(
      req.params.businessId
    );
    res.json({ success: true, averageMinutes: avgTime });
  } catch (error) {
    console.error("Get average time error:", error);
    res.status(500).json({ error: error.message });
  }
});
var pickup_default = router52;

// server/routes/registration.ts
var import_express53 = __toESM(require("express"));
init_cloudinaryService();
init_db();
init_schema_mysql();
var import_drizzle_orm77 = require("drizzle-orm");
var router53 = import_express53.default.Router();
router53.post("/upload-documents", async (req, res) => {
  try {
    const {
      userId,
      idDocument,
      idDocumentBack,
      autonomoDocument,
      profilePhoto,
      vehiclePhoto,
      vehicleDocument,
      insuranceDocument,
      vehicleType,
      vehiclePlate,
      vehicleBrand,
      vehicleModel,
      vehicleColor
    } = req.body;
    if (!userId) {
      return res.status(400).json({ error: "userId requerido" });
    }
    const uploadedUrls = {};
    if (idDocument) {
      uploadedUrls.idDocument = await CloudinaryService.uploadDocument(
        idDocument,
        "verification-docs",
        `id-front-${userId}`
      );
    }
    if (idDocumentBack) {
      uploadedUrls.idDocumentBack = await CloudinaryService.uploadDocument(
        idDocumentBack,
        "verification-docs",
        `id-back-${userId}`
      );
    }
    if (autonomoDocument) {
      uploadedUrls.autonomoDocument = await CloudinaryService.uploadDocument(
        autonomoDocument,
        "verification-docs",
        `autonomo-${userId}`
      );
    }
    if (profilePhoto) {
      uploadedUrls.profilePhoto = await CloudinaryService.uploadImage(
        profilePhoto,
        "profiles",
        `profile-${userId}`
      );
      await db.update(users).set({ profileImage: uploadedUrls.profilePhoto }).where((0, import_drizzle_orm77.eq)(users.id, userId));
    }
    if (vehiclePhoto) {
      uploadedUrls.vehiclePhoto = await CloudinaryService.uploadImage(
        vehiclePhoto,
        "profiles",
        `vehicle-${userId}`
      );
    }
    if (vehicleDocument) {
      uploadedUrls.vehicleDocument = await CloudinaryService.uploadDocument(
        vehicleDocument,
        "verification-docs",
        `vehicle-doc-${userId}`
      );
    }
    if (insuranceDocument) {
      uploadedUrls.insuranceDocument = await CloudinaryService.uploadDocument(
        insuranceDocument,
        "verification-docs",
        `insurance-${userId}`
      );
    }
    const userUpdates = { verificationStatus: "pending" };
    if (uploadedUrls.idDocument) userUpdates.idDocumentUrl = uploadedUrls.idDocument;
    if (uploadedUrls.idDocumentBack)
      userUpdates.idDocumentBackUrl = uploadedUrls.idDocumentBack;
    if (uploadedUrls.autonomoDocument)
      userUpdates.autonomoDocumentUrl = uploadedUrls.autonomoDocument;
    if (Object.keys(userUpdates).length > 1) {
      await db.update(users).set(userUpdates).where((0, import_drizzle_orm77.eq)(users.id, userId));
    }
    if (vehicleType) {
      const [existingDriver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm77.eq)(deliveryDrivers.userId, userId)).limit(1);
      const driverData = {
        vehicleType,
        vehiclePlate: vehiclePlate || null,
        vehicleBrand: vehicleBrand || null,
        vehicleModel: vehicleModel || null,
        vehicleColor: vehicleColor || null,
        vehiclePhoto: uploadedUrls.vehiclePhoto || null,
        // Permiso de circulación y seguro: persistirlos para la verificación
        vehicleLicensePhoto: uploadedUrls.vehicleDocument || null,
        vehicleInsurancePhoto: uploadedUrls.insuranceDocument || null
      };
      if (existingDriver) {
        await db.update(deliveryDrivers).set(driverData).where((0, import_drizzle_orm77.eq)(deliveryDrivers.userId, userId));
      } else {
        await db.insert(deliveryDrivers).values({
          id: crypto.randomUUID(),
          userId,
          ...driverData,
          isAvailable: false
        });
      }
      await db.update(users).set({ verificationStatus: "pending" }).where((0, import_drizzle_orm77.eq)(users.id, userId));
    }
    res.json({
      success: true,
      uploadedUrls,
      message: "Documentos subidos correctamente"
    });
  } catch (error) {
    console.error("Error uploading documents:", error);
    res.status(500).json({ error: error.message || "Error al subir documentos" });
  }
});
var registration_default = router53;

// server/routes/mapData.ts
var import_express54 = __toESM(require("express"));
init_db();
init_schema_mysql();
var import_drizzle_orm78 = require("drizzle-orm");
init_googleMapsService();
function isBusinessOpenLocal(openingHoursRaw, isOpenManual, now = /* @__PURE__ */ new Date()) {
  const manualFlag = isOpenManual === true || isOpenManual === 1 || isOpenManual === "1";
  if (!manualFlag) return false;
  if (!openingHoursRaw) return true;
  try {
    const hours = typeof openingHoursRaw === "string" ? JSON.parse(openingHoursRaw) : openingHoursRaw;
    const days = ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday"];
    const dayIndex = now.getDay();
    const dayKey = days[dayIndex];
    const todaySchedule = hours?.[dayKey];
    if (!todaySchedule || todaySchedule.closed) return false;
    const nowMinutes = now.getHours() * 60 + now.getMinutes();
    const timeToMinutes = (t) => {
      const [h, m] = t.split(":").map(Number);
      return h * 60 + (m || 0);
    };
    const isWithin = (open, close) => {
      const o = timeToMinutes(open);
      const c = timeToMinutes(close);
      if (c <= o) return nowMinutes >= o || nowMinutes < c;
      return nowMinutes >= o && nowMinutes < c;
    };
    if (isWithin(todaySchedule.open, todaySchedule.close)) return true;
    if (todaySchedule.eveningOpen && todaySchedule.eveningClose) {
      if (isWithin(todaySchedule.eveningOpen, todaySchedule.eveningClose)) return true;
    }
  } catch {
    return true;
  }
  return false;
}
var router54 = import_express54.default.Router();
router54.get("/nearby-businesses", authenticateToken, async (req, res) => {
  try {
    const { lat, lng, radiusKm = "5" } = req.query;
    if (!lat || !lng) {
      return res.status(400).json({ error: "lat and lng required" });
    }
    const latitude = parseFloat(lat);
    const longitude = parseFloat(lng);
    const radius = parseFloat(radiusKm);
    const allBusinesses = await db.select({
      id: businesses.id,
      name: businesses.name,
      image: businesses.image,
      address: businesses.address,
      latitude: businesses.latitude,
      longitude: businesses.longitude,
      categories: businesses.categories,
      isActive: businesses.isActive,
      openingHours: businesses.openingHours,
      ownerId: businesses.ownerId
    }).from(businesses).where(
      (0, import_drizzle_orm78.and)(
        (0, import_drizzle_orm78.eq)(businesses.isActive, true),
        import_drizzle_orm78.sql`${businesses.latitude} IS NOT NULL`,
        import_drizzle_orm78.sql`${businesses.longitude} IS NOT NULL`
      )
    );
    const nearby = allBusinesses.map((biz) => {
      const bizLat = parseFloat(biz.latitude);
      const bizLng = parseFloat(biz.longitude);
      if (isNaN(bizLat) || isNaN(bizLng)) return null;
      const distanceKm = googleMapsService.calculateHaversineDistance(
        latitude,
        longitude,
        bizLat,
        bizLng
      );
      if (distanceKm > radius) return null;
      let isOpen = false;
      try {
        isOpen = isBusinessOpenLocal(biz.openingHours, biz.isActive);
      } catch {
        isOpen = true;
      }
      return {
        id: biz.id,
        name: biz.name,
        image: biz.image,
        address: biz.address,
        latitude: bizLat,
        longitude: bizLng,
        categories: biz.categories,
        distanceKm: Math.round(distanceKm * 10) / 10,
        isOpen
      };
    }).filter(Boolean).sort((a, b) => a.distanceKm - b.distanceKm);
    res.json({ success: true, businesses: nearby });
  } catch (error) {
    console.error("Error fetching nearby businesses:", error);
    res.status(500).json({ error: error.message });
  }
});
router54.get("/route", authenticateToken, async (req, res) => {
  try {
    const { originLat, originLng, destLat, destLng } = req.query;
    if (!originLat || !originLng || !destLat || !destLng) {
      return res.status(400).json({ error: "Missing coordinates" });
    }
    const result = await googleMapsService.getDirections(
      parseFloat(originLat),
      parseFloat(originLng),
      parseFloat(destLat),
      parseFloat(destLng)
    );
    if (!result) {
      const distanceKm = googleMapsService.calculateHaversineDistance(
        parseFloat(originLat),
        parseFloat(originLng),
        parseFloat(destLat),
        parseFloat(destLng)
      );
      return res.json({
        success: true,
        fallback: true,
        polyline: "",
        distance: { text: `${distanceKm.toFixed(1)} km`, value: Math.round(distanceKm * 1e3) },
        duration: { text: `${googleMapsService.estimateDeliveryTimeMinutes(distanceKm)} min`, value: 0 },
        decodedPath: [
          { latitude: parseFloat(originLat), longitude: parseFloat(originLng) },
          { latitude: parseFloat(destLat), longitude: parseFloat(destLng) }
        ]
      });
    }
    const decodedPath = googleMapsService.decodePolyline(result.polyline);
    res.json({
      success: true,
      polyline: result.polyline,
      distance: result.distance,
      duration: result.duration,
      decodedPath,
      startLocation: result.startLocation,
      endLocation: result.endLocation
    });
  } catch (error) {
    console.error("Error fetching route:", error);
    res.status(500).json({ error: error.message });
  }
});
var mapData_default = router54;

// server/routes/adminSubscriptionPlans.ts
var import_express55 = __toESM(require("express"));
init_db();
init_schema_mysql();
var import_drizzle_orm79 = require("drizzle-orm");
var import_crypto6 = require("crypto");
var router55 = import_express55.default.Router();
router55.get(
  "/subscription-plans",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const plans = await db.select().from(subscriptionPlans).orderBy(subscriptionPlans.displayOrder);
      const benefits = await db.select().from(subscriptionBenefits);
      const result = plans.map((plan) => ({
        ...plan,
        benefits: benefits.filter((b) => b.plan === plan.planKey)
      }));
      res.json({ success: true, plans: result });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router55.put(
  "/subscription-plans/:planKey",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { planKey } = req.params;
      const { name, description, price, isActive, color } = req.body;
      await db.update(subscriptionPlans).set({
        name,
        description,
        price,
        isActive,
        color,
        updatedAt: /* @__PURE__ */ new Date()
      }).where((0, import_drizzle_orm79.eq)(subscriptionPlans.planKey, planKey));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router55.post(
  "/subscription-benefits",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { plan, benefitType, benefitValue, description } = req.body;
      if (!plan || !benefitType)
        return res.status(400).json({ error: "plan y benefitType son requeridos" });
      const id = (0, import_crypto6.randomUUID)();
      await db.insert(subscriptionBenefits).values({
        id,
        plan,
        benefitType,
        benefitValue: parseInt(benefitValue) || 0,
        description
      });
      res.json({ success: true, id });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router55.put(
  "/subscription-benefits/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { benefitType, benefitValue, description } = req.body;
      await db.update(subscriptionBenefits).set({
        benefitType,
        benefitValue: parseInt(benefitValue) || 0,
        description
      }).where((0, import_drizzle_orm79.eq)(subscriptionBenefits.id, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router55.delete(
  "/subscription-benefits/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      await db.delete(subscriptionBenefits).where((0, import_drizzle_orm79.eq)(subscriptionBenefits.id, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
router55.get(
  "/premium-subscribers",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const now = /* @__PURE__ */ new Date();
      const subs = await db.select({
        id: subscriptions.id,
        userId: subscriptions.userId,
        plan: subscriptions.plan,
        status: subscriptions.status,
        price: subscriptions.price,
        billingCycle: subscriptions.billingCycle,
        autoRenew: subscriptions.autoRenew,
        currentPeriodStart: subscriptions.currentPeriodStart,
        currentPeriodEnd: subscriptions.currentPeriodEnd,
        cancelledAt: subscriptions.cancelledAt,
        createdAt: subscriptions.createdAt,
        userName: users.name,
        userEmail: users.email
      }).from(subscriptions).leftJoin(users, (0, import_drizzle_orm79.eq)(subscriptions.userId, users.id)).where((0, import_drizzle_orm79.and)(import_drizzle_orm79.sql`${subscriptions.plan} != 'free'`)).orderBy((0, import_drizzle_orm79.desc)(subscriptions.createdAt));
      const result = subs.map((sub) => {
        const endDate = sub.currentPeriodEnd ? new Date(sub.currentPeriodEnd) : null;
        const daysLeft = endDate ? Math.max(
          0,
          Math.ceil(
            (endDate.getTime() - now.getTime()) / (1e3 * 60 * 60 * 24)
          )
        ) : 0;
        const isExpired = endDate ? endDate < now : true;
        return {
          ...sub,
          daysLeft,
          isExpired,
          daysRemaining: isExpired ? 0 : daysLeft
        };
      });
      const totalActive = result.filter(
        (r) => r.status === "active" && !r.isExpired
      ).length;
      const totalExpired = result.filter(
        (r) => r.isExpired || r.status === "expired"
      ).length;
      const revenue = result.filter((r) => r.status === "active").reduce((sum3, r) => sum3 + (r.price || 0), 0);
      res.json({
        success: true,
        subscribers: result,
        stats: {
          totalActive,
          totalExpired,
          totalRevenue: revenue
        }
      });
    } catch (error) {
      res.status(500).json({ error: error.message });
    }
  }
);
var adminSubscriptionPlans_default = router55;

// server/routes/adminTracking.ts
var import_express56 = __toESM(require("express"));
init_db();
var import_drizzle_orm80 = require("drizzle-orm");

// server/utils/opsAlerts.ts
var ALERT_NO_DRIVER_MIN = 10;
var ALERT_NO_RESPONSE_MIN = 5;
var ALERT_STALE_GPS_MIN = 5;
var ALERT_TOTAL_MIN = 60;
var DRIVER_ONLINE_WINDOW_MIN = 10;
var ACTIVE_STATUSES = [
  "pending",
  "accepted",
  "preparing",
  "ready",
  "assigned_driver",
  "picked_up",
  "on_the_way",
  "in_transit",
  "arriving"
];
var IN_TRANSIT_STATUSES = [
  "picked_up",
  "on_the_way",
  "in_transit",
  "arriving"
];
function minutesSince(value) {
  if (!value) return null;
  const t = new Date(value).getTime();
  if (Number.isNaN(t)) return null;
  return Math.max(0, Math.round((Date.now() - t) / 6e4));
}
function computeOrderAlerts(order) {
  const alerts = [];
  const mins = order.minutesActive ?? 0;
  if (!order.hasDriver && ["preparing", "ready"].includes(order.status) && mins >= ALERT_NO_DRIVER_MIN) {
    alerts.push({
      type: "no_driver",
      message: `Sin repartidor hace ${mins} min`
    });
  }
  if (order.status === "pending" && !order.businessResponseAt && mins >= ALERT_NO_RESPONSE_MIN) {
    alerts.push({
      type: "no_response",
      message: `El negocio no responde (${mins} min)`
    });
  }
  if (order.hasDriver && IN_TRANSIT_STATUSES.includes(order.status) && order.driverLastUpdateMinutes != null && order.driverLastUpdateMinutes >= ALERT_STALE_GPS_MIN) {
    alerts.push({
      type: "stale_gps",
      message: `GPS sin se\xF1al hace ${order.driverLastUpdateMinutes} min`
    });
  }
  if (mins >= ALERT_TOTAL_MIN) {
    alerts.push({
      type: "too_long",
      message: `Pedido abierto hace ${mins} min`
    });
  }
  return alerts;
}
function isDriverOnline(isAvailable, lastUpdateMinutes) {
  return !!isAvailable && lastUpdateMinutes != null && lastUpdateMinutes < DRIVER_ONLINE_WINDOW_MIN;
}

// server/routes/adminTracking.ts
var router56 = import_express56.default.Router();
async function fetchActiveOrders(limit = 80) {
  const statusList = import_drizzle_orm80.sql.join(
    ACTIVE_STATUSES.map((s) => import_drizzle_orm80.sql`${s}`),
    import_drizzle_orm80.sql`, `
  );
  const [rows] = await db.execute(import_drizzle_orm80.sql`
    SELECT
      o.id, o.order_number, o.status, o.total, o.subtotal, o.delivery_fee, o.payment_method,
      o.order_type, o.created_at, o.assigned_at, o.estimated_delivery,
      o.business_response_at, o.driver_picked_up_at, o.driver_arrived_at,
      o.delivery_address,
      o.delivery_latitude, o.delivery_longitude,
      o.delivery_person_id,
      u.name AS customer_name, u.phone AS customer_phone,
      b.id AS business_id, b.name AS business_name, b.type AS business_type,
      b.categories AS business_categories, b.phone AS business_phone,
      b.latitude AS business_lat, b.longitude AS business_lng,
      dd.current_latitude AS driver_lat,
      dd.current_longitude AS driver_lng,
      dd.last_location_update AS driver_last_update,
      dd.vehicle_type AS driver_vehicle,
      dd.rating AS driver_rating,
      du.name AS driver_name, du.phone AS driver_phone
    FROM orders o
    LEFT JOIN users u ON o.user_id = u.id
    LEFT JOIN businesses b ON o.business_id = b.id
    LEFT JOIN delivery_drivers dd ON o.delivery_person_id = dd.user_id
    LEFT JOIN users du ON dd.user_id = du.id
    WHERE o.status IN (${statusList})
      AND o.business_id IS NOT NULL
      AND o.deleted_at IS NULL
    ORDER BY o.created_at DESC
    LIMIT ${limit}
  `);
  return rows.map((r) => {
    const minutesActive = minutesSince(r.created_at);
    const driverLastUpdateMinutes = minutesSince(r.driver_last_update);
    const hasDriver = !!r.delivery_person_id;
    let address = r.delivery_address;
    try {
      if (typeof address === "string") address = JSON.parse(address);
    } catch {
    }
    const alerts = computeOrderAlerts({
      status: r.status,
      minutesActive,
      hasDriver,
      businessResponseAt: r.business_response_at,
      driverLastUpdateMinutes
    });
    return {
      id: r.id,
      status: r.status,
      orderType: r.order_type,
      total: Number(r.total) || 0,
      subtotal: Number(r.subtotal) || 0,
      deliveryFee: Number(r.delivery_fee) || 0,
      paymentMethod: r.payment_method,
      createdAt: r.created_at,
      assignedAt: r.assigned_at,
      estimatedDelivery: r.estimated_delivery,
      pickedUpAt: r.driver_picked_up_at,
      arrivedAt: r.driver_arrived_at,
      minutesActive,
      alerts,
      customer: {
        name: r.customer_name || "Cliente",
        phone: r.customer_phone || null,
        address: typeof address === "object" && address ? [address.street, address.city].filter(Boolean).join(", ") : address || null,
        lat: r.delivery_latitude ? parseFloat(r.delivery_latitude) : null,
        lng: r.delivery_longitude ? parseFloat(r.delivery_longitude) : null
      },
      business: r.business_lat ? {
        id: r.business_id,
        name: r.business_name || "Negocio",
        type: r.business_type,
        categories: r.business_categories,
        phone: r.business_phone || null,
        lat: parseFloat(r.business_lat),
        lng: parseFloat(r.business_lng)
      } : null,
      driver: hasDriver ? {
        id: r.delivery_person_id,
        name: r.driver_name || "Repartidor",
        phone: r.driver_phone || null,
        vehicleType: r.driver_vehicle,
        rating: r.driver_rating ? (r.driver_rating / 10).toFixed(1) : null,
        lat: r.driver_lat ? parseFloat(r.driver_lat) : null,
        lng: r.driver_lng ? parseFloat(r.driver_lng) : null,
        lastUpdate: r.driver_last_update,
        lastUpdateMinutes: driverLastUpdateMinutes
      } : null,
      // Compatibilidad con el consumidor anterior del mapa web
      delivery: r.delivery_latitude && r.delivery_longitude ? {
        lat: parseFloat(r.delivery_latitude),
        lng: parseFloat(r.delivery_longitude)
      } : null
    };
  });
}
router56.get(
  "/tracking/global",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const orders2 = await fetchActiveOrders(80);
      res.json({ success: true, orders: orders2 });
    } catch (error) {
      console.error("tracking/global error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var adminTracking_default = router56;

// server/routes/adminOps.ts
var import_express57 = __toESM(require("express"));
init_db();
var import_drizzle_orm81 = require("drizzle-orm");
var router57 = import_express57.default.Router();
var CACHE_TTL_MS = 5e3;
var cache3 = null;
async function fetchKpis() {
  const statusList = import_drizzle_orm81.sql.join(
    ACTIVE_STATUSES.map((s) => import_drizzle_orm81.sql`${s}`),
    import_drizzle_orm81.sql`, `
  );
  const [byStatusRows] = await db.execute(import_drizzle_orm81.sql`
    SELECT status, COUNT(*) AS c
    FROM orders
    WHERE status IN (${statusList})
    GROUP BY status
  `);
  const byStatus = {};
  let activeOrders = 0;
  for (const r of byStatusRows) {
    byStatus[r.status] = Number(r.c) || 0;
    activeOrders += Number(r.c) || 0;
  }
  const [noDriverRows] = await db.execute(import_drizzle_orm81.sql`
    SELECT COUNT(*) AS c
    FROM orders
    WHERE status IN (${statusList})
      AND (delivery_person_id IS NULL OR delivery_person_id = '')
      AND (order_type IS NULL OR order_type <> 'pickup')
  `);
  const [todayRows] = await db.execute(import_drizzle_orm81.sql`
    SELECT
      COUNT(*) AS orders_today,
      SUM(status = 'delivered') AS delivered_today,
      SUM(status = 'cancelled') AS cancelled_today,
      COALESCE(SUM(CASE WHEN status = 'delivered' THEN total ELSE 0 END), 0) AS revenue_today
    FROM orders
    WHERE created_at >= CURDATE()
  `);
  const [avgRows] = await db.execute(import_drizzle_orm81.sql`
    SELECT
      AVG(TIMESTAMPDIFF(MINUTE, created_at, delivered_at)) AS avg_total,
      AVG(TIMESTAMPDIFF(MINUTE, driver_picked_up_at, delivered_at)) AS avg_delivery
    FROM orders
    WHERE status = 'delivered'
      AND delivered_at IS NOT NULL
      AND delivered_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
      -- Descarta outliers (datos sucios o pedidos cerrados a mano días después)
      AND TIMESTAMPDIFF(MINUTE, created_at, delivered_at) BETWEEN 1 AND 240
  `);
  const [driverRows] = await db.execute(import_drizzle_orm81.sql`
    SELECT
      COUNT(*) AS total,
      SUM(is_available = 1) AS available,
      SUM(
        is_available = 1
        AND last_location_update IS NOT NULL
        AND last_location_update >= DATE_SUB(NOW(), INTERVAL ${DRIVER_ONLINE_WINDOW_MIN} MINUTE)
      ) AS online,
      SUM(is_blocked = 1) AS blocked
    FROM delivery_drivers
  `);
  const [bizRows] = await db.execute(import_drizzle_orm81.sql`
    SELECT
      COUNT(*) AS total,
      SUM(is_open = 1) AS open_now,
      SUM(is_paused = 1) AS paused,
      SUM(latitude IS NULL OR longitude IS NULL) AS without_coords
    FROM businesses
    WHERE is_active = 1
  `);
  const today = todayRows[0] || {};
  const avg = avgRows[0] || {};
  const drivers3 = driverRows[0] || {};
  const biz = bizRows[0] || {};
  const ordersToday = Number(today.orders_today) || 0;
  const cancelledToday = Number(today.cancelled_today) || 0;
  return {
    activeOrders,
    byStatus,
    ordersWithoutDriver: Number(noDriverRows[0]?.c) || 0,
    ordersToday,
    deliveredToday: Number(today.delivered_today) || 0,
    cancelledToday,
    cancellationRate: ordersToday > 0 ? Number((cancelledToday / ordersToday * 100).toFixed(1)) : 0,
    revenueToday: Number(today.revenue_today) || 0,
    // null cuando aún no hay entregas suficientes (mejor que un valor falso)
    avgTotalMinutes: avg.avg_total != null ? Math.round(Number(avg.avg_total)) : null,
    avgDeliveryMinutes: avg.avg_delivery != null ? Math.round(Number(avg.avg_delivery)) : null,
    drivers: {
      total: Number(drivers3.total) || 0,
      available: Number(drivers3.available) || 0,
      online: Number(drivers3.online) || 0,
      blocked: Number(drivers3.blocked) || 0
    },
    businesses: {
      total: Number(biz.total) || 0,
      open: Number(biz.open_now) || 0,
      paused: Number(biz.paused) || 0,
      withoutCoords: Number(biz.without_coords) || 0
    }
  };
}
async function fetchDriversForMap() {
  const [rows] = await db.execute(import_drizzle_orm81.sql`
    SELECT
      dd.user_id AS id, u.name, u.phone,
      dd.vehicle_type, dd.vehicle_plate, dd.rating, dd.total_deliveries,
      dd.is_available, dd.is_blocked,
      dd.current_latitude, dd.current_longitude, dd.last_location_update,
      (
        SELECT o.id FROM orders o
        WHERE o.delivery_person_id = dd.user_id
          AND o.status IN ('assigned_driver','picked_up','on_the_way','in_transit','arriving','ready')
        ORDER BY o.created_at DESC LIMIT 1
      ) AS active_order_id
    FROM delivery_drivers dd
    LEFT JOIN users u ON dd.user_id = u.id
    WHERE dd.current_latitude IS NOT NULL AND dd.current_longitude IS NOT NULL
  `);
  return rows.map((r) => {
    const lastUpdateMinutes = minutesSince(r.last_location_update);
    return {
      id: r.id,
      name: r.name || "Repartidor",
      phone: r.phone || null,
      vehicleType: r.vehicle_type,
      vehiclePlate: r.vehicle_plate,
      rating: r.rating ? (r.rating / 10).toFixed(1) : null,
      totalDeliveries: Number(r.total_deliveries) || 0,
      isAvailable: !!r.is_available,
      isBlocked: !!r.is_blocked,
      // "Conectado" = disponible y con GPS fresco
      isOnline: isDriverOnline(!!r.is_available, lastUpdateMinutes),
      lat: parseFloat(r.current_latitude),
      lng: parseFloat(r.current_longitude),
      lastUpdate: r.last_location_update,
      lastUpdateMinutes,
      staleGps: lastUpdateMinutes == null || lastUpdateMinutes >= DRIVER_ONLINE_WINDOW_MIN,
      activeOrderId: r.active_order_id || null
    };
  });
}
async function fetchBusinessesForMap() {
  const [rows] = await db.execute(import_drizzle_orm81.sql`
    SELECT
      b.id, b.name, b.type, b.categories, b.phone, b.address,
      b.latitude, b.longitude, b.is_open, b.is_paused, b.is_featured,
      b.rating, b.total_orders_completed,
      (
        SELECT COUNT(*) FROM orders o
        WHERE o.business_id = b.id
          AND o.status IN ('pending','accepted','preparing','ready','assigned_driver','picked_up','on_the_way','in_transit','arriving')
      ) AS active_orders
    FROM businesses b
    WHERE b.is_active = 1
      AND b.latitude IS NOT NULL AND b.longitude IS NOT NULL
  `);
  return rows.map((r) => ({
    id: r.id,
    name: r.name || "Negocio",
    type: r.type,
    categories: r.categories,
    phone: r.phone || null,
    address: r.address || null,
    lat: parseFloat(r.latitude),
    lng: parseFloat(r.longitude),
    isOpen: !!r.is_open,
    isPaused: !!r.is_paused,
    isFeatured: !!r.is_featured,
    rating: r.rating ? (r.rating / 10).toFixed(1) : null,
    totalOrders: Number(r.total_orders_completed) || 0,
    activeOrders: Number(r.active_orders) || 0
  }));
}
router57.get(
  "/ops/overview",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      if (cache3 && Date.now() - cache3.at < CACHE_TTL_MS) {
        return res.json({ ...cache3.payload, cached: true });
      }
      const [kpis, orders2, drivers3, businesses3] = await Promise.all([
        fetchKpis(),
        fetchActiveOrders(80),
        fetchDriversForMap(),
        fetchBusinessesForMap()
      ]);
      const alerts = orders2.filter((o) => o.alerts.length > 0).map((o) => ({
        orderId: o.id,
        status: o.status,
        businessName: o.business?.name ?? null,
        minutesActive: o.minutesActive,
        alerts: o.alerts
      }));
      const payload = {
        success: true,
        kpis: { ...kpis, alertCount: alerts.length },
        orders: orders2,
        drivers: drivers3,
        businesses: businesses3,
        alerts,
        updatedAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      cache3 = { at: Date.now(), payload };
      res.json(payload);
    } catch (error) {
      console.error("ops/overview error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router57.get(
  "/ops/nearby-drivers",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const orderId = req.query.orderId;
      if (!orderId) {
        return res.status(400).json({ error: "orderId requerido" });
      }
      const [orderRows] = await db.execute(import_drizzle_orm81.sql`
        SELECT o.id, o.delivery_person_id,
               b.latitude AS business_lat, b.longitude AS business_lng,
               b.name AS business_name
        FROM orders o
        LEFT JOIN businesses b ON o.business_id = b.id
        WHERE o.id = ${orderId}
        LIMIT 1
      `);
      const order = orderRows[0];
      if (!order) return res.status(404).json({ error: "Pedido no encontrado" });
      const originLat = order.business_lat ? parseFloat(order.business_lat) : null;
      const originLng = order.business_lng ? parseFloat(order.business_lng) : null;
      const allDrivers = await fetchDriversForMap();
      const { haversineMeters: haversineMeters2 } = await Promise.resolve().then(() => (init_trackingPipeline(), trackingPipeline_exports));
      const candidates = allDrivers.filter((d) => !d.isBlocked).map((d) => ({
        ...d,
        distanceKm: originLat != null && originLng != null ? Number(
          (haversineMeters2(originLat, originLng, d.lat, d.lng) / 1e3).toFixed(2)
        ) : null,
        busy: !!d.activeOrderId && d.activeOrderId !== orderId,
        isCurrent: d.id === order.delivery_person_id
      })).sort((a, b) => {
        if (a.busy !== b.busy) return a.busy ? 1 : -1;
        if (a.isOnline !== b.isOnline) return a.isOnline ? -1 : 1;
        return (a.distanceKm ?? 9999) - (b.distanceKm ?? 9999);
      });
      res.json({
        success: true,
        orderId,
        businessName: order.business_name ?? null,
        currentDriverId: order.delivery_person_id ?? null,
        drivers: candidates.slice(0, 15)
      });
    } catch (error) {
      console.error("ops/nearby-drivers error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var adminOps_default = router57;

// server/routes/businessCategories.ts
var import_express58 = __toESM(require("express"));
var router58 = import_express58.default.Router();
var DEFAULT_CATEGORIES = [
  {
    name: "Restaurantes",
    slug: "restaurant",
    icon: "coffee",
    color: "#F97316",
    description: "Comida preparada, men\xFAs y platos del d\xEDa",
    displayOrder: 1
  },
  {
    name: "Mercados",
    slug: "market",
    icon: "shopping-bag",
    color: "#10B981",
    description: "Supermercados, fruter\xEDas y alimentaci\xF3n",
    displayOrder: 2
  },
  {
    name: "Farmacias",
    slug: "pharmacy",
    icon: "plus-circle",
    color: "#3B82F6",
    description: "Medicamentos, parafarmacia y productos de salud",
    displayOrder: 3
  },
  {
    name: "Tiendas",
    slug: "store",
    icon: "package",
    color: "#8B5CF6",
    description: "Comercios locales y tiendas especializadas",
    displayOrder: 4
  },
  {
    name: "Ferreter\xEDas",
    slug: "hardware",
    icon: "tool",
    color: "#F59E0B",
    description: "Herramientas, materiales y bricolaje",
    displayOrder: 5
  },
  {
    name: "Papeler\xEDas",
    slug: "stationery",
    icon: "book",
    color: "#EC4899",
    description: "Material de oficina, libros y papeler\xEDa",
    displayOrder: 6
  },
  {
    name: "Otros",
    slug: "other",
    icon: "grid",
    color: "#6B7280",
    description: "Negocios que no encajan en otras categor\xEDas",
    displayOrder: 99
  }
];
router58.get(
  "/",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessCategories: businessCategories2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { asc } = await import("drizzle-orm");
      let cats = await db2.select().from(businessCategories2).orderBy(asc(businessCategories2.displayOrder));
      if (cats.length === 0) {
        await db2.insert(businessCategories2).values(DEFAULT_CATEGORIES);
        cats = await db2.select().from(businessCategories2).orderBy(asc(businessCategories2.displayOrder));
      }
      res.json({ success: true, categories: cats });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router58.get("/public", async (_req, res) => {
  try {
    const { businessCategories: businessCategories2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
    const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    const { asc, eq: eq79 } = await import("drizzle-orm");
    const cats = await db2.select().from(businessCategories2).where(eq79(businessCategories2.isActive, true)).orderBy(asc(businessCategories2.displayOrder));
    res.json({ success: true, categories: cats });
  } catch (error) {
    res.status(500).json({ success: false, error: error.message });
  }
});
router58.post(
  "/",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessCategories: businessCategories2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { name, slug, icon, color, description, isActive, displayOrder } = req.body;
      if (!name?.trim() || !slug?.trim()) {
        return res.status(400).json({ success: false, error: "name y slug son requeridos" });
      }
      const cleanSlug = slug.trim().toLowerCase().replace(/\s+/g, "-").replace(/[^a-z0-9-]/g, "");
      await db2.insert(businessCategories2).values({
        name: name.trim(),
        slug: cleanSlug,
        icon: icon ?? "grid",
        color: color ?? "#6B7280",
        description: description ?? null,
        isActive: isActive ?? true,
        displayOrder: displayOrder ?? 0
      });
      const [created] = await db2.select().from(businessCategories2).where(
        (await import("drizzle-orm")).eq(businessCategories2.slug, cleanSlug)
      ).limit(1);
      res.json({ success: true, category: created });
    } catch (error) {
      if (error.message?.includes("Duplicate")) {
        return res.status(409).json({
          success: false,
          error: "Ya existe una categor\xEDa con ese slug"
        });
      }
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router58.put(
  "/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessCategories: businessCategories2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      const { name, slug, icon, color, description, isActive, displayOrder } = req.body;
      const updates = {};
      if (name !== void 0) updates.name = name.trim();
      if (slug !== void 0)
        updates.slug = slug.trim().toLowerCase().replace(/\s+/g, "-").replace(/[^a-z0-9-]/g, "");
      if (icon !== void 0) updates.icon = icon;
      if (color !== void 0) updates.color = color;
      if (description !== void 0) updates.description = description;
      if (isActive !== void 0) updates.isActive = isActive;
      if (displayOrder !== void 0) updates.displayOrder = displayOrder;
      await db2.update(businessCategories2).set(updates).where(eq79(businessCategories2.id, req.params.id));
      const [updated] = await db2.select().from(businessCategories2).where(eq79(businessCategories2.id, req.params.id)).limit(1);
      res.json({ success: true, category: updated });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
router58.delete(
  "/:id",
  authenticateToken,
  requireRole("admin", "super_admin"),
  async (req, res) => {
    try {
      const { businessCategories: businessCategories2 } = await Promise.resolve().then(() => (init_schema_mysql(), schema_mysql_exports));
      const { db: db2 } = await Promise.resolve().then(() => (init_db(), db_exports));
      const { eq: eq79 } = await import("drizzle-orm");
      await db2.delete(businessCategories2).where(eq79(businessCategories2.id, req.params.id));
      res.json({ success: true });
    } catch (error) {
      res.status(500).json({ success: false, error: error.message });
    }
  }
);
var businessCategories_default = router58;

// server/routes/deliveryVerification.ts
var import_express59 = __toESM(require("express"));
init_db();
init_schema_mysql();
var import_drizzle_orm82 = require("drizzle-orm");
init_cloudinaryService();
var router59 = import_express59.default.Router();
var REQUIRED_DOCS = [
  "idDocumentUrl",
  "idDocumentBackUrl",
  "vehicleLicensePhoto",
  "vehiclePhoto"
];
router59.post("/submit", async (req, res) => {
  try {
    const authUser = req.user;
    const userId = req.body.userId || authUser?.id;
    if (!userId) {
      return res.status(400).json({ error: "userId requerido" });
    }
    const {
      // Document URLs (ya subidos a Cloudinary)
      idDocumentUrl,
      idDocumentBackUrl,
      autonomoDocumentUrl,
      vehiclePhoto,
      vehiclePlatePhoto,
      vehicleItvPhoto,
      vehicleInsurancePhoto,
      vehicleLicensePhoto,
      // Info del vehículo
      vehicleType,
      vehiclePlate,
      vehicleBrand,
      vehicleModel,
      vehicleColor
    } = req.body;
    const [user] = await db.select().from(users).where((0, import_drizzle_orm82.eq)(users.id, userId)).limit(1);
    if (!user) {
      return res.status(404).json({ error: "Usuario no encontrado" });
    }
    if (user.role !== "delivery_driver" && !req.body.applyAsDriver) {
      await db.update(users).set({ role: "delivery_driver", verificationStatus: "pending" }).where((0, import_drizzle_orm82.eq)(users.id, userId));
    } else {
      await db.update(users).set({ verificationStatus: "pending", updatedAt: /* @__PURE__ */ new Date() }).where((0, import_drizzle_orm82.eq)(users.id, userId));
    }
    const userUpdates = { verificationStatus: "pending" };
    if (idDocumentUrl) userUpdates.idDocumentUrl = idDocumentUrl;
    if (idDocumentBackUrl) userUpdates.idDocumentBackUrl = idDocumentBackUrl;
    if (autonomoDocumentUrl)
      userUpdates.autonomoDocumentUrl = autonomoDocumentUrl;
    await db.update(users).set(userUpdates).where((0, import_drizzle_orm82.eq)(users.id, userId));
    const [existingDriver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm82.eq)(deliveryDrivers.userId, userId)).limit(1);
    const driverUpdates = {
      vehicleType: vehicleType || null,
      vehiclePlate: vehiclePlate || null,
      vehicleBrand: vehicleBrand || null,
      vehicleModel: vehicleModel || null,
      vehicleColor: vehicleColor || null,
      vehiclePhoto: vehiclePhoto || null,
      vehiclePlatePhoto: vehiclePlatePhoto || null,
      vehicleItvPhoto: vehicleItvPhoto || null,
      vehicleInsurancePhoto: vehicleInsurancePhoto || null,
      vehicleLicensePhoto: vehicleLicensePhoto || null
    };
    if (existingDriver) {
      await db.update(deliveryDrivers).set(driverUpdates).where((0, import_drizzle_orm82.eq)(deliveryDrivers.userId, userId));
    } else {
      await db.insert(deliveryDrivers).values({
        id: crypto.randomUUID(),
        userId,
        ...driverUpdates,
        isAvailable: false,
        currentLatitude: null,
        currentLongitude: null
      });
    }
    try {
      const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      notifyAdmins2({
        type: "new_delivery_verification",
        userId,
        userName: user.name,
        message: `Nuevo repartidor pendiente de verificaci\xF3n: ${user.name}`
      });
    } catch {
    }
    res.json({
      success: true,
      message: "Documentos enviados. Tu verificaci\xF3n est\xE1 en revisi\xF3n.",
      status: "pending"
    });
  } catch (error) {
    console.error("Error submitting verification:", error);
    res.status(500).json({ error: error.message });
  }
});
router59.get("/status", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const [user] = await db.select().from(users).where((0, import_drizzle_orm82.eq)(users.id, userId)).limit(1);
    if (!user) {
      return res.status(404).json({ error: "Usuario no encontrado" });
    }
    const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm82.eq)(deliveryDrivers.userId, userId)).limit(1);
    const docs = {
      idDocumentUrl: !!user.idDocumentUrl,
      idDocumentBackUrl: !!user.idDocumentBackUrl,
      autonomoDocumentUrl: !!user.autonomoDocumentUrl,
      vehiclePhoto: !!driver?.vehiclePhoto,
      vehiclePlatePhoto: !!driver?.vehiclePlatePhoto,
      vehicleItvPhoto: !!driver?.vehicleItvPhoto,
      vehicleInsurancePhoto: !!driver?.vehicleInsurancePhoto,
      vehicleLicensePhoto: !!driver?.vehicleLicensePhoto
    };
    const completeDocs = REQUIRED_DOCS.filter(
      (doc) => docs[doc]
    );
    const isComplete = completeDocs.length === REQUIRED_DOCS.length;
    let status = "pending";
    if (user.isActive && user.verificationStatus === "verified") {
      status = "approved";
    } else if (isComplete) {
      status = "in_review";
    }
    res.json({
      success: true,
      status,
      isComplete,
      missingDocuments: REQUIRED_DOCS.filter(
        (doc) => !docs[doc]
      ),
      documents: docs,
      vehicleInfo: {
        vehicleType: driver?.vehicleType,
        vehiclePlate: driver?.vehiclePlate,
        vehicleBrand: driver?.vehicleBrand,
        vehicleModel: driver?.vehicleModel,
        vehicleColor: driver?.vehicleColor
      },
      rejectionReason: user.verificationNotes
    });
  } catch (error) {
    console.error("Error getting verification status:", error);
    res.status(500).json({ error: error.message });
  }
});
router59.post("/reset", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const [user] = await db.select().from(users).where((0, import_drizzle_orm82.eq)(users.id, userId)).limit(1);
    if (!user) {
      return res.status(404).json({ error: "Usuario no encontrado" });
    }
    if (user.isActive && user.verificationStatus === "verified") {
      return res.status(400).json({
        error: "Ya est\xE1s verificado. Contacta soporte para modificar tus documentos."
      });
    }
    await db.update(users).set({
      verificationStatus: "pending",
      verificationNotes: null
    }).where((0, import_drizzle_orm82.eq)(users.id, userId));
    res.json({
      success: true,
      message: "Verificaci\xF3n reiniciada. Por favor, env\xEDa tus documentos nuevamente."
    });
  } catch (error) {
    console.error("Error resetting verification:", error);
    res.status(500).json({ error: error.message });
  }
});
router59.post("/upload-document", authenticateToken, async (req, res) => {
  try {
    const userId = req.user.id;
    const { documentType, image } = req.body;
    if (!documentType || !image) {
      return res.status(400).json({ error: "documentType e image requeridos" });
    }
    if (!image.startsWith("data:image/")) {
      return res.status(400).json({ error: "Formato de imagen inv\xE1lido" });
    }
    const estimatedBytes = Math.ceil(image.length * 0.75);
    if (estimatedBytes > 5 * 1024 * 1024) {
      return res.status(400).json({ error: "La imagen es muy pesada. M\xE1ximo 5MB" });
    }
    const userFieldMap = {
      idDocumentUrl: "idDocumentUrl",
      idDocumentBackUrl: "idDocumentBackUrl",
      autonomoDocumentUrl: "autonomoDocumentUrl"
    };
    const driverFieldMap = {
      vehiclePhoto: "vehiclePhoto",
      vehiclePlatePhoto: "vehiclePlatePhoto",
      vehicleItvPhoto: "vehicleItvPhoto",
      vehicleInsurancePhoto: "vehicleInsurancePhoto",
      vehicleLicensePhoto: "vehicleLicensePhoto"
    };
    const isUserField = documentType in userFieldMap;
    const isDriverField = documentType in driverFieldMap;
    if (!isUserField && !isDriverField) {
      return res.status(400).json({ error: "Tipo de documento inv\xE1lido" });
    }
    const documentKey = isUserField ? userFieldMap[documentType] : driverFieldMap[documentType];
    const url = await CloudinaryService.uploadImage(
      image,
      "verification-docs",
      `driver-${userId}-${documentKey}`
    );
    if (isUserField) {
      const updates = { verificationStatus: "pending" };
      updates[userFieldMap[documentType]] = url;
      await db.update(users).set(updates).where((0, import_drizzle_orm82.eq)(users.id, userId));
    } else {
      const [driver] = await db.select().from(deliveryDrivers).where((0, import_drizzle_orm82.eq)(deliveryDrivers.userId, userId)).limit(1);
      const updates = {};
      updates[driverFieldMap[documentType]] = url;
      if (driver) {
        await db.update(deliveryDrivers).set(updates).where((0, import_drizzle_orm82.eq)(deliveryDrivers.userId, userId));
      } else {
        await db.insert(deliveryDrivers).values({
          id: crypto.randomUUID(),
          userId,
          ...updates,
          isAvailable: false,
          vehicleType: null
        });
      }
      await db.update(users).set({ verificationStatus: "pending" }).where((0, import_drizzle_orm82.eq)(users.id, userId));
    }
    try {
      const { notifyAdmins: notifyAdmins2 } = await Promise.resolve().then(() => (init_websocket(), websocket_exports));
      const [user] = await db.select({ name: users.name }).from(users).where((0, import_drizzle_orm82.eq)(users.id, userId)).limit(1);
      notifyAdmins2({
        type: "document_updated",
        userId,
        userName: user?.name || "Usuario",
        documentType,
        message: `Documento actualizado: ${documentType}`
      });
    } catch {
    }
    res.json({
      success: true,
      url,
      message: "Documento subido correctamente"
    });
  } catch (error) {
    console.error("Error uploading document:", error);
    res.status(500).json({ error: error.message });
  }
});
var deliveryVerification_default = router59;

// server/routes/appConfig.ts
var import_express60 = __toESM(require("express"));
var router60 = import_express60.default.Router();
router60.get("/", async (_req, res) => {
  res.json({
    success: true,
    commission: { platformPercent: 15, totalPercent: 15 },
    pricing: { minimumOrder: 5, serviceFee: 1 },
    deliveryFees: { tier1: 2.5, tier2: 4, tier3: 5, extraPerKm: 1 },
    deliveryZone: { name: "Soria", radius: 8, baseFee: 2.5, active: true },
    features: { cashPayments: true, biometricAuth: false, aiSupport: false, realTimeTracking: true }
  });
});
var appConfig_default = router60;

// server/routes/driverRoutes.ts
var import_express61 = require("express");
init_db();
init_orderNumberService();
init_schema_mysql();
var import_drizzle_orm83 = require("drizzle-orm");
init_googleMapsService();
init_websocket();
init_enhancedPushService();
var router61 = (0, import_express61.Router)();
var MAX_ROUTE_ORDERS = 6;
router61.post(
  "/route/optimize",
  authenticateToken,
  requireRole("delivery_driver"),
  async (req, res) => {
    try {
      const { orderIds } = req.body;
      if (!Array.isArray(orderIds) || orderIds.length === 0) {
        return res.status(400).json({ error: "orderIds requeridos" });
      }
      if (orderIds.length > MAX_ROUTE_ORDERS) {
        return res.status(400).json({ error: `M\xE1ximo ${MAX_ROUTE_ORDERS} pedidos por ruta` });
      }
      const userId = req.user.id;
      const [driver] = await db.select({
        lat: deliveryDrivers.currentLatitude,
        lng: deliveryDrivers.currentLongitude
      }).from(deliveryDrivers).where((0, import_drizzle_orm83.eq)(deliveryDrivers.userId, userId)).limit(1);
      const startLat = driver?.lat ? parseFloat(driver.lat) : 41.7636;
      const startLng = driver?.lng ? parseFloat(driver.lng) : -2.4677;
      const rows = await db.select({
        id: orders.id,
        businessName: orders.businessName,
        status: orders.status,
        items: orders.items,
        deliveryFee: orders.deliveryFee,
        total: orders.total,
        paymentMethod: orders.paymentMethod,
        deliveryAddress: orders.deliveryAddress,
        deliveryLatitude: orders.deliveryLatitude,
        deliveryLongitude: orders.deliveryLongitude,
        businessLatitude: businesses.latitude,
        businessLongitude: businesses.longitude,
        customerName: users.name
      }).from(orders).leftJoin(businesses, (0, import_drizzle_orm83.eq)(businesses.id, orders.businessId)).leftJoin(users, (0, import_drizzle_orm83.eq)(users.id, orders.userId)).where(
        (0, import_drizzle_orm83.and)(
          (0, import_drizzle_orm83.inArray)(orders.id, orderIds),
          (0, import_drizzle_orm83.eq)(orders.status, "ready"),
          (0, import_drizzle_orm83.isNull)(orders.deliveryPersonId)
        )
      );
      if (rows.length !== orderIds.length) {
        return res.status(400).json({
          error: "Algunos pedidos ya no est\xE1n disponibles (aceptados o entregados)"
        });
      }
      const nodes = [
        {
          id: "start",
          kind: "start",
          lat: startLat,
          lng: startLng,
          label: "Tu posici\xF3n"
        }
      ];
      const pickups = [];
      const drops = /* @__PURE__ */ new Map();
      for (const r of rows) {
        if (!r.businessLatitude || !r.businessLongitude || !r.deliveryLatitude || !r.deliveryLongitude) {
          return res.status(400).json({
            error: `El pedido ${await orderRefFromId(r.id)} no tiene coordenadas completas`
          });
        }
        pickups.push({
          id: `p_${r.id}`,
          kind: "pickup",
          orderId: r.id,
          lat: parseFloat(r.businessLatitude),
          lng: parseFloat(r.businessLongitude),
          label: `Recoger en ${r.businessName || "el negocio"}`
        });
        drops.set(r.id, {
          id: `d_${r.id}`,
          kind: "drop",
          orderId: r.id,
          lat: parseFloat(r.deliveryLatitude),
          lng: parseFloat(r.deliveryLongitude),
          label: `Entregar a ${r.customerName || "cliente"}`
        });
      }
      const all = [nodes[0], ...pickups, ...[...drops.values()]];
      const matrix = await googleMapsService.getDistanceMatrix(
        all.map((n) => ({ lat: n.lat, lng: n.lng })),
        all.map((n) => ({ lat: n.lat, lng: n.lng }))
      );
      const idx = new Map(all.map((n, i) => [n.id, i]));
      const sequence = [];
      const visitedPickups = /* @__PURE__ */ new Set();
      const visitedDrops = /* @__PURE__ */ new Set();
      let current = nodes[0];
      while (visitedDrops.size < drops.size) {
        const candidates = [];
        for (const p of pickups) {
          if (!visitedPickups.has(p.orderId)) candidates.push(p);
        }
        for (const [orderId, d] of drops) {
          if (visitedPickups.has(orderId) && !visitedDrops.has(orderId)) {
            candidates.push(d);
          }
        }
        if (!candidates.length) break;
        let best = null;
        let bestDuration = Infinity;
        const ci = idx.get(current.id);
        for (const c of candidates) {
          const di = idx.get(c.id);
          const cell = matrix[ci]?.[di];
          const dur = cell?.durationSeconds ?? Infinity;
          if (dur < bestDuration) {
            bestDuration = dur;
            best = c;
          }
        }
        if (!best) break;
        sequence.push(best);
        if (best.kind === "pickup") visitedPickups.add(best.orderId);
        else visitedDrops.add(best.orderId);
        current = best;
      }
      const legs = [];
      let totalDistanceMeters = 0;
      let totalDurationSeconds = 0;
      const path3 = [nodes[0], ...sequence];
      for (let i = 1; i < path3.length; i++) {
        const from = path3[i - 1];
        const to = path3[i];
        const cell = matrix[idx.get(from.id)]?.[idx.get(to.id)];
        const distanceMeters = cell?.distanceMeters ?? 0;
        const durationSeconds = cell?.durationSeconds ?? 0;
        totalDistanceMeters += distanceMeters;
        totalDurationSeconds += durationSeconds;
        legs.push({
          from: from.label,
          to: to.label,
          distanceMeters,
          durationMinutes: Math.max(1, Math.round(durationSeconds / 60))
        });
      }
      const ordersMap = new Map(
        rows.map((r) => [r.id, r])
      );
      const totalEarnings = rows.reduce(
        (s, r) => s + (Number(r.deliveryFee) || 0),
        0
      );
      res.json({
        success: true,
        route: {
          nodes: path3.map((n) => ({
            ...n,
            orderId: n.orderId || void 0,
            fee: n.orderId ? Number(ordersMap.get(n.orderId)?.deliveryFee) || 0 : 0,
            paymentMethod: n.orderId ? ordersMap.get(n.orderId)?.paymentMethod : null,
            address: n.kind === "drop" && n.orderId ? ordersMap.get(n.orderId)?.deliveryAddress : void 0
          })),
          legs,
          totalDistanceMeters,
          totalDistanceKm: (totalDistanceMeters / 1e3).toFixed(1),
          totalDurationMinutes: Math.max(1, Math.round(totalDurationSeconds / 60)),
          totalEarnings
        }
      });
    } catch (error) {
      console.error("route/optimize error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
router61.post(
  "/accept-route",
  authenticateToken,
  requireRole("delivery_driver"),
  async (req, res) => {
    try {
      const { orderIds } = req.body;
      if (!Array.isArray(orderIds) || orderIds.length === 0) {
        return res.status(400).json({ error: "orderIds requeridos" });
      }
      if (orderIds.length > MAX_ROUTE_ORDERS) {
        return res.status(400).json({ error: `M\xE1ximo ${MAX_ROUTE_ORDERS} pedidos por ruta` });
      }
      const userId = req.user.id;
      const rows = await db.select({
        id: orders.id,
        userId: orders.userId,
        businessId: orders.businessId,
        status: orders.status,
        deliveryPersonId: orders.deliveryPersonId
      }).from(orders).where((0, import_drizzle_orm83.inArray)(orders.id, orderIds));
      const available = rows.filter(
        (r) => r.status === "ready" && !r.deliveryPersonId
      );
      if (available.length !== orderIds.length) {
        return res.status(400).json({
          error: "Algunos pedidos ya no est\xE1n disponibles"
        });
      }
      const assignedAt = /* @__PURE__ */ new Date();
      for (const order of available) {
        await db.update(orders).set({
          deliveryPersonId: userId,
          status: "assigned",
          assignedAt,
          updatedAt: assignedAt
        }).where((0, import_drizzle_orm83.eq)(orders.id, order.id));
        notifyDriverAssigned(userId, {
          orderId: order.id,
          status: "assigned",
          assignedAt: assignedAt.toISOString()
        });
        if (order.userId) {
          sendPushToUser(order.userId, {
            title: "\u{1F6F5} Repartidor asignado",
            body: `Tu pedido ${await orderRefFromId(order.id)} est\xE1 en camino al negocio`,
            data: { orderId: order.id, screen: "OrderTracking" }
          }).catch(() => {
          });
        }
      }
      res.json({
        success: true,
        assigned: available.map((o) => o.id),
        message: `${available.length} pedidos asignados a tu ruta`
      });
    } catch (error) {
      console.error("accept-route error:", error);
      res.status(500).json({ error: error.message });
    }
  }
);
var driverRoutes_default = router61;

// server/routes.ts
var router62 = import_express62.default.Router();
var geocodePostOnly = (req, res, next) => {
  if (req.method === "POST") return geocodingLimiter(req, res, next);
  next();
};
router62.get("/config/maps-key", (_req, res) => {
  res.json({
    key: process.env.GOOGLE_MAPS_WEB_KEY || process.env.EXPO_PUBLIC_GOOGLE_MAPS_WEB_API_KEY || process.env.GOOGLE_MAPS_API_KEY || ""
  });
});
router62.post("/upload/payment-proof", authenticateToken, async (req, res) => {
  try {
    const { image } = req.body;
    if (!image) return res.status(400).json({ error: "image requerida" });
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const url = await CloudinaryService2.uploadImage(
      image,
      "comprobantes",
      `proof-${req.user.id}-${Date.now()}`
    );
    res.json({ success: true, url, imageUrl: url, proofUrl: url });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
});
router62.post("/upload/business-image", authenticateToken, async (req, res) => {
  try {
    const { image } = req.body;
    if (!image) return res.status(400).json({ error: "image requerida" });
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const url = await CloudinaryService2.uploadImage(
      image,
      "businesses",
      `biz-${req.user.id}-${Date.now()}`
    );
    res.json({ success: true, url, imageUrl: url });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
});
router62.post("/upload/product-image", authenticateToken, async (req, res) => {
  try {
    const { image } = req.body;
    if (!image) return res.status(400).json({ error: "image requerida" });
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const url = await CloudinaryService2.uploadImage(
      image,
      "products",
      `prod-${req.user.id}-${Date.now()}`
    );
    res.json({ success: true, url, imageUrl: url });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
});
router62.post("/upload/review-image", authenticateToken, async (req, res) => {
  try {
    const { image } = req.body;
    if (!image) return res.status(400).json({ error: "image requerida" });
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const url = await CloudinaryService2.uploadImage(
      image,
      "reviews",
      `review-${req.user.id}-${Date.now()}`
    );
    res.json({ success: true, url, imageUrl: url });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
});
router62.post("/upload/delivery-proof", authenticateToken, async (req, res) => {
  try {
    const { image } = req.body;
    if (!image) return res.status(400).json({ error: "image requerida" });
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const url = await CloudinaryService2.uploadImage(
      image,
      "delivery-proofs",
      `delivery-${req.user.id}-${Date.now()}`
    );
    res.json({ success: true, url, imageUrl: url });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
});
router62.post("/upload/image", authenticateToken, async (req, res) => {
  try {
    const { image, folder = "misc" } = req.body;
    if (!image) return res.status(400).json({ error: "image requerida" });
    const { CloudinaryService: CloudinaryService2 } = await Promise.resolve().then(() => (init_cloudinaryService(), cloudinaryService_exports));
    const url = await CloudinaryService2.uploadImage(
      image,
      folder,
      `img-${req.user.id}-${Date.now()}`
    );
    res.json({ success: true, url, imageUrl: url });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
});
router62.get("/health", (req, res) => {
  res.json({
    status: "ok",
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    environment: process.env.NODE_ENV
  });
});
router62.get("/settings/public", async (req, res) => {
  try {
    const { getPublicSettings: getPublicSettings2 } = await Promise.resolve().then(() => (init_systemSettingsService(), systemSettingsService_exports));
    const result = await getPublicSettings2();
    res.json(result);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
router62.get("/system/exchange-rate", async (req, res) => {
  try {
    const { exchangeRateService: exchangeRateService2 } = await Promise.resolve().then(() => (init_exchangeRateService(), exchangeRateService_exports));
    const result = await exchangeRateService2.getCurrentRate();
    res.json({
      success: true,
      rate: result.rate,
      source: result.source,
      lastUpdated: result.lastUpdated
    });
  } catch (error) {
    res.json({ success: true, rate: 36.5, source: "fallback" });
  }
});
router62.post("/coupons/validate", authenticateToken, async (req, res) => {
  try {
    const { AdvancedCouponService: AdvancedCouponService2 } = await Promise.resolve().then(() => (init_advancedCouponService(), advancedCouponService_exports));
    const { code, userId, orderTotal } = req.body;
    const context = {
      userId: userId || req.user.id,
      orderTotal: orderTotal || 0
    };
    const result = await AdvancedCouponService2.validateCoupon(code, context);
    res.json(result);
  } catch (error) {
    res.status(500).json({ valid: false, error: error.message });
  }
});
router62.get("/delivery-zones", async (_req, res) => {
  res.json({
    success: true,
    zones: [
      {
        id: "zone-centro",
        name: "Centro",
        deliveryFee: 300,
        maxDeliveryTime: 20,
        isActive: true,
        centerLatitude: "41.7636",
        centerLongitude: "-2.4677",
        radiusKm: 2
      },
      {
        id: "zone-norte",
        name: "Norte",
        deliveryFee: 350,
        maxDeliveryTime: 25,
        isActive: true,
        centerLatitude: "41.7750",
        centerLongitude: "-2.4677",
        radiusKm: 3
      },
      {
        id: "zone-sur",
        name: "Sur",
        deliveryFee: 350,
        maxDeliveryTime: 25,
        isActive: true,
        centerLatitude: "41.7500",
        centerLongitude: "-2.4677",
        radiusKm: 3
      },
      {
        id: "zone-este",
        name: "Este",
        deliveryFee: 400,
        maxDeliveryTime: 30,
        isActive: true,
        centerLatitude: "41.7636",
        centerLongitude: "-2.4400",
        radiusKm: 3
      }
    ]
  });
});
router62.get(
  "/levels/my-level",
  (_req, res) => res.json({ success: true, level: null })
);
router62.use("/auth", auth_default);
router62.use("/businesses", business_default);
router62.use("/business", business_default);
router62.use("/orders", orderRoutes_default);
router62.use("/users", users_default);
router62.use("/user", users_default);
router62.use("/addresses", geocodePostOnly, addressRoutes_default);
router62.use("/delivery-requests", deliveryRequestRoutes_default);
router62.use("/delivery", deliveryConfigRoutes_default);
router62.use("/driver", driverRoutes_default);
router62.use("/delivery", delivery_default);
router62.use("/delivery", deliveryRoutes_default);
router62.use("/delivery-verification", deliveryVerification_default);
router62.use("/payments", payments_default);
router62.use("/digital-payments", digitalPayments_default);
router62.use("/payment-accounts", paymentAccounts_default);
router62.use("/fund-release", fundRelease_default);
router62.use("/payouts", payoutRoutes_default);
router62.use("/wallet", walletRoutes_default);
router62.use("/wallet", wallet_default);
router62.use("/bank-account", bankAccountRoutes_default);
router62.use("/admin/issues", adminIssues_default);
router62.use("/admin/refunds", adminRefunds_default);
router62.use("/admin/orders", adminOrderActions_default);
router62.use("/admin", adminRoutes_default);
router62.use("/admin/finance", adminFinanceRoutes_default);
router62.use("/admin", adminExchangeRate_default);
router62.use("/support", support_default);
router62.use("/support", supportRoutes_default);
router62.use("/withdrawals", withdrawalRoutes_default);
router62.use("/cash-settlement", cashSettlementRoutes_default);
router62.use("/weekly-settlement", weeklySettlementRoutes_default);
router62.use("/audit", financialAuditRoutes_default);
router62.use("/favorites", favoritesRoutes_default);
router62.use("/business-verification", businessVerificationRoutes_default);
router62.use("/gps", gpsLimiter, gpsRoutes_default);
router62.use("/search", search_default);
router62.use("/coupons", coupons_default);
router62.use("/loyalty", loyalty_default);
router62.use("/favorites", favorites_default);
router62.use("/scheduled-orders", scheduledOrders_default);
router62.use("/ai", aiRecommendations_default);
router62.use("/tracking", enhancedTracking_default);
router62.use("/subscriptions", subscriptions_default);
router62.use("/smart-notifications", smartNotifications_default);
router62.use("/reviews", enhancedReviews_default);
router62.use("/analytics", businessAnalytics_default);
router62.use("/gamification", gamification_default);
router62.use("/gift-cards", giftCards_default);
router62.use("/group-orders", groupOrders_default);
router62.use("/referrals", referrals_default);
router62.use("/orders", orderChat_default);
router62.use("/orders", orders_default);
router62.use("/stripe", stripePaymentRoutes_default);
router62.use("/connect", stripeConnect_default);
router62.use("/business/stripe", stripeConnect_default);
router62.use("/pickup", pickup_default);
router62.use("/registration", registration_default);
router62.use("/admin/business-categories", businessCategories_default);
router62.use("/admin", adminSubscriptionPlans_default);
router62.use("/admin", adminTracking_default);
router62.use("/admin", adminOps_default);
router62.use("/business-categories", businessCategories_default);
router62.use("/map", mapData_default);
router62.use("/app-config", appConfig_default);
var routes_default = router62;

// server/server.ts
(0, import_dotenv2.config)({ path: ".env.local", override: true });
(0, import_dotenv2.config)({ path: ".env", override: false });
validateEnv();
var app = (0, import_express63.default)();
var httpServer = (0, import_http.createServer)(app);
var PORT = process.env.PORT || 5e3;
var isProduction2 = process.env.NODE_ENV === "production";
app.set("trust proxy", 1);
app.use(
  (0, import_helmet.default)({ contentSecurityPolicy: false, crossOriginEmbedderPolicy: false })
);
var ALLOWED_ORIGINS = [
  "http://localhost:8081",
  "http://localhost:3000",
  "https://comeya.es",
  "https://www.comeya.es",
  "https://app.comeya.es",
  process.env.FRONTEND_URL
].filter(Boolean);
app.use(
  (0, import_cors.default)({
    origin: (origin, cb) => {
      if (!origin) return cb(null, true);
      if (ALLOWED_ORIGINS.includes(origin)) return cb(null, true);
      cb(new Error(`CORS bloqueado: ${origin}`));
    },
    credentials: true
  })
);
app.use(
  "/api/",
  (0, import_express_rate_limit2.default)({
    windowMs: 15 * 60 * 1e3,
    max: isProduction2 ? 500 : 1e4,
    message: "Too many requests from this IP",
    skip: (req) => {
      const skipPaths = [
        "/api/orders",
        "/api/delivery/location",
        "/api/tracking",
        // El centro de operaciones del admin refresca cada pocos segundos y
        // puede estar abierto en varias pestañas a la vez
        "/api/admin/ops",
        "/api/admin/tracking"
      ];
      return skipPaths.some((p) => req.path.startsWith(p.replace("/api", "")));
    }
  })
);
app.use((req, res, next) => {
  console.log(`${req.method} ${req.originalUrl}`);
  next();
});
app.use("/api/connect/webhook", import_express63.default.raw({ type: "application/json" }));
app.use(
  "/api/payments/webhook/stripe",
  import_express63.default.raw({ type: "application/json" })
);
app.use(import_express63.default.json({ limit: "10mb" }));
app.use(import_express63.default.urlencoded({ extended: true }));
app.use((req, res, next) => {
  const originalJson = res.json.bind(res);
  res.json = (body) => {
    res.setHeader("Content-Type", "application/json; charset=utf-8");
    return originalJson(body);
  };
  next();
});
app.use(
  "/uploads",
  (req, res, next) => {
    res.header("Access-Control-Allow-Origin", "*");
    res.header("Cross-Origin-Resource-Policy", "cross-origin");
    next();
  },
  import_express63.default.static(import_path2.default.join(__dirname, "uploads"))
);
app.use(import_express63.default.static(import_path2.default.join(process.cwd(), "public"), { index: false }));
app.get("/privacy-policy", (_req, res) => {
  res.sendFile(import_path2.default.join(process.cwd(), "public", "privacy-policy.html"));
});
app.get("/delete-account", (_req, res) => {
  res.sendFile(import_path2.default.join(process.cwd(), "public", "delete-account.html"));
});
app.use("/assets", import_express63.default.static(import_path2.default.join(process.cwd(), "client/assets")));
app.use("/api", routes_default);
app.get("/health", (req, res) => {
  res.json({ status: "ok", timestamp: (/* @__PURE__ */ new Date()).toISOString() });
});
if (isProduction2) {
  const distIndex = import_path2.default.join(process.cwd(), "dist", "index.html");
  const hasFrontendBuild = import_fs2.default.existsSync(distIndex);
  app.use(import_express63.default.static(import_path2.default.join(process.cwd(), "dist")));
  app.use((req, res, next) => {
    if (req.path.startsWith("/api") || req.path === "/health") return next();
    if (!hasFrontendBuild) {
      return res.status(200).send(
        `<!doctype html><html lang="es"><head><meta charset="utf-8"><title>ComeYa API</title></head><body style="font-family:system-ui;text-align:center;padding:48px"><h1>\u{1F6F5} ComeYa API</h1><p>El backend est\xE1 funcionando.</p><p>La app web est\xE1 en <a href="${process.env.FRONTEND_URL || "https://app.comeya.es"}">${process.env.FRONTEND_URL || "https://app.comeya.es"}</a></p></body></html>`
      );
    }
    res.sendFile(distIndex);
  });
} else {
  app.get("/", (req, res) => {
    res.json({
      message: "ComeYa API Server",
      frontend: process.env.FRONTEND_URL || "http://localhost:8081"
    });
  });
}
app.use((err, req, res, next) => {
  if (err?.statusCode && err?.isOperational) {
    return res.status(err.statusCode).json({ error: err.message });
  }
  console.error("Server error:", err);
  res.status(500).json({ error: "Internal server error" });
});
app.use((req, res) => {
  res.status(404).json({ error: `Route not found: ${req.method} ${req.originalUrl}` });
});
var server = httpServer.listen(PORT, () => {
  console.log(`\u{1F680} Server running on port ${PORT}`);
  console.log(`\u{1F527} Environment: ${process.env.NODE_ENV || "development"}`);
  initializeWebSocket(httpServer);
  console.log("\u{1F50C} WebSocket initialized");
  if (!process.env.TWILIO_ACCOUNT_SID)
    console.warn("\u26A0\uFE0F  Twilio not configured");
  if (!process.env.PAGO_MOVIL_PHONE && !process.env.RABBIT_FOOD_PAGO_MOVIL_PHONE)
    console.warn(
      "\u26A0\uFE0F  Pago M\xF3vil no configurado (usa payment_receiving_accounts en BD)"
    );
  Promise.resolve().then(() => (init_db(), db_exports)).then(({ runStartupMigrations: runStartupMigrations2 }) => runStartupMigrations2()).then(() => {
    Promise.resolve().then(() => (init_businessHoursCron(), businessHoursCron_exports)).then(({ startBusinessHoursCron: startBusinessHoursCron2 }) => startBusinessHoursCron2()).catch(console.error);
    Promise.resolve().then(() => (init_weeklySettlementCron(), weeklySettlementCron_exports)).then(({ WeeklySettlementCron: WeeklySettlementCron2 }) => WeeklySettlementCron2.start()).catch(console.error);
    Promise.resolve().then(() => (init_autoConfirmDeliveryCron(), autoConfirmDeliveryCron_exports)).then(({ startAutoConfirmCron: startAutoConfirmCron2 }) => startAutoConfirmCron2()).catch(console.error);
    Promise.resolve().then(() => (init_pickupNotificationCron(), pickupNotificationCron_exports)).then(({ startPickupNotificationCron: startPickupNotificationCron2 }) => startPickupNotificationCron2()).catch(console.error);
    Promise.resolve().then(() => (init_staleOrdersCron(), staleOrdersCron_exports)).then(({ startStaleOrdersCron: startStaleOrdersCron2 }) => startStaleOrdersCron2()).catch(console.error);
    Promise.resolve().then(() => (init_stripeWebhookRegistration(), stripeWebhookRegistration_exports)).then(({ ensureStripeWebhook: ensureStripeWebhook2 }) => ensureStripeWebhook2()).catch(console.error);
    Promise.resolve().then(() => (init_backgroundJobs(), backgroundJobs_exports)).then(({ startBackgroundJobs: startBackgroundJobs2 }) => startBackgroundJobs2()).catch(console.error);
  }).catch((err) => console.error("Startup migrations error:", err.message));
});
